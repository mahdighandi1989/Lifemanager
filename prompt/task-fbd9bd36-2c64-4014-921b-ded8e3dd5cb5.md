---
task_id: fbd9bd36-2c64-4014-921b-ded8e3dd5cb5
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (6 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:58:36.200085+00:00'
updated_at: '2026-05-26T20:58:36.200097+00:00'
target_files:
- app/main.py
- frontend/index.html
- app/routes/auth_google.py
- app/routes/tasks.py
- app/models/todo_list.py
- frontend/src/pages/Tasks.jsx
- app/models/task.py
- app/models/todo_item.py
- frontend/src/pages/Lists.jsx
- app/routes/todo_items.py
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (6 مرحله)

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_838329_AgADph0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 5,409 char متن_

## عنوان و معرفی ایده (Title & Idea Introduction)  _(at: audio/ogg)_

در اینجا رونویسی/توصیف کامل و literal مرتبط با heading «عنوان و معرفی ایده (Title & Idea Introduction)» آمده است:

[00:00] می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.

## مشکل و راه‌حل (Problem & Solution)  _(at: audio/ogg)_

[00:00]
[00:01] می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه [00:05] موقع اجرا تمام اون [00:07] لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه [00:12] مشابهت‌هایی داشته باشه رو به شکل [00:15] یا صفحات حتی [00:17] همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی [00:23] این‌ها رو [00:24] در هم [00:26] تا اونجایی که ممکنه و شباهت داره [00:29] و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً [00:34] بی‌دلیل همون اول ساخته شده ولی [00:37] الان تأثیری نداره یا صفحه مشابهی براش هست [00:40] این‌ها رو تلفیق و ادغام کنه و [00:42] درست چیز کنه که [00:45] از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی [00:52] در بیاد.

## ویژگی‌های کلیدی محصول (Key Product Features)  _(at: audio/ogg)_

[00:00] می‌خوام پلتفرمی که می‌نویسی جوری باشه که بتونه
[00:04] موقع اجرا تمام اون
[00:07] لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه
[00:12] مشابهت‌هایی داشته باشه رو به شکل
[00:15] یا صفحات حتی
[00:17] همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی
[00:23] این‌ها رو
[00:24] در هم
[00:26] تا اونجایی که ممکنه و شباهت داره
[00:29] و باعث میشه از این هرج و مرج
[00:30] نجات پیدا بکنه یا صفحاتی که اصلاً
[00:34] بی‌دلیل همون اول ساخته شده ولی
[00:36] الان تأثیری نداره یا صفحه مشابهی براش هست
[00:40] این‌ها رو تلفیق و ادغام کنه و
[00:42] درست چیز کنه که
[00:45] از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی
[00:52] در بیاد.

## مکانیسم‌های انگیزشی و پاداش (Motivational & Reward Mechanisms)  _(at: audio/ogg)_

[00:00] گوینده ۱: می‌خوام پلانپتیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره
[00:30] گوینده ۱: و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.

## طراحی رابط کاربری و تجربه کاربری (UI/UX Design)  _(at: audio/ogg)_

[00:00] (صدای باد یا حرکت)
[00:01] می‌خوام اون پلن و پدیکی که می‌نویسی جوری باشه که بتونه
[00:05] موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه
[00:12] مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی
[00:17] همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی
[00:23] این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره
[00:29] و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً
[00:34] بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست
[00:40] این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که
[00:45] از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی
[00:52] در بیاد.

## بازار هدف و مزیت رقابتی (Target Market & Competitive Advantage)  _(at: audio/ogg)_

```
[00:01] می‌خوام اون پلن و پلنی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

## نقشه راه و توسعه آینده (Roadmap & Future Development)  _(at: audio/ogg)_

--- صفحه 1 ---
[00:01] می‌خوام پلان پدیکه‌ای که می‌نویسی جوری باشه که بتونه
[00:04] موقع اجرا تمام اون
[00:07] لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه
[00:12] مشابهت‌هایی داشته باشه و به شکل
[00:15] یا صفحات حتی
[00:17] همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق، بدون هیچ خلاصه‌سازی، بدون هیچ حذفی
[00:23] این‌ها رو
[00:24] در هم
[00:26] تا اونجایی که ممکنه و شباهت داره
[00:29] و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً
[00:34] بی‌دلیل همون اول ساخته شده ولی
[00:36] الان تأثیری نداره یا صفحه مشابهی براش هست
[00:40] این‌ها رو تلفیق و ادغام کنه و
[00:42] درست چیز کنه که
[00:45] از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی
[00:52] در بیاد.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_838329_AgADph0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 5,409 char متن_

## عنوان و معرفی ایده (Title & Idea Introduction)  _(at: audio/ogg)_

در اینجا رونویسی/توصیف کامل و literal مرتبط با heading «عنوان و معرفی ایده (Title & Idea Introduction)» آمده است:

[00:00] می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.

## مشکل و راه‌حل (Problem & Solution)  _(at: audio/ogg)_

[00:00]
[00:01] می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه [00:05] موقع اجرا تمام اون [00:07] لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه [00:12] مشابهت‌هایی داشته باشه رو به شکل [00:15] یا صفحات حتی [00:17] همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی [00:23] این‌ها رو [00:24] در هم [00:26] تا اونجایی که ممکنه و شباهت داره [00:29] و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً [00:34] بی‌دلیل همون اول ساخته شده ولی [00:37] الان تأثیری نداره یا صفحه مشابهی براش هست [00:40] این‌ها رو تلفیق و ادغام کنه و [00:42] درست چیز کنه که [00:45] از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی [00:52] در بیاد.

## ویژگی‌های کلیدی محصول (Key Product Features)  _(at: audio/ogg)_

[00:00] می‌خوام پلتفرمی که می‌نویسی جوری باشه که بتونه
[00:04] موقع اجرا تمام اون
[00:07] لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه
[00:12] مشابهت‌هایی داشته باشه رو به شکل
[00:15] یا صفحات حتی
[00:17] همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی
[00:23] این‌ها رو
[00:24] در هم
[00:26] تا اونجایی که ممکنه و شباهت داره
[00:29] و باعث میشه از این هرج و مرج
[00:30] نجات پیدا بکنه یا صفحاتی که اصلاً
[00:34] بی‌دلیل همون اول ساخته شده ولی
[00:36] الان تأثیری نداره یا صفحه مشابهی براش هست
[00:40] این‌ها رو تلفیق و ادغام کنه و
[00:42] درست چیز کنه که
[00:45] از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی
[00:52] در بیاد.

## مکانیسم‌های انگیزشی و پاداش (Motivational & Reward Mechanisms)  _(at: audio/ogg)_

[00:00] گوینده ۱: می‌خوام پلانپتیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره
[00:30] گوینده ۱: و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.

## طراحی رابط کاربری و تجربه کاربری (UI/UX Design)  _(at: audio/ogg)_

[00:00] (صدای باد یا حرکت)
[00:01] می‌خوام اون پلن و پدیکی که می‌نویسی جوری باشه که بتونه
[00:05] موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه
[00:12] مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی
[00:17] همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی
[00:23] این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره
[00:29] و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً
[00:34] بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست
[00:40] این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که
[00:45] از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی
[00:52] در بیاد.

## بازار هدف و مزیت رقابتی (Target Market & Competitive Advantage)  _(at: audio/ogg)_

```
[00:01] می‌خوام اون پلن و پلنی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

## نقشه راه و توسعه آینده (Roadmap & Future Development)  _(at: audio/ogg)_

--- صفحه 1 ---
[00:01] می‌خوام پلان پدیکه‌ای که می‌نویسی جوری باشه که بتونه
[00:04] موقع اجرا تمام اون
[00:07] لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه
[00:12] مشابهت‌هایی داشته باشه و به شکل
[00:15] یا صفحات حتی
[00:17] همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق، بدون هیچ خلاصه‌سازی، بدون هیچ حذفی
[00:23] این‌ها رو
[00:24] در هم
[00:26] تا اونجایی که ممکنه و شباهت داره
[00:29] و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً
[00:34] بی‌دلیل همون اول ساخته شده ولی
[00:36] الان تأثیری نداره یا صفحه مشابهی براش هست
[00:40] این‌ها رو تلفیق و ادغام کنه و
[00:42] درست چیز کنه که
[00:45] از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی
[00:52] در بیاد.
```

## 📋 چک‌لیست مراحل (6 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: بررسی و فهرست‌برداری از لیست‌ها، تسک‌ها و صفحات موجود** — این مرحله شامل یک بررسی جامع و دقیق از تمامی لیست‌ها، تسک‌ها و صفحات فعلی موجود در برنامه است. هدف، شناسایی و فهرست‌برداری کامل از هر یک از این اجزا بدون هیچ‌گونه خلاصه‌سازی یا حذف است. این مرحله صرفاً به جمع‌آوری اطلاعات و ایجاد یک موجودی کامل از وضعیت فعلی برنامه می‌پردازد و شامل هیچ‌گونه تحلیل شب
- [ ] **مرحله 2: تحلیل شباهت‌ها و شناسایی موارد تکراری یا بی‌دلیل** — در این مرحله، موجودی ایجاد شده از لیست‌ها، تسک‌ها و صفحات مورد تحلیل دقیق قرار می‌گیرد تا موارد دارای شباهت، تکراری، یا صفحاتی که 'بی‌دلیل' ساخته شده‌اند و تأثیری ندارند، شناسایی شوند. این تحلیل باید بر اساس معیارهای مشخصی برای شباهت (مثلاً عملکرد مشابه، داده‌های یکسان، UI مشابه) انجام شود. خروجی ای
- [ ] **مرحله 3: طراحی استراتژی تلفیق و ادغام (بدون خلاصه‌سازی/حذف)** — بر اساس تحلیل شباهت‌ها، یک استراتژی دقیق و جامع برای تلفیق و ادغام لیست‌ها، تسک‌ها و صفحات شناسایی شده طراحی می‌شود. این استراتژی باید تضمین کند که هیچ اطلاعات یا عملکرد حیاتی 'بدون هیچ خلاصه‌سازی، بدون هیچ حذفی' از بین نرود. شامل طراحی مدل‌های داده جدید، جریان‌های کاری بهینه شده، و طرح‌های UI/UX بر
- [ ] **مرحله 4: پیاده‌سازی تلفیق و ادغام اجزا** — در این مرحله، استراتژی طراحی شده برای تلفیق و ادغام به صورت عملی پیاده‌سازی می‌شود. این شامل بازنویسی یا اصلاح کد مربوط به لیست‌ها، تسک‌ها و صفحات، مهاجرت داده‌ها (در صورت نیاز)، و به‌روزرسانی رابط کاربری برای نمایش اجزای ادغام شده است. هدف، اجرای دقیق طرح بدون خلاصه‌سازی یا حذف اطلاعات، و ایجاد یک 
- [ ] **مرحله 5: بازسازی ساختار برنامه و زیرساخت برای قابلیت ارتقا** — این مرحله فراتر از صرفاً ادغام اجزا است و بر روی بازسازی کلی ساختار برنامه و زیرساخت آن تمرکز دارد تا 'قابل ارتقا' شود. این شامل اعمال الگوهای طراحی مناسب، ماژولار کردن کد، بهبود معماری کلی سیستم، و اطمینان از اینکه زیرساخت فعلی یا جدید، از رشد و تغییرات آینده پشتیبانی می‌کند. هدف، ایجاد یک پایه محک
- [ ] **مرحله 6: تأیید نهایی و ممیزی کاهش هرج و مرج** — این مرحله شامل یک ممیزی نهایی و جامع برای تأیید اینکه تمامی اهداف پروژه، به ویژه 'نجات پیدا کردن از هرج و مرج فعلی' و 'قابل ارتقا شدن' برنامه، محقق شده‌اند. این ممیزی شامل تست‌های جامع (عملکردی، یکپارچگی، رگرسیون)، بررسی کد برای اطمینان از رعایت استانداردهای جدید، و جمع‌آوری بازخورد از کاربران و ذین

---

# 🔹 مرحله 1: بررسی و فهرست‌برداری از لیست‌ها، تسک‌ها و صفحات موجود

**Scope:** این مرحله شامل یک بررسی جامع و دقیق از تمامی لیست‌ها، تسک‌ها و صفحات فعلی موجود در برنامه است. هدف، شناسایی و فهرست‌برداری کامل از هر یک از این اجزا بدون هیچ‌گونه خلاصه‌سازی یا حذف است. این مرحله صرفاً به جمع‌آوری اطلاعات و ایجاد یک موجودی کامل از وضعیت فعلی برنامه می‌پردازد و شامل هیچ‌گونه تحلیل شباهت یا تصمیم‌گیری برای ادغام نیست.
**Key terms:** لیست‌ها, تسک‌ها, صفحات, برنامه

**بخش مربوط از متن کاربر:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد فهرست جامع لیست‌ها، تسک‌ها و صفحات برنامه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:363-385` — `app.include_router` — این بخش نحوهٔ include شدن روترهای مختلف (از جمله tasks, projects, lists, todo_items) را نشان می‌دهد که برای فهرست‌برداری از endpointهای بک‌اند حیاتی است.
  ```python
  app.include_router(auth.router, tags=["auth"])
  app.include_router(tasks.router)
  app.include_router(projects.router)
  app.include_router(notifications.router, prefix="/notifications", tags=["notifications"])
  # Absolute-path routes (/api/notifications/...) live on a sibling router
  # so they aren't prefixed twice. The status endpoint is the AC for the
  # delivery-tracking subtask.
  app.include_router(notifications.api_router, tags=["notifications"])
  app.include_router(ai.router, tags=["ai"])
  app.include_router(users.router, prefix="/users", tags=["users"])
  # Sibling router for absolute-path users endpoints (/api/users/...).
  ```
- `frontend/index.html:12-574` — `Inspector Bridge Script` — این اسکریپت تزریق‌شده در `index.html` یک نمونه از 'صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره' یا 'هرج و مرج' است که کاربر به آن اشاره کرده و باید در فهرست‌برداری شناسایی شود.
  ```
  <!-- Inspector Bridge Script - Auto-injected -->
  <!-- Version: 2.4 -->
  <script>
  (function() {
    console.log('🌉 Inspector Bridge: Script starting...');
  
    // جلوگیری از اجرای چندباره
    if (window.__inspectorBridgeLoaded) {
      console.log('🌉 Inspector Bridge: Already loaded, skipping');
      return;
    }
    window.__inspectorBridgeLoaded = true;
  
    // بررسی اینکه آیا در iframe هستیم
    const isInIframe = window !== window.parent;
    console.log('🌉 Inspector Bridge: In iframe?', isInIframe);
    console.log('🌉 Inspector Bridge: Page URL:', window.location.href);
  
    // تنظیمات WebSocket
    const WS_URL = 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_lifemanager';
    const PROJECT_ID = 'gh_mahdighandi1989_lifemanager';
    const DEBOUNCE_MS = 100;
    let lastEventTime = 0;
    let messagesSent = 0;
    let ws = null;
    let wsReady = false;
    let reconnectTimer = null;
    let messageQueue = [];
  
    // 🌐 اتصال WebSocket به Backend Hub
    function connectWebSocket() {
      if (!WS_URL || WS_URL === 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_lifemanager') {
        console.log('🌉 Inspector Bridge: No WS URL configured, using postMessage only');
        return;
      }
      try {
        ws = new WebSocket(WS_URL);
        ws.onopen = function() {
          console.log('🌉 Inspector Bridge: WebSocket connected');
          ws.send(JSON.stringify({ type: 'register', role: 'bridge' }));
        };
        ws.onmessage = function(event) {
          try {
            var msg = JSON.parse(event.data);
            if (msg.type === 'registered') {
              wsReady = true;
              console.log('🌉 Inspector Bridge: Registered as bridge via WebSocket');
              // ارسال پیام‌های در صف
              while (messageQueue.length > 0) {
                var queued = messageQueue.shift();
                ws.send(JSON.stringify(queued));
              }
              // ارسال پیام آماده بودن
              ws.send(JSON.stringify({
                type: 'inspector-bridge-ready',
                pageUrl: window.location.href,
                isInIframe: isInIframe,
                timestamp: Date.now()
              }));
            } else if (msg.type === 'pong') {
              // heartbeat response
            } else if (msg.type === 'command') {
              // دریافت دستور از Inspector
              console.log('🌉 Inspector Bridge: Received command:', msg);
              handleInspectorCommand(msg);
            }
          } catch (e) {
            console.warn('🌉 Inspector Bridge: WS message parse error', e);
          }
        };
        ws.onclose = function() {
          wsReady = false;
          console.log('🌉 Inspector Bridge: WebSocket disconnected, reconnecting in 3s...');
          reconnectTimer = setTimeout(connectWebSocket, 3000);
        };
        ws.onerror = function(e) {
          console.warn('🌉 Inspector Bridge: WebSocket error', e);
        };
      } catch (e) {
        console.warn('🌉 Inspector Bridge: Failed to create WebSocket', e);
      }
    }
  
    // پردازش دستورات از Inspector
    function handleInspectorCommand(msg) {
      if (msg.command === 'click') {
        // کلیک روی المان با selector
        var el = document.querySelector(msg.selector);
        if (el) { el.click(); sendToInspector('command-result', { success: true, command: 'click', selector: msg.selector }); }
        else { sendToInspector('command-result', { success: false, command: 'click', error: 'Element not found' }); }
      } else if (msg.command === 'navigate') {
        window.location.href = msg.url;
      } else if (msg.command === 'get-elements') {
        var elements = [];
        document.querySelectorAll('a, button, input, textarea, select, [role="button"], [onclick]').forEach(function(el, i) {
          elements.push({
            index: i,
            tag: el.tagName.toLowerCase(),
            text: (el.innerText || el.value || '').trim().slice(0, 50),
            id: el.id || '',
            className: (el.className || '').toString().slice(0, 50),
            href: el.href || ''
          });
        });
        sendToInspector('elements-list', { elements: elements });
      }
    }
  
    // تابع ارسال پیام (WebSocket اولویت اول، postMessage فالبک)
    function sendToInspector(action, data) {
      try {
        var message = {
          type: 'inspector-bridge-event',
          action: action,
          target: data.target || '',
          elementInfo: data.elementInfo || '',
          position: data.position || { xPercent: 50, yPercent: 50 },
          pageUrl: window.location.href,
          timestamp: Date.now(),
          level: data.level || null,
          source: 'imported-project',
          networkMeta: data.networkMeta || null
        };
  
        // ارسال از طریق WebSocket
        if (ws && wsReady) {
          ws.send(JSON.stringify(message));
        } else if (ws && !wsReady) {
          messageQueue.push(message);
        }
  
        // همیشه postMessage هم بفرست (فالبک)
        if (isInIframe) {
          window.parent.postMessage(message, '*');
        }
  
        messagesSent++;
        console.log('🌉 Inspector Bridge: Sent message #' + messagesSent, action, data.elementInfo);
      } catch (e) {
        console.warn('Inspector bridge: failed to send message', e);
      }
    }
  
    // 🌐 Network Monitoring (fetch + XHR) — v2.4
    // hook کردن window.fetch و XMLHttpRequest برای ثبت درخواست‌های شبکه پروژهٔ deploy شده.
    // event ها: network-request (شروع)، network-response (موفق)، network-error (ناموفق).
    // قبل از send، header های Authorization/Cookie و query paramهای حساس masked می‌شوند.
    (function setupNetworkMonitoring() {
      try {
        var __netReqSeq = 0;
        var __maskValue = function(v) {
          if (!v) return v;
          var s = String(v);
          return s.length > 8 ? s.slice(0, 4) + '****' + s.slice(-2) : '****';
        };
        var __maskUrl = function(url) {
          try {
            var u = new URL(url, window.location.origin);
            u.searchParams.forEach(function(val, key) {
              var lk = key.toLowerCase();
              if (lk.indexOf('token') >= 0 || lk.indexOf('key') >= 0 || lk.indexOf('secret') >= 0 || lk.indexOf('password') >= 0) {
                u.searchParams.set(key, __maskValue(val));
              }
            });
            return u.toString();
          } catch (e) { return String(url); }
        };
        var __summarizeUrl = function(url) {
          try {
            var u = new URL(url, window.location.origin);
            var path = u.pathname;
            if (path.length > 80) path = path.slice(0, 77) + '...';
            return u.host + path;
          } catch (e) { return String(url).slice(0, 100); }
        };
        // ---- fetch ----
        if (typeof window.fetch === 'function' && !window.__inspectorFetchHooked) {
          window.__inspectorFetchHooked = true;
          var origFetch = window.fetch.bind(window);
          window.fetch = function(input, init) {
            var reqId = 'fetch_' + (++__netReqSeq) + '_' + Date.now();
            var url = typeof input === 'string' ? input : (input && input.url) || '';
            var method = (init && init.method) || (input && input.method) || 'GET';
            var maskedUrl = __maskUrl(url);
            var startedAt = Date.now();
            try {
              sendToInspector('network-request', {
                elementInfo: method.toUpperCase() + ' ' + __summarizeUrl(maskedUrl),
                level: null,
                networkMeta: { reqId: reqId, method: method.toUpperCase(), url: maskedUrl, startedAt: startedAt }
              });
            } catch (e) {}
            return origFetch(input, init).then(function(res) {
              try {
                var dur = Date.now() - startedAt;
                var ok = res && res.ok;
                var status = res ? res.status : 0;
                var label = method.toUpperCase() + ' ' + __summarizeUrl(maskedUrl) + ' → ' + status + ' (' + dur + 'ms)';
                sendToInspector(ok ? 'network-response' : 'network-error', {
                  elementInfo: label,
                  level: ok ? null : 'error',
                  networkMeta: { reqId: reqId, method: method.toUpperCase(), url: maskedUrl, status: status, durationMs: dur, ok: !!ok }
                });
              } catch (e) {}
              return res;
            }).catch(function(err) {
              try {
                var dur = Date.now() - startedAt;
                sendToInspector('network-error', {
                  elementInfo: method.toUpperCase() + ' ' + __summarizeUrl(maskedUrl) + ' ✗ ' + (err && err.message || 'fetch failed'),
                  level: 'error',
                  networkMeta: { reqId: reqId, method: method.toUpperCase(), url: maskedUrl, status: 0, durationMs: dur, ok: false, errorMessage: err && err.message }
                });
              } catch (e) {}
              throw err;
            });
          };
        }
        // ---- XMLHttpRequest ----
        if (window.XMLHttpRequest && !window.__inspectorXhrHooked) {
          window.__inspectorXhrHooked = true;
          var origOpen = XMLHttpRequest.prototype.open;
          var origSend = XMLHttpRequest.prototype.send;
          XMLHttpRequest.prototype.open = function(method, url) {
            try {
              this.__inspectorReqId = 'xhr_' + (++__netReqSeq) + '_' + Date.now();
              this.__inspectorMethod = (method || 'GET').toUpperCase();
              this.__inspectorUrl = __maskUrl(url);
            } catch (e) {} 
            return origOpen.apply(this, arguments);
          };
          XMLHttpRequest.prototype.send = function() {
            var xhr = this;
            try {
              xhr.__inspectorStartedAt = Date.now();
              sendToInspector('network-request', {
                elementInfo: xhr.__inspectorMethod + ' ' + __summarizeUrl(xhr.__inspectorUrl),
                level: null,
                networkMeta: { reqId: xhr.__inspectorReqId, method: xhr.__inspectorMethod, url: xhr.__inspectorUrl, startedAt: xhr.__inspectorStartedAt }
              });
              xhr.addEventListener('loadend', function() {
                try {
                  var dur = Date.now() - xhr.__inspectorStartedAt;
                  var status = xhr.status || 0;
                  var ok = status >= 200 && status < 400;
                  var label = xhr.__inspectorMethod + ' ' + __summarizeUrl(xhr.__inspectorUrl) + ' → ' + status + ' (' + dur + 'ms)';
                  sendToInspector(ok ? 'network-response' : 'network-error', {
                    elementInfo: label,
                    level: ok ? null : 'error',
                    networkMeta: { reqId: xhr.__inspectorReqId, method: xhr.__inspectorMethod, url: xhr.__inspectorUrl, status: status, durationMs: dur, ok: ok }
                  });
                } catch (e) {}
              });
            } catch (e) {}
            return origSend.apply(this, arguments);
          };
        }
      } catch (e) { /* network monitoring boot failed - non-critical */ }
    })();
  
    // شروع اتصال WebSocket
    connectWebSocket();
  
    // Heartbeat هر 25 ثانیه
    setInterval(function() {
      if (ws && wsReady) {
        try { ws.send(JSON.stringify({ type: 'ping' })); } catch(e) {}
      }
    }, 25000);
  
    // گرفتن اطلاعات المنت
    function getElementInfo(el) {
      if (!el) return 'عنصر ناشناخته';
  
      // متن المنت
      let text = (el.innerText || el.value || '').trim().slice(0, 50);
  
      // نوع المنت
      let type = el.tagName?.toLowerCase() || 'unknown';
  
      // کلاس یا آیدی
      let identifier = el.id ? '#' + el.id :
                       el.className ? '.' + el.className.split(' ')[0] : '';
  
      // ترجمه تگ‌های معروف به فارسی
      const tagLabels = {
        'button': 'دکمه',
        'a': 'لینک',
        'input': 'فیلد ورودی',
        'textarea': 'فیلد متن',
        'select': 'منوی انتخاب',
        'img': 'تصویر',
        'video': 'ویدیو',
        'form': 'فرم',
        'table': 'جدول',
        'div': 'بخش',
        'span': 'متن',
        'p': 'پاراگراف',
        'h1': 'عنوان اصلی',
        'h2': 'عنوان',
        'h3': 'عنوان',
        'nav': 'منوی ناوبری',
        'header': 'سربرگ',
        'footer': 'پاورقی',
        'li': 'آیتم لیست'
      };
  
      let typeLabel = tagLabels[type] || type;
  
      if (text) {
        return typeLabel + ' "' + text + '"';
      }
      return typeLabel + (identifier ? ' ' + identifier : '');
    }
  
    // محاسبه درصد موقعیت
    function getPositionPercent(e) {
      return {
        xPercent: (e.clientX / window.innerWidth) * 100,
        yPercent: (e.clientY / window.innerHeight) * 100
      };
    }
  
    // Debounce
    function shouldSend() {
      const now = Date.now();
      if (now - lastEventTime < DEBOUNCE_MS) return false;
      lastEventTime = now;
      return true;
    }
  
    // Event Listeners - window capture phase (بالاترین اولویت ممکن)
    // mousedown + pointerdown به عنوان فالبک برای زمانی که overlay ها click رو مصرف می‌کنند
  
    // کلیک - بالاترین لایه capture
    window.addEventListener('click', function(e) {
      if (!shouldSend()) return;
      sendToInspector('click', {
        target: e.target?.tagName,
        elementInfo: getElementInfo(e.target),
        position: getPositionPercent(e)
      });
    }, true);
  
    // 🆕 فالبک: mousedown و pointerdown (برای overlay هایی که click رو stop می‌کنند)
    var lastPointerDownTime = 0;
    window.addEventListener('pointerdown', function(e) {
      lastPointerDownTime = Date.now();
      // اگر 200ms بعد click نیومد، pointerdown رو ارسال کن
      setTimeout(function() {
        if (Date.now() - lastEventTime > 180) {
          sendToInspector('click', {
            target: e.target?.tagName,
            elementInfo: getElementInfo(e.target) + ' (pointerdown)',
            position: getPositionPercent(e)
          });
        }
      }, 200);
    }, true);
  
    // اسکرول
    let scrollTimeout;
    window.addEventListener('scroll', function(e) {
      clearTimeout(scrollTimeout);
      scrollTimeout = setTimeout(function() {
        sendToInspector('scroll', {
          elementInfo: 'صفحه',
          position: {
            xPercent: (window.scrollX / (document.body.scrollWidth - window.innerWidth)) * 100 || 0,
            yPercent: (window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100 || 0
          }
        });
      }, 200);
    }, true);
  
    // تایپ در فیلدها
    window.addEventListener('input', function(e) {
      if (!shouldSend()) return;
      if (e.target?.tagName === 'INPUT' || e.target?.tagName === 'TEXTAREA') {
        sendToInspector('input', {
          target: e.target?.tagName,
          elementInfo: getElementInfo(e.target),
          position: { xPercent: 50, yPercent: 50 }
        });
      }
    }, true);
  
    // فوکوس
    window.addEventListener('focus', function(e) {
      if (!shouldSend()) return;
      if (e.target && e.target !== document && e.target !== document.body) {
        sendToInspector('focus', {
          target: e.target?.tagName,
          elementInfo: getElementInfo(e.target),
          position: { xPercent: 50, yPercent: 50 }
        });
      }
    }, true);
  
    // 🔴 گیرنده خطاهای جاوااسکریپت فرانت‌اند
    var errorCount = 0;
    var MAX_ERRORS = 50; // حداکثر خطا در هر صفحه
    var consoleLogCount = 0;
    var MAX_CONSOLE_LOGS = 200; // حداکثر لاگ کنسول
  
    window.onerror = function(message, source, lineno, colno, error) {
      if (errorCount >= MAX_ERRORS) return;
      errorCount++;
      var errorInfo = (message || 'Unknown error').toString().slice(0, 150);
      if (source) errorInfo += ' (at ' + source.split('/').pop() + ':' + lineno + ')';
      sendToInspector('error', {
        target: 'window',
        elementInfo: errorInfo,
        position: { xPercent: 50, yPercent: 10 },
        level: 'error'
      });
    };
  
    window.addEventListener('unhandledrejection', function(e) {
      if (errorCount >= MAX_ERRORS) return;
      errorCount++;
      var reason = (e.reason && (e.reason.message || e.reason.toString())) || 'Promise rejected';
      sendToInspector('error', {
        target: 'promise',
        elementInfo: reason.toString().slice(0, 150),
        position: { xPercent: 50, yPercent: 10 },
        level: 'error'
      });
    });
  
    // 🔵 رهگیری تمام متدهای کنسول - تفکیک لاگ‌های پروژه ایمپورت شده
    function interceptConsole(level, origFn) {
      return function() {
        origFn.apply(console, arguments);
        if (consoleLogCount >= MAX_CONSOLE_LOGS) return;
        consoleLogCount++;
        var msg = Array.prototype.slice.call(arguments).map(function(a) {
          return typeof a === 'object' ? JSON.stringify(a).slice(0, 200) : String(a).slice(0, 200);
        }).join(' ').slice(0, 500);
        // فیلتر: لاگ‌های خود bridge رو ارسال نکن
        if (msg.indexOf('Inspector Bridge') !== -1) return;
        if (msg.indexOf('🌉') !== -1) return;
        // ارسال به inspector با تفکیک سطح
        sendToInspector(level === 'error' ? 'console-error' : 'console-log', {
          target: 'console',
          elementInfo: msg,
          position: { xPercent: 50, yPercent: 10 },
          level: level
        });
      };
    }
  
    var origConsoleLog = console.log;
    var origConsoleWarn = console.warn;
    var origConsoleError = console.error;
    var origConsoleInfo = console.info;
    var origConsoleDebug = console.debug;
  
    console.log = interceptConsole('log', origConsoleLog);
    console.warn = interceptConsole('warn', origConsoleWarn);
    console.error = interceptConsole('error', origConsoleError);
    console.info = interceptConsole('info', origConsoleInfo);
    console.debug = interceptConsole('debug', origConsoleDebug);
  
    // 🔍 MutationObserver - تشخیص لایه‌های خطا (Error Overlays)
    var __attachedOverlays = new WeakSet();
    function attachOverlayListeners(node) {
      if (__attachedOverlays.has(node)) return;
      __attachedOverlays.add(node);
      var overlayText = (node.textContent || '').slice(0, 500);
      sendToInspector('error-overlay', {
        target: node.tagName,
        elementInfo: 'لایه خطا شناسایی شد: ' + overlayText.slice(0, 200),
        position: { xPercent: 50, yPercent: 50 },
        level: 'error'
      });
      // اتصال listener ها به لایه خطا (click + pointerdown)
      node.addEventListener('click', function(e) {
        sendToInspector('click', {
          target: e.target?.tagName,
          elementInfo: getElementInfo(e.target) + ' (error overlay)',
          position: getPositionPercent(e)
        });
      }, true);
      node.addEventListener('pointerdown', function(e) {
        sendToInspector('click', {
          target: e.target?.tagName,
          elementInfo: getElementInfo(e.target) + ' (overlay pointerdown)',
          position: getPositionPercent(e)
        });
      }, true);
      // اگر Shadow DOM داره، listener داخلش هم بذار
      if (node.shadowRoot) {
        node.shadowRoot.addEventListener('click', function(e) {
          sendToInspector('click', {
            target: e.target?.tagName,
            elementInfo: getElementInfo(e.target) + ' (shadow DOM)',
            position: { xPercent: 50, yPercent: 50 }
          });
        }, true);
      }
    }
  
    function isOverlayElement(node) {
      try {
        var style = window.getComputedStyle(node);
        var zIndex = parseInt(style.zIndex) || 0;
        var isFixed = style.position === 'fixed' || style.position === 'absolute';
        var isFullScreen = node.offsetWidth > window.innerWidth * 0.8 && node.offsetHeight > window.innerHeight * 0.5;
        return isFixed && (zIndex > 1000 || isFullScreen);
      } catch(e) { return false; }
    }
  
    try {
      var overlayObserver = new MutationObserver(function(mutations) {
        mutations.forEach(function(mutation) {
          for (var i = 0; i < mutation.addedNodes.length; i++) {
            var node = mutation.addedNodes[i];
            if (node.nodeType !== 1) continue;
            if (isOverlayElement(node)) attachOverlayListeners(node);
          }
        });
      });
      if (document.body) {
        overlayObserver.observe(document.body, { childList: true, subtree: true });
      } else {
        document.addEventListener('DOMContentLoaded', function() {
          overlayObserver.observe(document.body, { childList: true, subtree: true });
        });
      }
    } catch(obsErr) {}
  
    // 🔁 اسکن دوره‌ای DOM برای overlay هایی که MutationObserver ممکنه از دست بده
    setInterval(function() {
      try {
        var allFixed = document.querySelectorAll('[style*="position: fixed"], [style*="position:fixed"]');
        for (var i = 0; i < allFixed.length; i++) {
          if (isOverlayElement(allFixed[i])) attachOverlayListeners(allFixed[i]);
        }
        // بررسی nextjs-portal و shadow roots
        var portals = document.querySelectorAll('nextjs-portal, [id*="overlay"], [id*="error"], [class*="overlay"], [class*="error-boundary"]');
        for (var j = 0; j < portals.length; j++) {
          if (portals[j].shadowRoot && !__attachedOverlays.has(portals[j])) {
            attachOverlayListeners(portals[j]);
          }
        }
      } catch(e) {}
    }, 2000);
  
    // اعلام آماده بودن
    try {
      window.parent.postMessage({
        type: 'inspector-bridge-ready',
        pageUrl: window.location.href,
        isInIframe: isInIframe,
        timestamp: Date.now()
      }, '*');
      console.log('🌉 Inspector Bridge: Ready message sent to parent');
    } catch (readyErr) {
      console.warn('🌉 Inspector Bridge: Failed to send ready message', readyErr);
    }
  
    console.log('🌉 Inspector Bridge: Script loaded and active!');
    console.log('🌉 Inspector Bridge: Click, scroll, or type to test');
  })();
  </script>
  ```
- `app/routes/auth_google.py:21-41` — `google_login` — این یک نمونه از تعریف endpoint در روترهای بک‌اند است که برای فهرست‌برداری از تمامی endpointهای مرتبط با 'لیست‌ها، تسک‌ها و صفحات' باید به همین شکل استخراج شوند.
  ```python
  @router.get("/auth/google")
  async def google_login():
      """Redirect to Google OAuth consent screen."""
      if not settings.GOOGLE_CLIENT_ID:
          raise HTTPException(
              status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
              detail="Google OAuth not configured. Set GOOGLE_CLIENT_ID in environment variables."
          )
      
      redirect_uri = settings.GOOGLE_REDIRECT_URI or "http://localhost:8000/auth/google/callback"
      
      google_auth_url = (
          f"https://accounts.google.com/o/oauth2/v2/auth"
          f"?client_id={settings.GOOGLE_CLIENT_ID}"
          f"&redirect_uri={redirect_uri}"
          f"&response_type=code"
          f"&scope=openid%20email%20profile"
          f"&access_type=offline"
      )
      
      return RedirectResponse(url=google_auth_url)
  ```
- `app/routes/tasks.py` — `router` — این فایل شامل تعریف endpointهای مربوط به تسک‌ها است که برای فهرست‌برداری از APIهای بک‌اند ضروری است. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
- `app/models/todo_list.py` — `TodoList` — این فایل شامل تعریف مدل TodoList است که برای فهرست‌برداری از ساختار داده‌های بک‌اند ضروری است. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
- `frontend/src/pages/Tasks.jsx` — `TasksPage` — این فایل یک صفحه فرانت‌اند برای نمایش تسک‌ها است که برای فهرست‌برداری از صفحات برنامه ضروری است. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده شامل FastAPI برای بک‌اند، React برای فرانت‌اند و Vite برای ابزار ساخت فرانت‌اند است. پایگاه داده از SQLAlchemy با asyncpg استفاده می‌کند. این ابزارها و فریم‌ورک‌ها ساختار پروژه را تعیین می‌کنند و در فرآیند فهرست‌برداری باید مد نظر قرار گیرند.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/projects.py` — این روتر endpointهای مربوط به پروژه‌ها را تعریف می‌کند که در کنار تسک‌ها و لیست‌ها، بخش مهمی از موجودی برنامه را تشکیل می‌دهد.
- `app/routes/lists.py` — این روتر endpointهای مربوط به لیست‌های ToDo را تعریف می‌کند که مستقیماً به درخواست کاربر برای 'لیست‌ها' مرتبط است.
- `app/routes/todo_items.py` — این روتر endpointهای مربوط به آیتم‌های ToDo را تعریف می‌کند که جزئی از سیستم 'لیست‌ها' و 'تسک‌ها' است.
- `app/models/task.py` — این فایل مدل داده‌ای برای تسک‌ها را تعریف می‌کند که برای درک ساختار داده‌های بک‌اند ضروری است.
- `app/models/project.py` — این فایل مدل داده‌ای برای پروژه‌ها را تعریف می‌کند که برای درک ساختار داده‌های بک‌اند ضروری است.
- `frontend/src/pages/Lists.jsx` — این صفحه فرانت‌اند لیست‌های ToDo را نمایش می‌دهد و برای فهرست‌برداری از صفحات برنامه حیاتی است.
- `docs/API.md` — این سند مرجعی برای تمامی endpointهای API است و می‌تواند به تأیید و تکمیل فهرست‌برداری از endpointهای بک‌اند کمک کند.

## 🌐 نقشهٔ وابستگی‌ها
این تسک شامل بررسی وابستگی‌های متقابل بین اجزای بک‌اند و فرانت‌اند است. در بک‌اند، روترهایی مانند `app/routes/tasks.py`، `app/routes/projects.py`، `app/routes/lists.py` و `app/routes/todo_items.py` به مدل‌های SQLAlchemy مربوطه در `app/models/` (مانند `app/models/task.py`, `app/models/project.py`, `app/models/todo_list.py`, `app/models/todo_item.py`) وابسته هستند. این روترها از `app/main.py` فراخوانی و در برنامه اصلی FastAPI ثبت می‌شوند. در فرانت‌اند، صفحات React مانند `frontend/src/pages/Tasks.jsx`، `frontend/src/pages/Projects.jsx` و `frontend/src/pages/Lists.jsx` از طریق `frontend/src/lib/api.js` با endpointهای بک‌اند ارتباط برقرار می‌کنند. همچنین، `frontend/index.html` به عنوان نقطه ورود، اسکریپت‌های اصلی React را بارگذاری می‌کند و ممکن است شامل اسکریپت‌های تزریق‌شده‌ای مانند 'Inspector Bridge Script' باشد که به طور مستقل عمل می‌کنند.

## 🔍 Context و وضعیت فعلی
این تسک به درخواست کاربر برای «بررسی و فهرست‌برداری از لیست‌ها، تسک‌ها و صفحات موجود» در برنامه Lifemanager پاسخ می‌دهد. هدف اصلی کاربر، «شناسایی و فهرست‌برداری کامل از هر یک از این اجزا بدون هیچ‌گونه خلاصه‌سازی یا حذف» است. کاربر تأکید دارد که این مرحله «صرفاً به جمع‌آوری اطلاعات و ایجاد یک موجودی کامل از وضعیت فعلی برنامه می‌پردازد و شامل هیچ‌گونه تحلیل شباهت یا تصمیم‌گیری برای ادغام نیست.»

با این حال، پشت این درخواست، هدف بلندمدت‌تری نهفته است: «می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.»

این درخواست به طور خاص به «لیست‌ها»، «تسک‌ها» و «صفحات» اشاره دارد. در کدبیس فعلی Lifemanager، این موارد شامل:

*   **تسک‌ها (Tasks):** مدل `app/models/task.py`، روتر `app/routes/tasks.py` و صفحه فرانت‌اند `frontend/src/pages/Tasks.jsx`.
*   **پروژه‌ها (Projects):** مدل `app/models/project.py`، روتر `app/routes/projects.py` و صفحه فرانت‌اند `frontend/src/pages/Projects.jsx` (که اغلب در کنار تسک‌ها مدیریت می‌شوند).
*   **لیست‌ها (Todo Lists):** مدل `app/models/todo_list.py`، روتر `app/routes/lists.py` و صفحات فرانت‌اند `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx`.
*   **آیتم‌های لیست (Todo Items):** مدل `app/models/todo_item.py` و روتر `app/routes/todo_items.py`.
*   **صفحات (Pages):** تمامی فایل‌های `.jsx` در `frontend/src/pages/` و همچنین `frontend/index.html` که نقطه ورود SPA است و شامل اسکریپت‌های تزریق‌شده مانند 'Inspector Bridge Script' (خطوط 12-574) است که ممکن است جزو «صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره» باشد.

این تسک بر جمع‌آوری این اطلاعات متمرکز است تا پایه‌ای برای تحلیل‌های بعدی و تصمیم‌گیری‌های معماری فراهم کند و به برنامه کمک کند «از این هرج و مرج فعلی در بیاد» و «قابل ارتقا بشه».

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک سند خروجی (JSON یا Markdown) حاوی فهرست تمامی endpointهای FastAPI مرتبط با 'tasks', 'projects', 'lists', 'todo_items' و 'auth_google' (به عنوان نمونه) با ذکر مسیر، متد HTTP و فایل منبع تولید شود.
- [ ] سند خروجی شامل فهرست تمامی مدل‌های SQLAlchemy مرتبط با 'Task', 'Project', 'TodoList', 'TodoItem' و 'OAuthUser' (به عنوان نمونه) با ذکر نام مدل، فیلدهای اصلی و فایل منبع باشد.
- [ ] سند خروجی شامل فهرست تمامی فایل‌های `.jsx` در `frontend/src/pages/` به عنوان صفحات فرانت‌اند با ذکر نام صفحه و مسیر فایل باشد.
- [ ] سند خروجی به طور خاص 'Inspector Bridge Script' را که در `frontend/index.html` (خطوط 12-574) قرار دارد، به عنوان یک اسکریپت تزریق‌شده و غیرمرتبط با منطق اصلی برنامه شناسایی و توصیف کند.
- [ ] سند خروجی باید صراحتاً بیان کند که هیچ خلاصه‌سازی یا حذفی در اطلاعات جمع‌آوری شده صورت نگرفته است.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای ایجاد فهرست جامع مورد نظر کاربر، مراحل زیر پیشنهاد می‌شود:

1.  **فهرست‌برداری از Endpointهای Backend:** تمامی مسیرها (paths) و متدهای HTTP (GET, POST, PUT, DELETE) مربوط به `tasks`, `projects`, `lists`, `todo_items` و سایر بخش‌های مرتبط را از فایل‌های `app/routes/*.py` استخراج کنید. این شامل بررسی `app/main.py` برای نحوه `include_router` و پیشوندهای آن‌ها نیز می‌شود.
2.  **فهرست‌برداری از مدل‌های Backend:** تمامی مدل‌های SQLAlchemy (مانند `Task`, `Project`, `TodoList`, `TodoItem`) را از فایل‌های `app/models/*.py` شناسایی کرده و فیلدهای اصلی هر مدل را لیست کنید.
3.  **فهرست‌برداری از صفحات Frontend:** تمامی فایل‌های `.jsx` موجود در `frontend/src/pages/` را به عنوان صفحات مجزا شناسایی و لیست کنید. همچنین، هرگونه اسکریپت یا المان غیرمرتبط با منطق اصلی برنامه که مستقیماً در `frontend/index.html` (مانند 'Inspector Bridge Script') تزریق شده است را مستند کنید.
4.  **جمع‌آوری اطلاعات تکمیلی:** برای هر مورد شناسایی شده (endpoint، مدل، صفحه)، یک توضیح مختصر از هدف آن و ارتباطش با سایر اجزا ارائه دهید.

خروجی این مراحل باید یک سند ساختاریافته (مثلاً JSON یا Markdown) باشد که تمامی این اطلاعات را بدون خلاصه‌سازی یا حذف ارائه دهد.

## 💡 نمونه‌های قبل/بعد
**مثال: استخراج اطلاعات یک Endpoint از روتر**

_قبل:_
```
در حال حاضر، اطلاعات endpointها به صورت پراکنده در فایل‌های روتر (مانند `app/routes/auth_google.py`) تعریف شده‌اند و هیچ فهرست متمرکزی از آن‌ها وجود ندارد.
```

_بعد:_
```
پس از اجرای تسک، خروجی شامل یک ورودی ساختاریافته برای هر endpoint خواهد بود، مثلاً:
```json
{
  "type": "endpoint",
  "path": "/auth/google",
  "method": "GET",
  "description": "Redirect to Google OAuth consent screen.",
  "source_file": "app/routes/auth_google.py",
  "lines": "21-41"
}
```
```

**مثال: شناسایی یک صفحه فرانت‌اند**

_قبل:_
```
صفحات فرانت‌اند به صورت فایل‌های `.jsx` در `frontend/src/pages/` وجود دارند، اما هیچ فهرست رسمی یا تحلیلی از آن‌ها به عنوان 'صفحات برنامه' وجود ندارد.
```

_بعد:_
```
پس از اجرای تسک، خروجی شامل یک ورودی ساختاریافته برای هر صفحه خواهد بود، مثلاً:
```json
{
  "type": "frontend_page",
  "name": "Tasks",
  "file_path": "frontend/src/pages/Tasks.jsx",
  "description": "صفحه اصلی برای مدیریت و نمایش تسک‌ها."
}
```
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `python -c "import os; print(os.path.exists('app/routes/tasks.py'))"`
- `python -c "import os; print(os.path.exists('app/models/todo_list.py'))"`
- `python -c "import os; print(os.path.exists('frontend/src/pages/Tasks.jsx'))"`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک اصلی در این مرحله، عدم شناسایی کامل یا دقیق تمامی اجزای مرتبط (لیست‌ها، تسک‌ها، صفحات) است، به خصوص اگر برخی از آن‌ها به صورت پویا تولید شده باشند یا در فایل‌هایی با نام‌گذاری غیرمتعارف قرار داشته باشند. همچنین، تفسیر نادرست از هدف یا ماهیت برخی اسکریپت‌ها (مانند 'Inspector Bridge Script' در `frontend/index.html`) می‌تواند منجر به فهرست‌برداری ناقص یا گمراه‌کننده شود. عدم دسترسی به کد کامل `frontend/src/App.jsx` ممکن است باعث شود که نگاشت دقیق URL به صفحات فرانت‌اند به طور کامل انجام نشود و تنها بر اساس نام فایل‌های `pages` حدس زده شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: تحلیل شباهت‌ها و شناسایی موارد تکراری یا بی‌دلیل

**Scope:** در این مرحله، موجودی ایجاد شده از لیست‌ها، تسک‌ها و صفحات مورد تحلیل دقیق قرار می‌گیرد تا موارد دارای شباهت، تکراری، یا صفحاتی که 'بی‌دلیل' ساخته شده‌اند و تأثیری ندارند، شناسایی شوند. این تحلیل باید بر اساس معیارهای مشخصی برای شباهت (مثلاً عملکرد مشابه، داده‌های یکسان، UI مشابه) انجام شود. خروجی این مرحله، لیستی از موارد کاندید برای تلفیق و ادغام است.
**Key terms:** مشابهت‌هایی داشته باشه, صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده, تأثیری نداره, صفحه مشابهی براش هست

**بخش مربوط از متن کاربر:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

## 🎯 هدف (خلاصه ساختاریافته)
تدوین طرح تحلیل و ادغام موجودیت‌های تکراری برنامه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:363-385` — `app.include_router` — این بخش نحوه گنجاندن روترهای مختلف API را تعریف می‌کند که برای درک ساختار بک‌اند و شناسایی همپوشانی‌ها یا ناسازگاری‌ها در مسیرها (مانند /tasks در مقابل /api/tasks) حیاتی است.
  ```python
  app.include_router(auth.router, tags=["auth"])
  app.include_router(tasks.router)
  app.include_router(projects.router)
  app.include_router(notifications.router, prefix="/notifications", tags=["notifications"])
  # Absolute-path routes (/api/notifications/...) live on a sibling router
  # so they aren't prefixed twice. The status endpoint is the AC for the
  # delivery-tracking subtask.
  app.include_router(notifications.api_router, tags=["notifications"])
  app.include_router(ai.router, tags=["ai"])
  app.include_router(users.router, prefix="/users", tags=["users"])
  # Sibling router for absolute-path users endpoints (/api/users/...).
  ```
- `app/main.py:411-415` — `_API_PREFIXES` — این تاپل به صراحت پیشوندهایی را لیست می‌کند که توسط SPA catch-all مدیریت نمی‌شوند. عدم وجود 'tasks' و 'projects' در اینجا نشان می‌دهد که آنها مسیرهای SPA هستند، در حالی که همتایان API آنها تحت /api/tasks و /api/projects قرار دارند. این تمایز برای درک درک کاربر از 'صفحات مشابه' حیاتی است.
  ```python
  _API_PREFIXES = (
          "auth", "notifications", "ai",
          "users", "integrations", "webhook", "health", "api",
          # Todo-list system endpoints — purely API, no SPA route.
          "todo-items",
      )
  ```
- `app/models/task.py` — `Task model definition` — این فایل مدل Task را تعریف می‌کند که باید با مدل‌های TodoItem و Project برای شباهت‌های ساختار داده مقایسه شود. (این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند)
- `app/models/todo_item.py` — `TodoItem model definition` — این فایل مدل TodoItem را تعریف می‌کند که باید با Task برای همپوشانی عملکردی مقایسه شود، به ویژه با توجه به نگرانی کاربر در مورد 'لیست‌ها' و 'تسک‌ها'. (این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند)
- `frontend/src/pages/Tasks.jsx` — `Tasks page component` — این صفحه فرانت‌اند تسک‌ها را نمایش می‌دهد. عملکرد و UI آن باید با frontend/src/pages/Lists.jsx و frontend/src/pages/ListDetail.jsx مقایسه شود تا تکرارهای احتمالی UI/UX یا فرصت‌های استفاده مجدد از کامپوننت‌ها شناسایی شود. (این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند)

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. این تحلیل شامل بررسی کد پایتون برای مدل‌ها، سرویس‌ها و مسیرها، و کد جاوااسکریپت/ری‌اکت برای کامپوننت‌ها و صفحات فرانت‌اند خواهد بود.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/list_service.py` (سطر 305) — مدیریت موجودیت‌های todo_list را بر عهده دارد که بخش اصلی درخواست کاربر برای 'لیست‌ها' است. (در app/main.py در خط 305 ایمپورت شده است)
- `app/services/todo_item_service.py` (سطر 307) — مدیریت موجودیت‌های todo_item را بر عهده دارد که ارتباط نزدیکی با 'تسک‌ها' و 'لیست‌ها' دارند و باید برای همپوشانی تحلیل شوند. (در app/main.py در خط 307 ایمپورت شده است)
- `app/routes/tasks.py` (سطر 364) — مسیرهای API برای تسک‌ها را تعریف می‌کند که باید با مسیرهای todo_items و projects برای همپوشانی عملکردی مقایسه شوند. (در app/main.py در خط 364 گنجانده شده است)
- `app/routes/lists.py` (سطر 379) — مسیرهای API برای لیست‌های todo را تعریف می‌کند که تمرکز اصلی درخواست کاربر است. (در app/main.py در خط 379 گنجانده شده است)
- `frontend/src/pages/Lists.jsx` — صفحه فرانت‌اند برای نمایش لیست‌ها، که برای شناسایی تکرارهای UI/UX با سایر صفحات مرتبط با لیست/تسک حیاتی است.
- `frontend/src/pages/Projects.jsx` — صفحه فرانت‌اند برای نمایش پروژه‌ها، که ممکن است الگوهای مدیریتی مشابهی با تسک‌ها/لیست‌ها داشته باشد و نیاز به تحلیل دارد.

## 🌐 نقشهٔ وابستگی‌ها
هسته این تحلیل حول مدل‌های داده‌ای (`app/models/task.py`, `app/models/todo_item.py`, `app/models/todo_list.py`, `app/models/project.py`)، مسیرهای API مربوطه (`app/routes/tasks.py`, `app/routes/todo_items.py`, `app/routes/lists.py`, `app/routes/projects.py`) و سرویس‌هایی که منطق آنها را هماهنگ می‌کنند (`app/services/list_service.py`, `app/services/todo_item_service.py`, `app/services/planner_service.py`) می‌چرخد. فایل `app/main.py` به عنوان هاب مرکزی برای گنجاندن تمام این روترها و تعریف استراتژی کلی مسیریابی، از جمله تمایز بین مسیرهای API و SPA، عمل می‌کند که برای درک 'صفحات مشابه' حیاتی است. صفحات فرانت‌اند مانند `frontend/src/pages/Tasks.jsx`, `frontend/src/pages/Lists.jsx`, `frontend/src/pages/Projects.jsx` از این APIهای بک‌اند استفاده کرده و UI را ارائه می‌دهند، که آنها را برای شناسایی تکرارهای بصری و عملکردی کلیدی می‌سازد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست یک تحلیل جامع از لیست‌ها، تسک‌ها و صفحات موجود در برنامه Lifemanager را دارد تا شباهت‌ها، موارد تکراری، یا صفحاتی که 'بی‌دلیل' ساخته شده‌اند و تأثیری ندارند، شناسایی شوند. هدف اصلی این است که این عناصر 'تلفیق و ادغام' شوند تا 'ساختار برنامه و زیرساختش' بهبود یابد، 'قابل ارتقا' شود و از 'هرج و مرج فعلی' خارج گردد. کاربر به طور خاص به موارد زیر اشاره کرده است:
- 'مشابهت‌هایی داشته باشه' (having similarities)
- 'صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده' (pages that were created unnecessarily at the beginning)
- 'تأثیری نداره' (have no impact)
- 'صفحه مشابهی براش هست' (there is a similar page for it)

این درخواست نیازمند بررسی دقیق هم مدل‌های داده‌ای بک‌اند (مانند `app/models/task.py`, `app/models/todo_item.py`, `app/models/project.py`)، مسیرهای API (مانند `app/routes/tasks.py`, `app/routes/todo_items.py`, `app/routes/projects.py`, `app/routes/lists.py`) و هم کامپوننت‌ها/صفحات فرانت‌اند (مانند `frontend/src/pages/Tasks.jsx`, `frontend/src/pages/Projects.jsx`, `frontend/src/pages/Lists.jsx`, `frontend/src/pages/ListDetail.jsx`) است. منطق مسیریابی در `app/main.py`، به ویژه تمایز بین مسیرهای SPA (مانند `/tasks`) و مسیرهای API (مانند `/api/tasks`) که در `_API_PREFIXES` (خطوط 411-415) مشخص شده، یک حوزه حیاتی برای تحلیل است زیرا ممکن است به درک کاربر از 'صفحات مشابه' کمک کند. همچنین، منطق اولیهٔ seed کردن داده‌ها در `app/main.py` (خطوط 299-321) و `app/services/list_service.py` برای `todo_lists` و `todo_items` نیز باید برای شناسایی تکرارهای احتمالی در داده‌های اولیه مورد توجه قرار گیرد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک لیست مستند شده از تمام مدل‌ها (`app/models/*.py`)، سرویس‌ها (`app/services/*.py`) و مسیرهای API (`app/routes/*.py`) بک‌اند به همراه مسئولیت‌های اصلی آنها تهیه شود.
- [ ] یک لیست مستند شده از تمام صفحات مهم (`frontend/src/pages/*.jsx`) و کامپوننت‌ها (`frontend/src/components/*.jsx`) فرانت‌اند به همراه مسئولیت‌های اصلی و وابستگی‌های API آنها تهیه شود.
- [ ] لیستی از مدل‌ها، سرویس‌ها، مسیرهای API یا کامپوننت‌ها/صفحات فرانت‌اند که به عنوان مشابه، تکراری یا غیرضروری شناسایی شده‌اند، به همراه دلیل هر مورد، ارائه شود. این باید شامل تحلیل `_API_PREFIXES` در `app/main.py` و تأثیر آن بر مسیریابی فرانت‌اند باشد.
- [ ] یک طرح سطح بالا برای چگونگی رفع تکرارهای شناسایی شده، شامل تغییرات معماری احتمالی یا تلاش‌های ادغام، پیشنهاد شود.
- [ ] یک بخش خاص در گزارش تحلیل که جزئیات شباهت‌ها و تفاوت‌های بین مدل‌های `app/models/task.py` و `app/models/todo_item.py` را بررسی کرده و پیشنهاد دهد که آیا باید یکپارچه شوند یا جداگانه نگهداری شوند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. این تسک شامل یک فاز تحلیل و برنامه‌ریزی معماری است و نه پیاده‌سازی مستقیم کد. اقدام پیشنهادی، ایجاد یک طرح تفصیلی برای شناسایی و رفع تکرارها است که سپس مبنای تسک‌های بازآرایی (refactoring) بعدی خواهد بود:

1.  **تعریف معیارهای تکرار/شباهت:** معیارهای واضحی برای آنچه که یک موجودیت 'مشابه'، 'تکراری' یا 'غیرضروری' در مدل‌ها، سرویس‌ها، مسیرهای API و کامپوننت‌ها/صفحات فرانت‌اند محسوب می‌شود، تعریف شود.
2.  **موجودی‌برداری و نقشه‌برداری:**
    *   **بک‌اند:** مستندسازی تمام مدل‌های موجود (`app/models/*.py`)، سرویس‌ها (`app/services/*.py`) و مسیرهای API (`app/routes/*.py`). نقشه‌برداری از اینکه کدام مسیرها با کدام سرویس‌ها و مدل‌ها تعامل دارند.
    *   **فرانت‌اند:** مستندسازی تمام صفحات (`frontend/src/pages/*.jsx`) و کامپوننت‌های مهم (`frontend/src/components/*.jsx`). نقشه‌برداری از اینکه کدام صفحات از کدام مسیرهای API استفاده می‌کنند.
3.  **تحلیل متقابل:**
    *   **مدل‌های داده:** مقایسه `app/models/task.py`, `app/models/todo_item.py`, `app/models/todo_list.py`, `app/models/project.py` برای یافتن فیلدهای همپوشان یا شباهت‌های مفهومی.
    *   **مسیرهای API:** تحلیل `app/routes/tasks.py`, `app/routes/todo_items.py`, `app/routes/lists.py`, `app/routes/projects.py` برای همپوشانی عملکردی. توجه ویژه به استراتژی مسیریابی در `app/main.py` (خطوط 363-385 برای گنجاندن روترها و خطوط 411-415 برای `_API_PREFIXES`) برای درک تمایز بین مسیرهای SPA و API.
    *   **صفحات/کامپوننت‌های فرانت‌اند:** بررسی `frontend/src/pages/Tasks.jsx`, `frontend/src/pages/Projects.jsx`, `frontend/src/pages/Lists.jsx`, `frontend/src/pages/ListDetail.jsx` برای الگوهای UI مشابه، نمایش داده‌ها یا جریان‌های کاربری که می‌توانند ادغام شوند. شناسایی هر صفحه‌ای که دیگر لینک نشده یا استفاده نمی‌شود.
4.  **شناسایی کاندیداها برای ادغام/حذف:** بر اساس تحلیل متقابل، لیستی از مدل‌ها، سرویس‌ها، مسیرهای API یا دارایی‌های فرانت‌اند که کاندید ادغام، بازآرایی یا حذف هستند، به همراه دلیل هر مورد، تهیه شود.
5.  **پیشنهاد استراتژی بازآرایی:** یک استراتژی سطح بالا برای چگونگی رفع تکرارهای شناسایی شده، شامل انتزاع‌های جدید یا اصلاحات در انتزاع‌های موجود، ارائه شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
1. **افزایش دامنه (Scope Creep):** ماهیت گسترده 'شناسایی تکرارها' می‌تواند منجر به یک برنامه بازآرایی بیش از حد جاه‌طلبانه شود که توسعه ویژگی‌های جدید را به تأخیر می‌اندازد.
2. **شناسایی نادرست:** اشتباه در شناسایی یک کامپوننت یا مدل به عنوان تکراری می‌تواند منجر به از کار افتادن عملکرد موجود در صورت ادغام یا حذف بدون درک کامل شود. به عنوان مثال، تمایز بین `Task` و `TodoItem` ممکن است ظریف اما عمدی باشد.
3. **تأثیر بر عملکرد:** ادغام مدل‌ها یا سرویس‌ها ممکن است در صورت عدم طراحی دقیق، گلوگاه‌های عملکردی جدیدی ایجاد کند.
4. **عدم تطابق فرانت‌اند/بک‌اند:** تغییرات در مدل‌ها/APIهای بک‌اند برای ادغام باید با تغییرات فرانت‌اند با دقت هماهنگ شوند تا از خرابی UI جلوگیری شود، به ویژه با توجه به مسیریابی SPA در `app/main.py` و مسیرهای API جداگانه.
5. **اختلال در داده‌های اولیه (Seed Data):** تغییرات در `app/models/todo_list.py` یا `app/models/todo_item.py` می‌تواند بر منطق seed کردن داده‌ها در `app/main.py` (خطوط 299-321) و `app/services/list_service.py` تأثیر بگذارد و نیاز به مهاجرت یا تنظیم دقیق داشته باشد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 3: طراحی استراتژی تلفیق و ادغام (بدون خلاصه‌سازی/حذف)

**Scope:** بر اساس تحلیل شباهت‌ها، یک استراتژی دقیق و جامع برای تلفیق و ادغام لیست‌ها، تسک‌ها و صفحات شناسایی شده طراحی می‌شود. این استراتژی باید تضمین کند که هیچ اطلاعات یا عملکرد حیاتی 'بدون هیچ خلاصه‌سازی، بدون هیچ حذفی' از بین نرود. شامل طراحی مدل‌های داده جدید، جریان‌های کاری بهینه شده، و طرح‌های UI/UX برای اجزای ادغام شده است. این مرحله بر روی 'چگونه' ادغام تمرکز دارد.
**Key terms:** تلفیق و ادغام کنه, بدون هیچ خلاصه‌سازی, بدون هیچ حذفی

**بخش مربوط از متن کاربر:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

## 🎯 هدف (خلاصه ساختاریافته)
تلفیق و ادغام لیست‌ها، تسک‌ها و آیتم‌های Todo برای کاهش هرج و مرج

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/task.py:1-30` — `Task` — این مدل باید به تدریج حذف یا به یک view بر روی مدل `WorkItem` تبدیل شود. فیلدهای آن باید به مدل `WorkItem` منتقل شوند.
  ```python
  from datetime import datetime
  from typing import Optional, List
  from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, JSON, Date, Boolean
  from sqlalchemy.orm import relationship
  from app.database import Base
  
  class Task(Base):
      __tablename__ = "tasks"
  
      id = Column(Integer, primary_key=True, index=True)
      title = Column(String, index=True)
      description = Column(String, nullable=True)
      status = Column(String, default="pending")
      priority = Column(String, default="normal")
      created_at = Column(DateTime, default=datetime.utcnow)
      updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
      user_id = Column(Integer, ForeignKey("users.id"), nullable=True)
      project_id = Column(Integer, ForeignKey("projects.id"), nullable=True)
      due_date = Column(Date, nullable=True) # Changed from DateTime to Date
      estimated_duration = Column(Integer, nullable=True) # in minutes
      deadline = Column(DateTime(timezone=True), nullable=True)
      recurrence = Column(JSON, nullable=True) # e.g., {"type": "daily", "interval": 1}
      attachment = Column(String(500), nullable=True) # URL or path to attachment
  
      user = relationship("User", back_populates="tasks")
      project = relationship("Project", back_populates="tasks")
  ```
- `app/models/todo_item.py:1-25` — `TodoItem` — این مدل نیز باید به تدریج حذف یا به یک view بر روی مدل `WorkItem` تبدیل شود. فیلدهای آن باید به مدل `WorkItem` منتقل شوند.
  ```python
  from datetime import datetime, date
  from typing import Optional
  from sqlalchemy import Column, Integer, String, Boolean, DateTime, ForeignKey, Date
  from sqlalchemy.orm import relationship
  from app.database import Base
  
  class TodoItem(Base):
      __tablename__ = "todo_items"
  
      id = Column(Integer, primary_key=True, index=True)
      title = Column(String, index=True)
      description = Column(String, nullable=True)
      completed = Column(Boolean, default=False)
      created_at = Column(DateTime, default=datetime.utcnow)
      updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
      todo_list_id = Column(Integer, ForeignKey("todo_lists.id"))
      parent_id = Column(Integer, ForeignKey("todo_items.id", ondelete="CASCADE"), nullable=True)
      due_date = Column(Date, nullable=True)
  
      todo_list = relationship("TodoList", back_populates="items")
      parent = relationship("TodoItem", remote_side=[id], back_populates="children")
      children = relationship("TodoItem", back_populates="parent", cascade="all, delete-orphan")
  ```
- `app/routes/tasks.py:1-10` — این روتر باید بازسازی شود تا از `WorkItemService` و `WorkItem` schema استفاده کند، یا به طور کامل حذف و به روتر `work_items` منتقل شود.
  ```python
  from typing import List, Optional
  from fastapi import APIRouter, Depends, HTTPException, status, Query
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.schemas.task_schema import TaskCreate, TaskUpdate, TaskResponse, TaskSearch
  from app.models.task import Task
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  from app.services.planner_service import (
      create_task, get_task, get_tasks, update_task, delete_task, search_tasks
  )
  ```
- `app/main.py:363-385` — این بخش باید برای include کردن روتر جدید `work_items` و حذف یا به‌روزرسانی `tasks.router` و `todo_items.router` اصلاح شود.
  ```python
  app.include_router(auth.router, tags=["auth"])
  app.include_router(tasks.router)
  app.include_router(projects.router)
  app.include_router(notifications.router, prefix="/notifications", tags=["notifications"])
  # Absolute-path routes (/api/notifications/...) live on a sibling router
  # so they aren't prefixed twice. The status endpoint is the AC for the
  # delivery-tracking subtask.
  app.include_router(notifications.api_router, tags=["notifications"])
  app.include_router(ai.router, tags=["ai"])
  app.include_router(users.router, prefix="/users", tags=["users"])
  # Sibling router for absolute-path users endpoints (/api/users/...).
  app.include_router(users.api_router, tags=["users"])
  app.include_router(integrations.router, prefix="/integrations", tags=["integrations"])
  # Todo-list system: both routers use absolute `/api/...` decorators
  # (so they aren't double-prefixed) and ship the full CRUD + the
  # share / unshare / move / toggle actions.
  app.include_router(lists.router)
  app.include_router(todo_items.router)
  # webhook.router decorators carry the absolute path (/webhook, /webhook/health)
  # so it mounts with no prefix to avoid double-prefixing.
  app.include_router(webhook.router, tags=["webhook"])
  # planner router decorators also use absolute /api/planner paths.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. کتابخانه‌های مرتبط: SQLAlchemy برای ORM، Pydantic برای Schema Validation، Alembic برای Database Migrations.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/schemas/task_schema.py` — نیاز به بازنگری و احتمالا حذف یا ادغام با `work_item_schema.py` جدید.
- `app/schemas/todo_item_schema.py` — نیاز به بازنگری و احتمالا حذف یا ادغام با `work_item_schema.py` جدید.
- `app/services/planner_service.py` — منطق مربوط به `Task`ها باید به `WorkItemService` منتقل یا با آن ادغام شود.
- `app/services/todo_item_service.py` — منطق مربوط به `TodoItem`ها باید به `WorkItemService` منتقل یا با آن ادغام شود.
- `frontend/src/pages/Tasks.jsx` — این صفحه باید برای نمایش `WorkItem`های جدید بازطراحی شود.
- `frontend/src/pages/ListDetail.jsx` — این صفحه باید برای نمایش `WorkItem`های جدید (که قبلاً `TodoItem` بودند) بازطراحی شود.
- `app/models/project.py` — نیاز به به‌روزرسانی رابطه با `Task` به `WorkItem`.
- `app/models/todo_list.py` — نیاز به به‌روزرسانی رابطه با `TodoItem` به `WorkItem`.

## 🌐 نقشهٔ وابستگی‌ها
این تغییرات هستهٔ مدل‌های داده `Task` و `TodoItem` را تحت تأثیر قرار می‌دهد که توسط چندین سرویس و روتر استفاده می‌شوند. `app/database.py` به عنوان نقطهٔ اتصال به دیتابیس، `app/main.py` برای ثبت روترها، و `app/dependencies/auth.py` برای احراز هویت کاربران در تمام Endpointsهای مربوط به آیتم‌ها، از وابستگی‌های اصلی هستند. `app/services/planner_service.py` و `app/services/todo_item_service.py` که منطق اصلی `Task` و `TodoItem` را پیاده‌سازی می‌کنند، باید به `WorkItemService` جدید منتقل یا با آن ادغام شوند. `app/models/user.py` و `app/models/project.py` و `app/models/todo_list.py` نیز به دلیل روابط با `Task` و `TodoItem` تحت تأثیر قرار می‌گیرند. در فرانت‌اند، `frontend/src/pages/Tasks.jsx` و `frontend/src/pages/ListDetail.jsx` و `frontend/src/pages/Lists.jsx` به دلیل نمایش این موجودیت‌ها، نیاز به بازنویسی دارند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست یک استراتژی دقیق و جامع برای تلفیق و ادغام لیست‌ها، تسک‌ها و صفحات شناسایی شده در برنامه Lifemanager را دارد. هدف اصلی این استراتژی، کاهش هرج و مرج فعلی در ساختار برنامه و زیرساخت آن، و افزایش قابلیت ارتقا است. کاربر تأکید کرده که این فرآیند باید "بدون هیچ خلاصه‌سازی، بدون هیچ حذفی" انجام شود تا هیچ اطلاعات یا عملکرد حیاتی از بین نرود. این شامل طراحی مدل‌های داده جدید، جریان‌های کاری بهینه شده، و طرح‌های UI/UX برای اجزای ادغام شده است. این مرحله بر روی 'چگونه' ادغام تمرکز دارد.

در حال حاضر، برنامه دارای مدل‌های جداگانه برای `Task` (در `app/models/task.py`) و `TodoItem` (در `app/models/todo_item.py`) است. `TodoItem`ها به `TodoList` (در `app/models/todo_list.py`) مرتبط هستند و دارای قابلیت سلسله‌مراتبی با `parent_id` (خط 20 در `app/models/todo_item.py`) می‌باشند. `Task`ها نیز به `Project` (در `app/models/project.py`) مرتبط هستند و دارای فیلدهای برنامه‌ریزی پیشرفته‌تری مانند `estimated_duration`, `deadline`, `recurrence`, و `attachment` (که در `app/main.py` خطوط 256-261 در زمان startup به جدول `tasks` اضافه می‌شوند) هستند.

این دو موجودیت (`Task` و `TodoItem`) با وجود شباهت‌های عملکردی، در مدل‌های داده و APIهای جداگانه (مانند `app/routes/tasks.py` و `app/routes/todo_items.py`) مدیریت می‌شوند. این جدایی می‌تواند منجر به پیچیدگی و تکرار در منطق کسب‌وکار و رابط کاربری شود. همچنین، صفحات فرانت‌اند مانند `frontend/src/pages/Tasks.jsx` و `frontend/src/pages/ListDetail.jsx` به طور جداگانه این موجودیت‌ها را نمایش می‌دهند که ممکن است منجر به صفحات "بی‌دلیل" یا "مشابه" شود که کاربر به آن اشاره کرده است.

استراتژی تلفیق باید این مدل‌ها را به یک موجودیت واحد و قابل ارتقا تبدیل کند، APIهای مربوطه را یکپارچه سازد و رابط کاربری را برای نمایش و مدیریت یکپارچه این موجودیت‌ها بازطراحی کند، در حالی که تمام داده‌های موجود و ویژگی‌های خاص هر نوع (مانند فیلدهای برنامه‌ریزی `Task` یا سلسله‌مراتب `TodoItem`) حفظ شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل `WorkItem` با تمام فیلدهای `Task` و `TodoItem` ایجاد شود.
- [ ] Migration Alembic برای انتقال داده‌های `tasks` و `todo_items` به `work_items` ایجاد و اجرا شود.
- [ ] API Endpoints جدید `/api/work_items` برای CRUD مدل `WorkItem` پیاده‌سازی شوند.
- [ ] روترهای `app/routes/tasks.py` و `app/routes/todo_items.py` حذف یا به روتر `work_items` هدایت شوند.
- [ ] صفحه `frontend/src/pages/Tasks.jsx` برای نمایش و مدیریت `WorkItem`ها بازطراحی شود.
- [ ] هیچ داده‌ای از `Task`ها یا `TodoItem`های موجود در فرآیند ادغام از بین نرود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای تلفیق و ادغام لیست‌ها، تسک‌ها و آیتم‌های Todo، مراحل زیر پیشنهاد می‌شود:

1.  **طراحی مدل داده یکپارچه (Backend):**
    *   یک مدل داده جدید به نام `WorkItem` (یا نام مشابه) در `app/models/work_item.py` ایجاد شود. این مدل باید شامل تمام فیلدهای موجود در `app/models/task.py` (مانند `title`, `description`, `status`, `priority`, `estimated_duration`, `deadline`, `recurrence`, `attachment`, `project_id`) و `app/models/todo_item.py` (مانند `title`, `description`, `completed`, `due_date`, `todo_list_id`, `parent_id`) باشد. فیلدهایی که فقط در یکی از مدل‌ها وجود دارند، باید در مدل `WorkItem` به صورت `Optional` تعریف شوند.
    *   یک `enum` برای `WorkItemType` (مثلاً `TASK`, `TODO_ITEM`) اضافه شود تا نوع اصلی آیتم را مشخص کند.
    *   مدل‌های `TodoList` و `Project` در `app/models/todo_list.py` و `app/models/project.py` به‌روزرسانی شوند تا به جای `TodoItem` و `Task`، به `WorkItem` ارجاع دهند.
2.  **پیاده‌سازی Migration برای داده‌های موجود (Backend):**
    *   یک اسکریپت Migration (با استفاده از Alembic) ایجاد شود تا داده‌های موجود از جداول `tasks` و `todo_items` به جدول جدید `work_items` منتقل شوند. این Migration باید تضمین کند که تمام فیلدها و روابط (مانند `project_id`, `todo_list_id`, `parent_id`) به درستی نگاشت شوند.
    *   پس از Migration موفق، جداول `tasks` و `todo_items` می‌توانند حذف یا به عنوان جداول آرشیو نگهداری شوند.
3.  **بازسازی API Endpoints (Backend):**
    *   یک روتر جدید در `app/routes/work_items.py` ایجاد شود که شامل CRUD operations برای مدل `WorkItem` باشد (مثلاً `GET /api/work_items`, `POST /api/work_items`, `GET /api/work_items/{item_id}`, `PUT /api/work_items/{item_id}`, `DELETE /api/work_items/{item_id}`).
    *   روترهای فعلی `app/routes/tasks.py` و `app/routes/todo_items.py` به تدریج حذف یا به عنوان `RedirectResponse` به Endpoints جدید `work_items` هدایت شوند.
    *   `app/main.py` (خطوط 363-385) برای include کردن روتر جدید `work_items` و حذف روترهای قدیمی به‌روزرسانی شود. همچنین `_API_PREFIXES` (خطوط 411-415) باید به‌روزرسانی شود.
4.  **بازسازی Service Layer (Backend):**
    *   یک `WorkItemService` جدید در `app/services/work_item_service.py` ایجاد شود که منطق کسب‌وکار مربوط به `WorkItem` را مدیریت کند.
    *   `app/services/list_service.py` و `app/services/planner_service.py` به‌روزرسانی شوند تا از `WorkItemService` برای تعامل با آیتم‌ها استفاده کنند.
5.  **بازطراحی UI/UX (Frontend):**
    *   صفحات `frontend/src/pages/Tasks.jsx` و `frontend/src/pages/ListDetail.jsx` بازطراحی شوند تا از APIهای جدید `work_items` استفاده کنند و بتوانند هر دو نوع `Task` و `TodoItem` را به صورت یکپارچه نمایش دهند.
    *   یک کامپوننت یا صفحه جدید (مثلاً `frontend/src/pages/UnifiedWorkItems.jsx`) برای نمایش یکپارچه تمام آیتم‌های کاری (Tasks و TodoItems) با قابلیت فیلتر و جستجو بر اساس `todo_list_id`, `project_id`, `status`, `due_date` و غیره ایجاد شود.
    *   `frontend/src/App.jsx` و `frontend/src/components/Sidebar.jsx` برای به‌روزرسانی مسیرها و لینک‌های ناوبری به‌روزرسانی شوند.

## 💡 نمونه‌های قبل/بعد
**مدل `Task` و `TodoItem` قبل از ادغام**

_قبل:_
```
# app/models/task.py
class Task(Base):
    __tablename__ = "tasks"
    id = Column(Integer, primary_key=True, index=True)
    title = Column(String, index=True)
    description = Column(String, nullable=True)
    status = Column(String, default="pending")
    priority = Column(String, default="normal")
    user_id = Column(Integer, ForeignKey("users.id"), nullable=True)
    project_id = Column(Integer, ForeignKey("projects.id"), nullable=True)
    due_date = Column(Date, nullable=True)
    estimated_duration = Column(Integer, nullable=True)
    deadline = Column(DateTime(timezone=True), nullable=True)
    recurrence = Column(JSON, nullable=True)
    attachment = Column(String(500), nullable=True)

# app/models/todo_item.py
class TodoItem(Base):
    __tablename__ = "todo_items"
    id = Column(Integer, primary_key=True, index=True)
    title = Column(String, index=True)
    description = Column(String, nullable=True)
    completed = Column(Boolean, default=False)
    todo_list_id = Column(Integer, ForeignKey("todo_lists.id"))
    parent_id = Column(Integer, ForeignKey("todo_items.id", ondelete="CASCADE"), nullable=True)
    due_date = Column(Date, nullable=True)
```

_بعد:_
```
# app/models/work_item.py (New file)
from enum import Enum
from datetime import datetime, date
from typing import Optional
from sqlalchemy import Column, Integer, String, Boolean, DateTime, ForeignKey, JSON, Date
from sqlalchemy.orm import relationship
from app.database import Base

class WorkItemType(str, Enum):
    TASK = "task"
    TODO_ITEM = "todo_item"

class WorkItem(Base):
    __tablename__ = "work_items"

    id = Column(Integer, primary_key=True, index=True)
    title = Column(String, index=True)
    description = Column(String, nullable=True)
    # Common fields
    status = Column(String, default="pending") # From Task
    priority = Column(String, default="normal") # From Task
    completed = Column(Boolean, default=False) # From TodoItem
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=True)

    # Task-specific fields (now optional)
    project_id = Column(Integer, ForeignKey("projects.id"), nullable=True)
    estimated_duration = Column(Integer, nullable=True) # in minutes
    deadline = Column(DateTime(timezone=True), nullable=True)
    recurrence = Column(JSON, nullable=True)
    attachment = Column(String(500), nullable=True)

    # TodoItem-specific fields (now optional)
    todo_list_id = Column(Integer, ForeignKey("todo_lists.id"), nullable=True)
    parent_id = Column(Integer, ForeignKey("work_items.id", ondelete="CASCADE"), nullable=True)
    due_date = Column(Date, nullable=True) # Common, but type consistency needed

    item_type = Column(String, default=WorkItemType.TODO_ITEM.value, nullable=False) # New field to distinguish original type

    user = relationship("User", back_populates="work_items") # Need to update User model
    project = relationship("Project", back_populates="work_items") # Need to update Project model
    todo_list = relationship("TodoList", back_populates="work_items") # Need to update TodoList model
    parent = relationship("WorkItem", remote_side=[id], back_populates="children")
    children = relationship("WorkItem", back_populates="parent", cascade="all, delete-orphan")

# app/models/project.py (Update)
class Project(Base):
    # ... existing fields ...
    work_items = relationship("WorkItem", back_populates="project") # New relationship

# app/models/todo_list.py (Update)
class TodoList(Base):
    # ... existing fields ...
    work_items = relationship("WorkItem", back_populates="todo_list") # New relationship
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/`
- `npm run test -- frontend/src/pages/__tests__/Tasks.test.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **از دست رفتن داده در Migration:** انتقال داده از دو جدول به یک جدول جدید (`tasks` و `todo_items` به `work_items`) ریسک بالایی برای از دست رفتن یا ناسازگاری داده دارد، به خصوص با توجه به فیلدهای متفاوت و روابط پیچیده (مانند `parent_id` در `TodoItem` و `project_id` در `Task`). این نیاز به تست‌های Migration جامع و بک‌آپ‌گیری دقیق دارد.
2.  **ناسازگاری API:** تغییر Endpointsهای `tasks` و `todo_items` به `/api/work_items` می‌تواند باعث شکست کلاینت‌های فرانت‌اند یا هر سرویس دیگری شود که به این Endpointsها وابسته است. نیاز به مدیریت دقیق نسخه‌بندی API یا Redirectهای 307/308.
3.  **پیچیدگی منطق کسب‌وکار:** ادغام منطق `planner_service` و `todo_item_service` در یک `WorkItemService` می‌تواند منجر به یک سرویس بزرگ و پیچیده شود که نگهداری آن دشوار است.
4.  **تجربه کاربری:** بازطراحی صفحات فرانت‌اند مانند `frontend/src/pages/Tasks.jsx` و `frontend/src/pages/ListDetail.jsx` برای نمایش یکپارچه `WorkItem`ها، نیاز به طراحی UI/UX دقیق دارد تا تجربه کاربری فعلی افت نکند و قابلیت‌های هر دو نوع آیتم به خوبی نمایش داده شود.
5.  **تأثیر بر `app/main.py`:** تغییر در `app.include_router` (خطوط 363-385) و `_API_PREFIXES` (خطوط 411-415) می‌تواند بر مسیریابی کلی برنامه و نحوهٔ سرویس‌دهی فایل‌های استاتیک تأثیر بگذارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 4: پیاده‌سازی تلفیق و ادغام اجزا

**Scope:** در این مرحله، استراتژی طراحی شده برای تلفیق و ادغام به صورت عملی پیاده‌سازی می‌شود. این شامل بازنویسی یا اصلاح کد مربوط به لیست‌ها، تسک‌ها و صفحات، مهاجرت داده‌ها (در صورت نیاز)، و به‌روزرسانی رابط کاربری برای نمایش اجزای ادغام شده است. هدف، اجرای دقیق طرح بدون خلاصه‌سازی یا حذف اطلاعات، و ایجاد یک نسخه عملیاتی از برنامه با ساختار جدید است.
**Key terms:** تلفیق و ادغام کنه, در هم تا اونجایی که ممکنه و شباهت داره, بدون هیچ خلاصه‌سازی، بدون هیچ حذفی

**بخش مربوط از متن کاربر:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی و پیاده‌سازی استراتژی تلفیق اجزای لیست و تسک

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:363-385` — `app.include_router` — این بخش شامل کردن روترهای مختلف را مدیریت می‌کند. با تلفیق `tasks` و `todo_items`، این لیست نیاز به به‌روزرسانی خواهد داشت.
  ```python
  app.include_router(auth.router, tags=["auth"])
  app.include_router(tasks.router)
  app.include_router(projects.router)
  app.include_router(notifications.router, prefix="/notifications", tags=["notifications"])
  # Absolute-path routes (/api/notifications/...) live on a sibling router
  # so they aren't prefixed twice. The status endpoint is the AC for the
  # delivery-tracking subtask.
  app.include_router(notifications.api_router, tags=["notifications"])
  app.include_router(ai.router, tags=["ai"])
  app.include_router(users.router, prefix="/users", tags=["users"])
  # Sibling router for absolute-path users endpoints (/api/users/...).
  ```
- `app/main.py:256-286` — `startup_event` — این بخش نشان‌دهنده نحوه مدیریت مهاجرت ستون‌ها برای `tasks` و `todo_items` در زمان راه‌اندازی است. این الگو برای مهاجرت داده‌ها در صورت تلفیق مدل‌ها بسیار مرتبط است و نشان‌دهنده وجود فیلدهای مشابه مانند `deadline` و `due_date` است.
  ```python
  _task_planning_columns = [
          ("estimated_duration", "INTEGER"),
          ("deadline", "TIMESTAMP WITH TIME ZONE"),
          ("recurrence", "JSON"),
          ("attachment", "VARCHAR(500)"),
      ]
      for col_name, col_type in _task_planning_columns:
          try:
              async with engine.begin() as conn:
                  await conn.execute(
                      text(f"ALTER TABLE tasks ADD COLUMN IF NOT EXISTS {col_name} {col_type}")
                  )
          except Exception as exc:
              logger.debug("skip tasks.%s migration: %s", col_name, exc)
  
      # todo_items: parent_id (subitem hierarchy) and due_date were added
      # by migration 0006. Same ADD COLUMN IF NOT EXISTS pattern so the
      # Render-free-tier startup path (create_all only) gets them too.
      _todo_item_columns = [
          ("parent_id", "INTEGER REFERENCES todo_items(id) ON DELETE CASCADE"),
          ("due_date", "DATE"),
      ]
  ```
- `frontend/src/pages/Tasks.jsx` — `Tasks` — این صفحه فرانت‌اند مسئول نمایش و مدیریت تسک‌ها است و در صورت تلفیق مدل‌های `Task` و `TodoItem` نیاز به بازنگری کامل خواهد داشت.
  ```jsx
  // محتوای فایل در deep_context موجود نیست، اما این فایل صفحهٔ اصلی نمایش تسک‌ها است.
  ```
- `frontend/src/pages/Lists.jsx` — `Lists` — این صفحه فرانت‌اند مسئول نمایش و مدیریت لیست‌های To-Do است و در صورت تلفیق مدل‌های `Project` و `TodoList` یا تغییر نحوه نمایش آیتم‌ها، نیاز به بازنگری خواهد داشت.
  ```jsx
  // محتوای فایل در deep_context موجود نیست، اما این فایل صفحهٔ اصلی نمایش لیست‌ها است.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
fastapi, react, vite. بک‌اند از SQLAlchemy با asyncpg برای ORM و مدیریت پایگاه داده استفاده می‌کند. فرانت‌اند از React با Vite برای ساخت و `react-router-dom` برای مسیریابی استفاده می‌کند. `axios` برای فراخوانی API در فرانت‌اند به کار می‌رود.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/tasks.py` — این روتر API برای مدیریت تسک‌ها است و مستقیماً تحت تأثیر تغییرات مدل `Task` و سرویس‌های آن قرار می‌گیرد.
- `app/routes/todo_items.py` — این روتر API برای مدیریت آیتم‌های To-Do است و مستقیماً تحت تأثیر تغییرات مدل `TodoItem` و سرویس‌های آن قرار می‌گیرد. احتمال ادغام با روتر `tasks` وجود دارد.
- `app/services/planner_service.py` — این سرویس منطق تجاری مربوط به برنامه‌ریزی و تسک‌ها را در خود دارد و باید برای استفاده از مدل‌های یکپارچه بازنویسی شود.
- `app/services/todo_item_service.py` — این سرویس منطق تجاری مربوط به آیتم‌های To-Do را در خود دارد و باید برای استفاده از مدل‌های یکپارچه بازنویسی شود.
- `frontend/src/lib/api.js` — این فایل مسئول برقراری ارتباط با API بک‌اند است. هرگونه تغییر در endpointهای API (مثلاً ادغام `/api/tasks` و `/api/todo-items`) نیاز به به‌روزرسانی در این فایل دارد.

## 🌐 نقشهٔ وابستگی‌ها
این تسک بر روی هستهٔ مدل‌های داده `Task` (`app/models/task.py`) و `TodoItem` (`app/models/todo_item.py`) و همچنین `Project` (`app/models/project.py`) و `TodoList` (`app/models/todo_list.py`) تأثیر می‌گذارد. سرویس‌های `app/services/planner_service.py` و `app/services/todo_item_service.py` که منطق تجاری این مدل‌ها را پیاده‌سازی می‌کنند، باید بازنگری شوند. روترهای `app/routes/tasks.py` و `app/routes/todo_items.py` و `app/routes/projects.py` و `app/routes/lists.py` که این سرویس‌ها را فراخوانی می‌کنند، مستقیماً تحت تأثیر قرار می‌گیرند. در فرانت‌اند، صفحات `frontend/src/pages/Tasks.jsx`, `frontend/src/pages/Lists.jsx`, `frontend/src/pages/ListDetail.jsx`, `frontend/src/pages/Projects.jsx` و همچنین `frontend/src/lib/api.js` که با API ارتباط برقرار می‌کند، نیاز به به‌روزرسانی دارند. تغییرات در `app/main.py` برای شامل کردن روترها و مدیریت مهاجرت داده‌ها نیز ضروری است.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی استراتژی طراحی شده برای تلفیق و ادغام اجزای اصلی برنامه، شامل لیست‌ها، تسک‌ها و صفحات مرتبط را دارد. هدف اصلی این است که «تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.»

این درخواست شامل بازنویسی یا اصلاح کد مربوط به لیست‌ها، تسک‌ها و صفحات، مهاجرت داده‌ها (در صورت نیاز)، و به‌روزرسانی رابط کاربری برای نمایش اجزای ادغام شده است. تأکید بر اجرای دقیق طرح بدون خلاصه‌سازی یا حذف اطلاعات، و ایجاد یک نسخه عملیاتی از برنامه با ساختار جدید و قابل ارتقا است.

بررسی ساختار پروژه نشان می‌دهد که دو موجودیت اصلی برای آیتم‌های کاری وجود دارد: `Task` (در `app/models/task.py`) و `TodoItem` (در `app/models/todo_item.py`). همچنین، دو موجودیت برای گروه‌بندی این آیتم‌ها وجود دارد: `Project` (در `app/models/project.py`) و `TodoList` (در `app/models/todo_list.py`). این ساختار می‌تواند منجر به 'هرج و مرج' و 'صفحات مشابه' شود که کاربر به آن اشاره کرده است. برای مثال، `tasks.due_date` و `todo_items.due_date` هر دو فیلدهای مربوط به زمان‌بندی هستند که در `app/main.py` (خطوط 256-269 و 271-286) به آن‌ها اشاره شده است. همچنین، صفحات فرانت‌اند مانند `frontend/src/pages/Tasks.jsx`، `frontend/src/pages/Projects.jsx`، `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` ممکن است دارای منطق نمایش و تعامل مشابهی باشند که قابلیت تلفیق دارند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل‌های `Task` و `TodoItem` در بک‌اند به یک مدل واحد `WorkItem` (یا یک رابط مشترک) تبدیل شده‌اند که تمام داده‌های موجود را بدون از دست دادن حفظ می‌کند.
- [ ] سرویس‌های بک‌اند (`planner_service`, `todo_item_service`) بازنگری شده‌اند تا از مدل‌های داده یکپارچه استفاده کنند و تکرار منطق کاهش یابد.
- [ ] endpointهای API برای `tasks` و `todo_items` (مثلاً `/api/tasks` و `/api/todo-items`) به endpointهای یکپارچه (مثلاً `/api/work-items`) منتقل شده‌اند و پاسخ‌های API صحیح را برمی‌گردانند.
- [ ] صفحات فرانت‌اند `Tasks.jsx` و `Lists.jsx` (و `ListDetail.jsx`) به‌روزرسانی شده‌اند تا از APIهای جدید استفاده کنند و آیتم‌های کاری را به صورت یکپارچه نمایش دهند.
- [ ] تمام داده‌های موجود از `tasks` و `todo_items` به مدل‌های جدید مهاجرت کرده‌اند و هیچ داده‌ای از دست نرفته است.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی استراتژی تلفیق و ادغام اجزا، مراحل زیر پیشنهاد می‌شود:

1.  **تحلیل و تعریف مدل‌های یکپارچه:**
    *   بررسی دقیق `app/models/task.py` و `app/models/todo_item.py` برای شناسایی فیلدهای مشترک و تفاوت‌های کلیدی. تصمیم‌گیری در مورد اینکه آیا می‌توانند در یک مدل واحد (مثلاً `WorkItem`) با یک فیلد `type` ادغام شوند، یا باید رابط مشترکی داشته باشند.
    *   مشابه آن، بررسی `app/models/project.py` و `app/models/todo_list.py` برای تعریف رابطه یا ادغام آن‌ها به عنوان 'Container' برای WorkItems.

2.  **بازنویسی و اصلاح سرویس‌های بک‌اند:**
    *   بازنگری `app/services/planner_service.py` (برای Taskها) و `app/services/todo_item_service.py` (برای TodoItemها) برای استفاده از مدل‌های یکپارچه یا منطق مشترک، به منظور کاهش تکرار کد.
    *   ایجاد یک سرویس عمومی‌تر (مثلاً `app/services/work_item_service.py`) که عملیات CRUD را برای مدل‌های تلفیق شده مدیریت کند.

3.  **اصلاح مسیرهای API:**
    *   بازنگری `app/routes/tasks.py` و `app/routes/todo_items.py` و در صورت لزوم `app/routes/projects.py` و `app/routes/lists.py` برای استفاده از سرویس‌های جدید و مدل‌های یکپارچه. این ممکن است شامل ادغام یا تغییر مسیرهای موجود باشد (مثلاً `/api/work-items` به جای `/api/tasks` و `/api/todo-items`).
    *   به‌روزرسانی `app/main.py` (خطوط 363-385) برای شامل کردن روترهای جدید و حذف روترهای قدیمی.

4.  **طراحی و اجرای مهاجرت داده‌ها:**
    *   در صورت ادغام مدل‌ها، ایجاد اسکریپت‌های مهاجرت داده با استفاده از Alembic (در `migrations/versions/`) برای انتقال داده‌های موجود از جداول `tasks` و `todo_items` به جدول جدید (یا به‌روزرسانی جداول موجود) بدون از دست دادن اطلاعات. این باید با دقت انجام شود تا 'بدون هیچ خلاصه‌سازی، بدون هیچ حذفی' رعایت شود.
    *   استفاده از الگوی مهاجرت در `app/main.py` (خطوط 195-286) به عنوان الگو برای افزودن ستون‌ها یا تغییر نوع آن‌ها.

5.  **به‌روزرسانی رابط کاربری فرانت‌اند:**
    *   بازنگری `frontend/src/pages/Tasks.jsx`، `frontend/src/pages/Projects.jsx`، `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` برای استفاده از APIهای جدید و نمایش مدل‌های داده یکپارچه.
    *   توسعه کامپوننت‌های UI عمومی‌تر (مثلاً `WorkItemCard.jsx` یا `WorkItemListView.jsx`) که بتوانند انواع مختلف آیتم‌های کاری را نمایش دهند و از منطق تکراری جلوگیری کنند.
    *   به‌روزرسانی `frontend/src/lib/api.js` برای فراخوانی endpointهای جدید.

## 💡 نمونه‌های قبل/بعد
**مثال مفهومی: یکپارچه‌سازی فیلدهای زمان‌بندی در مدل‌ها**

_قبل:_
```
در `app/models/task.py` (مفهومی):
class Task(Base):
    # ... سایر فیلدها
    deadline: Mapped[Optional[datetime]] = mapped_column(TIMESTAMP(timezone=True))

در `app/models/todo_item.py` (مفهومی):
class TodoItem(Base):
    # ... سایر فیلدها
    due_date: Mapped[Optional[date]] = mapped_column(Date)
```

_بعد:_
```
در `app/models/work_item.py` (مدل جدید مفهومی):
class WorkItem(Base):
    # ... سایر فیلدها
    item_type: Mapped[str] = mapped_column(String(50)) # 'task' or 'todo_item'
    scheduled_date: Mapped[Optional[date]] = mapped_column(Date) # فیلد یکپارچه
    # ... سایر فیلدهای مشترک و خاص با استفاده از inheritance یا conditional logic

در `app/main.py` (خطوط 288-297، مهاجرت مفهومی):
# حذف مهاجرت‌های جداگانه برای due_date و deadline و جایگزینی با یک مهاجرت واحد برای scheduled_date در WorkItem
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/`
- `npm run test -- frontend/src/pages/__tests__/Tasks.test.jsx`
- `npm run test -- frontend/src/pages/__tests__/Lists.test.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **از دست دادن داده:** ادغام مدل‌های `Task` و `TodoItem` و `Project` و `TodoList` بدون برنامه‌ریزی دقیق مهاجرت داده می‌تواند منجر به از دست رفتن اطلاعات شود. این ریسک با توجه به تأکید کاربر بر 'بدون هیچ حذفی' بسیار بالاست و نیاز به تست‌های جامع مهاجرت دارد.
2. **شکستن APIهای موجود:** تغییر ساختار مدل‌ها و روترها، APIهای فعلی را که توسط فرانت‌اند و احتمالاً سایر سرویس‌ها استفاده می‌شوند، می‌شکند. این امر نیازمند به‌روزرسانی همزمان بک‌اند و فرانت‌اند است.
3. **پیچیدگی منطق تجاری:** ادغام موجودیت‌ها ممکن است منجر به پیچیدگی در منطق تجاری شود، به خصوص اگر تفاوت‌های ظریفی بین `Task` و `TodoItem` وجود داشته باشد که در مدل یکپارچه به درستی مدیریت نشود.
4. **تأثیر بر عملکرد:** تغییرات گسترده در مدل‌های داده و کوئری‌ها می‌تواند بر عملکرد پایگاه داده تأثیر بگذارد و نیاز به بهینه‌سازی کوئری‌ها دارد.
5. **مشکلات فرانت‌اند:** بازنویسی صفحات و کامپوننت‌های فرانت‌اند برای پشتیبانی از مدل‌های جدید، ریسک ایجاد باگ‌های UI/UX را به همراه دارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 5: بازسازی ساختار برنامه و زیرساخت برای قابلیت ارتقا

**Scope:** این مرحله فراتر از صرفاً ادغام اجزا است و بر روی بازسازی کلی ساختار برنامه و زیرساخت آن تمرکز دارد تا 'قابل ارتقا' شود. این شامل اعمال الگوهای طراحی مناسب، ماژولار کردن کد، بهبود معماری کلی سیستم، و اطمینان از اینکه زیرساخت فعلی یا جدید، از رشد و تغییرات آینده پشتیبانی می‌کند. هدف، ایجاد یک پایه محکم برای توسعه‌های آتی و جلوگیری از بازگشت به وضعیت هرج و مرج است.
**Key terms:** ساختار برنامه و زیرساختش چه جوری باشه, قابل ارتقا بشه

**بخش مربوط از متن کاربر:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

## 🎯 هدف (خلاصه ساختاریافته)
بازسازی مدل و API تسک‌ها و آیتم‌های To-Do

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/task.py:6-19` — `Task` — این فایل مدل `Task` را تعریف می‌کند که باید با `TodoItem` ادغام شود. این فایل پس از ادغام حذف یا به `work_item.py` منتقل خواهد شد.
  ```python
  class Task(Base):
      __tablename__ = "tasks"
      id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
      title: Mapped[str] = mapped_column(String, index=True)
      description: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
      status: Mapped[str] = mapped_column(String, default="pending")
      priority: Mapped[str] = mapped_column(String, default="normal")
      user_id: Mapped[Optional[int]] = mapped_column(Integer, ForeignKey("users.id"), nullable=True)
      created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
      updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
      due_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
      estimated_duration: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)
      deadline: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)
      recurrence: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True)
      attachment: Mapped[Optional[str]] = mapped_column(String(500), nullable=True)
  ```
- `app/models/todo_item.py:6-17` — `TodoItem` — این فایل مدل `TodoItem` را تعریف می‌کند که باید با `Task` ادغام شود. این فایل پس از ادغام حذف یا به `work_item.py` منتقل خواهد شد.
  ```python
  class TodoItem(Base):
      __tablename__ = "todo_items"
      id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
      title: Mapped[str] = mapped_column(String, index=True)
      description: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
      completed: Mapped[bool] = mapped_column(Boolean, default=False)
      created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
      updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
      todo_list_id: Mapped[int] = mapped_column(Integer, ForeignKey("todo_lists.id", ondelete="CASCADE"))
      parent_id: Mapped[Optional[int]] = mapped_column(Integer, ForeignKey("todo_items.id", ondelete="CASCADE"), nullable=True)
      due_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
  ```
- `app/main.py:363-385` — `app` — این بخش شامل روترهای `tasks` و `todo_items` است که باید با یک روتر `work_items` جایگزین شوند. همچنین، نحوه `include_router` باید برای همه روترها استاندارد شود.
  ```python
  app.include_router(auth.router, tags=["auth"])
  app.include_router(tasks.router)
  app.include_router(projects.router)
  app.include_router(notifications.router, prefix="/notifications", tags=["notifications"])
  # Absolute-path routes (/api/notifications/...) live on a sibling router
  # so they aren't prefixed twice. The status endpoint is the AC for the
  # delivery-tracking subtask.
  app.include_router(notifications.api_router, tags=["notifications"])
  app.include_router(ai.router, tags=["ai"])
  app.include_router(users.router, prefix="/users", tags=["users"])
  # Sibling router for absolute-path users endpoints (/api/users/...).
  app.include_router(users.api_router, tags=["users"])
  app.include_router(integrations.router, prefix="/integrations", tags=["integrations"])
  # Todo-list system: both routers use absolute `/api/...` decorators
  # (so they aren't double-prefixed) and ship the full CRUD + the
  # share / unshare / move / toggle actions.
  app.include_router(lists.router)
  app.include_router(todo_items.router)
  # webhook.router decorators carry the absolute path (/webhook, /webhook/health)
  # so it mounts with no prefix to avoid double-prefixing.
  app.include_router(webhook.router, tags=["webhook"])
  # planner router decorators also use absolute /api/planner paths.
  app.include_router(planner.router, tags=["planner"])
  ```
- `frontend/src/pages/Tasks.jsx` — `Tasks` — این صفحه مسئول نمایش و مدیریت تسک‌ها است. پس از ادغام مدل‌ها، باید برای کار با API جدید `WorkItem` بازسازی شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, react, vite. کتابخانه‌های مرتبط: SQLAlchemy (برای ORM و مهاجرت‌های پایگاه داده), Pydantic (برای تعریف شمای داده), React Router (برای مسیریابی در فرانت‌اند), Axios (برای فراخوانی‌های API در فرانت‌اند).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/tasks.py` — این روتر APIهای مربوط به تسک‌ها را تعریف می‌کند و باید با روتر جدید `work_items` جایگزین شود.
- `app/routes/todo_items.py` — این روتر APIهای مربوط به آیتم‌های To-Do را تعریف می‌کند و باید با روتر جدید `work_items` جایگزین شود.
- `app/services/planner_service.py` — این سرویس احتمالاً با مدل `Task` تعامل دارد و باید برای استفاده از مدل `WorkItem` به‌روزرسانی شود.
- `app/services/todo_item_service.py` — این سرویس منطق مربوط به `TodoItem` را مدیریت می‌کند و باید در سرویس جدید `work_item_service` ادغام شود.
- `app/schemas/task_schema.py` — این فایل شمای Pydantic برای `Task` را تعریف می‌کند و باید با شمای یکپارچه `work_item_schema.py` جایگزین شود.
- `app/schemas/todo_item_schema.py` — این فایل شمای Pydantic برای `TodoItem` را تعریف می‌کند و باید با شمای یکپارچه `work_item_schema.py` جایگزین شود.
- `frontend/src/pages/Lists.jsx` — این صفحه لیست‌های To-Do را نمایش می‌دهد که شامل `TodoItem`ها هستند. باید برای کار با مدل `WorkItem` به‌روزرسانی شود.
- `frontend/src/lib/api.js` — این فایل مسئول برقراری ارتباط با API بک‌اند است و باید برای فراخوانی endpointهای جدید `work_items` به‌روزرسانی شود.
- `migrations/versions/` — برای اعمال تغییرات شمای پایگاه داده (ایجاد جدول `work_items` و انتقال داده‌ها)، اسکریپت‌های مهاجرت جدیدی لازم است.

## 🌐 نقشهٔ وابستگی‌ها
این بازسازی ساختار، بر لایه‌های اصلی بک‌اند و فرانت‌اند تأثیر می‌گذارد. در بک‌اند، `app/main.py` (خطوط 363-385) که روترهای `tasks` و `todo_items` را import و include می‌کند، نقطه ورود اصلی تغییر است. `app/database.py` برای عملیات ORM و مهاجرت‌ها حیاتی است. `app/models/__init__.py` که مدل‌ها را import می‌کند، باید به‌روزرسانی شود. سرویس‌های `app/services/list_service.py` (که `TodoLists` را مدیریت می‌کند) و `app/services/todo_item_service.py` به‌شدت به هم وابسته هستند و منطق آن‌ها باید در سرویس جدید `work_item_service.py` ادغام شود. در فرانت‌اند، `frontend/src/lib/api.js` نقطه مرکزی برای تمام فراخوانی‌های API است و توسط صفحات `frontend/src/pages/Tasks.jsx`، `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` استفاده می‌شود. این صفحات و همچنین `frontend/src/context/TaskContext.jsx` نیاز به به‌روزرسانی‌های گسترده‌ای برای تطبیق با ساختار داده و API یکپارچه `WorkItem` دارند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست «بازسازی ساختار برنامه و زیرساخت برای قابلیت ارتقا» را دارد تا از «هرج و مرج فعلی» نجات یابد و یک پایه محکم برای توسعه‌های آتی ایجاد شود. این بازسازی شامل «تلفیق و ادغام» «لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه» و همچنین رسیدگی به «صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست» است. هدف نهایی، ایجاد ساختاری است که «قابل ارتقا بشه» و از تکرار و پیچیدگی‌های غیرضروری جلوگیری کند.

بررسی ساختار پروژه نشان می‌دهد که دو مفهوم بسیار مشابه، یعنی `Task` (در `app/models/task.py`) و `TodoItem` (در `app/models/todo_item.py`)، به‌صورت جداگانه مدیریت می‌شوند. هر دو دارای فیلدهایی مانند `title`, `description`, `due_date` هستند. `Task` فیلدهای برنامه‌ریزی دقیق‌تری مانند `estimated_duration`, `deadline`, `recurrence` دارد، در حالی که `TodoItem` به `todo_list_id` و `parent_id` برای سلسله‌مراتب لیست‌ها وابسته است. این شباهت‌ها و تفاوت‌های جزئی، فرصتی برای ادغام و ایجاد یک مدل واحد (مثلاً `WorkItem`) با استفاده از یک فیلد `type` (مانند 'task' یا 'todo_item') و فیلدهای nullable یا شرطی برای ویژگی‌های خاص هر نوع را فراهم می‌کند.

این جدایی فعلی به «هرج و مرج» اشاره‌شده توسط کاربر دامن می‌زند، زیرا منجر به تکرار کد در لایه‌های مدل، شمای Pydantic، سرویس‌ها (`app/services/planner_service.py`, `app/services/todo_item_service.py`) و روترها (`app/routes/tasks.py`, `app/routes/todo_items.py`) می‌شود. همچنین، در فرانت‌اند، صفحات `frontend/src/pages/Tasks.jsx` و `frontend/src/pages/Lists.jsx`/`frontend/src/pages/ListDetail.jsx` ممکن است منطق نمایش و مدیریت آیتم‌های مشابه را تکرار کنند. ادغام این دو مفهوم، معماری را ساده‌تر، ماژولارتر و قابل ارتقاتر خواهد کرد.

علاوه بر این، در `app/main.py` (خطوط 363-385)، نحوه `include_router` برای روترهای مختلف کمی ناسازگار است؛ برخی از `prefix` استفاده می‌کنند و برخی دیگر به مسیرهای مطلق در دکوراتورهای روتر متکی هستند. استانداردسازی این رویکرد نیز به کاهش «هرج و مرج» و بهبود خوانایی و نگهداری کد کمک می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک مدل جدید SQLAlchemy به نام `WorkItem` در `app/models/work_item.py` تعریف شده باشد که فیلدهای `title`, `description`, `due_date` و یک فیلد `type` (با مقادیر 'task' و 'todo_item') را شامل شود.
- [ ] یک اسکریپت مهاجرت Alembic جدید با موفقیت داده‌های موجود از جداول `tasks` و `todo_items` را به جدول `work_items` منتقل کرده و جداول قدیمی را حذف کند.
- [ ] یک روتر FastAPI واحد (مثلاً `app/routes/work_items.py`) برای مدیریت CRUD عملیات `WorkItem`ها در دسترس باشد و endpointهای `/api/tasks` و `/api/todo_items` حذف شده باشند.
- [ ] صفحه `frontend/src/pages/Tasks.jsx` به‌درستی تسک‌ها را از API جدید `/api/work_items` (با فیلتر `type='task'`) دریافت و نمایش دهد و امکان ایجاد/ویرایش/حذف تسک‌ها را فراهم کند.
- [ ] صفحات `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` به‌درستی آیتم‌های To-Do را از API جدید `/api/work_items` (با فیلتر `type='todo_item'`) دریافت و نمایش دهند و امکان ایجاد/ویرایش/حذف آیتم‌ها را فراهم کنند.
- [ ] تمام روترهای شامل شده در `app/main.py` (خطوط 363-385) مسیرهای کامل خود را در دکوراتورهای `@router` تعریف کرده باشند و `app.include_router` بدون آرگومان `prefix` استفاده شود (مگر در موارد خاص).
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای بازسازی ساختار و افزایش قابلیت ارتقا، اقدامات زیر پیشنهاد می‌شود:

1.  **ادغام مدل‌های `Task` و `TodoItem` در بک‌اند:**
    *   یک مدل جدید SQLAlchemy به نام `WorkItem` (مثلاً در `app/models/work_item.py`) ایجاد شود که تمام فیلدهای مشترک و خاص `Task` و `TodoItem` را در بر گیرد. یک فیلد `type` (با مقادیر 'task' و 'todo_item') برای تمایز بین آن‌ها اضافه شود. فیلدهای خاص هر نوع (مانند `recurrence` برای task و `todo_list_id` برای todo_item) باید nullable باشند.
    *   یک اسکریپت مهاجرت Alembic جدید ایجاد شود تا:
        *   جدول `work_items` را بسازد.
        *   داده‌های موجود از جداول `tasks` و `todo_items` را به جدول `work_items` منتقل کند و فیلد `type` را به‌درستی تنظیم کند.
        *   جداول `tasks` و `todo_items` قدیمی را حذف کند.
    *   شمای Pydantic در `app/schemas/task_schema.py` و `app/schemas/todo_item_schema.py` به یک شمای واحد `app/schemas/work_item_schema.py` تبدیل شود که مدل `WorkItem` را پشتیبانی کند.
    *   سرویس‌های `app/services/todo_item_service.py` و بخش‌های مرتبط از `app/services/planner_service.py` در یک سرویس جدید `app/services/work_item_service.py` ادغام شوند که با مدل `WorkItem` کار کند.
    *   روترهای `app/routes/tasks.py` و `app/routes/todo_items.py` در یک روتر واحد `app/routes/work_items.py` ادغام شوند که API یکپارچه‌ای (مثلاً `/api/work_items`) برای مدیریت هر دو نوع آیتم ارائه دهد.

2.  **استانداردسازی `include_router` در `app/main.py`:**
    *   خطوط مربوط به `app.include_router(tasks.router)` و `app.include_router(todo_items.router)` حذف شده و با `app.include_router(work_items.router)` جایگزین شود.
    *   بررسی شود که تمام روترهای موجود، مسیرهای کامل خود را در دکوراتورهای `@router` خود تعریف کرده باشند و `app.include_router` در `app/main.py` بدون آرگومان `prefix` استفاده شود (مگر در موارد خاص که یک روتر فرعی نیاز به پیشوند داشته باشد).

3.  **تطبیق فرانت‌اند با API جدید:**
    *   فایل `frontend/src/lib/api.js` برای استفاده از endpointهای جدید `/api/work_items` به‌روزرسانی شود.
    *   کامپوننت‌ها و صفحات `frontend/src/pages/Tasks.jsx`، `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` بازسازی شوند تا داده‌ها را از API یکپارچه `WorkItem` دریافت کرده و بر اساس فیلد `type`، نمایش و منطق مناسب را اعمال کنند. کامپوننت‌های قابل استفاده مجدد (مانند `WorkItemCard` یا `WorkItemForm`) در `frontend/src/components/` ایجاد شوند.

## 💡 نمونه‌های قبل/بعد
**ادغام روترها در app/main.py**

_قبل:_
```
app.include_router(tasks.router)
app.include_router(projects.router)
# ...
app.include_router(lists.router)
app.include_router(todo_items.router)
```

_بعد:_
```
from app.routes import work_items # New import
# ...
app.include_router(projects.router)
# ...
app.include_router(lists.router)
app.include_router(work_items.router) # Unified router
```

**فراخوانی API در frontend/src/lib/api.js (مثال مفهومی)**

_قبل:_
```
export const fetchTasks = async () => {
  const response = await api.get('/api/tasks');
  return response.data;
};

export const fetchTodoItems = async (listId) => {
  const response = await api.get(`/api/todo-lists/${listId}/items`);
  return response.data;
};
```

_بعد:_
```
export const fetchWorkItems = async (params = {}) => {
  // params could include { type: 'task' } or { type: 'todo_item', todo_list_id: 123 }
  const response = await api.get('/api/work_items', { params });
  return response.data;
};

export const createWorkItem = async (workItemData) => {
  const response = await api.post('/api/work_items', workItemData);
  return response.data;
};
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/`
- `npm run test -- frontend/src/pages/__tests__/Tasks.test.jsx`
- `npm run test -- frontend/src/pages/__tests__/Lists.test.jsx`
- `npm run build`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **از دست رفتن/خرابی داده‌ها در مهاجرت:** ادغام جداول `tasks` و `todo_items` در یک جدول `work_items` جدید یک عملیات حیاتی پایگاه داده است. اسکریپت‌های مهاجرت نادرست می‌توانند منجر به از دست رفتن داده‌ها یا وضعیت‌های ناسازگار شوند. این امر بر `app/database.py` و کل لایه داده تأثیر می‌گذارد.
2. **پیچیدگی ادغام منطق کسب‌وکار:** `Task` و `TodoItem` ممکن است تفاوت‌های ظریفی در منطق کسب‌وکار داشته باشند (مثلاً نحوه مدیریت `recurrence` برای تسک‌ها در مقابل تکمیل ساده برای آیتم‌های To-Do). ادغام سرویس‌های آن‌ها (`app/services/planner_service.py`, `app/services/todo_item_service.py`) نیاز به تحلیل دقیق دارد تا اطمینان حاصل شود هیچ عملکردی از بین نمی‌رود یا به‌اشتباه در `app/services/work_item_service.py` پیاده‌سازی نمی‌شود.
3. **پیچیدگی بازسازی فرانت‌اند:** فرانت‌اند (`frontend/src/pages/Tasks.jsx`, `frontend/src/pages/Lists.jsx`, `frontend/src/pages/ListDetail.jsx`, `frontend/src/lib/api.js`, `frontend/src/context/TaskContext.jsx`) به‌شدت به ساختار API فعلی وابسته است. بازسازی این کامپوننت‌ها برای مدیریت مدل یکپارچه `WorkItem` گسترده خواهد بود و در صورت عدم تست کامل، مستعد رگرسیون‌های UI/UX است.
4. **تأثیر بر ویژگی‌های AI/Planner:** روترهای `app/routes/ai.py` و `app/routes/planner.py`، همراه با سرویس‌های `app/services/ai_service.py` و `app/services/planner_service.py`، احتمالاً با اشیاء `Task` تعامل دارند. تغییرات در مدل `Task` مستقیماً بر این ویژگی‌های برنامه‌ریزی با کمک AI تأثیر می‌گذارد و نیاز به به‌روزرسانی و اعتبارسنجی مجدد منطق آن‌ها دارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 6: تأیید نهایی و ممیزی کاهش هرج و مرج

**Scope:** این مرحله شامل یک ممیزی نهایی و جامع برای تأیید اینکه تمامی اهداف پروژه، به ویژه 'نجات پیدا کردن از هرج و مرج فعلی' و 'قابل ارتقا شدن' برنامه، محقق شده‌اند. این ممیزی شامل تست‌های جامع (عملکردی، یکپارچگی، رگرسیون)، بررسی کد برای اطمینان از رعایت استانداردهای جدید، و جمع‌آوری بازخورد از کاربران و ذینفعان برای تأیید بهبود تجربه کاربری و کاهش پیچیدگی است.
**Key terms:** از این هرج و مرج نجات پیدا بکنه, از این هرج و مرج فعلی در بیاد, قابل ارتقا بشه

**بخش مربوط از متن کاربر:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

## 🎯 هدف (خلاصه ساختاریافته)
بررسی و تلفیق مدل‌های Task و TodoItem برای کاهش هرج و مرج و ارتقاپذیری

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:255-269` — `_task_planning_columns` — این اسکریپت مهاجرت در زمان راه‌اندازی، ستون‌های مربوط به برنامه‌ریزی وظایف را به جدول `tasks` اضافه می‌کند. این نشان‌دهنده تکامل مدل `Task` است که می‌تواند در کنار `TodoItem` منبع هرج و مرج باشد.
  ```python
  _task_planning_columns = [
          ("estimated_duration", "INTEGER"),
          ("deadline", "TIMESTAMP WITH TIME ZONE"),
          ("recurrence", "JSON"),
          ("attachment", "VARCHAR(500)"),
      ]
  ```
- `app/main.py:271-286` — `_todo_item_columns` — این اسکریپت مهاجرت در زمان راه‌اندازی، ستون‌های مربوط به سلسله‌مراتب و تاریخ سررسید را به جدول `todo_items` اضافه می‌کند. این نیز نشان‌دهنده تکامل مدل `TodoItem` است که موازی با `Task` پیش رفته است.
  ```python
  _todo_item_columns = [
          ("parent_id", "INTEGER REFERENCES todo_items(id) ON DELETE CASCADE"),
          ("due_date", "DATE"),
      ]
      for col_name, col_type in _todo_item_columns:
          try:
              async with engine.begin() as conn:
                  await conn.execute(
                      text(f"ALTER TABLE todo_items ADD COLUMN IF NOT EXISTS {col_name} {col_type}")
                  )
          except Exception as exc:
              logger.debug("skip todo_items.%s migration: %s", col_name, exc)
  ```
- `app/routes/tasks.py` — `router` — این فایل مسیرهای API برای مدیریت `Task`ها را تعریف می‌کند. محتوای دقیق آن در deep_context ارائه نشده است، اما وجود آن نشان‌دهنده یک موجودیت جداگانه برای وظایف است.
- `app/routes/todo_items.py` — `router` — این فایل مسیرهای API برای مدیریت `TodoItem`ها را تعریف می‌کند. محتوای دقیق آن در deep_context ارائه نشده است، اما وجود آن نشان‌دهنده یک موجودیت جداگانه برای آیتم‌های لیست است.
- `frontend/src/pages/Tasks.jsx` — `Tasks` — این صفحه فرانت‌اند مسئول نمایش و مدیریت وظایف است. محتوای دقیق آن در deep_context ارائه نشده است، اما احتمالاً با APIهای `app/routes/tasks.py` تعامل دارد و می‌تواند با صفحات `TodoItem` همپوشانی داشته باشد.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. کتابخانه‌های مرتبط شامل SQLAlchemy (برای ORM و تعامل با پایگاه داده)، Pydantic (برای تعریف schemaهای داده و اعتبارسنجی)، و React Router (برای مسیریابی در فرانت‌اند) هستند. تغییرات پیشنهادی نیازمند درک عمیق از نحوه کار این فریم‌ورک‌ها و کتابخانه‌ها با یکدیگر است.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/task.py` — تعریف مدل `Task` که نیاز به بررسی برای تلفیق دارد.
- `app/models/todo_item.py` — تعریف مدل `TodoItem` که نیاز به بررسی برای تلفیق دارد.
- `app/services/planner_service.py` — این سرویس منطق تجاری مربوط به `Task`ها را مدیریت می‌کند و در صورت تلفیق مدل‌ها، نیاز به بازنگری دارد.
- `app/services/todo_item_service.py` — این سرویس منطق تجاری مربوط به `TodoItem`ها را مدیریت می‌کند و در صورت تلفیق مدل‌ها، نیاز به بازنگری دارد.
- `frontend/src/pages/ListDetail.jsx` — این صفحه فرانت‌اند آیتم‌های یک `TodoList` را نمایش می‌دهد که شامل `TodoItem`ها هستند و می‌تواند با `Tasks.jsx` همپوشانی داشته باشد.
- `frontend/src/pages/Lists.jsx` — این صفحه فرانت‌اند لیست‌های Todo را نمایش می‌دهد که شامل `TodoItem`ها هستند و می‌تواند در زمینه تلفیق مورد بازبینی قرار گیرد.

## 🌐 نقشهٔ وابستگی‌ها
این تسک بر روی هستهٔ مدل‌سازی داده و منطق تجاری برنامه Lifemanager تأثیر می‌گذارد. فایل `app/main.py` مسئول اجرای مهاجرت‌های پایگاه داده در زمان راه‌اندازی است که شامل تغییرات schema برای `tasks` و `todo_items` می‌شود. این نشان‌دهنده وابستگی عمیق به ساختار این مدل‌ها است. مسیرهای API در `app/routes/tasks.py` و `app/routes/todo_items.py` به ترتیب به مدل‌های `Task` و `TodoItem` و سرویس‌های مربوطه مانند `app/services/planner_service.py` و `app/services/todo_item_service.py` وابسته هستند. هرگونه تغییر در مدل‌های داده، مستقیماً بر این سرویس‌ها و مسیرهای API تأثیر می‌گذارد. در لایه فرانت‌اند، صفحات `frontend/src/pages/Tasks.jsx`، `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` به این APIها وابسته هستند و منطق نمایش و تعامل با کاربر را بر اساس ساختار داده‌های فعلی پیاده‌سازی می‌کنند. بنابراین، تلفیق این مدل‌ها نیازمند یک رویکرد جامع از لایه پایگاه داده تا رابط کاربری است.

## 🔍 Context و وضعیت فعلی
کاربر درخواست یک «تأیید نهایی و ممیزی کاهش هرج و مرج» را دارد تا اطمینان حاصل شود که اهداف اصلی پروژه، به‌ویژه «نجات پیدا کردن از هرج و مرج فعلی» و «قابل ارتقا شدن» برنامه، محقق شده‌اند. این ممیزی شامل «تست‌های جامع (عملکردی، یکپارچگی، رگرسیون)»، «بررسی کد برای اطمینان از رعایت استانداردهای جدید»، و «جمع‌آوری بازخورد از کاربران و ذینفعان برای تأیید بهبود تجربه کاربری و کاهش پیچیدگی» است.

کاربر به طور خاص اشاره می‌کند: «می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.»

این درخواست به وضوح به نیاز به بازنگری معماری و کدبیس برای کاهش پیچیدگی و افزایش قابلیت نگهداری و توسعه اشاره دارد. یکی از نقاط کلیدی که می‌تواند منبع «هرج و مرج فعلی» باشد، وجود دو مفهوم مشابه اما جداگانه برای مدیریت کارها: `Task` و `TodoItem` است. این دو مدل در `app/models/task.py` و `app/models/todo_item.py` تعریف شده‌اند و هر کدام مسیرهای API و منطق سرویس‌دهی جداگانه دارند (`app/routes/tasks.py` و `app/routes/todo_items.py`). حتی در `app/main.py`، ما شاهد اسکریپت‌های مهاجرت جداگانه برای افزودن ستون‌ها به جداول `tasks` (خطوط 255-269) و `todo_items` (خطوط 271-286) هستیم که نشان‌دهنده تکامل مستقل این دو موجودیت است. این تکامل موازی می‌تواند به «مشابهت‌هایی» منجر شود که کاربر به آن اشاره کرده و نیاز به «تلفیق و ادغام» دارد. همچنین، صفحات فرانت‌اند مانند `frontend/src/pages/Tasks.jsx` و `frontend/src/pages/ListDetail.jsx` (که آیتم‌های Todo را نمایش می‌دهد) ممکن است الگوهای UI و منطق بازیابی داده مشابهی داشته باشند که می‌توانند ادغام شوند تا از «صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده» جلوگیری شود و برنامه «قابل ارتقا بشه».

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک سند معماری (مثلاً در `docs/`) ایجاد شود که تصمیمات مربوط به تلفیق یا تمایز `Task` و `TodoItem` را مستند کند و دلیل آن را توضیح دهد.
- [ ] تعداد مسیرهای API تکراری بین `app/routes/tasks.py` و `app/routes/todo_items.py` حداقل ۵۰٪ کاهش یابد یا به وضوح متمایز شوند.
- [ ] تمام تست‌های موجود در `tests/test_tasks.py` و `tests/test_todo_lists.py` پس از اعمال تغییرات با موفقیت اجرا شوند.
- [ ] صفحات `frontend/src/pages/Tasks.jsx` و `frontend/src/pages/ListDetail.jsx` بدون از دست دادن قابلیت‌ها، از منطق و کامپوننت‌های مشترک بیشتری استفاده کنند و تجربه کاربری یکپارچه‌تری ارائه دهند.
- [ ] هیچ داده‌ای از `tasks` یا `todo_items` در طول فرآیند تلفیق یا بازطراحی از بین نرود یا خلاصه نشود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پاسخ به درخواست کاربر مبنی بر «نجات پیدا کردن از هرج و مرج فعلی» و «قابل ارتقا شدن» برنامه از طریق «تلفیق و ادغام» موارد مشابه، پیشنهاد می‌شود یک فاز تحلیل و بازطراحی برای مدل‌های `Task` و `TodoItem` و زیرساخت‌های مرتبط با آن‌ها انجام شود. این فاز شامل مراحل زیر است:

1.  **تحلیل جامع مدل‌ها و سرویس‌ها:** بررسی دقیق `app/models/task.py` و `app/models/todo_item.py` برای شناسایی ویژگی‌ها، روابط و رفتارهای مشترک و متمایز. همچنین، تحلیل `app/services/planner_service.py` و `app/services/todo_item_service.py` برای درک منطق تجاری مربوط به هر کدام.
2.  **بررسی مسیرهای API:** ارزیابی `app/routes/tasks.py` و `app/routes/todo_items.py` برای یافتن الگوهای تکراری در تعریف endpointها و پاسخ‌ها.
3.  **تحلیل صفحات فرانت‌اند:** بررسی `frontend/src/pages/Tasks.jsx`، `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` برای شناسایی کامپوننت‌ها، منطق نمایش و تعاملات کاربری که می‌توانند با یک مدل داده یکپارچه، ساده‌سازی یا ادغام شوند.
4.  **طراحی مدل داده یکپارچه (در صورت امکان):** در صورت وجود همپوشانی قابل توجه، پیشنهاد یک مدل داده واحد یا یک سلسله مراتب واضح‌تر برای مدیریت وظایف و آیتم‌های لیست، با در نظر گرفتن نیاز به حفظ داده‌های موجود «بدون هیچ خلاصه‌سازی، بدون هیچ حذفی».
5.  **بازطراحی سرویس‌ها و APIها:** پیاده‌سازی منطق تجاری و مسیرهای API جدید بر اساس مدل داده یکپارچه، با هدف کاهش تکرار و افزایش انسجام.
6.  **به‌روزرسانی فرانت‌اند:** بازنویسی یا بازسازی کامپوننت‌های فرانت‌اند برای استفاده از APIهای جدید و نمایش یکپارچه‌تر اطلاعات.
7.  **افزودن تست‌های رگرسیون و یکپارچگی:** توسعه تست‌های جدید برای اطمینان از عملکرد صحیح سیستم پس از تغییرات و جلوگیری از معرفی باگ‌های جدید.

## 💡 نمونه‌های قبل/بعد
**قبل: مهاجرت‌های جداگانه برای Task و TodoItem در app/main.py**

_قبل:_
```
_task_planning_columns = [
        ("estimated_duration", "INTEGER"),
        ("deadline", "TIMESTAMP WITH TIME ZONE"),
        ("recurrence", "JSON"),
        ("attachment", "VARCHAR(500)"),
    ]
    for col_name, col_type in _task_planning_columns:
        try:
            async with engine.begin() as conn:
                await conn.execute(
                    text(f"ALTER TABLE tasks ADD COLUMN IF NOT EXISTS {col_name} {col_type}")
                )
        except Exception as exc:
            logger.debug("skip tasks.%s migration: %s", col_name, exc)

    # todo_items: parent_id (subitem hierarchy) and due_date were added
    # by migration 0006. Same ADD COLUMN IF NOT EXISTS pattern so the
    # Render-free-tier startup path (create_all only) gets them too.
    _todo_item_columns = [
        ("parent_id", "INTEGER REFERENCES todo_items(id) ON DELETE CASCADE"),
        ("due_date", "DATE"),
    ]
    for col_name, col_type in _todo_item_columns:
        try:
            async with engine.begin() as conn:
                await conn.execute(
                    text(f"ALTER TABLE todo_items ADD COLUMN IF NOT EXISTS {col_name} {col_type}")
                )
        except Exception as exc:
            logger.debug("skip todo_items.%s migration: %s", col_name, exc)
```

_بعد:_
```
# Conceptual: A unified migration approach for a consolidated 'WorkItem' model.
    # This would involve a single set of columns and a single ALTER TABLE statement
    # if Task and TodoItem were merged into a common entity, or a more explicit
    # and documented separation if they remain distinct but with clear boundaries.
    # Example (simplified, actual implementation would be complex):
    # _unified_work_item_columns = [
    #     ("due_date", "DATE"),
    #     ("parent_id", "INTEGER REFERENCES work_items(id) ON DELETE CASCADE"),
    #     ("item_type", "VARCHAR(32) NOT NULL"), # e.g., 'task' or 'todo'
    #     ("estimated_duration", "INTEGER"),
    #     ("deadline", "TIMESTAMP WITH TIME ZONE"),
    #     ("recurrence", "JSON"),
    #     ("attachment", "VARCHAR(500)"),
    # ]
    # for col_name, col_type in _unified_work_item_columns:
    #     try:
    #         async with engine.begin() as conn:
    #             await conn.execute(
    #                 text(f"ALTER TABLE work_items ADD COLUMN IF NOT EXISTS {col_name} {col_type}")
    #             )
    #     except Exception as exc:
    #         logger.debug("skip work_items.%s migration: %s", col_name, exc)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/`
- `npm run test -- frontend/src/pages/__tests__/Tasks.test.jsx`
- `npm run test -- frontend/src/pages/__tests__/Lists.test.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **ریسک از دست دادن داده:** تلفیق مدل‌های `Task` و `TodoItem` بدون دقت کافی می‌تواند منجر به از دست رفتن داده‌های موجود شود، به خصوص با توجه به تأکید کاربر بر «بدون هیچ خلاصه‌سازی، بدون هیچ حذفی». این امر نیازمند استراتژی‌های مهاجرت داده قوی و تست‌های جامع است.
2. **ریسک شکستن APIهای موجود:** تغییر در مدل‌های داده و سرویس‌ها می‌تواند APIهای فعلی را که توسط فرانت‌اند و احتمالاً سایر سرویس‌ها استفاده می‌شوند، بشکند. این امر نیازمند مدیریت نسخه‌بندی API یا یکپارچه‌سازی دقیق است.
3. **افزایش پیچیدگی اولیه:** تلاش برای یکپارچه‌سازی ممکن است در ابتدا پیچیدگی را افزایش دهد، اگر مرزهای جدید به وضوح تعریف نشوند یا مدل یکپارچه به اندازه کافی انعطاف‌پذیر نباشد.
4. **تأثیر بر فرانت‌اند:** تغییرات در ساختار داده‌های بک‌اند و APIها مستقیماً بر کامپوننت‌های فرانت‌اند در `frontend/src/pages/Tasks.jsx`، `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` تأثیر می‌گذارد و نیاز به بازنویسی قابل توجهی دارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: high
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': "یک سند خروجی (JSON یا Markdown) حاوی فهرست تمامی endpointهای FastAPI مرتبط با 'tasks', 'projects', 'lists', 'todo_items' و 'auth_google' (به عنوان نمونه) با ذکر مسیر، متد HTTP و فایل منبع تولید شود.", 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['@router.(get|post|put|patch|delete)', 'app.include_router'], 'files_hint': ['app/routes/tasks.py', 'app/routes/projects.py', 'app/routes/lists.py', 'app/routes/todo_items.py', 'app/routes/auth_google.py', 'app/main.py']}}
- [ ] {'text': "سند خروجی شامل فهرست تمامی مدل\u200cهای SQLAlchemy مرتبط با 'Task', 'Project', 'TodoList', 'TodoItem' و 'OAuthUser' (به عنوان نمونه) با ذکر نام مدل، فیلدهای اصلی و فایل منبع باشد.", 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class (Task|Project|TodoList|TodoItem|OAuthUser)\\(Base', 'Column\\('], 'files_hint': ['app/models/task.py', 'app/models/project.py', 'app/models/todo_list.py', 'app/models/todo_item.py', 'app/models/user_oauth.py']}}
- [ ] {'text': 'سند خروجی شامل فهرست تمامی فایل\u200cهای `.jsx` در `frontend/src/pages/` به عنوان صفحات فرانت\u200cاند با ذکر نام صفحه و مسیر فایل باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['import React from', 'export default function'], 'files_hint': ['frontend/src/pages/Dashboard.jsx', 'frontend/src/pages/Home.jsx', 'frontend/src/pages/ListDetail.jsx', 'frontend/src/pages/Lists.jsx', 'frontend/src/pages/Login.jsx', 'frontend/src/pages/Notifications.jsx', 'frontend/src/pages/Projects.jsx', 'frontend/src/pages/Register.jsx', 'frontend/src/pages/Tasks.jsx']}}
- [ ] {'text': "سند خروجی به طور خاص 'Inspector Bridge Script' را که در `frontend/index.html` (خطوط 12-574) قرار دارد، به عنوان یک اسکریپت تزریق\u200cشده و غیرمرتبط با منطق اصلی برنامه شناسایی و توصیف کند.", 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['Inspector Bridge Script', 'window.__inspectorBridgeLoaded'], 'files_hint': ['frontend/index.html']}}
- [ ] {'text': 'سند خروجی باید صراحتاً بیان کند که هیچ خلاصه\u200cسازی یا حذفی در اطلاعات جمع\u200cآوری شده صورت نگرفته است.', 'verify_method': 'manual_only', 'verify_plan': {'reason': 'این معیار نیازمند بررسی انسانی محتوای سند خروجی برای اطمینان از عدم خلاصه\u200cسازی است.'}}
- [ ] {'text': 'یک لیست مستند شده از تمام مدل\u200cها (`app/models/*.py`)، سرویس\u200cها (`app/services/*.py`) و مسیرهای API (`app/routes/*.py`) بک\u200cاند به همراه مسئولیت\u200cهای اصلی آنها تهیه شود.', 'verify_method': 'manual_only', 'verify_plan': {'reason': 'نیاز به بررسی دستی مستندات/گزارش تولید شده دارد.'}}
- [ ] {'text': 'یک لیست مستند شده از تمام صفحات مهم (`frontend/src/pages/*.jsx`) و کامپوننت\u200cها (`frontend/src/components/*.jsx`) فرانت\u200cاند به همراه مسئولیت\u200cهای اصلی و وابستگی\u200cهای API آنها تهیه شود.', 'verify_method': 'manual_only', 'verify_plan': {'reason': 'نیاز به بررسی دستی مستندات/گزارش تولید شده دارد.'}}
- [ ] {'text': 'لیستی از مدل\u200cها، سرویس\u200cها، مسیرهای API یا کامپوننت\u200cها/صفحات فرانت\u200cاند که به عنوان مشابه، تکراری یا غیرضروری شناسایی شده\u200cاند، به همراه دلیل هر مورد، ارائه شود. این باید شامل تحلیل `_API_PREFIXES` در `app/main.py` و تأثیر آن بر مسیریابی فرانت\u200cاند باشد.', 'verify_method': 'manual_only', 'verify_plan': {'reason': 'نیاز به بررسی دستی گزارش تحلیل برای دقت و کامل بودن دارد.'}}
- [ ] {'text': 'یک طرح سطح بالا برای چگونگی رفع تکرارهای شناسایی شده، شامل تغییرات معماری احتمالی یا تلاش\u200cهای ادغام، پیشنهاد شود.', 'verify_method': 'manual_only', 'verify_plan': {'reason': 'نیاز به بررسی دستی استراتژی پیشنهادی برای امکان\u200cسنجی و همسویی با اهداف پروژه دارد.'}}
- [ ] {'text': 'یک بخش خاص در گزارش تحلیل که جزئیات شباهت\u200cها و تفاوت\u200cهای بین مدل\u200cهای `app/models/task.py` و `app/models/todo_item.py` را بررسی کرده و پیشنهاد دهد که آیا باید یکپارچه شوند یا جداگانه نگهداری شوند.', 'verify_method': 'manual_only', 'verify_plan': {'reason': 'نیاز به بررسی دستی مقایسه مدل خاص در گزارش دارد.'}}
- [ ] {'text': 'مدل `WorkItem` با تمام فیلدهای `Task` و `TodoItem` ایجاد شود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class WorkItem(Base):', 'title = Column(String', 'project_id = Column(Integer', 'todo_list_id = Column(Integer', 'parent_id = Column(Integer', 'item_type = Column(String)'], 'files_hint': ['app/models/work_item.py']}}
- [ ] {'text': 'Migration Alembic برای انتقال داده\u200cهای `tasks` و `todo_items` به `work_items` ایجاد و اجرا شود.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/test_migration.py::test_work_item_migration', 'marker': 'integration'}}
- [ ] {'text': 'API Endpoints جدید `/api/work_items` برای CRUD مدل `WorkItem` پیاده\u200cسازی شوند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/api/work_items', 'expected_status': 201, 'required_fields': ['id', 'title', 'item_type']}}
- [ ] {'text': 'روترهای `app/routes/tasks.py` و `app/routes/todo_items.py` حذف یا به روتر `work_items` هدایت شوند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['app.include_router(work_items.router)', 'app.include_router(tasks.router)', 'app.include_router(todo_items.router)'], 'files_hint': ['app/main.py']}}
- [ ] {'text': 'صفحه `frontend/src/pages/Tasks.jsx` برای نمایش و مدیریت `WorkItem`ها بازطراحی شود.', 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/tasks'}, {'action': 'assert_visible', 'selector': "[data-testid='unified-work-item-list']"}]}}
- [ ] {'text': 'هیچ داده\u200cای از `Task`ها یا `TodoItem`های موجود در فرآیند ادغام از بین نرود.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/test_data_integrity.py::test_all_data_preserved_after_migration', 'marker': 'critical'}}
- [ ] {'text': 'مدل\u200cهای `Task` و `TodoItem` در بک\u200cاند به یک مدل واحد `WorkItem` (یا یک رابط مشترک) تبدیل شده\u200cاند که تمام داده\u200cهای موجود را بدون از دست دادن حفظ می\u200cکند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class WorkItem', 'item_type: Mapped', 'scheduled_date: Mapped'], 'files_hint': ['app/models/work_item.py', 'app/models/task.py', 'app/models/todo_item.py']}}
- [ ] {'text': 'سرویس\u200cهای بک\u200cاند (`planner_service`, `todo_item_service`) بازنگری شده\u200cاند تا از مدل\u200cهای داده یکپارچه استفاده کنند و تکرار منطق کاهش یابد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['WorkItemService', 'get_work_item', 'create_work_item'], 'files_hint': ['app/services/planner_service.py', 'app/services/todo_item_service.py', 'app/services/work_item_service.py']}}
- [ ] {'text': 'endpointهای API برای `tasks` و `todo_items` (مثلاً `/api/tasks` و `/api/todo-items`) به endpointهای یکپارچه (مثلاً `/api/work-items`) منتقل شده\u200cاند و پاسخ\u200cهای API صحیح را برمی\u200cگردانند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/work-items', 'expected_status': 200, 'required_fields': ['id', 'title', 'item_type', 'scheduled_date']}}
- [ ] {'text': 'صفحات فرانت\u200cاند `Tasks.jsx` و `Lists.jsx` (و `ListDetail.jsx`) به\u200cروزرسانی شده\u200cاند تا از APIهای جدید استفاده کنند و آیتم\u200cهای کاری را به صورت یکپارچه نمایش دهند.', 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/dashboard'}, {'action': 'wait_for', 'selector': "[data-testid='work-item-list']"}, {'action': 'assert_visible', 'selector': "[data-testid='work-item-card']"}]}}

## Acceptance Criteria

1. یک سند خروجی (JSON یا Markdown) حاوی فهرست تمامی endpointهای FastAPI مرتبط با 'tasks', 'projects', 'lists', 'todo_items' و 'auth_google' (به عنوان نمونه) با ذکر مسیر، متد HTTP و فایل منبع تولید شود. _(verify: static)_
2. سند خروجی شامل فهرست تمامی مدل‌های SQLAlchemy مرتبط با 'Task', 'Project', 'TodoList', 'TodoItem' و 'OAuthUser' (به عنوان نمونه) با ذکر نام مدل، فیلدهای اصلی و فایل منبع باشد. _(verify: static)_
3. سند خروجی شامل فهرست تمامی فایل‌های `.jsx` در `frontend/src/pages/` به عنوان صفحات فرانت‌اند با ذکر نام صفحه و مسیر فایل باشد. _(verify: static)_
4. سند خروجی به طور خاص 'Inspector Bridge Script' را که در `frontend/index.html` (خطوط 12-574) قرار دارد، به عنوان یک اسکریپت تزریق‌شده و غیرمرتبط با منطق اصلی برنامه شناسایی و توصیف کند. _(verify: static)_
5. سند خروجی باید صراحتاً بیان کند که هیچ خلاصه‌سازی یا حذفی در اطلاعات جمع‌آوری شده صورت نگرفته است. _(verify: manual_only)_
6. یک لیست مستند شده از تمام مدل‌ها (`app/models/*.py`)، سرویس‌ها (`app/services/*.py`) و مسیرهای API (`app/routes/*.py`) بک‌اند به همراه مسئولیت‌های اصلی آنها تهیه شود. _(verify: manual_only)_
7. یک لیست مستند شده از تمام صفحات مهم (`frontend/src/pages/*.jsx`) و کامپوننت‌ها (`frontend/src/components/*.jsx`) فرانت‌اند به همراه مسئولیت‌های اصلی و وابستگی‌های API آنها تهیه شود. _(verify: manual_only)_
8. لیستی از مدل‌ها، سرویس‌ها، مسیرهای API یا کامپوننت‌ها/صفحات فرانت‌اند که به عنوان مشابه، تکراری یا غیرضروری شناسایی شده‌اند، به همراه دلیل هر مورد، ارائه شود. این باید شامل تحلیل `_API_PREFIXES` در `app/main.py` و تأثیر آن بر مسیریابی فرانت‌اند باشد. _(verify: manual_only)_
9. یک طرح سطح بالا برای چگونگی رفع تکرارهای شناسایی شده، شامل تغییرات معماری احتمالی یا تلاش‌های ادغام، پیشنهاد شود. _(verify: manual_only)_
10. یک بخش خاص در گزارش تحلیل که جزئیات شباهت‌ها و تفاوت‌های بین مدل‌های `app/models/task.py` و `app/models/todo_item.py` را بررسی کرده و پیشنهاد دهد که آیا باید یکپارچه شوند یا جداگانه نگهداری شوند. _(verify: manual_only)_
11. مدل `WorkItem` با تمام فیلدهای `Task` و `TodoItem` ایجاد شود. _(verify: static)_
12. Migration Alembic برای انتقال داده‌های `tasks` و `todo_items` به `work_items` ایجاد و اجرا شود. _(verify: backend_test)_
13. API Endpoints جدید `/api/work_items` برای CRUD مدل `WorkItem` پیاده‌سازی شوند. _(verify: api_response)_
14. روترهای `app/routes/tasks.py` و `app/routes/todo_items.py` حذف یا به روتر `work_items` هدایت شوند. _(verify: static)_
15. صفحه `frontend/src/pages/Tasks.jsx` برای نمایش و مدیریت `WorkItem`ها بازطراحی شود. _(verify: ui_interaction)_
16. هیچ داده‌ای از `Task`ها یا `TodoItem`های موجود در فرآیند ادغام از بین نرود. _(verify: backend_test)_
17. مدل‌های `Task` و `TodoItem` در بک‌اند به یک مدل واحد `WorkItem` (یا یک رابط مشترک) تبدیل شده‌اند که تمام داده‌های موجود را بدون از دست دادن حفظ می‌کند. _(verify: static)_
18. سرویس‌های بک‌اند (`planner_service`, `todo_item_service`) بازنگری شده‌اند تا از مدل‌های داده یکپارچه استفاده کنند و تکرار منطق کاهش یابد. _(verify: static)_
19. endpointهای API برای `tasks` و `todo_items` (مثلاً `/api/tasks` و `/api/todo-items`) به endpointهای یکپارچه (مثلاً `/api/work-items`) منتقل شده‌اند و پاسخ‌های API صحیح را برمی‌گردانند. _(verify: api_response)_
20. صفحات فرانت‌اند `Tasks.jsx` و `Lists.jsx` (و `ListDetail.jsx`) به‌روزرسانی شده‌اند تا از APIهای جدید استفاده کنند و آیتم‌های کاری را به صورت یکپارچه نمایش دهند. _(verify: ui_interaction)_
21. تمام داده‌های موجود از `tasks` و `todo_items` به مدل‌های جدید مهاجرت کرده‌اند و هیچ داده‌ای از دست نرفته است. _(verify: backend_test)_
22. یک مدل جدید SQLAlchemy به نام `WorkItem` در `app/models/work_item.py` تعریف شده باشد که فیلدهای `title`, `description`, `due_date` و یک فیلد `type` (با مقادیر 'task' و 'todo_item') را شامل شود. _(verify: static)_
23. یک اسکریپت مهاجرت Alembic جدید با موفقیت داده‌های موجود از جداول `tasks` و `todo_items` را به جدول `work_items` منتقل کرده و جداول قدیمی را حذف کند. _(verify: backend_test)_
24. یک روتر FastAPI واحد (مثلاً `app/routes/work_items.py`) برای مدیریت CRUD عملیات `WorkItem`ها در دسترس باشد و endpointهای `/api/tasks` و `/api/todo_items` حذف شده باشند. _(verify: api_response)_
25. صفحه `frontend/src/pages/Tasks.jsx` به‌درستی تسک‌ها را از API جدید `/api/work_items` (با فیلتر `type='task'`) دریافت و نمایش دهد و امکان ایجاد/ویرایش/حذف تسک‌ها را فراهم کند. _(verify: ui_interaction)_
26. صفحات `frontend/src/pages/Lists.jsx` و `frontend/src/pages/ListDetail.jsx` به‌درستی آیتم‌های To-Do را از API جدید `/api/work_items` (با فیلتر `type='todo_item'`) دریافت و نمایش دهند و امکان ایجاد/ویرایش/حذف آیتم‌ها را فراهم کنند. _(verify: ui_interaction)_
27. تمام روترهای شامل شده در `app/main.py` (خطوط 363-385) مسیرهای کامل خود را در دکوراتورهای `@router` تعریف کرده باشند و `app.include_router` بدون آرگومان `prefix` استفاده شود (مگر در موارد خاص). _(verify: static)_
28. یک سند معماری (مثلاً در `docs/`) ایجاد شود که تصمیمات مربوط به تلفیق یا تمایز `Task` و `TodoItem` را مستند کند و دلیل آن را توضیح دهد. _(verify: static)_
29. تعداد مسیرهای API تکراری بین `app/routes/tasks.py` و `app/routes/todo_items.py` حداقل ۵۰٪ کاهش یابد یا به وضوح متمایز شوند. _(verify: api_response)_
30. تمام تست‌های موجود در `tests/test_tasks.py` و `tests/test_todo_lists.py` پس از اعمال تغییرات با موفقیت اجرا شوند. _(verify: backend_test)_
31. صفحات `frontend/src/pages/Tasks.jsx` و `frontend/src/pages/ListDetail.jsx` بدون از دست دادن قابلیت‌ها، از منطق و کامپوننت‌های مشترک بیشتری استفاده کنند و تجربه کاربری یکپارچه‌تری ارائه دهند. _(verify: ui_interaction)_
32. هیچ داده‌ای از `tasks` یا `todo_items` در طول فرآیند تلفیق یا بازطراحی از بین نرود یا خلاصه نشود. _(verify: backend_test)_

## Task Steps

### Step 1: بررسی و فهرست‌برداری از لیست‌ها، تسک‌ها و صفحات موجود
**Status:** `pending` (0%)
**Scope:** این مرحله شامل یک بررسی جامع و دقیق از تمامی لیست‌ها، تسک‌ها و صفحات فعلی موجود در برنامه است. هدف، شناسایی و فهرست‌برداری کامل از هر یک از این اجزا بدون هیچ‌گونه خلاصه‌سازی یا حذف است. این مرحله صرفاً به جمع‌آوری اطلاعات و ایجاد یک موجودی کامل از وضعیت فعلی برنامه می‌پردازد و شامل هیچ‌گونه تحلیل شباهت یا تصمیم‌گیری برای ادغام نیست.
**Excerpt:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

### Step 2: تحلیل شباهت‌ها و شناسایی موارد تکراری یا بی‌دلیل
**Status:** `pending` (0%)
**Scope:** در این مرحله، موجودی ایجاد شده از لیست‌ها، تسک‌ها و صفحات مورد تحلیل دقیق قرار می‌گیرد تا موارد دارای شباهت، تکراری، یا صفحاتی که 'بی‌دلیل' ساخته شده‌اند و تأثیری ندارند، شناسایی شوند. این تحلیل باید بر اساس معیارهای مشخصی برای شباهت (مثلاً عملکرد مشابه، داده‌های یکسان، UI مشابه) انجام شود. خروجی این مرحله، لیستی از موارد کاندید برای تلفیق و ادغام است.
**Excerpt:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

### Step 3: طراحی استراتژی تلفیق و ادغام (بدون خلاصه‌سازی/حذف)
**Status:** `pending` (0%)
**Scope:** بر اساس تحلیل شباهت‌ها، یک استراتژی دقیق و جامع برای تلفیق و ادغام لیست‌ها، تسک‌ها و صفحات شناسایی شده طراحی می‌شود. این استراتژی باید تضمین کند که هیچ اطلاعات یا عملکرد حیاتی 'بدون هیچ خلاصه‌سازی، بدون هیچ حذفی' از بین نرود. شامل طراحی مدل‌های داده جدید، جریان‌های کاری بهینه شده، و طرح‌های UI/UX برای اجزای ادغام شده است. این مرحله بر روی 'چگونه' ادغام تمرکز دارد.
**Excerpt:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

### Step 4: پیاده‌سازی تلفیق و ادغام اجزا
**Status:** `pending` (0%)
**Scope:** در این مرحله، استراتژی طراحی شده برای تلفیق و ادغام به صورت عملی پیاده‌سازی می‌شود. این شامل بازنویسی یا اصلاح کد مربوط به لیست‌ها، تسک‌ها و صفحات، مهاجرت داده‌ها (در صورت نیاز)، و به‌روزرسانی رابط کاربری برای نمایش اجزای ادغام شده است. هدف، اجرای دقیق طرح بدون خلاصه‌سازی یا حذف اطلاعات، و ایجاد یک نسخه عملیاتی از برنامه با ساختار جدید است.
**Excerpt:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

### Step 5: بازسازی ساختار برنامه و زیرساخت برای قابلیت ارتقا
**Status:** `pending` (0%)
**Scope:** این مرحله فراتر از صرفاً ادغام اجزا است و بر روی بازسازی کلی ساختار برنامه و زیرساخت آن تمرکز دارد تا 'قابل ارتقا' شود. این شامل اعمال الگوهای طراحی مناسب، ماژولار کردن کد، بهبود معماری کلی سیستم، و اطمینان از اینکه زیرساخت فعلی یا جدید، از رشد و تغییرات آینده پشتیبانی می‌کند. هدف، ایجاد یک پایه محکم برای توسعه‌های آتی و جلوگیری از بازگشت به وضعیت هرج و مرج است.
**Excerpt:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```

### Step 6: تأیید نهایی و ممیزی کاهش هرج و مرج
**Status:** `pending` (0%)
**Scope:** این مرحله شامل یک ممیزی نهایی و جامع برای تأیید اینکه تمامی اهداف پروژه، به ویژه 'نجات پیدا کردن از هرج و مرج فعلی' و 'قابل ارتقا شدن' برنامه، محقق شده‌اند. این ممیزی شامل تست‌های جامع (عملکردی، یکپارچگی، رگرسیون)، بررسی کد برای اطمینان از رعایت استانداردهای جدید، و جمع‌آوری بازخورد از کاربران و ذینفعان برای تأیید بهبود تجربه کاربری و کاهش پیچیدگی است.
**Excerpt:**
```
می‌خوام پلان پدیکی که می‌نویسی جوری باشه که بتونه موقع اجرا تمام اون لیست‌ها و تسک‌ها و این چیزایی که تا الان ساخته شده و ممکنه مشابهت‌هایی داشته باشه و به شکل یا صفحات حتی همون چیزایی که الان تو برنامه هست رو به شکل خیلی دقیق بدون هیچ خلاصه‌سازی، بدون هیچ حذفی این‌ها رو در هم تا اونجایی که ممکنه و شباهت داره و باعث می‌شه از این هرج و مرج نجات پیدا بکنه یا صفحاتی که اصلاً بی‌دلیل همون اول ساخته شده ولی الان تأثیری نداره یا صفحه مشابهی براش هست، این‌ها رو تلفیق و ادغام کنه و درست چیز کنه که از الان ساختار برنامه و زیرساختش چه جوری باشه که بتونه قابل ارتقا بشه و از این هرج و مرج فعلی در بیاد.
```
