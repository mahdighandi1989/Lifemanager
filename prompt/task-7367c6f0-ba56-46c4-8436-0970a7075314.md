---
task_id: 7367c6f0-ba56-46c4-8436-0970a7075314
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: applied_externally_pending_verify
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T21:18:45.683523+00:00'
updated_at: '2026-05-28T17:49:40.239880+00:00'
target_files:
- app/routes/auth_google.py
- app/main.py
- app/models/user_oauth.py
- app/services/local_file_service.py
- app/routes/integrations.py
- app/config.py
- app/services/google_auth.py
- app/routes/local_files.py
- app/services/media_text_extractor.py
- app/models/local_file_entry.py
- frontend/src/pages/Lists.jsx
- frontend/src/pages/ListDetail.jsx
- app/services/ai/image_service.py
- app/services/ai/nlp_service.py
- app/routes/ai.py
- backend/app/services/integration_service.py
- app/services/integration_service.py
- app/tasks.py
- backend/scripts/migrate_fts5_to_es.py
- frontend/src/pages/Dashboard.jsx
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_103432_AgADrx0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 6,000 char متن_

## درخواست اصلی و هدف کاربر از فایل صوتی  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه و تمام داده‌های اصل داده‌ها اونجا ذخیره بشه چون الان پایگاه داده‌م توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه و بقیه موارد منتقل بشه به گوگل درایو و قابل فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده‌ای که توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و اینها ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین. و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه. همه داده‌ها توی فولدر گوگل درایو هم منتقل بشه مثلاً صوت‌هایی که یه وقت می‌فرستم عکسا فلان چیزا اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه.

## شرح ایده کسب‌وکار مطرح شده  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و
[00:06] از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه
[00:12] و تمام داده‌های اصلی داده‌ها اونجا ذخیره بشه. چون الان پایگاه داده‌ام توی رندر دارم ولی اون یک گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه
[00:23] و بقیه موارد
[00:27] منتقل بشه به گوگل درایو و قابل
[00:30] فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه سرعت دسترسی بیشتر باشه و اینها ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین.
[00:44] و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه.
[00:55] همه داده‌ها توی فولدر
[00:59] گوگل درایو هم
[01:00] منتقل بشه. مثلاً صوت‌هایی که یه وقت می‌فرستم، عکسا، فلان چیزا، اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره
[01:12] و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و
[01:22] قابل دسترسی باشه خلاصه.

## مولفه‌های کلیدی مورد نیاز برای استخراج  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و
[00:06] از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه
[00:12] و تمام داده‌های اصلی، داده‌ها اونجا ذخیره بشه.
[00:16] چون الان پایگاه داده‌ام توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه
[00:23] و بقیه موارد
[00:27] منتقل بشه به گوگل درایو و قابل
[00:30] فراخوانی و دیدن و اینا هم باشه البته.
[00:32] حالا اون پایگاه داده توی رندر نمیدونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و اینها
[00:38] ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین.
[00:44] و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً
[00:48] اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه.
[00:55] همه داده‌ها توی
[00:58] فولدر گوگل درایو هم
[01:00] منتقل بشه. مثلاً صوت‌هایی که یه وقت می‌فرستم، عکسا، فلان چیزا،
[01:04] اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
[01:07] و اون فایل‌های چیز اگر دیگه ضرورتی نداره
[01:12] و تو پایگاه داده اصلی توی رندر هم نمیتونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم
[01:19] فراخوانیش کنم و ببینمش و
[01:22] قابل دسترسی باشه خلاصه.

## درخواست‌های تکمیلی و پیشنهادات مورد انتظار  _(at: audio/ogg)_

### درخواست‌های تکمیلی و پیشنهادات مورد انتظار

[00:00] گوینده ۱: می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و
[00:05] گوینده ۱: از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه
[00:11] گوینده ۱: و تمام داده‌های اصلی، داده‌ها اونجا ذخیره بشه. چون الان پایگاه داده‌م توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه
[00:22] گوینده ۱: و بقیه موارد
[00:26] گوینده ۱: منتقل بشه به گوگل درایو و قابل
[00:30] گوینده ۱: فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و این‌ها
[00:38] گوینده ۱: ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین.
[00:44] گوینده ۱: و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه.
[00:55] گوینده ۱: همه داده‌ها توی فولدر گوگل درایو هم
[01:00] گوینده ۱: منتقل بشه. مثلاً صوت‌هایی که یه وقت می‌فرستم، عکسا، فلان چیزا، اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
[01:07] گوینده ۱: و اون فایل‌های چیز اگر دیگه ضرورتی نداره
[01:12] گوینده ۱: و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید به منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم
[01:19] گوینده ۱: فراخوانیش کنم و ببینمش و
[01:22] گوینده ۱: قابل دسترسی باشه خلاصه.

## فرمت خروجی مطلوب کاربر  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه و تمام داده‌های اصلی داده‌ها اونجا ذخیره بشه چون الان پایگاه داده‌ام توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه و بقیه موارد منتقل بشه به گوگل درایو و قابل فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده‌ای که توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و اینها ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین.
[00:43] و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه. همه داده‌ها توی فولدر گوگل درایو هم منتقل بشه مثلاً صوت‌هایی که یه وقت می‌فرستم، عکسا، فلان چیزا، اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_103432_AgADrx0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 6,000 char متن_

## درخواست اصلی و هدف کاربر از فایل صوتی  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه و تمام داده‌های اصل داده‌ها اونجا ذخیره بشه چون الان پایگاه داده‌م توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه و بقیه موارد منتقل بشه به گوگل درایو و قابل فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده‌ای که توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و اینها ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین. و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه. همه داده‌ها توی فولدر گوگل درایو هم منتقل بشه مثلاً صوت‌هایی که یه وقت می‌فرستم عکسا فلان چیزا اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه.

## شرح ایده کسب‌وکار مطرح شده  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و
[00:06] از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه
[00:12] و تمام داده‌های اصلی داده‌ها اونجا ذخیره بشه. چون الان پایگاه داده‌ام توی رندر دارم ولی اون یک گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه
[00:23] و بقیه موارد
[00:27] منتقل بشه به گوگل درایو و قابل
[00:30] فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه سرعت دسترسی بیشتر باشه و اینها ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین.
[00:44] و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه.
[00:55] همه داده‌ها توی فولدر
[00:59] گوگل درایو هم
[01:00] منتقل بشه. مثلاً صوت‌هایی که یه وقت می‌فرستم، عکسا، فلان چیزا، اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره
[01:12] و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و
[01:22] قابل دسترسی باشه خلاصه.

## مولفه‌های کلیدی مورد نیاز برای استخراج  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و
[00:06] از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه
[00:12] و تمام داده‌های اصلی، داده‌ها اونجا ذخیره بشه.
[00:16] چون الان پایگاه داده‌ام توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه
[00:23] و بقیه موارد
[00:27] منتقل بشه به گوگل درایو و قابل
[00:30] فراخوانی و دیدن و اینا هم باشه البته.
[00:32] حالا اون پایگاه داده توی رندر نمیدونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و اینها
[00:38] ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین.
[00:44] و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً
[00:48] اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه.
[00:55] همه داده‌ها توی
[00:58] فولدر گوگل درایو هم
[01:00] منتقل بشه. مثلاً صوت‌هایی که یه وقت می‌فرستم، عکسا، فلان چیزا،
[01:04] اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
[01:07] و اون فایل‌های چیز اگر دیگه ضرورتی نداره
[01:12] و تو پایگاه داده اصلی توی رندر هم نمیتونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم
[01:19] فراخوانیش کنم و ببینمش و
[01:22] قابل دسترسی باشه خلاصه.

## درخواست‌های تکمیلی و پیشنهادات مورد انتظار  _(at: audio/ogg)_

### درخواست‌های تکمیلی و پیشنهادات مورد انتظار

[00:00] گوینده ۱: می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و
[00:05] گوینده ۱: از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه
[00:11] گوینده ۱: و تمام داده‌های اصلی، داده‌ها اونجا ذخیره بشه. چون الان پایگاه داده‌م توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه
[00:22] گوینده ۱: و بقیه موارد
[00:26] گوینده ۱: منتقل بشه به گوگل درایو و قابل
[00:30] گوینده ۱: فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و این‌ها
[00:38] گوینده ۱: ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین.
[00:44] گوینده ۱: و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه.
[00:55] گوینده ۱: همه داده‌ها توی فولدر گوگل درایو هم
[01:00] گوینده ۱: منتقل بشه. مثلاً صوت‌هایی که یه وقت می‌فرستم، عکسا، فلان چیزا، اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
[01:07] گوینده ۱: و اون فایل‌های چیز اگر دیگه ضرورتی نداره
[01:12] گوینده ۱: و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید به منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم
[01:19] گوینده ۱: فراخوانیش کنم و ببینمش و
[01:22] گوینده ۱: قابل دسترسی باشه خلاصه.

## فرمت خروجی مطلوب کاربر  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه و تمام داده‌های اصلی داده‌ها اونجا ذخیره بشه چون الان پایگاه داده‌ام توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه و بقیه موارد منتقل بشه به گوگل درایو و قابل فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده‌ای که توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و اینها ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین.
[00:43] و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه. همه داده‌ها توی فولدر گوگل درایو هم منتقل بشه مثلاً صوت‌هایی که یه وقت می‌فرستم، عکسا، فلان چیزا، اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه.
```

## 📋 چک‌لیست مراحل (20 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد ساختار فولدر اصلی در Google Drive و تعریف Google Sheet مرکزی** — این مرحله شامل ایجاد یک فولدر اصلی (root folder) در Google Drive برای ذخیره تمام داده‌های غیرضروری و فایل‌های رسانه‌ای است. همچنین یک Google Sheet به عنوان شیت مرکزی (master sheet) برای ثبت و رهگیری تمام داده‌ها و فایل‌های منتقل‌شده ایجاد می‌شود. این مرحله فقط به ایجاد ساختار اولیه می‌پردازد و شامل 
- [ ] **مرحله 2: پیاده‌سازی احراز هویت Google Drive و Google Sheets API در برنامه** — این مرحله شامل تنظیم و پیاده‌سازی OAuth 2.0 برای دسترسی به Google Drive و Google Sheets APIs است. باید از Google Cloud Console یک پروژه ایجاد شود، credentials (Client ID و Client Secret) دریافت شود، و کتابخانه‌های مربوطه (google-api-python-client, google-auth-oauthlib) نصب شوند. نکته حیاتی: توکن احر
- [ ] **مرحله 3: طراحی و پیاده‌سازی مدل داده برای رهگیری فایل‌ها و داده‌های منتقل‌شده در Google Sheet** — این مرحله شامل طراحی ساختار (schema) Google Sheet مرکزی است. ستون‌های مورد نیاز عبارتند از: ID (شناسه یکتا), FileName (نام فایل), FileType (نوع فایل: صوتی، تصویری، متنی), GoogleDriveLink (لینک فایل در Google Drive), OriginalDBLocation (محل اصلی در پایگاه داده Render), TransferDate (تاریخ انتقال), St
- [ ] **مرحله 4: پیاده‌سازی منطق آپلود فایل به Google Drive و ثبت در Google Sheet** — این مرحله شامل نوشتن تابعی است که یک فایل (صوتی، تصویری، یا متنی) را از مسیر محلی یا URL دریافت کرده، آن را به فولدر اصلی Google Drive آپلود می‌کند، لینک اشتراک‌گذاری (shareable link) تولید می‌کند، و سپس یک ردیف جدید در Google Sheet با اطلاعات فایل ثبت می‌کند. نکته حیاتی: فایل‌ها باید با permission 
- [ ] **مرحله 5: پیاده‌سازی منطق استخراج متن از فایل‌های صوتی و تصویری (OCR/ASR)** — این مرحله شامل یکپارچه‌سازی یک سرویس تبدیل گفتار به متن (Speech-to-Text) مانند Google Cloud Speech-to-Text یا Whisper برای فایل‌های صوتی، و یک سرویس تشخیص متن (OCR) مانند Google Cloud Vision یا Tesseract برای تصاویر است. متن استخراج‌شده باید در یک فایل متنی (.txt) ذخیره شده و همراه با فایل اصلی به G
- [ ] **مرحله 6: طراحی و پیاده‌سازی منطق تصمیم‌گیری برای انتقال داده‌ها (Data Tiering Logic)** — این مرحله شامل پیاده‌سازی یک سیستم رتبه‌بندی (tiering system) برای داده‌ها است. داده‌ها بر اساس معیارهایی مانند: تعداد دفعات دسترسی (access frequency), حجم داده, و重要性 (criticality) به سه دسته تقسیم می‌شوند: Hot (در Render بماند), Warm (در Render بماند اما با اولویت کمتر), Cold (به Google Drive منتقل
- [ ] **مرحله 7: ایجاد API endpoint برای فراخوانی و مشاهده فایل‌های منتقل‌شده به Google Drive از داخل برنامه** — این مرحله شامل ایجاد یک API endpoint (مثلاً GET /api/files/{file_id}) است که با دریافت شناسه فایل از Google Sheet، لینک Google Drive را بازیابی کرده و فایل را به صورت مستقیم (stream) یا از طریق redirect به کاربر نمایش می‌دهد. همچنین یک endpoint لیست (GET /api/files) برای نمایش تمام فایل‌های منتقل‌شد
- [ ] **مرحله 8: طراحی و پیاده‌سازی رابط کاربری (UI) برای مدیریت و مشاهده داده‌های منتقل‌شده** — این مرحله شامل ایجاد صفحات frontend برای نمایش لیست فایل‌های منتقل‌شده (با قابلیت جستجو، فیلتر بر اساس نوع فایل و تاریخ)، نمایش جزئیات هر فایل (نام، نوع، تاریخ انتقال، لینک)، و دکمه‌های عملیات (مشاهده، دانلود، حذف از Google Drive). همچنین یک بخش برای نمایش وضعیت انتقال‌های خودکار (آخرین زمان اجرا، ت
- [ ] **مرحله 9: پیاده‌سازی قابلیت جستجوی پیشرفته در داده‌های منتقل‌شده (شامل متن استخراج‌شده)** — این مرحله شامل افزودن قابلیت جستجوی full-text بر روی متن‌های استخراج‌شده از فایل‌های صوتی و تصویری است. متن‌های استخراج‌شده (که در فایل‌های .txt در Google Drive ذخیره شده‌اند) باید index شده و قابل جستجو باشند. می‌توان از Elasticsearch یا یک راه‌حل ساده‌تر مانند SQLite FTS5 برای این کار استفاده کرد.
- [ ] **مرحله 10: نوشتن تست‌های واحد (Unit Tests) برای منطق انتقال داده و API endpoints** — این مرحله شامل نوشتن تست‌های واحد برای ماژول‌های کلیدی است: تابع آپلود به Google Drive (با mock کردن API calls), تابع استخراج متن (با mock کردن سرویس‌های OCR/ASR), منطق تصمیم‌گیری انتقال (data tiering logic), و API endpoints (با استفاده از TestClient). حداقل ۸۰% پوشش کد (code coverage) برای این ماژو
- [ ] **مرحله 11: نوشتن تست‌های یکپارچه‌سازی (Integration Tests) با سرویس‌های واقعی Google** — این مرحله شامل نوشتن تست‌هایی است که با سرویس‌های واقعی Google Drive و Sheets ارتباط برقرار می‌کنند (با استفاده از یک حساب تستی). این تست‌ها عملیات واقعی آپلود، خواندن، و حذف فایل‌ها را انجام می‌دهند. یک فولدر تستی جداگانه در Google Drive برای این منظور ایجاد می‌شود که پس از اتمام تست‌ها پاک می‌شود.
- [ ] **مرحله 12: پیاده‌سازی مکانیزم fallback و error handling برای خطاهای Google API** — این مرحله شامل پیاده‌سازی یک سیستم مدیریت خطا برای زمان‌هایی که Google API در دسترس نیست یا با محدودیت نرخ (rate limit) مواجه می‌شود. باید از تکنیک‌هایی مانند retry with exponential backoff, circuit breaker, و fallback به ذخیره‌سازی محلی (local storage) استفاده شود. همچنین یک صف (queue) برای عملیات‌
- [ ] **مرحله 13: بهینه‌سازی عملکرد: پیاده‌سازی کش (Caching) برای لینک‌های Google Drive** — این مرحله شامل پیاده‌سازی یک لایه کش (cache) برای لینک‌های Google Drive است تا از فراخوانی مکرر API برای فایل‌های پراستفاده جلوگیری شود. می‌توان از Redis (اگر در دسترس است) یا یک کش درون‌حافظه‌ای (in-memory cache) با TTL (Time-To-Live) استفاده کرد. نکته حیاتی: کش باید به صورت خودکار invalidate شود ا
- [ ] **مرحله 14: افزودن قابلیت آرشیو خودکار فایل‌های قدیمی از Google Drive به Google Cloud Storage (Nearline/Coldline)** — این مرحله شامل پیاده‌سازی یک سیاست چرخه حیات (lifecycle policy) برای فایل‌های Google Drive است. فایل‌هایی که بیش از ۶ ماه از آخرین دسترسی به آن‌ها می‌گذرد، به طور خودکار به Google Cloud Storage با کلاس Nearline یا Coldline منتقل می‌شوند (برای کاهش هزینه‌ها). یک رکورد از انتقال در Google Sheet با وضع
- [ ] **مرحله 15: پیاده‌سازی قابلیت جستجوی پیشرفته با استفاده از Elasticsearch برای متن‌های استخراج‌شده** — این مرحله شامل جایگزینی SQLite FTS5 با Elasticsearch برای جستجوی full-text قدرتمندتر است. Elasticsearch باید روی یک سرویس ابری (مثل Elastic Cloud) یا در Docker container مستقر شود. ایندکس‌ها باید شامل فیلدهای: file_name, extracted_text, file_type, upload_date باشند. نکته حیاتی: باید از bulk indexing
- [ ] **مرحله 16: مهاجرت داده‌های موجود از SQLite FTS5 به Elasticsearch** — این مرحله شامل نوشتن یک اسکریپت یکباره (one-time migration script) است که تمام متن‌های استخراج‌شده و metadata موجود در SQLite FTS5 را خوانده و به Elasticsearch bulk index کند. اسکریپت باید قابلیت resume از نقطه قطع شده (checkpoint) را داشته باشد. نکته حیاتی: قبل از اجرا، یک backup از SQLite گرفته شو
- [ ] **مرحله 17: افزودن قابلیت اشتراک‌گذاری فایل‌ها با لینک موقت (Temporary Share Links)** — این مرحله شامل پیاده‌سازی یک سیستم اشتراک‌گذاری موقت است. کاربر می‌تواند برای هر فایل یک لینک موقت با تاریخ انقضا (مثلاً ۲۴ ساعت) و حداکثر تعداد دفعات مشاهده (مثلاً ۵ بار) ایجاد کند. لینک‌ها باید با JWT امضا شوند تا از دستکاری جلوگیری شود. نکته حیاتی: لینک‌های منقضی‌شده باید به طور خودکار پاکسازی شو
- [ ] **مرحله 18: پیاده‌سازی قابلیت برچسب‌گذاری (Tagging) و دسته‌بندی فایل‌ها** — این مرحله شامل افزودن قابلیت برچسب‌گذاری (tags) به فایل‌ها است. کاربر می‌تواند برای هر فایل چندین برچسب تعریف کند (مثلاً 'مهم', 'مالی', 'صورت‌جلسه'). برچسب‌ها در یک ستون جداگانه در Google Sheet ذخیره می‌شوند و در Elasticsearch نیز index می‌شوند. یک صفحه مدیریت برچسب‌ها برای ایجاد/ویرایش/حذف برچسب‌ها
- [ ] **مرحله 19: افزودن قابلیت نمایش آمار و داشبورد (Dashboard) از داده‌های منتقل‌شده** — این مرحله شامل ایجاد یک صفحه داشبورد با آمار زیر است: تعداد کل فایل‌های منتقل‌شده, حجم کل داده‌های منتقل‌شده, تفکیک بر اساس نوع فایل (صوتی/تصویری/متنی), تعداد فایل‌های آرشیو شده در Google Cloud Storage, و نمودار روند انتقال در هفته/ماه. داده‌ها از Google Sheet و Elasticsearch جمع‌آوری می‌شوند. نکته 
- [ ] **مرحله 20: نوشتن مستندات فنی (Technical Documentation) و راهنمای کاربر (User Guide)** — این مرحله شامل نوشتن مستندات کامل برای توسعه‌دهندگان و کاربران نهایی است. مستندات فنی شامل: معماری سیستم, نحوه راه‌اندازی (setup), متغیرهای محیطی, API reference, و توضیح ماژول‌ها. راهنمای کاربر شامل: نحوه آپلود فایل, جستجو, برچسب‌گذاری, اشتراک‌گذاری, و مشاهده آمار. نکته حیاتی: مستندات باید به زبان ف

---

# 🔹 مرحله 1: ایجاد ساختار فولدر اصلی در Google Drive و تعریف Google Sheet مرکزی

**Scope:** این مرحله شامل ایجاد یک فولدر اصلی (root folder) در Google Drive برای ذخیره تمام داده‌های غیرضروری و فایل‌های رسانه‌ای است. همچنین یک Google Sheet به عنوان شیت مرکزی (master sheet) برای ثبت و رهگیری تمام داده‌ها و فایل‌های منتقل‌شده ایجاد می‌شود. این مرحله فقط به ایجاد ساختار اولیه می‌پردازد و شامل منطق انتقال داده یا API calls نیست. نکته حیاتی: نام فولدر و شیت باید در متغیرهای محیطی (environment variables) ذخیره شوند تا در مراحل بعدی قابل استفاده باشند.
**Key terms:** Google Drive, Google Sheet, root folder, master sheet, environment variables

**بخش مربوط از متن کاربر:**
```
می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه و تمام داده‌های اصل داده‌ها اونجا ذخیره بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد ساختار فولدر اصلی Google Drive و شیت مرکزی

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک فولدر اصلی (root folder) در Google Drive برای ذخیره تمام داده‌های غیرضروری و فایل‌های رسانه‌ای و همچنین یک Google Sheet به عنوان شیت مرکزی (master sheet) برای ثبت و رهگیری تمام داده‌ها و فایل‌های منتقل‌شده را دارد. این مرحله فقط به ایجاد ساختار اولیه می‌پردازد و شامل منطق انتقال داده یا API calls نیست. نکته حیاتی: نام فولدر و شیت باید در متغیرهای محیطی (environment variables) ذخیره شوند تا در مراحل بعدی قابل استفاده باشند. کاربر می‌خواهد برنامه پایگاه داده اصلیش به Google Drive باشد و از Google Sheet هم استفاده کند. در کد فعلی پروژه، فایل `app/services/google_auth.py` (خطوط 1-28) شامل توابع `verify_google_token` و `exchange_code_for_token` است که از کتابخانه httpx برای ارتباط با Google OAuth استفاده می‌کند. همچنین فایل `app/routes/auth_google.py` (خطوط 1-421) مسیرهای OAuth را مدیریت می‌کند. فایل `app/core/config.py` تنظیمات GOOGLE_CLIENT_ID و GOOGLE_CLIENT_SECRET را دارد. فایل `app/main.py` (خطوط 623-626) به صورت شرطی روتر Google OAuth را mount می‌کند. فایل `app/models/user_oauth.py` مدل OAuthUser را تعریف می‌کند. فایل `app/schemas/user_oauth.py` شِماهای OAuth را دارد. فایل `app/database.py` سشن دیتابیس را مدیریت می‌کند. فایل `app/config.py` تنظیمات عمومی را دارد. فایل `requirements.txt` شامل httpx و python-jose است. فایل `app/services/__init__.py` سرویس‌ها را export می‌کند. فایل `app/routes/__init__.py` روترها را export می‌کند. فایل `app/dependencies/auth.py` وابستگی احراز هویت را دارد. فایل `app/middleware.py` میان‌افزارها را دارد. فایل `config/settings.py` تنظیمات محیطی را دارد. فایل `app/models/user.py` مدل User را دارد. فایل `app/services/self_improvement_service.py` سرویس خودسازی را دارد. فایل `app/rate_limit.py` محدودیت نرخ را دارد. فایل `app/celery_app.py` تنظیمات Celery را دارد. فایل `app/tasks.py` تسک‌های پس‌زمینه را دارد. فایل `app/webhook/router.py` روتر webhook را دارد. فایل `app/models/__init__.py` مدل‌ها را export می‌کند. فایل `app/schemas/__init__.py` شِماها را export می‌کند. فایل `app/services/ai/__init__.py` سرویس‌های AI را دارد. فایل `app/services/integrations/__init__.py` سرویس‌های یکپارچه‌سازی را دارد. فایل `app/services/integrations/external_project_interface.py` رابط پروژه خارجی را دارد. فایل `app/services/auth_service.py` سرویس احراز هویت را دارد. فایل `app/services/crypt_service.py` سرویس رمزنگاری را دارد. فایل `app/services/data_classification_service.py` سرویس طبقه‌بندی داده را دارد. فایل `app/services/integration_service.py` سرویس یکپارچه‌سازی را دارد. فایل `app/services/list_service.py` سرویس لیست را دارد. فایل `app/services/local_file_service.py` سرویس فایل محلی را دارد. فایل `app/services/notification_service.py` سرویس اعلان را دارد. فایل `app/services/person_service.py` سرویس شخص را دارد. فایل `app/services/planner_service.py` سرویس برنامه‌ریز را دارد. فایل `app/services/todo_item_service.py` سرویس آیتم کارها را دارد. فایل `app/models/ai_model_config.py` مدل تنظیمات AI را دارد. فایل `app/models/ai_provider.py

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی احراز هویت Google Drive و Google Sheets API در برنامه

**Scope:** این مرحله شامل تنظیم و پیاده‌سازی OAuth 2.0 برای دسترسی به Google Drive و Google Sheets APIs است. باید از Google Cloud Console یک پروژه ایجاد شود، credentials (Client ID و Client Secret) دریافت شود، و کتابخانه‌های مربوطه (google-api-python-client, google-auth-oauthlib) نصب شوند. نکته حیاتی: توکن احراز هویت باید به صورت امن ذخیره شود (مثلاً در متغیر محیطی یا فایل token.json) و قابلیت refresh خودکار داشته باشد.
**Key terms:** OAuth 2.0, Google Cloud Console, Client ID, Client Secret, google-api-python-client, google-auth-oauthlib, token.json

**بخش مربوط از متن کاربر:**
```
پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی OAuth 2.0 برای Google Drive و Google Sheets API

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/auth_google.py:1-609` — `auth_google.router` — این فایل الگوی OAuth 2.0 موجود را نشان می‌دهد که باید برای Google Drive/Sheets API تکرار شود. روتر فعلی فقط برای Google Sign-In است.
  ```python
  router = APIRouter(prefix='/auth/google', tags=['google-auth'])
  
  @router.get('/login')
  async def google_login(request: Request):
      ...
  
  @router.get('/callback')
  async def google_callback(request: Request, code: str = None):
      ...
  ```
- `app/main.py:623-626` — `auth_google conditional mount` — الگوی mount شرطی برای روتر Google Drive باید مشابه اینجا پیاده‌سازی شود.
  ```python
  if settings.GOOGLE_CLIENT_ID:
      from app.routes import auth_google
      app.include_router(auth_google.router, tags=['google-auth'])
      logger.info('Google OAuth router mounted (GOOGLE_CLIENT_ID is set)')
  ```
- `app/models/user_oauth.py:1-50` — `UserOAuth model` — این مدل برای ذخیره توکن‌های OAuth استفاده می‌شود. باید provider='google_drive' برای توکن‌های Google Drive/Sheets اضافه شود.
  ```python
  class UserOAuth(Base):
      __tablename__ = 'user_oauth'
      id = Column(Integer, primary_key=True)
      user_id = Column(Integer, ForeignKey('users.id'))
      provider = Column(String(50))
      access_token = Column(Text)
      refresh_token = Column(Text)
      token_expiry = Column(DateTime)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/config.py` — نیاز به افزودن متغیرهای GOOGLE_DRIVE_CLIENT_ID, GOOGLE_DRIVE_CLIENT_SECRET, GOOG

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی احراز هویت Google Drive و Google Sheets API در برنامه را دارد. این شامل تنظیم OAuth 2.0 با استفاده از Google Cloud Console، دریافت Client ID و Client Secret، نصب کتابخانه‌های google-api-python-client و google-auth-oauthlib است. نکته حیاتی: توکن احراز هویت باید به صورت امن ذخیره شود (مثلاً در متغیر محیطی یا فایل token.json) و قابلیت refresh خودکار داشته باشد. کاربر اشاره کرده که 'پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه'. کلیدواژه‌ها: OAuth 2.0, Google Cloud Console, Client ID, Client Secret, google-api-python-client, google-auth-oauthlib, token.json. در کد فعلی، فایل app/routes/auth_google.py (خطوط 1-609) یک OAuth flow برای Google Sign-In دارد که از GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET / GOOGLE_REDIRECT_URI استفاده می‌کند (app/config.py). این روتر در app/main.py (خطوط 623-626) به صورت شرطی mount می‌شود: if settings.GOOGLE_CLIENT_ID: app.include_router(auth_google.router). اما این flow فقط برای احراز هویت کاربر (Google Sign-In) است، نه برای دسترسی به Google Drive/Sheets API. نیاز به یک OAuth 2.0 flow جدید با scopeهای https://www.googleapis.com/auth/drive و https://www.googleapis.com/auth/spreadsheets داریم.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن کتابخانه‌های google-api-python-client و google-auth-oauthlib به requirements.txt. 2. ایجاد یک سرویس جدید app/services/google_drive_service.py برای مدیریت OAuth 2.0 flow و ذخیره امن توکن. 3. ایجاد یک روتر جدید app/routes/google_drive.py با endpointهای /api/google/auth (شروع OAuth), /api/google/callback (دریافت کد), /api/google/status (بررسی وضعیت توکن). 4. ذخیره توکن در دیتابیس (مدل UserOAuth موجود در app/models/user_oauth.py) با refresh خودکار. 5. افزودن متغیرهای محیطی GOOGLE_DRIVE_CLIENT_ID, GOOGLE_DRIVE_CLIENT_SECRET, GOOGLE_DRIVE_REDIRECT_URI به app/config.py. 6. mount کردن روتر جدید در app/main.py مشابه auth_google. 7. پیاده‌سازی توابع read/write برای Google Sheets در سرویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 3: طراحی و پیاده‌سازی مدل داده برای رهگیری فایل‌ها و داده‌های منتقل‌شده در Google Sheet

**Scope:** این مرحله شامل طراحی ساختار (schema) Google Sheet مرکزی است. ستون‌های مورد نیاز عبارتند از: ID (شناسه یکتا), FileName (نام فایل), FileType (نوع فایل: صوتی، تصویری، متنی), GoogleDriveLink (لینک فایل در Google Drive), OriginalDBLocation (محل اصلی در پایگاه داده Render), TransferDate (تاریخ انتقال), Status (وضعیت: active/archived), و Notes. همچنین یک تابع برای افزودن ردیف جدید به شیت پیاده‌سازی می‌شود.
**Key terms:** Google Sheet, schema, ID, FileName, FileType, GoogleDriveLink, OriginalDBLocation, TransferDate, Status, Notes

**بخش مربوط از متن کاربر:**
```
توی شیت باید همه چیزا ثبت بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی مدل داده و تابع ثبت ردیف در Google Sheet

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/local_file_service.py:1-50` — `کل فایل` — این سرویس مدیریت فایل‌های محلی را انجام می‌دهد و باید پس از آپلود فایل، تابع add_row از GoogleSheetService را فراخوانی کند.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `app/routes/integrations.py:1-30` — `router` — این روتر برای مدیریت یکپارچه‌سازی‌های خارجی استفاده می‌شود و endpoint جدید POST /api/integrations/google-sheet/add-row باید به آن اضافه شود.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `app/config.py:1-50` — `settings`
  ```python
  فایل deep
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی و پیاده‌سازی مدل داده برای رهگیری فایل‌ها و داده‌های منتقل‌شده در Google Sheet را دارد. این شامل طراحی ساختار (schema) Google Sheet مرکزی با ستون‌های: ID (شناسه یکتا), FileName (نام فایل), FileType (نوع فایل: صوتی، تصویری، متنی), GoogleDriveLink (لینک فایل در Google Drive), OriginalDBLocation (محل اصلی در پایگاه داده Render), TransferDate (تاریخ انتقال), Status (وضعیت: active/archived), و Notes است. همچنین یک تابع برای افزودن ردیف جدید به شیت پیاده‌سازی می‌شود. کاربر تأکید کرده: 'توی شیت باید همه چیزا ثبت بشه'. کلیدواژه‌ها: Google Sheet, schema, ID, FileName, FileType, GoogleDriveLink, OriginalDBLocation, TransferDate, Status, Notes. در کد فعلی پروژه، فایل‌های مرتبط با Google Drive و ذخیره‌سازی فایل شامل app/routes/local_files.py (مسیر آپلود فایل‌های محلی)، app/services/local_file_service.py (سرویس مدیریت فایل‌های محلی)، و app/models/local_file_entry.py (مدل داده فایل‌های محلی) هستند. همچنین app/routes/integrations.py و app/services/integration_service.py برای مدیریت یکپارچه‌سازی‌های خارجی (از جمله Google) وجود دارند. مدل LocalFileEntry در app/models/local_file_entry.py شامل فیلدهای filename, file_type, file_path, upload_date, user_id, status است که می‌تواند پایه‌ای برای schema Google Sheet باشد. مسیر app/routes/location.py و app/services/notification_service.py نیز ممکن است برای انتقال داده به Google Sheet استفاده شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل جدید به نام app/services/google_sheet_service.py که شامل کلاس GoogleSheetService با متدهای: __init__ (دریافت credentials و sheet_id از settings)، add_row (افزودن ردیف جدید با پارامترهای ID, FileName, FileType, GoogleDriveLink, OriginalDBLocation, TransferDate, Status, Notes)، get_all_rows (دریافت همه ردیف‌ها)، update_row (به‌روزرسانی ردیف بر اساس ID). 2. افزودن تنظیمات GOOGLE_SHEET_CREDENTIALS و GOOGLE_SHEET_ID به app/config.py (فایل settings). 3. ایجاد یک endpoint جدید در app/routes/integrations.py با مسیر POST /api/integrations/google-sheet/add-row که تابع add_row را فراخوانی کند. 4. به‌روزرسانی app/services/local_file_service.py برای فراخوانی خودکار google_sheet_service.add_row پس از آپلود فایل. 5. افزودن وابستگی google-api-python-client و google-auth-httplib2 به requirements.txt. 6. ایجاد تست در tests/test_google_sheet_service.py برای بررسی افزودن ردیف.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: پیاده‌سازی منطق آپلود فایل به Google Drive و ثبت در Google Sheet

**Scope:** این مرحله شامل نوشتن تابعی است که یک فایل (صوتی، تصویری، یا متنی) را از مسیر محلی یا URL دریافت کرده، آن را به فولدر اصلی Google Drive آپلود می‌کند، لینک اشتراک‌گذاری (shareable link) تولید می‌کند، و سپس یک ردیف جدید در Google Sheet با اطلاعات فایل ثبت می‌کند. نکته حیاتی: فایل‌ها باید با permission 'Anyone with the link can view' آپلود شوند تا در برنامه قابل فراخوانی باشند.
**Key terms:** upload, Google Drive, shareable link, Google Sheet, permission, Anyone with the link

**بخش مربوط از متن کاربر:**
```
مثلاً صوت‌هایی که یه وقت می‌فرستم عکسا فلان چیزا اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی آپلود فایل به Google Drive و ثبت در Google Sheet

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/google_auth.py:1-28`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی منطق آپلود فایل به Google Drive و ثبت اطلاعات در Google Sheet را داده است. فایل‌ها می‌توانند صوتی، تصویری، یا متنی باشند و از مسیر محلی یا URL دریافت شوند. فایل باید به فولدر اصلی Google Drive آپلود شود، لینک اشتراک‌گذاری (shareable link) با permission 'Anyone with the link can view' تولید شود، و سپس یک ردیف جدید در Google Sheet با اطلاعات فایل ثبت گردد. کاربر اشاره کرده: «مثلاً صوت‌هایی که یه وقت می‌فرستم عکسا فلان چیزا اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو». کلیدواژه‌ها: upload, Google Drive, shareable link, Google Sheet, permission, Anyone with the link. در کد فعلی پروژه، فایل `app/routes/local_files.py` مسیر `app/services/local_file_service.py` را برای مدیریت فایل‌های محلی فراخوانی می‌کند. همچنین `app/routes/integrations.py` و `app/services/integration_service.py` زیرساخت سرویس‌های یکپارچه‌سازی را دارند. فایل `app/services/google_auth.py` (خطوط 1-28) از `httpx` برای ارتباط با Google OAuth استفاده می‌کند که می‌تواند الگویی برای ارتباط با Google Drive API و Google Sheets API باشد. فایل `app/core/config.py` تنظیمات GOOGLE_CLIENT_ID و GOOGLE_CLIENT_SECRET را دارد که برای احراز هویت Google Drive و Sheets قابل استفاده است. فایل `app/models/integration.py` مدل‌های ذخیره‌سازی توکن‌های OAuth را دارد. فایل `app/routes/auth_google.py` (خطوط 1-17) از `app.services.google_auth` و `app.core.config` import می‌کند. فایل `app/main.py` (خطوط 623-626) Google OAuth router را به صورت شرطی mount می‌کند. فایل `app/services/local_file_service.py` سرویس مدیریت فایل‌های محلی را ارائه می‌دهد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `app/services/google_drive_service.py` که شامل توابع زیر باشد:
   - `upload_file_to_drive(file_path: str, mime_type: str) -> dict`: دریافت مسیر فایل یا URL، آپلود به Google Drive با permission 'Anyone with the link can view'، بازگرداندن لینک اشتراک‌گذاری و metadata.
   - `create_shareable_link(file_id: str) -> str`: تولید لینک با permission 'Anyone with the link can view'.
   - `append_to_sheet(sheet_id: str, row_data: list) -> None`: افزودن ردیف جدید به Google Sheet شامل نام فایل، لینک، تاریخ آپلود، نوع فایل.
2. استفاده از کتابخانه `google-api-python-client` و `google-auth-oauthlib` (اضافه به `requirements.txt`).
3. ایجاد endpoint جدید در `app/routes/local_files.py` یا `app/routes/integrations.py` با مسیر `/api/files/upload-to-drive` که فایل را دریافت کرده و به Google Drive آپلود کند.
4. ذخیره‌سازی mapping بین فایل محلی و لینک Google Drive در دیتابیس (احتمالاً در `app/models/local_file_entry.py`).
5. استفاده از `app/core/config.py` برای خواندن GOOGLE_DRIVE_FOLDER_ID و GOOGLE_SHEET_ID از environment variables.
6. به‌روزرسانی `app/main.py` برای mount کردن router جدید در صورت تنظیم بودن GOOGLE_CLIENT_ID.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: پیاده‌سازی منطق استخراج متن از فایل‌های صوتی و تصویری (OCR/ASR)

**Scope:** این مرحله شامل یکپارچه‌سازی یک سرویس تبدیل گفتار به متن (Speech-to-Text) مانند Google Cloud Speech-to-Text یا Whisper برای فایل‌های صوتی، و یک سرویس تشخیص متن (OCR) مانند Google Cloud Vision یا Tesseract برای تصاویر است. متن استخراج‌شده باید در یک فایل متنی (.txt) ذخیره شده و همراه با فایل اصلی به Google Drive آپلود شود. نکته حیاتی: اگر سرویس ابری استفاده می‌شود، باید هزینه‌ها و محدودیت‌های API در نظر گرفته شود.
**Key terms:** Speech-to-Text, OCR, Google Cloud Speech-to-Text, Google Cloud Vision, Whisper, Tesseract, text extraction

**بخش مربوط از متن کاربر:**
```
اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن OCR/ASR به سرویس فایل‌های محلی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/local_files.py:1-50` — `router` — فایل روتر فعلی برای مدیریت فایل‌های محلی. نیاز به افزودن endpoint جدید برای استخراج متن و اصلاح endpoint آپلود موجود دارد.
  ```python
  from fastapi import APIRouter, Depends, HTTPException, UploadFile, File
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  from app.services.local_file_service import *
  ```
- `app/services/local_file_service.py:1-30` — `upload_file` — سرویس اصلی مدیریت فایل‌ها. تابع upload_file باید اصلاح شود تا پس از آپلود، در صورت نیاز OCR/ASR انجام دهد.
  ```python
  async def upload_file(db: AsyncSession, user: User, file: UploadFile) -> dict:
      # ... existing upload logic ...
      # پس از آپلود فایل اصلی، باید OCR/ASR انجام شود
      return {"file_id": file_entry.id, "url": drive_url}
  ```
- `app/services/media_text_extractor.py:1-100` — `extract_text_from_image`
  ```python
  import pytesseract
  from PIL import Image
  
  def extract_text_from_image(file_path: str) -> str:
      """Extract text from image using Tesseract OCR."""
      try:
          image = Image.open(file_path)
          text = pytesseract.image_to_string(image, lang='fas+eng')
          return text.strip()
      except Exception as e:
          raise ValueError(f"
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
پیاده‌سازی منطق استخراج متن از فایل‌های صوتی و تصویری (OCR/ASR) در پروژه. کاربر درخواست 'پیاده‌سازی منطق استخراج متن از فایل‌های صوتی و تصویری (OCR/ASR)' را داده و اشاره کرده 'اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه'. کلیدواژه‌های اصلی: Speech-to-Text, OCR, Google Cloud Speech-to-Text, Google Cloud Vision, Whisper, Tesseract, text extraction. این قابلیت باید در مسیر آپلود فایل‌های محلی (app/routes/local_files.py و app/services/local_file_service.py) یکپارچه شود. در کد فعلی، app/routes/local_files.py از app/services/local_file_service.py برای مدیریت فایل‌ها استفاده می‌کند. سرویس local_file_service احتمالاً فایل‌ها را در Google Drive آپلود می‌کند (بر اساس context پروژه). متن استخراج‌شده باید در یک فایل متنی (.txt) ذخیره شده و همراه با فایل اصلی به Google Drive آپلود شود. نکته حیاتی: اگر سرویس ابری استفاده می‌شود، باید هزینه‌ها و محدودیت‌های API در نظر گرفته شود. کاربر همچنین اشاره کرده 'اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه' که نشان‌دهنده نیاز به استخراج خودکار متن از فایل‌های آپلودی است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام app/services/media_text_extractor.py که شامل دو تابع اصلی باشد: extract_text_from_image(file_path: str) -> str برای OCR با استفاده از Tesseract (کتابخانه pytesseract) یا Google Cloud Vision، و extract_text_from_audio(file_path: str) -> str برای ASR با استفاده از Whisper (کتابخانه openai-whisper) یا Google Cloud Speech-to-Text. 2. افزودن وابستگی‌های جدید به requirements.txt: pytesseract, Pillow, openai-whisper (یا google-cloud-speech, google-cloud-vision). 3. اصلاح تابع آپلود در app/services/local_file_service.py به‌گونه‌ای که پس از آپلود فایل اصلی، در صورت تشخیص فایل صوتی یا تصویری، متن استخراج شده و به‌عنوان یک فایل .txt همراه با فایل اصلی آپلود شود. 4. افزودن یک endpoint جدید در app/routes/local_files.py مانند POST /api/local-files/extract-text که امکان استخراج متن از یک فایل موجود را فراهم کند. 5. ذخیره متن استخراج‌شده در یک فایل .txt با نام base فایل اصلی (مثلاً image.jpg -> image.txt) و آپلود آن در همان مسیر Google Drive.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: طراحی و پیاده‌سازی منطق تصمیم‌گیری برای انتقال داده‌ها (Data Tiering Logic)

**Scope:** این مرحله شامل پیاده‌سازی یک سیستم رتبه‌بندی (tiering system) برای داده‌ها است. داده‌ها بر اساس معیارهایی مانند: تعداد دفعات دسترسی (access frequency), حجم داده, و重要性 (criticality) به سه دسته تقسیم می‌شوند: Hot (در Render بماند), Warm (در Render بماند اما با اولویت کمتر), Cold (به Google Drive منتقل شود). یک تابع schedule (مثلاً cron job) برای بررسی دوره‌ای داده‌ها و انتقال خودکار داده‌های Cold به Google Drive پیاده‌سازی می‌شود.
**Key terms:** data tiering, Hot, Warm, Cold, access frequency, cron job, transfer logic

**بخش مربوط از متن کاربر:**
```
می‌خوام چیزایی که خیلی ضروریه اونجا باشه و بقیه موارد منتقل بشه به گوگل درایو ... تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سیستم رتبه‌بندی و انتقال داده (Data Tiering) به Google Drive

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:190-523` — `startup_event` — این فایل deep-read شده است. startup_event باید برای اجرای seed داده‌های tiering و راه‌اندازی cron job به‌روزرسانی شود.
  ```python
  @app.on_event("startup")
  async def startup_event():
      try:
          async with engine.begin() as conn:
              await conn.run_sync(Base.metadata.create_all)
          logger.info("✅ Database tables created successfully")
      except Exception as e:
          logger.critical(f"❌ CRITICAL: Database connection failed: {e}")
          logger.info("   App will continue without database — set DATABASE_URL in Render env vars.")
  ```
- `app/services/google_auth.py:11-28` — `verify_google_token, exchange_code_for_token`
  ```python
  async def verify_google_token(token: str) -> Optional[dict]:
      """Verify Google OAuth token and return user info."""
      try:
          async with httpx.AsyncClient() as client:
              response = await client.get(
                  f"https://oauth2.googleapis.com
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
طراحی و پیاده‌سازی منطق تصمیم‌گیری برای انتقال داده‌ها (Data Tiering Logic) بر اساس درخواست کاربر: «می‌خوام چیزایی که خیلی ضروریه اونجا باشه و بقیه موارد منتقل بشه به گوگل درایو ... تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو». این سیستم داده‌ها را بر اساس معیارهایی مانند: تعداد دفعات دسترسی (access frequency), حجم داده, و重要性 (criticality) به سه دسته تقسیم می‌کند: Hot (در Render بماند), Warm (در Render بماند اما با اولویت کمتر), Cold (به Google Drive منتقل شود). یک تابع schedule (مثلاً cron job) برای بررسی دوره‌ای داده‌ها و انتقال خودکار داده‌های Cold به Google Drive پیاده‌سازی می‌شود. کلیدواژه‌ها: data tiering, Hot, Warm, Cold, access frequency, cron job, transfer logic. در کد فعلی، فایل‌های app/main.py (خطوط 190-523) شامل startup_event با migration‌های متعدد و seed data است، اما هیچ منطق tiering یا انتقال داده به سرویس خارجی مانند Google Drive وجود ندارد. فایل app/services/google_auth.py (خطوط 1-28) از httpx برای ارتباط با Google APIs استفاده می‌کند که می‌تواند به عنوان الگو برای ارتباط با Google Drive API استفاده شود. فایل app/routes/location.py (کامیت 30a1431) شامل endpoint /api/location ingest است که نشان‌دهنده وجود داده‌های مکانی قابل tiering است. فایل app/models/user_location.py مدل داده‌های مکانی را تعریف می‌کند. فایل app/config.py و app/core/config.py تنظیمات پروژه را مدیریت می‌کنند که باید متغیرهای محیطی جدید برای Google Drive API و تنظیمات tiering به آن‌ها اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید app/services/data_tiering_service.py شامل: کلاس DataTieringService با متدهای tier_data (تعیین رتبه داده), transfer_to_google_drive (انتقال فایل به Google Drive با استفاده از httpx مشابه google_auth.py), schedule_tiering_check (تنظیم cron job با celery). 2. ایجاد مدل جدید app/models/data_tiering.py شامل: مدل DataTieringLog با فیلدهای record_type, record_id, tier (Hot/Warm/Cold), last_accessed, access_count, transferred_to_gdrive, transferred_at. 3. ایجاد جدول migration جدید migrations/versions/0012_data_tiering.py. 4. افزودن متغیرهای محیطی GOOGLE_DRIVE_FOLDER_ID, TIERING_CRON_SCHEDULE, HOT_THRESHOLD_DAYS, COLD_THRESHOLD_DAYS به app/config.py و app/core/config.py. 5. افزودن تابع schedule در app/celery_app.py برای اجرای دوره‌ای بررسی tiering. 6. ایجاد endpoint جدید در app/routes/location.py (یا فایل جدید app/routes/data_tiering.py) برای نمایش وضعیت tiering داده‌ها. 7. به‌روزرسانی app/main.py برای mount کردن روتر جدید و اجرای seed داده‌های tiering در startup_event. 8. پیاده‌سازی معیارهای tiering: Hot (access_count > 100 در 30 روز یا criticality بالا), Warm (access_count بین 10 تا 100), Cold (access_count < 10 یا آخرین دسترسی بیش از 90 روز قبل). 9. استفاده از Google Drive API (مشابه verify_google_token در google_auth.py خطوط 11-22) برای آپلود فایل‌های Cold.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: ایجاد API endpoint برای فراخوانی و مشاهده فایل‌های منتقل‌شده به Google Drive از داخل برنامه

**Scope:** این مرحله شامل ایجاد یک API endpoint (مثلاً GET /api/files/{file_id}) است که با دریافت شناسه فایل از Google Sheet، لینک Google Drive را بازیابی کرده و فایل را به صورت مستقیم (stream) یا از طریق redirect به کاربر نمایش می‌دهد. همچنین یک endpoint لیست (GET /api/files) برای نمایش تمام فایل‌های منتقل‌شده با قابلیت جستجو و فیلتر پیاده‌سازی می‌شود. نکته حیاتی: دسترسی به این endpointها باید احراز هویت شود.
**Key terms:** API endpoint, GET /api/files/{file_id}, GET /api/files, stream, redirect, authentication

**بخش مربوط از متن کاربر:**
```
باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد API endpoint برای مشاهده فایل‌های Google Drive

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/local_files.py:1-50` — `router` — فایل deep-read نشده — بر اساس ساختار سطحی. این فایل برای endpointهای فایل‌های محلی است. باید endpointهای جدید به اینجا اضافه شوند.
  ```python
  from fastapi import APIRouter, Depends, HTTPException, Query
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  from app.services.local_file_service import LocalFileService
  from app.schemas.local_file_entry_schema import LocalFileEntryResponse
  
  router = APIRouter(prefix="/api/files", tags=["files"])
  
  @router.get("/", response_model=List[LocalFileEntryResponse])
  async def get_files(
      search: Optional[str] = Query(None),
      status: Optional[str] = Query(None),
      page: int = Query(1, ge=1),
      limit: int = Query(20, ge=1, le=100),
      current_user: User = Depends(get_current_user)
  ):
      service = LocalFileService()
      return await service.get_all_files(current_user.id, search, status, page, limit)
  ```
- `app/models/local_file_entry.py:1-30` — `LocalFileEntry` — فایل deep-read نشده — بر اساس ساختار سطحی. باید فیلدهای drive_file_id, drive_url, synced_at به این مدل اضافه شوند.
  ```python
  from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Boolean
  from sqlalchemy.sql import func
  from app.database import Base
  
  class LocalFileEntry(Base):
      __tablename__ = "local_file_entries"
      
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
      filename = Column(String(255), nullable=False)
      filepath = Column(String(512), nullable=False)
      file_size = Column(Integer, nullable=True)
      mime_type = Column(String(100), nullable=True)
      created_at = Column(DateTime(timezone=True), server_default=func.now())
      updated_at = Column(DateTime(timezone=True), onupdate=func.now())
  ```
- `app/services/local_file_service.py:1-40` — `LocalFileService` — فایل deep-read نشده — بر اساس ساختار سطحی. باید متدهای get_all_files و get_file_by_id به این سرویس اضافه شوند.
  ```python
  from sqlalchemy.orm import Session
  from app.database import get_db
  from app.models.local_file_entry import LocalFileEntry
  from app.schemas.local_file_entry_schema import LocalFileEntryCreate, LocalFileEntryUpdate
  
  class LocalFileService:
      def __init__(self):
          self.db = next(get_db())
      
      async def create_file(self, file_data: LocalFileEntryCreate, user_id: int):
          db_file = LocalFileEntry(**file_data.dict(), user_id=user_id)
          self.db.add(db_file)
          self.db.commit()
          self.db.refresh(db_file)
          return db_file
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: FastAPI (پایتون) برای بک‌اند، React (جاوااسکریپت) برای فرانت‌اند، SQLAlchemy برای ORM، PostgreSQL برای دیتابیس، Google Drive API برای ذخیره‌سازی فایل. کتابخانه‌های مورد نیاز: google-api-python-client, google-auth-oauthlib, aiofiles.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/integration_service.py` (سطر 1) — برای بازیابی لینک Google Drive از طریق API گوگل با استفاده از توکن OAuth کاربر. باید متد getDriveFileUrl به این سرویس اضافه شود.
- `app/models/user_oauth.py` (سطر 1) — ذخیره توکن‌های OAuth کاربران برای دسترسی به Google Drive API. برای احراز هویت درخواست‌های Google Drive استفاده می‌شود.
- `app/schemas/local_file_entry_schema.py` (سطر 1) — برای تعریف اسکیماهای response و request شامل فیلدهای جدید Google Drive. باید به‌روزرسانی شود.
- `frontend/src/lib/api.js` (سطر 1) — برای افزودن متدهای getFiles() و getFileById() جهت فراخوانی endpointهای جدید از سمت فرانت‌اند.
- `frontend/src/App.jsx` (سطر 1) — برای افزودن مسیر جدید /drive-files به روتینگ برنامه و نمایش کامپوننت DriveFiles.
- `app/dependencies/auth.py` (سطر 1) — برای استفاده از get_current_user به عنوان وابستگی احراز هویت در endpointهای جدید.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های متعددی وابسته است: app/routes/local_files.py (روتر اصلی)، app/models/local_file_entry.py (مدل دیتابیس با فیلدهای جدید)، app/services/local_file_service.py (سرویس business logic)، app/services/integration_service.py (ارتباط با Google Drive API)، app/models/user_oauth.py (توکن‌های OAuth)، app/schemas/local_file_entry_schema.py (اسکیماهای API)، frontend/src/lib/api.js (کلاینت API فرانت‌اند)، frontend/src/App.jsx (روتینگ)، frontend/src/pages/DriveFiles.jsx (کامپوننت جدید). همچنین به کتابخانه‌های google-api-python-client (برای ارتباط با Google Drive API) و aiofiles (برای stream فایل) نیاز است.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد API endpoint برای فراخوانی و مشاهده فایل‌های منتقل‌شده به Google Drive از داخل برنامه را دارد. این شامل دو endpoint است: GET /api/files/{file_id} که با دریافت شناسه فایل از Google Sheet، لینک Google Drive را بازیابی کرده و فایل را به صورت مستقیم (stream) یا از طریق redirect به کاربر نمایش می‌دهد. همچنین GET /api/files برای نمایش تمام فایل‌های منتقل‌شده با قابلیت جستجو و فیلتر. نکته حیاتی: دسترسی به این endpointها باید احراز هویت شود. کاربر تأکید کرده: 'باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه'. کلیدواژه‌ها: API endpoint, GET /api/files/{file_id}, GET /api/files, stream, redirect, authentication.

بر اساس بررسی ساختار پروژه، فایل‌های مرتبط موجود: app/routes/local_files.py (مسیر فایل‌های محلی)، app/models/local_file_entry.py (مدل دیتابیس فایل‌های محلی)، app/services/local_file_service.py (سرویس فایل‌های محلی)، app/schemas/local_file_entry_schema.py (اسکیما). همچنین app/routes/integrations.py و app/services/integration_service.py برای مدیریت یکپارچه‌سازی‌ها (از جمله Google Drive) وجود دارند. مدل‌های app/models/integration.py و app/models/user_oauth.py برای ذخیره توکن‌های OAuth و تنظیمات یکپارچه‌سازی استفاده می‌شوند. فایل frontend/src/lib/api.js برای فراخوانی API از سمت فرانت‌اند استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] GET /api/files با هدر Authorization معتبر باید لیست فایل‌های کاربر را با status 200 برگرداند
- [ ] GET /api/files بدون هدر Authorization باید status 401 برگرداند
- [ ] GET /api/files/{file_id} با file_id معتبر باید اطلاعات فایل را با status 200 برگرداند
- [ ] GET /api/files/{file_id} با file_id نامعتبر باید status 404 برگرداند
- [ ] مدل LocalFileEntry باید فیلدهای drive_file_id, drive_url, synced_at را داشته باشد
- [ ] کامپوننت DriveFiles در فرانت‌اند باید لیست فایل‌ها را نمایش دهد و با کلیک روی هر فایل، لینک Google Drive باز شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد endpoint جدید GET /api/files در app/routes/local_files.py با decorator @router.get("/files") و وابستگی Depends(get_current_user) برای احراز هویت. این endpoint باید از سرویس local_file_service.getAllFiles() استفاده کند که فایل‌های منتقل‌شده به Google Drive را از دیتابیس (مدل LocalFileEntry) با قابلیت جستجو و فیلتر (پارامترهای query: search, status, page, limit) برگرداند.
2. ایجاد endpoint GET /api/files/{file_id} در همان فایل که با دریافت file_id، از local_file_service.getFileById(file_id) برای یافتن رکورد استفاده کند. سپس با استفاده از integration_service.getDriveFileUrl(record.drive_file_id) لینک Google Drive را بازیابی کرده و یا redirect (HTTP 302) به URL مستقیم فایل انجام دهد یا فایل را stream کند.
3. به‌روزرسانی مدل LocalFileEntry (app/models/local_file_entry.py) برای افزودن فیلدهای drive_file_id (String, nullable), drive_url (String, nullable), synced_at (DateTime, nullable) در صورت عدم وجود.
4. به‌روزرسانی اسکیما (app/schemas/local_file_entry_schema.py) برای شامل شدن فیلدهای جدید.
5. افزودن متد getAllFiles و getFileById به local_file_service (app/services/local_file_service.py).
6. افزودن متد getDriveFileUrl به integration_service (app/services/integration_service.py) که با استفاده از توکن OAuth ذخیره‌شده در user_oauth، از Google Drive API فایل را بازیابی کند.
7. ایجاد کامپوننت فرانت‌اند (مثلاً frontend/src/pages/DriveFiles.jsx) برای نمایش لیست فایل‌ها با قابلیت جستجو و کلیک برای مشاهده.
8. افزودن مسیر جدید به frontend/src/App.jsx و منوی ناوبری.
9. افزودن متدهای getFiles و getFileById به frontend/src/lib/api.js.

## 💡 نمونه‌های قبل/بعد
**افزودن endpoint GET /api/files به local_files.py**

_قبل:_
```
# فایل app/routes/local_files.py (فعلی)
from fastapi import APIRouter

router = APIRouter(prefix="/api/local-files", tags=["local-files"])

@router.get("/")
async def get_local_files():
    return {"message": "List local files"}
```

_بعد:_
```
# فایل app/routes/local_files.py (پس از تغییر)
from fastapi import APIRouter, Depends, HTTPException, Query
from typing import List, Optional
from app.dependencies.auth import get_current_user
from app.models.user import User
from app.services.local_file_service import LocalFileService
from app.schemas.local_file_entry_schema import LocalFileEntryResponse

router = APIRouter(prefix="/api/files", tags=["files"])

@router.get("/", response_model=List[LocalFileEntryResponse])
async def get_files(
    search: Optional[str] = Query(None),
    status: Optional[str] = Query(None),
    page: int = Query(1, ge=1),
    limit: int = Query(20, ge=1, le=100),
    current_user: User = Depends(get_current_user)
):
    """
    GET /api/files - دریافت لیست تمام فایل‌های منتقل‌شده به Google Drive
    با قابلیت جستجو و فیلتر
    """
    service = LocalFileService()
    return await service.get_all_files(current_user.id, search, status, page, limit)

@router.get("/{file_id}", response_model=LocalFileEntryResponse)
async def get_file(
    file_id: int,
    current_user: User = Depends(get_current_user)
):
    """
    GET /api/files/{file_id} - دریافت اطلاعات و لینک یک فایل خاص
    با redirect به Google Drive یا stream مستقیم
    """
    service = LocalFileService()
    file_record = await service.get_file_by_id(file_id, current_user.id)
    if not file_record:
        raise HTTPException(status_code=404, detail="File not found")
    return file_record
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_local_files.py -v`
- `npm run test -- --run frontend/src/__tests__/DriveFiles.test.jsx`
- `python -m pytest backend/tests/test_integration_service.py -v`

## ⚠️ ریسک‌ها و موارد احتیاط
1. تغییر در app/models/local_file_entry.py نیاز به migration دیتابیس دارد (فایل‌های migrations/versions/) و ممکن است با داده‌های موجود conflict ایجاد کند. 2. app/services/integration_service.py به توکن‌های OAuth معتبر وابسته است؛ اگر توکن منقضی شده باشد، endpointهای Google Drive با خطا مواجه می‌شوند. 3. افزودن endpointهای جدید به app/routes/local_files.py ممکن است با routeهای موجود تداخل prefix داشته باشد (prefix از /api/local-files به /api/files تغییر می‌کند). 4. stream کردن فایل‌های حجیم از Google Drive ممکن است باعث timeout یا مصرف بالای حافظه شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: طراحی و پیاده‌سازی رابط کاربری (UI) برای مدیریت و مشاهده داده‌های منتقل‌شده

**Scope:** این مرحله شامل ایجاد صفحات frontend برای نمایش لیست فایل‌های منتقل‌شده (با قابلیت جستجو، فیلتر بر اساس نوع فایل و تاریخ)، نمایش جزئیات هر فایل (نام، نوع، تاریخ انتقال، لینک)، و دکمه‌های عملیات (مشاهده، دانلود، حذف از Google Drive). همچنین یک بخش برای نمایش وضعیت انتقال‌های خودکار (آخرین زمان اجرا، تعداد فایل‌های منتقل‌شده) اضافه می‌شود. نکته حیاتی: UI باید responsive باشد و در موبایل هم کار کند.
**Key terms:** frontend, UI, list page, detail page, search, filter, responsive

**بخش مربوط از متن کاربر:**
```
باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد صفحات UI برای مدیریت فایل‌های منتقل‌شده به Google Drive

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/Lists.jsx:1-161` — `Lists` — این فایل deep-read شده و الگوی مناسبی برای ساختار صفحه لیست فایل‌های منتقل‌شده است. از useState, useEffect, fetch با API_BASE استفاده می‌کند.
  ```jsx
  function Lists() {
    const [lists, setLists] = useState([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState(null);
  
    const fetchLists = async () => {
      try {
        const res = await fetch(`${API_BASE}/lists`);
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        const data = await res.json();
        setLists(Array.isArray(data) ? data : []);
        setError(null);
      } catch (e) {
        setError('خطا در دریافت لیست‌ها: ' + e.message);
      } finally {
        setLoading(false);
      }
    };
  ```
- `frontend/src/pages/ListDetail.jsx:1-478` — `ListDetail`
  ```jsx
  function ListDetail() {
    const { listId } = useParams();
    const id = parseInt(listId, 10);
    const [list, setList] = useState(null);
    const [items, setItems] = useState([]);
    const [allLists, setAllLists] = useState([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState(null);
  
    const fetchAll = async () => {
      try {
        const [listRes, listsRes] = await Promise.all([
          fetch(`${API_BASE}/lists/${id}`),
          fetch(`${API_BASE}/lists`),
        ]);
        if
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی و پیاده‌سازی رابط کاربری (UI) برای مدیریت و مشاهده داده‌های منتقل‌شده را دارد. این شامل ایجاد صفحات frontend برای نمایش لیست فایل‌های منتقل‌شده (با قابلیت جستجو، فیلتر بر اساس نوع فایل و تاریخ)، نمایش جزئیات هر فایل (نام، نوع، تاریخ انتقال، لینک)، و دکمه‌های عملیات (مشاهده، دانلود، حذف از Google Drive) است. همچنین یک بخش برای نمایش وضعیت انتقال‌های خودکار (آخرین زمان اجرا، تعداد فایل‌های منتقل‌شده) اضافه می‌شود. کاربر تأکید کرده که UI باید responsive باشد و در موبایل هم کار کند. کاربر همچنین اشاره کرده: 'باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه'. کلیدواژه‌ها: frontend, UI, list page, detail page, search, filter, responsive. در کد فعلی، فایل‌های frontend/src/pages/Lists.jsx و frontend/src/pages/ListDetail.jsx الگوی مناسبی برای ساختار لیست و جزئیات هستند. فایل frontend/src/components/Sidebar.jsx برای افزودن لینک به صفحه جدید استفاده می‌شود. فایل frontend/src/App.jsx برای مسیریابی استفاده می‌شود. فایل app/routes/local_files.py (موجود در ساختار پروژه) احتمالاً endpointهای مربوط به فایل‌های محلی را مدیریت می‌کند. فایل app/services/local_file_service.py سرویس مربوطه است. فایل app/models/local_file_entry.py مدل داده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد صفحه جدید frontend/src/pages/FileTransfers.jsx با ساختار مشابه Lists.jsx: شامل فرم جستجو، فیلتر بر اساس نوع فایل (dropdown) و تاریخ (date picker)، لیست فایل‌ها با قابلیت کلیک برای نمایش جزئیات. 2. ایجاد صفحه frontend/src/pages/FileTransferDetail.jsx مشابه ListDetail.jsx برای نمایش جزئیات هر فایل (نام، نوع، تاریخ انتقال، لینک) و دکمه‌های عملیات (مشاهده، دانلود، حذف از Google Drive). 3. افزودن بخش وضعیت انتقال‌های خودکار در بالای صفحه FileTransfers.jsx (آخرین زمان اجرا، تعداد فایل‌های منتقل‌شده). 4. افزودن مسیرهای جدید به frontend/src/App.jsx: /file-transfers و /file-transfers/:id. 5. افزودن لینک به Sidebar.jsx برای دسترسی به صفحه جدید. 6. اطمینان از responsive بودن با استفاده از کلاس‌های Tailwind CSS (grid-cols-1 sm:grid-cols-2, flex-col md:flex-row). 7. استفاده از API_BASE = '/api' و endpointهای موجود در app/routes/local_files.py (GET /api/local-files, GET /api/local-files/{id}, DELETE /api/local-files/{id}).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 9: پیاده‌سازی قابلیت جستجوی پیشرفته در داده‌های منتقل‌شده (شامل متن استخراج‌شده)

**Scope:** این مرحله شامل افزودن قابلیت جستجوی full-text بر روی متن‌های استخراج‌شده از فایل‌های صوتی و تصویری است. متن‌های استخراج‌شده (که در فایل‌های .txt در Google Drive ذخیره شده‌اند) باید index شده و قابل جستجو باشند. می‌توان از Elasticsearch یا یک راه‌حل ساده‌تر مانند SQLite FTS5 برای این کار استفاده کرد. نکته حیاتی: جستجو باید همزمان روی نام فایل و متن استخراج‌شده انجام شود.
**Key terms:** full-text search, index, Elasticsearch, SQLite FTS5, text extraction, search

**بخش مربوط از متن کاربر:**
```
اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن جستجوی full-text بر متن استخراج‌شده فایل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/local_file_entry.py:1-30` — `LocalFileEntry` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مدل فعلی احتمالاً فیلدهای id, filename, file_path, user_id, created_at را دارد. باید فیلد extracted_text از نوع TEXT اضافه شود.
- `app/routes/local_files.py:1-50` — `router` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. باید endpoint GET /api/local-files/search به این router اضافه شود.
- `app/services/local_file_service.py:1-50` — `LocalFileService` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. باید هنگام ذخیره فایل، متن استخراج‌شده را در فیلد extracted_text ذخیره کند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت جستجوی پیشرفته در داده‌های منتقل‌شده (شامل متن استخراج‌شده) را دارد. این مرحله شامل افزودن قابلیت جستجوی full-text بر روی متن‌های استخراج‌شده از فایل‌های صوتی و تصویری است. متن‌های استخراج‌شده (که در فایل‌های .txt در Google Drive ذخیره شده‌اند) باید index شده و قابل جستجو باشند. کاربر پیشنهاد استفاده از Elasticsearch یا یک راه‌حل ساده‌تر مانند SQLite FTS5 را مطرح کرده است. نکته حیاتی: جستجو باید همزمان روی نام فایل و متن استخراج‌شده انجام شود. کاربر اشاره کرده که "اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه". کلیدواژه‌های اصلی: full-text search, index, Elasticsearch, SQLite FTS5, text extraction, search.

در کد فعلی پروژه، مدل `local_file_entry.py` در `app/models/` وجود دارد که می‌تواند برای ذخیره‌سازی متن استخراج‌شده استفاده شود. همچنین مسیر `app/routes/local_files.py` و `app/services/local_file_service.py` برای مدیریت فایل‌ها وجود دارند. فایل `app/models/local_file_entry.py` در ساختار پروژه دیده می‌شود اما deep-read نشده است. فایل‌های مرتبط با Google Drive integration در `app/services/integrations/` و `app/routes/integrations.py` قرار دارند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی مدل `app/models/local_file_entry.py` برای افزودن فیلد `extracted_text` از نوع TEXT برای ذخیره متن استخراج‌شده.
2. ایجاد یک سرویس جدید `app/services/search_service.py` که از SQLite FTS5 برای ایندکس کردن و جستجوی full-text استفاده کند.
3. در سرویس جدید، یک تابع `build_fts_index` برای ایجاد جدول FTS5 از روی داده‌های `local_file_entry`.
4. یک تابع `search_files(query: str)` که جستجوی همزمان روی `filename` و `extracted_text` انجام دهد.
5. افزودن endpoint جدید در `app/routes/local_files.py` با مسیر `/api/local-files/search` که query parameter دریافت کرده و نتایج جستجو را برگرداند.
6. به‌روزرسانی `app/services/local_file_service.py` برای ذخیره متن استخراج‌شده در فیلد جدید هنگام آپلود/پردازش فایل.
7. ایجاد migration در `migrations/versions/` برای افزودن فیلد `extracted_text` به جدول `local_file_entries`.
8. افزودن تست‌های مربوطه در `backend/tests/test_search.py`.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 10: نوشتن تست‌های واحد (Unit Tests) برای منطق انتقال داده و API endpoints

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای ماژول‌های کلیدی است: تابع آپلود به Google Drive (با mock کردن API calls), تابع استخراج متن (با mock کردن سرویس‌های OCR/ASR), منطق تصمیم‌گیری انتقال (data tiering logic), و API endpoints (با استفاده از TestClient). حداقل ۸۰% پوشش کد (code coverage) برای این ماژول‌ها هدفگذاری شود. نکته حیاتی: تست‌ها نباید به سرویس‌های واقعی Google متصل شوند — همه چیز mock شود.
**Key terms:** unit tests, mock, pytest, TestClient, code coverage, upload, OCR, ASR, data tiering

**بخش مربوط از متن کاربر:**
```
(نیاز به تست برای اطمینان از صحت عملکرد)
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای ماژول‌های انتقال داده و API

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/local_file_service.py:1-50` — `upload_to_google_drive` — تابع آپلود به Google Drive که باید با mock کردن httpx.AsyncClient تست شود
  ```python
  from app.services.local_file_service import upload_to_google_drive
  # این تابع با httpx.AsyncClient به Google Drive API متصل می‌شود
  ```
- `app/services/ai/image_service.py:1-30` — `extract_text_from_image` — تابع استخراج متن با OCR که باید با mock کردن سرویس خارجی تست شود
  ```python
  from app.services.ai.image_service import extract_text_from_image
  # این تابع سرویس OCR را برای استخراج متن از تصویر فراخوانی می‌کند
  ```
- `app/services/ai/nlp_service.py:1-30` — `transcribe_audio` — تابع تبدیل صوت به متن با ASR که باید با mock کردن سرویس خارجی تست شود
  ```python
  from app.services.ai.nlp_service import transcribe_audio
  # این تابع سرویس ASR را برای تبدیل صوت به متن فراخوانی می‌کند
  ```
- `app/routes/local_files.py:1-50` — `upload_file` — API endpoint آپلود فایل که باید با TestClient تست شود
  ```python
  from app.routes.local_files import router
  # endpoint آپلود فایل در مسیر /api/files/upload
  ```
- `app/routes/ai.py:1-50` — `process_file` — API endpoint پردازش فایل که باید با TestClient تست شود
  ```python
  from app.routes.ai import router
  # endpoint پردازش فایل در مسیر /api/ai/process
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
نوشتن تست‌های واحد (Unit Tests) برای منطق انتقال داده و API endpoints. این مرحله شامل نوشتن تست‌های واحد برای ماژول‌های کلیدی است: تابع آپلود به Google Drive (با mock کردن API calls), تابع استخراج متن (با mock کردن سرویس‌های OCR/ASR), منطق تصمیم‌گیری انتقال (data tiering logic), و API endpoints (با استفاده از TestClient). حداقل ۸۰% پوشش کد (code coverage) برای این ماژول‌ها هدفگذاری شود. نکته حیاتی: تست‌ها نباید به سرویس‌های واقعی Google متصل شوند — همه چیز mock شود. کلیدواژه‌ها: unit tests, mock, pytest, TestClient, code coverage, upload, OCR, ASR, data tiering. با توجه به بررسی کدبیس، ماژول‌های مرتبط با آپلود فایل و استخراج متن در فایل‌های app/services/local_file_service.py و app/routes/local_files.py قرار دارند. همچنین سرویس‌های AI شامل app/services/ai/image_service.py و app/services/ai/nlp_service.py برای OCR/ASR استفاده می‌شوند. منطق تصمیم‌گیری انتقال (data tiering) در حال حاضر در کدبیس پیاده‌سازی نشده و باید ایجاد شود. API endpoints مربوط به فایل‌ها در app/routes/local_files.py (مسیر /api/files) و app/routes/ai.py (مسیر /api/ai/process) قرار دارند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل تست جدید tests/unit/test_local_file_service.py با mock کردن httpx.AsyncClient برای شبیه‌سازی آپلود به Google Drive. 2. ایجاد فایل تست tests/unit/test_ai_services.py با mock کردن سرویس‌های OCR/ASR در app/services/ai/image_service.py و app/services/ai/nlp_service.py. 3. ایجاد ماژول data tiering logic در app/services/data_classification_service.py و نوشتن تست برای آن. 4. ایجاد فایل تست tests/integration/test_api_endpoints.py با استفاده از TestClient برای تست endpointهای /api/files/upload و /api/ai/process. 5. تنظیم فایل pytest.ini یا pyproject.toml برای اندازه‌گیری code coverage با pytest-cov. 6. اطمینان از mock کامل سرویس‌های خارجی (Google Drive API, OCR service, ASR service) در تمام تست‌ها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 11: نوشتن تست‌های یکپارچه‌سازی (Integration Tests) با سرویس‌های واقعی Google

**Scope:** این مرحله شامل نوشتن تست‌هایی است که با سرویس‌های واقعی Google Drive و Sheets ارتباط برقرار می‌کنند (با استفاده از یک حساب تستی). این تست‌ها عملیات واقعی آپلود، خواندن، و حذف فایل‌ها را انجام می‌دهند. یک فولدر تستی جداگانه در Google Drive برای این منظور ایجاد می‌شود که پس از اتمام تست‌ها پاک می‌شود. نکته حیاتی: این تست‌ها باید با یک flag (مثلاً --run-integration) از تست‌های واحد جدا شوند.
**Key terms:** integration tests, Google Drive API, Google Sheets API, test account, pytest, --run-integration

**بخش مربوط از متن کاربر:**
```
(نیاز به تست یکپارچه‌سازی برای اطمینان از ارتباط صحیح با Google APIs)
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های یکپارچه‌سازی Google Drive و Sheets

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/integration_service.py:1-50` — `IntegrationService` — این سرویس اصلی برای مدیریت یکپارچه‌سازی‌ها است و باید در تست‌های یکپارچه‌سازی
  ```python
  from app.schemas.integration_schema import IntegrationCreate, IntegrationUpdate, IntegrationOut
  from app.services.integrations.external_project_interface import ExternalProjectInterface
  
  class IntegrationService:
      def __init__(self, db: AsyncSession):
          self.db = db
          self.external_interface = ExternalProjectInterface()
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های یکپارچه‌سازی (Integration Tests) با سرویس‌های واقعی Google Drive و Google Sheets را دارد. این تست‌ها باید با استفاده از یک حساب تستی (test account) و با سرویس‌های واقعی Google ارتباط برقرار کنند. عملیات واقعی آپلود، خواندن، و حذف فایل‌ها در Google Drive انجام می‌شود. یک فولدر تستی جداگانه در Google Drive برای این منظور ایجاد می‌شود که پس از اتمام تست‌ها پاک می‌شود. نکته حیاتی: این تست‌ها باید با یک flag (مثلاً --run-integration) از تست‌های واحد جدا شوند. کلیدواژه‌های اصلی: integration tests, Google Drive API, Google Sheets API, test account, pytest, --run-integration. در کد فعلی پروژه، فایل‌های app/services/integration_service.py و app/services/integrations/external_project_interface.py و app/routes/integrations.py وجود دارند که سرویس‌های یکپارچه‌سازی را مدیریت می‌کنند. همچنین فایل app/services/google_auth.py برای احراز هویت Google OAuth وجود دارد. فایل app/main.py در خطوط 623-626 نشان می‌دهد که Google OAuth router به صورت شرطی mount می‌شود. فایل app/routes/auth_google.py شامل endpointهای مربوط به Google OAuth است. فایل app/models/integration.py مدل داده‌های یکپارچه‌سازی را تعریف می‌کند. فایل app/schemas/integration_schema.py شامل schemaهای مربوط به یکپارچه‌سازی است. فایل app/services/integrations/__init__.py پکیج سرویس‌های یکپارچه‌سازی را تعریف می‌کند. فایل app/services/integrations/external_project_interface.py شامل رابط پروژه‌های خارجی است. فایل app/routes/__init__.py و app/routes/_serializers.py نیز مرتبط هستند. فایل requirements.txt شامل کتابخانه‌های httpx==0.28.1 و pytest==8.3.4 و pytest-asyncio==0.24.0 است که برای تست‌های یکپارچه‌سازی مفید هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل تست جدید در مسیر backend/tests/test_google_integration.py با استفاده از pytest و flag --run-integration. 2. استفاده از pytest.mark.skipif با شرط os.getenv('RUN_INTEGRATION_TESTS') != 'true' برای جدا کردن تست‌های یکپارچه‌سازی از تست‌های واحد. 3. ایجاد یک کلاس TestGoogleDriveIntegration که شامل متدهای setUp/tearDown برای ایجاد و پاک‌سازی فولدر تستی در Google Drive باشد. 4. نوشتن تست‌های زیر: test_upload_file_to_drive (آپلود فایل واقعی)، test_read_file_from_drive (خواندن فایل)، test_delete_file_from_drive (حذف فایل)، test_create_sheet (ایجاد شیت جدید)، test_read_sheet_data (خواندن داده از شیت)، test_update_sheet_data (به‌روزرسانی داده در شیت). 5. استفاده از سرویس google_auth.py برای احراز هویت با حساب تستی. 6. استفاده از integration_service.py برای فراخوانی عملیات‌های Google Drive و Sheets. 7. ایجاد فایل conftest.py در backend/tests/ با fixtureهای لازم برای راه‌اندازی سرویس‌های Google. 8. اضافه کردن اسکریپت run-integration-tests.sh که متغیر محیطی RUN_INTEGRATION_TESTS=true را تنظیم کرده و pytest --run-integration را اجرا می‌کند. 9. مستندسازی در docs/INTEGRATION_TESTS.md درباره نحوه راه‌اندازی حساب تستی و اجرای تست‌ها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 12: پیاده‌سازی مکانیزم fallback و error handling برای خطاهای Google API

**Scope:** این مرحله شامل پیاده‌سازی یک سیستم مدیریت خطا برای زمان‌هایی که Google API در دسترس نیست یا با محدودیت نرخ (rate limit) مواجه می‌شود. باید از تکنیک‌هایی مانند retry with exponential backoff, circuit breaker, و fallback به ذخیره‌سازی محلی (local storage) استفاده شود. همچنین یک صف (queue) برای عملیات‌های ناموفق ایجاد می‌شود که بعداً دوباره امتحان شوند. نکته حیاتی: خطاها باید در Google Sheet (در یک ستون 'Error Log') ثبت شوند.
**Key terms:** error handling, retry, exponential backoff, circuit breaker, fallback, local storage, queue, Error Log

**بخش مربوط از متن کاربر:**
```
(نیاز به مدیریت خطا برای اطمینان از پایداری)
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی مکانیزم fallback و error handling برای Google API

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/auth_google.py:1-50` — `auth_google.router` — این فایل نقطه اصلی ادغام با Google API است و باید فراخوانی‌های API در آن با retry و circuit breaker پوشش داده شود.
  ```python
  فایل deep-read شده است اما محتوای کامل آن در context موجود نیست. بر اساس ساختار پروژه، این فایل شامل مسیرهای OAuth گوگل است.
  ```
- `app/services/integration_service.py:1-30` — `IntegrationService`
  ```python
  ف
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
این تسک شامل پیاده‌سازی یک سیستم مدیریت خطا برای زمان‌هایی است که Google API در دسترس نیست یا با محدودیت نرخ (rate limit) مواجه می‌شود. باید از تکنیک‌هایی مانند retry with exponential backoff, circuit breaker, و fallback به ذخیره‌سازی محلی (local storage) استفاده شود. همچنین یک صف (queue) برای عملیات‌های ناموفق ایجاد می‌شود که بعداً دوباره امتحان شوند. نکته حیاتی: خطاها باید در Google Sheet (در یک ستون 'Error Log') ثبت شوند.

کلیدواژه‌ها: error handling, retry, exponential backoff, circuit breaker, fallback, local storage, queue, Error Log

بر اساس کد واقعی پروژه:
- فایل `app/routes/auth_google.py` (deep-read شده) حاوی مسیر OAuth گوگل است و نقطه شروع ادغام با Google API است.
- فایل `app/services/google_auth.py` (در ساختار پروژه موجود است) سرویس احراز هویت گوگل را مدیریت می‌کند.
- فایل `app/config.py` (import شده در `app/main.py` خط 16) شامل تنظیمات GOOGLE_CLIENT_ID است که در خط 623 `app/main.py` برای mount کردن router auth_google استفاده می‌شود.
- فایل `app/routes/integrations.py` (deep-read شده) مسیرهای یکپارچه‌سازی را مدیریت می‌کند و می‌تواند میزبان منطق Google Sheets باشد.
- فایل `app/services/integration_service.py` (در ساختار موجود است) سرویس یکپارچه‌سازی را مدیریت می‌کند.
- فایل `app/models/integration.py` (در ساختار موجود است) مدل داده یکپارچه‌سازی را تعریف می‌کند.
- فایل `app/tasks.py` (در ساختار موجود است) وظایف Celery را مدیریت می‌کند و می‌تواند برای queue عملیات‌های ناموفق استفاده شود.
- فایل `app/celery_app.py` (در ساختار موجود است) برنامه Celery را پیکربندی می‌کند.
- فایل `app/database.py` (import شده در `app/main.py` خط 17) برای ذخیره‌سازی محلی خطاها استفاده می‌شود.
- فایل `frontend/src/lib/api.js` (در ساختار موجود است) کلاینت API فرانت‌اند را مدیریت می‌کند و می‌تواند fallback سمت کلاینت را پیاده‌سازی کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `app/services/google_api_service.py` که شامل:
   - تابع `call_with_retry(func, max_retries=3, base_delay=1.0)` با exponential backoff (delay = base_delay * 2^attempt)
   - کلاس `CircuitBreaker` با state (OPEN/CLOSED/HALF_OPEN)، failure_threshold، recovery_timeout
   - تابع `fallback_to_local_storage(data, key)` برای ذخیره‌سازی محلی خطاها
   - تابع `enqueue_failed_operation(operation_type, payload, error)` برای افزودن به صف Celery

2. ایجاد یک تابع در `app/services/integration_service.py` به نام `log_error_to_google_sheet(sheet_id, error_data)` که خطاها را در ستون 'Error Log' ثبت می‌کند.

3. به‌روزرسانی `app/routes/auth_google.py` برای استفاده از `call_with_retry` در فراخوانی‌های Google API.

4. ایجاد یک task Celery در `app/tasks.py` به نام `retry_failed_google_operations` که عملیات‌های صف‌شده را دوباره امتحان می‌کند.

5. به‌روزرسانی `frontend/src/lib/api.js` برای پیاده‌سازی fallback سمت کلاینت با localStorage.

6. افزودن ستون 'Error Log' به شمای Google Sheet در `app/services/integration_service.py`.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 13: بهینه‌سازی عملکرد: پیاده‌سازی کش (Caching) برای لینک‌های Google Drive

**Scope:** این مرحله شامل پیاده‌سازی یک لایه کش (cache) برای لینک‌های Google Drive است تا از فراخوانی مکرر API برای فایل‌های پراستفاده جلوگیری شود. می‌توان از Redis (اگر در دسترس است) یا یک کش درون‌حافظه‌ای (in-memory cache) با TTL (Time-To-Live) استفاده کرد. نکته حیاتی: کش باید به صورت خودکار invalidate شود اگر فایل در Google Drive حذف یا جابه‌جا شود.
**Key terms:** cache, Redis, in-memory cache, TTL, cache invalidation, performance

**بخش مربوط از متن کاربر:**
```
(نیاز به بهینه‌سازی برای سرعت دسترسی)
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی کش برای لینک‌های Google Drive

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/local_file_service.py:1-50` — `get_google_drive_link` — فایل سرویس فایل‌های محلی که احتمالاً شامل تابع دریافت لینک Google Drive است. باید متدهای کش به اینجا اضافه شود.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `app/routes/local_files.py:1-30` — `router` — روتر فایل‌های محلی که endpointهای مربوط به فایل‌ها را شامل می‌شود. باید endpoint جدید برای invalidate کش اضافه شود.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `app/config.py:1-50` — `settings` — فایل تنظیمات که شامل Redis URL و سایر تنظیمات است. باید تنظیمات TTL و سایز کش به اینجا اضافه شود.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/celery_app.py` (سطر 1) — برای وظایف پس‌زمینه مانند invalidate خودکار کش یا polling دوره‌ای
- `app/main.py` (سطر 190) — برای ثبت middleware یا startup event مربوط به کش
- `app/database.py` (سطر 1) — برای ذخیره‌سازی metadata فایل‌ها و ارتباط با کش
- `app/models/local_file_entry.py` (سطر 1) — مدل داده‌ای فایل‌های محلی که ممکن است نیاز به فیلدهای جدید برای کش داشته باشد
- `app/routes/__init__.py` (سطر 1) — برای اطمینان از import صحیح سرویس کش جدید

## 🌐 نقشهٔ وابستگی‌ها
این تغییرات بر چندین بخش پروژه تأثیر می‌گذارد: 1) `

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک لایه کش (Caching) برای لینک‌های Google Drive داده است تا از فراخوانی مکرر API برای فایل‌های پراستفاده جلوگیری شود. کلیدواژه‌های اصلی: cache, Redis, in-memory cache, TTL, cache invalidation, performance. کاربر اشاره کرده که می‌توان از Redis (اگر در دسترس است) یا یک کش درون‌حافظه‌ای (in-memory cache) با TTL (Time-To-Live) استفاده کرد. نکته حیاتی که کاربر تأکید کرده: کش باید به صورت خودکار invalidate شود اگر فایل در Google Drive حذف یا جابه‌جا شود. این درخواست با توجه به ساختار پروژه که شامل سرویس‌های مختلف (مانند app/services/local_file_service.py و app/routes/local_files.py) و استفاده از Redis در app/celery_app.py و app/config.py است، باید پیاده‌سازی شود. در کد فعلی، فایل app/services/local_file_service.py احتمالاً مسئول مدیریت فایل‌های محلی و لینک‌های Google Drive است. همچنین app/config.py شامل تنظیمات Redis است که می‌توان از آن برای کش استفاده کرد. فایل app/routes/local_files.py شامل endpointهای مربوط به فایل‌های محلی است که باید از کش بهره‌مند شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس کش جدید به نام `app/services/cache_service.py` با کلاس `GoogleDriveCache` که از Redis (اولویت) یا in-memory cache با TTL استفاده کند.
2. افزودن متدهای `get_cached_link(key: str) -> Optional[str]` و `set_cached_link(key: str, link: str, ttl: int = 3600)` و `invalidate_cache(key: str)`.
3. در `app/services/local_file_service.py`، متدهای فعلی که لینک‌های Google Drive را دریافت می‌کنند (مانند `get_google_drive_link`) را تغییر دهیم تا ابتدا کش را چک کنند.
4. پیاده‌سازی مکانیزم invalidate خودکار: با استفاده از webhookهای Google Drive یا polling دوره‌ای برای بررسی تغییرات فایل‌ها.
5. افزودن یک endpoint جدید در `app/routes/local_files.py` برای invalidate دستی کش (مثلاً `POST /api/local-files/cache/invalidate/{file_id}`).
6. به‌روزرسانی `app/config.py` برای افزودن تنظیمات مربوط به TTL پیش‌فرض و حداکثر سایز کش.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 14: افزودن قابلیت آرشیو خودکار فایل‌های قدیمی از Google Drive به Google Cloud Storage (Nearline/Coldline)

**Scope:** این مرحله شامل پیاده‌سازی یک سیاست چرخه حیات (lifecycle policy) برای فایل‌های Google Drive است. فایل‌هایی که بیش از ۶ ماه از آخرین دسترسی به آن‌ها می‌گذرد، به طور خودکار به Google Cloud Storage با کلاس Nearline یا Coldline منتقل می‌شوند (برای کاهش هزینه‌ها). یک رکورد از انتقال در Google Sheet با وضعیت 'Archived' ثبت می‌شود. نکته حیاتی: این مرحله نیاز به فعال‌سازی Google Cloud Storage API و تنظیم bucket permissions دارد.
**Key terms:** lifecycle policy, Google Cloud Storage, Nearline, Coldline, archive, bucket

**بخش مربوط از متن کاربر:**
```
اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن lifecycle policy آرشیو خودکار فایل‌های Google Drive به GCS

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/local_files.py:1-10` — `router` — این فایل endpointهای مدیریت فایل‌های محلی را دارد. باید endpoint جدیدی برای فعال‌سازی lifecycle policy و مشاهده وضعیت آرشیو به آن اضافه شود.
  ```python
  from fastapi import APIRouter, Depends, HTTPException, UploadFile, File, Form, Query
  from sqlalchemy.ext.asyncio import AsyncSession
  from typing import Optional, List
  from app.database import get_db
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  from app.services.local_file_service import *
  ```
- `app/services/local_file_service.py:1-10` — `service functions` — این سرویس باید توابع جدیدی برای تشخیص فایل‌های قدیمی (بیش از ۶ ماه) و فراخوانی سرویس آرشیو دریافت کند.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `app/tasks.py:1-10` — `Celery tasks` — یک تسک Celery جدید با نام `archive_old_files_task` باید اضافه شود که به صورت دوره‌ای (مثلاً هفتگی) اجرا شده و فایل‌های قدیمی را آرشیو کند.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/local_file_entry.py` — مدل ذخیره‌سازی فایل‌ها که باید فیلد `last_accessed_at` برای تشخیص فایل

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت آرشیو خودکار فایل‌های قدیمی از Google Drive به Google Cloud Storage (Nearline/Coldline) را دارد. این شامل پیاده‌سازی یک سیاست چرخه حیات (lifecycle policy) برای فایل‌های Google Drive است. فایل‌هایی که بیش از ۶ ماه از آخرین دسترسی به آن‌ها می‌گذرد، به طور خودکار به Google Cloud Storage با کلاس Nearline یا Coldline منتقل می‌شوند (برای کاهش هزینه‌ها). یک رکورد از انتقال در Google Sheet با وضعیت 'Archived' ثبت می‌شود. نکته حیاتی: این مرحله نیاز به فعال‌سازی Google Cloud Storage API و تنظیم bucket permissions دارد. کاربر در بخش دیگری اشاره کرده: 'اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو'. کلیدواژه‌ها: lifecycle policy, Google Cloud Storage, Nearline, Coldline, archive, bucket. در کد فعلی پروژه، فایل `app/routes/local_files.py` و `app/services/local_file_service.py` مسئول مدیریت فایل‌های محلی هستند اما هیچ ارتباطی با Google Drive یا Google Cloud Storage ندارند. فایل `app/services/integration_service.py` و `app/routes/integrations.py` زیرساخت integrations را فراهم می‌کنند که می‌توان از آن برای اتصال به Google Drive API استفاده کرد. فایل `app/models/local_file_entry.py` مدل ذخیره‌سازی فایل‌ها را تعریف می‌کند. هیچ کدی برای Google Drive API یا Google Cloud Storage در پروژه وجود ندارد و این یک قابلیت کاملاً جدید است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید `app/services/google_drive_service.py` برای اتصال به Google Drive API و لیست/دانلود فایل‌ها. 2. ایجاد یک سرویس جدید `app/services/google_cloud_storage_service.py` برای آپلود فایل‌ها به GCS bucket با کلاس Nearline/Coldline. 3. ایجاد یک مدل جدید `app/models/archive_record.py` برای ثبت رکوردهای آرشیو با وضعیت 'Archived'. 4. ایجاد یک تسک Celery در `app/tasks.py` با نام `archive_old_files_task` که به صورت دوره‌ای اجرا شود. 5. افزودن یک endpoint جدید در `app/routes/local_files.py` یا `app/routes/integrations.py` برای فعال‌سازی/پیکربندی lifecycle policy. 6. تنظیم Google Cloud Storage API و bucket permissions در مستندات. 7. ثبت رکورد انتقال در Google Sheet با استفاده از Google Sheets API.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 15: پیاده‌سازی قابلیت جستجوی پیشرفته با استفاده از Elasticsearch برای متن‌های استخراج‌شده

**Scope:** این مرحله شامل جایگزینی SQLite FTS5 با Elasticsearch برای جستجوی full-text قدرتمندتر است. Elasticsearch باید روی یک سرویس ابری (مثل Elastic Cloud) یا در Docker container مستقر شود. ایندکس‌ها باید شامل فیلدهای: file_name, extracted_text, file_type, upload_date باشند. نکته حیاتی: باید از bulk indexing برای ایندکس کردن دسته‌ای فایل‌های موجود استفاده شود.
**Key terms:** Elasticsearch, Elastic Cloud, Docker, index, bulk indexing, full-text search

**بخش مربوط از متن کاربر:**
```
اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
```

## 🎯 هدف (خلاصه ساختاریافته)
جایگزینی SQLite FTS5 با Elasticsearch برای جستجوی full-text پیشرفته

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/local_files.py:1-50` — `router` — این فایل endpointهای مدیریت فایل‌های محلی را دارد. باید یک endpoint جدید POST /api/local-files/search به آن اضافه شود.
  ```python
  from fastapi import APIRouter, Depends, HTTPException, UploadFile, File, Form, Query
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.services.local_file_service import LocalFileService
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  
  router = APIRouter(prefix="/api/local-files", tags=["local-files"])
  ```
- `app/services/local_file_service.py:1-30` — `LocalFileService` — این سرویس شامل توابع CRUD برای LocalFileEntry است. باید تابعی برای bulk indexing تمام فایل‌ها به Elasticsearch اضافه شود.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `app/models/local_file_entry.py:1-30` — `LocalFileEntry` — مدل LocalFileEntry شامل فیلدهای file_name, extracted_text, file_type, upload_date است که برای ایندکس Elasticsearch استفاده می‌شوند.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/config.py` — برای اضافه کردن تنظیمات Elasticsearch مانند ELASTICSEARCH_URL و ELASTICSEARCH_INDEX_NAME

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت جستجوی پیشرفته با استفاده از Elasticsearch برای متن‌های استخراج‌شده را دارد. این مرحله شامل جایگزینی SQLite FTS5 با Elasticsearch برای جستجوی full-text قدرتمندتر است. Elasticsearch باید روی یک سرویس ابری (مثل Elastic Cloud) یا در Docker container مستقر شود. ایندکس‌ها باید شامل فیلدهای: file_name, extracted_text, file_type, upload_date باشند. نکته حیاتی: باید از bulk indexing برای ایندکس کردن دسته‌ای فایل‌های موجود استفاده شود. کاربر اشاره کرده: "اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه". کلیدواژه‌ها: Elasticsearch, Elastic Cloud, Docker, index, bulk indexing, full-text search. در کد فعلی پروژه، فایل‌های مرتبط با مدیریت فایل‌های محلی در app/routes/local_files.py و app/services/local_file_service.py و app/models/local_file_entry.py وجود دارند. مدل LocalFileEntry در app/models/local_file_entry.py دارای فیلدهای file_name، extracted_text، file_type و upload_date است که دقیقاً با فیلدهای مورد نیاز برای ایندکس Elasticsearch مطابقت دارد. در حال حاضر، جستجوی full-text در پروژه وجود ندارد و SQLite FTS5 استفاده نمی‌شود (بررسی کد نشان می‌دهد که هیچ اشاره‌ای به FTS5 در فایل‌های deep-read شده وجود ندارد). بنابراین، این تسک اساساً افزودن قابلیت جستجوی جدید با Elasticsearch است، نه جایگزینی.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام app/services/elasticsearch_service.py که شامل توابع: create_index(index_name, mapping), bulk_index_documents(index_name, documents), search_index(index_name, query, fields) و delete_index(index_name) باشد. 2. در app/services/local_file_service.py، تابعی برای bulk indexing تمام فایل‌های موجود از دیتابیس به Elasticsearch اضافه شود (با استفاده از LocalFileEntry model). 3. در app/routes/local_files.py، یک endpoint جدید POST /api/local-files/search اضافه شود که query را دریافت کرده و از Elasticsearch برای جستجو در فیلدهای file_name, extracted_text, file_type, upload_date استفاده کند. 4. در app/main.py، در startup_event، یک تابع برای بررسی و ایجاد ایندکس Elasticsearch در صورت عدم وجود اضافه شود. 5. تنظیمات Elasticsearch (URL, index name) به app/config.py یا config/settings.py اضافه شود. 6. وابستگی elasticsearch-py به requirements.txt اضافه شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 16: مهاجرت داده‌های موجود از SQLite FTS5 به Elasticsearch

**Scope:** این مرحله شامل نوشتن یک اسکریپت یکباره (one-time migration script) است که تمام متن‌های استخراج‌شده و metadata موجود در SQLite FTS5 را خوانده و به Elasticsearch bulk index کند. اسکریپت باید قابلیت resume از نقطه قطع شده (checkpoint) را داشته باشد. نکته حیاتی: قبل از اجرا، یک backup از SQLite گرفته شود.
**Key terms:** migration script, SQLite FTS5, Elasticsearch, bulk index, checkpoint, backup

**بخش مربوط از متن کاربر:**
```
(مهاجرت داده‌های موجود)
```

## 🎯 هدف (خلاصه ساختاریافته)
اسکریپت مهاجرت یکباره SQLite FTS5 به Elasticsearch با قابلیت resume

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/scripts/migrate_fts5_to_es.py:1-150` — `new_file` — این فایل جدید باید ایجاد شود. مسیر پیشنهادی بر اساس ساختار پروژه (وجود پوشه scripts در ریشه نیست، ولی می‌توان ایجاد کرد).
- `app/services/local_file_service.py:1-50` — `LocalFileService` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. احتمالاً متدهایی برای خواندن داده‌های FTS5 دارد.
- `app/models/local_file_entry.py:1-30` — `LocalFileEntry` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مدل داده‌های فایل محلی را تعریف می‌کند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. کتابخانه‌های مرتبط: SQLite (از طریق SQLAlchemy)، Elasticsearch (جدید).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/local_files.py` (سطر 1) — این روتر endpointهای مربوط به فایل‌های محلی را ارائه می‌دهد و ممکن است پس از مهاجرت نیاز به تغییر داشته باشد تا از Elasticsearch بخواند.
- `app/database.py` (سطر 1) — احتمالاً اتصال به SQLite را مدیریت می‌کند و اسکریپت مهاجرت باید از آن استفاده کند.
- `app/config.py` (سطر 1) — تنظیمات Elasticsearch (URL، ایندکس) باید در این فایل یا یک فایل config جدید اضافه شود.

## 🌐 نقشهٔ وابستگی‌ها
این اسکریپت جدید به `app/models/local_file_entry.py` برای مدل داده، `app/database.py` برای اتصال SQLite، و یک کتابخانه جدید `elasticsearch-py` وابسته است. همچنین باید در `requirements.txt` اضافه شود. فایل `app/routes/local_files.py` پس از مهاجرت باید به‌روز شود تا از Elasticsearch به جای SQLite FTS5 بخواند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن یک اسکریپت یکباره (one-time migration script) داده است که تمام متن‌های استخراج‌شده و metadata موجود در SQLite FTS5 را خوانده و به Elasticsearch bulk index کند. اسکریپت باید قابلیت resume از نقطه قطع شده (checkpoint) را داشته باشد. نکته حیاتی: قبل از اجرا، یک backup از SQLite گرفته شود. کلیدواژه‌های اصلی: migration script, SQLite FTS5, Elasticsearch, bulk index, checkpoint, backup. در کد فعلی پروژه، فایل `app/services/local_file_service.py` (که در deep-read موجود نیست ولی در ساختار پروژه دیده می‌شود) احتمالاً مسئول مدیریت فایل‌های محلی و استخراج متن است. همچنین فایل `app/models/local_file_entry.py` مدل داده‌های فایل را تعریف می‌کند. فایل `app/routes/local_files.py` endpointهای مربوطه را ارائه می‌دهد. هیچ فایل deep-read شده‌ای مستقیماً با Elasticsearch مرتبط نیست، بنابراین اسکریپت جدید باید از صفر نوشته شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک اسکریپت جدید در مسیر `backend/scripts/migrate_fts5_to_es.py`. 2. اسکریپت باید از کتابخانه `elasticsearch-py` برای bulk indexing استفاده کند. 3. یک جدول checkpoint در SQLite (مثلاً `migration_checkpoint`) ایجاد کند که آخرین ID پردازش‌شده را ذخیره کند. 4. در شروع، checkpoint را بخواند و از آن نقطه ادامه دهد. 5. قبل از هر عملیات، از دیتابیس SQLite backup بگیرد (با `sqlite3.backup()`). 6. داده‌ها را از جدول FTS5 (احتمالاً `local_file_entry_fts`) بخواند و به Elasticsearch بفرستد. 7. پس از اتمام، لاگ موفقیت ثبت کند.

## 💡 نمونه‌های قبل/بعد
**نمونه کد اسکریپت مهاجرت (بخش checkpoint)**

_بعد:_
```
import sqlite3
import json
from elasticsearch import Elasticsearch, helpers

def get_checkpoint(db_path: str) -> int:
    conn = sqlite3.connect(db_path)
    cursor = conn.cursor()
    cursor.execute('CREATE TABLE IF NOT EXISTS migration_checkpoint (id INTEGER PRIMARY
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 17: افزودن قابلیت اشتراک‌گذاری فایل‌ها با لینک موقت (Temporary Share Links)

**Scope:** این مرحله شامل پیاده‌سازی یک سیستم اشتراک‌گذاری موقت است. کاربر می‌تواند برای هر فایل یک لینک موقت با تاریخ انقضا (مثلاً ۲۴ ساعت) و حداکثر تعداد دفعات مشاهده (مثلاً ۵ بار) ایجاد کند. لینک‌ها باید با JWT امضا شوند تا از دستکاری جلوگیری شود. نکته حیاتی: لینک‌های منقضی‌شده باید به طور خودکار پاکسازی شوند.
**Key terms:** temporary share link, expiration, JWT, access count, cleanup

**بخش مربوط از متن کاربر:**
```
قابل فراخوانی و دیدن و اینا هم باشه البته
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن سیستم اشتراک‌گذاری فایل با لینک موقت JWT

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/local_file_entry.py:1-50` — `LocalFileEntry` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مدل LocalFileEntry احتمالاً در این فایل تعریف شده است. باید مدل ShareLink را به آن اضافه کرد.
- `app/services/local_file_service.py:1-100` — `LocalFileService` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. منطق اصلی اشتراک‌گذاری (create_share_link, access_share_link, revoke_share_link) باید در این سرویس پیاده‌سازی شود.
- `app/routes/local_files.py:1-50` — `router` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. سه endpoint جدید باید به این روتر اضافه شود.
- `app/main.py:591-592` — `app.include_router(local_files.router)` — روتر local_files در main.py ثبت شده است. endpointهای جدید به طور خودکار در دسترس خواهند بود.
  ```python
  app.include_router(local_files.router)
  ```
- `app/tasks.py:1-50` — `celery_app` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. تسک پاکسازی خودکار لینک‌های منقضی باید در این فایل تعریف شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/config.py` (سطر 1) — برای خواندن JWT_SECRET_KEY و تنظیمات انقضا استفاده می‌شود.
- `app/services/crypt_service.py` (سطر 1) — برای امضای JWT و verify کردن آن استفاده می‌شود.
- `app/database.py` (سطر 1) — برای دسترسی به SessionLocal و engine برای عملیات دیتابیس استفاده می‌شود.
- `app/celery_app.py`

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت اشتراک‌گذاری فایل‌ها با لینک موقت (Temporary Share Links) داده است. این سیستم باید امکان ایجاد لینک موقت با تاریخ انقضا (مثلاً ۲۴ ساعت) و حداکثر تعداد دفعات مشاهده (مثلاً ۵ بار) را فراهم کند. لینک‌ها باید با JWT امضا شوند تا از دستکاری جلوگیری شود. نکته حیاتی: لینک‌های منقضی‌شده باید به طور خودکار پاکسازی شوند. کاربر همچنین اشاره کرده: "قابل فراخوانی و دیدن و اینا هم باشه البته". کلیدواژه‌های اصلی: temporary share link, expiration, JWT, access count, cleanup.

شواهد در کد واقعی پروژه:
- فایل `app/routes/local_files.py` وجود دارد (مسیر /api/local-files) که می‌توان endpoint جدید به آن اضافه کرد.
- فایل `app/services/local_file_service.py` وجود دارد که می‌توان منطق اشتراک‌گذاری را در آن پیاده‌سازی کرد.
- فایل `app/models/local_file_entry.py` وجود دارد که مدل دیتابیس فایل‌ها را تعریف می‌کند.
- فایل `app/config.py` و `app/core/config.py` برای تنظیمات JWT_SECRET_KEY و TIME_TO_LIVE استفاده می‌شوند.
- فایل `app/services/crypt_service.py` برای امضای JWT موجود است.
- فایل `app/routes/__init__.py` برای ثبت روترها استفاده می‌شود.
- فایل `app/main.py` (خطوط 591-592) روتر local_files را ثبت کرده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل جدید `ShareLink` در `app/models/local_file_entry.py` با فیلدهای: id, file_id (FK به LocalFileEntry), token (UUID), expires_at (datetime), max_access_count (int), current_access_count (int, default 0), created_at, is_revoked (boolean).
2. ایجاد جدول share_links در دیتابیس از طریق migration جدید در `migrations/versions/`.
3. افزودن تابع `create_share_link(file_id, expires_in_hours=24, max_access_count=5)` در `app/services/local_file_service.py` که:
   - یک JWT با payload شامل file_id, token, exp (زمان انقضا) و max_access_count امضا کند.
   - رکورد ShareLink را در دیتابیس ذخیره کند.
   - لینک موقت را برگرداند.
4. افزودن تابع `access_share_link(token)` در همان سرویس که:
   - JWT را verify کند.
   - رکورد ShareLink را از دیتابیس بخواند.
   - اگر منقضی شده یا revoked یا access_count >= max_access_count باشد، 403 برگرداند.
   - current_access_count را افزایش دهد.
   - فایل مربوطه را برگرداند.
5. افزودن endpoint جدید در `app/routes/local_files.py`:
   - POST /api/local-files/{file_id}/share-link (ایجاد لینک)
   - GET /api/local-files/share/{token} (دسترسی به فایل از طریق لینک)
   - DELETE /api/local-files/share/{token} (ابطال لینک)
6. پیاده‌سازی تسک پاکسازی خودکار در `app/tasks.py` با celery beat که هر ساعت اجرا شود و share_links منقضی‌شده را حذف کند.
7. ثبت تسک پاکسازی در `app/celery_app.py`.
8. افزودن دکمه "ایجاد لینک اشتراک" در فرانت‌اند (فایل‌های `frontend/src/pages/...` و `frontend/src/components/...`).
9. نمایش لینک موقت در UI و امکان کپی کردن آن.
10. صفحه نمایش فایل از طریق لینک موقت (احتمالاً یک صفحه ساده در فرانت‌اند).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 18: پیاده‌سازی قابلیت برچسب‌گذاری (Tagging) و دسته‌بندی فایل‌ها

**Scope:** این مرحله شامل افزودن قابلیت برچسب‌گذاری (tags) به فایل‌ها است. کاربر می‌تواند برای هر فایل چندین برچسب تعریف کند (مثلاً 'مهم', 'مالی', 'صورت‌جلسه'). برچسب‌ها در یک ستون جداگانه در Google Sheet ذخیره می‌شوند و در Elasticsearch نیز index می‌شوند. یک صفحه مدیریت برچسب‌ها برای ایجاد/ویرایش/حذف برچسب‌ها ایجاد می‌شود. نکته حیاتی: برچسب‌ها باید case-insensitive و بدون فاصله اضافی ذخیره شوند.
**Key terms:** tagging, tags, Google Sheet, Elasticsearch, tag management

**بخش مربوط از متن کاربر:**
```
(نیاز به دسته‌بندی برای مدیریت بهتر)
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت برچسب‌گذاری (Tagging) به فایل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/local_file_entry.py:1-30` — `LocalFileEntry` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، این مدل در app/models/local_file_entry.py است.
  ```python
  مدل فعلی LocalFileEntry که باید فیلد tags به آن اضافه شود.
  ```
- `app/routes/local_files.py:1-50` — `router` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، این فایل شامل endpointهای CRUD برای فایل‌ها است.
  ```python
  روتر فعلی local_files که باید endpointهای مدیریت برچسب به آن اضافه شود.
  ```
- `app/services/local_file_service.py:1-30` — `LocalFileService` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، این سرویس وظیفه مدیریت فایل‌ها را دارد.
  ```python
  سرویس فعلی local_file_service که باید منطق ذخیره برچسب‌ها در Google Sheet و Elasticsearch به آن اضافه شود.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/integration_service.py` (سطر 1) — این سرویس وظیفه مدیریت Google Sheet را دارد و برای ذخیره برچسب‌ها در ستون جداگانه باید به‌روزرسانی شود.
- `app/services/ai/nlp_service.py` (سطر 1) — این سرویس برای index کردن در Elasticsearch استفاده می‌شود و باید قابلیت index کردن برچسب‌ها به آن اضافه شود.
- `app/schemas/local_file_entry_schema.py` (سطر 1) — این فایل شامل Pydantic schemaهای مربوط به LocalFileEntry است و باید فیلد tags به آن اضافه شود.
- `frontend/src/lib/api.js`

## 🔍 Context و وضعیت فعلی
پیاده‌سازی قابلیت برچسب‌گذاری (Tagging) و دسته‌بندی فایل‌ها. این مرحله شامل افزودن قابلیت برچسب‌گذاری (tags) به فایل‌ها است. کاربر می‌تواند برای هر فایل چندین برچسب تعریف کند (مثلاً 'مهم', 'مالی', 'صورت‌جلسه'). برچسب‌ها در یک ستون جداگانه در Google Sheet ذخیره می‌شوند و در Elasticsearch نیز index می‌شوند. یک صفحه مدیریت برچسب‌ها برای ایجاد/ویرایش/حذف برچسب‌ها ایجاد می‌شود. نکته حیاتی: برچسب‌ها باید case-insensitive و بدون فاصله اضافی ذخیره شوند. کلیدواژه‌ها: tagging, tags, Google Sheet, Elasticsearch, tag management. بر اساس کد موجود، فایل‌های مرتبط با مدیریت فایل‌ها در app/routes/local_files.py و app/services/local_file_service.py قرار دارند. مدل LocalFileEntry در app/models/local_file_entry.py تعریف شده است. همچنین مسیر Google Sheet در app/services/integration_service.py و app/services/integrations/external_project_interface.py مدیریت می‌شود. برای Elasticsearch، از app/services/ai/nlp_service.py می‌توان برای index کردن استفاده کرد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلد tags (JSONB یا ARRAY of TEXT) به مدل LocalFileEntry در app/models/local_file_entry.py. 2. ایجاد جدول/مدل جداگانه Tag در app/models/ برای مدیریت برچسب‌ها با قابلیت case-insensitive. 3. افزودن endpointهای CRUD برای برچسب‌ها در app/routes/local_files.py (مثلاً POST /api/files/{id}/tags, DELETE /api/files/{id}/tags/{tag}). 4. به‌روزرسانی سرویس local_file_service.py برای ذخیره برچسب‌ها در Google Sheet (ستون جداگانه) و Elasticsearch. 5. ایجاد صفحه مدیریت برچسب‌ها در frontend (احتمالاً در frontend/src/pages/ یا کامپوننت جدید). 6. پیاده‌سازی نرمال‌سازی برچسب‌ها (lowercase, strip) در لایه سرویس. 7. افزودن قابلیت جستجوی فایل‌ها بر اساس برچسب در endpointهای GET.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 19: افزودن قابلیت نمایش آمار و داشبورد (Dashboard) از داده‌های منتقل‌شده

**Scope:** این مرحله شامل ایجاد یک صفحه داشبورد با آمار زیر است: تعداد کل فایل‌های منتقل‌شده, حجم کل داده‌های منتقل‌شده, تفکیک بر اساس نوع فایل (صوتی/تصویری/متنی), تعداد فایل‌های آرشیو شده در Google Cloud Storage, و نمودار روند انتقال در هفته/ماه. داده‌ها از Google Sheet و Elasticsearch جمع‌آوری می‌شوند. نکته حیاتی: آمار باید به صورت real-time یا near-real-time به‌روز شود.
**Key terms:** dashboard, statistics, chart, real-time, Google Sheet, Elasticsearch

**بخش مربوط از متن کاربر:**
```
(نیاز به داشبورد برای مدیریت)
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن صفحه داشبورد آمار انتقال فایل با داده‌های real-time از Google Sheet و Elasticsearch

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/Dashboard.jsx:25-62` — `Dashboard` — این کامپوننت اصلی Dashboard است که باید برای نمایش آمار انتقال فایل توسعه یابد. در حال حاضر فقط آمار tasks و projects را نشان می‌دهد.
  ```jsx
  function Dashboard() {
    const [stats, setStats] = useState({ tasks: 0, projects: 0, completed: 0 });
    const [loading, setLoading] = useState(true);
    const [apiStatus, setApiStatus] = useState('checking');
  
    useEffect(() => {
      const fetchStats = async () => {
        try {
          const [tasksRes, projectsRes] = await Promise.all([
            fetch(`${API_BASE}/tasks`),
            fetch(`${API_BASE}/projects`),
          ]);
  ```
- `app/routes/local_files.py:1-30` — `router`
  ```python
  from fastapi import APIRouter, Depends, HTTPException, status, Request
  from sqlalchemy.ext.asyncio import AsyncSession
  from typing import
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت نمایش آمار و داشبورد (Dashboard) از داده‌های منتقل‌شده را دارد. این شامل ایجاد یک صفحه داشبورد با آمار زیر است: تعداد کل فایل‌های منتقل‌شده, حجم کل داده‌های منتقل‌شده, تفکیک بر اساس نوع فایل (صوتی/تصویری/متنی), تعداد فایل‌های آرشیو شده در Google Cloud Storage, و نمودار روند انتقال در هفته/ماه. داده‌ها از Google Sheet و Elasticsearch جمع‌آوری می‌شوند. نکته حیاتی: آمار باید به صورت real-time یا near-real-time به‌روز شود. کلیدواژه‌ها: dashboard, statistics, chart, real-time, Google Sheet, Elasticsearch. در کد فعلی پروژه، صفحه Dashboard.jsx در frontend/src/pages/Dashboard.jsx وجود دارد که از endpointهای /api/tasks و /api/projects برای نمایش آمار وظایف و پروژه‌ها استفاده می‌کند (خطوط 33-35). همچنین در app/main.py مسیر /api/oversight/status (خط 544) برای وضعیت feature flagها وجود دارد. اما هیچ endpoint یا سرویسی برای آمار انتقال فایل، Google Sheet یا Elasticsearch در کد فعلی وجود ندارد. پروژه از stack fastapi, react, vite استفاده می‌کند. فایل‌های مرتبط با انتقال فایل شامل app/routes/local_files.py و app/services/local_file_service.py هستند. همچنین app/services/integration_service.py و app/routes/integrations.py برای یکپارچه‌سازی با سرویس‌های خارجی وجود دارند. برای پیاده‌سازی این درخواست، نیاز به ایجاد endpointهای جدید API برای جمع‌آوری آمار از Google Sheet و Elasticsearch، ایجاد سرویس‌های backend برای پردازش داده‌ها، و توسعه کامپوننت Dashboard در frontend برای نمایش نمودارها و آمار real-time است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد سرویس جدید app/services/transfer_stats_service.py برای جمع‌آوری آمار انتقال فایل از Google Sheet و Elasticsearch. 2. ایجاد endpoint جدید در app/routes/local_files.py با مسیر /api/transfer-stats که آمار را به صورت JSON برمی‌گرداند. 3. به‌روزرسانی frontend/src/pages/Dashboard.jsx برای فراخوانی endpoint جدید و نمایش آمار با استفاده از کتابخانه نمودار (مثلاً Chart.js یا Recharts). 4. افزودن قابلیت real-time با استفاده از polling (setInterval در frontend) یا WebSocket. 5. ایجاد مدل‌های جدید در app/models/ برای ذخیره آمار انتقال فایل در صورت نیاز. 6. به‌روزرسانی تست‌های موجود frontend/src/components/__tests__/Dashboard.test.jsx.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 20: نوشتن مستندات فنی (Technical Documentation) و راهنمای کاربر (User Guide)

**Scope:** این مرحله شامل نوشتن مستندات کامل برای توسعه‌دهندگان و کاربران نهایی است. مستندات فنی شامل: معماری سیستم, نحوه راه‌اندازی (setup), متغیرهای محیطی, API reference, و توضیح ماژول‌ها. راهنمای کاربر شامل: نحوه آپلود فایل, جستجو, برچسب‌گذاری, اشتراک‌گذاری, و مشاهده آمار. نکته حیاتی: مستندات باید به زبان فارسی (برای کاربران) و انگلیسی (برای توسعه‌دهندگان) نوشته شوند.
**Key terms:** technical documentation, user guide, API reference, setup guide, Persian, English

**بخش مربوط از متن کاربر:**
```
(نیاز به مستندات برای استفاده)
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن مستندات فنی و راهنمای کاربر دو زبانه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:42-627` — `app (FastAPI instance)`
  ```python
  app = FastAPI(title="Lifemanager API", version="0.1.0")
  
  # --- CORS --------------------------------------------------------------------
  # Strict allowlist driven by the ALLOWED_ORIGINS env var...
  
  app.add_middleware(StrictCORSMiddleware)
  
  # --- Rate limiting -----------------------------------------------------------
  app.state.limiter = limiter
  app.add_middleware(SlowAPIMiddleware)
  
  # Include routers.
  app.include_router(auth.router, tags=["auth"])
  app.include_router(tasks.router)
  app.include_router(projects.router)
  app.include_router(notifications.router, prefix="/notifications", tags=["notifications"])
  app.include_router(notifications.api_router, tags=["notifications"])
  app.include_router(ai.router, tags=["ai"])
  app.include_router(users.router, prefix="/users", tags=["users"])
  app.include_router(users.api_router, tags=["users"])
  app.include_router(integrations.router, prefix="/integrations", tags=["integrations"])
  app.include_router(lists.router)
  app.include_router(todo_items.router)
  app.include_router(local_files.router)
  app.include_router(person.router)
  app.include_router(finance.router)
  app.include_router(location.router)
  app.include_router(webhook.router, tags=["webhook"])
  app.include_router(planner.router
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن مستندات فنی (Technical Documentation) و راهنمای کاربر (User Guide) برای پروژه Lifemanager را دارد. این درخواست شامل دو بخش اصلی است: ۱) مستندات فنی برای توسعه‌دهندگان شامل معماری سیستم، نحوه راه‌اندازی (setup)، متغیرهای محیطی، API reference، و توضیح ماژول‌ها. ۲) راهنمای کاربر برای کاربران نهایی شامل نحوه آپلود فایل، جستجو، برچسب‌گذاری، اشتراک‌گذاری، و مشاهده آمار. نکته حیاتی که کاربر تأکید کرده: مستندات باید به زبان فارسی (برای کاربران) و انگلیسی (برای توسعه‌دهندگان) نوشته شوند. کلیدواژه‌های اصلی: technical documentation, user guide, API reference, setup guide, Persian, English. بر اساس کد واقعی پروژه، فایل‌های کلیدی که باید مستند شوند عبارتند از: app/main.py (شامل CORS middleware، rate limiting، health endpoints، و startup event با migration‌های مختلف)، frontend/index.html (شامل Inspector Bridge Script برای WebSocket و network monitoring)، app/routes/auth.py (endpoint‌های احراز هویت)، app/routes/lists.py (سیستم todo-list)، app/routes/self_improvement.py (ماژول خودسازی)، app/routes/location.py (مکانیابی)، app/routes/finance.py (مدیریت مالی)، app/routes/ai.py (هوش مصنوعی)، app/routes/notifications.py (اعلان‌ها)، و app/routes/tasks.py (وظایف). همچنین فایل‌های config/settings.py و app/config.py برای متغیرهای محیطی، و migrations/ برای تاریخچه تغییرات دیتابیس. Stack تشخیص داده شده: fastapi, react, vite. فایل‌های requirements.txt و package.json برای وابستگی‌ها.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد پوشه docs/technical/ برای مستندات فنی انگلیسی و docs/user-guide/ برای راهنمای کاربر فارسی. ۲. نوشتن فایل docs/technical/ARCHITECTURE.md شامل معماری سیستم با استناد به app/main.py (خطوط 42-627 برای routerها و middlewareها)، app/database.py (اتصال دیتابیس)، و app/models/ (مدل‌های داده). ۳. نوشتن docs/technical/SETUP.md شامل نحوه راه‌اندازی با Docker (docker-compose.yml)، متغیرهای محیطی از .env.example و config/settings.py، و دستورات build از package.json. ۴. نوشتن docs/technical/API_REFERENCE.md با استناد به تمام routerهای موجود در app/main.py خطوط 573-616 (auth, tasks, projects, notifications, ai, users, integrations, lists, todo_items, local_files, person, finance, location, webhook, planner, self_improvement). ۵. نوشتن docs/technical/MODULES.md شامل توضیح ماژول‌های اصلی: self_improvement (app/services/self_improvement_service.py و app/services/_self_improvement_seed_data.py)، todo-list (app/services/list_service.py و app/services/_todo_seed_data.py)، AI (app/services/ai/ شامل model_service.py, nlp_service.py, image_service.py, provider_service.py)، location (app/routes/location.py و app/models/user_location.py)، finance (app/routes/finance.py و app/models/finance.py). ۶. نوشتن docs/user-guide/README.md به فارسی شامل نحوه آپلود فایل (app/routes/local_files.py و app/services/local_file_service.py)، جستجو (از طریق frontend/src/pages/)، برچسب‌گذاری (احتمالاً از طریق todo-list system)، اشتراک‌گذاری (از طریق app/routes/lists.py با endpointهای share/unshare)، و مشاهده آمار (از طریق app/routes/self_improvement.py با endpoint overview). ۷. به‌روزرسانی README.md اصلی برای لینک به مستندات جدید.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: docs
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': 'GET /api/files با هدر Authorization معتبر باید لیست فایل\u200cهای کاربر را با status 200 برگرداند', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/files', 'expected_status': 200, 'required_fields': ['id', 'filename', 'drive_url', 'synced_at']}}
- [ ] {'text': 'GET /api/files بدون هدر Authorization باید status 401 برگرداند', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/files', 'expected_status': 401, 'required_fields': ['detail']}}
- [ ] {'text': 'GET /api/files/{file_id} با file_id معتبر باید اطلاعات فایل را با status 200 برگرداند', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/files/1', 'expected_status': 200, 'required_fields': ['id', 'filename', 'drive_url']}}
- [ ] {'text': 'GET /api/files/{file_id} با file_id نامعتبر باید status 404 برگرداند', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/files/99999', 'expected_status': 404, 'required_fields': ['detail']}}
- [ ] {'text': 'مدل LocalFileEntry باید فیلدهای drive_file_id, drive_url, synced_at را داشته باشد', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['drive_file_id', 'drive_url', 'synced_at'], 'files_hint': ['app/models/local_file_entry.py']}}
- [ ] {'text': 'کامپوننت DriveFiles در فرانت\u200cاند باید لیست فایل\u200cها را نمایش دهد و با کلیک روی هر فایل، لینک Google Drive باز شود', 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/drive-files'}, {'action': 'wait_for', 'selector': "[data-testid='file-list']", 'timeout_ms': 5000}, {'action': 'assert_visible', 'selector': "[data-testid='file-item']"}, {'action': 'click', 'selector': "[data-testid='file-item']:first-child"}, {'action': 'wait_for', 'selector': "[data-testid='file-detail']", 'timeout_ms': 3000}]}}

## Acceptance Criteria

1. GET /api/files با هدر Authorization معتبر باید لیست فایل‌های کاربر را با status 200 برگرداند _(verify: api_response)_
2. GET /api/files بدون هدر Authorization باید status 401 برگرداند _(verify: api_response)_
3. GET /api/files/{file_id} با file_id معتبر باید اطلاعات فایل را با status 200 برگرداند _(verify: api_response)_
4. GET /api/files/{file_id} با file_id نامعتبر باید status 404 برگرداند _(verify: api_response)_
5. مدل LocalFileEntry باید فیلدهای drive_file_id, drive_url, synced_at را داشته باشد _(verify: static)_
6. کامپوننت DriveFiles در فرانت‌اند باید لیست فایل‌ها را نمایش دهد و با کلیک روی هر فایل، لینک Google Drive باز شود _(verify: ui_interaction)_

## Task Steps

### Step 1: ایجاد ساختار فولدر اصلی در Google Drive و تعریف Google Sheet مرکزی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک فولدر اصلی (root folder) در Google Drive برای ذخیره تمام داده‌های غیرضروری و فایل‌های رسانه‌ای است. همچنین یک Google Sheet به عنوان شیت مرکزی (master sheet) برای ثبت و رهگیری تمام داده‌ها و فایل‌های منتقل‌شده ایجاد می‌شود. این مرحله فقط به ایجاد ساختار اولیه می‌پردازد و شامل منطق انتقال داده یا API calls نیست. نکته حیاتی: نام فولدر و شیت باید در متغیرهای محیطی (environment variables) ذخیره شوند تا در مراحل بعدی قابل استفاده باشند.
**Excerpt:**
```
می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه و تمام داده‌های اصل داده‌ها اونجا ذخیره بشه
```

### Step 2: پیاده‌سازی احراز هویت Google Drive و Google Sheets API در برنامه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تنظیم و پیاده‌سازی OAuth 2.0 برای دسترسی به Google Drive و Google Sheets APIs است. باید از Google Cloud Console یک پروژه ایجاد شود، credentials (Client ID و Client Secret) دریافت شود، و کتابخانه‌های مربوطه (google-api-python-client, google-auth-oauthlib) نصب شوند. نکته حیاتی: توکن احراز هویت باید به صورت امن ذخیره شود (مثلاً در متغیر محیطی یا فایل token.json) و قابلیت refresh خودکار داشته باشد.
**Excerpt:**
```
پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه
```

### Step 3: طراحی و پیاده‌سازی مدل داده برای رهگیری فایل‌ها و داده‌های منتقل‌شده در Google Sheet
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی ساختار (schema) Google Sheet مرکزی است. ستون‌های مورد نیاز عبارتند از: ID (شناسه یکتا), FileName (نام فایل), FileType (نوع فایل: صوتی، تصویری، متنی), GoogleDriveLink (لینک فایل در Google Drive), OriginalDBLocation (محل اصلی در پایگاه داده Render), TransferDate (تاریخ انتقال), Status (وضعیت: active/archived), و Notes. همچنین یک تابع برای افزودن ردیف جدید به شیت پیاده‌سازی می‌شود.
**Excerpt:**
```
توی شیت باید همه چیزا ثبت بشه
```

### Step 4: پیاده‌سازی منطق آپلود فایل به Google Drive و ثبت در Google Sheet
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تابعی است که یک فایل (صوتی، تصویری، یا متنی) را از مسیر محلی یا URL دریافت کرده، آن را به فولدر اصلی Google Drive آپلود می‌کند، لینک اشتراک‌گذاری (shareable link) تولید می‌کند، و سپس یک ردیف جدید در Google Sheet با اطلاعات فایل ثبت می‌کند. نکته حیاتی: فایل‌ها باید با permission 'Anyone with the link can view' آپلود شوند تا در برنامه قابل فراخوانی باشند.
**Excerpt:**
```
مثلاً صوت‌هایی که یه وقت می‌فرستم عکسا فلان چیزا اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو
```

### Step 5: پیاده‌سازی منطق استخراج متن از فایل‌های صوتی و تصویری (OCR/ASR)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل یکپارچه‌سازی یک سرویس تبدیل گفتار به متن (Speech-to-Text) مانند Google Cloud Speech-to-Text یا Whisper برای فایل‌های صوتی، و یک سرویس تشخیص متن (OCR) مانند Google Cloud Vision یا Tesseract برای تصاویر است. متن استخراج‌شده باید در یک فایل متنی (.txt) ذخیره شده و همراه با فایل اصلی به Google Drive آپلود شود. نکته حیاتی: اگر سرویس ابری استفاده می‌شود، باید هزینه‌ها و محدودیت‌های API در نظر گرفته شود.
**Excerpt:**
```
اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
```

### Step 6: طراحی و پیاده‌سازی منطق تصمیم‌گیری برای انتقال داده‌ها (Data Tiering Logic)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک سیستم رتبه‌بندی (tiering system) برای داده‌ها است. داده‌ها بر اساس معیارهایی مانند: تعداد دفعات دسترسی (access frequency), حجم داده, و重要性 (criticality) به سه دسته تقسیم می‌شوند: Hot (در Render بماند), Warm (در Render بماند اما با اولویت کمتر), Cold (به Google Drive منتقل شود). یک تابع schedule (مثلاً cron job) برای بررسی دوره‌ای داده‌ها و انتقال خودکار داده‌های Cold به Google Drive پیاده‌سازی می‌شود.
**Excerpt:**
```
می‌خوام چیزایی که خیلی ضروریه اونجا باشه و بقیه موارد منتقل بشه به گوگل درایو ... تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو
```

### Step 7: ایجاد API endpoint برای فراخوانی و مشاهده فایل‌های منتقل‌شده به Google Drive از داخل برنامه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک API endpoint (مثلاً GET /api/files/{file_id}) است که با دریافت شناسه فایل از Google Sheet، لینک Google Drive را بازیابی کرده و فایل را به صورت مستقیم (stream) یا از طریق redirect به کاربر نمایش می‌دهد. همچنین یک endpoint لیست (GET /api/files) برای نمایش تمام فایل‌های منتقل‌شده با قابلیت جستجو و فیلتر پیاده‌سازی می‌شود. نکته حیاتی: دسترسی به این endpointها باید احراز هویت شود.
**Excerpt:**
```
باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه
```

### Step 8: طراحی و پیاده‌سازی رابط کاربری (UI) برای مدیریت و مشاهده داده‌های منتقل‌شده
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد صفحات frontend برای نمایش لیست فایل‌های منتقل‌شده (با قابلیت جستجو، فیلتر بر اساس نوع فایل و تاریخ)، نمایش جزئیات هر فایل (نام، نوع، تاریخ انتقال، لینک)، و دکمه‌های عملیات (مشاهده، دانلود، حذف از Google Drive). همچنین یک بخش برای نمایش وضعیت انتقال‌های خودکار (آخرین زمان اجرا، تعداد فایل‌های منتقل‌شده) اضافه می‌شود. نکته حیاتی: UI باید responsive باشد و در موبایل هم کار کند.
**Excerpt:**
```
باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه
```

### Step 9: پیاده‌سازی قابلیت جستجوی پیشرفته در داده‌های منتقل‌شده (شامل متن استخراج‌شده)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت جستجوی full-text بر روی متن‌های استخراج‌شده از فایل‌های صوتی و تصویری است. متن‌های استخراج‌شده (که در فایل‌های .txt در Google Drive ذخیره شده‌اند) باید index شده و قابل جستجو باشند. می‌توان از Elasticsearch یا یک راه‌حل ساده‌تر مانند SQLite FTS5 برای این کار استفاده کرد. نکته حیاتی: جستجو باید همزمان روی نام فایل و متن استخراج‌شده انجام شود.
**Excerpt:**
```
اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
```

### Step 10: نوشتن تست‌های واحد (Unit Tests) برای منطق انتقال داده و API endpoints
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای ماژول‌های کلیدی است: تابع آپلود به Google Drive (با mock کردن API calls), تابع استخراج متن (با mock کردن سرویس‌های OCR/ASR), منطق تصمیم‌گیری انتقال (data tiering logic), و API endpoints (با استفاده از TestClient). حداقل ۸۰% پوشش کد (code coverage) برای این ماژول‌ها هدفگذاری شود. نکته حیاتی: تست‌ها نباید به سرویس‌های واقعی Google متصل شوند — همه چیز mock شود.
**Excerpt:**
```
(نیاز به تست برای اطمینان از صحت عملکرد)
```

### Step 11: نوشتن تست‌های یکپارچه‌سازی (Integration Tests) با سرویس‌های واقعی Google
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌هایی است که با سرویس‌های واقعی Google Drive و Sheets ارتباط برقرار می‌کنند (با استفاده از یک حساب تستی). این تست‌ها عملیات واقعی آپلود، خواندن، و حذف فایل‌ها را انجام می‌دهند. یک فولدر تستی جداگانه در Google Drive برای این منظور ایجاد می‌شود که پس از اتمام تست‌ها پاک می‌شود. نکته حیاتی: این تست‌ها باید با یک flag (مثلاً --run-integration) از تست‌های واحد جدا شوند.
**Excerpt:**
```
(نیاز به تست یکپارچه‌سازی برای اطمینان از ارتباط صحیح با Google APIs)
```

### Step 12: پیاده‌سازی مکانیزم fallback و error handling برای خطاهای Google API
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک سیستم مدیریت خطا برای زمان‌هایی که Google API در دسترس نیست یا با محدودیت نرخ (rate limit) مواجه می‌شود. باید از تکنیک‌هایی مانند retry with exponential backoff, circuit breaker, و fallback به ذخیره‌سازی محلی (local storage) استفاده شود. همچنین یک صف (queue) برای عملیات‌های ناموفق ایجاد می‌شود که بعداً دوباره امتحان شوند. نکته حیاتی: خطاها باید در Google Sheet (در یک ستون 'Error Log') ثبت شوند.
**Excerpt:**
```
(نیاز به مدیریت خطا برای اطمینان از پایداری)
```

### Step 13: بهینه‌سازی عملکرد: پیاده‌سازی کش (Caching) برای لینک‌های Google Drive
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک لایه کش (cache) برای لینک‌های Google Drive است تا از فراخوانی مکرر API برای فایل‌های پراستفاده جلوگیری شود. می‌توان از Redis (اگر در دسترس است) یا یک کش درون‌حافظه‌ای (in-memory cache) با TTL (Time-To-Live) استفاده کرد. نکته حیاتی: کش باید به صورت خودکار invalidate شود اگر فایل در Google Drive حذف یا جابه‌جا شود.
**Excerpt:**
```
(نیاز به بهینه‌سازی برای سرعت دسترسی)
```

### Step 14: افزودن قابلیت آرشیو خودکار فایل‌های قدیمی از Google Drive به Google Cloud Storage (Nearline/Coldline)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک سیاست چرخه حیات (lifecycle policy) برای فایل‌های Google Drive است. فایل‌هایی که بیش از ۶ ماه از آخرین دسترسی به آن‌ها می‌گذرد، به طور خودکار به Google Cloud Storage با کلاس Nearline یا Coldline منتقل می‌شوند (برای کاهش هزینه‌ها). یک رکورد از انتقال در Google Sheet با وضعیت 'Archived' ثبت می‌شود. نکته حیاتی: این مرحله نیاز به فعال‌سازی Google Cloud Storage API و تنظیم bucket permissions دارد.
**Excerpt:**
```
اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو
```

### Step 15: پیاده‌سازی قابلیت جستجوی پیشرفته با استفاده از Elasticsearch برای متن‌های استخراج‌شده
**Status:** `pending` (0%)
**Scope:** این مرحله شامل جایگزینی SQLite FTS5 با Elasticsearch برای جستجوی full-text قدرتمندتر است. Elasticsearch باید روی یک سرویس ابری (مثل Elastic Cloud) یا در Docker container مستقر شود. ایندکس‌ها باید شامل فیلدهای: file_name, extracted_text, file_type, upload_date باشند. نکته حیاتی: باید از bulk indexing برای ایندکس کردن دسته‌ای فایل‌های موجود استفاده شود.
**Excerpt:**
```
اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه
```

### Step 16: مهاجرت داده‌های موجود از SQLite FTS5 به Elasticsearch
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن یک اسکریپت یکباره (one-time migration script) است که تمام متن‌های استخراج‌شده و metadata موجود در SQLite FTS5 را خوانده و به Elasticsearch bulk index کند. اسکریپت باید قابلیت resume از نقطه قطع شده (checkpoint) را داشته باشد. نکته حیاتی: قبل از اجرا، یک backup از SQLite گرفته شود.
**Excerpt:**
```
(مهاجرت داده‌های موجود)
```

### Step 17: افزودن قابلیت اشتراک‌گذاری فایل‌ها با لینک موقت (Temporary Share Links)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک سیستم اشتراک‌گذاری موقت است. کاربر می‌تواند برای هر فایل یک لینک موقت با تاریخ انقضا (مثلاً ۲۴ ساعت) و حداکثر تعداد دفعات مشاهده (مثلاً ۵ بار) ایجاد کند. لینک‌ها باید با JWT امضا شوند تا از دستکاری جلوگیری شود. نکته حیاتی: لینک‌های منقضی‌شده باید به طور خودکار پاکسازی شوند.
**Excerpt:**
```
قابل فراخوانی و دیدن و اینا هم باشه البته
```

### Step 18: پیاده‌سازی قابلیت برچسب‌گذاری (Tagging) و دسته‌بندی فایل‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت برچسب‌گذاری (tags) به فایل‌ها است. کاربر می‌تواند برای هر فایل چندین برچسب تعریف کند (مثلاً 'مهم', 'مالی', 'صورت‌جلسه'). برچسب‌ها در یک ستون جداگانه در Google Sheet ذخیره می‌شوند و در Elasticsearch نیز index می‌شوند. یک صفحه مدیریت برچسب‌ها برای ایجاد/ویرایش/حذف برچسب‌ها ایجاد می‌شود. نکته حیاتی: برچسب‌ها باید case-insensitive و بدون فاصله اضافی ذخیره شوند.
**Excerpt:**
```
(نیاز به دسته‌بندی برای مدیریت بهتر)
```

### Step 19: افزودن قابلیت نمایش آمار و داشبورد (Dashboard) از داده‌های منتقل‌شده
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک صفحه داشبورد با آمار زیر است: تعداد کل فایل‌های منتقل‌شده, حجم کل داده‌های منتقل‌شده, تفکیک بر اساس نوع فایل (صوتی/تصویری/متنی), تعداد فایل‌های آرشیو شده در Google Cloud Storage, و نمودار روند انتقال در هفته/ماه. داده‌ها از Google Sheet و Elasticsearch جمع‌آوری می‌شوند. نکته حیاتی: آمار باید به صورت real-time یا near-real-time به‌روز شود.
**Excerpt:**
```
(نیاز به داشبورد برای مدیریت)
```

### Step 20: نوشتن مستندات فنی (Technical Documentation) و راهنمای کاربر (User Guide)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن مستندات کامل برای توسعه‌دهندگان و کاربران نهایی است. مستندات فنی شامل: معماری سیستم, نحوه راه‌اندازی (setup), متغیرهای محیطی, API reference, و توضیح ماژول‌ها. راهنمای کاربر شامل: نحوه آپلود فایل, جستجو, برچسب‌گذاری, اشتراک‌گذاری, و مشاهده آمار. نکته حیاتی: مستندات باید به زبان فارسی (برای کاربران) و انگلیسی (برای توسعه‌دهندگان) نوشته شوند.
**Excerpt:**
```
(نیاز به مستندات برای استفاده)
```
