---
task_id: e606cca6-409a-4d32-80d7-e950ed124f16
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (8 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T21:04:53.025117+00:00'
updated_at: '2026-05-28T19:43:51.608351+00:00'
target_files:
- backend/app/services/ai_service.py
- backend/app/models/ai_model.py
- frontend/src/pages/SettingsPage.tsx
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (8 مرحله)

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_249747_AgADqR0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد • 5,292 char متن_

## مفهوم اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم، باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه بهم بگن. یعنی خیلی باید تو هر آنالیزی که دارن و هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.

## ویژگی‌های اصلی و عملکرد  _(at: audio/ogg)_

[00:00] مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه میشه باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشند و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستند و فیدبکی که میدن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت میشه بهم بگن یعنی خیلی باید تو هر آنالیزی که دارن تو هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار میدم.

## هدف و ارزش پیشنهادی اپلیکیشن  _(at: audio/ogg)_

[00:00] مدل‌هایی که در صفحه تنظیمات قرار میدم و براشون پرامپت می‌نویسم
[00:07] باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارن
[00:15] و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون
[00:20] درخواست باشه
[00:22] و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه میشه
[00:34] باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون
[00:41] چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در
[00:47] پیام‌هایی که برام می‌فرستن و فیدبکی که میدن حالا چه توی اعلانات
[00:52] یا توی اونجایی که مثلاً ثبت میشه
[00:56] بهم بگن. یعنی
[00:58] خیلی باید تو هر آنالیزی که دارن و هر بررسی که دارن تو این مورد باید خیلی
[01:04] داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار میدم.

## معیارهای فیلتر و انتخاب مسیر  _(at: audio/ogg)_

### معیارهای فیلتر و انتخاب مسیر

[00:00] مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم، باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارن و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه بهم بگن. یعنی خیلی باید تو هر آنالیزی که دارن، تو هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.

## قابلیت‌های اجتماعی و تعاملی  _(at: audio/ogg)_

مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم، باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند. و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه. ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن، چه من باید انجام بدم، چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه، بهم بگن. یعنی خیلی باید تو هر آنالیزی که دارن، تو هر بررسی که دارن، تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.

## درخواست از مشاور  _(at: audio/ogg)_

[00:00] گوینده ۱: مدل‌هایی که
[00:02] در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم،
[00:07] باید بدونن نسبت به
[00:10] تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند
[00:15] و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون
[00:21] درخواست باشه
[00:22] و درخواست کامل ببینن و هیچ محدودیت توکنی
[00:26] براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] [00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه
[00:34] باید حسب کارهایی که مثلاً انجام دادم چه
[00:38] عکس‌العملی داشته باشن و حسب اون
[00:40] چیزهایی که من باید انجام بدن چه
[00:43] من باید انجام بدم چه باید
[00:45] واکنشی باید در
[00:47] پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات
[00:52] یا توی اونجایی که مثلاً
[00:54] ثبت می‌شه
[00:56] بهم بگن. یعنی
[00:58] خیلی باید تو هر آنالیزی که دارن، تو هر بررسی که دارن تو این مورد باید خیلی
[01:00] [01:00] داینامیک و
[01:06] هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس
[01:10] چیز براشون قرار می‌دم.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند. بخش‌های بعدی توسط AI ساختار داده شده‌اند و ممکن است ناقص باشند — این متن مرجع اصلی است.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_249747_AgADqR0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد • 5,292 char متن_

## مفهوم اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم، باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه بهم بگن. یعنی خیلی باید تو هر آنالیزی که دارن و هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.

## ویژگی‌های اصلی و عملکرد  _(at: audio/ogg)_

[00:00] مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه میشه باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشند و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستند و فیدبکی که میدن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت میشه بهم بگن یعنی خیلی باید تو هر آنالیزی که دارن تو هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار میدم.

## هدف و ارزش پیشنهادی اپلیکیشن  _(at: audio/ogg)_

[00:00] مدل‌هایی که در صفحه تنظیمات قرار میدم و براشون پرامپت می‌نویسم
[00:07] باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارن
[00:15] و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون
[00:20] درخواست باشه
[00:22] و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه میشه
[00:34] باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون
[00:41] چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در
[00:47] پیام‌هایی که برام می‌فرستن و فیدبکی که میدن حالا چه توی اعلانات
[00:52] یا توی اونجایی که مثلاً ثبت میشه
[00:56] بهم بگن. یعنی
[00:58] خیلی باید تو هر آنالیزی که دارن و هر بررسی که دارن تو این مورد باید خیلی
[01:04] داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار میدم.

## معیارهای فیلتر و انتخاب مسیر  _(at: audio/ogg)_

### معیارهای فیلتر و انتخاب مسیر

[00:00] مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم، باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارن و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه بهم بگن. یعنی خیلی باید تو هر آنالیزی که دارن، تو هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.

## قابلیت‌های اجتماعی و تعاملی  _(at: audio/ogg)_

مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم، باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند. و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه. ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن، چه من باید انجام بدم، چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه، بهم بگن. یعنی خیلی باید تو هر آنالیزی که دارن، تو هر بررسی که دارن، تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.

## درخواست از مشاور  _(at: audio/ogg)_

[00:00] گوینده ۱: مدل‌هایی که
[00:02] در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم،
[00:07] باید بدونن نسبت به
[00:10] تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند
[00:15] و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون
[00:21] درخواست باشه
[00:22] و درخواست کامل ببینن و هیچ محدودیت توکنی
[00:26] براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] [00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه
[00:34] باید حسب کارهایی که مثلاً انجام دادم چه
[00:38] عکس‌العملی داشته باشن و حسب اون
[00:40] چیزهایی که من باید انجام بدن چه
[00:43] من باید انجام بدم چه باید
[00:45] واکنشی باید در
[00:47] پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات
[00:52] یا توی اونجایی که مثلاً
[00:54] ثبت می‌شه
[00:56] بهم بگن. یعنی
[00:58] خیلی باید تو هر آنالیزی که دارن، تو هر بررسی که دارن تو این مورد باید خیلی
[01:00] [01:00] داینامیک و
[01:06] هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس
[01:10] چیز براشون قرار می‌دم.
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی سیستم هوشمند و داینامیک مدل‌های AI در تنظیمات

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/ai_service.py:1-150` — `class AIService` — کلاس اصلی سرویس AI که باید به DynamicAIModel تغییر کند تا context-aware و داینامیک شود
  ```python
  class AIService:
      def __init__(self, model_name: str, api_key: str):
          self.model_name = model_name
          self.api_key = api_key
          self.prompt_template = None
  
      def generate_response(self, prompt: str) -> str:
          # Current implementation has hardcoded prompt handling
          return self._call_model(prompt)
  ```
- `backend/app/models/ai_model.py:1-80` — `class AIModelConfig` — مدل داده فعلی که باید فیلدهای context_rules, dynamic_behavior, feedback_type به آن اضافه شود
  ```python
  class AIModelConfig(Base):
      __tablename__ = 'ai_model_configs'
      id = Column(Integer, primary_key=True)
      model_name = Column(String(100), nullable=False)
      prompt_template = Column(Text, nullable=True)
      max_tokens = Column(Integer, default=4096)
      temperature = Column(Float, default=0.7)
  ```
- `frontend/src/pages/SettingsPage.tsx:50-120` — `SettingsPage` — صفحه تنظیمات که باید بخش پیکربندی داینامیک مدل‌های AI به آن اضافه شود
  ```tsx
  const SettingsPage: React.FC = () => {
    const [aiModels, setAiModels] = useState<AIModelConfig[]>([]);
    // Current implementation only shows basic model config
    return (
      <div className="settings-page">
        <h1>تنظیمات</h1>
        <AIModelConfig models={aiModels} />
      </div>
    );
  };
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack: Python (FastAPI, SQLAlchemy, Celery) + React (TypeScript, Vite). کتابخانه‌های مرتبط: openai (برای مدل‌های AI)، langchain (برای مدیریت context)، celery (برای پردازش async)، react-query (برای مدیریت state فرانت‌اند).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/ai_settings.py` (سطر 1) — روت‌های API برای ذخیره و بازیابی تنظیمات مدل‌های AI که باید به‌روز شوند
- `frontend/src/components/AIModelConfig.tsx` (سطر 1) — کامپوننت پیکربندی مدل AI که باید فرم‌های داینامیک و context-aware به آن اضافه شود
- `backend/app/services/notification_service.py` (سطر 1) — سرویس اعلانات که باید متد تولید فیدبک هوشمند بر اساس تحلیل مدل AI به آن اضافه شود
- `backend/app/models/task.py` (سطر 1) — مدل تسک‌ها که مدل AI باید به آن دسترسی داشته باشد برای context-awareness
- `backend/app/core/dependencies.py` (سطر 1) — وابستگی‌های اصلی که ممکن است نیاز به تزریق سرویس جدید DynamicAIModel داشته باشند

## 🌐 نقشهٔ وابستگی‌ها
این تغییرات بر فایل‌های زیر تأثیر می‌گذارد: 1) `backend/app/services/ai_service.py` - بازنویسی کامل کلاس AIService به DynamicAIModel. 2) `backend/app/models/ai_model.py` - افزودن فیلدهای جدید context_rules, dynamic_behavior, feedback_type. 3) `backend/app/api/routes/ai_settings.py` - به‌روزرسانی endpointها برای پشتیبانی از فیلدهای جدید. 4) `frontend/src/pages/SettingsPage.tsx` - افزودن بخش پیکربندی داینامیک. 5) `frontend/src/components/AIModelConfig.tsx` - بازنویسی کامپوننت برای پشتیبانی از پرامپت‌های داینامیک. 6) `backend/app/services/notification_service.py` - افزودن متد generate_smart_feedback. 7) `backend/app/models/task.py` - اطمینان از دسترسی مدل AI به داده‌های تسک.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک سیستم هوشمند و داینامیک برای مدل‌های AI در صفحه تنظیمات را دارد. مدل‌هایی که در صفحه تنظیمات قرار می‌دهد و برایشان پرامپت می‌نویسد باید نسبت به تسک‌ها و وظایف تعریف‌شده آگاه باشند، داینامیک عمل کنند (هاردکد نباشند)، در چارچوب پرامپت تعریف‌شده باقی بمانند، درخواست کامل را ببینند (بدون محدودیت توکن)، و هزینه برایشان مسئله نباشد. همچنین باید بر اساس وظایف انجام‌شده و وظایف باقی‌مانده عکس‌العمل مناسب نشان دهند و فیدبک هوشمند از طریق اعلانات یا بخش ثبت‌شده ارائه دهند. این سیستم باید در هر تحلیل و بررسی‌ای که انجام می‌دهد داینامیک و هوشمند عمل کند اما همچنان در چارچوب پرامپت تعریف‌شده در باکس تنظیمات باقی بماند. بر اساس بررسی کد واقعی پروژه، فایل‌های مرتبط شامل `backend/app/services/ai_service.py` (سرویس اصلی AI)، `backend/app/models/ai_model.py` (مدل‌های داده AI)، `backend/app/api/routes/ai_settings.py` (روت‌های تنظیمات AI)، `frontend/src/pages/SettingsPage.tsx` (صفحه تنظیمات فرانت‌اند)، و `frontend/src/components/AIModelConfig.tsx` (کامپوننت پیکربندی مدل AI) هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل‌های AI در صفحه تنظیمات باید بتوانند پرامپت داینامیک دریافت کنند و بر اساس تسک‌های فعلی و انجام‌شده عکس‌العمل نشان دهند
- [ ] سیستم باید بدون محدودیت توکن کار کند و درخواست کامل را به مدل ارسال کند
- [ ] مدل‌های AI باید فیدبک هوشمند از طریق اعلانات یا بخش ثبت‌شده ارائه دهند
- [ ] پرامپت‌های تعریف‌شده در باکس تنظیمات باید چارچوب عملکرد مدل را مشخص کنند و مدل خارج از آن عمل نکند
- [ ] هزینه استفاده از مدل‌ها نباید مسئله‌ای باشد (سیستم باید از مدل‌های رایگان یا بهینه‌شده استفاده کند)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. **بازبینی و تحلیل فایل‌های موجود**: ابتدا فایل‌های `backend/app/services/ai_service.py` و `backend/app/models/ai_model.py` را برای درک ساختار فعلی مدل‌های AI و نحوه تعامل آن‌ها با تسک‌ها بررسی کن. 2. **طراحی معماری جدید**: یک سیستم پلاگین‌محور برای مدل‌های AI طراحی کن که هر مدل بتواند پرامپت داینامیک خود را از صفحه تنظیمات دریافت کند و به صورت real-time به تسک‌ها و وظایف دسترسی داشته باشد. 3. **پیاده‌سازی Context-Awareness**: در `backend/app/services/ai_service.py` یک کلاس `DynamicAIModel` ایجاد کن که متد `get_context(tasks, completed_tasks)` را داشته باشد تا مدل بتواند بر اساس وظایف فعلی و انجام‌شده عکس‌العمل نشان دهد. 4. **حذف محدودیت توکن**: از streaming و chunking برای ارسال درخواست کامل به مدل استفاده کن (بدون محدودیت توکن). 5. **سیستم فیدبک هوشمند**: در `backend/app/services/notification_service.py` یک متد `generate_smart_feedback(model_response, user_tasks)` اضافه کن که بر اساس تحلیل مدل، فیدبک مناسب را در اعلانات نمایش دهد. 6. **به‌روزرسانی فرانت‌اند**: در `frontend/src/pages/SettingsPage.tsx` و `frontend/src/components/AIModelConfig.tsx` فرم‌های پیکربندی را برای پشتیبانی از پرامپت‌های داینامیک و تنظیمات context-aware به‌روز کن.

## 💡 نمونه‌های قبل/بعد
**تغییر در سرویس AI برای پشتیبانی از context-awareness**

_قبل:_
```
class AIService:
    def generate_response(self, prompt: str) -> str:
        return self._call_model(prompt)
```

_بعد:_
```
class DynamicAIModel:
    def __init__(self, config: AIModelConfig):
        self.config = config
        self.context_provider = TaskContextProvider()

    async def generate_smart_response(self, user_id: int) -> str:
        tasks = await self.context_provider.get_user_tasks(user_id)
        completed_tasks = await self.context_provider.get_completed_tasks(user_id)
        enriched_prompt = self._enrich_prompt(tasks, completed_tasks)
        return await self._call_model_stream(enriched_prompt)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_ai_service.py -v`
- `npm run test -- --testPathPattern='AIModelConfig'`
- `python -m pytest backend/tests/test_notification_service.py -v`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک اصلی در `backend/app/services/ai_service.py` است: تغییر از AIService به DynamicAIModel ممکن است باعث شکستن endpointهای موجود شود که توسط `backend/app/api/routes/ai_settings.py` و `backend/app/api/routes/task_routes.py` استفاده می‌شوند. همچنین افزودن context-awareness ممکن است latency را افزایش دهد و نیاز به بهینه‌سازی caching در `backend/app/core/cache.py` دارد. تغییر در مدل `AIModelConfig` نیاز به migration دیتابیس دارد که باید با Alembic مدیریت شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

## Acceptance Criteria

1. مدل‌های AI در صفحه تنظیمات باید بتوانند پرامپت داینامیک دریافت کنند و بر اساس تسک‌های فعلی و انجام‌شده عکس‌العمل نشان دهند _(verify: api_response)_
2. سیستم باید بدون محدودیت توکن کار کند و درخواست کامل را به مدل ارسال کند _(verify: backend_test)_
3. مدل‌های AI باید فیدبک هوشمند از طریق اعلانات یا بخش ثبت‌شده ارائه دهند _(verify: ui_interaction)_
4. پرامپت‌های تعریف‌شده در باکس تنظیمات باید چارچوب عملکرد مدل را مشخص کنند و مدل خارج از آن عمل نکند _(verify: static)_
5. هزینه استفاده از مدل‌ها نباید مسئله‌ای باشد (سیستم باید از مدل‌های رایگان یا بهینه‌شده استفاده کند) _(verify: manual_only)_

## Task Steps

### Step 1: پیاده‌سازی رابط کاربری تنظیمات مدل و ورودی پرامپت
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک رابط کاربری در صفحه تنظیمات اپلیکیشن است که به کاربر امکان می‌دهد مدل‌های هوش مصنوعی را انتخاب یا پیکربندی کند. همچنین، یک 'باکس' یا ناحیه ورودی متنی برای نوشتن و مدیریت پرامپت‌های مرتبط با هر مدل پیکربندی‌شده باید فراهم شود. تمرکز این مرحله صرفاً بر روی عناصر UI برای انتخاب مدل و ورود پرامپت است.
**Excerpt:**
```
مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم، باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
```

### Step 2: توسعه سرویس بک‌اند برای مدیریت پرامپت و مدل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک سرویس بک‌اند است که مسئول ذخیره‌سازی، بازیابی و ارتباط دادن پرامپت‌ها با مدل‌های هوش مصنوعی خاصی است که توسط کاربر پیکربندی شده‌اند. این سرویس باید اطمینان حاصل کند که هنگام فراخوانی یک مدل، پرامپت صحیح و تعریف‌شده توسط کاربر بازیابی و ارسال می‌شود. این مرحله مدیریت داده‌های اساسی برای پرامپت‌ها و مدل‌ها را ایجاد می‌کند.
**Excerpt:**
```
مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
```

### Step 3: پیاده‌سازی قابلیت درک وظیفه دینامیک توسط مدل‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه منطق اصلی است که مدل‌های هوش مصنوعی را قادر می‌سازد تا وظایف و مسئولیت‌های محول شده خود را بر اساس پرامپت ارائه شده به صورت دینامیک تفسیر و درک کنند. این امر مستلزم اطمینان از این است که پاسخ مدل هاردکد نیست، بلکه یک تفسیر دینامیک از وظیفه است که به شدت به دستورالعمل‌های پرامپت پایبند است. این مرحله بر توانایی مدل در استخراج نقش خود از پرامپت تمرکز دارد.
**Excerpt:**
```
باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
```

### Step 4: تأمین کامل زمینه درخواست برای مدل‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی مکانیزمی است که اطمینان حاصل می‌کند مدل هوش مصنوعی کل درخواست کاربر و تمام زمینه مرتبط را بدون هیچگونه برش یا خلاصه‌سازی به دلیل محدودیت‌های توکن دریافت می‌کند. این مرحله همچنین به معنای انتخاب یا پیکربندی سرویس‌های هوش مصنوعی است که می‌توانند زمینه‌های بزرگ را مدیریت کنند و در آن هزینه استفاده از توکن بالاتر برای این ویژگی خاص عامل بازدارنده‌ای نیست.
**Excerpt:**
```
درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه. ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه بهم بگن.
```

### Step 5: پیاده‌سازی واکنش زمینه‌ای به اقدامات کاربر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه سیستمی برای تغذیه اطلاعات مربوط به اقدامات کاربر، وظایف تکمیل شده و جریان‌های کاری در حال انجام به زمینه مدل هوش مصنوعی است. مدل باید بتواند پاسخ‌ها یا رفتار خود را به صورت دینامیک بر اساس این فعالیت کاربر در زمان واقعی تنظیم کند و درک خود را از پیشرفت و تاریخچه کاربر نشان دهد.
**Excerpt:**
```
ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه بهم بگن.
```

### Step 6: توسعه مکانیزم بازخورد و راهنمایی فعال
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی مکانیزمی برای مدل هوش مصنوعی است تا به طور فعال بازخورد، پیشنهادات و راهنمایی‌هایی را در مورد *آنچه کاربر باید در ادامه انجام دهد* ارائه دهد. این بازخورد باید از طریق کانال‌های مختلفی از جمله پیام‌های درون‌برنامه‌ای، اعلان‌ها یا گزارش‌ها/سوابق پایدار ارائه شود. این مرحله بر کانال‌های خروجی و ماهیت فعال راهنمایی هوش مصنوعی تمرکز دارد.
**Excerpt:**
```
و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه بهم بگن. یعنی خیلی باید تو هر آنالیزی که دارن و هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.
```

### Step 7: پیاده‌سازی هسته تحلیل دینامیک و هوشمند
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه و یکپارچه‌سازی قابلیت‌های اصلی هوش مصنوعی برای انجام تحلیل دینامیک و هوشمند بر روی زمینه ارائه شده (اقدامات کاربر، وظایف، پرامپت‌ها) است. این امر مستلزم بهره‌گیری از استدلال پیشرفته و تشخیص الگو برای استخراج بینش‌ها، تصمیم‌گیری و فرمول‌بندی پاسخ‌هایی است که فراتر از منطق ساده مبتنی بر قانون هستند.
**Excerpt:**
```
یعنی خیلی باید تو هر آنالیزی که دارن و هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.
```

### Step 8: اعمال محدودیت‌های پرامپت در باکس ورودی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی مکانیزم‌های اجرایی سختگیرانه برای اطمینان از این است که تمام عملیات دینامیک و هوشمند انجام شده توسط مدل هوش مصنوعی، از جمله تحلیل، واکنش‌ها و بازخورد آن، به شدت در چارچوب و هدف تعریف شده توسط پرامپت کاربر در 'باکس' تعیین شده باقی می‌ماند. این امر از 'خارج شدن از مسیر' هوش مصنوعی یا تولید پاسخ‌هایی خارج از محدودیت‌های مشخص شده توسط کاربر جلوگیری می‌کند.
**Excerpt:**
```
یعنی خیلی باید تو هر آنالیزی که دارن و هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.
```
