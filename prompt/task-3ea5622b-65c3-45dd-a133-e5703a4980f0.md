---
task_id: 3ea5622b-65c3-45dd-a133-e5703a4980f0
title: همگام‌سازی migrationهای Alembic و startup
type: other
priority: medium
execution_priority: 3100
status: pending
external_status: claimed
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:27:03.826742+00:00'
updated_at: '2026-06-05T04:53:54.962824+00:00'
target_files:
- app/main.py
- alembic.ini
- migrations/env.py
---

# همگام‌سازی migrationهای Alembic و startup

## Raw Idea

همگام‌سازی migrationهای Alembic و startup
---
[scan #2 at 2026-06-02T21:10:50.817378+00:00]
فایل app/main.py نقطه ورود اصلی برنامه است و شامل منطق startup (اتصال دیتابیس، migration‌های startup، CORS middleware، rate limiting، exception handlers) می‌باشد. هیچ فایل تست مستقیمی برای این ماژول وجود ندارد. با توجه به اینکه ۲۰ فایل دیگر این ماژول را import می‌کنند (hub اصلی)، عدم پوشش تست در این

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
همگام‌سازی migrationهای Alembic و startup
```

## 📋 چک‌لیست مراحل (5 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: بررسی و همگام‌سازی migrationهای موجود با state دیتابیس** — این مرحله شامل بررسی تمام migrationهای موجود در دایرکتوری alembic/versions و مقایسه آنها با state فعلی دیتابیس (schema) است. باید مشخص شود کدام migrationها اجرا نشده‌اند، کدام migrationها با schema فعلی conflict دارند، و آیا migration head با state دیتابیس sync است. خارج از این مرحله: ایجاد migratio
- [x] **مرحله 2: بررسی و اصلاح فرآیند startup برای اجرای خودکار migrationها** — این مرحله شامل بررسی کد startup اپلیکیشن (معمولاً در main.py یا app/__init__.py یا فایل‌های مشابه) است تا مشخص شود آیا migrationها به‌صورت خودکار در startup اجرا می‌شوند یا خیر. اگر اجرا می‌شوند، باید بررسی شود که آیا این کار با best practices سازگار است (مثلاً استفاده از alembic upgrade head). اگر 
- [x] **مرحله 3: ایجاد migration جدید برای رفع هرگونه discrepancy بین schema و کد** — اگر در مرحله 1 مشخص شد که migration head با state دیتابیس هماهنگ نیست، باید یک migration جدید ایجاد شود تا این discrepancy را رفع کند. این migration باید با دستور `alembic revision --autogenerate` یا دستی ایجاد شود. خارج از این مرحله: تغییر کد اپلیکیشن، تغییر startup flow. نکته حیاتی: migration جدید
- [x] **مرحله 4: اضافه کردن لاگینگ و error handling برای فرآیند migration در startup** — این مرحله شامل اضافه کردن لاگینگ مناسب (با استفاده از logging library) برای فرآیند اجرای migration در startup است. همچنین باید error handling اضافه شود تا اگر migration با خطا مواجه شد، اپلیکیشن crash نکند و خطا به‌درستی گزارش شود. خارج از این مرحله: تغییر منطق migration، تغییر دیتابیس state. نکته ح
- [~] **مرحله 5: اضافه کردن تست برای فرآیند startup migration** — این مرحله شامل نوشتن تست‌های unit/integration برای فرآیند startup migration است. تست‌ها باید موارد زیر را پوشش دهند: 1) migration موفق در startup، 2) migration ناموفق و handling خطا، 3) عدم اجرای migration در محیط production (اگر چنین منطقی وجود دارد). خارج از این مرحله: تست migrationهای فردی، تست د

---

# 🔹 مرحله 1: بررسی و همگام‌سازی migrationهای موجود با state دیتابیس

**Scope:** این مرحله شامل بررسی تمام migrationهای موجود در دایرکتوری alembic/versions و مقایسه آنها با state فعلی دیتابیس (schema) است. باید مشخص شود کدام migrationها اجرا نشده‌اند، کدام migrationها با schema فعلی conflict دارند، و آیا migration head با state دیتابیس sync است. خارج از این مرحله: ایجاد migration جدید، تغییر کد اپلیکیشن، یا تغییر در startup flow. نکته حیاتی: قبل از هر اقدامی، از دیتابیس فعلی backup گرفته شود.
**Key terms:** alembic/versions, migration head, database schema, alembic current, alembic history

**بخش مربوط از متن کاربر:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

## 🎯 هدف (خلاصه ساختاریافته)
بررسی و همگام‌سازی migrationهای Alembic با state دیتابیس

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
این تسک به بررسی و همگام‌سازی بین migrationهای موجود در دایرکتوری `alembic/versions` و state فعلی دیتابیس (schema) می‌پردازد. کاربر درخواست کرده است که مشخص شود کدام migrationها اجرا نشده‌اند، کدام migrationها با schema فعلی conflict دارند، و آیا migration head با state دیتابیس sync است. خارج از این مرحله: ایجاد migration جدید، تغییر کد اپلیکیشن، یا تغییر در startup flow. نکته حیاتی: قبل از هر اقدامی، از دیتابیس فعلی backup گرفته شود. بخش مربوط از درخواست اصلی کاربر: "همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد." کلیدواژه‌ها: `alembic/versions`, `migration head`, `database schema`, `alembic current`, `alembic history`. در کد واقعی پروژه، فایل `app/main.py` (خطوط 190-523) شامل یک `startup_event` است که مستقیماً migrationهای متعددی را از طریق `ALTER TABLE` اجرا می‌کند (مثلاً خطوط 209-217 برای `user_id`، خطوط 223-239 برای `notifications`، خطوط 244-255 برای `users`، خطوط 261-274 برای `tasks`، خطوط 279-290 برای `todo_items`، خطوط 300-307 برای `todo_items.content`، خطوط 316-323 برای `todo_lists.name`، خطوط 328-335 برای `tasks.due_date`). این startup flow موازی با migrationهای Alembic کار می‌کند و ممکن است باعث عدم هماهنگی شود. فایل‌های migration موجود در `migrations/versions/` شامل `0001_initial_users.py`, `0002_all_models.py`, `0003_task_planning_fields.py`, `0004_todo_lists.py`, `0005_seed_default_todo_lists.py`, `0006_todo_item_parent_and_due.py`, `0007_seed_todo_items_from_pdfs.py`, `0008_self_improvement_module.py`, `0009_self_improvement_full_content.py`, `0010_todo_item_content_to_text.py`, `0011_user_profile_columns.py` هستند. فایل `migrations/env.py` و `alembic.ini` نیز برای پیکربندی Alembic وجود دارند. فایل `app/database.py` که توسط 42 فایل import می‌شود، `Base` و `engine` را تعریف می‌کند که در startup و migrationها استفاده می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ابتدا از دیتابیس فعلی backup گرفته شود (طبق درخواست کاربر).
2. اجرای دستور `alembic current` برای مشاهده migration head فعلی و مقایسه با state دیتابیس.
3. اجرای دستور `alembic history` برای مشاهده تاریخچه کامل migrationها.
4. بررسی فایل `app/main.py` (خطوط 190-523) و مقایسه `ALTER TABLE`های موجود در startup_event با migrationهای موجود در `migrations/versions/`. برای مثال، migration `0003_task_planning_fields.py` ستون‌های `estimated_duration`, `deadline`, `recurrence`, `attachment` را به جدول `tasks` اضافه می‌کند، در حالی که startup_event در خطوط 261-274 همین ستون‌ها را از طریق `ALTER TABLE tasks ADD COLUMN IF NOT EXISTS` اضافه می‌کند. این دوگانگی باید بررسی شود.
5. بررسی فایل `migrations/versions/0011_user_profile_columns.py` که ستون‌های `bio` و `display_name` را به جدول `users` اضافه می‌کند، و مقایسه با startup_event در خطوط 244-255.
6. بررسی فایل `migrations/versions/0010_todo_item_content_to_text.py` که نوع ستون `content` را در جدول `todo_items` به `TEXT` تغییر می‌دهد، و مقایسه با startup_event در خطوط 300-307.
7.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: بررسی و اصلاح فرآیند startup برای اجرای خودکار migrationها

**Scope:** این مرحله شامل بررسی کد startup اپلیکیشن (معمولاً در main.py یا app/__init__.py یا فایل‌های مشابه) است تا مشخص شود آیا migrationها به‌صورت خودکار در startup اجرا می‌شوند یا خیر. اگر اجرا می‌شوند، باید بررسی شود که آیا این کار با best practices سازگار است (مثلاً استفاده از alembic upgrade head). اگر اجرا نمی‌شوند، باید منطق لازم اضافه شود. خارج از این مرحله: تغییر migrationها، تغییر دیتابیس state. نکته حیاتی: اجرای خودکار migration در production می‌تواند خطرناک باشد — باید با flag یا environment variable کنترل شود.
**Key terms:** main.py, app/__init__.py, startup event, alembic upgrade head, ENVIRONMENT variable

**بخش مربوط از متن کاربر:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

## 🎯 هدف (خلاصه ساختاریافته)
بررسی و اصلاح فرآیند startup برای اجرای خودکار migrationهای Alembic

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:190-523` — `startup_event` — این تابع startup اصلی است که باید منطق اجرای خودکار migration به آن اضافه شود. در حال حاضر فقط از create_all استفاده می‌کند.
  ```python
  @app.on_event("startup")
  async def startup_event():
      try:
          async with engine.begin() as conn:
              await conn.run_sync(Base.metadata.create_all)
          logger.info("✅ Database tables created successfully")
      except Exception as e:
          logger.critical(f"❌ CRITICAL: Database connection failed: {e}")
          logger.info("   App will continue without database — set DATABASE_URL in Render env vars.")
  ```
- `app/main.py:209-335`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
بررسی و اصلاح فرآیند startup اپلیکیشن (در فایل app/main.py) برای اجرای خودکار migrationها. کاربر درخواست داده که مشخص شود آیا migrationها به‌صورت خودکار در startup اجرا می‌شوند یا خیر. اگر اجرا می‌شوند، باید بررسی شود که آیا این کار با best practices سازگار است (مثلاً استفاده از `alembic upgrade head`). اگر اجرا نمی‌شوند، باید منطق لازم اضافه شود. خارج از این مرحله: تغییر migrationها، تغییر دیتابیس state. نکته حیاتی: اجرای خودکار migration در production می‌تواند خطرناک باشد — باید با flag یا environment variable کنترل شود. بخش مربوط از درخواست اصلی کاربر: «همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.» کلیدواژه‌ها: main.py, app/__init__.py, startup event, alembic upgrade head, ENVIRONMENT variable. در کد فعلی app/main.py (خطوط 190-523) یک startup event وجود دارد که از `Base.metadata.create_all` (خط 194) و چندین ALTER TABLE دستی (خطوط 209-335) استفاده می‌کند، اما هیچ فراخوانی به `alembic upgrade head` وجود ندارد. این رویکرد با best practices سازگار نیست زیرا: 1) `create_all` فقط جداول جدید را می‌سازد و migrationهای موجود را اعمال نمی‌کند، 2) ALTER TABLEهای دستی شکننده و غیرقابل ردیابی هستند، 3) در production، اجرای خودکار migration بدون کنترل می‌تواند خطرناک باشد. فایل alembic.ini در ریشه پروژه وجود دارد و پوشه migrations/versions حاوی 11 فایل migration است (0001 تا 0011).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل app/main.py، درون تابع startup_event (خط 191)، یک بخش جدید اضافه کن که با استفاده از subprocess یا alembic command API، دستور `alembic upgrade head` را اجرا کند. 2. این بخش باید با یک environment variable به نام `RUN_ALEMBIC_MIGRATIONS_ON_STARTUP` کنترل شود (پیش‌فرض: false). 3. اگر متغیر برابر 'true' باشد و ENVIRONMENT برابر 'development' باشد، migration اجرا شود. 4. اگر ENVIRONMENT برابر 'production' باشد، حتی اگر متغیر true باشد، migration اجرا نشود و یک warning لاگ شود. 5. از `alembic.config` و `alembic.command` برای فراخوانی برنامه‌ای استفاده کن (نه subprocess shell). 6. خطاهای migration را catch کن و در لاگ ثبت کن، اما اجازه نده startup fail شود. 7. مسیر alembic.ini را به‌صورت absolute از ریشه پروژه تنظیم کن. 8. در .env.example یک خط جدید برای `RUN_ALEMBIC_MIGRATIONS_ON_STARTUP=false` اضافه کن. 9. در docs/API.md یا README.md یک بخش درباره این flag اضافه کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: ایجاد migration جدید برای رفع هرگونه discrepancy بین schema و کد

**Scope:** اگر در مرحله 1 مشخص شد که migration head با state دیتابیس هماهنگ نیست، باید یک migration جدید ایجاد شود تا این discrepancy را رفع کند. این migration باید با دستور `alembic revision --autogenerate` یا دستی ایجاد شود. خارج از این مرحله: تغییر کد اپلیکیشن، تغییر startup flow. نکته حیاتی: migration جدید باید review شود و فقط تغییرات ضروری را شامل شود.
**Key terms:** alembic revision --autogenerate, migration file, upgrade(), downgrade()

**بخش مربوط از متن کاربر:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد migration جدید برای رفع discrepancy بین schema و کد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:189-523` — `startup_event`
  ```python
  @app.on_event("startup")
  async def startup_event():
      try:
          async with engine.begin() as conn:
              await conn.run_sync(Base.metadata.create_all)
          logger.info("✅ Database tables created successfully")
      except Exception as e:
          logger.critical(f"❌ CRITICAL: Database connection failed: {e}")
          logger.info("   App will continue without database — set DATABASE_URL in Render env vars.")
  
      # Best-effort idempotent migration: tasks.user_id and projects.user_id
      # used to be NOT NULL. The current model declares them nullable, but
      # Base.metadata.create_all does NOT alter existing tables — so an
      # anonymous POST /api/{tasks,projects} hits the legacy NOT NULL
      # constraint and blows up with 500. ALTER COLUMN runs in its own
      # transaction per table so a SQLite/permission failure on one doesn't
      # roll back the other. Errors are swallowed: the column is already
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک migration جدید برای رفع هرگونه discrepancy بین schema و کد را دارد. این درخواست بر اساس بررسی هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن است. اگر در مرحله 1 مشخص شد که migration head با state دیتابیس هماهنگ نیست، باید یک migration جدید ایجاد شود تا این discrepancy را رفع کند. این migration باید با دستور `alembic revision --autogenerate` یا دستی ایجاد شود. خارج از این مرحله: تغییر کد اپلیکیشن، تغییر startup flow. نکته حیاتی: migration جدید باید review شود و فقط تغییرات ضروری را شامل شود. بخش مربوط از درخواست اصلی کاربر: همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد. کلیدواژه‌ها: `alembic revision --autogenerate`, migration file, upgrade(), downgrade(). در کد فعلی، فایل `app/main.py` (خطوط 189-523) شامل یک startup event است که به‌صورت دستی ALTER TABLE و ADD COLUMN انجام می‌دهد (برای tasks.user_id, projects.user_id, notifications.*, users.*, tasks.*, todo_items.*, todo_lists.*). این تغییرات در migrationهای Alembic (فایل‌های 0001 تا 0010 در `migrations/versions/`) نیز وجود دارند، اما startup event آن‌ها را دوباره اجرا می‌کند. این دوگانگی می‌تواند باعث discrepancy شود. همچنین فایل `alembic.ini` و `migrations/env.py` برای پیکربندی Alembic وجود دارند. فایل `migrations/versions/0010_todo_item_content_to_text.py` آخرین migration است. بررسی شود که آیا migration head با state دیتابیس هماهنگ است یا خیر.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ابتدا با دستور `alembic current` بررسی کن که migration head کدام است و آیا با state دیتابیس هماهنگ است. 2. اگر discrepancy وجود دارد، با دستور `alembic revision --autogenerate -m "sync schema with code"` یک migration جدید ایجاد کن. 3. فایل migration جدید را در `migrations/versions/` بررسی و ویرایش کن تا فقط تغییرات ضروری را شامل شود (مثلاً اضافه کردن ستون‌هایی که در startup event وجود دارند اما در migration نیستند). 4. توابع `upgrade()` و `downgrade()` را در فایل migration جدید پیاده‌سازی کن. 5. migration را با `alembic upgrade head` اجرا کن. 6. startup event در `app/main.py` (خطوط 189-523) را بررسی کن تا مطمئن شوی که تغییرات تکراری حذف شده‌اند یا با migration هماهنگ هستند. 7. تست کن که دیتابیس پس از migration با schema کد هماهنگ است.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: اضافه کردن لاگینگ و error handling برای فرآیند migration در startup

**Scope:** این مرحله شامل اضافه کردن لاگینگ مناسب (با استفاده از logging library) برای فرآیند اجرای migration در startup است. همچنین باید error handling اضافه شود تا اگر migration با خطا مواجه شد، اپلیکیشن crash نکند و خطا به‌درستی گزارش شود. خارج از این مرحله: تغییر منطق migration، تغییر دیتابیس state. نکته حیاتی: خطاهای migration باید به‌صورت واضح لاگ شوند و اپلیکیشن باید graceful degradation داشته باشد.
**Key terms:** logging, logger, try/except, alembic.command.upgrade, sys.exit

**بخش مربوط از متن کاربر:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن لاگینگ و error handling به فرآیند migration در startup

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:190-523` — `startup_event` — این تابع startup اصلی است که migration باید به آن اضافه شود. در حال حاضر فقط create_all را اجرا می‌کند.
  ```python
  @app.on_event("startup")
  async def startup_event():
      try:
          async with engine.begin() as conn:
              await conn.run_sync(Base.metadata.create_all)
          logger.info("✅ Database tables created successfully")
      except Exception as e:
          logger.critical(f"❌ CRITICAL: Database connection failed: {e}")
          logger.info("   App will continue without database — set DATABASE_URL in Render env vars.")
  ```
- `alembic.ini:1-50` — `alembic configuration` — فایل کانفیگ Alembic که باید در startup برای اجرای migrationها استفاده شود.
  ```
  [alembic]
  script_location = migrations
  sqlalchemy.url = driver://user:pass@localhost/dbname
  ```
- `migrations/env.py:1-80` — `run_migrations_online` — این فایل حاوی منطق اجرای migrationهاست و باید توسط alembic.command.upgrade فراخوانی شود.
  ```python
  def run_migrations_online():
      connectable = engine_from_config(
          config.get_section(config.config_ini_section, {}),
          prefix="sqlalchemy.",
          poolclass=pool.NullPool,
      )
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/config.py` (سطر 10) — شامل تنظیمات DATABASE_URL که برای اتصال به دیتابیس در migration استفاده می‌شود
- `app/database.py` (سطر 25) — شامل engine و SessionLocal که برای اجرای migrationها نیاز است

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن لاگینگ مناسب (با استفاده از logging library) و error handling برای فرآیند اجرای migration در startup اپلیکیشن را دارد. هدف این است که اگر migration با خطا مواجه شد، اپلیکیشن crash نکند و خطا به‌درستی گزارش شود. خارج از این مرحله: تغییر منطق migration، تغییر دیتابیس state. نکته حیاتی: خطاهای migration باید به‌صورت واضح لاگ شوند و اپلیکیشن باید graceful degradation داشته باشد. این درخواست بخشی از همگام‌سازی migrationهای Alembic و startup است. کلیدواژه‌ها: logging, logger, try/except, alembic.command.upgrade, sys.exit. در کد فعلی (app/main.py)، فرآیند startup شامل اجرای migrationها نیست و فقط از Base.metadata.create_all استفاده می‌کند. فایل alembic.ini و پوشه migrations/ وجود دارند اما در startup فراخوانی نمی‌شوند. فایل‌های مرتبط: app/main.py (خطوط 190-523)، alembic.ini، migrations/env.py.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در app/main.py، در تابع startup_event() (خط 190)، یک بلوک جدید برای اجرای migrationهای Alembic اضافه کن. 2. از alembic.command.upgrade برای اجرای migrationها استفاده کن. 3. کل فرآیند را در try/except بپیچ تا خطاها گرفته شوند. 4. از logging.getLogger(__name__) برای لاگ کردن خطاها استفاده کن. 5. در صورت خطا، به جای sys.exit، فقط خطا را لاگ کن و اجازه بده اپلیکیشن با graceful degradation ادامه دهد. 6. اطمینان حاصل کن که migrationها فقط یک بار اجرا شوند (idempotent). 7. از settings.DATABASE_URL برای اتصال به دیتابیس استفاده کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: small

---

# 🔹 مرحله 5: اضافه کردن تست برای فرآیند startup migration

**Scope:** این مرحله شامل نوشتن تست‌های unit/integration برای فرآیند startup migration است. تست‌ها باید موارد زیر را پوشش دهند: 1) migration موفق در startup، 2) migration ناموفق و handling خطا، 3) عدم اجرای migration در محیط production (اگر چنین منطقی وجود دارد). خارج از این مرحله: تست migrationهای فردی، تست دیتابیس state. نکته حیاتی: تست‌ها باید با دیتابیس test اجرا شوند و state دیتابیس را پس از تست پاک کنند.
**Key terms:** pytest, unittest, mock, alembic.command.upgrade, test database

**بخش مربوط از متن کاربر:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن تست‌های unit/integration برای startup migration

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن تست برای فرآیند startup migration در فایل `app/main.py` (خطوط 190-523) را دارد. این تست‌ها باید با استفاده از `pytest`, `unittest.mock`, و `alembic.command.upgrade` نوشته شوند و موارد زیر را پوشش دهند: 1) migration موفق در startup (شبیه‌سازی موفقیت `Base.metadata.create_all` و ALTER TABLE‌های idempotent)، 2) migration ناموفق و handling خطا (شبیه‌سازی Exception در `engine.begin()` و بررسی اینکه `logger.critical` صدا زده شده و اپلیکیشن ادامه می‌دهد)، 3) عدم اجرای migration در محیط production (اگر چنین منطقی وجود دارد — در کد فعلی `app/main.py` چنین منطقی وجود ندارد، اما تست باید این سناریو را نیز پوشش دهد). نکته حیاتی: تست‌ها باید با دیتابیس test (احتمالاً SQLite in-memory از طریق `aiosqlite`) اجرا شوند و state دیتابیس را پس از تست پاک کنند (با استفاده از fixtureهای `pytest` با scope function). بخش مربوط از درخواست اصلی کاربر: "همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد." کلیدواژه‌ها: `pytest`, `unittest`, `mock`, `alembic.command.upgrade`, `test database`. فایل‌های مرتبط در کد: `tests/test_database_startup.py` (الگوی موجود برای تست startup)، `app/main.py` (خطوط 190-523 حاوی startup migration logic)، `app/database.py` (حاوی `engine` و `Base`).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل تست جدید `tests/test_startup_migration.py`. 2. نوشتن fixture برای ایجاد یک AsyncSession test database با SQLite in-memory (با استفاده از `aiosqlite` و `create_async_engine`). 3. نوشتن تست `test_startup_migration_success`: با mock کردن `engine.begin()` برای موفقیت، فراخوانی `startup_event` (از `app.main`) و بررسی اینکه `logger.info` با پیام "Database tables created successfully" صدا زده شده است. 4. نوشتن تست `test_startup_migration_failure_logs_critical`: با mock کردن `engine.begin()` برای raise Exception، فراخوانی `startup_event` و بررسی اینکه `logger.critical` صدا زده شده و اپلیکیشن ادامه می‌دهد (مشابه `tests/test_database_startup.py::test_startup_database_failure_logs_critical`). 5. نوشتن تست `test_startup_migration_idempotent_alter_columns`: mock کردن `engine.begin()` برای موفقیت، فراخوانی `startup_event` دو بار و بررسی اینکه ALTER TABLE‌ها فقط یک بار اجرا شده‌اند (با شمارش تعداد calls روی `conn.execute`). 6. نوشتن تست `test_startup_migration_skipped_in_production`: اگر منطق شرطی برای محیط production وجود دارد (در کد فعلی وجود ندارد)، mock کردن `settings.ENVIRONMENT` به "production" و بررسی اینکه migration اجرا نمی‌شود. 7. استفاده از `pytest.mark.asyncio` برای تمام تست‌ها. 8. پاک کردن state دیتابیس پس از هر تست با استفاده از fixture با scope function و drop_all tables.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. اعمال تغییر بدون شکستن تست‌های موجود _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: static)_
3. type-check موفق است _(verify: static)_

## Task Steps

### Step 1: بررسی و همگام‌سازی migrationهای موجود با state دیتابیس
**Status:** `done` (100%)
**Scope:** این مرحله شامل بررسی تمام migrationهای موجود در دایرکتوری alembic/versions و مقایسه آنها با state فعلی دیتابیس (schema) است. باید مشخص شود کدام migrationها اجرا نشده‌اند، کدام migrationها با schema فعلی conflict دارند، و آیا migration head با state دیتابیس sync است. خارج از این مرحله: ایجاد migration جدید، تغییر کد اپلیکیشن، یا تغییر در startup flow. نکته حیاتی: قبل از هر اقدامی، از دیتابیس فعلی backup گرفته شود.
**Excerpt:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

### Step 2: بررسی و اصلاح فرآیند startup برای اجرای خودکار migrationها
**Status:** `done` (100%)
**Scope:** این مرحله شامل بررسی کد startup اپلیکیشن (معمولاً در main.py یا app/__init__.py یا فایل‌های مشابه) است تا مشخص شود آیا migrationها به‌صورت خودکار در startup اجرا می‌شوند یا خیر. اگر اجرا می‌شوند، باید بررسی شود که آیا این کار با best practices سازگار است (مثلاً استفاده از alembic upgrade head). اگر اجرا نمی‌شوند، باید منطق لازم اضافه شود. خارج از این مرحله: تغییر migrationها، تغییر دیتابیس state. نکته حیاتی: اجرای خودکار migration در production می‌تواند خطرناک باشد — باید با flag یا environment variable کنترل شود.
**Excerpt:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

### Step 3: ایجاد migration جدید برای رفع هرگونه discrepancy بین schema و کد
**Status:** `done` (100%)
**Scope:** اگر در مرحله 1 مشخص شد که migration head با state دیتابیس هماهنگ نیست، باید یک migration جدید ایجاد شود تا این discrepancy را رفع کند. این migration باید با دستور `alembic revision --autogenerate` یا دستی ایجاد شود. خارج از این مرحله: تغییر کد اپلیکیشن، تغییر startup flow. نکته حیاتی: migration جدید باید review شود و فقط تغییرات ضروری را شامل شود.
**Excerpt:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

### Step 4: اضافه کردن لاگینگ و error handling برای فرآیند migration در startup
**Status:** `done` (100%)
**Scope:** این مرحله شامل اضافه کردن لاگینگ مناسب (با استفاده از logging library) برای فرآیند اجرای migration در startup است. همچنین باید error handling اضافه شود تا اگر migration با خطا مواجه شد، اپلیکیشن crash نکند و خطا به‌درستی گزارش شود. خارج از این مرحله: تغییر منطق migration، تغییر دیتابیس state. نکته حیاتی: خطاهای migration باید به‌صورت واضح لاگ شوند و اپلیکیشن باید graceful degradation داشته باشد.
**Excerpt:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```

### Step 5: اضافه کردن تست برای فرآیند startup migration
**Status:** `partial` (80%)
**Scope:** این مرحله شامل نوشتن تست‌های unit/integration برای فرآیند startup migration است. تست‌ها باید موارد زیر را پوشش دهند: 1) migration موفق در startup، 2) migration ناموفق و handling خطا، 3) عدم اجرای migration در محیط production (اگر چنین منطقی وجود دارد). خارج از این مرحله: تست migrationهای فردی، تست دیتابیس state. نکته حیاتی: تست‌ها باید با دیتابیس test اجرا شوند و state دیتابیس را پس از تست پاک کنند.
**Excerpt:**
```
همگام‌سازی migrationهای Alembic و startup — این درخواست به بررسی و هماهنگ‌سازی بین migrationهای Alembic و فرآیند startup اپلیکیشن اشاره دارد.
```
