---
task_id: 1a08ded2-2801-4389-905c-94972c928461
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:15:14.628371+00:00'
updated_at: '2026-05-28T20:03:07.859256+00:00'
target_files:
- app/models/ai_provider.py
- app
- app/routes/ai.py
- frontend/src/pages/Dashboard.jsx
- app/models/ai_model_config.py
- app/services/ai/model_service.py
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_292354_AgADih0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 4,248 char متن_

## شرح ایده اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## ویژگی‌های کلیدی و قابلیت‌ها  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پرپلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## اهداف و مزایای اصلی  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پروایدرها رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پروایدرها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم، اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## بازار هدف  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جمنی، کلاد، گراک، پلکسیتی همچین چیزی. و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## پتانسیل رشد و چشم‌انداز  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پروواید پرووایدها رو بتونم قرار بدم مثل دیپ سیک، جی‌پی‌تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_292354_AgADih0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 4,248 char متن_

## شرح ایده اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## ویژگی‌های کلیدی و قابلیت‌ها  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پرپلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## اهداف و مزایای اصلی  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پروایدرها رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پروایدرها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم، اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## بازار هدف  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جمنی، کلاد، گراک، پلکسیتی همچین چیزی. و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## پتانسیل رشد و چشم‌انداز  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پروواید پرووایدها رو بتونم قرار بدم مثل دیپ سیک، جی‌پی‌تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.
```

## 📋 چک‌لیست مراحل (7 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد صفحه تنظیمات اصلی با قابلیت مدیریت Providerها و مدل‌ها** — این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند لیست Providerهای هوش مصنوعی (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و چند مورد دیگر) را مشاهده، اضافه و مدیریت کند. برای هر Provider، کاربر باید بتواند مدل‌های مختلف آن را mapping و اضافه کند. این صفحه باید ا
- [ ] **مرحله 2: اضافه کردن جعبه پرامپت قابل ویرایش برای هر Provider/مدل** — این مرحله شامل افزودن یک جعبه متن (text box) در صفحه تنظیمات، در کنار هر Provider یا مدل است که کاربر بتواند یک پرامپت (دستورالعمل) سفارشی برای آن بنویسد. این پرامپت باید همیشه قابل ویرایش باشد و کاربر بتواند هر زمان که بخواهد آن را تغییر دهد. خارج از این مرحله: اجرای واقعی آنالیز با استفاده از پرام
- [ ] **مرحله 3: ایجاد مکانیزم دسترسی مدل‌ها به صفحات و داده‌های سیستم** — این مرحله شامل پیاده‌سازی یک سیستم اشتراک‌گذاری داده است که به مدل‌های تعریف‌شده در مرحله 1 اجازه می‌دهد به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشند. این دسترسی باید به صورت خودکار و پویا باشد، به طوری که هر صفحه یا داده جدیدی که اضافه می‌شود، بلافاصله در دسترس مدل‌ها قرار گیرد. خارج از این 
- [ ] **مرحله 4: پیاده‌سازی قابلیت آنالیز داده‌ها توسط مدل‌ها بر اساس پرامپت** — این مرحله شامل پیاده‌سازی هسته اصلی قابلیت آنالیز است. مدل‌های تعریف‌شده باید بتوانند با استفاده از پرامپت اختصاصی خود (مرحله 2) و داده‌های موجود در سیستم (مرحله 3)، یک آنالیز کامل انجام دهند. کاربر باید بتواند از طریق یک رابط کاربری، درخواست آنالیز دهد و نتیجه را دریافت کند. خارج از این مرحله: طراح
- [ ] **مرحله 5: ایجاد رابط کاربری برای نمایش نتایج آنالیز** — این مرحله شامل طراحی و پیاده‌سازی یک رابط کاربری مناسب برای نمایش نتایج آنالیز است. نتایج باید به صورت خوانا و ساختاریافته نمایش داده شوند. کاربر باید بتواند تاریخچه آنالیزها را مشاهده کند و نتایج قبلی را مرور نماید. خارج از این مرحله: ذخیره‌سازی طولانی‌مدت نتایج. نکته حیاتی: UI باید responsive باشد
- [ ] **مرحله 6: پیاده‌سازی قابلیت ویرایش پرامپت در هر زمان** — این مرحله تضمین می‌کند که کاربر می‌تواند پرامپت اختصاصی هر مدل را در هر زمان و بدون محدودیت ویرایش کند. تغییرات باید بلافاصله ذخیره شوند و برای آنالیزهای بعدی اعمال گردند. خارج از این مرحله: اعتبارسنجی پیشرفته پرامپت. نکته حیاتی: ویرایش پرامپت نباید بر آنالیزهای در حال اجرا تأثیر بگذارد.
- [ ] **مرحله 7: تست و اعتبارسنجی یکپارچگی سیستم** — این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی‌شده است: مدیریت Providerها و مدل‌ها، ذخیره و ویرایش پرامپت، دسترسی به داده‌ها، اجرای آنالیز، و نمایش نتایج. تست‌ها باید شامل unit tests, integration tests و end-to-end tests باشند. خارج از این مرحله: تست performance و security. نکته حیاتی: تمام سنار

---

# 🔹 مرحله 1: ایجاد صفحه تنظیمات اصلی با قابلیت مدیریت Providerها و مدل‌ها

**Scope:** این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند لیست Providerهای هوش مصنوعی (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و چند مورد دیگر) را مشاهده، اضافه و مدیریت کند. برای هر Provider، کاربر باید بتواند مدل‌های مختلف آن را mapping و اضافه کند. این صفحه باید امکان ذخیره و ویرایش تنظیمات را فراهم کند. خارج از این مرحله: قابلیت آنالیز داده‌ها، جعبه پرامپت، و دسترسی به صفحات دیگر. نکته حیاتی: این صفحه پایه و اساس کل سیستم است و باید با در نظر گرفتن توسعه‌پذیری برای اضافه شدن Providerهای جدید طراحی شود.
**Key terms:** صفحه تنظیمات, Provider, DeepSeek, GPT, Gemini, Claude, Grok, Perplexity, مدل, mapping

**بخش مربوط از متن کاربر:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد صفحه تنظیمات اصلی با مدیریت Providerها و مدل‌های هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-23` — `AIProvider`
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک صفحه تنظیمات جدید در سیستم LifeManager داده است که در آن بتواند لیست Providerهای هوش مصنوعی (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و چند مورد دیگر) را مشاهده، اضافه و مدیریت کند. برای هر Provider، کاربر باید بتواند مدل‌های مختلف آن را mapping و اضافه کند. این صفحه باید امکان ذخیره و ویرایش تنظیمات را فراهم کند. خارج از این مرحله: قابلیت آنالیز داده‌ها، جعبه پرامپت، و دسترسی به صفحات دیگر. نکته حیاتی: این صفحه پایه و اساس کل سیستم است و باید با در نظر گرفتن توسعه‌پذیری برای اضافه شدن Providerهای جدید طراحی شود.

بررسی کد واقعی پروژه:
- مدل `AIProvider` در `app/models/ai_provider.py` (خطوط 14-23) از قبل وجود دارد و شامل فیلدهای `id`, `user_id`, `name`, `description`, `is_enabled`, `created_at`, `updated_at` است. این مدل پایه‌ای برای ذخیره Providerها فراهم می‌کند.
- مدل `GlobalAnalysisPrompt` در `app/models/ai_provider.py` (خطوط 26-42) برای ذخیره پرامپت سراسری وجود دارد.
- فایل `app/services/ai/provider_service.py` شامل تابع `call_openai_chat` (خطوط 36-75) است که درخواست HTTP به OpenAI می‌فرستد. این تابع برای پشتیبانی از Providerهای جدید باید توسعه یابد.
- فایل `app/routes/ai.py` شامل endpointهای مربوط به AI است (مانند `/ai/providers` برای CRUD Providerها).
- فایل `app/schemas/ai_provider_schema.py` احتمالاً شامل Pydantic schemaهای مربوط به Providerها است.
- در `app/main.py` (خطوط 581-582) روترهای AI و Users mount شده‌اند.
- در `frontend/src/` هیچ صفحه تنظیمات اختصاصی برای AI Providerها وجود ندارد.

کلیدواژه‌های کاربر: صفحه تنظیمات, Provider, DeepSeek, GPT, Gemini, Claude, Grok, Perplexity, مدل, mapping

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک صفحه فرانت‌اند جدید در `frontend/src/pages/Settings.jsx` با مسیر `/settings` در روتر.
2. افزودن یک کامپوننت `ProviderList` در `frontend/src/components/ProviderList.jsx` برای نمایش لیست Providerها.
3. افزودن یک کامپوننت `ProviderForm` در `frontend/src/components/ProviderForm.jsx` برای افزودن/ویرایش Provider.
4. افزودن یک کامپوننت `ModelMapping` در `frontend/src/components/ModelMapping.jsx` برای mapping مدل‌ها به Providerها.
5. ایجاد endpointهای جدید در `app/routes/ai.py` برای مدیریت مدل‌های هر Provider (GET/POST/PUT/DELETE `/ai/providers/{provider_id}/models`).
6. ایجاد مدل جدید `AIModel` در `app/models/ai_model_config.py` (که از قبل وجود دارد) با فیلدهای `id`, `provider_id`, `name`, `display_name`, `is_enabled`, `max_tokens`, `temperature`.
7. به‌روزرسانی `app/services/ai/provider_service.py` برای پشتیبانی از Providerهای مختلف (نه فقط OpenAI).
8. افزودن ناوبری به صفحه تنظیمات در `frontend/src/components/Sidebar.jsx`.
9. اطمینان از توسعه‌پذیری با استفاده از الگوی Strategy Pattern در سرویس‌ها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 2: اضافه کردن جعبه پرامپت قابل ویرایش برای هر Provider/مدل

**Scope:** این مرحله شامل افزودن یک جعبه متن (text box) در صفحه تنظیمات، در کنار هر Provider یا مدل است که کاربر بتواند یک پرامپت (دستورالعمل) سفارشی برای آن بنویسد. این پرامپت باید همیشه قابل ویرایش باشد و کاربر بتواند هر زمان که بخواهد آن را تغییر دهد. خارج از این مرحله: اجرای واقعی آنالیز با استفاده از پرامپت. نکته حیاتی: پرامپت باید به صورت پویا ذخیره شود و در زمان آنالیز توسط مدل خوانده شود.
**Key terms:** جعبه پرامپت, پرامپت, ویرایش, صفحه تنظیمات, مدل

**بخش مربوط از متن کاربر:**
```
شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن جعبه پرامپت قابل ویرایش برای هر AI Provider/مدل

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-23` — `AIProvider`
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن یک جعبه متن (text box) در صفحه تنظیمات، در کنار هر Provider یا مدل را دارد که بتواند یک پرامپت (دستورالعمل) سفارشی برای آن بنویسد. این پرامپت باید همیشه قابل ویرایش باشد و کاربر بتواند هر زمان که بخواهد آن را تغییر دهد. خارج از این مرحله: اجرای واقعی آنالیز با استفاده از پرامپت. نکته حیاتی: پرامپت باید به صورت پویا ذخیره شود و در زمان آنالیز توسط مدل خوانده شود. کاربر می‌گوید: «شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه». کلیدواژه‌ها: جعبه پرامپت, پرامپت, ویرایش, صفحه تنظیمات, مدل.

شواهد در کد واقعی پروژه:
- فایل `app/models/ai_provider.py` شامل مدل `AIProvider` با فیلدهای `name`, `description`, `is_enabled` است (خطوط 14-23). این مدل فاقد فیلد `custom_prompt` است.
- فایل `app/models/ai_provider.py` شامل مدل `GlobalAnalysisPrompt` (خطوط 26-42) است که یک پرامپت سراسری برای همه تحلیل‌ها دارد، اما این پرامپت per-provider نیست.
- فایل `app/routes/ai.py` شامل endpointهای `/ai/providers` برای CRUD روی AIProviderها است.
- فایل `app/schemas/ai_provider_schema.py` احتمالاً شامل Pydantic schema برای AIProvider است.
- فایل `frontend/index.html` (خطوط 1-8) نشان‌دهنده frontend با React/Vite است.
- فایل `app/services/ai/provider_service.py` (خطوط 36-75) تابع `call_openai_chat` را دارد که پرامپت را به OpenAI می‌فرستد.

نیاز به افزودن فیلد `custom_prompt` به مدل `AIProvider`، به‌روزرسانی schema، endpointها و frontend برای نمایش و ویرایش جعبه پرامپت در صفحه تنظیمات وجود دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلد `custom_prompt` از نوع `Text` به مدل `AIProvider` در `app/models/ai_provider.py` (خط 19).
2. به‌روزرسانی Pydantic schema در `app/schemas/ai_provider_schema.py` برای شامل شدن `custom_prompt`.
3. به‌روزرسانی endpointهای CRUD در `app/routes/ai.py` برای پشتیبانی از فیلد جدید.
4. ایجاد migration جدید در `migrations/versions/` برای افزودن ستون `custom_prompt` به جدول `ai_providers`.
5. افزودن جعبه متن (textarea) در frontend در صفحه تنظیمات (احتمالاً `frontend/src/pages/Settings.jsx` یا کامپوننت مشابه) در کنار هر Provider.
6. اطمینان از اینکه پرامپت به صورت پویا ذخیره می‌شود و در زمان آنالیز توسط مدل خوانده می‌شود (در `app/services/ai/provider_service.py` یا `app/services/ai/nlp_service.py`).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: ایجاد مکانیزم دسترسی مدل‌ها به صفحات و داده‌های سیستم

**Scope:** این مرحله شامل پیاده‌سازی یک سیستم اشتراک‌گذاری داده است که به مدل‌های تعریف‌شده در مرحله 1 اجازه می‌دهد به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشند. این دسترسی باید به صورت خودکار و پویا باشد، به طوری که هر صفحه یا داده جدیدی که اضافه می‌شود، بلافاصله در دسترس مدل‌ها قرار گیرد. خارج از این مرحله: اجرای واقعی آنالیز. نکته حیاتی: این مکانیزم باید از یک API یا event-driven architecture استفاده کند تا داده‌ها به صورت real-time در دسترس باشند.
**Key terms:** دسترسی به صفحات, داده‌های داخل صفحه, دسترسی خودکار, real-time, API

**بخش مربوط از متن کاربر:**
```
اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. ... هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد مکانیزم دسترسی مدل‌ها به صفحات و داده‌های سیستم از طریق API event-driven

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک مکانیزم اشتراک‌گذاری داده را دارد که به مدل‌های تعریف‌شده (احتمالاً مدل‌های AI در app/models/ai_model_config.py و app/models/ai_provider.py) اجازه می‌دهد به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشند. این دسترسی باید به صورت خودکار و پویا باشد، به طوری که هر صفحه یا داده جدیدی که اضافه می‌شود، بلافاصله در دسترس مدل‌ها قرار گیرد. کاربر تأکید دارد: 'این مکانیزم باید از یک API یا event-driven architecture استفاده کند تا داده‌ها به صورت real-time در دسترس باشند.' کلیدواژه‌های اصلی: 'دسترسی به صفحات', 'داده‌های داخل صفحه', 'دسترسی خودکار', 'real-time', 'API'. در کد فعلی، مدل‌های داده در app/models/ تعریف شده‌اند (مانند task.py, project.py, todo_item.py, todo_list.py, notification.py, person.py, finance.py, user.py, local_file_entry.py, self_improvement.py). صفحات فرانت‌اند در frontend/src/pages/ (مانند Dashboard.jsx, Home.jsx, Tasks.jsx, Projects.jsx, Lists.jsx, ListDetail.jsx, Notifications.jsx, Login.jsx, Register.jsx) قرار دارند. سرویس‌های backend در app/services/ (مانند ai_service.py, list_service.py, todo_item_service.py, notification_service.py, person_service.py, planner_service.py, self_improvement_service.py, local_file_service.py, integration_service.py) وظیفه منطق تجاری را بر عهده دارند. روترها در app/routes/ (مانند ai.py, tasks.py, projects.py, lists.py, todo_items.py, notifications.py, users.py, auth.py, planner.py, self_improvement.py, finance.py, person.py, local_files.py, location.py, integrations.py, webhook.py) API endpoints را ارائه می‌دهند. کاربر خواسته 'خارج از این مرحله: اجرای واقعی آنالیز' انجام شود، یعنی تمرکز بر زیرساخت دسترسی است، نه خود تحلیل. همچنین 'نکته حیاتی: این مکانیزم باید از یک API یا event-driven architecture استفاده کند تا داده‌ها به صورت real-time در دسترس باشند.'

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام app/services/data_access_service.py که یک API مرکزی برای دسترسی مدل‌ها به تمام صفحات و داده‌های سیستم فراهم کند. این سرویس باید از یک event-driven architecture (با استفاده از Redis Pub/Sub یا Celery events که در حال حاضر در app/celery_app.py و app/tasks.py موجود است) برای انتشار رویدادهای 'data_created', 'data_updated', 'data_deleted' استفاده کند. 2. ایجاد یک endpoint جدید در app/routes/ai.py (یا یک روتر جدید app/routes/data_access.py) با مسیر /api/data-access/pages که لیست تمام صفحات (از frontend/src/pages/) را به صورت پویا برگرداند. 3. ایجاد endpoint /api/data-access/{page_name}/data که داده‌های مربوط به هر صفحه را از طریق سرویس‌های مربوطه (مانند list_service, task_service, project_service) جمع‌آوری کند. 4. پیاده‌سازی یک WebSocket endpoint (با استفاده از fastapi.WebSocket) در app/main.py یا یک فایل جدید برای اتصال real-time مدل‌ها به رویدادهای داده. 5. اصلاح app/services/ai_service.py و app/services/ai/nlp_service.py تا از data_access_service برای دریافت داده‌های context استفاده کنند. 6. اضافه کردن یک hook در app/routes/lists.py, app/routes/todo_items.py, app/routes/tasks.py, app/routes/projects.py, app/routes/notifications.py, app/routes/person.py, app/routes/finance.py, app/routes/local_files.py, app/routes/self_improvement.py که پس از هر CREATE/UPDATE/DELETE رویداد publish کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 4: پیاده‌سازی قابلیت آنالیز داده‌ها توسط مدل‌ها بر اساس پرامپت

**Scope:** این مرحله شامل پیاده‌سازی هسته اصلی قابلیت آنالیز است. مدل‌های تعریف‌شده باید بتوانند با استفاده از پرامپت اختصاصی خود (مرحله 2) و داده‌های موجود در سیستم (مرحله 3)، یک آنالیز کامل انجام دهند. کاربر باید بتواند از طریق یک رابط کاربری، درخواست آنالیز دهد و نتیجه را دریافت کند. خارج از این مرحله: طراحی UI پیشرفته برای نمایش نتایج. نکته حیاتی: این مرحله نیاز به integration با APIهای خارجی Providerها دارد.
**Key terms:** آنالیز, پرامپت, داده‌ها, API خارجی, Provider

**بخش مربوط از متن کاربر:**
```
و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی هسته آنالیز داده توسط مدل‌های AI با پرامپت اختصاصی

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت آنالیز داده‌ها توسط مدل‌های تعریف‌شده بر اساس پرامپت اختصاصی را دارد. بر اساس متن کاربر: «مدل‌های تعریف‌شده باید بتوانند با استفاده از پرامپت اختصاصی خود (مرحله 2) و داده‌های موجود در سیستم (مرحله 3)، یک آنالیز کامل انجام دهند. کاربر باید بتواند از طریق یک رابط کاربری، درخواست آنالیز دهد و نتیجه را دریافت کند. خارج از این مرحله: طراحی UI پیشرفته برای نمایش نتایج. نکته حیاتی: این مرحله نیاز به integration با APIهای خارجی Providerها دارد.» و «و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن». کلیدواژه‌های اصلی: آنالیز, پرامپت, داده‌ها, API خارجی, Provider.

شواهد در کد:
- فایل `app/services/ai/provider_service.py` (خطوط 36-75) تابع `call_openai_chat` را دارد که با OpenAI API تماس می‌گیرد و از `prompt`، `model`، `max_tokens`، `temperature` استفاده می‌کند.
- فایل `app/models/ai_provider.py` (خطوط 14-23) مدل `AIProvider` را تعریف کرده با فیلدهای `name`، `description`، `is_enabled`.
- فایل `app/models/ai_provider.py` (خطوط 26-42) مدل `GlobalAnalysisPrompt` را تعریف کرده با فیلد `prompt_text`.
- فایل `app/routes/ai.py` (مسیرهای `/ai/generate`، `/ai/query`، `/ai/providers`، `/ai/global-prompt`) زیرساخت API برای AI را فراهم کرده.
- فایل `app/services/ai/nlp_service.py` (import شده توسط `app/routes/ai.py`) سرویس NLP را مدیریت می‌کند.
- فایل `app/services/ai/model_service.py` سرویس CRUD برای مدل‌های AI را مدیریت می‌کند.

نیاز به ایجاد یک endpoint جدید مانند `POST /ai/analyze` است که:
1. پرامپت اختصاصی کاربر (از `GlobalAnalysisPrompt`) را دریافت کند.
2. داده‌های موجود در سیستم (تسک‌ها، پروژه‌ها، todo items) را جمع‌آوری کند.
3. با استفاده از `call_openai_chat` یا providerهای دیگر، آنالیز را انجام دهد.
4. نتیجه را به کاربر برگرداند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: ایجاد رابط کاربری برای نمایش نتایج آنالیز

**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک رابط کاربری مناسب برای نمایش نتایج آنالیز است. نتایج باید به صورت خوانا و ساختاریافته نمایش داده شوند. کاربر باید بتواند تاریخچه آنالیزها را مشاهده کند و نتایج قبلی را مرور نماید. خارج از این مرحله: ذخیره‌سازی طولانی‌مدت نتایج. نکته حیاتی: UI باید responsive باشد و نتایج را به صورت real-time یا پس از اتمام آنالیز نمایش دهد.
**Key terms:** رابط کاربری, نتایج آنالیز, تاریخچه, نمایش

**بخش مربوط از متن کاربر:**
```
کار آنالیز رو به شکلی که میگم انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد رابط کاربری نمایش نتایج آنالیز با تاریخچه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:1-50` — `router` — این فایل شامل endpointهای AI است. باید یک endpoint جدید برای تاریخچه آنالیز اضافه شود.
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  
  router = APIRouter(prefix='/ai', tags=['ai'])
  ```
- `frontend/src/pages/Dashboard.jsx:1-30` — `Dashboard` — این صفحه داشبورد اصلی است. می‌توان یک بخش جدید برای نمایش نتایج آنالیز به آن اضافه کرد یا یک صفحه مجزا ایجاد نمود.
  ```jsx
  import React, { useState, useEffect } from 'react';
  import { useAuth } from '../context/AuthContext';
  import { fetchTasks, fetchProjects } from '../lib/api';
  
  const Dashboard = () => {
    const { user } = useAuth();
    const [tasks, setTasks] = useState([]);
    const [projects, setProjects] = useState([]);
  
    useEffect(() => {
      const loadData = async () => {
        try {
          const tasksData = await fetchTasks();
          setTasks(tasksData);
          const projectsData = await fetchProjects();
          setProjects(projectsData);
        } catch (error) {
          console.error('Failed to load dashboard data:', error);
        }
      };
      loadData();
    }, []);
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/ai/nlp_service.py` — این سرویس پردازش NLP را انجام می‌

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک رابط کاربری (UI) برای نمایش نتایج آنالیز داده‌ها را دارد. این رابط باید نتایج را به صورت خوانا و ساختاریافته نمایش دهد. کاربر باید بتواند تاریخچه آنالیزها را مشاهده کرده و نتایج قبلی را مرور نماید. ذخیره‌سازی طولانی‌مدت نتایج خارج از این مرحله است. نکته حیاتی: UI باید responsive باشد و نتایج را به صورت real-time یا پس از اتمام آنالیز نمایش دهد. بخش مربوط از درخواست اصلی کاربر: 'کار آنالیز رو به شکلی که میگم انجام بدن'. کلیدواژه‌ها: رابط کاربری, نتایج آنالیز, تاریخچه, نمایش. در کد فعلی پروژه، هیچ endpoint یا صفحه‌ای برای نمایش نتایج آنالیز وجود ندارد. فایل‌های مرتبط شامل app/routes/ai.py (endpointهای AI مانند /ai/generate و /ai/query) و frontend/src/pages/Dashboard.jsx (صفحه داشبورد) هستند. همچنین app/services/ai/nlp_service.py و app/services/ai/model_service.py سرویس‌های پردازش AI را ارائه می‌دهند. برای پیاده‌سازی، نیاز به یک صفحه جدید در frontend (مثلاً AnalysisResults.jsx) و احتمالاً یک endpoint جدید در backend برای ذخیره و بازیابی تاریخچه آنالیزها داریم.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک صفحه جدید frontend به نام AnalysisResults.jsx در frontend/src/pages/ که نتایج آنالیز را نمایش دهد. 2. افزودن یک endpoint جدید در app/routes/ai.py (مثلاً GET /api/analysis/history) برای دریافت تاریخچه آنالیزها. 3. ایجاد یک سرویس جدید در app/services/analysis_service.py برای مدیریت منطق تجاری آنالیز. 4. به‌روزرسانی app/main.py برای ثبت مسیر جدید. 5. افزودن یک کامپوننت ResultCard.jsx در frontend/src/components/ برای نمایش هر نتیجه به صورت ساختاریافته. 6. پیاده‌سازی قابلیت real-time با استفاده از WebSocket یا polling در frontend. 7. اطمینان از responsive بودن UI با استفاده از Tailwind CSS.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: پیاده‌سازی قابلیت ویرایش پرامپت در هر زمان

**Scope:** این مرحله تضمین می‌کند که کاربر می‌تواند پرامپت اختصاصی هر مدل را در هر زمان و بدون محدودیت ویرایش کند. تغییرات باید بلافاصله ذخیره شوند و برای آنالیزهای بعدی اعمال گردند. خارج از این مرحله: اعتبارسنجی پیشرفته پرامپت. نکته حیاتی: ویرایش پرامپت نباید بر آنالیزهای در حال اجرا تأثیر بگذارد.
**Key terms:** ویرایش, پرامپت, ذخیره, بی‌محدودیت

**بخش مربوط از متن کاربر:**
```
این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت ویرایش پرامپت مدل‌های AI در هر زمان

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:مشخص نیست در deep-read موجود` — `router for /ai/configs` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، endpoint‌های CRUD برای configs در اینجا هستند.
- `app/models/ai_model_config.py:مشخص نیست در deep-read موجود` — `class AiModelConfig` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مدل احتمالاً دارای فیلد system_prompt است.
- `app/services/ai/model_service.py:مشخص نیست در deep-read موجود` — `function for updating model config` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. تابع جدید update_model_prompt باید اضافه شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/schemas/ai_schema.py` — نیاز به به

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت ویرایش پرامپت اختصاصی هر مدل AI را در هر زمان و بدون محدودیت داده است. کلیدواژه‌های اصلی: ویرایش, پرامپت, ذخیره, بی‌محدودیت. متن خام کاربر: «این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم». تغییرات باید بلافاصله ذخیره شوند و برای آنالیزهای بعدی اعمال گردند. خارج از این مرحله: اعتبارسنجی پیشرفته پرامپت. نکته حیاتی: ویرایش پرامپت نباید بر آنالیزهای در حال اجرا تأثیر بگذارد. در کد فعلی، مدل‌های AI از طریق endpoint‌های /ai/configs (GET, POST, PATCH, DELETE) در app/routes/ai.py مدیریت می‌شوند. مدل AiModelConfig در app/models/ai_model_config.py دارای فیلد system_prompt است که پرامپت سیستم را ذخیره می‌کند. همچنین endpoint /ai/global-prompt (GET, PUT) برای پرامپت سراسری وجود دارد. برای تحقق این درخواست، باید یک endpoint جدید PATCH /ai/configs/{config_id}/prompt یا PUT /ai/configs/{config_id}/prompt ایجاد شود که فقط فیلد system_prompt را به‌روزرسانی کند، بدون تأثیر بر آنالیزهای در حال اجرا (با استفاده از نسخه‌بندی یا snapshot). همچنین باید frontend مربوطه در صفحه تنظیمات AI (احتمالاً در frontend/src/pages/ یا کامپوننت‌های مرتبط) اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد endpoint جدید PATCH /ai/configs/{config_id}/prompt در app/routes/ai.py که فقط فیلد system_prompt مدل AiModelConfig را به‌روزرسانی کند. 2. افزودن تابع update_model_prompt در app/services/ai/model_service.py برای اعمال تغییرات. 3. اطمینان از عدم تأثیر بر آنالیزهای در حال اجرا: استفاده از مکانیزم snapshot یا versioning برای پرامپت‌ها (ذخیره نسخه قبلی در فیلد history یا جدول جداگانه). 4. افزودن frontend: یک فیلد متنی (textarea) در صفحه تنظیمات AI که پرامپت فعلی را نمایش دهد و با دکمه «ذخیره» تغییرات را اعمال کند. 5. به‌روزرسانی Pydantic schema در app/schemas/ai_schema.py برای پشتیبانی از این عملیات. 6. افزودن تست‌های unit و integration برای endpoint جدید.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 7: تست و اعتبارسنجی یکپارچگی سیستم

**Scope:** این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی‌شده است: مدیریت Providerها و مدل‌ها، ذخیره و ویرایش پرامپت، دسترسی به داده‌ها، اجرای آنالیز، و نمایش نتایج. تست‌ها باید شامل unit tests, integration tests و end-to-end tests باشند. خارج از این مرحله: تست performance و security. نکته حیاتی: تمام سناریوهای کاربری اصلی باید پوشش داده شوند.
**Key terms:** تست, اعتبارسنجی, unit test, integration test, e2e test

**بخش مربوط از متن کاربر:**
```
تمام مراحل بالا باید به صورت یکپارچه کار کنند
```

## 🎯 هدف (خلاصه ساختاریافته)
تست و اعتبارسنجی یکپارچگی سیستم مدیریت AI و Providerها

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست تست و اعتبارسنجی یکپارچگی سیستم را داده است. این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی‌شده است: مدیریت Providerها و مدل‌ها، ذخیره و ویرایش پرامپت، دسترسی به داده‌ها، اجرای آنالیز، و نمایش نتایج. تست‌ها باید شامل unit tests, integration tests و end-to-end tests باشند. خارج از این مرحله: تست performance و security. نکته حیاتی: تمام سناریوهای کاربری اصلی باید پوشش داده شوند. بخش مربوط از درخواست اصلی کاربر: 'تمام مراحل بالا باید به صورت یکپارچه کار کنند'. کلیدواژه‌ها: تست, اعتبارسنجی, unit test, integration test, e2e test.

شواهد در کد واقعی پروژه:
- فایل `app/routes/ai.py` شامل endpointهای مدیریت AI است: POST /ai/generate, POST /ai/feedback, GET /ai/metrics, GET/POST/PATCH/DELETE /ai/configs[/{id}], GET/POST/PATCH/DELETE /ai/providers[/{id}], GET/PUT /ai/global-prompt, POST /ai/query.
- فایل `app/services/ai/provider_service.py` مدیریت Providerها را انجام می‌دهد.
- فایل `app/services/ai/model_service.py` مدیریت مدل‌ها را انجام می‌دهد.
- فایل `app/services/ai/nlp_service.py` سرویس NLP را ارائه می‌دهد.
- فایل `app/models/ai_provider.py` مدل Provider را تعریف می‌کند.
- فایل `app/models/ai_model_config.py` مدل Config را تعریف می‌کند.
- فایل `app/schemas/ai_provider_schema.py` و `app/schemas/ai_schema.py` schemaهای مربوطه را تعریف می‌کنند.
- فایل `app/routes/integrations.py` شامل endpointهای مدیریت Integration است.
- فایل `app/services/integration_service.py` سرویس Integration را ارائه می‌دهد.
- فایل `app/models/integration.py` مدل Integration را تعریف می‌کند.
- فایل `app/schemas/integration_schema.py` schemaهای Integration را تعریف می‌کند.
- فایل `app/main.py` شامل startup event و middlewareهای مختلف است.
- فایل `frontend/src/pages/Dashboard.jsx` صفحه داشبورد را نمایش می‌دهد.
- فایل `frontend/src/context/AuthContext.jsx` context احراز هویت را مدیریت می‌کند.
- فایل `frontend/src/lib/api.js` توابع API را تعریف می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل تست واحد برای سرویس‌های AI: `backend/tests/test_ai_services.py` برای تست `app/services/ai/provider_service.py` و `app/services/ai/model_service.py`.
2. ایجاد فایل تست واحد برای سرویس Integration: `backend/tests/test_integration_service.py` برای تست `app/services/integration_service.py`.
3. ایجاد فایل تست یکپارچگی برای endpointهای AI: `backend/tests/test_ai_integration.py` برای تست POST /ai/generate, POST /ai/query, GET/POST/PATCH/DELETE /ai/configs[/{id}], GET/POST/PATCH/DELETE /ai/providers[/{id}], GET/PUT /ai/global-prompt.
4. ایجاد فایل تست یکپارچگی برای endpointهای Integration: `backend/tests/test_integration_integration.py` برای تست GET/POST/PATCH/DELETE /integrations.
5. ایجاد فایل تست end-to-end برای سناریوی کامل کاربری: `backend/tests/e2e/test_full_scenario.py` که شامل ایجاد Provider، ایجاد Config، اجرای query، و نمایش نتایج است.
6. اطمینان از پوشش تمام سناریوهای کاربری اصلی: ایجاد Provider جدید، ویرایش Provider، حذف Provider، ایجاد Config جدید، ویرایش Config، حذف Config، ذخیره پرامپت global، ویرایش پرامپت

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. جداول `ai_providers` و `ai_models` در پایگاه داده ایجاد شده باشند و جدول `ai_model_configs` با فیلدهای جدید `ai_model_id`, `alias`, `custom_api_key`, `custom_config_json`, `is_default_for_user` و حذف `model_name`, `provider` به‌روزرسانی شده باشد. _(verify: backend_test)_
2. مسیرهای API جدید برای CRUD ارائه‌دهندگان (مثلاً `POST /ai/providers`, `GET /ai/providers/{id}`) و مدل‌ها (مثلاً `POST /ai/models`, `GET /ai/models/{id}`) در `app/routes/ai.py` قابل دسترسی باشند. _(verify: api_response)_
3. توابع سرویس در `app/services/ai/provider_service.py` و `app/services/ai/model_service.py` (یا فایل‌های مشابه) برای مدیریت `AIProvider` و `AIModel` و `AIModelConfig` (کاربر-محور) به درستی پیاده‌سازی شده باشند. _(verify: static)_
4. یک صفحه تنظیمات در فرانت‌اند (مثلاً `frontend/src/pages/AISettings.jsx`) وجود داشته باشد که امکان مشاهده، افزودن و ویرایش ارائه‌دهندگان و مدل‌های هوش مصنوعی را فراهم کند. _(verify: ui_interaction)_
5. کلیدهای API (چه در سطح ارائه‌دهنده و چه در سطح کاربر) قبل از ذخیره در پایگاه داده رمزنگاری شوند و هنگام بازیابی رمزگشایی شوند. _(verify: backend_test)_
6. فایل `app/models/ai_provider.py` باید شامل تعریف کلاس `AIProvider` با فیلدهای `id`, `name`, `description`, `is_enabled`, `user_id`, `created_at`, `updated_at` باشد. _(verify: static)_
7. فایل `app/schemas/ai_provider_schema.py` باید شامل شمای Pydantic `AIProviderResponse` با فیلدهای `id`, `name`, `description`, `is_enabled`, `user_id`, `created_at`, `updated_at` باشد. _(verify: static)_
8. endpoint `POST /api/ai/providers` باید با احراز هویت ادمین، یک ارائه‌دهنده جدید ایجاد کرده و با کد وضعیت 200 یا 201 و جزئیات ارائه‌دهنده پاسخ دهد. _(verify: api_response)_
9. endpoint `GET /api/ai/providers` باید با احراز هویت ادمین، لیستی از ارائه‌دهندگان هوش مصنوعی را برگرداند. _(verify: api_response)_
10. کاربران غیر ادمین که تلاش می‌کنند به endpointهای مدیریت ارائه‌دهندگان (مانند `POST /api/ai/providers`) دسترسی پیدا کنند، باید خطای 403 Forbidden دریافت کنند. _(verify: api_response)_
11. یک اسکریپت مهاجرت Alembic جدید باید جدول `ai_providers` را در دیتابیس ایجاد کند. _(verify: backend_test)_
12. مدل `AIModelConfig` در `app/models/ai_model_config.py` باید فیلد `provider` از نوع `str` داشته باشد. _(verify: static)_
13. شمای `AIModelConfigCreate` در `app/schemas/ai_schema.py` باید شامل فیلد `provider` از نوع `str` باشد. _(verify: static)_
14. `POST /api/ai/configs` باید امکان ایجاد یک مدل جدید را با تعیین `provider` فراهم کند و کد وضعیت 201 برگرداند. پاسخ باید شامل `id`, `name`, و `provider` باشد. _(verify: api_response)_
15. `GET /api/ai/configs` باید لیست مدل‌ها را برگرداند و از پارامتر کوئری `provider` برای فیلتر کردن پشتیبانی کند. اگر `provider` مشخص شود، فقط مدل‌های آن ارائه‌دهنده برگردانده شوند. _(verify: api_response)_
16. `PATCH /api/ai/configs/{config_id}` باید امکان به‌روزرسانی فیلد `provider` یک مدل موجود را فراهم کند و کد وضعیت 200 برگرداند. _(verify: api_response)_
17. `DELETE /api/ai/configs/{config_id}` باید یک مدل را حذف کند و کد وضعیت 204 برگرداند. _(verify: api_response)_
18. مستندات `docs/API.md` باید به‌روزرسانی شود تا فیلد `provider` در شمای مدل‌ها و قابلیت فیلتر کردن با پارامتر کوئری `provider` را منعکس کند. _(verify: manual_only)_
19. جدول `global_analysis_prompts` باید در پایگاه داده با فیلدهای `id`, `prompt_text`, `last_edited_at`, و `edited_by_user_id` وجود داشته باشد. _(verify: static)_
20. endpoint `GET /api/ai/global-prompt` باید پرامپت تحلیل جهانی را برگرداند (یا یک پرامپت پیش‌فرض اگر وجود نداشت). _(verify: api_response)_
21. endpoint `PUT /api/ai/global-prompt` باید بتواند پرامپت را به‌روزرسانی کند و فیلدهای `last_edited_at` و `edited_by_user_id` را به‌درستی تنظیم کند. _(verify: api_response)_
22. سرویس‌های AI (مانند `app/services/ai_service.py` یا `app/services/ai/nlp_service.py`) باید بتوانند پرامپت تحلیل جهانی را از `global_prompt_service` دریافت و در عملیات خود استفاده کنند. _(verify: static)_
23. یک migration Alembic برای ایجاد جدول `global_analysis_prompts` باید با موفقیت اعمال شود. _(verify: backend_test)_
24. فایل‌های `app/models/analysis_prompt.py` و `app/services/ai/analysis_prompt_service.py` ایجاد شده و شامل مدل و سرویس مربوطه باشند. _(verify: static)_
25. endpoint `GET /api/ai/analysis_prompt` با موفقیت پرامپت تحلیل جهانی را برمی‌گرداند (کد وضعیت 200) و اگر پرامپتی وجود نداشته باشد، یک پرامپت پیش‌فرض خالی یا اولیه را برمی‌گرداند. _(verify: api_response)_
26. endpoint `PUT /api/ai/analysis_prompt` برای کاربران غیر ادمین با کد وضعیت 403 (Forbidden) پاسخ می‌دهد. _(verify: api_response)_
27. endpoint `PUT /api/ai/analysis_prompt` برای کاربر ادمین با موفقیت پرامپت را به‌روزرسانی می‌کند (کد وضعیت 200) و محتوای به‌روزرسانی شده را برمی‌گرداند. _(verify: api_response)_
28. پس از به‌روزرسانی پرامپت توسط ادمین، فراخوانی مجدد `GET /api/ai/analysis_prompt` محتوای به‌روزرسانی شده را برمی‌گرداند که نشان‌دهنده ذخیره صحیح در پایگاه داده است. _(verify: api_response)_
29. فایل `app/services/ai/ai_data_access_service.py` ایجاد شده و شامل توابعی برای بازیابی داده‌های `Task`, `Project`, `TodoItem` و `Notification` برای یک `user_id` مشخص است. _(verify: static)_
30. یک endpoint جدید `GET /api/ai/user_data_context` در `app/routes/ai.py` تعریف شده که نیاز به احراز هویت دارد و داده‌های متنی کاربر را برمی‌گرداند. _(verify: api_response)_
31. داده‌های بازگشتی از `GET /api/ai/user_data_context` فقط شامل اطلاعات مربوط به کاربر احراز هویت شده است و هیچ داده‌ای از کاربران دیگر را افشا نمی‌کند. _(verify: backend_test)_
32. شمای Pydantic `AIContextResponse` و `AIContextItem` در `app/schemas/ai_schema.py` تعریف شده‌اند که ساختار داده‌های بازگشتی برای AI را مشخص می‌کنند. _(verify: static)_
33. سرویس `app/services/ai_service.py` قابلیت استفاده از داده‌های 'context' فراهم شده توسط `ai_data_access_service` را در توابع تولید/پرس‌وجوی AI دارد. _(verify: static)_
34. یک endpoint جدید `POST /api/ai/analyze` در `app/routes/ai.py` تعریف شده باشد که `AIAnalysisRequest` را دریافت و `AIAnalysisResult` را برگرداند. _(verify: static)_
35. متد `orchestrate_analysis` در `app/services/ai_service.py` پیاده‌سازی شده باشد که پرامپت، داده‌های مرتبط را دریافت و مدل AI را فراخوانی کند. _(verify: static)_
36. ارسال یک درخواست `POST` به `/api/ai/analyze` با `model_id` و `prompt` معتبر، پاسخ `200 OK` با یک `AIAnalysisResult` معتبر را برگرداند. _(verify: api_response)_
37. اگر `FEATURE_AI_ENABLED` در تنظیمات `app/core/config.py` غیرفعال باشد، endpoint `/api/ai/analyze` باید `403 Forbidden` برگرداند. _(verify: api_response)_
38. تست‌های واحد برای متد `orchestrate_analysis` در `tests/test_ai_service.py` اضافه شده باشد که سناریوهای مختلف (انتخاب مدل، بازیابی داده، فراخوانی API) را پوشش دهد. _(verify: backend_test)_
39. فایل جدید `frontend/src/pages/Settings.jsx` ایجاد شده و یک کامپوننت React را export می‌کند. _(verify: static)_
40. صفحه تنظیمات از طریق URL `/settings` قابل دسترسی است و در داخل کامپوننت `Layout` (شامل Header، Sidebar و Footer) رندر می‌شود. _(verify: ui_interaction)_
41. صفحه `Settings` شامل بخش‌های نگهدارنده مجزا برای «مدیریت ارائه‌دهندگان AI»، «مدیریت مدل‌های AI» و «جعبه پرامپت تحلیل» است. _(verify: ui_interaction)_
42. یک لینک ناوبری به صفحه «تنظیمات» در کامپوننت `Sidebar` وجود دارد. _(verify: ui_interaction)_
43. فایل `frontend/src/App.jsx` شامل مسیر جدید برای `/settings` است. _(verify: static)_
44. کاربر می‌تواند به صفحه جدید تنظیمات ارائه‌دهندگان هوش مصنوعی از طریق مسیر `/settings` دسترسی پیدا کند. _(verify: ui_interaction)_
45. صفحه تنظیمات باید لیستی از ارائه‌دهندگان هوش مصنوعی موجود را نمایش دهد (با فراخوانی `GET /api/ai/configs`). _(verify: ui_interaction)_
46. کاربر می‌تواند از طریق یک فرم در صفحه تنظیمات، ارائه‌دهنده هوش مصنوعی جدیدی را اضافه کند (با فراخوانی `POST /api/ai/configs`). _(verify: ui_interaction)_
47. کاربر می‌تواند جزئیات یک ارائه‌دهنده هوش مصنوعی موجود را ویرایش کند (با فراخوانی `PATCH /api/ai/configs/{config_id}`). _(verify: ui_interaction)_
48. کاربر می‌تواند یک ارائه‌دهنده هوش مصنوعی را از لیست حذف کند (با فراخوانی `DELETE /api/ai/configs/{config_id}`). _(verify: ui_interaction)_
49. یک لینک به صفحه تنظیمات ارائه‌دهندگان هوش مصنوعی در نوار کناری (Sidebar) یا سربرگ (Header) فرانت‌اند وجود دارد. _(verify: ui_interaction)_
50. مسیر `/settings/ai-models` در فرانت‌اند قابل دسترسی باشد و صفحه مدیریت مدل‌های AI را نمایش دهد. _(verify: ui_interaction)_
51. صفحه مدیریت مدل‌ها، لیستی از مدل‌های AI موجود را از endpoint `/api/ai/configs` دریافت و نمایش دهد. این لیست باید شامل فیلدهای `id`, `name`, و `provider` باشد. _(verify: api_response)_
52. کاربر بتواند از طریق فرم، یک مدل AI جدید اضافه کند که با `POST` به `/api/ai/configs` ارسال شود و پس از موفقیت، مدل جدید در لیست نمایش داده شود. _(verify: ui_interaction)_
53. کاربر بتواند یک مدل AI موجود را ویرایش کند که با `PATCH` به `/api/ai/configs/{id}` ارسال شود و تغییرات در لیست منعکس شود. _(verify: ui_interaction)_
54. کاربر بتواند یک مدل AI موجود را حذف کند که با `DELETE` به `/api/ai/configs/{id}` ارسال شود و از لیست حذف شود. _(verify: ui_interaction)_
55. فیلدهای فرم افزودن/ویرایش مدل شامل گزینه‌ای برای انتخاب ارائه‌دهنده (provider) باشد که از لیست ارائه‌دهندگان موجود پر شود. _(verify: static)_
56. جدول `global_settings` با ستون‌های `id`, `key` (UNIQUE), `value` در پایگاه داده ایجاد شود. _(verify: static)_
57. endpoint `GET /api/settings/global-analysis-prompt` برای ادمین، پرامپت تحلیل جهانی را برمی‌گرداند (یا یک مقدار پیش‌فرض اگر وجود نداشته باشد). _(verify: api_response)_
58. endpoint `PUT /api/settings/global-analysis-prompt` برای ادمین، پرامپت تحلیل جهانی را به‌روزرسانی می‌کند. _(verify: api_response)_
59. دسترسی به endpointهای `global-analysis-prompt` برای کاربران غیرادمین با خطای 403 Forbidden مواجه شود. _(verify: api_response)_
60. یک لینک 'Settings' در `frontend/src/components/Sidebar.jsx` به مسیر `/settings` اضافه شود. _(verify: static)_
61. صفحه `/settings` در فرانت‌اند یک `textarea` نمایش دهد که با مقدار فعلی پرامپت تحلیل جهانی پر شده است. _(verify: ui_interaction)_
62. دکمه 'ذخیره' در صفحه تنظیمات، تغییرات را به بک‌اند ارسال کرده و پرامپت را به‌روزرسانی کند. _(verify: ui_interaction)_
63. دکمه 'لغو' در صفحه تنظیمات، تغییرات محلی را نادیده گرفته و مقدار اصلی پرامپت را بازیابی کند. _(verify: ui_interaction)_
64. فایل `app/services/event_publisher.py` و تابع `publish_data_change_event` در آن وجود داشته باشد. _(verify: static)_
65. وظیفه Celery به نام `process_ai_ingestion_event` در `app/tasks.py` تعریف شده باشد که `entity_type`, `entity_id`, و `action` را به‌عنوان آرگومان می‌پذیرد. _(verify: static)_
66. پس از ایجاد یک `TodoItem` جدید (مثلاً از طریق `app/routes/todo_items.py` که `todo_item_service` را فراخوانی می‌کند)، یک وظیفه `process_ai_ingestion_event` با `entity_type='todo_item'`, `entity_id=<new_item_id>`, `action='created'` در صف Celery قرار گیرد. _(verify: backend_test)_
67. سرویس `app/services/ai_ingestion_service.py` بتواند یک `TodoItem` را با `ID` بازیابی کرده و محتوای متنی آن (مانند `description`) را به `app/services/ai/nlp_service.py` برای پردازش ارسال کند. _(verify: backend_test)_
68. تابع `analyze_content` در `app/services/ai/nlp_service.py` بتواند محتوای متنی را از انواع مختلف موجودیت‌ها پردازش کرده و یک نتیجه تحلیل (حداقل یک دیکشنری با فیلدهای `summary` و `keywords`) برگرداند. _(verify: static)_

## Task Steps

### Step 1: ایجاد صفحه تنظیمات اصلی با قابلیت مدیریت Providerها و مدل‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند لیست Providerهای هوش مصنوعی (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و چند مورد دیگر) را مشاهده، اضافه و مدیریت کند. برای هر Provider، کاربر باید بتواند مدل‌های مختلف آن را mapping و اضافه کند. این صفحه باید امکان ذخیره و ویرایش تنظیمات را فراهم کند. خارج از این مرحله: قابلیت آنالیز داده‌ها، جعبه پرامپت، و دسترسی به صفحات دیگر. نکته حیاتی: این صفحه پایه و اساس کل سیستم است و باید با در نظر گرفتن توسعه‌پذیری برای اضافه شدن Providerهای جدید طراحی شود.
**Excerpt:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

### Step 2: اضافه کردن جعبه پرامپت قابل ویرایش برای هر Provider/مدل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن یک جعبه متن (text box) در صفحه تنظیمات، در کنار هر Provider یا مدل است که کاربر بتواند یک پرامپت (دستورالعمل) سفارشی برای آن بنویسد. این پرامپت باید همیشه قابل ویرایش باشد و کاربر بتواند هر زمان که بخواهد آن را تغییر دهد. خارج از این مرحله: اجرای واقعی آنالیز با استفاده از پرامپت. نکته حیاتی: پرامپت باید به صورت پویا ذخیره شود و در زمان آنالیز توسط مدل خوانده شود.
**Excerpt:**
```
شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه
```

### Step 3: ایجاد مکانیزم دسترسی مدل‌ها به صفحات و داده‌های سیستم
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک سیستم اشتراک‌گذاری داده است که به مدل‌های تعریف‌شده در مرحله 1 اجازه می‌دهد به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشند. این دسترسی باید به صورت خودکار و پویا باشد، به طوری که هر صفحه یا داده جدیدی که اضافه می‌شود، بلافاصله در دسترس مدل‌ها قرار گیرد. خارج از این مرحله: اجرای واقعی آنالیز. نکته حیاتی: این مکانیزم باید از یک API یا event-driven architecture استفاده کند تا داده‌ها به صورت real-time در دسترس باشند.
**Excerpt:**
```
اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. ... هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.
```

### Step 4: پیاده‌سازی قابلیت آنالیز داده‌ها توسط مدل‌ها بر اساس پرامپت
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی هسته اصلی قابلیت آنالیز است. مدل‌های تعریف‌شده باید بتوانند با استفاده از پرامپت اختصاصی خود (مرحله 2) و داده‌های موجود در سیستم (مرحله 3)، یک آنالیز کامل انجام دهند. کاربر باید بتواند از طریق یک رابط کاربری، درخواست آنالیز دهد و نتیجه را دریافت کند. خارج از این مرحله: طراحی UI پیشرفته برای نمایش نتایج. نکته حیاتی: این مرحله نیاز به integration با APIهای خارجی Providerها دارد.
**Excerpt:**
```
و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

### Step 5: ایجاد رابط کاربری برای نمایش نتایج آنالیز
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک رابط کاربری مناسب برای نمایش نتایج آنالیز است. نتایج باید به صورت خوانا و ساختاریافته نمایش داده شوند. کاربر باید بتواند تاریخچه آنالیزها را مشاهده کند و نتایج قبلی را مرور نماید. خارج از این مرحله: ذخیره‌سازی طولانی‌مدت نتایج. نکته حیاتی: UI باید responsive باشد و نتایج را به صورت real-time یا پس از اتمام آنالیز نمایش دهد.
**Excerpt:**
```
کار آنالیز رو به شکلی که میگم انجام بدن
```

### Step 6: پیاده‌سازی قابلیت ویرایش پرامپت در هر زمان
**Status:** `pending` (0%)
**Scope:** این مرحله تضمین می‌کند که کاربر می‌تواند پرامپت اختصاصی هر مدل را در هر زمان و بدون محدودیت ویرایش کند. تغییرات باید بلافاصله ذخیره شوند و برای آنالیزهای بعدی اعمال گردند. خارج از این مرحله: اعتبارسنجی پیشرفته پرامپت. نکته حیاتی: ویرایش پرامپت نباید بر آنالیزهای در حال اجرا تأثیر بگذارد.
**Excerpt:**
```
این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم
```

### Step 7: تست و اعتبارسنجی یکپارچگی سیستم
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تست کامل تمام قابلیت‌های پیاده‌سازی‌شده است: مدیریت Providerها و مدل‌ها، ذخیره و ویرایش پرامپت، دسترسی به داده‌ها، اجرای آنالیز، و نمایش نتایج. تست‌ها باید شامل unit tests, integration tests و end-to-end tests باشند. خارج از این مرحله: تست performance و security. نکته حیاتی: تمام سناریوهای کاربری اصلی باید پوشش داده شوند.
**Excerpt:**
```
تمام مراحل بالا باید به صورت یکپارچه کار کنند
```
