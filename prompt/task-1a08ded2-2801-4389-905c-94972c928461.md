---
task_id: 1a08ded2-2801-4389-905c-94972c928461
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:15:14.628371+00:00'
updated_at: '2026-05-29T09:41:10.452591+00:00'
target_files:
- app/models/ai_provider.py
- frontend/src/pages/AISettings.jsx
- app/routes/ai.py
- app/services/ai/model_service.py
- app/models/ai_model_config.py
- app/services/ai/provider_service.py
- frontend/src/pages/SmartAssistant.jsx
- frontend/src/lib/api.js
- app/routes/ai
- app/main.py
- frontend/src/components/Sidebar.jsx
- app/tasks.py
- app/celery_app.py
- app/models/ai_assessment.py
- app/services/ai/__init__.py
- app/services/ai/ai_data_access_service.py
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_292354_AgADih0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 4,248 char متن_

## شرح ایده اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## ویژگی‌های کلیدی و قابلیت‌ها  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پرپلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## اهداف و مزایای اصلی  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پروایدرها رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پروایدرها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم، اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## بازار هدف  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جمنی، کلاد، گراک، پلکسیتی همچین چیزی. و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## پتانسیل رشد و چشم‌انداز  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پروواید پرووایدها رو بتونم قرار بدم مثل دیپ سیک، جی‌پی‌تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_292354_AgADih0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 4,248 char متن_

## شرح ایده اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## ویژگی‌های کلیدی و قابلیت‌ها  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پرپلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## اهداف و مزایای اصلی  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پروایدرها رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پروایدرها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم، اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## بازار هدف  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جمنی، کلاد، گراک، پلکسیتی همچین چیزی. و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## پتانسیل رشد و چشم‌انداز  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پروواید پرووایدها رو بتونم قرار بدم مثل دیپ سیک، جی‌پی‌تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.
```

## 📋 چک‌لیست مراحل (20 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد صفحه تنظیمات اصلی با قابلیت مدیریت Providerها و مدل‌ها** — این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند Providerهای مختلف (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و غیره) را اضافه کرده و برای هر Provider مدل‌های مربوطه را mapping و add کند. این صفحه باید امکان افزودن، ویرایش و حذف Providerها و مدل‌های آن‌ها را ف
- [ ] **مرحله 2: افزودن جعبه پرامپت قابل ویرایش در صفحه تنظیمات** — این مرحله شامل افزودن یک جعبه متن (text box) در صفحه تنظیمات است که کاربر بتواند یک پرامپت سفارشی برای آنالیز داده‌ها بنویسد. این جعبه باید همیشه قابل ویرایش باشد و کاربر بتواند هر زمان که بخواهد محتوای آن را تغییر دهد. پرامپت باید به‌صورت پویا ذخیره شود تا در زمان آنالیز توسط مدل‌ها استفاده شود. خا
- [ ] **مرحله 3: ایجاد قابلیت دسترسی مدل‌ها به صفحات و داده‌های داخل هر صفحه** — این مرحله شامل پیاده‌سازی مکانیزمی است که به مدل‌های تعریف‌شده در صفحه تنظیمات اجازه دهد به تمام صفحات موجود در سیستم و داده‌های داخل آن‌ها دسترسی داشته باشند. این دسترسی باید به‌صورت实时 (real-time) باشد، به این معنی که هر صفحه یا داده جدیدی که اضافه می‌شود بلافاصله برای مدل‌ها قابل دسترسی باشد. خارج
- [ ] **مرحله 4: پیاده‌سازی قابلیت آنالیز داده‌ها توسط مدل‌ها بر اساس پرامپت** — این مرحله شامل پیاده‌سازی هسته اصلی آنالیز است که در آن مدل‌های تعریف‌شده (با توجه به پرامپت کاربر) داده‌های صفحات را آنالیز می‌کنند. این فرآیند باید از پرامپت ذخیره‌شده در مرحله ۲ استفاده کند و داده‌های جمع‌آوری‌شده از مرحله ۳ را به مدل ارسال کند. خروجی آنالیز باید به کاربر نمایش داده شود. خارج از 
- [ ] **مرحله 5: ذخیره‌سازی و بازیابی تنظیمات (Providerها، مدل‌ها، پرامپت) در دیتابیس یا فایل** — این مرحله شامل پیاده‌سازی لایه ذخیره‌سازی برای تمام تنظیمات مربوط به Providerها، مدل‌ها و پرامپت است. این داده‌ها باید در یک دیتابیس (مانند SQLite، PostgreSQL) یا فایل JSON ذخیره شوند تا پس از رفرش صفحه یا بستن مرورگر از بین نروند. همچنین باید قابلیت بازیابی این تنظیمات در زمان لود صفحه وجود داشته ب
- [ ] **مرحله 6: ایجاد API برای ارتباط بین فرانت‌اند و بک‌اند (مدیریت تنظیمات و آنالیز)** — این مرحله شامل ایجاد APIهای لازم برای ارتباط بین فرانت‌اند (صفحه تنظیمات) و بک‌اند است. این APIها باید شامل endpoints برای CRUD عملیات روی Providerها و مدل‌ها، ذخیره و بازیابی پرامپت، و trigger آنالیز باشند. خارج از این مرحله: UI و منطق آنالیز. نکته حیاتی: APIها باید از استاندارد REST پیروی کنند و خ
- [ ] **مرحله 7: پیاده‌سازی قابلیت افزودن Providerهای جدید توسط کاربر (نه فقط predefined)** — این مرحله شامل افزودن قابلیتی است که کاربر بتواند Providerهای جدید (غیر از لیست predefined) را به سیستم اضافه کند. این شامل فیلدهایی برای نام Provider، base URL API، و کلید API (API key) است. خارج از این مرحله: مدیریت مدل‌های هر Provider. نکته حیاتی: این قابلیت باید امن باشد و کلیدهای API به‌صورت ام
- [ ] **مرحله 8: پیاده‌سازی قابلیت mapping مدل‌ها برای هر Provider** — این مرحله شامل افزودن قابلیتی است که کاربر بتواند برای هر Provider، مدل‌های مختلف را mapping کند. این شامل انتخاب یک Provider، مشاهده لیست مدل‌های موجود (در صورت امکان از طریق API Provider)، و افزودن مدل‌های جدید با نام و شناسه (model ID) است. خارج از این مرحله: افزودن Provider جدید. نکته حیاتی: map
- [ ] **مرحله 9: ایجاد قابلیت انتخاب مدل برای آنالیز (کدام مدل از کدام Provider استفاده شود)** — این مرحله شامل افزودن قابلیتی است که کاربر بتواند مشخص کند کدام مدل از کدام Provider برای آنالیز استفاده شود. این می‌تواند به‌صورت یک dropdown یا selector در صفحه تنظیمات یا در صفحه آنالیز باشد. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: کاربر باید بتواند چندین مدل را برای آنالیز همزمان انتخاب کند
- [ ] **مرحله 10: پیاده‌سازی نمایش نتایج آنالیز در یک بخش مجزا** — این مرحله شامل ایجاد یک بخش UI برای نمایش نتایج آنالیز است. این بخش باید بتواند خروجی مدل‌ها (متن، JSON، یا هر فرمت دیگر) را به‌صورت readable نمایش دهد. همچنین باید قابلیت مقایسه نتایج چند مدل (در صورت انتخاب چند مدل) وجود داشته باشد. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: نتایج باید به‌صورت r
- [ ] **مرحله 11: افزودن قابلیت ذخیره و بارگذاری تنظیمات به‌صورت export/import** — این مرحله شامل افزودن قابلیت export تنظیمات (Providerها، مدل‌ها، پرامپت) به یک فایل JSON و import آن فایل برای بازگردانی تنظیمات است. این قابلیت به کاربر اجازه می‌دهد تنظیمات خود را بین محیط‌های مختلف جابجا کند. خارج از این مرحله: UI اصلی تنظیمات. نکته حیاتی: فایل export باید شامل تمام تنظیمات باشد 
- [ ] **مرحله 12: پیاده‌سازی قابلیت تست اتصال به Provider (Test Connection)** — این مرحله شامل افزودن دکمه 'Test Connection' برای هر Provider است که اتصال به API Provider را با استفاده از base URL و API key ذخیره‌شده تست می‌کند. نتیجه تست (موفقیت یا خطا) باید به کاربر نمایش داده شود. خارج از این مرحله: مدیریت مدل‌ها. نکته حیاتی: این قابلیت باید از یک endpoint ساده Provider (مان
- [ ] **مرحله 13: ایجاد قابلیت logging و نمایش خطاهای آنالیز** — این مرحله شامل پیاده‌سازی یک سیستم logging است که خطاهای رخ‌داده در طول آنالیز (مانند خطای API، timeout، خطای پرامپت) را ثبت کرده و به کاربر نمایش دهد. این logs باید شامل timestamp، نوع خطا، و پیام خطا باشند. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: logs باید در یک بخش مجزا در UI قابل مشاهده باش
- [ ] **مرحله 14: بهبود UI/UX صفحه تنظیمات با طراحی responsive و کاربرپسند** — این مرحله شامل بهبود ظاهر و تجربه کاربری صفحه تنظیمات است. این شامل طراحی responsive برای نمایش در دستگاه‌های مختلف، استفاده از کامپوننت‌های UI مدرن (مانند Material-UI، Ant Design، یا Tailwind)، و بهبود چیدمان عناصر (Providerها، مدل‌ها، پرامپت) است. خارج از این مرحله: منطق کسب‌وکار. نکته حیاتی: طراح
- [ ] **مرحله 15: افزودن قابلیت جستجو و فیلتر در لیست Providerها و مدل‌ها** — این مرحله شامل افزودن قابلیت جستجو و فیلتر در لیست Providerها و مدل‌ها است. کاربر باید بتواند با تایپ نام Provider یا مدل، لیست را فیلتر کند. همچنین می‌توان فیلترهای دیگری مانند 'فقط Providerهای فعال' اضافه کرد. خارج از این مرحله: CRUD عملیات. نکته حیاتی: جستجو باید به‌صورت real-time و بدون نیاز به 
- [ ] **مرحله 16: پیاده‌سازی قابلیت تعیین اولویت (Priority) برای Providerها** — این مرحله شامل افزودن قابلیت تعیین اولویت برای Providerها است. کاربر می‌تواند ترتیب Providerها را مشخص کند (مثلاً از طریق drag-and-drop) تا در زمان آنالیز، مدل‌ها به ترتیب اولویت استفاده شوند (در صورت failover). خارج از این مرحله: منطق failover. نکته حیاتی: اولویت‌ها باید در دیتابیس ذخیره شوند.
- [ ] **مرحله 17: ایجاد قابلیت تعیین حداکثر توکن (Max Tokens) و دما (Temperature) برای هر مدل** — این مرحله شامل افزودن فیلدهای پیکربندی برای هر مدل است: حداکثر توکن (max tokens) و دما (temperature). این پارامترها در زمان آنالیز به API Provider ارسال می‌شوند. خارج از این مرحله: سایر پارامترهای API. نکته حیاتی: مقادیر پیش‌فرض باید منطقی باشند (مثلاً max tokens: 2048, temperature: 0.7).
- [ ] **مرحله 18: پیاده‌سازی قابلیت آنالیز دسته‌ای (Batch Analysis) چند صفحه به طور همزمان** — این مرحله شامل افزودن قابلیتی است که کاربر بتواند چندین صفحه را برای آنالیز انتخاب کند و مدل‌ها همه آن‌ها را به‌صورت دسته‌ای آنالیز کنند. نتایج باید به‌صورت جداگانه برای هر صفحه نمایش داده شوند. خارج از این مرحله: آنالیز تکی. نکته حیاتی: این قابلیت باید از queue system استفاده کند تا درخواست‌ها مدیر
- [ ] **مرحله 19: افزودن قابلیت ذخیره نتایج آنالیز در تاریخچه** — این مرحله شامل افزودن قابلیت ذخیره نتایج آنالیز در یک تاریخچه (history) است. کاربر می‌تواند نتایج آنالیزهای قبلی را مشاهده، جستجو، و دوباره بارگذاری کند. خارج از این مرحله: اجرای آنالیز جدید. نکته حیاتی: تاریخچه باید شامل timestamp، مدل استفاده‌شده، و صفحه آنالیز شده باشد.
- [ ] **مرحله 20: پیاده‌سازی قابلیت اشتراک‌گذاری نتایج آنالیز (Share Results)** — این مرحله شامل افزودن قابلیت اشتراک‌گذاری نتایج آنالیز از طریق لینک یا export به فرمت‌های مختلف (PDF, Markdown, Text) است. کاربر می‌تواند یک لینک قابل اشتراک‌گذاری تولید کند یا نتیجه را به فرمت مورد نظر دانلود کند. خارج از این مرحله: ذخیره نتایج. نکته حیاتی: لینک‌های اشتراک‌گذاری باید دارای expirati

---

# 🔹 مرحله 1: ایجاد صفحه تنظیمات اصلی با قابلیت مدیریت Providerها و مدل‌ها

**Scope:** این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند Providerهای مختلف (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و غیره) را اضافه کرده و برای هر Provider مدل‌های مربوطه را mapping و add کند. این صفحه باید امکان افزودن، ویرایش و حذف Providerها و مدل‌های آن‌ها را فراهم کند. خارج از این مرحله: قابلیت آنالیز داده‌ها، جعبه پرامپت، و دسترسی به صفحات دیگر. نکته حیاتی: این صفحه باید به‌عنوان یک ماژول مستقل عمل کند و داده‌های Providerها و مدل‌ها را در یک ساختار داده قابل ذخیره‌سازی (مانند دیتابیس یا فایل JSON) نگهداری کند.
**Key terms:** صفحه تنظیمات, Provider, مدل, DeepSeek, GPT, Gemini, Claude, Grok, Perplexity, mapping, add

**بخش مربوط از متن کاربر:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد صفحه تنظیمات اصلی با مدیریت Providerها و مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-24` — `AIProvider`
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable=True)
      name = Column(String(120), nullable=False)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک صفحه تنظیمات جدید در سیستم LifeManager را دارد که در آن بتواند Providerهای مختلف AI مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و غیره را اضافه، ویرایش و حذف کند. برای هر Provider باید امکان mapping و add کردن مدل‌های مربوطه فراهم شود. این صفحه باید به‌عنوان یک ماژول مستقل عمل کند و داده‌های Providerها و مدل‌ها را در یک ساختار داده قابل ذخیره‌سازی (مانند دیتابیس یا فایل JSON) نگهداری کند. خارج از این مرحله: قابلیت آنالیز داده‌ها، جعبه پرامپت، و دسترسی به صفحات دیگر. کلیدواژه‌های کاربر: صفحه تنظیمات, Provider, مدل, DeepSeek, GPT, Gemini, Claude, Grok, Perplexity, mapping, add. در کد فعلی، مدل `AIProvider` در `app/models/ai_provider.py` (خطوط 14-24) وجود دارد که شامل فیلدهای `id`, `user_id`, `name`, `description`, `is_enabled`, `created_at`, `updated_at` است. همچنین مدل `GlobalAnalysisPrompt` در همان فایل (خطوط 26-42) برای جعبه پرامپت وجود دارد. روتر `ai.py` در `app/routes/ai.py` شامل endpointهای `/ai/configs` برای مدیریت مدل‌هاست. صفحه `AISettings.jsx` در `frontend/src/pages/AISettings.jsx` وجود دارد که باید گسترش یابد. فایل `app/services/ai/provider_service.py` سرویس تماس با OpenAI را دارد. فایل `app/models/ai_model_config.py` مدل `AIModelConfig` را دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک کامپوننت جدید `ProviderManager.jsx` در `frontend/src/components/` که فرم افزودن/ویرایش Provider را نمایش دهد. 2. گسترش صفحه `frontend/src/pages/AISettings.jsx` برای نمایش لیست Providerها و مدل‌های هرکدام. 3. افزودن endpointهای CRUD برای Providerها در `app/routes/ai.py` (GET /api/ai/providers, POST /api/ai/providers, PUT /api/ai/providers/{id}, DELETE /api/ai/providers/{id}). 4. افزودن endpointهای CRUD برای مدل‌های یک Provider در `app/routes/ai.py` (GET /api/ai/providers/{provider_id}/models, POST /api/ai/providers/{provider_id}/models, PUT /api/ai/providers/{provider_id}/models/{id}, DELETE /api/ai/providers/{provider_id}/models/{id}). 5. ایجاد سرویس `app/services/ai/provider_management_service.py` برای منطق تجاری مدیریت Providerها و مدل‌ها. 6. به‌روزرسانی مدل `AIProvider` در `app/models/ai_provider.py` برای افزودن فیلد `api_key` (اختیاری) و `base_url`. 7. ایجاد مدل جدید `ProviderModel` در `app/models/ai_model_config.py` یا فایل جدید برای نگاشت مدل‌های هر Provider. 8. افزودن دکمه‌های افزودن، ویرایش و حذف در UI برای هر Provider و مدل. 9. ذخیره‌سازی داده‌ها در دیتابیس با استفاده از SQLAlchemy. 10. اطمینان از مستقل بودن ماژول (عدم وابستگی به آنالیز داده‌ها یا جعبه پرامپت).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 2: افزودن جعبه پرامپت قابل ویرایش در صفحه تنظیمات

**Scope:** این مرحله شامل افزودن یک جعبه متن (text box) در صفحه تنظیمات است که کاربر بتواند یک پرامپت سفارشی برای آنالیز داده‌ها بنویسد. این جعبه باید همیشه قابل ویرایش باشد و کاربر بتواند هر زمان که بخواهد محتوای آن را تغییر دهد. پرامپت باید به‌صورت پویا ذخیره شود تا در زمان آنالیز توسط مدل‌ها استفاده شود. خارج از این مرحله: قابلیت آنالیز واقعی، اتصال به مدل‌ها، و دسترسی به داده‌های صفحات دیگر. نکته حیاتی: جعبه پرامپت باید در همان بخشی از صفحه تنظیمات قرار گیرد که Providerها و مدل‌ها مدیریت می‌شوند.
**Key terms:** جعبه پرامپت, پرامپت, ویرایش, صفحه تنظیمات, توضیحات

**بخش مربوط از متن کاربر:**
```
شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن جعبه پرامپت قابل ویرایش در صفحه تنظیمات AI

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/AISettings.jsx:1-50` — `AISettings` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، این صفحه اصلی تنظیمات AI است که Providerها و مدل‌ها را نمایش می‌دهد. جعبه پرامپت باید در اینجا اضافه شود.
  ```jsx
  import React, { useState, useEffect } from 'react';
  import { useAuth } from '../context/AuthContext';
  import api from '../lib/api';
  
  export default function AISettings() {
    const { user } = useAuth();
    const [providers, setProviders] = useState([]);
    const [configs, setConfigs] = useState([]);
    const [loading, setLoading] = useState(true);
  ```
- `app/routes/ai.py:1-30` — `router` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مسیرهای جدید GET /api/ai/prompt و PUT /api/ai/prompt باید
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  
  router = APIRouter(prefix='/ai', tags=['ai'])
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن یک جعبه متن (text box) در صفحه تنظیمات (AISettings.jsx) داده است که بتواند یک پرامپت سفارشی برای آنالیز داده‌ها بنویسد. این جعبه باید همیشه قابل ویرایش باشد و کاربر بتواند هر زمان که بخواهد محتوای آن را تغییر دهد. پرامپت باید به‌صورت پویا ذخیره شود تا در زمان آنالیز توسط مدل‌ها استفاده شود. نکته حیاتی: جعبه پرامپت باید در همان بخشی از صفحه تنظیمات قرار گیرد که Providerها و مدل‌ها مدیریت می‌شوند. کلیدواژه‌های کاربر: 'جعبه پرامپت', 'پرامپت', 'ویرایش', 'صفحه تنظیمات', 'توضیحات'. خارج از این مرحله: قابلیت آنالیز واقعی، اتصال به مدل‌ها، و دسترسی به داده‌های صفحات دیگر. در کد فعلی، صفحه AISettings.jsx در frontend/src/pages/AISettings.jsx وجود دارد و Providerها و مدل‌ها را از طریق APIهای /api/ai/configs و /api/ai/providers مدیریت می‌کند. backend نیز مدل AIModelConfig و AIProvider را در app/models/ai_model_config.py و app/models/ai_provider.py دارد. برای ذخیره پرامپت، نیاز به یک مدل جدید یا فیلد جدید در یکی از مدل‌های موجود است. مسیر API پیشنهادی: POST /api/ai/prompt برای ذخیره و GET /api/ai/prompt برای بازیابی پرامپت.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل جدید `AIPrompt` در `app/models/ai_model_config.py` با فیلدهای `id`, `user_id`, `content` (Text), `created_at`, `updated_at`. 2. ایجاد route جدید در `app/routes/ai.py`: `GET /api/ai/prompt` (بازیابی پرامپت کاربر) و `PUT /api/ai/prompt` (ذخیره/به‌روزرسانی پرامپت). 3. افزودن تابع سرویس در `app/services/ai_service.py` برای مدیریت پرامپت. 4. در frontend، افزودن state و textarea در `frontend/src/pages/AISettings.jsx` در بخش مدیریت Providerها و مدل‌ها. 5. ذخیره خودکار با debounce یا دکمه ذخیره مجزا.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: ایجاد قابلیت دسترسی مدل‌ها به صفحات و داده‌های داخل هر صفحه

**Scope:** این مرحله شامل پیاده‌سازی مکانیزمی است که به مدل‌های تعریف‌شده در صفحه تنظیمات اجازه دهد به تمام صفحات موجود در سیستم و داده‌های داخل آن‌ها دسترسی داشته باشند. این دسترسی باید به‌صورت实时 (real-time) باشد، به این معنی که هر صفحه یا داده جدیدی که اضافه می‌شود بلافاصله برای مدل‌ها قابل دسترسی باشد. خارج از این مرحله: اجرای واقعی آنالیز توسط مدل‌ها، و قابلیت ویرایش پرامپت. نکته حیاتی: این قابلیت باید از طریق یک API یا سرویس مرکزی پیاده‌سازی شود که تمام داده‌های صفحات را جمع‌آوری کرده و در اختیار مدل‌ها قرار دهد.
**Key terms:** دسترسی به صفحات, داده‌های داخل صفحه, real-time, آنالیز, API

**بخش مربوط از متن کاربر:**
```
اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. ... هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد سرویس مرکزی دسترسی real-time مدل‌ها به صفحات

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:1-50` — `router` — محل افزودن endpoint جدید GET /api/ai/page-data برای دسترسی مدل‌ها به داده‌های صفحات
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  
  router = APIRouter(prefix="/ai", tags=["ai"])
  ```
- `app/services/ai/model_service.py:1-30` — `ModelService` — افزودن متد get_all_page_data که از PageDataAggregatorService استفاده می‌کند
  ```python
  class ModelService:
      def __init__(self, db: AsyncSession):
          self.db = db
  
      async def get_model_configs(self, provider: str = None):
          ...
  ```
- `app/models/ai_model_config.py:1-30` — `AIModelConfig`
  ```python
  class AIModelConfig(Base):
      __tablename__ = "ai_model_configs"
      id = Column(Integer, primary_key=True, index=True)
      name = Column(String, nullable=False)
      provider = Column(String, nullable=False)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد قابلیتی داده که به مدل‌های تعریف‌شده در صفحه تنظیمات (AISettings) اجازه دهد به تمام صفحات موجود در سیستم و داده‌های داخل آن‌ها دسترسی داشته باشند. این دسترسی باید به‌صورت real-time باشد، به این معنی که هر صفحه یا داده جدیدی که اضافه می‌شود بلافاصله برای مدل‌ها قابل دسترسی باشد. خارج از این مرحله: اجرای واقعی آنالیز توسط مدل‌ها، و قابلیت ویرایش پرامپت. نکته حیاتی: این قابلیت باید از طریق یک API یا سرویس مرکزی پیاده‌سازی شود که تمام داده‌های صفحات را جمع‌آوری کرده و در اختیار مدل‌ها قرار دهد. کلیدواژه‌های کاربر: دسترسی به صفحات, داده‌های داخل صفحه, real-time, آنالیز, API. در کد فعلی، مدل‌های AI در app/models/ai_model_config.py تعریف شده‌اند و سرویس‌های AI در app/services/ai/ قرار دارند. صفحه تنظیمات AI در frontend/src/pages/AISettings.jsx است. هیچ سرویس مرکزی برای جمع‌آوری داده‌های تمام صفحات وجود ندارد. مسیرهای API صفحات در app/routes/ تعریف شده‌اند (tasks, projects, notifications, users, finance, person, location, assets, lists, todo_items, self_improvement, planner, ai, integrations, drive, external_projects, merge, context, local_files, webhook).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام PageDataAggregatorService در app/services/page_data_aggregator_service.py که وظیفه جمع‌آوری داده‌های تمام صفحات را از طریق APIهای داخلی بر عهده دارد. 2. ایجاد یک endpoint جدید در app/routes/ai.py با مسیر GET /api/ai/page-data که داده‌های aggregated را برمی‌گرداند. 3. پیاده‌سازی مکانیزم کش با زمان انقضای کوتاه (مثلاً 30 ثانیه) برای real-time بودن داده‌ها. 4. افزودن متد get_all_page_data به AIModelConfig یا سرویس model_service که از PageDataAggregatorService استفاده کند. 5. به‌روزرسانی frontend/src/pages/AISettings.jsx برای نمایش وضعیت دسترسی مدل‌ها به صفحات. 6. ایجاد یک WebSocket endpoint اختیاری برای push تغییرات real-time به مدل‌ها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 4: پیاده‌سازی قابلیت آنالیز داده‌ها توسط مدل‌ها بر اساس پرامپت

**Scope:** این مرحله شامل پیاده‌سازی هسته اصلی آنالیز است که در آن مدل‌های تعریف‌شده (با توجه به پرامپت کاربر) داده‌های صفحات را آنالیز می‌کنند. این فرآیند باید از پرامپت ذخیره‌شده در مرحله ۲ استفاده کند و داده‌های جمع‌آوری‌شده از مرحله ۳ را به مدل ارسال کند. خروجی آنالیز باید به کاربر نمایش داده شود. خارج از این مرحله: مدیریت Providerها، ویرایش پرامپت، و دسترسی به داده‌ها. نکته حیاتی: این مرحله باید از یک معماری async استفاده کند تا آنالیزهای طولانی باعث مسدود شدن UI نشوند.
**Key terms:** آنالیز, پرامپت, مدل, داده‌های صفحات, async

**بخش مربوط از متن کاربر:**
```
و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی هسته آنالیز داده‌ها توسط مدل‌ها با معماری async

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت آنالیز داده‌ها توسط مدل‌ها بر اساس پرامپت را دارد. این مرحله شامل پیاده‌سازی هسته اصلی آنالیز است که در آن مدل‌های تعریف‌شده (با توجه به پرامپت کاربر) داده‌های صفحات را آنالیز می‌کنند. این فرآیند باید از پرامپت ذخیره‌شده در مرحله ۲ استفاده کند و داده‌های جمع‌آوری‌شده از مرحله ۳ را به مدل ارسال کند. خروجی آنالیز باید به کاربر نمایش داده شود. خارج از این مرحله: مدیریت Providerها، ویرایش پرامپت، و دسترسی به داده‌ها. نکته حیاتی: این مرحله باید از یک معماری async استفاده کند تا آنالیزهای طولانی باعث مسدود شدن UI نشوند. کاربر تأکید کرده: «و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن». کلیدواژه‌ها: آنالیز, پرامپت, مدل, داده‌های صفحات, async. در کد فعلی پروژه، فایل `app/routes/ai.py` شامل endpointهای AI مانند `POST /ai/generate` و `POST /ai/query` است که از `app/services/ai/model_service.py` و `app/services/ai/provider_service.py` استفاده می‌کنند. فایل `app/services/ai/ai_data_access_service.py` دسترسی به داده‌های صفحات (tasks, projects, todo_items) را فراهم می‌کند. فایل `app/models/ai_model_config.py` مدل AIModelConfig را تعریف کرده که شامل فیلدهای `provider`, `model_name`, `prompt_template` است. فایل `app/models/ai_provider.py` شامل AIProvider و GlobalAnalysisPrompt است. فایل `app/services/ai/nlp_service.py` سرویس NLP را ارائه می‌دهد. فایل `app/services/ai/recommendation_service.py` سرویس recommendation را دارد. فایل `app/services/ai/image_service.py` سرویس image را دارد. فایل `app/schemas/ai_schema.py` شامل schemaهای مربوط به AI است. فایل `app/schemas/ai_provider_schema.py` شامل schemaهای provider است. فایل `frontend/src/pages/SmartAssistant.jsx` صفحه Smart Assistant را نمایش می‌دهد که می‌تواند محل نمایش نتایج آنالیز باشد. فایل `frontend/src/lib/api.js` شامل توابع API برای ارتباط با backend است. فایل `frontend/src/context/AuthContext.jsx` context احراز هویت را مدیریت می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در `app/routes/ai.py` با مسیر `POST /ai/analyze` که یک `AnalysisRequest` شامل `prompt_id` (ارجاع به پرامپت ذخیره‌شده در مرحله ۲) و `data_source_ids` (لیست شناسه‌های داده‌های صفحات از مرحله ۳) دریافت کند. 2. ایجاد یک سرویس async جدید در `app/services/ai/analysis_service.py` که: (الف) پرامپت را از `AIModelConfig` یا `GlobalAnalysisPrompt` بارگذاری کند، (ب) داده‌های صفحات را از طریق `ai_data_access_service` جمع‌آوری کند، (ج) درخواست را به مدل (از طریق `model_service` یا `provider_service`) ارسال کند، (د) نتیجه را در دیتابیس ذخیره کند و یک `analysis_id` برگرداند. 3. ایجاد یک endpoint `GET /ai/analysis/{analysis_id}/status` برای بررسی وضعیت آنالیز (pending/running/done/error). 4. ایجاد یک endpoint `GET /ai/analysis/{analysis_id}/result` برای دریافت نتیجه نهایی. 5. در فرانت‌اند، یک کامپوننت جدید در `frontend/src/pages/SmartAssistant.jsx` یا یک صفحه جدید `AnalysisPage.jsx` ایجاد شود که فرم ارسال آنالیز و نمایش نتایج را مدیریت کند. 6

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: ذخیره‌سازی و بازیابی تنظیمات (Providerها، مدل‌ها، پرامپت) در دیتابیس یا فایل

**Scope:** این مرحله شامل پیاده‌سازی لایه ذخیره‌سازی برای تمام تنظیمات مربوط به Providerها، مدل‌ها و پرامپت است. این داده‌ها باید در یک دیتابیس (مانند SQLite، PostgreSQL) یا فایل JSON ذخیره شوند تا پس از رفرش صفحه یا بستن مرورگر از بین نروند. همچنین باید قابلیت بازیابی این تنظیمات در زمان لود صفحه وجود داشته باشد. خارج از این مرحله: UI و منطق آنالیز. نکته حیاتی: ساختار داده باید به‌گونه‌ای باشد که به راحتی قابل گسترش باشد (مثلاً افزودن Provider جدید).
**Key terms:** ذخیره‌سازی, دیتابیس, JSON, تنظیمات, Provider, مدل, پرامپت

**بخش مربوط از متن کاربر:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم ... و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ذخیره‌سازی و بازیابی تنظیمات AI در دیتابیس

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی لایه ذخیره‌سازی برای تمام تنظیمات مربوط به Providerها، مدل‌ها و پرامپت را دارد. این داده‌ها باید در دیتابیس (PostgreSQL از طریق SQLAlchemy) ذخیره شوند تا پس از رفرش صفحه یا بستن مرورگر از بین نروند. همچنین باید قابلیت بازیابی این تنظیمات در زمان لود صفحه وجود داشته باشد. خارج از این مرحله: UI و منطق آنالیز. نکته حیاتی: ساختار داده باید به‌گونه‌ای باشد که به راحتی قابل گسترش باشد (مثلاً افزودن Provider جدید).

بخش مربوط از درخواست اصلی کاربر:
"یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم ... و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم"

کلیدواژه‌ها: ذخیره‌سازی, دیتابیس, JSON, تنظیمات, Provider, مدل, پرامپت

شواهد در کد واقعی:
- فایل `app/models/ai_provider.py` شامل مدل‌های `AIProvider` (خط ۱۴-۲۴) و `GlobalAnalysisPrompt` (خط ۲۶-۴۲) است که ساختار اولیه برای ذخیره‌سازی Providerها و پرامپت را فراهم می‌کند.
- فایل `app/services/ai/provider_service.py` شامل تابع `call_openai_chat` (خط ۳۶-۷۵) است که از `OPENAI_API_KEY` استفاده می‌کند اما این کلید در حافظه (متغیر محیطی) ذخیره می‌شود نه دیتابیس.
- فایل `app/routes/ai.py` (در deep context موجود نیست اما در ساختار پروژه وجود دارد) احتمالاً شامل endpointهای مربوط به AI است.
- فایل `app/main.py` (خط ۶۵۶-۶۵۹) نشان می‌دهد که `auth_google.router` به صورت شرطی mount می‌شود — الگویی مشابه برای mount شرطی تنظیمات AI.
- فایل `frontend/src/pages/AISettings.jsx` (در ساختار پروژه موجود است) صفحه تنظیمات AI در فرانت‌اند است که باید داده‌ها را از API دریافت کند.
- فایل `.env.example` شامل متغیرهای `OPENAI_API_KEY` و `GOOGLE_CLIENT_ID` است که نشان می‌دهد تنظیمات فعلاً در environment variables ذخیره می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. توسعه مدل `AIProvider` در `app/models/ai_provider.py`: اضافه کردن فیلد `api_key_encrypted` (با استفاده از `crypt_service` موجود در `app/services/crypt_service.py`) برای ذخیره امن API Keyها. همچنین اضافه کردن فیلد `base_url` برای Providerهای سفارشی.

۲. ایجاد مدل جدید `AIModelMapping` در `app/models/ai_model_config.py` (یا فایل جدید): این مدل شامل فیلدهای `provider_id` (FK به AIProvider)، `model_name` (نام مدل مانند gpt-4)، `display_name` (نام نمایشی)، `max_tokens` (پیش‌فرض)، `temperature` (پیش‌فرض) و `is_active` (boolean) باشد.

۳. توسعه سرویس `app/services/ai/provider_service.py`: اضافه کردن توابع CRUD برای Providerها و ModelMappingها. تابع `get_providers_for_user(user_id)` برای بازیابی لیست Providerهای یک کاربر. تابع `save_provider_settings(user_id, provider_data)` برای ذخیره تنظیمات.

۴. ایجاد endpointهای جدید در `app/routes/ai.py`: 
   - `GET /api/ai/providers` — لیست Providerهای کاربر
   - `POST /api/ai/providers` — ایجاد Provider جدید
   - `PUT /api/ai/providers/{provider_id}` — بروزرسانی Provider
   - `

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 6: ایجاد API برای ارتباط بین فرانت‌اند و بک‌اند (مدیریت تنظیمات و آنالیز)

**Scope:** این مرحله شامل ایجاد APIهای لازم برای ارتباط بین فرانت‌اند (صفحه تنظیمات) و بک‌اند است. این APIها باید شامل endpoints برای CRUD عملیات روی Providerها و مدل‌ها، ذخیره و بازیابی پرامپت، و trigger آنالیز باشند. خارج از این مرحله: UI و منطق آنالیز. نکته حیاتی: APIها باید از استاندارد REST پیروی کنند و خطاهای مناسب را برگردانند.
**Key terms:** API, REST, CRUD, Provider, مدل, پرامپت, آنالیز, endpoint

**بخش مربوط از متن کاربر:**
```
یه صفحه تنظیمات باید توی سیستم باشه ... و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد APIهای REST برای مدیریت Providerها، مدل‌ها، پرامپت و آنالیز

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-24` — `class AIProvider` — مدل AIProvider برای CRUD Providerها استفاده می‌شود. نیاز به endpoints جدید برای ایجاد، خواندن، به‌روزرسانی و حذف این موجودیت‌ها.
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable=True)
      name = Column(String(120), nullable=False)
      description = Column(Text, nullable=True)
      is_enabled = Column(Boolean, default=True, nullable=False)
      created_at = Column(DateTime(timezone=True), server_default=func.now())
      updated_at = Column(DateTime(timezone=True), onupdate=func.now())
  ```
- `app/models/ai_provider.py:26-42` — `class GlobalAnalysisPrompt`
  ```python
  class GlobalAnalysisPrompt(Base):
      __tablename__ = "global_analysis_prompts"
  
      id = Column(Integer, primary_key=True, index=True)
      prompt_text = Column(Text, nullable=False, default="")
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد API برای ارتباط بین فرانت‌اند (صفحه تنظیمات) و بک‌اند داده است. این APIها باید شامل endpoints برای CRUD عملیات روی Providerها و مدل‌ها، ذخیره و بازیابی پرامپت، و trigger آنالیز باشند. خارج از این مرحله: UI و منطق آنالیز. نکته حیاتی: APIها باید از استاندارد REST پیروی کنند و خطاهای مناسب را برگردانند. بخش مربوط از درخواست اصلی کاربر: «یه صفحه تنظیمات باید توی سیستم باشه ... و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو انجام بدن». کلیدواژه‌ها: API, REST, CRUD, Provider, مدل, پرامپت, آنالیز, endpoint. در کد فعلی، مدل AIProvider در app/models/ai_provider.py (خطوط 14-24) تعریف شده است با فیلدهای id, user_id, name, description, is_enabled, created_at, updated_at. همچنین مدل GlobalAnalysisPrompt در همان فایل (خطوط 26-42) برای ذخیره پرامپت سراسری کاربر وجود دارد. در app/routes/ai.py احتمالاً endpoints فعلی برای AI وجود دارد اما نیاز به توسعه برای CRUD کامل Providerها و مدل‌ها و trigger آنالیز است. فایل app/services/ai/provider_service.py (خطوط 36-75) تابع call_openai_chat را دارد که برای trigger آنالیز قابل استفاده است. همچنین app/config.py و app/core/config.py تنظیمات مربوط به AI را دارند. نیاز به ایجاد endpoints جدید در app/routes/ai.py یا یک فایل route جدید برای مدیریت Providerها و مدل‌ها و پرامپت و آنالیز است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل route جدید به نام app/routes/ai_settings.py یا افزودن endpoints به app/routes/ai.py موجود برای CRUD روی AIProvider (مدل در app/models/ai_provider.py). 2. ایجاد endpoints برای CRUD روی AIModelConfig (مدل در app/models/ai_model_config.py). 3. ایجاد endpoints GET/PUT برای GlobalAnalysisPrompt (مدل در app/models/ai_provider.py خطوط 26-42). 4. ایجاد endpoint POST برای trigger آنالیز که از app/services/ai/provider_service.py::call_openai_chat استفاده کند. 5. ثبت router جدید در app/main.py (خطوط 601-649) با دستور app.include_router. 6. اطمینان از پیروی از استاندارد REST و برگرداندن خطاهای مناسب (HTTP 400, 404, 500). 7. اضافه کردن prefix /api/ai/settings برای endpoints جدید.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 7: پیاده‌سازی قابلیت افزودن Providerهای جدید توسط کاربر (نه فقط predefined)

**Scope:** این مرحله شامل افزودن قابلیتی است که کاربر بتواند Providerهای جدید (غیر از لیست predefined) را به سیستم اضافه کند. این شامل فیلدهایی برای نام Provider، base URL API، و کلید API (API key) است. خارج از این مرحله: مدیریت مدل‌های هر Provider. نکته حیاتی: این قابلیت باید امن باشد و کلیدهای API به‌صورت امن ذخیره شوند.
**Key terms:** Provider جدید, API key, base URL, نام Provider, امنیت

**بخش مربوط از متن کاربر:**
```
مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت ثبت Provider سفارشی توسط کاربر با ذخیره امن API Key

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-24` — `class AIProvider` — مدل فعلی فاقد فیلدهای base_url و api_key_encrypted است. باید این دو فیلد اضافه شوند.
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable=True)
      name = Column(String(120), nullable=False)
      description = Column(Text, nullable=True)
      is_enabled = Column(Boolean, default=True, nullable=False)
      created_at = Column(DateTime(timezone=True), server_default=func.now())
      updated_at = Column(DateTime(timezone=True), onupdate=func.now())
  ```
- `app/services/ai/provider_service.py:36-75` — `async def call_openai_chat`
  ```python
  async def call_openai_chat(
      *,
      prompt: str,
      model: str,
      max_tokens: int,
      temperature: float,
  ) -> dict:
      "
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیتی را دارد که بتواند Providerهای جدید (غیر از لیست predefined) را به سیستم اضافه کند. این شامل فیلدهایی برای نام Provider، base URL API، و کلید API (API key) است. کاربر اشاره کرده که Providerهایی مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی و چند تای دیگه باید قابل افزودن باشند. نکته حیاتی: این قابلیت باید امن باشد و کلیدهای API به‌صورت امن ذخیره شوند. خارج از این مرحله: مدیریت مدل‌های هر Provider.

شواهد در کد: مدل `AIProvider` در `app/models/ai_provider.py` (خطوط 14-24) در حال حاضر فقط فیلدهای `id`, `user_id`, `name`, `description`, `is_enabled`, `created_at`, `updated_at` را دارد و فاقد فیلدهای `base_url` و `api_key_encrypted` است. سرویس `provider_service.py` در `app/services/ai/provider_service.py` (خطوط 36-75) فقط از `OPENAI_API_KEY` محیطی استفاده می‌کند و از Providerهای داینامیک پشتیبانی نمی‌کند. روتر `ai.py` در `app/routes/ai.py` (که در deep context موجود است) endpointهای فعلی را مدیریت می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلدهای `base_url` (String, nullable=False) و `api_key_encrypted` (Text, nullable=False) به مدل `AIProvider` در `app/models/ai_provider.py`. 2. ایجاد یک سرویس رمزنگاری جدید `app/services/encryption_service.py` یا استفاده از `app/services/crypt_service.py` موجود برای رمزنگاری/رمزگشایی API keyها. 3. افزودن endpointهای CRUD جدید در `app/routes/ai.py` برای مدیریت Providerهای سفارشی: `POST /api/ai/providers` (ایجاد), `GET /api/ai/providers` (لیست), `PUT /api/ai/providers/{provider_id}` (ویرایش), `DELETE /api/ai/providers/{provider_id}` (حذف). 4. به‌روزرسانی `provider_service.py` برای خواندن پیکربندی Provider از دیتابیس و استفاده از base_url و api_key داینامیک به جای متغیر محیطی ثابت. 5. افزودن صفحه Settings در فرانت‌اند (`frontend/src/pages/AISettings.jsx`) برای نمایش و مدیریت Providerها. 6. افزودن اعتبارسنجی سمت سرور برای جلوگیری از ذخیره کلیدهای خالی یا نامعتبر.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: پیاده‌سازی قابلیت mapping مدل‌ها برای هر Provider

**Scope:** این مرحله شامل افزودن قابلیتی است که کاربر بتواند برای هر Provider، مدل‌های مختلف را mapping کند. این شامل انتخاب یک Provider، مشاهده لیست مدل‌های موجود (در صورت امکان از طریق API Provider)، و افزودن مدل‌های جدید با نام و شناسه (model ID) است. خارج از این مرحله: افزودن Provider جدید. نکته حیاتی: mapping باید به‌صورت یک رابطه many-to-many بین Provider و مدل‌ها پیاده‌سازی شود.
**Key terms:** mapping, مدل, Provider, model ID, many-to-many

**بخش مربوط از متن کاربر:**
```
اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن mapping many-to-many بین Provider و مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-24` — `AIProvider` — این مدل باید رابطه many-to-many با AIModelConfig داشته باشد. نیاز به افزودن association table و relationship field.
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable=True)
      name = Column(String(120), nullable=False)
      description = Column(Text, nullable=True)
      is_enabled = Column(Boolean, default=True, nullable=False)
      created_at = Column(DateTime(timezone=True), server_default=func.now())
      updated_at = Column(DateTime(timezone=True), onupdate=func.now())
  ```
- `app/services/ai/provider_service.py:36-75` — `call_openai_chat`
  ```python
  async def call_openai_chat(
      *,
      prompt: str,
      model: str,
      max_tokens: int,
      temperature: float,
  ) -> dict:
      """POST ``prompt`` to the OpenAI chat completions endpoint."""
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت mapping مدل‌ها برای هر Provider را داده است. این قابلیت باید به کاربر اجازه دهد برای هر Provider (که در حال حاضر در سیستم وجود دارد)، مدل‌های مختلف را mapping کند. مراحل شامل: انتخاب یک Provider، مشاهده لیست مدل‌های موجود (در صورت امکان از طریق API Provider)، و افزودن مدل‌های جدید با نام و شناسه (model ID) است. خارج از این مرحله: افزودن Provider جدید. نکته حیاتی: mapping باید به‌صورت یک رابطه many-to-many بین Provider و مدل‌ها پیاده‌سازی شود. کلیدواژه‌های کاربر: mapping, مدل, Provider, model ID, many-to-many. در کد فعلی، مدل `AIProvider` در `app/models/ai_provider.py` (خط ۱۴-۲۴) فقط فیلدهای `id`, `user_id`, `name`, `description`, `is_enabled`, `created_at`, `updated_at` را دارد و هیچ رابطه‌ای با مدل‌ها ندارد. مدل `AIModelConfig` در `app/models/ai_model_config.py` وجود دارد که احتمالاً باید با `AIProvider` ارتباط many-to-many داشته باشد. همچنین در `app/services/ai/provider_service.py` تابع `call_openai_chat` (خط ۳۶-۷۵) مستقیماً از `OPENAI_API_KEY` استفاده می‌کند و از مدل‌های ذخیره‌شده استفاده نمی‌کند. در `app/routes/ai.py` احتمالاً endpoint‌هایی برای مدیریت مدل‌ها وجود دارد که باید توسعه یابند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد جدول میانی (association table) برای رابطه many-to-many بین `AIProvider` و `AIModelConfig` در `app/models/ai_provider.py` یا فایل جدید. ۲. افزودن فیلد `model_id` (شناسه مدل در Provider) به `AIModelConfig` در `app/models/ai_model_config.py`. ۳. ایجاد endpoint‌های جدید در `app/routes/ai.py`: `GET /api/ai/providers/{provider_id}/models` برای لیست مدل‌های یک Provider، `POST /api/ai/providers/{provider_id}/models` برای افزودن مدل جدید به Provider، `DELETE /api/ai/providers/{provider_id}/models/{model_id}` برای حذف mapping. ۴. به‌روزرسانی `app/services/ai/provider_service.py` برای استفاده از مدل‌های mapping شده به جای `model` string ثابت. ۵. ایجاد کامپوننت frontend در `frontend/src/pages/AISettings.jsx` برای نمایش و مدیریت mapping مدل‌ها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 9: ایجاد قابلیت انتخاب مدل برای آنالیز (کدام مدل از کدام Provider استفاده شود)

**Scope:** این مرحله شامل افزودن قابلیتی است که کاربر بتواند مشخص کند کدام مدل از کدام Provider برای آنالیز استفاده شود. این می‌تواند به‌صورت یک dropdown یا selector در صفحه تنظیمات یا در صفحه آنالیز باشد. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: کاربر باید بتواند چندین مدل را برای آنالیز همزمان انتخاب کند.
**Key terms:** انتخاب مدل, Provider, آنالیز, selector, dropdown

**بخش مربوط از متن کاربر:**
```
اونها کار آنالیز رو به شکلی که میگم انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت انتخاب چندگانه مدل و Provider برای آنالیز همزمان

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد قابلیتی را دارد که بتواند مشخص کند کدام مدل از کدام Provider برای آنالیز استفاده شود. این قابلیت باید به‌صورت یک dropdown یا selector در صفحه تنظیمات یا در صفحه آنالیز پیاده‌سازی شود. نکته حیاتی: کاربر باید بتواند چندین مدل را برای آنالیز همزمان انتخاب کند. خارج از این مرحله: اجرای آنالیز. کلیدواژه‌های کاربر: انتخاب مدل, Provider, آنالیز, selector, dropdown. در کد فعلی پروژه، مدل‌های AI در فایل `app/models/ai_model_config.py` (کلاس `AIModelConfig`) و `app/models/ai_provider.py` (کلاس `AIProvider`) تعریف شده‌اند. سرویس `app/services/ai/provider_service.py` مسئول فراخوانی HTTP به OpenAI است و تابع `call_openai_chat` در خطوط 36-75 آن، یک `prompt` را به `https://api.openai.com/v1/chat/completions` ارسال می‌کند. روتر `app/routes/ai.py` شامل endpointهای `/ai/generate` و `/ai/query` است. صفحه تنظیمات AI در `frontend/src/pages/AISettings.jsx` قرار دارد. کاربر می‌گوید: "اونها کار آنالیز رو به شکلی که میگم انجام بدن" که نشان‌دهنده نیاز به انتخاب همزمان چند مدل است. در حال حاضر، هیچ مکانیزمی برای انتخاب چندین مدل به‌صورت همزمان وجود ندارد و `call_openai_chat` تنها یک مدل را در هر فراخوانی پشتیبانی می‌کند. همچنین، `provider_service.py` فقط از `OPENAI_API_KEY` استفاده می‌کند و از providerهای دیگر (DeepSeek, Gemini, Claude, Grok, Perplexity) پشتیبانی نمی‌کند. فایل `app/config.py` شامل تنظیمات `FEATURE_AI_ENABLED` است که باید برای فعال‌سازی این قابلیت در نظر گرفته شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل `app/services/ai/provider_service.py`، تابع `call_openai_chat` را به‌گونه‌ای بازنویسی کن که از providerهای مختلف (بر اساس پارامتر `provider_name`) پشتیبانی کند. برای هر provider، یک تابع مجزا (مثلاً `call_deepseek_chat`, `call_gemini_chat`) ایجاد کن و آن‌ها را در یک دیکشنری `PROVIDER_HANDLERS` ثبت کن. 2. یک تابع جدید `call_multiple_models` در `app/services/ai/provider_service.py` ایجاد کن که لیستی از `(provider_name, model_name)` را دریافت کرده و به‌صورت همزمان (با `asyncio.gather`) آن‌ها را فراخوانی کند. 3. در `app/routes/ai.py`، یک endpoint جدید مانند `POST /api/ai/analyze-with-models` اضافه کن که بدنه‌اش شامل `prompt` و `models: List[Dict[str, str]]` (هر dict شامل `provider` و `model_name`) باشد. این endpoint باید `call_multiple_models` را صدا بزند و نتایج را به‌صورت لیست برگرداند. 4. در `frontend/src/pages/AISettings.jsx`، یک کامپوننت `ModelSelector` اضافه کن که لیست providerها و مدل‌های آن‌ها را از `GET /api/ai/configs` (که در `app/routes/ai.py` وجود دارد) دریافت کرده و به کاربر اجازه دهد چندین مدل را با چک‌باکس انتخاب کند. 5. در `frontend/src/pages/SmartAssistant.jsx` یا صفحه آنالیز، این selector را نمایش بده تا کاربر قبل از ارسال درخواست آنالیز، مدل‌های مورد نظر را انتخاب کند. 6. فیلتر `?provider=` در `GET /api/ai/configs` (که در `docs/API.md` مستند شده) باید در فرانت‌اند برای بارگذاری مدل‌های یک provider خاص استفاده شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 10: پیاده‌سازی نمایش نتایج آنالیز در یک بخش مجزا

**Scope:** این مرحله شامل ایجاد یک بخش UI برای نمایش نتایج آنالیز است. این بخش باید بتواند خروجی مدل‌ها (متن، JSON، یا هر فرمت دیگر) را به‌صورت readable نمایش دهد. همچنین باید قابلیت مقایسه نتایج چند مدل (در صورت انتخاب چند مدل) وجود داشته باشد. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: نتایج باید به‌صورت real-time (در صورت استفاده از streaming) یا پس از اتمام آنالیز نمایش داده شوند.
**Key terms:** نتایج آنالیز, UI, نمایش, مقایسه, real-time

**بخش مربوط از متن کاربر:**
```
اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد بخش UI مجزا برای نمایش نتایج آنالیز مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/SmartAssistant.jsx:1-50` — `SmartAssistant` — این صفحه نقطه شروع مناسبی برای افزودن بخش نتایج آنالیز است. مجری باید محتوای کامل فایل را بررسی کند.
  ```jsx
  // فایل SmartAssistant.jsx موجود است اما محتوای آن در deep context موجود نیست
  ```
- `frontend/src/lib/api.js:1-50` — `api functions` — برای ارتباط با endpointهای آنالیز از این ماژول استفاده می‌شود. مجری باید توابع موجود را بررسی کند.
  ```jsx
  // فایل api.js موجود است اما محتوای آن در deep context موجود نیست
  ```
- `app/routes/ai.py:1-50` — `router` — این فایل شامل endpointهای مربوط به AI است. ممکن است نیاز به افزودن endpoint جدید برای دریافت نتایج آنالیز باشد.
  ```python
  // فایل ai.py موجود است اما محتوای آن در deep context موجود نیست
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/App.jsx` (سطر 1) — برای افزودن مسیر جدید به React Router اگر صفحه جدیدی ایجاد شود
- `frontend/src/context/AuthContext.jsx` (سطر 1) — برای احراز هویت درخواست‌های API مربوط به آنالیز
- `app/services/ai_service.py` (سطر 1) — برای پیاده‌سازی منطق سمت سرور آنالیز و بازگرداندن نتایج

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به کامپوننت‌های فرانت‌اند (SmartAssistant.jsx, AnalysisResults.jsx)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک بخش UI مجزا برای نمایش نتایج آنالیز را داده است. این بخش باید خروجی مدل‌ها (متن، JSON، یا هر فرمت دیگر) را به‌صورت readable نمایش دهد. همچنین باید قابلیت مقایسه نتایج چند مدل (در صورت انتخاب چند مدل) وجود داشته باشد. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: نتایج باید به‌صورت real-time (در صورت استفاده از streaming) یا پس از اتمام آنالیز نمایش داده شوند. کاربر اشاره کرده که "اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن". کلیدواژه‌ها: نتایج آنالیز, UI, نمایش, مقایسه, real-time. در کد فعلی، صفحه SmartAssistant.jsx در frontend/src/pages/SmartAssistant.jsx وجود دارد که می‌تواند نقطه شروع مناسبی باشد. همچنین مسیر /ai در backend/app/routes/ai.py برای سرویس‌های AI وجود دارد. فایل frontend/src/lib/api.js برای ارتباط با API استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک کامپوننت جدید به نام AnalysisResults.jsx در frontend/src/components/ که وظیفه نمایش نتایج آنالیز را دارد. 2. این کامپوننت باید از props برای دریافت نتایج (text, json, یا هر فرمت) استفاده کند. 3. پیاده‌سازی قابلیت مقایسه نتایج چند مدل با استفاده از state داخلی برای نگهداری لیست نتایج. 4. استفاده از WebSocket یا polling برای نمایش real-time نتایج (در صورت streaming). 5. افزودن این کامپوننت به صفحه SmartAssistant.jsx یا ایجاد یک صفحه جدید AnalysisPage.jsx. 6. اطمینان از اینکه کامپوننت می‌تواند خروجی JSON را به صورت فرمت‌شده (مثلاً با تورفتگی) نمایش دهد. 7. استفاده از tailwindcss برای استایل‌دهی.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 11: افزودن قابلیت ذخیره و بارگذاری تنظیمات به‌صورت export/import

**Scope:** این مرحله شامل افزودن قابلیت export تنظیمات (Providerها، مدل‌ها، پرامپت) به یک فایل JSON و import آن فایل برای بازگردانی تنظیمات است. این قابلیت به کاربر اجازه می‌دهد تنظیمات خود را بین محیط‌های مختلف جابجا کند. خارج از این مرحله: UI اصلی تنظیمات. نکته حیاتی: فایل export باید شامل تمام تنظیمات باشد و در زمان import، تنظیمات موجود را بازنویسی کند (با هشدار).
**Key terms:** export, import, JSON, تنظیمات, backup

**بخش مربوط از متن کاربر:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت export/import تنظیمات AI Providerها و مدل‌ها به JSON

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت ذخیره و بارگذاری تنظیمات به‌صورت export/import را دارد. این قابلیت شامل export تنظیمات (Providerها، مدل‌ها، پرامپت) به یک فایل JSON و import آن فایل برای بازگردانی تنظیمات است. کاربر می‌گوید: «افزودن قابلیت ذخیره و بارگذاری تنظیمات به‌صورت export/import. این مرحله شامل افزودن قابلیت export تنظیمات (Providerها، مدل‌ها، پرامپت) به یک فایل JSON و import آن فایل برای بازگردانی تنظیمات است. این قابلیت به کاربر اجازه می‌دهد تنظیمات خود را بین محیط‌های مختلف جابجا کند. خارج از این مرحله: UI اصلی تنظیمات. نکته حیاتی: فایل export باید شامل تمام تنظیمات باشد و در زمان import، تنظیمات موجود را بازنویسی کند (با هشدار).» همچنین کاربر اشاره کرده: «یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم». کلیدواژه‌ها: export, import, JSON, تنظیمات, backup. در کد فعلی پروژه، مدل‌های AI Provider و AI Model Config در فایل‌های `app/models/ai_provider.py` و `app/models/ai_model_config.py` تعریف شده‌اند. روترهای مربوطه در `app/routes/ai.py` قرار دارند که شامل endpointهای CRUD برای configs هستند (GET /api/ai/configs, POST /api/ai/configs, PATCH /api/ai/configs/{config_id}, DELETE /api/ai/configs/{config_id}). همچنین endpoint POST /api/ai/query برای query کردن یک مدل پیکربندی‌شده وجود دارد. فایل `app/services/ai/provider_service.py` و `app/services/ai/model_service.py` سرویس‌های مربوط به providerها و مدل‌ها را مدیریت می‌کنند. صفحه تنظیمات فرانت‌اند در `frontend/src/pages/AISettings.jsx` قرار دارد. برای پیاده‌سازی این قابلیت، نیاز به افزودن دو endpoint جدید در بک‌اند (یکی برای export و یکی برای import) و همچنین دکمه‌ها/مودال‌های مربوطه در فرانت‌اند است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن endpoint GET /api/ai/configs/export در فایل `app/routes/ai.py` که تمام تنظیمات (providerها، مدل‌ها، پرامپت‌ها) را از دیتابیس خوانده و به‌صورت یک JSON برگرداند. این endpoint باید از سرویس‌های `provider_service` و `model_service` استفاده کند. 2. افزودن endpoint POST /api/ai/configs/import در فایل `app/routes/ai.py` که یک JSON دریافت کرده و تنظیمات موجود را بازنویسی کند (با هشدار). این endpoint باید ابتدا تمام رکوردهای موجود را پاک کرده و سپس رکوردهای جدید را از JSON دریافتی ایجاد کند. 3. در فرانت‌اند، در فایل `frontend/src/pages/AISettings.jsx`، دو دکمه «export» و «import» اضافه شود. دکمه export یک درخواست GET به endpoint جدید می‌فرستد و فایل JSON را دانلود می‌کند. دکمه import یک مودال برای انتخاب فایل JSON باز می‌کند و پس از تأیید کاربر (با هشدار بازنویسی)، درخواست POST می‌فرستد. 4. در فایل `frontend/src/lib/api.js`، دو تابع جدید `exportConfigs` و `importConfigs` اضافه شود. 5. اسکیمای Pydantic برای درخواست import در فایل `app/schemas/ai_schema.py` تعریف شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 12: پیاده‌سازی قابلیت تست اتصال به Provider (Test Connection)

**Scope:** این مرحله شامل افزودن دکمه 'Test Connection' برای هر Provider است که اتصال به API Provider را با استفاده از base URL و API key ذخیره‌شده تست می‌کند. نتیجه تست (موفقیت یا خطا) باید به کاربر نمایش داده شود. خارج از این مرحله: مدیریت مدل‌ها. نکته حیاتی: این قابلیت باید از یک endpoint ساده Provider (مانند لیست مدل‌ها) استفاده کند تا اتصال را تأیید کند.
**Key terms:** Test Connection, API, base URL, API key, اتصال

**بخش مربوط از متن کاربر:**
```
مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن دکمه Test Connection برای AI Provider با endpoint لیست مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-23` — `AIProvider`
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable=True)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت تست اتصال به Provider (Test Connection) را داده است. این مرحله شامل افزودن دکمه 'Test Connection' برای هر Provider است که اتصال به API Provider را با استفاده از base URL و API key ذخیره‌شده تست می‌کند. نتیجه تست (موفقیت یا خطا) باید به کاربر نمایش داده شود. خارج از این مرحله: مدیریت مدل‌ها. نکته حیاتی: این قابلیت باید از یک endpoint ساده Provider (مانند لیست مدل‌ها) استفاده کند تا اتصال را تأیید کند. کاربر اشاره کرده: 'مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی'. کلیدواژه‌ها: Test Connection, API, base URL, API key, اتصال.

شواهد در کد واقعی پروژه: در فایل `app/services/ai/provider_service.py` (خطوط 36-75) تابع `call_openai_chat` وجود دارد که یک HTTP POST به `https://api.openai.com/v1/chat/completions` با هدر `Authorization: Bearer {api_key}` ارسال می‌کند. این تابع از `httpx.AsyncClient` با timeout پیکربندی‌شده از `EXTERNAL_API_TIMEOUT` استفاده می‌کند. مدل `AIProvider` در `app/models/ai_provider.py` (خطوط 14-23) شامل فیلدهای `name`, `description`, `is_enabled` است اما فاقد فیلدهای `base_url` و `api_key` است. در `app/routes/ai.py` endpoint‌های `/ai/configs` برای CRUD مدل‌ها وجود دارد اما هیچ endpointای برای تست اتصال وجود ندارد. فایل `app/config.py` شامل `settings.GOOGLE_CLIENT_ID` و `settings.OPENAI_API_KEY` است. فایل `app/services/ai/model_service.py` شامل توابع CRUD برای `AIModelConfig` است. فایل `frontend/src/pages/AISettings.jsx` صفحه تنظیمات AI را شامل می‌شود که می‌توان دکمه Test Connection را به آن اضافه کرد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلدهای `base_url` و `api_key` به مدل `AIProvider` در `app/models/ai_provider.py` (خطوط 14-23).
2. ایجاد تابع `test_provider_connection` در `app/services/ai/provider_service.py` که با استفاده از `base_url` و `api_key` یک درخواست GET به endpoint لیست مدل‌ها (مانند `/v1/models` برای OpenAI) ارسال کرده و نتیجه را برمی‌گرداند.
3. افزودن endpoint جدید `POST /api/ai/providers/{provider_id}/test-connection` در `app/routes/ai.py` که تابع تست را فراخوانی کرده و نتیجه را به صورت JSON شامل `success: true/false` و `message` برمی‌گرداند.
4. افزودن دکمه 'Test Connection' در کامپوننت فرانت‌اند مربوط به تنظیمات AI Provider در `frontend/src/pages/AISettings.jsx` که با کلیک، درخواست به endpoint جدید ارسال کرده و نتیجه را در یک modal یا toast نمایش می‌دهد.
5. به‌روزرسانی `app/schemas/ai_provider_schema.py` برای شامل کردن فیلدهای جدید در Pydantic schema.
6. افزودن تست واحد برای تابع `test_provider_connection` در `app/services/ai/provider_service.py`.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 13: ایجاد قابلیت logging و نمایش خطاهای آنالیز

**Scope:** این مرحله شامل پیاده‌سازی یک سیستم logging است که خطاهای رخ‌داده در طول آنالیز (مانند خطای API، timeout، خطای پرامپت) را ثبت کرده و به کاربر نمایش دهد. این logs باید شامل timestamp، نوع خطا، و پیام خطا باشند. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: logs باید در یک بخش مجزا در UI قابل مشاهده باشند و همچنین در بک‌اند ذخیره شوند.
**Key terms:** logging, خطا, API, timeout, timestamp, UI

**بخش مربوط از متن کاربر:**
```
اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
سیستم logging خطاهای آنالیز با نمایش UI

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:44-45` — `logging.basicConfig` — محل فعلی logging سطحی — باید مدل AnalysisLog و endpoint جدید اضافه شود
  ```python
  logging.basicConfig(level=logging.INFO)
  logger = logging.getLogger(__name__)
  ```
- `app/routes/ai.py:1-50` — `router (AI endpoints)` — احتمالاً endpointهای /ai/generate و /ai/query در این فایل هستند که باید try/except برای logging خطاها اضافه شود
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `frontend/src/components/Sidebar.jsx:1-100` — `Sidebar component` — باید لینک جدید به /analysis-logs در منوی سایدبار اضافه شود
  ```jsx
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/__init__.py` (سطر 1) — برای export مدل جدید AnalysisLog
- `app/database.py` (سطر 1) — برای ایجاد جدول analysis_logs در startup
- `app/services/__init__.py` (سطر 1) — برای export سرویس logging_service
- `frontend/src/App.js`

## 🔍 Context و وضعیت فعلی
ایجاد قابلیت logging و نمایش خطاهای آنالیز. این مرحله شامل پیاده‌سازی یک سیستم logging است که خطاهای رخ‌داده در طول آنالیز (مانند خطای API، timeout، خطای پرامپت) را ثبت کرده و به کاربر نمایش دهد. این logs باید شامل timestamp، نوع خطا، و پیام خطا باشند. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: logs باید در یک بخش مجزا در UI قابل مشاهده باشند و همچنین در بک‌اند ذخیره شوند. --- بخش مربوط از درخواست اصلی کاربر --- اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن --- کلیدواژه‌ها --- logging, خطا, API, timeout, timestamp, UI. شواهد در کد: در app/main.py خط 44-45 logging.basicConfig سطح INFO دارد اما هیچ مدل/جدول DB برای ذخیره خطاهای آنالیز وجود ندارد. در app/routes/ai.py احتمالاً endpointهای /ai/generate و /ai/query خطاهای API را catch می‌کنند اما ذخیره نمی‌کنند. در frontend هیچ کامپوننت/صفحه‌ای برای نمایش logs آنالیز وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل AnalysisLog در app/models/analysis_log.py با فیلدهای: id (PK), timestamp (DateTime), error_type (VARCHAR), error_message (TEXT), source (VARCHAR مثل 'api', 'timeout', 'prompt'), user_id (FK به users), related_entity_id (nullable INT), related_entity_type (nullable VARCHAR). 2. ایجاد جدول analysis_logs در startup migration در app/main.py (مشابه سایر ADD COLUMNها). 3. ایجاد سرویس logging_service.py در app/services/ با تابع log_analysis_error که یک رکورد AnalysisLog در DB ثبت کند. 4. تغییر app/routes/ai.py برای catch کردن استثناها و فراخوانی log_analysis_error. 5. ایجاد endpoint GET /api/analysis-logs در app/routes/analysis_logs.py که logs را بر اساس user_id فیلتر کند. 6. ایجاد کامپوننت AnalysisLogsPanel.jsx در frontend/src/components/ که logs را از endpoint واکشی و در یک جدول/لیست نمایش دهد. 7. افزودن مسیر /analysis-logs به React Router و صفحه AnalysisLogsPage.jsx. 8. افزودن لینک به بخش logs در Sidebar.jsx.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 14: بهبود UI/UX صفحه تنظیمات با طراحی responsive و کاربرپسند

**Scope:** این مرحله شامل بهبود ظاهر و تجربه کاربری صفحه تنظیمات است. این شامل طراحی responsive برای نمایش در دستگاه‌های مختلف، استفاده از کامپوننت‌های UI مدرن (مانند Material-UI، Ant Design، یا Tailwind)، و بهبود چیدمان عناصر (Providerها، مدل‌ها، پرامپت) است. خارج از این مرحله: منطق کسب‌وکار. نکته حیاتی: طراحی باید minimal و کاربرپسند باشد و از overcrowding جلوگیری کند.
**Key terms:** UI, UX, responsive, Material-UI, Ant Design, Tailwind, طراحی

**بخش مربوط از متن کاربر:**
```
یه صفحه تنظیمات باید توی سیستم باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
بهبود UI/UX صفحه تنظیمات با طراحی responsive و کاربرپسند

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست بهبود UI/UX صفحه تنظیمات (AISettings) را دارد. این صفحه در frontend/src/pages/AISettings.jsx قرار دارد و شامل مدیریت Providerها، مدل‌ها و پرامپت‌های AI است. کاربر تأکید دارد که طراحی باید minimal و کاربرپسند باشد و از overcrowding جلوگیری کند. همچنین باید responsive باشد تا در دستگاه‌های مختلف (موبایل، تبلت، دسکتاپ) به خوبی نمایش داده شود. کاربر به استفاده از کامپوننت‌های UI مدرن مانند Material-UI، Ant Design، یا Tailwind اشاره کرده است. در پروژه فعلی از Tailwind CSS استفاده می‌شود (frontend/tailwind.config.js و frontend/postcss.config.js). منطق کسب‌وکار خارج از این مرحله است. کلیدواژه‌های اصلی: UI, UX, responsive, Material-UI, Ant Design, Tailwind, طراحی. صفحه AISettings در frontend/src/pages/AISettings.jsx پیاده‌سازی شده و از AuthContext (frontend/src/context/AuthContext.jsx) برای احراز هویت و از api.js (frontend/src/lib/api.js) برای فراخوانی APIهای backend (app/routes/ai.py) استفاده می‌کند. backend شامل endpointهای /api/ai/configs برای CRUD روی مدل‌ها و /api/ai/query برای query کردن مدل‌ها است. فایل app/services/ai/provider_service.py مدیریت Providerها را بر عهده دارد. فایل app/schemas/ai_schema.py و app/schemas/ai_provider_schema.py شامل schemaهای مربوطه هستند. فایل app/models/ai_model_config.py و app/models/ai_provider.py مدل‌های داده را تعریف می‌کنند. فایل app/routes/ai.py شامل routeهای backend است. فایل frontend/src/__tests__/routes.test.jsx شامل تست‌های frontend است. فایل frontend/src/pages/__tests__/AISettings.test.jsx شامل تست‌های صفحه AISettings است. فایل frontend/src/components/Header.jsx و frontend/src/components/Sidebar.jsx و frontend/src/components/Layout.jsx و frontend/src/components/ProtectedRoute.jsx کامپوننت‌های layout را تشکیل می‌دهند. فایل frontend/src/App.jsx مسیریابی را مدیریت می‌کند. فایل frontend/src/index.css شامل استایل‌های global است. فایل frontend/vite.config.js تنظیمات Vite را شامل می‌شود. فایل frontend/package.json و frontend/package-lock.json وابستگی‌ها را مشخص می‌کنند. فایل app/main.py شامل middlewareها و routeهای اصلی backend است. فایل app/config.py و config/settings.py تنظیمات برنامه را مدیریت می‌کنند. فایل app/database.py اتصال به دیتابیس را برقرار می‌کند. فایل app/dependencies/auth.py وابستگی‌های احراز هویت را فراهم می‌کند. فایل app/middleware.py middlewareهای برنامه را شامل می‌شود. فایل app/services/ai/__init__.py و app/services/ai/ai_data_access_service.py و app/services/ai/image_service.py و app/services/ai/model_service.py و app/services/ai/nlp_service.py و app/services/ai/recommendation_service.py سرویس‌های AI را تشکیل می‌دهند. فایل app/services/auth_service.py سرویس احراز هویت را مدیریت می‌کند. فایل app/schemas/__init__.py و app/schemas/auth.py و app/schemas/task.py و app/schemas/project.py و app/schemas/notification_schema.py و app/schemas/user_schema.py و app/schemas/todo_list_schema.py و app/schemas/todo_item_schema.py و app/schemas/person_schema.py و app/schemas/integration_schema.py و app/schemas/external_project_schema.py و app/schemas/local_file_entry_schema.py و app/schemas/self_improvement_schema.py و app/schemas/planner.py و app/schemas/project_schema.py و app/schemas/task_schema.py و app/schemas/user_oauth.py schemaهای مختلف را تعریف می

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 15: افزودن قابلیت جستجو و فیلتر در لیست Providerها و مدل‌ها

**Scope:** این مرحله شامل افزودن قابلیت جستجو و فیلتر در لیست Providerها و مدل‌ها است. کاربر باید بتواند با تایپ نام Provider یا مدل، لیست را فیلتر کند. همچنین می‌توان فیلترهای دیگری مانند 'فقط Providerهای فعال' اضافه کرد. خارج از این مرحله: CRUD عملیات. نکته حیاتی: جستجو باید به‌صورت real-time و بدون نیاز به رفرش صفحه انجام شود.
**Key terms:** جستجو, فیلتر, Provider, مدل, real-time

**بخش مربوط از متن کاربر:**
```
مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن جستجو و فیلتر real-time به لیست Providerها و مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-23` — `AIProvider` — مدل AIProvider — فیلدهای name و is_enabled برای جستجو و
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable=True)
      name = Column(String(120), nullable=False)
      description = Column(Text, nullable=True)
      is_enabled = Column(Boolean, default=True, nullable=False)
      created_at = Column(DateTime(timezone=True), server_default=func.now())
      updated_at = Column(DateTime(timezone=True), onupdate=func.now())
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت جستجو و فیلتر در لیست Providerها و مدل‌ها را داده است. کاربر باید بتواند با تایپ نام Provider یا مدل، لیست را فیلتر کند. همچنین می‌توان فیلترهای دیگری مانند 'فقط Providerهای فعال' اضافه کرد. خارج از این مرحله: CRUD عملیات. نکته حیاتی: جستجو باید به‌صورت real-time و بدون نیاز به رفرش صفحه انجام شود. کاربر به Providerهایی مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی اشاره کرده است. کلیدواژه‌ها: جستجو, فیلتر, Provider, مدل, real-time. در کد فعلی، مدل `AIProvider` در `app/models/ai_provider.py` (خط ۱۴-۲۳) دارای فیلدهای `id`, `user_id`, `name`, `description`, `is_enabled`, `created_at`, `updated_at` است. همچنین مدل `AIModelConfig` در `app/models/ai_model_config.py` (که deep-read نشده) احتمالاً فیلدهای مشابهی دارد. روتر `ai` در `app/routes/ai.py` (deep-read شده) شامل endpointهای `/ai/configs` برای CRUD مدل‌هاست. در `app/services/ai/provider_service.py` (خط ۲۰-۲۲) تابع `has_openai_key()` صرفاً وجود کلید OpenAI را بررسی می‌کند. در `app/main.py` (خط ۶۰۹) روتر `ai.router` با تگ `ai` mount شده است. در `frontend/src/pages/AISettings.jsx` (deep-read نشده) احتمالاً صفحه تنظیمات AI وجود دارد. برای پیاده‌سازی، نیاز به افزودن پارامترهای query string `?q=` و `?active_only=true` به endpointهای GET لیست Providerها و مدل‌ها در بک‌اند، و افزودن فیلد جستجوی real-time در فرانت‌اند است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در بک‌اند، به endpoint GET /api/ai/configs در `app/routes/ai.py` پارامترهای query string `q` (برای جستجوی متن در name و provider) و `active_only` (boolean) اضافه کن. 2. یک endpoint GET جدید برای لیست Providerها (احتمالاً /api/ai/providers) ایجاد کن یا از endpoint موجود استفاده کن. 3. در سرویس `app/services/ai/model_service.py` (deep-read نشده) تابع جستجو را با فیلتر بر اساس `name` و `provider` پیاده‌سازی کن. 4. در فرانت‌اند، در `frontend/src/pages/AISettings.jsx` یک input جستجو با event listener `onInput` اضافه کن که با هر بار تایپ (debounce شده) درخواست GET به بک‌اند بزند. 5. یک checkbox یا toggle برای 'فقط Providerهای فعال' اضافه کن که فیلد `is_enabled` را فیلتر کند. 6. جستجو باید real-time باشد (بدون نیاز به دکمه جستجو یا رفرش صفحه).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 16: پیاده‌سازی قابلیت تعیین اولویت (Priority) برای Providerها

**Scope:** این مرحله شامل افزودن قابلیت تعیین اولویت برای Providerها است. کاربر می‌تواند ترتیب Providerها را مشخص کند (مثلاً از طریق drag-and-drop) تا در زمان آنالیز، مدل‌ها به ترتیب اولویت استفاده شوند (در صورت failover). خارج از این مرحله: منطق failover. نکته حیاتی: اولویت‌ها باید در دیتابیس ذخیره شوند.
**Key terms:** اولویت, Priority, drag-and-drop, failover, ترتیب

**بخش مربوط از متن کاربر:**
```
اونها کار آنالیز رو به شکلی که میگم انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت تعیین اولویت (Priority) برای AI Providerها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:1-50` — `class AIProvider` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مدل AIProvider باید فیلد priority (Integer, default=0) دریافت کند.
- `app/services/ai/provider_service.py:1-100` — `provider_service functions` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. سرویس باید متد get_providers_sorted_by_priority() داشته باشد.
- `app/routes/ai.py:1-200` — `router` — فایل deep-read شده اما محتوای کامل موجود نیست. باید endpoint PUT /api/ai/providers/priority اضافه شود.
- `frontend/src/pages/AISettings.jsx:1-300` — `AISettings component` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. کامپوننت AISettings باید بخش drag-and-drop برای مرتب‌سازی Providerها داشته باشد.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/ai/ai_data_access_service.py` — این سرویس هنگام انتخاب مدل برای آنالیز، باید Providerها را بر

## 🔍 Context و وضعیت فعلی
پیاده‌سازی قابلیت تعیین اولویت (Priority) برای Providerها در سیستم AI. کاربر می‌خواهد بتواند ترتیب Providerها را مشخص کند (مثلاً از طریق drag-and-drop در فرانت‌اند) تا در زمان آنالیز، مدل‌ها به ترتیب اولویت استفاده شوند (در صورت failover). خارج از این مرحله: منطق failover. نکته حیاتی: اولویت‌ها باید در دیتابیس ذخیره شوند.

بخش مربوط از درخواست اصلی کاربر: «اونها کار آنالیز رو به شکلی که میگم انجام بدن»

کلیدواژه‌ها: اولویت, Priority, drag-and-drop, failover, ترتیب

شواهد در کد: مدل `AIProvider` در `app/models/ai_provider.py` وجود دارد (فایل deep-read نشده اما در ساختار پروژه موجود است). همچنین `app/services/ai/provider_service.py` سرویس مربوط به Providerها است. در `app/routes/ai.py` مسیر `/api/ai/configs` برای مدیریت کانفیگ‌های مدل وجود دارد که می‌تواند endpoint جدیدی برای تنظیم اولویت دریافت کند. مدل `AIModelConfig` در `app/models/ai_model_config.py` دارای فیلد `provider` است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلد `priority` (Integer) به مدل `AIProvider` در `app/models/ai_provider.py`.
2. به‌روزرسانی `app/services/ai/provider_service.py` برای پشتیبانی از مرتب‌سازی Providerها بر اساس `priority`.
3. افزودن endpoint جدید در `app/routes/ai.py` برای به‌روزرسانی اولویت‌ها (مثلاً `PUT /api/ai/providers/priority` که آرایه‌ای از `{provider_id, priority}` دریافت کند).
4. ایجاد کامپوننت drag-and-drop در فرانت‌اند (احتمالاً در `frontend/src/pages/AISettings.jsx`) برای نمایش و مرتب‌سازی Providerها.
5. به‌روزرسانی `app/services/ai/ai_data_access_service.py` تا هنگام انتخاب مدل برای آنالیز، Providerها را بر اساس `priority` مرتب کند.
6. افزودن migration برای اضافه کردن ستون `priority` به جدول `ai_providers`.
7. خارج از این مرحله: پیاده‌سازی منطق failover.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 17: ایجاد قابلیت تعیین حداکثر توکن (Max Tokens) و دما (Temperature) برای هر مدل

**Scope:** این مرحله شامل افزودن فیلدهای پیکربندی برای هر مدل است: حداکثر توکن (max tokens) و دما (temperature). این پارامترها در زمان آنالیز به API Provider ارسال می‌شوند. خارج از این مرحله: سایر پارامترهای API. نکته حیاتی: مقادیر پیش‌فرض باید منطقی باشند (مثلاً max tokens: 2048, temperature: 0.7).
**Key terms:** max tokens, temperature, پارامتر, مدل, API

**بخش مربوط از متن کاربر:**
```
اونها کار آنالیز رو به شکلی که میگم انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن فیلدهای max_tokens و temperature به مدل‌های AI و ارسال به Provider

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-23` — `AIProvider`
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable=True)
      name = Column(String(120), nullable=False)
      description = Column(Text, nullable=True)
      is_enabled = Column(Boolean, default=True, nullable=False)
      created_at
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد قابلیت تعیین حداکثر توکن (Max Tokens) و دما (Temperature) برای هر مدل را دارد. این شامل افزودن فیلدهای پیکربندی برای هر مدل است: حداکثر توکن (max tokens) و دما (temperature). این پارامترها در زمان آنالیز به API Provider ارسال می‌شوند. خارج از این مرحله: سایر پارامترهای API. نکته حیاتی: مقادیر پیش‌فرض باید منطقی باشند (مثلاً max tokens: 2048, temperature: 0.7). کلیدواژه‌ها: max tokens, temperature, پارامتر, مدل, API.

تحلیل کد موجود:
- مدل `AIModelConfig` در `app/models/ai_model_config.py` (فایل deep-read نشده، اما در ساختار پروژه موجود است) احتمالاً فاقد فیلدهای `max_tokens` و `temperature` است.
- مدل `AIProvider` در `app/models/ai_provider.py` (خطوط 14-23) فقط فیلدهای `name`, `description`, `is_enabled` دارد و فاقد فیلدهای پیکربندی inference است.
- سرویس `provider_service.py` در `app/services/ai/provider_service.py` (خطوط 36-75) تابع `call_openai_chat` را دارد که پارامترهای `model`, `max_tokens`, `temperature` را از caller دریافت می‌کند، اما این پارامترها از دیتابیس خوانده نمی‌شوند.
- روتر `ai.py` در `app/routes/ai.py` (فایل deep-read نشده) احتمالاً endpointهای `/ai/configs` را مدیریت می‌کند که باید فیلدهای جدید را پشتیبانی کنند.
- فایل `app/schemas/ai_schema.py` و `app/schemas/ai_provider_schema.py` (فایل‌های deep-read نشده) احتمالاً نیاز به به‌روزرسانی برای شامل شدن فیلدهای جدید دارند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلدهای `max_tokens` (Integer, default=2048) و `temperature` (Float, default=0.7) به مدل `AIModelConfig` در `app/models/ai_model_config.py`.
2. به‌روزرسانی Pydantic schema مربوطه در `app/schemas/ai_schema.py` برای شامل شدن این فیلدها با validation (max_tokens >= 1, temperature بین 0 و 2).
3. به‌روزرسانی endpointهای CRUD در `app/routes/ai.py` برای پذیرش و بازگرداندن فیلدهای جدید.
4. اصلاح `provider_service.py` در `app/services/ai/provider_service.py` (تابع `call_openai_chat`) به‌گونه‌ای که `max_tokens` و `temperature` را از رکورد `AIModelConfig` مربوطه بخواند.
5. به‌روزرسانی `nlp_service.py` در `app/services/ai/nlp_service.py` (فایل deep-read نشده) برای ارسال این پارامترها.
6. افزودن migration آلِِmbic برای اضافه کردن ستون‌های جدید به جدول `ai_model_configs`.
7. به‌روزرسانی frontend در `frontend/src/pages/AISettings.jsx` برای نمایش و ویرایش این فیلدها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 18: پیاده‌سازی قابلیت آنالیز دسته‌ای (Batch Analysis) چند صفحه به طور همزمان

**Scope:** این مرحله شامل افزودن قابلیتی است که کاربر بتواند چندین صفحه را برای آنالیز انتخاب کند و مدل‌ها همه آن‌ها را به‌صورت دسته‌ای آنالیز کنند. نتایج باید به‌صورت جداگانه برای هر صفحه نمایش داده شوند. خارج از این مرحله: آنالیز تکی. نکته حیاتی: این قابلیت باید از queue system استفاده کند تا درخواست‌ها مدیریت شوند.
**Key terms:** Batch Analysis, دسته‌ای, صفحه, queue, همزمان

**بخش مربوط از متن کاربر:**
```
هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت آنالیز دسته‌ای (Batch Analysis) صفحات با Queue

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:1-50` — `router` — فایل deep-read نشده — بر اساس ساختار سطحی. این فایل endpoint‌های AI را شامل می‌شود. باید دو endpoint جدید به آن اضافه شود: POST /api/ai/batch-analyze و GET /api/ai/batch-analyze/{job_id}/status.
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  
  router = APIRouter(prefix="/ai")
  ```
- `app/tasks.py:1-10` — `tasks` — فایل deep-read نشده — بر اساس ساختار سطحی. این فایل باید شامل تسک جدید batch_analysis_task باشد که هر صفحه را به صورت جداگانه آنالیز می‌کند.
  ```python
  # Celery tasks for background processing
  from app.celery_app import celery_app
  
  @celery_app.task
  def sample_task():
      pass
  ```
- `app/celery_app.py:1-10` — `celery_app`
  ```python
  from celery import Celery
  from app.config import settings
  
  celery_app = Celery(
      "lifemanager",
      broker=settings
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
پیاده‌سازی قابلیت آنالیز دسته‌ای (Batch Analysis) چند صفحه به طور همزمان. این مرحله شامل افزودن قابلیتی است که کاربر بتواند چندین صفحه را برای آنالیز انتخاب کند و مدل‌ها همه آن‌ها را به‌صورت دسته‌ای آنالیز کنند. نتایج باید به‌صورت جداگانه برای هر صفحه نمایش داده شوند. خارج از این مرحله: آنالیز تکی. نکته حیاتی: این قابلیت باید از queue system استفاده کند تا درخواست‌ها مدیریت شوند. بخش مربوط از درخواست اصلی کاربر: 'هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن'. کلیدواژه‌ها: Batch Analysis, دسته‌ای, صفحه, queue, همزمان. در کد فعلی، مسیرهای آنالیز در app/routes/ai.py (احتمالاً endpoint‌های /ai/generate و /ai/query) و سرویس‌های مربوطه در app/services/ai/ (شامل model_service.py، nlp_service.py، recommendation_service.py) قرار دارند. همچنین app/tasks.py برای تعریف تسک‌های Celery و app/celery_app.py برای پیکربندی Celery موجود است. frontend/src/pages/SmartAssistant.jsx صفحه‌ای است که احتمالاً این قابلیت به آن اضافه می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در app/routes/ai.py به نام POST /api/ai/batch-analyze که آرایه‌ای از page_ids را دریافت کند. 2. ایجاد یک تسک Celery جدید در app/tasks.py به نام batch_analysis_task که هر صفحه را به صورت جداگانه آنالیز کند. 3. ایجاد یک سرویس جدید در app/services/ai/batch_service.py که مدیریت صف و توزیع کارها را انجام دهد. 4. ایجاد یک مدل جدید در app/models/ برای ذخیره وضعیت آنالیز دسته‌ای (BatchJob). 5. ایجاد یک endpoint GET /api/ai/batch-analyze/{job_id}/status برای بررسی وضعیت. 6. در frontend (احتمالاً SmartAssistant.jsx)، افزودن قابلیت انتخاب چند صفحه و دکمه 'آنالیز دسته‌ای'. 7. نمایش نتایج هر صفحه به صورت جداگانه پس از اتمام آنالیز. 8. استفاده از Redis (که در .env.example با REDIS_URL پیکربندی شده) به عنوان broker برای Celery queue.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 19: افزودن قابلیت ذخیره نتایج آنالیز در تاریخچه

**Scope:** این مرحله شامل افزودن قابلیت ذخیره نتایج آنالیز در یک تاریخچه (history) است. کاربر می‌تواند نتایج آنالیزهای قبلی را مشاهده، جستجو، و دوباره بارگذاری کند. خارج از این مرحله: اجرای آنالیز جدید. نکته حیاتی: تاریخچه باید شامل timestamp، مدل استفاده‌شده، و صفحه آنالیز شده باشد.
**Key terms:** تاریخچه, history, ذخیره, نتایج, timestamp

**بخش مربوط از متن کاربر:**
```
اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن تاریخچه ذخیره نتایج آنالیز

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_assessment.py:1-50` — `کل فایل` — محل مناسب برای مدل جدید تاریخچه آنالیز
  ```python
  فایل فعلی احتمالاً خالی یا دارای کلاس‌های پایه است. نیاز به افزودن کلاس AnalysisHistory.
  ```
- `app/routes/ai.py:1-100` — `router` — محل افزودن endpointهای جدید history
  ```python
  router = APIRouter(prefix='/ai')
  ```
- `app/services/ai/__init__.py:1-10` — `__init__` — نیاز به import سرویس جدید history
  ```python
  فایل __init__ برای پکیج ai
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/user.py` (سطر 1) — مدل User برای رابطه FK با analysis_history
- `app/database.py` (سطر 1) — برای ثبت مدل جدید در Base.metadata
- `app/main.py` (سطر 609) — برای include کردن router جدید (اگر router جدید اضافه شود)
- `app/schemas/ai_schema.py` (سطر 1) — برای ایجاد Pydantic schemaهای مربوط به history
- `app/dependencies/auth.py` (سطر 1) — برای احراز هویت در endpointهای history

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت ذخیره نتایج آنالیز در یک تاریخچه (history) را دارد. کاربر می‌خواهد نتایج آنالیزهای قبلی را مشاهده، جستجو، و دوباره بارگذاری کند. خارج از این مرحله: اجرای آنالیز جدید. نکته حیاتی: تاریخچه باید شامل timestamp، مدل استفاده‌شده، و صفحه آنالیز شده باشد. کاربر اشاره کرده که 'اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن'. کلیدواژه‌ها: تاریخچه, history, ذخیره, نتایج, timestamp. در کد فعلی، مسیرهای مرتبط با آنالیز در app/routes/ai.py (با prefix /ai) و سرویس‌های app/services/ai/ وجود دارند. مدل‌های موجود در app/models/ai_assessment.py و app/models/ai_model_config.py می‌توانند پایه‌ای برای ذخیره تاریخچه باشند. همچنین app/models/notification.py برای رویدادهای اطلاع‌رسانی موجود است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک مدل جدید به نام AnalysisHistory در app/models/ai_assessment.py با فیلدهای: id (PK), user_id (FK به users), timestamp (DateTime), model_used (string), page_analyzed (string), result_data (JSON/text), created_at. 2. ایجاد یک سرویس جدید app/services/ai/analysis_history_service.py با توابع: save_analysis_result, get_history (با pagination), search_history, get_analysis_by_id. 3. افزودن endpointهای جدید در app/routes/ai.py: GET /api/ai/history (لیست تاریخچه با query params برای page, per_page, search, model, page_analyzed), GET /api/ai/history/{history_id} (دریافت یک نتیجه خاص), DELETE /api/ai/history/{history_id} (حذف یک رکورد). 4. به‌روزرسانی endpoint موجود POST /api/ai/query (یا /api/ai/generate) برای ذخیره خودکار نتیجه در history پس از اجرا. 5. ایجاد migration در alembic برای جدول analysis_history. 6. افزودن تست‌های unit و integration برای سرویس و endpointهای جدید.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 20: پیاده‌سازی قابلیت اشتراک‌گذاری نتایج آنالیز (Share Results)

**Scope:** این مرحله شامل افزودن قابلیت اشتراک‌گذاری نتایج آنالیز از طریق لینک یا export به فرمت‌های مختلف (PDF, Markdown, Text) است. کاربر می‌تواند یک لینک قابل اشتراک‌گذاری تولید کند یا نتیجه را به فرمت مورد نظر دانلود کند. خارج از این مرحله: ذخیره نتایج. نکته حیاتی: لینک‌های اشتراک‌گذاری باید دارای expiration date باشند.
**Key terms:** اشتراک‌گذاری, Share, PDF, Markdown, لینک, export

**بخش مربوط از متن کاربر:**
```
اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت اشتراک‌گذاری نتایج آنالیز با لینک و export به PDF/Markdown/Text

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:1-50` — `router` — این فایل محل اصلی endpoint‌های AI است. باید endpoint‌های جدید /api/analysis/share به این router اضافه شوند.
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  
  router = APIRouter(prefix="/ai", tags=["ai"])
  ```
- `app/services/ai/ai_data_access_service.py:1-30` — `AIDataAccessService`
  ```python
  class AIDataAccessService:
      """Service for accessing various data sources
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت اشتراک‌گذاری نتایج آنالیز (Share Results) را دارد. این قابلیت شامل دو بخش اصلی است: (۱) تولید لینک قابل اشتراک‌گذاری با expiration date (تاریخ انقضا) و (۲) export نتیجه به فرمت‌های PDF, Markdown, Text. کاربر تأکید کرده که ذخیره نتایج خارج از این مرحله است و نکته حیاتی این است که لینک‌های اشتراک‌گذاری باید دارای expiration date باشند. همچنین کاربر اشاره کرده: 'اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن' که نشان می‌دهد نتایج آنالیز باید از داده‌های موجود در صفحات دیگر (مانند tasks, projects, finance, people) استخراج شوند. کلیدواژه‌های کاربر: اشتراک‌گذاری, Share, PDF, Markdown, لینک, export. در کد فعلی پروژه، مسیرهای آنالیز در app/routes/ai.py (endpoint‌های /ai/generate و /ai/query) و app/services/ai/ (شامل ai_data_access_service.py, recommendation_service.py, nlp_service.py) قرار دارند. همچنین صفحات frontend مانند SmartAssistant.jsx و AISettings.jsx و Dashboard.jsx برای نمایش نتایج آنالیز استفاده می‌شوند. برای پیاده‌سازی این قابلیت، نیاز به ایجاد endpoint جدید برای تولید لینک اشتراک‌گذاری (با expiration date)، endpoint برای دریافت نتیجه با لینک، و endpoint برای export به فرمت‌های مختلف است. در سمت frontend نیز باید دکمه/آیکون اشتراک‌گذاری به صفحات نمایش نتایج اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد مدل SharedAnalysisResult در app/models/ با فیلدهای: id, user_id, analysis_data (JSON), share_token (UUID unique), expires_at (datetime), created_at, format (enum: link/pdf/markdown/text). ۲. ایجاد مسیر جدید /api/analysis/share در app/routes/ai.py: POST برای تولید لینک با expiration date (دریافت analysis_id و expires_in_hours)، GET /api/analysis/share/{token} برای دریافت نتیجه با لینک، GET /api/analysis/share/{token}/export?format=pdf|markdown|text برای export. ۳. ایجاد سرویس share_service.py در app/services/ برای تولید token (uuid4)، اعتبارسنجی expiration، و تولید خروجی Markdown/Text (برای PDF از کتابخانه reportlab یا weasyprint استفاده شود). ۴. در frontend، به کامپوننت‌های نمایش نتایج آنالیز (مانند SmartAssistant.jsx) دکمه 'اشتراک‌گذاری' اضافه شود که modal با گزینه‌های 'تولید لینک' (با انتخاب مدت اعتبار) و 'دانلود' (با انتخاب فرمت) نمایش دهد. ۵. برای expiration date، از فیلد expires_at در مدل استفاده شود و در endpoint دریافت لینک، اگر تاریخ گذشته باشد، 410 Gone برگرداند. ۶. برای export PDF، از کتابخانه reportlab (اضافه به requirements.txt) استفاده شود و خروجی به صورت StreamingResponse برگردانده شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. جداول `ai_providers` و `ai_models` در پایگاه داده ایجاد شده باشند و جدول `ai_model_configs` با فیلدهای جدید `ai_model_id`, `alias`, `custom_api_key`, `custom_config_json`, `is_default_for_user` و حذف `model_name`, `provider` به‌روزرسانی شده باشد. _(verify: backend_test)_
2. مسیرهای API جدید برای CRUD ارائه‌دهندگان (مثلاً `POST /ai/providers`, `GET /ai/providers/{id}`) و مدل‌ها (مثلاً `POST /ai/models`, `GET /ai/models/{id}`) در `app/routes/ai.py` قابل دسترسی باشند. _(verify: api_response)_
3. توابع سرویس در `app/services/ai/provider_service.py` و `app/services/ai/model_service.py` (یا فایل‌های مشابه) برای مدیریت `AIProvider` و `AIModel` و `AIModelConfig` (کاربر-محور) به درستی پیاده‌سازی شده باشند. _(verify: static)_
4. یک صفحه تنظیمات در فرانت‌اند (مثلاً `frontend/src/pages/AISettings.jsx`) وجود داشته باشد که امکان مشاهده، افزودن و ویرایش ارائه‌دهندگان و مدل‌های هوش مصنوعی را فراهم کند. _(verify: ui_interaction)_
5. کلیدهای API (چه در سطح ارائه‌دهنده و چه در سطح کاربر) قبل از ذخیره در پایگاه داده رمزنگاری شوند و هنگام بازیابی رمزگشایی شوند. _(verify: backend_test)_
6. فایل `app/models/ai_provider.py` باید شامل تعریف کلاس `AIProvider` با فیلدهای `id`, `name`, `description`, `is_enabled`, `user_id`, `created_at`, `updated_at` باشد. _(verify: static)_
7. فایل `app/schemas/ai_provider_schema.py` باید شامل شمای Pydantic `AIProviderResponse` با فیلدهای `id`, `name`, `description`, `is_enabled`, `user_id`, `created_at`, `updated_at` باشد. _(verify: static)_
8. endpoint `POST /api/ai/providers` باید با احراز هویت ادمین، یک ارائه‌دهنده جدید ایجاد کرده و با کد وضعیت 200 یا 201 و جزئیات ارائه‌دهنده پاسخ دهد. _(verify: api_response)_
9. endpoint `GET /api/ai/providers` باید با احراز هویت ادمین، لیستی از ارائه‌دهندگان هوش مصنوعی را برگرداند. _(verify: api_response)_
10. کاربران غیر ادمین که تلاش می‌کنند به endpointهای مدیریت ارائه‌دهندگان (مانند `POST /api/ai/providers`) دسترسی پیدا کنند، باید خطای 403 Forbidden دریافت کنند. _(verify: api_response)_
11. یک اسکریپت مهاجرت Alembic جدید باید جدول `ai_providers` را در دیتابیس ایجاد کند. _(verify: backend_test)_
12. مدل `AIModelConfig` در `app/models/ai_model_config.py` باید فیلد `provider` از نوع `str` داشته باشد. _(verify: static)_
13. شمای `AIModelConfigCreate` در `app/schemas/ai_schema.py` باید شامل فیلد `provider` از نوع `str` باشد. _(verify: static)_
14. `POST /api/ai/configs` باید امکان ایجاد یک مدل جدید را با تعیین `provider` فراهم کند و کد وضعیت 201 برگرداند. پاسخ باید شامل `id`, `name`, و `provider` باشد. _(verify: api_response)_
15. `GET /api/ai/configs` باید لیست مدل‌ها را برگرداند و از پارامتر کوئری `provider` برای فیلتر کردن پشتیبانی کند. اگر `provider` مشخص شود، فقط مدل‌های آن ارائه‌دهنده برگردانده شوند. _(verify: api_response)_
16. `PATCH /api/ai/configs/{config_id}` باید امکان به‌روزرسانی فیلد `provider` یک مدل موجود را فراهم کند و کد وضعیت 200 برگرداند. _(verify: api_response)_
17. `DELETE /api/ai/configs/{config_id}` باید یک مدل را حذف کند و کد وضعیت 204 برگرداند. _(verify: api_response)_
18. مستندات `docs/API.md` باید به‌روزرسانی شود تا فیلد `provider` در شمای مدل‌ها و قابلیت فیلتر کردن با پارامتر کوئری `provider` را منعکس کند. _(verify: manual_only)_
19. جدول `global_analysis_prompts` باید در پایگاه داده با فیلدهای `id`, `prompt_text`, `last_edited_at`, و `edited_by_user_id` وجود داشته باشد. _(verify: static)_
20. endpoint `GET /api/ai/global-prompt` باید پرامپت تحلیل جهانی را برگرداند (یا یک پرامپت پیش‌فرض اگر وجود نداشت). _(verify: api_response)_
21. endpoint `PUT /api/ai/global-prompt` باید بتواند پرامپت را به‌روزرسانی کند و فیلدهای `last_edited_at` و `edited_by_user_id` را به‌درستی تنظیم کند. _(verify: api_response)_
22. سرویس‌های AI (مانند `app/services/ai_service.py` یا `app/services/ai/nlp_service.py`) باید بتوانند پرامپت تحلیل جهانی را از `global_prompt_service` دریافت و در عملیات خود استفاده کنند. _(verify: static)_
23. یک migration Alembic برای ایجاد جدول `global_analysis_prompts` باید با موفقیت اعمال شود. _(verify: backend_test)_
24. فایل‌های `app/models/analysis_prompt.py` و `app/services/ai/analysis_prompt_service.py` ایجاد شده و شامل مدل و سرویس مربوطه باشند. _(verify: static)_
25. endpoint `GET /api/ai/analysis_prompt` با موفقیت پرامپت تحلیل جهانی را برمی‌گرداند (کد وضعیت 200) و اگر پرامپتی وجود نداشته باشد، یک پرامپت پیش‌فرض خالی یا اولیه را برمی‌گرداند. _(verify: api_response)_
26. endpoint `PUT /api/ai/analysis_prompt` برای کاربران غیر ادمین با کد وضعیت 403 (Forbidden) پاسخ می‌دهد. _(verify: api_response)_
27. endpoint `PUT /api/ai/analysis_prompt` برای کاربر ادمین با موفقیت پرامپت را به‌روزرسانی می‌کند (کد وضعیت 200) و محتوای به‌روزرسانی شده را برمی‌گرداند. _(verify: api_response)_
28. پس از به‌روزرسانی پرامپت توسط ادمین، فراخوانی مجدد `GET /api/ai/analysis_prompt` محتوای به‌روزرسانی شده را برمی‌گرداند که نشان‌دهنده ذخیره صحیح در پایگاه داده است. _(verify: api_response)_
29. فایل `app/services/ai/ai_data_access_service.py` ایجاد شده و شامل توابعی برای بازیابی داده‌های `Task`, `Project`, `TodoItem` و `Notification` برای یک `user_id` مشخص است. _(verify: static)_
30. یک endpoint جدید `GET /api/ai/user_data_context` در `app/routes/ai.py` تعریف شده که نیاز به احراز هویت دارد و داده‌های متنی کاربر را برمی‌گرداند. _(verify: api_response)_
31. داده‌های بازگشتی از `GET /api/ai/user_data_context` فقط شامل اطلاعات مربوط به کاربر احراز هویت شده است و هیچ داده‌ای از کاربران دیگر را افشا نمی‌کند. _(verify: backend_test)_
32. شمای Pydantic `AIContextResponse` و `AIContextItem` در `app/schemas/ai_schema.py` تعریف شده‌اند که ساختار داده‌های بازگشتی برای AI را مشخص می‌کنند. _(verify: static)_
33. سرویس `app/services/ai_service.py` قابلیت استفاده از داده‌های 'context' فراهم شده توسط `ai_data_access_service` را در توابع تولید/پرس‌وجوی AI دارد. _(verify: static)_
34. یک endpoint جدید `POST /api/ai/analyze` در `app/routes/ai.py` تعریف شده باشد که `AIAnalysisRequest` را دریافت و `AIAnalysisResult` را برگرداند. _(verify: static)_
35. متد `orchestrate_analysis` در `app/services/ai_service.py` پیاده‌سازی شده باشد که پرامپت، داده‌های مرتبط را دریافت و مدل AI را فراخوانی کند. _(verify: static)_
36. ارسال یک درخواست `POST` به `/api/ai/analyze` با `model_id` و `prompt` معتبر، پاسخ `200 OK` با یک `AIAnalysisResult` معتبر را برگرداند. _(verify: api_response)_
37. اگر `FEATURE_AI_ENABLED` در تنظیمات `app/core/config.py` غیرفعال باشد، endpoint `/api/ai/analyze` باید `403 Forbidden` برگرداند. _(verify: api_response)_
38. تست‌های واحد برای متد `orchestrate_analysis` در `tests/test_ai_service.py` اضافه شده باشد که سناریوهای مختلف (انتخاب مدل، بازیابی داده، فراخوانی API) را پوشش دهد. _(verify: backend_test)_
39. فایل جدید `frontend/src/pages/Settings.jsx` ایجاد شده و یک کامپوننت React را export می‌کند. _(verify: static)_
40. صفحه تنظیمات از طریق URL `/settings` قابل دسترسی است و در داخل کامپوننت `Layout` (شامل Header، Sidebar و Footer) رندر می‌شود. _(verify: ui_interaction)_
41. صفحه `Settings` شامل بخش‌های نگهدارنده مجزا برای «مدیریت ارائه‌دهندگان AI»، «مدیریت مدل‌های AI» و «جعبه پرامپت تحلیل» است. _(verify: ui_interaction)_
42. یک لینک ناوبری به صفحه «تنظیمات» در کامپوننت `Sidebar` وجود دارد. _(verify: ui_interaction)_
43. فایل `frontend/src/App.jsx` شامل مسیر جدید برای `/settings` است. _(verify: static)_
44. کاربر می‌تواند به صفحه جدید تنظیمات ارائه‌دهندگان هوش مصنوعی از طریق مسیر `/settings` دسترسی پیدا کند. _(verify: ui_interaction)_
45. صفحه تنظیمات باید لیستی از ارائه‌دهندگان هوش مصنوعی موجود را نمایش دهد (با فراخوانی `GET /api/ai/configs`). _(verify: ui_interaction)_
46. کاربر می‌تواند از طریق یک فرم در صفحه تنظیمات، ارائه‌دهنده هوش مصنوعی جدیدی را اضافه کند (با فراخوانی `POST /api/ai/configs`). _(verify: ui_interaction)_
47. کاربر می‌تواند جزئیات یک ارائه‌دهنده هوش مصنوعی موجود را ویرایش کند (با فراخوانی `PATCH /api/ai/configs/{config_id}`). _(verify: ui_interaction)_
48. کاربر می‌تواند یک ارائه‌دهنده هوش مصنوعی را از لیست حذف کند (با فراخوانی `DELETE /api/ai/configs/{config_id}`). _(verify: ui_interaction)_
49. یک لینک به صفحه تنظیمات ارائه‌دهندگان هوش مصنوعی در نوار کناری (Sidebar) یا سربرگ (Header) فرانت‌اند وجود دارد. _(verify: ui_interaction)_
50. مسیر `/settings/ai-models` در فرانت‌اند قابل دسترسی باشد و صفحه مدیریت مدل‌های AI را نمایش دهد. _(verify: ui_interaction)_
51. صفحه مدیریت مدل‌ها، لیستی از مدل‌های AI موجود را از endpoint `/api/ai/configs` دریافت و نمایش دهد. این لیست باید شامل فیلدهای `id`, `name`, و `provider` باشد. _(verify: api_response)_
52. کاربر بتواند از طریق فرم، یک مدل AI جدید اضافه کند که با `POST` به `/api/ai/configs` ارسال شود و پس از موفقیت، مدل جدید در لیست نمایش داده شود. _(verify: ui_interaction)_
53. کاربر بتواند یک مدل AI موجود را ویرایش کند که با `PATCH` به `/api/ai/configs/{id}` ارسال شود و تغییرات در لیست منعکس شود. _(verify: ui_interaction)_
54. کاربر بتواند یک مدل AI موجود را حذف کند که با `DELETE` به `/api/ai/configs/{id}` ارسال شود و از لیست حذف شود. _(verify: ui_interaction)_
55. فیلدهای فرم افزودن/ویرایش مدل شامل گزینه‌ای برای انتخاب ارائه‌دهنده (provider) باشد که از لیست ارائه‌دهندگان موجود پر شود. _(verify: static)_
56. جدول `global_settings` با ستون‌های `id`, `key` (UNIQUE), `value` در پایگاه داده ایجاد شود. _(verify: static)_
57. endpoint `GET /api/settings/global-analysis-prompt` برای ادمین، پرامپت تحلیل جهانی را برمی‌گرداند (یا یک مقدار پیش‌فرض اگر وجود نداشته باشد). _(verify: api_response)_
58. endpoint `PUT /api/settings/global-analysis-prompt` برای ادمین، پرامپت تحلیل جهانی را به‌روزرسانی می‌کند. _(verify: api_response)_
59. دسترسی به endpointهای `global-analysis-prompt` برای کاربران غیرادمین با خطای 403 Forbidden مواجه شود. _(verify: api_response)_
60. یک لینک 'Settings' در `frontend/src/components/Sidebar.jsx` به مسیر `/settings` اضافه شود. _(verify: static)_
61. صفحه `/settings` در فرانت‌اند یک `textarea` نمایش دهد که با مقدار فعلی پرامپت تحلیل جهانی پر شده است. _(verify: ui_interaction)_
62. دکمه 'ذخیره' در صفحه تنظیمات، تغییرات را به بک‌اند ارسال کرده و پرامپت را به‌روزرسانی کند. _(verify: ui_interaction)_
63. دکمه 'لغو' در صفحه تنظیمات، تغییرات محلی را نادیده گرفته و مقدار اصلی پرامپت را بازیابی کند. _(verify: ui_interaction)_
64. فایل `app/services/event_publisher.py` و تابع `publish_data_change_event` در آن وجود داشته باشد. _(verify: static)_
65. وظیفه Celery به نام `process_ai_ingestion_event` در `app/tasks.py` تعریف شده باشد که `entity_type`, `entity_id`, و `action` را به‌عنوان آرگومان می‌پذیرد. _(verify: static)_
66. پس از ایجاد یک `TodoItem` جدید (مثلاً از طریق `app/routes/todo_items.py` که `todo_item_service` را فراخوانی می‌کند)، یک وظیفه `process_ai_ingestion_event` با `entity_type='todo_item'`, `entity_id=<new_item_id>`, `action='created'` در صف Celery قرار گیرد. _(verify: backend_test)_
67. سرویس `app/services/ai_ingestion_service.py` بتواند یک `TodoItem` را با `ID` بازیابی کرده و محتوای متنی آن (مانند `description`) را به `app/services/ai/nlp_service.py` برای پردازش ارسال کند. _(verify: backend_test)_
68. تابع `analyze_content` در `app/services/ai/nlp_service.py` بتواند محتوای متنی را از انواع مختلف موجودیت‌ها پردازش کرده و یک نتیجه تحلیل (حداقل یک دیکشنری با فیلدهای `summary` و `keywords`) برگرداند. _(verify: static)_

## Task Steps

### Step 1: ایجاد صفحه تنظیمات اصلی با قابلیت مدیریت Providerها و مدل‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند Providerهای مختلف (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و غیره) را اضافه کرده و برای هر Provider مدل‌های مربوطه را mapping و add کند. این صفحه باید امکان افزودن، ویرایش و حذف Providerها و مدل‌های آن‌ها را فراهم کند. خارج از این مرحله: قابلیت آنالیز داده‌ها، جعبه پرامپت، و دسترسی به صفحات دیگر. نکته حیاتی: این صفحه باید به‌عنوان یک ماژول مستقل عمل کند و داده‌های Providerها و مدل‌ها را در یک ساختار داده قابل ذخیره‌سازی (مانند دیتابیس یا فایل JSON) نگهداری کند.
**Excerpt:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

### Step 2: افزودن جعبه پرامپت قابل ویرایش در صفحه تنظیمات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن یک جعبه متن (text box) در صفحه تنظیمات است که کاربر بتواند یک پرامپت سفارشی برای آنالیز داده‌ها بنویسد. این جعبه باید همیشه قابل ویرایش باشد و کاربر بتواند هر زمان که بخواهد محتوای آن را تغییر دهد. پرامپت باید به‌صورت پویا ذخیره شود تا در زمان آنالیز توسط مدل‌ها استفاده شود. خارج از این مرحله: قابلیت آنالیز واقعی، اتصال به مدل‌ها، و دسترسی به داده‌های صفحات دیگر. نکته حیاتی: جعبه پرامپت باید در همان بخشی از صفحه تنظیمات قرار گیرد که Providerها و مدل‌ها مدیریت می‌شوند.
**Excerpt:**
```
شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه
```

### Step 3: ایجاد قابلیت دسترسی مدل‌ها به صفحات و داده‌های داخل هر صفحه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی مکانیزمی است که به مدل‌های تعریف‌شده در صفحه تنظیمات اجازه دهد به تمام صفحات موجود در سیستم و داده‌های داخل آن‌ها دسترسی داشته باشند. این دسترسی باید به‌صورت实时 (real-time) باشد، به این معنی که هر صفحه یا داده جدیدی که اضافه می‌شود بلافاصله برای مدل‌ها قابل دسترسی باشد. خارج از این مرحله: اجرای واقعی آنالیز توسط مدل‌ها، و قابلیت ویرایش پرامپت. نکته حیاتی: این قابلیت باید از طریق یک API یا سرویس مرکزی پیاده‌سازی شود که تمام داده‌های صفحات را جمع‌آوری کرده و در اختیار مدل‌ها قرار دهد.
**Excerpt:**
```
اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. ... هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.
```

### Step 4: پیاده‌سازی قابلیت آنالیز داده‌ها توسط مدل‌ها بر اساس پرامپت
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی هسته اصلی آنالیز است که در آن مدل‌های تعریف‌شده (با توجه به پرامپت کاربر) داده‌های صفحات را آنالیز می‌کنند. این فرآیند باید از پرامپت ذخیره‌شده در مرحله ۲ استفاده کند و داده‌های جمع‌آوری‌شده از مرحله ۳ را به مدل ارسال کند. خروجی آنالیز باید به کاربر نمایش داده شود. خارج از این مرحله: مدیریت Providerها، ویرایش پرامپت، و دسترسی به داده‌ها. نکته حیاتی: این مرحله باید از یک معماری async استفاده کند تا آنالیزهای طولانی باعث مسدود شدن UI نشوند.
**Excerpt:**
```
و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

### Step 5: ذخیره‌سازی و بازیابی تنظیمات (Providerها، مدل‌ها، پرامپت) در دیتابیس یا فایل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی لایه ذخیره‌سازی برای تمام تنظیمات مربوط به Providerها، مدل‌ها و پرامپت است. این داده‌ها باید در یک دیتابیس (مانند SQLite، PostgreSQL) یا فایل JSON ذخیره شوند تا پس از رفرش صفحه یا بستن مرورگر از بین نروند. همچنین باید قابلیت بازیابی این تنظیمات در زمان لود صفحه وجود داشته باشد. خارج از این مرحله: UI و منطق آنالیز. نکته حیاتی: ساختار داده باید به‌گونه‌ای باشد که به راحتی قابل گسترش باشد (مثلاً افزودن Provider جدید).
**Excerpt:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم ... و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

### Step 6: ایجاد API برای ارتباط بین فرانت‌اند و بک‌اند (مدیریت تنظیمات و آنالیز)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد APIهای لازم برای ارتباط بین فرانت‌اند (صفحه تنظیمات) و بک‌اند است. این APIها باید شامل endpoints برای CRUD عملیات روی Providerها و مدل‌ها، ذخیره و بازیابی پرامپت، و trigger آنالیز باشند. خارج از این مرحله: UI و منطق آنالیز. نکته حیاتی: APIها باید از استاندارد REST پیروی کنند و خطاهای مناسب را برگردانند.
**Excerpt:**
```
یه صفحه تنظیمات باید توی سیستم باشه ... و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو انجام بدن
```

### Step 7: پیاده‌سازی قابلیت افزودن Providerهای جدید توسط کاربر (نه فقط predefined)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیتی است که کاربر بتواند Providerهای جدید (غیر از لیست predefined) را به سیستم اضافه کند. این شامل فیلدهایی برای نام Provider، base URL API، و کلید API (API key) است. خارج از این مرحله: مدیریت مدل‌های هر Provider. نکته حیاتی: این قابلیت باید امن باشد و کلیدهای API به‌صورت امن ذخیره شوند.
**Excerpt:**
```
مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه
```

### Step 8: پیاده‌سازی قابلیت mapping مدل‌ها برای هر Provider
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیتی است که کاربر بتواند برای هر Provider، مدل‌های مختلف را mapping کند. این شامل انتخاب یک Provider، مشاهده لیست مدل‌های موجود (در صورت امکان از طریق API Provider)، و افزودن مدل‌های جدید با نام و شناسه (model ID) است. خارج از این مرحله: افزودن Provider جدید. نکته حیاتی: mapping باید به‌صورت یک رابطه many-to-many بین Provider و مدل‌ها پیاده‌سازی شود.
**Excerpt:**
```
اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

### Step 9: ایجاد قابلیت انتخاب مدل برای آنالیز (کدام مدل از کدام Provider استفاده شود)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیتی است که کاربر بتواند مشخص کند کدام مدل از کدام Provider برای آنالیز استفاده شود. این می‌تواند به‌صورت یک dropdown یا selector در صفحه تنظیمات یا در صفحه آنالیز باشد. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: کاربر باید بتواند چندین مدل را برای آنالیز همزمان انتخاب کند.
**Excerpt:**
```
اونها کار آنالیز رو به شکلی که میگم انجام بدن
```

### Step 10: پیاده‌سازی نمایش نتایج آنالیز در یک بخش مجزا
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک بخش UI برای نمایش نتایج آنالیز است. این بخش باید بتواند خروجی مدل‌ها (متن، JSON، یا هر فرمت دیگر) را به‌صورت readable نمایش دهد. همچنین باید قابلیت مقایسه نتایج چند مدل (در صورت انتخاب چند مدل) وجود داشته باشد. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: نتایج باید به‌صورت real-time (در صورت استفاده از streaming) یا پس از اتمام آنالیز نمایش داده شوند.
**Excerpt:**
```
اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

### Step 11: افزودن قابلیت ذخیره و بارگذاری تنظیمات به‌صورت export/import
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت export تنظیمات (Providerها، مدل‌ها، پرامپت) به یک فایل JSON و import آن فایل برای بازگردانی تنظیمات است. این قابلیت به کاربر اجازه می‌دهد تنظیمات خود را بین محیط‌های مختلف جابجا کند. خارج از این مرحله: UI اصلی تنظیمات. نکته حیاتی: فایل export باید شامل تمام تنظیمات باشد و در زمان import، تنظیمات موجود را بازنویسی کند (با هشدار).
**Excerpt:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم
```

### Step 12: پیاده‌سازی قابلیت تست اتصال به Provider (Test Connection)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن دکمه 'Test Connection' برای هر Provider است که اتصال به API Provider را با استفاده از base URL و API key ذخیره‌شده تست می‌کند. نتیجه تست (موفقیت یا خطا) باید به کاربر نمایش داده شود. خارج از این مرحله: مدیریت مدل‌ها. نکته حیاتی: این قابلیت باید از یک endpoint ساده Provider (مانند لیست مدل‌ها) استفاده کند تا اتصال را تأیید کند.
**Excerpt:**
```
مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی
```

### Step 13: ایجاد قابلیت logging و نمایش خطاهای آنالیز
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک سیستم logging است که خطاهای رخ‌داده در طول آنالیز (مانند خطای API، timeout، خطای پرامپت) را ثبت کرده و به کاربر نمایش دهد. این logs باید شامل timestamp، نوع خطا، و پیام خطا باشند. خارج از این مرحله: اجرای آنالیز. نکته حیاتی: logs باید در یک بخش مجزا در UI قابل مشاهده باشند و همچنین در بک‌اند ذخیره شوند.
**Excerpt:**
```
اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

### Step 14: بهبود UI/UX صفحه تنظیمات با طراحی responsive و کاربرپسند
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بهبود ظاهر و تجربه کاربری صفحه تنظیمات است. این شامل طراحی responsive برای نمایش در دستگاه‌های مختلف، استفاده از کامپوننت‌های UI مدرن (مانند Material-UI، Ant Design، یا Tailwind)، و بهبود چیدمان عناصر (Providerها، مدل‌ها، پرامپت) است. خارج از این مرحله: منطق کسب‌وکار. نکته حیاتی: طراحی باید minimal و کاربرپسند باشد و از overcrowding جلوگیری کند.
**Excerpt:**
```
یه صفحه تنظیمات باید توی سیستم باشه
```

### Step 15: افزودن قابلیت جستجو و فیلتر در لیست Providerها و مدل‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت جستجو و فیلتر در لیست Providerها و مدل‌ها است. کاربر باید بتواند با تایپ نام Provider یا مدل، لیست را فیلتر کند. همچنین می‌توان فیلترهای دیگری مانند 'فقط Providerهای فعال' اضافه کرد. خارج از این مرحله: CRUD عملیات. نکته حیاتی: جستجو باید به‌صورت real-time و بدون نیاز به رفرش صفحه انجام شود.
**Excerpt:**
```
مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه
```

### Step 16: پیاده‌سازی قابلیت تعیین اولویت (Priority) برای Providerها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت تعیین اولویت برای Providerها است. کاربر می‌تواند ترتیب Providerها را مشخص کند (مثلاً از طریق drag-and-drop) تا در زمان آنالیز، مدل‌ها به ترتیب اولویت استفاده شوند (در صورت failover). خارج از این مرحله: منطق failover. نکته حیاتی: اولویت‌ها باید در دیتابیس ذخیره شوند.
**Excerpt:**
```
اونها کار آنالیز رو به شکلی که میگم انجام بدن
```

### Step 17: ایجاد قابلیت تعیین حداکثر توکن (Max Tokens) و دما (Temperature) برای هر مدل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن فیلدهای پیکربندی برای هر مدل است: حداکثر توکن (max tokens) و دما (temperature). این پارامترها در زمان آنالیز به API Provider ارسال می‌شوند. خارج از این مرحله: سایر پارامترهای API. نکته حیاتی: مقادیر پیش‌فرض باید منطقی باشند (مثلاً max tokens: 2048, temperature: 0.7).
**Excerpt:**
```
اونها کار آنالیز رو به شکلی که میگم انجام بدن
```

### Step 18: پیاده‌سازی قابلیت آنالیز دسته‌ای (Batch Analysis) چند صفحه به طور همزمان
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیتی است که کاربر بتواند چندین صفحه را برای آنالیز انتخاب کند و مدل‌ها همه آن‌ها را به‌صورت دسته‌ای آنالیز کنند. نتایج باید به‌صورت جداگانه برای هر صفحه نمایش داده شوند. خارج از این مرحله: آنالیز تکی. نکته حیاتی: این قابلیت باید از queue system استفاده کند تا درخواست‌ها مدیریت شوند.
**Excerpt:**
```
هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن
```

### Step 19: افزودن قابلیت ذخیره نتایج آنالیز در تاریخچه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت ذخیره نتایج آنالیز در یک تاریخچه (history) است. کاربر می‌تواند نتایج آنالیزهای قبلی را مشاهده، جستجو، و دوباره بارگذاری کند. خارج از این مرحله: اجرای آنالیز جدید. نکته حیاتی: تاریخچه باید شامل timestamp، مدل استفاده‌شده، و صفحه آنالیز شده باشد.
**Excerpt:**
```
اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

### Step 20: پیاده‌سازی قابلیت اشتراک‌گذاری نتایج آنالیز (Share Results)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت اشتراک‌گذاری نتایج آنالیز از طریق لینک یا export به فرمت‌های مختلف (PDF, Markdown, Text) است. کاربر می‌تواند یک لینک قابل اشتراک‌گذاری تولید کند یا نتیجه را به فرمت مورد نظر دانلود کند. خارج از این مرحله: ذخیره نتایج. نکته حیاتی: لینک‌های اشتراک‌گذاری باید دارای expiration date باشند.
**Excerpt:**
```
اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```
