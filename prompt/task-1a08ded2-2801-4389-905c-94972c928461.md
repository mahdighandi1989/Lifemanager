---
task_id: 1a08ded2-2801-4389-905c-94972c928461
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:15:14.628371+00:00'
updated_at: '2026-05-28T20:03:09.166409+00:00'
target_files:
- app/models/ai_provider.py
- app/routes/ai.py
- app/schemas/ai_provider_schema.py
- app/models/ai_model_config.py
- app/services/ai/nlp_service
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_292354_AgADih0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 4,248 char متن_

## شرح ایده اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## ویژگی‌های کلیدی و قابلیت‌ها  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پرپلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## اهداف و مزایای اصلی  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پروایدرها رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پروایدرها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم، اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## بازار هدف  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جمنی، کلاد، گراک، پلکسیتی همچین چیزی. و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## پتانسیل رشد و چشم‌انداز  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پروواید پرووایدها رو بتونم قرار بدم مثل دیپ سیک، جی‌پی‌تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_292354_AgADih0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 4,248 char متن_

## شرح ایده اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن. شکلی هم که بهش میگم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و اینها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## ویژگی‌های کلیدی و قابلیت‌ها  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پرپلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## اهداف و مزایای اصلی  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پروایدرها رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جِمینای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پروایدرها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم، اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## بازار هدف  _(at: audio/ogg)_

[00:00] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ‌سیک، جی‌پی‌تی، جمنی، کلاد، گراک، پلکسیتی همچین چیزی. و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن، یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه. و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.

## پتانسیل رشد و چشم‌انداز  _(at: audio/ogg)_

[00:01] یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پروواید پرووایدها رو بتونم قرار بدم مثل دیپ سیک، جی‌پی‌تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدها رو مپینگ کنم و اد کنم و اون‌ها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که می‌گم انجام بدن. شکلی هم که بهش می‌گم باید داخل همون صفحه تنظیمات یه تو اون قسمتی که مدل‌ها و این‌ها مپینگ شدن چیز کنن یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه و حسب این پرامپت و این دستورات که هر سری من می‌تونم تغییرش بدم اون‌ها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن و هر صفحه و هر داده‌ای هم که اضافه می‌شه این‌ها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن.
```

## 📋 چک‌لیست مراحل (6 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد صفحه تنظیمات اصلی با بخش مدیریت Providerها** — این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند لیست Providerهای هوش مصنوعی (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و چند مورد دیگر) را مدیریت کند. این صفحه باید امکان افزودن Provider جدید، ویرایش نام Provider و حذف Provider را فراهم کند. خارج از این مرحله
- [ ] **مرحله 2: افزودن قابلیت مدیریت مدل‌های هر Provider (Mapping و Add)** — این مرحله شامل افزودن قابلیت به صفحه تنظیمات است که در آن کاربر بتواند برای هر Provider، مدل‌های مختلف را تعریف کند (Mapping). کاربر باید بتواند مدل جدید اضافه کند، نام مدل را مشخص کند، و مدل را به Provider مربوطه متصل کند. خارج از این مرحله: اتصال به APIهای واقعی، بخش پرامپت، و دسترسی به صفحات دیگر
- [ ] **مرحله 3: ایجاد بخش پرامپت قابل ویرایش در صفحه تنظیمات** — این مرحله شامل افزودن یک جعبه متن (Textarea) در صفحه تنظیمات است که کاربر بتواند یک پرامپت سفارشی برای آنالیز بنویسد. این پرامپت باید همیشه قابل ویرایش باشد و تغییرات آن بلافاصله ذخیره شود. خارج از این مرحله: اجرای آنالیز، اتصال به مدل‌ها، و دسترسی به داده‌های صفحات دیگر. نکته حیاتی: پرامپت باید در 
- [ ] **مرحله 4: ایجاد سرویس دسترسی مدل‌ها به داده‌های صفحات دیگر** — این مرحله شامل ایجاد یک سرویس یا API است که به مدل‌های تعریف‌شده اجازه می‌دهد به داده‌های موجود در صفحات دیگر سیستم دسترسی داشته باشند. این سرویس باید بتواند داده‌های تمام صفحات را جمع‌آوری کرده و در قالبی قابل استفاده برای مدل‌ها (مانند JSON) ارائه دهد. خارج از این مرحله: اجرای واقعی آنالیز توسط مد
- [ ] **مرحله 5: ایجاد قابلیت آنالیز خودکار با استفاده از پرامپت و داده‌های صفحات** — این مرحله شامل ایجاد یک قابلیت است که با استفاده از پرامپت تعریف‌شده توسط کاربر و داده‌های جمع‌آوری‌شده از صفحات، یک آنالیز خودکار انجام دهد. این قابلیت باید مدل انتخاب‌شده را فراخوانی کند، پرامپت و داده‌ها را به آن ارسال کند، و نتیجه را نمایش دهد. خارج از این مرحله: مدیریت خطاهای API، و بهینه‌سازی.
- [ ] **مرحله 6: ایجاد قابلیت به‌روزرسانی خودکار دسترسی به داده‌ها هنگام افزودن صفحات جدید** — این مرحله شامل ایجاد یک مکانیزم است که به‌محض افزودن یک صفحه جدید به سیستم، داده‌های آن صفحه به‌طور خودکار در دسترس سرویس آنالیز قرار گیرد. این می‌تواند از طریق hooks, events, یا polling انجام شود. خارج از این مرحله: مدیریت صفحات موجود، و بهینه‌سازی. نکته حیاتی: این قابلیت باید بدون نیاز به تنظیمات 

---

# 🔹 مرحله 1: ایجاد صفحه تنظیمات اصلی با بخش مدیریت Providerها

**Scope:** این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند لیست Providerهای هوش مصنوعی (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و چند مورد دیگر) را مدیریت کند. این صفحه باید امکان افزودن Provider جدید، ویرایش نام Provider و حذف Provider را فراهم کند. خارج از این مرحله: اتصال به APIهای واقعی، مدیریت مدل‌های هر Provider، و بخش پرامپت. نکته حیاتی: این صفحه باید به‌صورت پویا عمل کند و Providerها را از یک منبع داده (مانند دیتابیس یا فایل کانفیگ) بارگذاری کند.
**Key terms:** صفحه تنظیمات, Provider, DeepSeek, GPT, Gemini, Claude, Grok, Perplexity, مدیریت Providerها

**بخش مربوط از متن کاربر:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد صفحه تنظیمات اصلی با مدیریت پویای Providerهای هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-23` — `AIProvider`
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک صفحه تنظیمات جدید در سیستم LifeManager داده است که در آن بتواند لیست Providerهای هوش مصنوعی (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و چند مورد دیگر) را مدیریت کند. این صفحه باید امکان افزودن Provider جدید، ویرایش نام Provider و حذف Provider را فراهم کند. خارج از این مرحله: اتصال به APIهای واقعی، مدیریت مدل‌های هر Provider، و بخش پرامپت. نکته حیاتی: این صفحه باید به‌صورت پویا عمل کند و Providerها را از یک منبع داده (مانند دیتابیس یا فایل کانفیگ) بارگذاری کند. متن خام کاربر: «یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم». کلیدواژه‌ها: صفحه تنظیمات, Provider, DeepSeek, GPT, Gemini, Claude, Grok, Perplexity, مدیریت Providerها. در کد فعلی، مدل `AIProvider` در `app/models/ai_provider.py` (خطوط 14-23) وجود دارد که شامل فیلدهای `id`, `user_id`, `name`, `description`, `is_enabled`, `created_at`, `updated_at` است. همچنین مدل `GlobalAnalysisPrompt` در همان فایل (خطوط 26-42) برای پرامپت سراسری وجود دارد. روتر `ai` در `app/routes/ai.py` شامل endpointهای `/ai/providers` برای CRUD روی Providerها است. اما صفحه فرانت‌اند برای مدیریت این Providerها وجود ندارد. کاربر صراحتاً خواسته که این صفحه پویا باشد و Providerها را از دیتابیس بارگذاری کند، نه سخت‌کد شده.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک صفحه فرانت‌اند جدید در `frontend/src/pages/Settings.jsx` که شامل یک بخش مدیریت Providerها باشد. 2. این صفحه باید از endpointهای موجود `/ai/providers` (GET برای لیست، POST برای ایجاد، PATCH برای ویرایش، DELETE برای حذف) استفاده کند. 3. در این صفحه، یک جدول یا لیست از Providerها نمایش داده شود که هر ردیف شامل نام Provider (DeepSeek, GPT, Gemini, Claude, Grok, Perplexity و ...) و دکمه‌های ویرایش/حذف باشد. 4. یک دکمه «افزودن Provider جدید» که یک مودال باز کند با فیلد نام (و احتمالاً توضیحات). 5. پس از هر عملیات (افزودن/ویرایش/حذف)، لیست به‌صورت پویا به‌روزرسانی شود (re-fetch از API). 6. مسیر `/settings` در روتر فرانت‌اند (احتمالاً `frontend/src/App.jsx`) اضافه شود. 7. یک لینک به این صفحه در منوی ناوبری (کامپوننت `Sidebar.jsx` یا `Header.jsx`) قرار گیرد. 8. از `axios` (که در `frontend/src/lib/api.js` پیکربندی شده) برای فراخوانی API استفاده شود. 9. مدل `AIProvider` در بک‌اند از قبل وجود دارد و endpointهای `/ai/providers` در `app/routes/ai.py` پیاده‌سازی شده‌اند، بنابراین نیازی به تغییر بک‌اند نیست مگر برای اضافه کردن فیلدهای بیشتر در صورت نیاز.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: افزودن قابلیت مدیریت مدل‌های هر Provider (Mapping و Add)

**Scope:** این مرحله شامل افزودن قابلیت به صفحه تنظیمات است که در آن کاربر بتواند برای هر Provider، مدل‌های مختلف را تعریف کند (Mapping). کاربر باید بتواند مدل جدید اضافه کند، نام مدل را مشخص کند، و مدل را به Provider مربوطه متصل کند. خارج از این مرحله: اتصال به APIهای واقعی، بخش پرامپت، و دسترسی به صفحات دیگر. نکته حیاتی: هر مدل باید به یک Provider خاص متصل باشد و این ارتباط باید در دیتابیس ذخیره شود.
**Key terms:** مدل, Mapping, Provider, Add, مدیریت مدل

**بخش مربوط از متن کاربر:**
```
اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت مدیریت مدل‌های هر Provider (Mapping و Add)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_provider.py:14-23` — `AIProvider` — مدل فعلی AIProvider فاقد رابطه با مدل‌ها است. باید مدل جدید AIProviderModel اضافه شود و رابطه one-to-many بین AIProvider و AIProviderModel برقرار شود.
  ```python
  class AIProvider(Base):
      __tablename__ = "ai_providers"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), index=True, nullable=True)
      name = Column(String(120), nullable=False)
      description = Column(Text, nullable=True)
      is_enabled = Column(Boolean, default=True, nullable=False)
      created_at = Column(DateTime(timezone=True), server_default=func.now())
      updated_at = Column(DateTime(timezone=True), onupdate=func.now())
  ```
- `app/routes/ai.py:1-50` — `router` — فایل routes/ai.py حاوی endpointهای مربوط به AI است. باید endpointهای جدید برای مدیریت مدل‌های Provider به این فایل اضافه شود. این فایل deep-read نشده است — مجری باید مسیر را خود تأیید کند.
  ```python
  router = APIRouter(prefix="/ai", tags=["ai"])
  ```
- `app/schemas/ai_provider_schema.py:1-30` — `AIProviderSchema`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت مدیریت مدل‌های هر Provider را در صفحه تنظیمات دارد. این قابلیت شامل Mapping مدل‌ها به Providerهای مختلف و Add کردن مدل‌های جدید است. کاربر باید بتواند برای هر Provider (مانند DeepSeek, GPT, Gemini, Claude, Grok, Perplexity که در مدل AIProvider در فایل app/models/ai_provider.py در خط 4-6 اشاره شده) مدل‌های مختلف را تعریف کند. هر مدل باید به یک Provider خاص متصل باشد و این ارتباط باید در دیتابیس ذخیره شود. خارج از این مرحله: اتصال به APIهای واقعی، بخش پرامپت، و دسترسی به صفحات دیگر. کلیدواژه‌های کاربر: مدل, Mapping, Provider, Add, مدیریت مدل. در کد فعلی، مدل AIProvider در app/models/ai_provider.py (خطوط 14-23) فقط شامل فیلدهای id, user_id, name, description, is_enabled, created_at, updated_at است و هیچ جدول یا مدلی برای ذخیره مدل‌های متصل به Provider وجود ندارد. همچنین در app/routes/ai.py endpointهای /ai/providers برای CRUD Providerها وجود دارد (طبق docs/API.md) اما endpointای برای مدیریت مدل‌های هر Provider وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل جدید AIProviderModel در app/models/ai_provider.py با فیلدهای: id, provider_id (FK به AIProvider.id), name (نام مدل مانند gpt-4, gpt-3.5-turbo), is_enabled, created_at, updated_at. 2. ایجاد Pydantic schema برای AIProviderModel در app/schemas/ai_provider_schema.py. 3. افزودن endpointهای CRUD برای مدل‌های Provider در app/routes/ai.py: GET /ai/providers/{provider_id}/models (لیست مدل‌های یک Provider), POST /ai/providers/{provider_id}/models (افزودن مدل جدید), DELETE /ai/providers/{provider_id}/models/{model_id} (حذف مدل). 4. ایجاد سرویس مدیریت مدل‌ها در app/services/ai/model_service.py (که هم‌اکنون برای CRUD مدل‌های AI Model Config وجود دارد). 5. به‌روزرسانی frontend برای نمایش و مدیریت مدل‌های هر Provider در صفحه تنظیمات.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: ایجاد بخش پرامپت قابل ویرایش در صفحه تنظیمات

**Scope:** این مرحله شامل افزودن یک جعبه متن (Textarea) در صفحه تنظیمات است که کاربر بتواند یک پرامپت سفارشی برای آنالیز بنویسد. این پرامپت باید همیشه قابل ویرایش باشد و تغییرات آن بلافاصله ذخیره شود. خارج از این مرحله: اجرای آنالیز، اتصال به مدل‌ها، و دسترسی به داده‌های صفحات دیگر. نکته حیاتی: پرامپت باید در دیتابیس یا فایل کانفیگ ذخیره شود و پس از هر بار ویرایش، به‌روزرسانی شود.
**Key terms:** پرامپت, جعبه متن, ویرایش, ذخیره, تنظیمات

**بخش مربوط از متن کاربر:**
```
یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن جعبه متن پرامپت قابل ویرایش در صفحه تنظیمات

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_model_config.py:1-50` — `AiModelConfig`
  ```python
  class AiModelConfig(Base):
      __tablename__ = "ai_model_configs"
      id = Column(Integer, primary_key=True)
      user_id = Column(Integer, ForeignKey("users.id"), nullable=True)
      model
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک بخش پرامپت قابل ویرایش در صفحه تنظیمات را دارد. این بخش شامل یک جعبه متن (Textarea) است که کاربر بتواند یک پرامپت سفارشی برای آنالیز بنویسد. این پرامپت باید همیشه قابل ویرایش باشد و تغییرات آن بلافاصله ذخیره شود. خارج از این مرحله: اجرای آنالیز، اتصال به مدل‌ها، و دسترسی به داده‌های صفحات دیگر. نکته حیاتی: پرامپت باید در دیتابیس یا فایل کانفیگ ذخیره شود و پس از هر بار ویرایش، به‌روزرسانی شود. کلیدواژه‌های کاربر: پرامپت, جعبه متن, ویرایش, ذخیره, تنظیمات. در کد فعلی پروژه، صفحه تنظیمات (Settings) وجود ندارد. فایل‌های frontend/src/pages/ شامل Dashboard.jsx, Home.jsx, ListDetail.jsx, Login.jsx, Notifications.jsx, Projects.jsx, Register.jsx, Tasks.jsx هستند و هیچکدام صفحه تنظیمات نیستند. در بک‌اند، مدل‌های موجود در app/models/ شامل ai_model_config.py, ai_provider.py, finance.py, integration.py, local_file_entry.py, notification.py, person.py, project.py, self_improvement.py, task.py, todo_item.py, todo_list.py, user.py, user_location.py, user_oauth.py, webhook_event.py هستند. مدل ai_model_config.py (در app/models/ai_model_config.py) دارای فیلدهای system_prompt, user_prompt, temperature, max_tokens, model_name است که می‌تواند برای ذخیره پرامپت استفاده شود. همچنین app/config.py و config/settings.py شامل تنظیمات برنامه هستند. روتر app/routes/ai.py شامل endpointهای /ai/configs برای مدیریت کانفیگ‌های مدل است. برای ذخیره پرامپت، می‌توان از مدل AiModelConfig یا یک مدل جدید (مثلاً UserPrompt) استفاده کرد. صفحه تنظیمات باید در frontend/src/pages/ ایجاد شود و یک Textarea با قابلیت ذخیره خودکار (auto-save) داشته باشد. بک‌اند نیاز به یک endpoint جدید مانند PATCH /api/users/prompt یا POST /api/ai/global-prompt دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل جدید UserPrompt در app/models/user_prompt.py با فیلدهای: id (Integer PK), user_id (FK به User), prompt_text (Text), updated_at (DateTime). 2. ایجاد schema جدید در app/schemas/ با نام user_prompt_schema.py شامل UserPromptCreate, UserPromptUpdate, UserPromptResponse. 3. ایجاد سرویس جدید app/services/prompt_service.py با توابع get_prompt(user_id), upsert_prompt(user_id, prompt_text). 4. ایجاد روتر جدید app/routes/prompts.py با endpointهای: GET /api/prompts (دریافت پرامپت کاربر), PUT /api/prompts (به‌روزرسانی پرامپت). 5. ثبت روتر در app/main.py (خطوط 569-616). 6. ایجاد صفحه تنظیمات frontend/src/pages/Settings.jsx با یک Textarea که مقدار آن از API خوانده شود و با هر بار تایپ (debounce 500ms) به سرور ارسال شود. 7. افزودن مسیر /settings در frontend/src/App.jsx. 8. افزودن لینک به تنظیمات در frontend/src/components/Sidebar.jsx یا Header.jsx. 9. ایجاد migration جدید در migrations/versions/ برای جدول user_prompts. 10. افزودن تست‌های unit برای سرویس و endpoint.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ایجاد سرویس دسترسی مدل‌ها به داده‌های صفحات دیگر

**Scope:** این مرحله شامل ایجاد یک سرویس یا API است که به مدل‌های تعریف‌شده اجازه می‌دهد به داده‌های موجود در صفحات دیگر سیستم دسترسی داشته باشند. این سرویس باید بتواند داده‌های تمام صفحات را جمع‌آوری کرده و در قالبی قابل استفاده برای مدل‌ها (مانند JSON) ارائه دهد. خارج از این مرحله: اجرای واقعی آنالیز توسط مدل‌ها، و نمایش نتایج. نکته حیاتی: این سرویس باید به‌صورت پویا با هر صفحه جدیدی که اضافه می‌شود، به‌روزرسانی شود.
**Key terms:** دسترسی به داده, صفحات, سرویس, API, جمع‌آوری داده

**بخش مربوط از متن کاربر:**
```
اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد سرویس دسترسی مدل‌ها به داده‌های صفحات

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:1-150` — `router` — فایل اصلی routeهای AI — endpoint جدید GET /ai/data-context باید اینجا اضافه شود
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.services.ai_service import AIService
  
  router = APIRouter(prefix="/ai", tags=["ai"])
  
  @router.post("/generate")
  async def generate_text(request: GenerateRequest, db: AsyncSession = Depends(get_db)):
      service = AIService(db)
      result = await service.generate(request.prompt, request.model_config_id)
      return result
  ```
- `app/services/ai/nlp_service`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک سرویس یا API کرده که به مدل‌های تعریف‌شده (AI models) اجازه دهد به داده‌های موجود در صفحات دیگر سیستم دسترسی داشته باشند. کلیدواژه‌های اصلی: دسترسی به داده, صفحات, سرویس, API, جمع‌آوری داده. کاربر تأکید کرده: «اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن». نکته حیاتی: این سرویس باید به‌صورت پویا با هر صفحه جدیدی که اضافه می‌شود، به‌روزرسانی شود. خارج از این مرحله: اجرای واقعی آنالیز توسط مدل‌ها و نمایش نتایج.

شواهد در کد واقعی: در `app/routes/ai.py` endpoint‌هایی مانند `POST /ai/generate` (خط 45)، `POST /ai/query` (خط 120) و `GET /ai/configs` (خط 80) وجود دارند که با مدل‌های AI سروکار دارند. در `app/services/ai/nlp_service.py` سرویس NLP وجود دارد که می‌تواند داده‌ها را پردازش کند. صفحات موجود در پروژه شامل: Tasks (`app/routes/tasks.py`), Projects (`app/routes/projects.py`), Notifications (`app/routes/notifications.py`), Todo Lists (`app/routes/lists.py`), Users (`app/routes/users.py`), Finance (`app/routes/finance.py`), Persons (`app/routes/person.py`), Locations (`app/routes/location.py`), Self-improvement (`app/routes/self_improvement.py`), Local Files (`app/routes/local_files.py`), Integrations (`app/routes/integrations.py`).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید `app/services/data_access_service.py` که یک سرویس مرکزی برای جمع‌آوری داده از تمام صفحات باشد.
2. در این سرویس، یک تابع `collect_all_page_data(db_session) -> dict` ایجاد کن که:
   - از تمام سرویس‌های موجود (مانند `app/services/list_service.py`, `app/services/todo_item_service.py`, `app/services/person_service.py`, `app/services/notification_service.py`, `app/services/self_improvement_service.py`, `app/services/local_file_service.py`, `app/services/integration_service.py`) داده‌ها را جمع‌آوری کند.
   - داده‌ها را در قالب JSON ساختاریافته برگرداند.
3. ایجاد endpoint جدید در `app/routes/ai.py`:
   - `GET /ai/data-context` که این سرویس را فراخوانی کرده و داده‌های جمع‌آوری‌شده را برگرداند.
4. برای پویایی: از introspection خودکار SQLAlchemy models استفاده کن تا با اضافه شدن هر مدل جدید، به‌طور خودکار شناسایی شود.
5. ایجاد یک registry در `app/services/__init__.py` که هر سرویس جدید خودش را ثبت کند.
6. به‌روزرسانی `app/services/ai/nlp_service.py` تا بتواند از این داده‌ها برای context استفاده کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: ایجاد قابلیت آنالیز خودکار با استفاده از پرامپت و داده‌های صفحات

**Scope:** این مرحله شامل ایجاد یک قابلیت است که با استفاده از پرامپت تعریف‌شده توسط کاربر و داده‌های جمع‌آوری‌شده از صفحات، یک آنالیز خودکار انجام دهد. این قابلیت باید مدل انتخاب‌شده را فراخوانی کند، پرامپت و داده‌ها را به آن ارسال کند، و نتیجه را نمایش دهد. خارج از این مرحله: مدیریت خطاهای API، و بهینه‌سازی. نکته حیاتی: این قابلیت باید از Provider و مدل انتخاب‌شده توسط کاربر استفاده کند.
**Key terms:** آنالیز, پرامپت, داده‌های صفحات, مدل, اجرای آنالیز

**بخش مربوط از متن کاربر:**
```
حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت آنالیز خودکار با پرامپت کاربر و داده‌های صفحات

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک قابلیت آنالیز خودکار را دارد که با استفاده از پرامپت تعریف‌شده توسط کاربر و داده‌های جمع‌آوری‌شده از صفحات، یک آنالیز خودکار انجام دهد. این قابلیت باید مدل انتخاب‌شده را فراخوانی کند، پرامپت و داده‌ها را به آن ارسال کند، و نتیجه را نمایش دهد. خارج از این مرحله: مدیریت خطاهای API، و بهینه‌سازی. نکته حیاتی: این قابلیت باید از Provider و مدل انتخاب‌شده توسط کاربر استفاده کند. متن اصلی کاربر: «حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن». کلیدواژه‌ها: آنالیز, پرامپت, داده‌های صفحات, مدل, اجرای آنالیز. در کد فعلی، مسیرهای مرتبط با AI در فایل `app/routes/ai.py` (خطوط ۱-۲۰۰) شامل endpointهای `/ai/generate`، `/ai/query`، `/ai/configs`، `/ai/providers` و `/ai/global-prompt` هستند. مدل AIProvider در `app/models/ai_provider.py` (خطوط ۱۴-۲۳) تعریف شده و فیلدهای `name`، `description`، `is_enabled` را دارد. سرویس `app/services/ai/provider_service.py` (خطوط ۳۶-۷۵) تابع `call_openai_chat` را برای فراخوانی OpenAI پیاده‌سازی کرده است. همچنین `app/services/ai/nlp_service.py` (خطوط ۱-۱۰۰) تابع `generate_text` را برای orchestration و fallback دارد. برای پیاده‌سازی این قابلیت، باید یک endpoint جدید مانند `/ai/analyze` ایجاد شود که پرامپت کاربر و شناسه‌های صفحات (مثلاً todo-list یا task) را دریافت کند، داده‌های آن صفحات را از دیتابیس بخواند، با استفاده از provider و مدل انتخاب‌شده (از `AIProvider` و `AIModelConfig`) پرامپت را به همراه داده‌ها به مدل ارسال کند، و نتیجه را برگرداند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید POST /ai/analyze در `app/routes/ai.py` که بدنه درخواست شامل `prompt` (رشته)، `page_ids` (لیست شناسه‌های صفحات مانند todo_list_id یا task_id)، و `provider_id` (شناسه provider انتخابی کاربر) باشد. 2. ایجاد یک تابع سرویس جدید `analyze_with_pages` در `app/services/ai/nlp_service.py` که داده‌های صفحات را از دیتابیس (با استفاده از `list_service` یا `task_service`) بخواند، آن‌ها را به یک متن واحد تبدیل کند، و سپس با استفاده از `provider_service.call_openai_chat` (یا provider مشابه) پرامپت + داده‌ها را به مدل ارسال کند. 3. استفاده از `GlobalAnalysisPrompt` (مدل در `app/models/ai_provider.py` خطوط ۲۶-۴۲) برای ذخیره پرامپت سراسری که کاربر در تنظیمات ویرایش می‌کند. 4. در endpoint جدید، ابتدا `global_analysis_prompt` کاربر را از دیتابیس بخوان، سپس پرامپت ارسالی کاربر را به آن اضافه کن (یا ترکیب کن). 5. داده‌های صفحات را با استفاده از `app/services/list_service.py` (برای todo-lists) و `app/services/task_service.py` (برای tasks) بخوان و به صورت متن فرمت کن. 6. provider و مدل را از `AIProvider` و `AIModelConfig` (از `app/routes/ai.py` endpointهای `/ai/providers` و `/ai/configs`) دریافت کن. 7. نتیجه را به صورت JSON شامل `analysis_text`، `model_used`، `tokens_used` برگردان.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: ایجاد قابلیت به‌روزرسانی خودکار دسترسی به داده‌ها هنگام افزودن صفحات جدید

**Scope:** این مرحله شامل ایجاد یک مکانیزم است که به‌محض افزودن یک صفحه جدید به سیستم، داده‌های آن صفحه به‌طور خودکار در دسترس سرویس آنالیز قرار گیرد. این می‌تواند از طریق hooks, events, یا polling انجام شود. خارج از این مرحله: مدیریت صفحات موجود، و بهینه‌سازی. نکته حیاتی: این قابلیت باید بدون نیاز به تنظیمات دستی کار کند.
**Key terms:** صفحه جدید, دسترسی خودکار, به‌روزرسانی, hooks, events

**بخش مربوط از متن کاربر:**
```
هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد مکانیزم به‌روزرسانی خودکار دسترسی داده‌ها برای صفحات جدید

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک قابلیت به‌روزرسانی خودکار دسترسی به داده‌ها هنگام افزودن صفحات جدید را دارد. متن خام کاربر: «ایجاد قابلیت به‌روزرسانی خودکار دسترسی به داده‌ها هنگام افزودن صفحات جدید. این مرحله شامل ایجاد یک مکانیزم است که به‌محض افزودن یک صفحه جدید به سیستم، داده‌های آن صفحه به‌طور خودکار در دسترس سرویس آنالیز قرار گیرد. این می‌تواند از طریق hooks, events, یا polling انجام شود. خارج از این مرحله: مدیریت صفحات موجود، و بهینه‌سازی. نکته حیاتی: این قابلیت باید بدون نیاز به تنظیمات دستی کار کند. --- بخش مربوط از درخواست اصلی کاربر --- هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن --- کلیدواژه‌ها --- صفحه جدید, دسترسی خودکار, به‌روزرسانی, hooks, events». تحلیل: پروژه Lifemanager یک برنامه FastAPI + React است که صفحات مختلفی مانند Tasks, Projects, Notifications, AI, Users, Integrations, Todo-lists, Self-improvement, Planner, Finance, Location, Local-files, Person, Webhook دارد. در کد فعلی، هر صفحه یک روتر مجزا در `app/routes/` دارد (مانند `app/routes/tasks.py`, `app/routes/projects.py`, `app/routes/ai.py`, `app/routes/notifications.py`, `app/routes/lists.py`, `app/routes/todo_items.py`, `app/routes/self_improvement.py`, `app/routes/planner.py`, `app/routes/finance.py`, `app/routes/location.py`, `app/routes/local_files.py`, `app/routes/person.py`, `app/routes/webhook.py`, `app/routes/users.py`, `app/routes/integrations.py`). همچنین frontend صفحاتی مانند `Dashboard.jsx`, `Home.jsx`, `ListDetail.jsx`, `Tasks.jsx`, `Projects.jsx`, `Notifications.jsx`, `Login.jsx`, `Register.jsx`, `Lists.jsx` دارد. سرویس آنالیز مشخصی در کدبیس وجود ندارد؛ نزدیک‌ترین سرویس‌ها `app/services/ai/nlp_service.py` (برای پردازش متن) و `app/services/ai_service.py` (سرویس AI عمومی) هستند. کاربر می‌خواهد وقتی یک صفحه جدید (مثلاً یک روتر جدید یا یک کامپوننت جدید) به سیستم اضافه می‌شود، داده‌های آن صفحه به‌طور خودکار در دسترس سرویس آنالیز قرار گیرد، بدون نیاز به تنظیمات دستی. این می‌تواند از طریق hooks (مثلاً event hook در startup)، events (مانند publish/subscribe با Redis یا Celery)، یا polling (بررسی دوره‌ای) انجام شود. نکته حیاتی: بدون نیاز به تنظیمات دستی.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `app/services/auto_discovery_service.py` که وظیفه شناسایی خودکار صفحات جدید را دارد. این سرویس باید از یک مکانیزم مانند اسکن دایرکتوری `app/routes/` برای یافتن فایل‌های روتر جدید، یا استفاده از یک hook در `app/main.py` در رویداد startup برای ثبت خودکار روترها استفاده کند. 2. ایجاد یک Event Bus ساده (مثلاً با استفاده از Redis Pub/Sub یا یک صف داخلی) تا وقتی صفحه جدیدی شناسایی شد، یک رویداد `page_discovered` منتشر کند. 3. ایجاد یک subscriber (مثلاً در `app/services/analytics_service.py` که باید ساخته شود) که به این رویداد گوش دهد و داده‌های صفحه جدید را به سرویس آنالیز (که مشخص نیست دقیقاً کجاست، احتمالاً `app/services/ai/nlp_service.py` یا یک سرویس جدید) ثبت کند. 4. در `app/main.py`، در تابع `startup_event` (خط 191)، یک مرحله اضافه شود که `auto_discovery_service` را فراخوانی کند تا روترهای موجود را اسکن کرده

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. جداول `ai_providers` و `ai_models` در پایگاه داده ایجاد شده باشند و جدول `ai_model_configs` با فیلدهای جدید `ai_model_id`, `alias`, `custom_api_key`, `custom_config_json`, `is_default_for_user` و حذف `model_name`, `provider` به‌روزرسانی شده باشد. _(verify: backend_test)_
2. مسیرهای API جدید برای CRUD ارائه‌دهندگان (مثلاً `POST /ai/providers`, `GET /ai/providers/{id}`) و مدل‌ها (مثلاً `POST /ai/models`, `GET /ai/models/{id}`) در `app/routes/ai.py` قابل دسترسی باشند. _(verify: api_response)_
3. توابع سرویس در `app/services/ai/provider_service.py` و `app/services/ai/model_service.py` (یا فایل‌های مشابه) برای مدیریت `AIProvider` و `AIModel` و `AIModelConfig` (کاربر-محور) به درستی پیاده‌سازی شده باشند. _(verify: static)_
4. یک صفحه تنظیمات در فرانت‌اند (مثلاً `frontend/src/pages/AISettings.jsx`) وجود داشته باشد که امکان مشاهده، افزودن و ویرایش ارائه‌دهندگان و مدل‌های هوش مصنوعی را فراهم کند. _(verify: ui_interaction)_
5. کلیدهای API (چه در سطح ارائه‌دهنده و چه در سطح کاربر) قبل از ذخیره در پایگاه داده رمزنگاری شوند و هنگام بازیابی رمزگشایی شوند. _(verify: backend_test)_
6. فایل `app/models/ai_provider.py` باید شامل تعریف کلاس `AIProvider` با فیلدهای `id`, `name`, `description`, `is_enabled`, `user_id`, `created_at`, `updated_at` باشد. _(verify: static)_
7. فایل `app/schemas/ai_provider_schema.py` باید شامل شمای Pydantic `AIProviderResponse` با فیلدهای `id`, `name`, `description`, `is_enabled`, `user_id`, `created_at`, `updated_at` باشد. _(verify: static)_
8. endpoint `POST /api/ai/providers` باید با احراز هویت ادمین، یک ارائه‌دهنده جدید ایجاد کرده و با کد وضعیت 200 یا 201 و جزئیات ارائه‌دهنده پاسخ دهد. _(verify: api_response)_
9. endpoint `GET /api/ai/providers` باید با احراز هویت ادمین، لیستی از ارائه‌دهندگان هوش مصنوعی را برگرداند. _(verify: api_response)_
10. کاربران غیر ادمین که تلاش می‌کنند به endpointهای مدیریت ارائه‌دهندگان (مانند `POST /api/ai/providers`) دسترسی پیدا کنند، باید خطای 403 Forbidden دریافت کنند. _(verify: api_response)_
11. یک اسکریپت مهاجرت Alembic جدید باید جدول `ai_providers` را در دیتابیس ایجاد کند. _(verify: backend_test)_
12. مدل `AIModelConfig` در `app/models/ai_model_config.py` باید فیلد `provider` از نوع `str` داشته باشد. _(verify: static)_
13. شمای `AIModelConfigCreate` در `app/schemas/ai_schema.py` باید شامل فیلد `provider` از نوع `str` باشد. _(verify: static)_
14. `POST /api/ai/configs` باید امکان ایجاد یک مدل جدید را با تعیین `provider` فراهم کند و کد وضعیت 201 برگرداند. پاسخ باید شامل `id`, `name`, و `provider` باشد. _(verify: api_response)_
15. `GET /api/ai/configs` باید لیست مدل‌ها را برگرداند و از پارامتر کوئری `provider` برای فیلتر کردن پشتیبانی کند. اگر `provider` مشخص شود، فقط مدل‌های آن ارائه‌دهنده برگردانده شوند. _(verify: api_response)_
16. `PATCH /api/ai/configs/{config_id}` باید امکان به‌روزرسانی فیلد `provider` یک مدل موجود را فراهم کند و کد وضعیت 200 برگرداند. _(verify: api_response)_
17. `DELETE /api/ai/configs/{config_id}` باید یک مدل را حذف کند و کد وضعیت 204 برگرداند. _(verify: api_response)_
18. مستندات `docs/API.md` باید به‌روزرسانی شود تا فیلد `provider` در شمای مدل‌ها و قابلیت فیلتر کردن با پارامتر کوئری `provider` را منعکس کند. _(verify: manual_only)_
19. جدول `global_analysis_prompts` باید در پایگاه داده با فیلدهای `id`, `prompt_text`, `last_edited_at`, و `edited_by_user_id` وجود داشته باشد. _(verify: static)_
20. endpoint `GET /api/ai/global-prompt` باید پرامپت تحلیل جهانی را برگرداند (یا یک پرامپت پیش‌فرض اگر وجود نداشت). _(verify: api_response)_
21. endpoint `PUT /api/ai/global-prompt` باید بتواند پرامپت را به‌روزرسانی کند و فیلدهای `last_edited_at` و `edited_by_user_id` را به‌درستی تنظیم کند. _(verify: api_response)_
22. سرویس‌های AI (مانند `app/services/ai_service.py` یا `app/services/ai/nlp_service.py`) باید بتوانند پرامپت تحلیل جهانی را از `global_prompt_service` دریافت و در عملیات خود استفاده کنند. _(verify: static)_
23. یک migration Alembic برای ایجاد جدول `global_analysis_prompts` باید با موفقیت اعمال شود. _(verify: backend_test)_
24. فایل‌های `app/models/analysis_prompt.py` و `app/services/ai/analysis_prompt_service.py` ایجاد شده و شامل مدل و سرویس مربوطه باشند. _(verify: static)_
25. endpoint `GET /api/ai/analysis_prompt` با موفقیت پرامپت تحلیل جهانی را برمی‌گرداند (کد وضعیت 200) و اگر پرامپتی وجود نداشته باشد، یک پرامپت پیش‌فرض خالی یا اولیه را برمی‌گرداند. _(verify: api_response)_
26. endpoint `PUT /api/ai/analysis_prompt` برای کاربران غیر ادمین با کد وضعیت 403 (Forbidden) پاسخ می‌دهد. _(verify: api_response)_
27. endpoint `PUT /api/ai/analysis_prompt` برای کاربر ادمین با موفقیت پرامپت را به‌روزرسانی می‌کند (کد وضعیت 200) و محتوای به‌روزرسانی شده را برمی‌گرداند. _(verify: api_response)_
28. پس از به‌روزرسانی پرامپت توسط ادمین، فراخوانی مجدد `GET /api/ai/analysis_prompt` محتوای به‌روزرسانی شده را برمی‌گرداند که نشان‌دهنده ذخیره صحیح در پایگاه داده است. _(verify: api_response)_
29. فایل `app/services/ai/ai_data_access_service.py` ایجاد شده و شامل توابعی برای بازیابی داده‌های `Task`, `Project`, `TodoItem` و `Notification` برای یک `user_id` مشخص است. _(verify: static)_
30. یک endpoint جدید `GET /api/ai/user_data_context` در `app/routes/ai.py` تعریف شده که نیاز به احراز هویت دارد و داده‌های متنی کاربر را برمی‌گرداند. _(verify: api_response)_
31. داده‌های بازگشتی از `GET /api/ai/user_data_context` فقط شامل اطلاعات مربوط به کاربر احراز هویت شده است و هیچ داده‌ای از کاربران دیگر را افشا نمی‌کند. _(verify: backend_test)_
32. شمای Pydantic `AIContextResponse` و `AIContextItem` در `app/schemas/ai_schema.py` تعریف شده‌اند که ساختار داده‌های بازگشتی برای AI را مشخص می‌کنند. _(verify: static)_
33. سرویس `app/services/ai_service.py` قابلیت استفاده از داده‌های 'context' فراهم شده توسط `ai_data_access_service` را در توابع تولید/پرس‌وجوی AI دارد. _(verify: static)_
34. یک endpoint جدید `POST /api/ai/analyze` در `app/routes/ai.py` تعریف شده باشد که `AIAnalysisRequest` را دریافت و `AIAnalysisResult` را برگرداند. _(verify: static)_
35. متد `orchestrate_analysis` در `app/services/ai_service.py` پیاده‌سازی شده باشد که پرامپت، داده‌های مرتبط را دریافت و مدل AI را فراخوانی کند. _(verify: static)_
36. ارسال یک درخواست `POST` به `/api/ai/analyze` با `model_id` و `prompt` معتبر، پاسخ `200 OK` با یک `AIAnalysisResult` معتبر را برگرداند. _(verify: api_response)_
37. اگر `FEATURE_AI_ENABLED` در تنظیمات `app/core/config.py` غیرفعال باشد، endpoint `/api/ai/analyze` باید `403 Forbidden` برگرداند. _(verify: api_response)_
38. تست‌های واحد برای متد `orchestrate_analysis` در `tests/test_ai_service.py` اضافه شده باشد که سناریوهای مختلف (انتخاب مدل، بازیابی داده، فراخوانی API) را پوشش دهد. _(verify: backend_test)_
39. فایل جدید `frontend/src/pages/Settings.jsx` ایجاد شده و یک کامپوننت React را export می‌کند. _(verify: static)_
40. صفحه تنظیمات از طریق URL `/settings` قابل دسترسی است و در داخل کامپوننت `Layout` (شامل Header، Sidebar و Footer) رندر می‌شود. _(verify: ui_interaction)_
41. صفحه `Settings` شامل بخش‌های نگهدارنده مجزا برای «مدیریت ارائه‌دهندگان AI»، «مدیریت مدل‌های AI» و «جعبه پرامپت تحلیل» است. _(verify: ui_interaction)_
42. یک لینک ناوبری به صفحه «تنظیمات» در کامپوننت `Sidebar` وجود دارد. _(verify: ui_interaction)_
43. فایل `frontend/src/App.jsx` شامل مسیر جدید برای `/settings` است. _(verify: static)_
44. کاربر می‌تواند به صفحه جدید تنظیمات ارائه‌دهندگان هوش مصنوعی از طریق مسیر `/settings` دسترسی پیدا کند. _(verify: ui_interaction)_
45. صفحه تنظیمات باید لیستی از ارائه‌دهندگان هوش مصنوعی موجود را نمایش دهد (با فراخوانی `GET /api/ai/configs`). _(verify: ui_interaction)_
46. کاربر می‌تواند از طریق یک فرم در صفحه تنظیمات، ارائه‌دهنده هوش مصنوعی جدیدی را اضافه کند (با فراخوانی `POST /api/ai/configs`). _(verify: ui_interaction)_
47. کاربر می‌تواند جزئیات یک ارائه‌دهنده هوش مصنوعی موجود را ویرایش کند (با فراخوانی `PATCH /api/ai/configs/{config_id}`). _(verify: ui_interaction)_
48. کاربر می‌تواند یک ارائه‌دهنده هوش مصنوعی را از لیست حذف کند (با فراخوانی `DELETE /api/ai/configs/{config_id}`). _(verify: ui_interaction)_
49. یک لینک به صفحه تنظیمات ارائه‌دهندگان هوش مصنوعی در نوار کناری (Sidebar) یا سربرگ (Header) فرانت‌اند وجود دارد. _(verify: ui_interaction)_
50. مسیر `/settings/ai-models` در فرانت‌اند قابل دسترسی باشد و صفحه مدیریت مدل‌های AI را نمایش دهد. _(verify: ui_interaction)_
51. صفحه مدیریت مدل‌ها، لیستی از مدل‌های AI موجود را از endpoint `/api/ai/configs` دریافت و نمایش دهد. این لیست باید شامل فیلدهای `id`, `name`, و `provider` باشد. _(verify: api_response)_
52. کاربر بتواند از طریق فرم، یک مدل AI جدید اضافه کند که با `POST` به `/api/ai/configs` ارسال شود و پس از موفقیت، مدل جدید در لیست نمایش داده شود. _(verify: ui_interaction)_
53. کاربر بتواند یک مدل AI موجود را ویرایش کند که با `PATCH` به `/api/ai/configs/{id}` ارسال شود و تغییرات در لیست منعکس شود. _(verify: ui_interaction)_
54. کاربر بتواند یک مدل AI موجود را حذف کند که با `DELETE` به `/api/ai/configs/{id}` ارسال شود و از لیست حذف شود. _(verify: ui_interaction)_
55. فیلدهای فرم افزودن/ویرایش مدل شامل گزینه‌ای برای انتخاب ارائه‌دهنده (provider) باشد که از لیست ارائه‌دهندگان موجود پر شود. _(verify: static)_
56. جدول `global_settings` با ستون‌های `id`, `key` (UNIQUE), `value` در پایگاه داده ایجاد شود. _(verify: static)_
57. endpoint `GET /api/settings/global-analysis-prompt` برای ادمین، پرامپت تحلیل جهانی را برمی‌گرداند (یا یک مقدار پیش‌فرض اگر وجود نداشته باشد). _(verify: api_response)_
58. endpoint `PUT /api/settings/global-analysis-prompt` برای ادمین، پرامپت تحلیل جهانی را به‌روزرسانی می‌کند. _(verify: api_response)_
59. دسترسی به endpointهای `global-analysis-prompt` برای کاربران غیرادمین با خطای 403 Forbidden مواجه شود. _(verify: api_response)_
60. یک لینک 'Settings' در `frontend/src/components/Sidebar.jsx` به مسیر `/settings` اضافه شود. _(verify: static)_
61. صفحه `/settings` در فرانت‌اند یک `textarea` نمایش دهد که با مقدار فعلی پرامپت تحلیل جهانی پر شده است. _(verify: ui_interaction)_
62. دکمه 'ذخیره' در صفحه تنظیمات، تغییرات را به بک‌اند ارسال کرده و پرامپت را به‌روزرسانی کند. _(verify: ui_interaction)_
63. دکمه 'لغو' در صفحه تنظیمات، تغییرات محلی را نادیده گرفته و مقدار اصلی پرامپت را بازیابی کند. _(verify: ui_interaction)_
64. فایل `app/services/event_publisher.py` و تابع `publish_data_change_event` در آن وجود داشته باشد. _(verify: static)_
65. وظیفه Celery به نام `process_ai_ingestion_event` در `app/tasks.py` تعریف شده باشد که `entity_type`, `entity_id`, و `action` را به‌عنوان آرگومان می‌پذیرد. _(verify: static)_
66. پس از ایجاد یک `TodoItem` جدید (مثلاً از طریق `app/routes/todo_items.py` که `todo_item_service` را فراخوانی می‌کند)، یک وظیفه `process_ai_ingestion_event` با `entity_type='todo_item'`, `entity_id=<new_item_id>`, `action='created'` در صف Celery قرار گیرد. _(verify: backend_test)_
67. سرویس `app/services/ai_ingestion_service.py` بتواند یک `TodoItem` را با `ID` بازیابی کرده و محتوای متنی آن (مانند `description`) را به `app/services/ai/nlp_service.py` برای پردازش ارسال کند. _(verify: backend_test)_
68. تابع `analyze_content` در `app/services/ai/nlp_service.py` بتواند محتوای متنی را از انواع مختلف موجودیت‌ها پردازش کرده و یک نتیجه تحلیل (حداقل یک دیکشنری با فیلدهای `summary` و `keywords`) برگرداند. _(verify: static)_

## Task Steps

### Step 1: ایجاد صفحه تنظیمات اصلی با بخش مدیریت Providerها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک صفحه تنظیمات جدید در سیستم است که در آن کاربر بتواند لیست Providerهای هوش مصنوعی (مانند DeepSeek، GPT، Gemini، Claude، Grok، Perplexity و چند مورد دیگر) را مدیریت کند. این صفحه باید امکان افزودن Provider جدید، ویرایش نام Provider و حذف Provider را فراهم کند. خارج از این مرحله: اتصال به APIهای واقعی، مدیریت مدل‌های هر Provider، و بخش پرامپت. نکته حیاتی: این صفحه باید به‌صورت پویا عمل کند و Providerها را از یک منبع داده (مانند دیتابیس یا فایل کانفیگ) بارگذاری کند.
**Excerpt:**
```
یه صفحه تنظیمات باید توی سیستم باشه که تو اون مدل‌ها رو هم یعنی پرو پرووایدرا رو بتونم قرار بدم مثل دیپ سیک، جی پی تی، جیمنای، کلاد، گراک، پلکسیتی همچین چیزی و چند تای دیگه و اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

### Step 2: افزودن قابلیت مدیریت مدل‌های هر Provider (Mapping و Add)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت به صفحه تنظیمات است که در آن کاربر بتواند برای هر Provider، مدل‌های مختلف را تعریف کند (Mapping). کاربر باید بتواند مدل جدید اضافه کند، نام مدل را مشخص کند، و مدل را به Provider مربوطه متصل کند. خارج از این مرحله: اتصال به APIهای واقعی، بخش پرامپت، و دسترسی به صفحات دیگر. نکته حیاتی: هر مدل باید به یک Provider خاص متصل باشد و این ارتباط باید در دیتابیس ذخیره شود.
**Excerpt:**
```
اونجا بتونم مدل‌های هر کدوم از این پرووایدرا رو مپینگ کنم و اد کنم
```

### Step 3: ایجاد بخش پرامپت قابل ویرایش در صفحه تنظیمات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن یک جعبه متن (Textarea) در صفحه تنظیمات است که کاربر بتواند یک پرامپت سفارشی برای آنالیز بنویسد. این پرامپت باید همیشه قابل ویرایش باشد و تغییرات آن بلافاصله ذخیره شود. خارج از این مرحله: اجرای آنالیز، اتصال به مدل‌ها، و دسترسی به داده‌های صفحات دیگر. نکته حیاتی: پرامپت باید در دیتابیس یا فایل کانفیگ ذخیره شود و پس از هر بار ویرایش، به‌روزرسانی شود.
**Excerpt:**
```
یه جعبه‌ای باشه اونجا توضیحات یعنی یه پرامپتی براش بنویسم که این پرامپت هم همیشه بتونه قابل ویرایش باشه
```

### Step 4: ایجاد سرویس دسترسی مدل‌ها به داده‌های صفحات دیگر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سرویس یا API است که به مدل‌های تعریف‌شده اجازه می‌دهد به داده‌های موجود در صفحات دیگر سیستم دسترسی داشته باشند. این سرویس باید بتواند داده‌های تمام صفحات را جمع‌آوری کرده و در قالبی قابل استفاده برای مدل‌ها (مانند JSON) ارائه دهد. خارج از این مرحله: اجرای واقعی آنالیز توسط مدل‌ها، و نمایش نتایج. نکته حیاتی: این سرویس باید به‌صورت پویا با هر صفحه جدیدی که اضافه می‌شود، به‌روزرسانی شود.
**Excerpt:**
```
اونها بتونن به صفحات و داده‌های داخل هر صفحه دسترسی داشته باشن و کار آنالیز رو به شکلی که میگم انجام بدن
```

### Step 5: ایجاد قابلیت آنالیز خودکار با استفاده از پرامپت و داده‌های صفحات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک قابلیت است که با استفاده از پرامپت تعریف‌شده توسط کاربر و داده‌های جمع‌آوری‌شده از صفحات، یک آنالیز خودکار انجام دهد. این قابلیت باید مدل انتخاب‌شده را فراخوانی کند، پرامپت و داده‌ها را به آن ارسال کند، و نتیجه را نمایش دهد. خارج از این مرحله: مدیریت خطاهای API، و بهینه‌سازی. نکته حیاتی: این قابلیت باید از Provider و مدل انتخاب‌شده توسط کاربر استفاده کند.
**Excerpt:**
```
حسب این پرامپت و این دستورات که هر سری من بهش بدم اونها کار آنالیز رو نسبت به داده‌هایی که توی صفحات دیگه هستن انجام بدن
```

### Step 6: ایجاد قابلیت به‌روزرسانی خودکار دسترسی به داده‌ها هنگام افزودن صفحات جدید
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک مکانیزم است که به‌محض افزودن یک صفحه جدید به سیستم، داده‌های آن صفحه به‌طور خودکار در دسترس سرویس آنالیز قرار گیرد. این می‌تواند از طریق hooks, events, یا polling انجام شود. خارج از این مرحله: مدیریت صفحات موجود، و بهینه‌سازی. نکته حیاتی: این قابلیت باید بدون نیاز به تنظیمات دستی کار کند.
**Excerpt:**
```
هر صفحه و هر داده‌ای هم که اضافه میشه اینها باید سریع دسترسی بهش داشته باشن تا تو آنالیز دخالتش بدن
```
