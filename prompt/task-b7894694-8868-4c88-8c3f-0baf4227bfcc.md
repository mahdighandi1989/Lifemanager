---
task_id: b7894694-8868-4c88-8c3f-0baf4227bfcc
title: افزودن تست‌های integration برای سرویس‌های اصلی
type: feature_request
priority: high
execution_priority: 2300
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-19T20:23:24.923615+00:00'
updated_at: '2026-05-26T22:57:04.242568+00:00'
target_files:
- tests/conftest.py
---

# افزودن تست‌های integration برای سرویس‌های اصلی

## Raw Idea

در فایل tests/، تست‌های unit برای routeها وجود دارد اما تست‌های integration برای سرویس‌های اصلی مانند ai_service و auth_service وجود ندارد. این باعث می‌شود که خطاهای integration بین سرویس‌ها و دیتابیس شناسایی نشوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
عدم وجود تست‌های integration برای سرویس‌های اصلی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `tests/conftest.py:1-30` — `test fixtures` — این fixture برای تست‌های integration مناسب نیست
  ```python
  import pytest
  from app.database import get_db
  
  @pytest.fixture
  def db_session():
      db = get_db()
      yield db
      db.close()
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
pytest + SQLAlchemy + FastAPI TestClient

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `tests/test_ai.py` (سطر 1) — تست‌های unit برای AI route
- `tests/test_auth.py` (سطر 1) — تست‌های unit برای Auth route
- `app/services/ai_service.py` (سطر 1) — سرویسی که باید تست شود
- `app/services/auth_service.py` (سطر 1) — سرویسی که باید تست شود

## 🌐 نقشهٔ وابستگی‌ها
این تغییر بر تمام سرویس‌هایی که نیاز به تست integration دارند تأثیر می‌گذارد

## 🔍 Context و وضعیت فعلی
در فایل tests/، تست‌های unit برای routeها وجود دارد اما تست‌های integration برای سرویس‌های اصلی مانند ai_service و auth_service وجود ندارد. این باعث می‌شود که خطاهای integration بین سرویس‌ها و دیتابیس شناسایی نشوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست‌های integration برای AIService و AuthService اضافه شود
- [ ] تست‌ها از test database استفاده کنند
- [ ] تست‌ها به صورت isolated اجرا شوند
- [ ] پوشش تستی حداقل 80% برای سرویس‌های اصلی
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. اضافه کردن تست‌های integration با استفاده از pytest و test database

## 💡 نمونه‌های قبل/بعد
**اضافه کردن تست integration برای AuthService**

_قبل:_
```
# No integration test exists
```

_بعد:_
```
async def test_auth_service_login_integration(db_session):
    # Create test user
    user = User(email="test@test.com", username="test", hashed_password="hashed")
    db_session.add(user)
    db_session.commit()
    
    # Test login
    auth_service = AuthService(db=db_session)
    result = await auth_service.login("test@test.com", "password")
    assert result is not None
    assert result.email == "test@test.com"
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_integration.py -v`
- `coverage run -m pytest tests/ && coverage report`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به setup و teardown مناسب برای test database

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. تست‌های integration برای AIService و AuthService اضافه شود _(verify: static)_
2. تست‌ها از test database استفاده کنند _(verify: static)_
3. تست‌ها به صورت isolated اجرا شوند _(verify: backend_test)_
4. پوشش تستی حداقل 80% برای سرویس‌های اصلی _(verify: manual_only)_
