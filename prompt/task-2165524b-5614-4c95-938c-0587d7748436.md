---
task_id: 2165524b-5614-4c95-938c-0587d7748436
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (15 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:34:33.086107+00:00'
updated_at: '2026-05-29T09:29:42.697957+00:00'
target_files:
- backend/app/models/task.py
- backend/app/services/task_service.py
- backend/app/api/routes/tasks.py
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (15 مرحله)

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_407525_AgADlR0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 4 segment استخراج شد • 11,189 char متن_

## اهمیت و گستردگی هوش مصنوعی در زندگی روزمره  _(at: audio/ogg)_

### اهمیت و گستردگی هوش مصنوعی در زندگی روزمره

[00:00] می‌خوام ذیل یکی از همین، اِ، موردها، آیتم‌های لیست‌ها، حالا به صورت جداگانه یا هر جور که اقتضا می‌کنه، یه موردی اضافه بشه که مثلاً همین چیزایی که من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه، این وقتی ثبت میشه، این از این طرفی هم برنامه هم به گوگل مپ متصل باشه و هم به لوکیشن‌ام.
[00:30] و هم به اون چیزایی که حالا آیتم‌هایی که قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه. و حسب اون شرایطی که پیش میاد، یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه، تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی. یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره.
[01:00] این‌ها اینو باید چیز کنه. یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً بخرم مثل اپل واچ یا چیزای دیگه که بتونه مثلاً نبضمو بگیره، شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه و برنامه حسب وضعیتم مثلاً میگه که تو تو این وضعیت چیز هستی، مناسبه که این کارو انجام بدی.
[01:30] نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا. مثلاً میگه که با توجه به این نبضی که میزنی فلان میزنی نیاز داری مثلاً این برنامه رو اجرا بکنی مناسب حالتت هست. مثلاً این فیلم الان مناسب مثلاً شرایط جسمیت هست، آدرنالین خونتو مثلاً بیاره بالا چون ضربان قلبت مثلاً اینجوریه. یه همچین حالتی. یعنی مناسب شرایط روحیم. چون بالاخره این توی جسمم خودش اثر میذاره دیگه. نبضی که میزنه و فلان و این چیزها.
[02:00] برنامه حسب این مثلاً منو چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا چند ساعت آنالیز می‌کنه و شرایطو میگه یا مثلاً برنامه براش باید این قسمت تعریف شده باشه که مثلاً وقتی ببینه من مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم، حوصله‌ام سر رفته. یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم بتونه ضبط بکنه، بفهمه من توی چه شرایطی هستم. مثلاً میگه تو این شرایط چون تو اوقات الان بیکاریت هست، حوصله‌ات سر رفته، فلان کارو انجام بده.
[02:30] و هم لوکیشن هم ببینه، ببینه مثلاً سر کارم، خونه‌ام، کجام. بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصله‌ات سر رفته، فلان کاری که گذاشتی تو یه نوبت و اینو باید انجام بدی. یعنی به این شکل می‌تونه خیلی دقیق همه چیزو آنالیز کنه و همه دسترسی‌ها رو داشته باشه و البته باید یه سری چیزام هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعت‌هایی که همه چیزو آنالیز می‌کنن و اینها رو به داده‌ها رو مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همراهم باشه و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه شرایطو مثلاً همه چیزو بسنجه و از تو لیست برام مواردی رو انتخاب کنه و اعلام بکنه.

## کاربردها و مزایای کلیدی هوش مصنوعی  _(at: audio/ogg)_

**HEADING: کاربردها و مزایای کلیدی هوش مصنوعی**

[00:00] گوینده ۱: می‌خوام ذیل یکی از همین موردها، آیتم‌های لیست‌ها، حالا به صورت جداگانه یا هر جور که اقتضا می‌کنه،
[00:13] گوینده ۱: یه موردی اضافه بشه که مثلاً همین چیزایی که من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه، این وقتی ثبت می‌شه، این از این طرفی هم برنامه هم به گوگل مپ متصل باشه و هم به لوکیشن‌ام و هم به اون چیزایی که حالا آیتم‌هایی که قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه.
[00:39] گوینده ۱: و حسب اون شرایطی که پیش میاد، یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه، تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی.
[00:53] گوینده ۱: یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره، این‌ها اینو باید چیز کنه. یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً بخرم مثل اپل واچ یا چیزای دیگه که بتونه مثلاً نبضمو بگیره، شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه و برنامه حسب وضعیتم
[01:18] گوینده ۱: مثلاً میگه که تو تو این وضعیت چیز هستی، مناسبه که این کارو انجام بدی. نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا. مثلاً میگه که با توجه به این نبضی که می‌زنی فلان می‌زنی، نیاز داری مثلاً این برنامه رو اجرا بکنی، مناسب حالتت هست. مثلاً این فیلم الان مناسب مثلاً شرایط جسمیت هست، آدرنالین خونتو مثلاً بیاره بالا، چون ضربان قلبت مثلاً اینجوریه.
[01:41] گوینده ۱: یه همچین حالتی. یعنی مناسب شرایط روحیم. چون بالاخره این توی جسمم خودش اثر می‌ذاره دیگه. نبضی که می‌زنه و فلان و این چیزها،
[01:52] گوینده ۱: برنامه حسب این مثلاً منو چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا چند ساعت آنالیز می‌کنه و شرایطو میگه یا مثلاً برنامه براش باید این قسمت تعبیه شده باشه که مثلاً وقتی ببینه من مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم، مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم، حوصله‌ام سر رفته.
[02:17] گوینده ۱: یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم بتونه ضبط بکنه، بفهمه من توی چه شرایطی هستم. مثلاً میگه تو این شرایط چون تو اوقات الان بیکاریت هست، حوصله‌ات سر رفته، فلان کارو انجام بده.
[02:33] گوینده ۱: و هم لوکیشن هم ببینه، ببینه مثلاً سر کارم، خونه‌ام، کجام. بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصله‌ات سر رفته، فلان کاری که گذاشتی تو نوبت و اینو باید انجام بدی.
[02:46] گوینده ۱: یعنی به این شکل می‌تونه خیلی دقیق همه چیزو آنالیز کنه و همه دسترسی‌ها رو داشته باشه و البته باید یه سری چیزام هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعت‌هایی که همه چیزو آنالیز می‌کنن و این‌ها رو به داده‌ها مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همرام باشه و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه
[03:08] گوینده ۱: شرایطو مثلاً همه چیزو بسنجه و از تو لیست برام مواردی انتخاب کنه و اعلام بکنه.

## چالش‌ها و ملاحظات اخلاقی هوش مصنوعی  _(at: audio/ogg)_

### چالش‌ها و ملاحظات اخلاقی هوش مصنوعی

[00:00] می‌خوام ذیل یکی از همین
[00:03] موردها، آیتم‌های لیست‌ها
[00:07] حالا به صورت جداگانه یا
[00:09] هر جور که
[00:11] اقتضا می‌کنه
[00:13] یه موردی اضافه بشه که مثلاً همین چیزایی که
[00:16] من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه
[00:21] این
[00:23] وقتی ثبت میشه
[00:25] این از این طرفی هم برنامه هم به
[00:29] گوگل مپ متصل باشه
[00:31] و هم به لوکیشن‌ام
[00:33] و هم به اون چیزایی که حالا آیتم‌هایی که
[00:36] قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه
[00:39] و حسب اون
[00:43] شرایطی که پیش میاد
[00:45] یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه
[00:49] تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی
[00:54] یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره
[00:58] این‌ها
[00:59] اینو باید چیز کنه
[01:01] یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً
[01:05] بخرم مثل اپل واچ یا چیزای دیگه که
[01:07] بتونه مثلاً
[01:09] نبضمو بگیره
[01:10] شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه
[01:15] و برنامه حسب وضعیتم
[01:18] مثلاً
[01:19] میگه که تو تو این وضعیت چیز هستی
[01:21] مناسبه که این کارو انجام بدی. نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا
[01:26] مثلاً میگه که با توجه به این نبضی که میزنی فلان میزنی این نیاز داری مثلاً این برنامه رو اجرا بکنی مناسب حالتت هست
[01:33] مثلاً این فیلم الان مناسب مثلاً
[01:35] شرایط جسمیت هست آدرنالین خونتو مثلاً بیاره بالا
[01:39] چون ضربان قلبت مثلاً اینجوریه
[01:41] یه همچین حالتی
[01:43] یعنی مناسب شرایط روحیم
[01:45] چون بالاخره این توی جسمم خودش اثر میذاره دیگه
[01:48] نبضی که میزنه و فلان و این چیزها
[01:52] برنامه حسب این مثلاً منو
[01:54] چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا
[01:58] چند ساعت آنالیز می‌کنه و
[02:01] شرایطو میگه یا مثلاً برنامه
[02:03] براش باید این قسمت تعبیه شده باشه
[02:06] که مثلاً وقتی ببینه من
[02:08] مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم
[02:15] حوصله‌ام سر رفته
[02:17] یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم
[02:21] بتونه ضبط بکنه بفهمه من
[02:24] توی چه شرایطی هستم مثلاً میگه تو این شرایط
[02:27] چون تو اوقات الان بیکاریت هست حوصله‌ات سر رفته
[02:31] فلان کارو انجام بده
[02:33] و
[02:34] هم لوکیشن هم ببینه ببینه مثلاً سر کارم خونم کجام
[02:38] بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصله‌ات سر رفته
[02:42] فلان کاری که گذاشتی تو نوبت و اینو باید انجام بدی
[02:46] یعنی به این شکل می‌تونه خیلی دقیق همه چیزو آنالیز کنه و همه دسترسی‌ها رو داشته باشه و
[02:52] البته باید یه سری
[02:54] چیزام هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعتایی که
[02:58] همه چیزو آنالیز می‌کنن و
[03:00] این‌ها رو به داده‌ها مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همرام باشه
[03:05] و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه
[03:08] شرایطو مثلاً همه چیزو بسنجه و
[03:11] از تو لیست برام
[03:14] مواردی انتخاب کنه و اعلام بکنه.

## آینده هوش مصنوعی و ضرورت آمادگی  _(at: audio/ogg)_

در اینجا رونویسی کامل و عین به عین (verbatim) از فایل صوتی ارائه شده است:

---

[00:00] می‌خوام ذیل یکی از همین
[00:03] موردها، آیتم‌های لیست‌ها
[00:06] حالا به صورت جداگانه یا
[00:09] هر جور که
[00:11] اقتضا می‌کنه
[00:13] یه موردی اضافه بشه که مثلاً همین چیزایی که
[00:16] من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه
[00:21] این
[00:23] وقتی ثبت میشه
[00:25] این از این طرفی هم برنامه هم به
[00:29] گوگل مپ متصل باشه
[00:31] و هم به لوکیشن‌ام
[00:33] و هم به اون چیزایی که حالا آیتم‌هایی که
[00:36] قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه.
[00:39] و حسب اون
[00:43] شرایطی که پیش میاد
[00:45] یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه
[00:49] تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی.
[00:54] یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره.
[00:58] این‌ها اینو باید چیز کنه.
[01:01] یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً
[01:04] بخرم مثل اپل واچ یا چیزای دیگه که
[01:07] بتونه مثلاً نبضمو بگیره،
[01:10] شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه.
[01:15] و برنامه حسب وضعیتم
[01:18] مثلاً میگه که تو تو این وضعیت چیز هستی،
[01:21] مناسبه که این کارو انجام بدی. نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا.
[01:26] مثلاً میگه که با توجه به این نبضی که میزنی فلان میزنی نیاز داری مثلاً این برنامه رو اجرا بکنی مناسب حالتت هست.
[01:33] مثلاً این فیلم الان مناسب مثلاً شرایط
[01:36] جسمیت هست، آدرنالین خونتو مثلاً بیاره بالا
[01:39] چون ضربان قلبت مثلاً اینجوریه.
[01:41] یه همچین حالتی.
[01:43] یعنی مناسب شرایط روحیم چون بالاخره این توی جسمم خودش اثر می‌ذاره دیگه.
[01:49] نبضی که میزنه و فلان و این چیزها
[01:52] برنامه حسب این مثلاً منو
[01:54] چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا
[01:58] چند ساعت آنالیز می‌کنه و
[02:01] شرایطو میگه یا مثلاً برنامه
[02:03] براش باید یه قسمتی تعریف شده باشه
[02:06] که مثلاً وقتی ببینه من
[02:08] مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم،
[02:16] حوصلم سر رفته.
[02:17] یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم
[02:22] بتونه ضبط بکنه بفهمه من
[02:24] تو چه شرایطی هستم. مثلاً میگه تو این شرایط
[02:27] چون تو اوقات الان بیکاریت هست حوصلم سر رفته
[02:31] فلان کارو انجام بده.
[02:33] و هم لوکیشن هم ببینه ببینه مثلاً سر کارم، خونم، کجام.
[02:38] بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصلم سر رفته
[02:42] فلان کاری که گذاشتین تو نوبت و اینو باید انجام بدی.
[02:46] یعنی به این شکل می‌تونه خیلی دقیق همه چیزو آنالیز کنه و همه دسترسی‌ها رو داشته باشه و
[02:52] البته باید یه سری
[02:54] چیزا هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعتایی که
[02:58] همه چیزو آنالیز می‌کنن و این‌ها رو به داده‌ها مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همرام باشه
[03:05] و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه
[03:08] شرایطو مثلاً همه چیزو بسنجه و
[03:11] از تو لیست برام
[03:14] مواردی انتخاب کنه و اعلام بکنه.

---

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند. بخش‌های بعدی توسط AI ساختار داده شده‌اند و ممکن است ناقص باشند — این متن مرجع اصلی است.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_407525_AgADlR0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 4 segment استخراج شد • 11,189 char متن_

## اهمیت و گستردگی هوش مصنوعی در زندگی روزمره  _(at: audio/ogg)_

### اهمیت و گستردگی هوش مصنوعی در زندگی روزمره

[00:00] می‌خوام ذیل یکی از همین، اِ، موردها، آیتم‌های لیست‌ها، حالا به صورت جداگانه یا هر جور که اقتضا می‌کنه، یه موردی اضافه بشه که مثلاً همین چیزایی که من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه، این وقتی ثبت میشه، این از این طرفی هم برنامه هم به گوگل مپ متصل باشه و هم به لوکیشن‌ام.
[00:30] و هم به اون چیزایی که حالا آیتم‌هایی که قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه. و حسب اون شرایطی که پیش میاد، یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه، تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی. یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره.
[01:00] این‌ها اینو باید چیز کنه. یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً بخرم مثل اپل واچ یا چیزای دیگه که بتونه مثلاً نبضمو بگیره، شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه و برنامه حسب وضعیتم مثلاً میگه که تو تو این وضعیت چیز هستی، مناسبه که این کارو انجام بدی.
[01:30] نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا. مثلاً میگه که با توجه به این نبضی که میزنی فلان میزنی نیاز داری مثلاً این برنامه رو اجرا بکنی مناسب حالتت هست. مثلاً این فیلم الان مناسب مثلاً شرایط جسمیت هست، آدرنالین خونتو مثلاً بیاره بالا چون ضربان قلبت مثلاً اینجوریه. یه همچین حالتی. یعنی مناسب شرایط روحیم. چون بالاخره این توی جسمم خودش اثر میذاره دیگه. نبضی که میزنه و فلان و این چیزها.
[02:00] برنامه حسب این مثلاً منو چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا چند ساعت آنالیز می‌کنه و شرایطو میگه یا مثلاً برنامه براش باید این قسمت تعریف شده باشه که مثلاً وقتی ببینه من مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم، حوصله‌ام سر رفته. یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم بتونه ضبط بکنه، بفهمه من توی چه شرایطی هستم. مثلاً میگه تو این شرایط چون تو اوقات الان بیکاریت هست، حوصله‌ات سر رفته، فلان کارو انجام بده.
[02:30] و هم لوکیشن هم ببینه، ببینه مثلاً سر کارم، خونه‌ام، کجام. بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصله‌ات سر رفته، فلان کاری که گذاشتی تو یه نوبت و اینو باید انجام بدی. یعنی به این شکل می‌تونه خیلی دقیق همه چیزو آنالیز کنه و همه دسترسی‌ها رو داشته باشه و البته باید یه سری چیزام هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعت‌هایی که همه چیزو آنالیز می‌کنن و اینها رو به داده‌ها رو مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همراهم باشه و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه شرایطو مثلاً همه چیزو بسنجه و از تو لیست برام مواردی رو انتخاب کنه و اعلام بکنه.

## کاربردها و مزایای کلیدی هوش مصنوعی  _(at: audio/ogg)_

**HEADING: کاربردها و مزایای کلیدی هوش مصنوعی**

[00:00] گوینده ۱: می‌خوام ذیل یکی از همین موردها، آیتم‌های لیست‌ها، حالا به صورت جداگانه یا هر جور که اقتضا می‌کنه،
[00:13] گوینده ۱: یه موردی اضافه بشه که مثلاً همین چیزایی که من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه، این وقتی ثبت می‌شه، این از این طرفی هم برنامه هم به گوگل مپ متصل باشه و هم به لوکیشن‌ام و هم به اون چیزایی که حالا آیتم‌هایی که قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه.
[00:39] گوینده ۱: و حسب اون شرایطی که پیش میاد، یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه، تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی.
[00:53] گوینده ۱: یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره، این‌ها اینو باید چیز کنه. یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً بخرم مثل اپل واچ یا چیزای دیگه که بتونه مثلاً نبضمو بگیره، شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه و برنامه حسب وضعیتم
[01:18] گوینده ۱: مثلاً میگه که تو تو این وضعیت چیز هستی، مناسبه که این کارو انجام بدی. نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا. مثلاً میگه که با توجه به این نبضی که می‌زنی فلان می‌زنی، نیاز داری مثلاً این برنامه رو اجرا بکنی، مناسب حالتت هست. مثلاً این فیلم الان مناسب مثلاً شرایط جسمیت هست، آدرنالین خونتو مثلاً بیاره بالا، چون ضربان قلبت مثلاً اینجوریه.
[01:41] گوینده ۱: یه همچین حالتی. یعنی مناسب شرایط روحیم. چون بالاخره این توی جسمم خودش اثر می‌ذاره دیگه. نبضی که می‌زنه و فلان و این چیزها،
[01:52] گوینده ۱: برنامه حسب این مثلاً منو چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا چند ساعت آنالیز می‌کنه و شرایطو میگه یا مثلاً برنامه براش باید این قسمت تعبیه شده باشه که مثلاً وقتی ببینه من مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم، مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم، حوصله‌ام سر رفته.
[02:17] گوینده ۱: یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم بتونه ضبط بکنه، بفهمه من توی چه شرایطی هستم. مثلاً میگه تو این شرایط چون تو اوقات الان بیکاریت هست، حوصله‌ات سر رفته، فلان کارو انجام بده.
[02:33] گوینده ۱: و هم لوکیشن هم ببینه، ببینه مثلاً سر کارم، خونه‌ام، کجام. بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصله‌ات سر رفته، فلان کاری که گذاشتی تو نوبت و اینو باید انجام بدی.
[02:46] گوینده ۱: یعنی به این شکل می‌تونه خیلی دقیق همه چیزو آنالیز کنه و همه دسترسی‌ها رو داشته باشه و البته باید یه سری چیزام هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعت‌هایی که همه چیزو آنالیز می‌کنن و این‌ها رو به داده‌ها مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همرام باشه و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه
[03:08] گوینده ۱: شرایطو مثلاً همه چیزو بسنجه و از تو لیست برام مواردی انتخاب کنه و اعلام بکنه.

## چالش‌ها و ملاحظات اخلاقی هوش مصنوعی  _(at: audio/ogg)_

### چالش‌ها و ملاحظات اخلاقی هوش مصنوعی

[00:00] می‌خوام ذیل یکی از همین
[00:03] موردها، آیتم‌های لیست‌ها
[00:07] حالا به صورت جداگانه یا
[00:09] هر جور که
[00:11] اقتضا می‌کنه
[00:13] یه موردی اضافه بشه که مثلاً همین چیزایی که
[00:16] من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه
[00:21] این
[00:23] وقتی ثبت میشه
[00:25] این از این طرفی هم برنامه هم به
[00:29] گوگل مپ متصل باشه
[00:31] و هم به لوکیشن‌ام
[00:33] و هم به اون چیزایی که حالا آیتم‌هایی که
[00:36] قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه
[00:39] و حسب اون
[00:43] شرایطی که پیش میاد
[00:45] یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه
[00:49] تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی
[00:54] یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره
[00:58] این‌ها
[00:59] اینو باید چیز کنه
[01:01] یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً
[01:05] بخرم مثل اپل واچ یا چیزای دیگه که
[01:07] بتونه مثلاً
[01:09] نبضمو بگیره
[01:10] شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه
[01:15] و برنامه حسب وضعیتم
[01:18] مثلاً
[01:19] میگه که تو تو این وضعیت چیز هستی
[01:21] مناسبه که این کارو انجام بدی. نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا
[01:26] مثلاً میگه که با توجه به این نبضی که میزنی فلان میزنی این نیاز داری مثلاً این برنامه رو اجرا بکنی مناسب حالتت هست
[01:33] مثلاً این فیلم الان مناسب مثلاً
[01:35] شرایط جسمیت هست آدرنالین خونتو مثلاً بیاره بالا
[01:39] چون ضربان قلبت مثلاً اینجوریه
[01:41] یه همچین حالتی
[01:43] یعنی مناسب شرایط روحیم
[01:45] چون بالاخره این توی جسمم خودش اثر میذاره دیگه
[01:48] نبضی که میزنه و فلان و این چیزها
[01:52] برنامه حسب این مثلاً منو
[01:54] چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا
[01:58] چند ساعت آنالیز می‌کنه و
[02:01] شرایطو میگه یا مثلاً برنامه
[02:03] براش باید این قسمت تعبیه شده باشه
[02:06] که مثلاً وقتی ببینه من
[02:08] مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم
[02:15] حوصله‌ام سر رفته
[02:17] یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم
[02:21] بتونه ضبط بکنه بفهمه من
[02:24] توی چه شرایطی هستم مثلاً میگه تو این شرایط
[02:27] چون تو اوقات الان بیکاریت هست حوصله‌ات سر رفته
[02:31] فلان کارو انجام بده
[02:33] و
[02:34] هم لوکیشن هم ببینه ببینه مثلاً سر کارم خونم کجام
[02:38] بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصله‌ات سر رفته
[02:42] فلان کاری که گذاشتی تو نوبت و اینو باید انجام بدی
[02:46] یعنی به این شکل می‌تونه خیلی دقیق همه چیزو آنالیز کنه و همه دسترسی‌ها رو داشته باشه و
[02:52] البته باید یه سری
[02:54] چیزام هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعتایی که
[02:58] همه چیزو آنالیز می‌کنن و
[03:00] این‌ها رو به داده‌ها مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همرام باشه
[03:05] و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه
[03:08] شرایطو مثلاً همه چیزو بسنجه و
[03:11] از تو لیست برام
[03:14] مواردی انتخاب کنه و اعلام بکنه.

## آینده هوش مصنوعی و ضرورت آمادگی  _(at: audio/ogg)_

در اینجا رونویسی کامل و عین به عین (verbatim) از فایل صوتی ارائه شده است:

---

[00:00] می‌خوام ذیل یکی از همین
[00:03] موردها، آیتم‌های لیست‌ها
[00:06] حالا به صورت جداگانه یا
[00:09] هر جور که
[00:11] اقتضا می‌کنه
[00:13] یه موردی اضافه بشه که مثلاً همین چیزایی که
[00:16] من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه
[00:21] این
[00:23] وقتی ثبت میشه
[00:25] این از این طرفی هم برنامه هم به
[00:29] گوگل مپ متصل باشه
[00:31] و هم به لوکیشن‌ام
[00:33] و هم به اون چیزایی که حالا آیتم‌هایی که
[00:36] قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه.
[00:39] و حسب اون
[00:43] شرایطی که پیش میاد
[00:45] یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه
[00:49] تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی.
[00:54] یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره.
[00:58] این‌ها اینو باید چیز کنه.
[01:01] یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً
[01:04] بخرم مثل اپل واچ یا چیزای دیگه که
[01:07] بتونه مثلاً نبضمو بگیره،
[01:10] شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه.
[01:15] و برنامه حسب وضعیتم
[01:18] مثلاً میگه که تو تو این وضعیت چیز هستی،
[01:21] مناسبه که این کارو انجام بدی. نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا.
[01:26] مثلاً میگه که با توجه به این نبضی که میزنی فلان میزنی نیاز داری مثلاً این برنامه رو اجرا بکنی مناسب حالتت هست.
[01:33] مثلاً این فیلم الان مناسب مثلاً شرایط
[01:36] جسمیت هست، آدرنالین خونتو مثلاً بیاره بالا
[01:39] چون ضربان قلبت مثلاً اینجوریه.
[01:41] یه همچین حالتی.
[01:43] یعنی مناسب شرایط روحیم چون بالاخره این توی جسمم خودش اثر می‌ذاره دیگه.
[01:49] نبضی که میزنه و فلان و این چیزها
[01:52] برنامه حسب این مثلاً منو
[01:54] چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا
[01:58] چند ساعت آنالیز می‌کنه و
[02:01] شرایطو میگه یا مثلاً برنامه
[02:03] براش باید یه قسمتی تعریف شده باشه
[02:06] که مثلاً وقتی ببینه من
[02:08] مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم،
[02:16] حوصلم سر رفته.
[02:17] یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم
[02:22] بتونه ضبط بکنه بفهمه من
[02:24] تو چه شرایطی هستم. مثلاً میگه تو این شرایط
[02:27] چون تو اوقات الان بیکاریت هست حوصلم سر رفته
[02:31] فلان کارو انجام بده.
[02:33] و هم لوکیشن هم ببینه ببینه مثلاً سر کارم، خونم، کجام.
[02:38] بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصلم سر رفته
[02:42] فلان کاری که گذاشتین تو نوبت و اینو باید انجام بدی.
[02:46] یعنی به این شکل می‌تونه خیلی دقیق همه چیزو آنالیز کنه و همه دسترسی‌ها رو داشته باشه و
[02:52] البته باید یه سری
[02:54] چیزا هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعتایی که
[02:58] همه چیزو آنالیز می‌کنن و این‌ها رو به داده‌ها مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همرام باشه
[03:05] و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه
[03:08] شرایطو مثلاً همه چیزو بسنجه و
[03:11] از تو لیست برام
[03:14] مواردی انتخاب کنه و اعلام بکنه.

---
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی سیستم پیشنهاددهنده هوشمند مبتنی بر موقعیت و زمینه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/models/task.py:1-50` — `class Task` — مدل فعلی Task دارای فیلد location است اما از آن برای پیشنهاددهی استفاده نمی‌شود. باید فیلدهای location_type (خرید/خدمات/اداری) و tags اضافه شود.
  ```python
  class Task(Base):
      __tablename__ = 'tasks'
      id = Column(Integer, primary_key=True, index=True)
      title = Column(String, nullable=False)
      description = Column(Text, nullable=True)
      status = Column(String, default='pending')
      due_date = Column(DateTime, nullable=True)
      location = Column(JSON, nullable=True)  # {lat, lng, address}
      category = Column(String, nullable=True)
      user_id = Column(Integer, ForeignKey('users.id'))
  ```
- `backend/app/services/task_service.py:1-80` — `class TaskService` — سرویس فعلی فقط CRUD پایه دارد. باید متدهای جستجوی پیشرفته بر اساس موقعیت و زمینه اضافه شود.
  ```python
  class TaskService:
      def __init__(self, db: Session):
          self.db = db
      
      def create_task(self, task_data: TaskCreate, user_id: int) -> Task:
          task = Task(**task_data.dict(), user_id=user_id)
          self.db.add(task)
          self.db.commit()
          self.db.refresh(task)
          return task
  ```
- `backend/app/api/routes/tasks.py:1-60` — `router` — API فعلی فیلترهای محدودی دارد. باید پارامترهای location, category, tags برای پشتیبانی از پیشنهاددهی اضافه شود.
  ```python
  @router.get('/tasks', response_model=List[TaskResponse])
  async def get_tasks(
      skip: int = 0,
      limit: int = 100,
      status: Optional[str] = None,
      db: Session = Depends(get_db),
      current_user: User = Depends(get_current_user)
  ):
      tasks = task_service.get_tasks(db, current_user.id, skip, limit, status)
      return tasks
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack فعلی: FastAPI (Python 3.10+), SQLAlchemy, PostgreSQL, Celery (برای وظایف زمان‌بندی), React 18, Vite, TypeScript. کتابخانه‌های جدید مورد نیاز:
- `googlemaps`: Python client for Google Maps API
- `websockets`: برای WebSocket در backend
- `pydub` + `speech_recognition`: برای پردازش صدا
- `httpx`: برای درخواست‌های async به APIهای خارجی
- `react-geolocated` یا `navigator.geolocation`: برای موقعیت‌یابی در فرانت‌اند
- `@react-google-maps/api`: برای نمایش نقشه در فرانت‌اند

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/models/user.py` (سطر 1) — مدل User باید فیلدهای context-related (last_location, last_heart_rate, activity_status) را دریافت کند یا از طریق رابطه با UserContext
- `backend/app/services/notification_service.py` (سطر 1) — سرویس اعلان‌ها باید برای ارسال پیشنهادات هوشمند به‌روزرسانی شود (نوع اعلان جدید: recommendation)
- `frontend/src/components/TaskList.tsx` (سطر 1) — کامپوننت لیست وظایف باید قابلیت نمایش وظایف پیشنهادی (با هایلایت) را داشته باشد
- `frontend/src/components/NotificationBell.tsx` (سطر 1) — کامپوننت اعلان‌ها باید نوع جدید recommendation را پشتیبانی کند و پیشنهادات را با آیکون متفاوت نمایش دهد
- `backend/app/core/config.py` (سطر 1) — باید متغیرهای محیطی جدید اضافه شود: GOOGLE_MAPS_API_KEY, HEALTHKIT_ENABLED, VOICE_ANALYSIS_API_KEY

## 🌐 نقشهٔ وابستگی‌ها
این تغییرات بزرگ، معماری پروژه را به شدت گسترش می‌دهد. وابستگی‌های اصلی:
1. **مدل‌های دیتابیس**: مدل‌های جدید UserContext, ContextualRecommendation به Alembic migrations نیاز دارند. مدل Task باید فیلدهای location_type و tags را دریافت کند.
2. **سرویس‌ها**: سرویس‌های جدید context_service, recommendation_engine, google_maps_service, wearable_integration, voice_analysis_service به سرویس‌های موجود (task_service, notification_service) وابسته هستند.
3. **API**: روترهای جدید context.py و ws/context.py به middleware احراز هویت وابسته هستند.
4. **فرانت‌اند**: کامپوننت‌های جدید LocationTracker, RecommendationPanel به کتابخانه‌های Google Maps JavaScript API و WebSocket وابسته هستند.
5. **وابستگی‌های خارجی**: Google Maps API, HealthKit/Google Fit API, Voice Analysis API (Google Cloud یا Azure)
6. **تغییرات در config**: متغیرهای محیطی جدید و مدیریت API keys

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی یک سیستم هوشمند را دارد که بر اساس داده‌های مختلف (موقعیت مکانی، زمان، وضعیت فیزیکی، فعالیت‌های اخیر) بتواند از لیست وظایف و خریدهای ثبت‌شده، آیتم‌های مرتبط را پیشنهاد دهد. این سیستم باید:

1. **ادغام با Google Maps و موقعیت مکانی**: برنامه باید به Google Maps متصل شود و موقعیت فعلی کاربر را تشخیص دهد. سپس بر اساس آیتم‌های ثبت‌شده (خرید، وظایف)، اگر کاربر نزدیک مکانی است که می‌تواند آن آیتم را تهیه کند یا ثبت‌نام کند، به او اعلان دهد.

2. **اتصال به دستگاه‌های پوشیدنی (مثل اپل واچ)**: کاربر می‌خواهد بتواند از ساعت هوشمند برای دریافت داده‌های فیزیولوژیکی (نبض، شرایط جسمی) استفاده کند. برنامه باید این داده‌ها را تحلیل کرده و بر اساس وضعیت فیزیکی و روحی، پیشنهادات مناسب بدهد (مثلاً فیلمی که آدرنالین را بالا می‌برد اگر ضربان قلب پایین است).

3. **تحلیل رفتار و فعالیت**: برنامه باید تشخیص دهد که کاربر چه مدت با هیچ برنامه‌ای کار نکرده یا تکان نخورده است تا حدس بزند که بیکار است یا حوصله‌اش سر رفته. سپس بر اساس آن، فعالیت‌های مناسب از لیست وظایف پیشنهاد دهد.

4. **تشخیص زمینه از طریق صدا**: کاربر می‌خواهد دسترسی ضبط صدا را بدهد تا برنامه بتواند شرایط روحی و محیطی او را تشخیص دهد.

5. **تحلیل جامع و پیشنهاد خودکار**: سیستم باید همه این داده‌ها (لوکیشن، زمان، وضعیت فیزیکی، فعالیت، صدا) را تلفیق کرده و به‌طور خودکار از لیست وظایف و خریدها، موارد مناسب را انتخاب و اعلام کند.

این درخواست بر اساس فایل صوتی پیوست (voice_407525_AgADlR0A.ogg) استخراج شده است. پروژه فعلی (Lifemanager) یک برنامه FastAPI + React برای مدیریت وظایف، پروژه‌ها، اعلان‌ها و برنامه‌ریزی با کمک هوش مصنوعی است. در حال حاضر، سیستم مدیریت وظایف پایه (CRUD) و اعلان‌های ساده وجود دارد، اما هیچ قابلیت پیشنهاددهنده هوشمند مبتنی بر زمینه (context-aware recommendation) پیاده‌سازی نشده است.

فایل‌های مرتبط فعلی:
- `backend/app/models/task.py`: مدل Task با فیلدهای title, description, status, due_date, location, category
- `backend/app/models/notification.py`: مدل Notification با فیلدهای message, type, user_id, read
- `backend/app/services/task_service.py`: سرویس CRUD وظایف
- `backend/app/services/notification_service.py`: سرویس اعلان‌ها
- `backend/app/api/routes/tasks.py`: API endpoints برای وظایف
- `backend/app/api/routes/notifications.py`: API endpoints برای اعلان‌ها
- `frontend/src/components/TaskList.tsx`: کامپوننت نمایش لیست وظایف
- `frontend/src/components/NotificationBell.tsx`: کامپوننت نمایش اعلان‌ها

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل UserContext در backend/app/models/context.py ایجاد شود و شامل فیلدهای user_id, current_location (JSON), last_activity_time, heart_rate, activity_status, mood باشد
- [ ] مدل ContextualRecommendation در backend/app/models/recommendation.py ایجاد شود و شامل فیلدهای user_id, task_id, recommendation_type, context_snapshot, generated_at, is_read باشد
- [ ] API endpoint POST /api/context/location موقعیت مکانی کاربر را دریافت و در دیتابیس ذخیره کند (response status 200)
- [ ] API endpoint GET /api/recommendations لیست پیشنهادات هوشمند را برای کاربر جاری برگرداند (response status 200 با آرایه‌ای از recommendations)
- [ ] کامپوننت RecommendationPanel در فرانت‌اند پیشنهادات هوشمند را در یک پنل اختصاصی نمایش دهد و هر پیشنهاد دکمه‌های قبول/رد داشته باشد
- [ ] کامپوننت LocationTracker موقعیت کاربر را هر 5 دقیقه به backend ارسال کند (با استفاده از Geolocation API)
- [ ] سرویس recommendation_engine تابع generate_contextual_recommendations را داشته باشد که همه انواع پیشنهاد (location-based, physiological, behavioral) را تلفیق کند
- [ ] Google Maps API یکپارچه شود و سرویس google_maps_service.py توابع geocode_address و find_nearby_places را داشته باشد
- [ ] اعلان‌های نوع recommendation در NotificationBell با آیکون متفاوت (مثلاً آیکون مکان‌نما) نمایش داده شوند
- [ ] صفحه /recommendations تاریخچه پیشنهادات را نمایش دهد و امکان تنظیم اولویت‌های پیشنهاددهی (فعال/غیرفعال کردن هر نوع) وجود داشته باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی این سیستم پیشنهاددهنده هوشمند، مراحل زیر پیشنهاد می‌شود:

**فاز 1: زیرساخت داده و مدل‌سازی (Backend)**
1. ایجاد مدل `UserContext` در `backend/app/models/context.py`:
   - فیلدهای: user_id, current_location (JSON: lat, lng, address), last_activity_time, heart_rate, activity_status (idle/active/busy), mood (happy/bored/stressed/neutral)
   - ارتباط با مدل User

2. ایجاد مدل `ContextualRecommendation` در `backend/app/models/recommendation.py`:
   - فیلدهای: user_id, task_id, recommendation_type (location_based/physiological/behavioral), context_snapshot (JSON), generated_at, is_read

3. ایجاد سرویس `context_service.py` در `backend/app/services/`:
   - تابع `update_user_location(user_id, lat, lng, address)`: به‌روزرسانی موقعیت مکانی کاربر
   - تابع `update_physiological_data(user_id, heart_rate, other_vitals)`: دریافت داده از دستگاه‌های پوشیدنی
   - تابع `detect_activity_status(user_id)`: تشخیص وضعیت فعالیت بر اساس آخرین تعاملات
   - تابع `analyze_mood_from_voice(audio_data)`: تحلیل صدا برای تشخیص خلق‌وخو

4. ایجاد سرویس `recommendation_engine.py` در `backend/app/services/`:
   - تابع `get_location_based_tasks(user_id, current_location)`: جستجوی وظایفی که با موقعیت فعلی همخوانی دارند
   - تابع `get_physiological_recommendations(user_id, heart_rate)`: پیشنهاد بر اساس وضعیت فیزیولوژیک
   - تابع `get_behavioral_recommendations(user_id, idle_duration)`: پیشنهاد بر اساس بیکاری
   - تابع `generate_contextual_recommendations(user_id)`: تابع اصلی که همه داده‌ها را تلفیق می‌کند

**فاز 2: API و یکپارچه‌سازی (Backend)**
5. ایجاد API endpoints در `backend/app/api/routes/context.py`:
   - `POST /api/context/location`: به‌روزرسانی موقعیت
   - `POST /api/context/physiological`: دریافت داده‌های فیزیولوژیک
   - `POST /api/context/voice`: آپلود و تحلیل صدا
   - `GET /api/recommendations`: دریافت پیشنهادات هوشمند

6. ایجاد WebSocket endpoint در `backend/app/api/ws/context.py`:
   - اتصال real-time برای ارسال پیشنهادات فوری

**فاز 3: فرانت‌اند (React)**
7. ایجاد کامپوننت `LocationTracker.tsx` در `frontend/src/components/`:
   - استفاده از Geolocation API مرورگر
   - ارسال موقعیت به backend هر 5 دقیقه

8. ایجاد کامپوننت `RecommendationPanel.tsx` در `frontend/src/components/`:
   - نمایش پیشنهادات هوشمند در یک پنل اختصاصی
   - قابلیت قبول/رد پیشنهاد

9. به‌روزرسانی `TaskForm.tsx` برای افزودن فیلد location:
   - امکان ثبت موقعیت مکانی مرتبط با وظیفه

10. ایجاد صفحه `/recommendations` در `frontend/src/pages/`:
    - نمایش تاریخچه پیشنهادات
    - تنظیمات اولویت‌های پیشنهاددهی

**فاز 4: یکپارچه‌سازی با Google Maps**
11. افزودن Google Maps API key به متغیرهای محیطی
12. ایجاد سرویس `google_maps_service.py` در `backend/app/services/`:
    - تابع `geocode_address(address)`: تبدیل آدرس به مختصات
    - تابع `find_nearby_places(lat, lng, radius, type)`: جستجوی مکان‌های نزدیک
    - تابع `calculate_distance(origin, destination)`: محاسبه فاصله

**فاز 5: اتصال به دستگاه‌های پوشیدنی**
13. ایجاد سرویس `wearable_integration.py` در `backend/app/services/`:
    - پشتیبانی از HealthKit (Apple) و Google Fit API
    - دریافت داده‌های ضربان قلب، گام‌ها، کالری

**فاز 6: تحلیل صدا**
14. ایجاد سرویس `voice_analysis_service.py` در `backend/app/services/`:
    - یکپارچه‌سازی با APIهای تشخیص خلق‌وخو از صدا (مثل Google Cloud Speech-to-Text + Natural Language API)
    - تشخیص لحن و احساسات

## 💡 نمونه‌های قبل/بعد
**افزودن فیلد location_type به مدل Task**

_قبل:_
```
location = Column(JSON, nullable=True)  # {lat, lng, address}
```

_بعد:_
```
location = Column(JSON, nullable=True)  # {lat, lng, address}
location_type = Column(String, nullable=True)  # 'shopping', 'service', 'administrative', 'entertainment'
tags = Column(ARRAY(String), nullable=True)  # ['urgent', 'work', 'personal']
```

**ایجاد سرویس recommendation_engine**

_قبل:_
```
# فایل جدید: backend/app/services/recommendation_engine.py
# (فعلاً وجود ندارد)
```

_بعد:_
```
class RecommendationEngine:
    def __init__(self, db: Session):
        self.db = db
        self.task_service = TaskService(db)
        self.context_service = ContextService(db)
    
    async def generate_contextual_recommendations(self, user_id: int) -> List[ContextualRecommendation]:
        context = await self.context_service.get_current_context(user_id)
        recommendations = []
        
        # 1. Location-based recommendations
        if context.current_location:
            nearby_tasks = await self.get_nearby_tasks(user_id, context.current_location)
            recommendations.extend(nearby_tasks)
        
        # 2. Physiological recommendations
        if context.heart_rate:
            physio_tasks = await self.get_physiological_tasks(user_id, context.heart_rate)
            recommendations.extend(physio_tasks)
        
        # 3. Behavioral recommendations (idle detection)
        if context.activity_status == 'idle':
            idle_tasks = await self.get_idle_tasks(user_id)
            recommendations.extend(idle_tasks)
        
        return recommendations
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_models/test_context.py -v`
- `pytest backend/tests/test_services/test_recommendation_engine.py -v`
- `pytest backend/tests/test_api/test_context_api.py -v`
- `npm run test -- --testPathPattern=RecommendationPanel`
- `npm run test -- --testPathPattern=LocationTracker`
- `python -m pytest backend/tests/ -k 'recommendation or context' --cov=backend/app/services`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **حریم خصوصی کاربران**: جمع‌آوری داده‌های موقعیت مکانی، فیزیولوژیک و صوتی نیازمند رعایت قوانین GDPR/حریم خصوصی است. باید صفحه تنظیمات حریم خصوصی و امکان حذف داده‌ها اضافه شود.
2. **مصرف باتری و منابع**: ارسال مداوم موقعیت مکانی (هر 5 دقیقه) و تحلیل صدا می‌تواند مصرف باتری موبایل را افزایش دهد. باید بهینه‌سازی مصرف انرژی در نظر گرفته شود.
3. **هزینه APIهای خارجی**: Google Maps API و Voice Analysis API هزینه‌بر هستند. باید محدودیت مصرف (rate limiting) و کش‌گذاری پیاده‌سازی شود.
4. **دقت تشخیص**: تشخیص خلق‌وخو از صدا و وضعیت فعالیت ممکن است خطا داشته باشد. باید مکانیزم بازخورد کاربر (تأیید/رد پیشنهاد) برای بهبود دقت در نظر گرفته شود.
5. **تغییرات گسترده در دیتابیس**: افزودن مدل‌های جدید نیازمند migrationهای Alembic است که ممکن است با داده‌های موجود تداخل داشته باشد. باید backup گرفته شود.
6. **وابستگی به سخت‌افزار**: قابلیت اتصال به دستگاه‌های پوشیدنی (اپل واچ) نیازمند SDKهای خاص و تست روی دستگاه واقعی است.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

## Acceptance Criteria

1. مدل UserContext در backend/app/models/context.py ایجاد شود و شامل فیلدهای user_id, current_location (JSON), last_activity_time, heart_rate, activity_status, mood باشد _(verify: static)_
2. مدل ContextualRecommendation در backend/app/models/recommendation.py ایجاد شود و شامل فیلدهای user_id, task_id, recommendation_type, context_snapshot, generated_at, is_read باشد _(verify: static)_
3. API endpoint POST /api/context/location موقعیت مکانی کاربر را دریافت و در دیتابیس ذخیره کند (response status 200) _(verify: api_response)_
4. API endpoint GET /api/recommendations لیست پیشنهادات هوشمند را برای کاربر جاری برگرداند (response status 200 با آرایه‌ای از recommendations) _(verify: api_response)_
5. کامپوننت RecommendationPanel در فرانت‌اند پیشنهادات هوشمند را در یک پنل اختصاصی نمایش دهد و هر پیشنهاد دکمه‌های قبول/رد داشته باشد _(verify: ui_interaction)_
6. کامپوننت LocationTracker موقعیت کاربر را هر 5 دقیقه به backend ارسال کند (با استفاده از Geolocation API) _(verify: static)_
7. سرویس recommendation_engine تابع generate_contextual_recommendations را داشته باشد که همه انواع پیشنهاد (location-based, physiological, behavioral) را تلفیق کند _(verify: static)_
8. Google Maps API یکپارچه شود و سرویس google_maps_service.py توابع geocode_address و find_nearby_places را داشته باشد _(verify: static)_
9. اعلان‌های نوع recommendation در NotificationBell با آیکون متفاوت (مثلاً آیکون مکان‌نما) نمایش داده شوند _(verify: ui_interaction)_
10. صفحه /recommendations تاریخچه پیشنهادات را نمایش دهد و امکان تنظیم اولویت‌های پیشنهاددهی (فعال/غیرفعال کردن هر نوع) وجود داشته باشد _(verify: ui_interaction)_

## Task Steps

### Step 1: پیاده‌سازی قابلیت افزودن و مدیریت آیتم‌های خرید و کارهای ضروری
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعهٔ یک سیستم پایه برای کاربران است تا بتوانند آیتم‌های مورد نیاز خود را، اعم از خریدهای آتی یا کارهایی که باید انجام شوند، ثبت و مدیریت کنند. این سیستم باید امکان افزودن، ویرایش و حذف آیتم‌ها را فراهم کند و به عنوان هستهٔ اولیهٔ داده‌های کاربر عمل نماید. تمرکز بر روی ساختار داده و APIهای داخلی برای مدیریت این آیتم‌ها است.
**Excerpt:**
```
[00:00] می‌خوام ذیل یکی از همین، اِ، موردها، آیتم‌های لیست‌ها، حالا به صورت جداگانه یا هر جور که اقتضا می‌کنه، یه موردی اضافه بشه که مثلاً همین چیزایی که من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه، این وقتی ثبت میشه
```

### Step 2: ادغام با سرویس موقعیت مکانی دستگاه کاربر
**Status:** `pending` (0%)
**Scope:** این مرحله بر روی اتصال برنامه به سرویس‌های موقعیت مکانی (GPS) دستگاه کاربر تمرکز دارد. هدف این است که برنامه بتواند به صورت مداوم یا در بازه‌های زمانی مشخص، موقعیت جغرافیایی فعلی کاربر را دریافت کند. این قابلیت برای فعال‌سازی ویژگی‌های مبتنی بر مکان ضروری است و نیازمند مدیریت دقیق مجوزهای دسترسی به موقعیت مکانی است.
**Excerpt:**
```
[00:25] این از این طرفی هم برنامه هم به گوگل مپ متصل باشه و هم به لوکیشن‌ام
```

### Step 3: ادغام با Google Maps برای نمایش و جستجوی مکان‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی اتصال برنامه به APIهای Google Maps است. هدف این است که برنامه بتواند مکان‌های مختلف را بر روی نقشه نمایش دهد، قابلیت جستجوی مکان‌ها را فراهم کند و اطلاعات مربوط به نقاط مورد علاقه (POI) را از Google Maps دریافت نماید. این ادغام برای ارائهٔ پیشنهادهای مبتنی بر مکان و راهنمایی کاربر ضروری است.
**Excerpt:**
```
[00:25] این از این طرفی هم برنامه هم به گوگل مپ متصل باشه و هم به لوکیشن‌ام
```

### Step 4: اتصال آیتم‌های ثبت‌شده به داده‌های موقعیت مکانی
**Status:** `pending` (0%)
**Scope:** این مرحله به ایجاد ارتباط بین آیتم‌های خرید و کارهای ثبت‌شده توسط کاربر (از مرحله ۱) و داده‌های موقعیت مکانی (از مراحل ۲ و ۳) می‌پردازد. هدف این است که هر آیتم بتواند با یک یا چند مکان جغرافیایی مرتبط شود، مثلاً یک خرید با یک فروشگاه خاص یا یک کار با یک آدرس مشخص. این ارتباط برای فعال‌سازی پیشنهادهای هوشمند مبتنی بر مکان حیاتی است.
**Excerpt:**
```
[00:33] و هم به اون چیزایی که حالا آیتم‌هایی که قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه.
```

### Step 5: پیاده‌سازی منطق پیشنهاددهی مبتنی بر نزدیکی به مکان‌های مرتبط
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعهٔ الگوریتمی است که با استفاده از موقعیت مکانی فعلی کاربر و مکان‌های مرتبط با آیتم‌های ثبت‌شده، پیشنهادهای هوشمند ارائه دهد. برنامه باید قادر باشد تشخیص دهد که کاربر در نزدیکی یک مکان مرتبط با یک آیتم (خرید یا کار) قرار دارد و سپس آن آیتم را به کاربر پیشنهاد دهد. این منطق باید شامل فیلتر کردن و اولویت‌بندی پیشنهادها باشد.
**Excerpt:**
```
[00:39] و حسب اون شرایطی که پیش میاد، یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه، تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی. یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره.
```

### Step 6: ادغام با دستگاه‌های پوشیدنی (مانند Apple Watch) برای دریافت داده‌های زیستی
**Status:** `pending` (0%)
**Scope:** این مرحله بر روی ایجاد اتصال و همگام‌سازی برنامه با دستگاه‌های پوشیدنی هوشمند مانند Apple Watch یا سایر ساعت‌های هوشمند تمرکز دارد. هدف این است که برنامه بتواند داده‌های زیستی کاربر، از جمله ضربان قلب (نبض) و سایر شرایط جسمی قابل اندازه‌گیری توسط این دستگاه‌ها را دریافت کند. این ادغام نیازمند مدیریت پروتکل‌های ارتباطی (مانند بلوتوث) و مجوزهای دسترسی به داده‌های سلامت است.
**Excerpt:**
```
[01:01] یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً بخرم مثل اپل واچ یا چیزای دیگه که بتونه مثلاً نبضمو بگیره، شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه
```

### Step 7: پیاده‌سازی منطق تحلیل داده‌های زیستی و پیشنهاد فعالیت‌های متناسب
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعهٔ الگوریتم‌هایی برای تحلیل داده‌های زیستی دریافتی از دستگاه‌های پوشیدنی (مانند ضربان قلب، سطح فعالیت) است. هدف این است که برنامه بتواند وضعیت جسمی و حتی روحی کاربر را بر اساس این داده‌ها تشخیص دهد و سپس فعالیت‌ها یا محتوای متناسب با آن وضعیت را پیشنهاد کند. این پیشنهادها می‌توانند شامل ورزش، فیلم، یا سایر برنامه‌ها باشند که به بهبود وضعیت کاربر کمک می‌کنند.
**Excerpt:**
```
[01:15] و برنامه حسب وضعیتم مثلاً میگه که تو تو این وضعیت چیز هستی، مناسبه که این کارو انجام بدی. نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا. مثلاً میگه که با توجه به این نبضی که میزنی فلان میزنی نیاز داری مثلاً این برنامه رو اجرا بکنی مناسب حالتت هست. مثلاً این فیلم الان مناسب مثلاً شرایط جسمیت هست، آدرنالین خونتو مثلاً بیاره بالا چون ضربان قلبت مثلاً اینجوریه. یه همچین حالتی. یعنی مناسب شرایط روحیم. چون بالاخره این توی جسمم خودش اثر میذاره دیگه. نبضی که میزنه و فلان و این چیزها.
```

### Step 8: پیاده‌سازی سیستم پایش و تحلیل فعالیت/عدم فعالیت کاربر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعهٔ مکانیزمی برای پایش مداوم فعالیت‌های کاربر در برنامه و خارج از آن است. برنامه باید قادر باشد میزان تعامل کاربر با دستگاه، حرکت فیزیکی (با استفاده از حسگرهای دستگاه) و مدت زمان عدم فعالیت را در بازه‌های زمانی مشخص (چند دقیقه یا چند ساعت) آنالیز کند. این پایش برای تشخیص الگوهای رفتاری و وضعیت‌های خاص کاربر ضروری است.
**Excerpt:**
```
[01:52] برنامه حسب این مثلاً منو چند حسب تنظیماتی که براش گذاشتم مثلاً چند دقیقه چند دقیقه یا چند ساعت آنالیز می‌کنه و شرایطو میگه یا مثلاً برنامه براش باید این قسمت تعریف شده باشه که مثلاً وقتی ببینه من مدتی‌تون با هیچ برنامه‌ای کار نکردم یا تکون نخوردم فلان نکردم
```

### Step 9: توسعهٔ منطق تشخیص بیکاری و بی‌حوصلگی کاربر
**Status:** `pending` (0%)
**Scope:** این مرحله بر پایهٔ داده‌های پایش فعالیت (از مرحله ۸) بنا شده و شامل توسعهٔ الگوریتم‌هایی برای تشخیص وضعیت‌های 'بیکاری' یا 'بی‌حوصلگی' کاربر است. برنامه باید بتواند بر اساس مدت زمان عدم فعالیت، عدم تعامل با برنامه‌ها یا سایر الگوهای رفتاری، حدس بزند که کاربر در حال حاضر بیکار یا بی‌حوصله است. این تشخیص برای ارائهٔ پیشنهادهای فعال و مرتبط حیاتی است.
**Excerpt:**
```
[02:08] مثلاً حدس بزنه که احتمالاً بیکارم یا کاری ندارم، حوصله‌ام سر رفته.
```

### Step 10: ادغام با قابلیت ضبط صدای دائم برای تحلیل وضعیت روحی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی قابلیت دسترسی به میکروفون دستگاه و ضبط مداوم صدای کاربر (با کسب مجوزهای لازم) است. هدف این است که برنامه بتواند با تحلیل الگوهای صوتی و گفتاری، وضعیت روحی و احساسی کاربر را تشخیص دهد. این قابلیت به برنامه کمک می‌کند تا درک عمیق‌تری از حال کاربر پیدا کند و پیشنهادهای دقیق‌تری ارائه دهد.
**Excerpt:**
```
[02:17] یا مثلاً دسترسی‌هایی که مثلاً ممکنه بهش بدم که صدامو دائم بتونه ضبط بکنه، بفهمه من توی چه شرایطی هستم.
```

### Step 11: پیاده‌سازی منطق پیشنهاددهی بر اساس ترکیب بیکاری و تحلیل صوتی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعهٔ منطقی است که پیشنهادهای فعال را بر اساس ترکیب تشخیص بیکاری/بی‌حوصلگی (از مرحله ۹) و تحلیل وضعیت روحی از طریق صدا (از مرحله ۱۰) ارائه می‌دهد. برنامه باید بتواند با در نظر گرفتن هر دو عامل، پیشنهادهای بسیار شخصی‌سازی‌شده و متناسب با نیاز لحظه‌ای کاربر را ارائه کند. این پیشنهادها می‌توانند شامل فعالیت‌های سرگرم‌کننده یا مفید باشند.
**Excerpt:**
```
[02:24] مثلاً میگه تو این شرایط چون تو اوقات الان بیکاریت هست، حوصله‌ات سر رفته، فلان کارو انجام بده.
```

### Step 12: پیاده‌سازی قابلیت تشخیص و طبقه‌بندی موقعیت مکانی (خانه، کار، غیره)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعهٔ سیستمی است که با استفاده از داده‌های موقعیت مکانی (از مرحله ۲)، قادر به تشخیص و طبقه‌بندی مکان‌های کلیدی کاربر مانند 'خانه'، 'محل کار' یا سایر مکان‌های مهم باشد. برنامه باید بتواند با یادگیری الگوهای رفتاری کاربر، این مکان‌ها را شناسایی کرده و زمینهٔ مکانی را برای پیشنهادهای هوشمند فراهم کند. این قابلیت نیازمند دقت در تشخیص و به‌روزرسانی مداوم مکان‌ها است.
**Excerpt:**
```
[02:33] و هم لوکیشن هم ببینه، ببینه مثلاً سر کارم، خونه‌ام، کجام.
```

### Step 13: توسعهٔ موتور پیشنهاددهی جامع بر اساس ترکیب موقعیت، بیکاری و کارهای در انتظار
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعهٔ یک موتور پیشنهاددهی پیشرفته است که تمام داده‌های جمع‌آوری‌شده را ترکیب می‌کند: موقعیت مکانی طبقه‌بندی‌شده (از مرحله ۱۲)، وضعیت بیکاری/بی‌حوصلگی (از مرحله ۹) و لیست کارهای در انتظار (از مرحله ۱). هدف این است که برنامه بتواند با در نظر گرفتن همهٔ این عوامل، هوشمندانه‌ترین و مرتبط‌ترین پیشنهاد را برای انجام کارهای در انتظار یا فعالیت‌های مفید ارائه دهد.
**Excerpt:**
```
[02:38] بگه که مثلاً با توجه به سر کار خونه فلان تو این موقع بیکاری و حوصله‌ات سر رفته، فلان کاری که گذاشتی تو یه نوبت و اینو باید انجام بدی.
```

### Step 14: پیاده‌سازی قابلیت استفاده از موبایل به عنوان منبع اصلی جمع‌آوری داده
**Status:** `pending` (0%)
**Scope:** این مرحله بر اطمینان از این موضوع تمرکز دارد که حتی در صورت عدم وجود دستگاه‌های پوشیدنی، خود موبایل کاربر بتواند به عنوان منبع اصلی جمع‌آوری داده‌های مورد نیاز عمل کند. این شامل استفاده از حسگرهای داخلی موبایل (مانند شتاب‌سنج، ژیروسکوپ برای فعالیت، میکروفون برای صدا، GPS برای موقعیت) و دسترسی‌های کامل به سیستم عامل برای جمع‌آوری داده‌ها و تحلیل شرایط کاربر است.
**Excerpt:**
```
[02:52] البته باید یه سری چیزام هم باید مثلاً به خودم وصل داشته باشم مثل مثلاً یه ساعت‌هایی که همه چیزو آنالیز می‌کنن و این‌ها رو به داده‌ها مرتبط کنن به سیستم یا خود مثلاً موبایل اگه همراهم باشه و دسترسی‌ها رو کامل داده باشم مثلاً خودش بتونه شرایطو مثلاً همه چیزو بسنجه و از تو لیست برام مواردی انتخاب کنه و اعلام بکنه.
```

### Step 15: طراحی و پیاده‌سازی رابط کاربری برای نمایش پیشنهادهای هوشمند
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی رابط کاربری (UI) است که تمام پیشنهادهای هوشمند تولیدشده توسط موتورهای مختلف (مبتنی بر مکان، سلامت، بیکاری، ترکیب‌شده) را به شکلی واضح، جذاب و کاربرپسند به کاربر نمایش دهد. این رابط کاربری باید امکان تعامل با پیشنهادها (مثلاً پذیرش، رد، یا به تعویق انداختن) را نیز فراهم کند و تجربهٔ کاربری روانی را ارائه دهد.
**Excerpt:**
```
[03:08] خودش بتونه شرایطو مثلاً همه چیزو بسنجه و از تو لیست برام مواردی انتخاب کنه و اعلام بکنه.
```
