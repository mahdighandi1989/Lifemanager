---
task_id: 14e65214-77a8-409b-80dc-9a328ec646da
title: Implement User Interest & Profile System
type: other
priority: medium
execution_priority: 3100
status: pending
external_status: claimed
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-29T12:20:14.940616+00:00'
updated_at: '2026-06-05T06:18:06.410520+00:00'
target_files:
- app/models/user_interest.py
- app/schemas/user_interest_schema.py
- app/services/user_interest_service.py
- app/routes/interests.py
- app/main.py
- app/models/user.py
- app/services/ai/recommendation_service.py
- app/routes/ai.py
- app/schemas/ai_schema.py
- app/models/context.py
- app/services/ai/nlp_service.py
- app/routes/context.py
- app/models/ai_assessment.py
- frontend/src/components/RecommendationPanel.jsx
---

# Implement User Interest & Profile System

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_808505_AgADXiEA.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 4,178 char متن_

## هدف اصلی کاربر از ارسال فایل صوتی  _(at: audio/ogg)_

[00:00] می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینو تو پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.

## نقش مورد انتظار از هوش مصنوعی/مشاور  _(at: audio/ogg)_

--- صفحه 1 ---
[00:00] می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی خب مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است. یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینا رو تو پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تره خیلی دقیق نه به صورت کلیشه‌ای.

## معیارهای تولید سرفصل‌ها  _(at: audio/ogg)_

[00:00] گوینده ۱: می‌خوام مدل‌های موجود توی برنامه
[00:03] طوری باشن که
[00:06] از روی مثلاً داده‌هایی که بهشون دادم
[00:09] حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن
[00:13] توی زمینه‌های مختلف، یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه
[00:18] و مطمئن بشن که این علاقه است، حالا مثلاً ممکنه یه چیز دیگه‌ای باشه
[00:22] ممکنه اصلاً به علاقه نداشته باشه اونجا جدا
[00:25] ولی مثلاً تشخیص میدن این علاقه است یا چیز دیگه است
[00:29] یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینا رو تو پروفایل من تحلیل بکنن و
[00:34] حسب علائق من پیشنهادات مختلف نسبت به اون موضوع
[00:38] بدن و همینطور
[00:40] روحیات منو تحلیل کنن، شخصیت منو
[00:43] روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده
[00:46] برای من مثلاً نظر شغلی و
[00:49] چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم
[00:52] موفق‌تر خیلی دقیق، نه به صورت کلیشه‌ای.

## مثال‌های سرفصل‌های پیشنهادی کاربر (سناریوهای فرضی)  _(at: audio/ogg)_

مثال‌های سرفصل‌های پیشنهادی کاربر (سناریوهای فرضی)

[00:00] می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم، حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن، توی زمینه‌های مختلف، یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن هم بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است. یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینو تو پروفایل من تحلیل بکنن و حسب علایق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.

## محدودیت‌ها و فرمت خروجی مورد انتظار  _(at: audio/ogg)_

[00:00] می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم، حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن، توی زمینه‌های مختلف، یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن هم بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً بهش علاقه نداشته باشم اون جدا. ولی خب مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است. یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینو تو پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین‌چیزو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_808505_AgADXiEA.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 4,178 char متن_

## هدف اصلی کاربر از ارسال فایل صوتی  _(at: audio/ogg)_

[00:00] می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینو تو پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.

## نقش مورد انتظار از هوش مصنوعی/مشاور  _(at: audio/ogg)_

--- صفحه 1 ---
[00:00] می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی خب مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است. یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینا رو تو پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تره خیلی دقیق نه به صورت کلیشه‌ای.

## معیارهای تولید سرفصل‌ها  _(at: audio/ogg)_

[00:00] گوینده ۱: می‌خوام مدل‌های موجود توی برنامه
[00:03] طوری باشن که
[00:06] از روی مثلاً داده‌هایی که بهشون دادم
[00:09] حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن
[00:13] توی زمینه‌های مختلف، یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه
[00:18] و مطمئن بشن که این علاقه است، حالا مثلاً ممکنه یه چیز دیگه‌ای باشه
[00:22] ممکنه اصلاً به علاقه نداشته باشه اونجا جدا
[00:25] ولی مثلاً تشخیص میدن این علاقه است یا چیز دیگه است
[00:29] یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینا رو تو پروفایل من تحلیل بکنن و
[00:34] حسب علائق من پیشنهادات مختلف نسبت به اون موضوع
[00:38] بدن و همینطور
[00:40] روحیات منو تحلیل کنن، شخصیت منو
[00:43] روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده
[00:46] برای من مثلاً نظر شغلی و
[00:49] چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم
[00:52] موفق‌تر خیلی دقیق، نه به صورت کلیشه‌ای.

## مثال‌های سرفصل‌های پیشنهادی کاربر (سناریوهای فرضی)  _(at: audio/ogg)_

مثال‌های سرفصل‌های پیشنهادی کاربر (سناریوهای فرضی)

[00:00] می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم، حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن، توی زمینه‌های مختلف، یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن هم بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است. یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینو تو پروفایل من تحلیل بکنن و حسب علایق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.

## محدودیت‌ها و فرمت خروجی مورد انتظار  _(at: audio/ogg)_

[00:00] می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم، حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن، توی زمینه‌های مختلف، یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن هم بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً بهش علاقه نداشته باشم اون جدا. ولی خب مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است. یا سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن، اینو تو پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین‌چیزو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

## 📋 چک‌لیست مراحل (8 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: طراحی و پیاده‌سازی زیرساخت دریافت و ذخیره‌سازی داده‌های ورودی کاربر** — این مرحله شامل طراحی و پیاده‌سازی مکانیزم‌هایی است که کاربر بتواند داده‌ها و لیست‌های خود را به برنامه ارائه دهد. این داده‌ها می‌توانند شامل هر نوع اطلاعاتی باشند که برای شناسایی علایق و سلیقه‌ها لازم است. تمرکز بر روی کانال‌های ورودی و ذخیره‌سازی اولیه این داده‌ها برای پردازش‌های بعدی است. این مرحل
- [ ] **مرحله 2: توسعه مدل شناسایی علایق کاربر در زمینه‌های مختلف** — این مرحله بر توسعه یا ادغام مدل‌های هوش مصنوعی تمرکز دارد که قادر به شناسایی علایق کاربر از داده‌های ورودی (جمع‌آوری شده در مرحله قبل) باشند. نکته حیاتی این است که مدل باید توانایی 'اطمینان یافتن' از اینکه یک مورد واقعاً علاقه است یا خیر را داشته باشد و بتواند آن را از موارد بی‌ربط یا موقتی تشخیص ده
- [ ] **مرحله 3: طراحی و پیاده‌سازی ساختار پروفایل کاربر و موتور تحلیل علایق و سلیقه‌ها** — این مرحله شامل طراحی پایگاه داده و ساختار داده‌ای برای ذخیره و مدیریت پروفایل کاربر است. همچنین، پیاده‌سازی یک موتور تحلیل مرکزی که علایق و سلیقه‌های شناسایی شده (از مراحل ۲ و ۳) را دریافت کرده، آن‌ها را در پروفایل کاربر ادغام و تحلیل می‌کند تا یک دیدگاه جامع و یکپارچه از ترجیحات کاربر ایجاد شود. ای
- [ ] **مرحله 4: توسعه موتور تولید پیشنهادات متنوع بر اساس علایق و سلیقه‌ها** — این مرحله بر پیاده‌سازی سیستمی تمرکز دارد که با استفاده از علایق و سلیقه‌های تحلیل شده در پروفایل کاربر (مرحله ۴)، پیشنهادات متنوع و مرتبطی را تولید کند. این موتور باید قادر باشد پیشنهادات را در زمینه‌های مختلف مرتبط با علایق کاربر ارائه دهد و از تکرار یا ارائه پیشنهادات کلیشه‌ای پرهیز کند. هدف، ارا
- [ ] **مرحله 5: توسعه مدل تحلیل روحیات کاربر** — این مرحله شامل توسعه یا ادغام مدل‌های هوش مصنوعی است که قادر به تحلیل روحیات و وضعیت عاطفی کاربر باشند. این تحلیل ممکن است بر اساس داده‌های متنی، صوتی، یا رفتاری کاربر انجام شود. هدف، درک وضعیت روحی کاربر برای ارائه پیشنهادات یا راهنمایی‌های متناسب‌تر است. این مدل باید بتواند تغییرات روحیات را نیز ت
- [ ] **مرحله 6: توسعه مدل روانشناسی و تحلیل شخصیت کاربر** — این مرحله به توسعه یا ادغام مدل‌های هوش مصنوعی می‌پردازد که قادر به انجام روانشناسی و تحلیل شخصیت کاربر باشند. این تحلیل باید عمیق‌تر از صرفاً علایق یا روحیات باشد و به ویژگی‌های پایدار شخصیتی بپردازد. مدل باید بتواند ابعاد مختلف شخصیت را شناسایی کند تا در ترسیم مسیرهای آینده کاربر مورد استفاده قرار
- [ ] **مرحله 7: ادغام و تحلیل جامع روحیات و شخصیت در پروفایل کاربر** — این مرحله شامل ادغام خروجی‌های مدل‌های تحلیل روحیات (مرحله ۶) و شخصیت (مرحله ۷) با پروفایل موجود کاربر (مرحله ۴) است. هدف، ایجاد یک دیدگاه جامع و یکپارچه از وضعیت روحی و ویژگی‌های شخصیتی کاربر در کنار علایق و سلیقه‌های اوست. این تحلیل جامع، پایه و اساس ترسیم آینده شغلی و سایر پیشنهادات بلندمدت خواهد
- [ ] **مرحله 8: توسعه موتور ترسیم آینده شغلی و مسیرهای زندگی (دقیق و غیرکلیشه‌ای)** — این مرحله شامل پیاده‌سازی موتور نهایی است که با استفاده از تمامی اطلاعات تحلیل شده در پروفایل جامع کاربر (علایق، سلیقه‌ها، روحیات، شخصیت - از مراحل ۴ و ۸)، قادر به ترسیم نظرات شغلی و مسیرهای مختلف زندگی باشد. تاکید بر 'خیلی دقیق، نه به صورت کلیشه‌ای' است، به این معنی که موتور باید از الگوریتم‌های پی

---

# 🔹 مرحله 1: طراحی و پیاده‌سازی زیرساخت دریافت و ذخیره‌سازی داده‌های ورودی کاربر

**Scope:** این مرحله شامل طراحی و پیاده‌سازی مکانیزم‌هایی است که کاربر بتواند داده‌ها و لیست‌های خود را به برنامه ارائه دهد. این داده‌ها می‌توانند شامل هر نوع اطلاعاتی باشند که برای شناسایی علایق و سلیقه‌ها لازم است. تمرکز بر روی کانال‌های ورودی و ذخیره‌سازی اولیه این داده‌ها برای پردازش‌های بعدی است. این مرحله شامل تحلیل خود داده‌ها نمی‌شود، بلکه فقط جمع‌آوری و نگهداری آن‌ها را پوشش می‌دهد.
**Key terms:** داده‌هایی که بهشون دادم, لیست‌هایی که دادن, مدل‌های موجود توی برنامه

**بخش مربوط از متن کاربر:**
```
می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی زیرساخت دریافت و ذخیره علایق کاربر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/user_interest.py` — `UserInterest` — فایل جدید برای تعریف مدل SQLAlchemy `UserInterest`.
  ```python
  from sqlalchemy import Column, Integer, String, Float, ForeignKey, DateTime, func
  from sqlalchemy.orm import relationship
  from app.database import Base
  
  class UserInterest(Base):
      __tablename__ = "user_interests"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
      interest_type = Column(String(64), index=True, nullable=False)
      value = Column(String, nullable=False)
      source = Column(String(64), nullable=True)
      confidence_score = Column(Float, nullable=True)
      created_at = Column(DateTime(timezone=True), server_default=func.now())
      updated_at = Column(DateTime(timezone=True), onupdate=func.now())
  
      user = relationship("User", back_populates="interests")
  ```
- `app/schemas/user_interest_schema.py` — `UserInterestCreate, UserInterestResponse` — فایل جدید برای تعریف شمای Pydantic برای اعتبارسنجی داده‌های ورودی و خروجی.
  ```python
  from pydantic import BaseModel, Field
  from datetime import datetime
  from typing import Optional
  
  class UserInterestBase(BaseModel):
      interest_type: str = Field(..., max_length=64)
      value: str
      source: Optional[str] = Field(None, max_length=64)
      confidence_score: Optional[float] = Field(None, ge=0.0, le=1.0)
  
  class UserInterestCreate(UserInterestBase):
      pass
  
  class UserInterestResponse(UserInterestBase):
      id: int
      user_id: int
      created_at: datetime
      updated_at: datetime
  
      class Config:
          from_attributes = True
  ```
- `app/services/user_interest_service.py` — `UserInterestService` — فایل جدید برای منطق کسب‌وکار مربوط به علایق کاربر.
  ```python
  from sqlalchemy.ext.asyncio import AsyncSession
  from sqlalchemy import select, delete
  from app.models.user_interest import UserInterest
  from app.schemas.user_interest_schema import UserInterestCreate
  from typing import List, Optional
  
  class UserInterestService:
      def __init__(self, db: AsyncSession):
          self.db = db
  
      async def create_interest(self, user_id: int, interest_data: UserInterestCreate) -> UserInterest:
          new_interest = UserInterest(user_id=user_id, **interest_data.model_dump())
          self.db.add(new_interest)
          await self.db.commit()
          await self.db.refresh(new_interest)
          return new_interest
  
      async def get_interests_by_user(self, user_id: int) -> List[UserInterest]:
          result = await self.db.execute(
              select(UserInterest).where(UserInterest.user_id == user_id)
          )
          return result.scalars().all()
  
      async def get_interest_by_id(self, interest_id: int) -> Optional[UserInterest]:
          result = await self.db.execute(
              select(UserInterest).where(UserInterest.id == interest_id)
          )
          return result.scalar_one_or_none()
  
      async def delete_interest(self, interest_id: int, user_id: int) -> bool:
          interest = await self.get_interest_by_id(interest_id)
          if not interest or interest.user_id != user_id:
              return False
          await self.db.execute(
              delete(UserInterest).where(UserInterest.id == interest_id)
          )
          await self.db.commit()
          return True
  ```
- `app/routes/interests.py` — `router` — فایل جدید برای تعریف endpointهای API مربوط به علایق کاربر.
  ```python
  from fastapi import APIRouter, Depends, HTTPException, status
  from sqlalchemy.ext.asyncio import AsyncSession
  from typing import List
  
  from app.dependencies.auth import get_current_user
  from app.database import get_db
  from app.models.user import User
  from app.schemas.user_interest_schema import UserInterestCreate, UserInterestResponse
  from app.services.user_interest_service import UserInterestService
  
  router = APIRouter(prefix="/api/interests", tags=["interests"])
  
  @router.post("/", response_model=UserInterestResponse, status_code=status.HTTP_201_CREATED)
  async def create_user_interest(
      interest: UserInterestCreate,
      current_user: User = Depends(get_current_user),
      db: AsyncSession = Depends(get_db)
  ):
      service = UserInterestService(db)
      new_interest = await service.create_interest(user_id=current_user.id, interest_data=interest)
      return new_interest
  
  @router.get("/", response_model=List[UserInterestResponse])
  async def get_user_interests(
      current_user: User = Depends(get_current_user),
      db: AsyncSession = Depends(get_db)
  ):
      service = UserInterestService(db)
      interests = await service.get_interests_by_user(user_id=current_user.id)
      return interests
  
  @router.delete("/{interest_id}", status_code=status.HTTP_204_NO_CONTENT)
  async def delete_user_interest(
      interest_id: int,
      current_user: User = Depends(get_current_user),
      db: AsyncSession = Depends(get_db)
  ):
      service = UserInterestService(db)
      success = await service.delete_interest(interest_id=interest_id, user_id=current_user.id)
      if not success:
          raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Interest not found or not authorized")
      return
  ```
- `app/main.py:657-658` — `app.include_router` — اضافه کردن روتر جدید `interests` به برنامه FastAPI.
  ```python
  app.include_router(planner.router, tags=["planner"])
  app.include_router(self_improvement.router, tags=["self-improvement"])
  ```
- `app/models/user.py` — `User` — اضافه کردن رابطه `interests` به مدل `User` برای اتصال به `UserInterest`.
  ```python
  # In app/models/user.py (conceptual snippet)
  from sqlalchemy.orm import relationship
  # ... other imports
  
  class User(Base):
      # ... existing columns
      interests = relationship("UserInterest", back_populates="user", cascade="all, delete-orphan")
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. این پیاده‌سازی از FastAPI برای backend API، SQLAlchemy برای ORM و تعامل با پایگاه داده، و Pydantic برای تعریف و اعتبارسنجی شمای داده‌ها استفاده می‌کند. احراز هویت از طریق `app.dependencies.auth.get_current_user` انجام می‌شود که با JWT کار می‌کند. پایگاه داده از طریق `app.database.engine` و `AsyncSession` مدیریت می‌شود.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/__init__.py` — مدل `UserInterest` جدید باید در این فایل import شود تا توسط Base.metadata.create_all شناسایی شود.
- `app/schemas/__init__.py` — شمای `UserInterest` جدید باید در این فایل import شود تا به عنوان بخشی از شمای کلی پروژه در دسترس باشد.
- `app/routes/__init__.py` — روتر `interests` جدید باید در این فایل import شود تا در `app/main.py` قابل استفاده باشد.
- `app/services/__init__.py` — سرویس `UserInterestService` جدید باید در این فایل import شود تا به عنوان بخشی از سرویس‌های پروژه در دسترس باشد.
- `app/services/ai/ai_data_access_service.py` — این سرویس مسئول دسترسی به داده‌ها برای مدل‌های AI است و باید برای بازیابی داده‌های `UserInterest` به‌روزرسانی شود.
- `app/services/ai/recommendation_service.py` — این سرویس از داده‌های کاربر برای تولید توصیه‌ها استفاده می‌کند و در آینده باید داده‌های `UserInterest` را مصرف کند.
- `app/dependencies/auth.py` — روتر `interests` از `get_current_user` برای احراز هویت استفاده می‌کند.

## 🌐 نقشهٔ وابستگی‌ها
این تسک یک موجودیت جدید (`UserInterest`) را به مدل‌های پایگاه داده اضافه می‌کند که به مدل `User` وابسته است. این مدل از طریق یک سرویس (`UserInterestService`) مدیریت می‌شود که عملیات CRUD را انجام می‌دهد. یک روتر FastAPI (`app/routes/interests.py`) برای ارائه API به این سرویس اضافه می‌شود و در `app/main.py` مونت می‌گردد. شمای Pydantic مربوطه در `app/schemas/user_interest_schema.py` تعریف می‌شود. در نهایت، سرویس‌های هوش مصنوعی مانند `app/services/ai/ai_data_access_service.py` و `app/services/ai/recommendation_service.py` در آینده از این داده‌ها برای بهبود عملکرد خود استفاده خواهند کرد. فایل‌های `app/database.py` (برای Base و SessionLocal) و `app/dependencies/auth.py` (برای احراز هویت) نیز به طور غیرمستقیم توسط این کامپوننت‌های جدید استفاده می‌شوند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی و پیاده‌سازی مکانیزم‌هایی را دارد که بتواند داده‌ها و لیست‌های خود را به برنامه ارائه دهد. این داده‌ها می‌توانند شامل هر نوع اطلاعاتی باشند که برای شناسایی علایق و سلیقه‌ها لازم است. تمرکز بر روی کانال‌های ورودی و ذخیره‌سازی اولیه این داده‌ها برای پردازش‌های بعدی است. این مرحله شامل تحلیل خود داده‌ها نمی‌شود، بلکه فقط جمع‌آوری و نگهداری آن‌ها را پوشش می‌دهد. کاربر به صراحت بیان کرده است: «می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است.»

برای تحقق این درخواست، نیاز به ایجاد یک مدل داده جدید برای ذخیره‌سازی علایق کاربر، تعریف شمای Pydantic برای آن، ایجاد سرویس‌های مربوط به CRUD و افزودن endpointهای API در FastAPI است. این زیرساخت به مدل‌های موجود در برنامه، به‌ویژه سرویس‌های هوش مصنوعی مانند `app/services/ai/recommendation_service.py` و `app/services/ai/ai_data_access_service.py`، اجازه می‌دهد تا در آینده از این داده‌ها برای شناسایی علایق و ارائه توصیه‌ها استفاده کنند. مدل `app/models/context.py` که شامل `UserContext` و `Recommendation` است، نشان‌دهنده وجود زیرساخت‌های مرتبط با داده‌های کاربر و توصیه‌هاست که این ویژگی جدید می‌تواند آن را تکمیل کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک جدول جدید به نام `user_interests` در پایگاه داده ایجاد شود که شامل ستون‌های `id`, `user_id`, `interest_type`, `value`, `source`, `confidence_score`, `created_at`, و `updated_at` باشد.
- [ ] درخواست `POST /api/interests` با داده‌های معتبر، یک علاقه جدید برای کاربر احراز هویت شده ایجاد کند و پاسخ `201 Created` با شیء علاقه ایجاد شده را برگرداند.
- [ ] درخواست `GET /api/interests` لیست علایق مربوط به کاربر احراز هویت شده را برگرداند.
- [ ] درخواست `DELETE /api/interests/{interest_id}` یک علاقه خاص را حذف کند و پاسخ `204 No Content` برگرداند. اگر علاقه وجود نداشته باشد یا کاربر مالک آن نباشد، `404 Not Found` برگرداند.
- [ ] سرویس `app/services/ai/ai_data_access_service.py` قابلیت بازیابی داده‌های `UserInterest` را داشته باشد (با افزودن یک متد جدید).
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی زیرساخت دریافت و ذخیره‌سازی داده‌های ورودی کاربر برای شناسایی علایق، مراحل زیر پیشنهاد می‌شود:

1.  **ایجاد مدل `UserInterest`:** یک مدل جدید SQLAlchemy به نام `UserInterest` در فایل `app/models/user_interest.py` ایجاد شود. این مدل شامل فیلدهایی مانند `user_id` (کلید خارجی به `User`), `interest_type` (نوع علاقه، مثلاً 'hobby', 'topic'), `value` (مقدار علاقه، مثلاً 'reading', 'AI'), `source` (منبع داده، مثلاً 'manual_input', 'task_analysis'), و `confidence_score` (امتیاز اطمینان برای مدل‌های AI) خواهد بود.
2.  **به‌روزرسانی مدل `User`:** در فایل `app/models/user.py`، یک رابطه (relationship) به مدل `UserInterest` اضافه شود تا امکان دسترسی به علایق کاربر از طریق شیء `User` فراهم شود.
3.  **تعریف شمای Pydantic:** شمای Pydantic برای `UserInterest` (شامل `UserInterestCreate` و `UserInterestResponse`) در فایل `app/schemas/user_interest_schema.py` تعریف شود تا برای اعتبارسنجی داده‌های ورودی و خروجی API استفاده شود.
4.  **ایجاد سرویس `UserInterestService`:** یک سرویس جدید به نام `UserInterestService` در فایل `app/services/user_interest_service.py` ایجاد شود. این سرویس مسئول منطق کسب‌وکار برای عملیات CRUD (ایجاد، خواندن، به‌روزرسانی، حذف) روی مدل `UserInterest` خواهد بود.
5.  **ایجاد روتر FastAPI:** یک روتر جدید FastAPI در فایل `app/routes/interests.py` ایجاد شود که endpointهای زیر را ارائه دهد:
    *   `POST /api/interests`: برای ایجاد یک علاقه جدید توسط کاربر احراز هویت شده.
    *   `GET /api/interests`: برای دریافت لیست علایق کاربر احراز هویت شده.
    *   `DELETE /api/interests/{interest_id}`: برای حذف یک علاقه خاص توسط کاربر احراز هویت شده.
6.  **مونت کردن روتر در `app/main.py`:** روتر `interests` در فایل `app/main.py` مونت شود تا endpointهای آن در دسترس قرار گیرند.
7.  **یکپارچه‌سازی با سرویس‌های AI (فاز بعدی):** در آینده، `app/services/ai/ai_data_access_service.py` و `app/services/ai/recommendation_service.py` برای استفاده از داده‌های `UserInterest` به‌روزرسانی خواهند شد تا توصیه‌های دقیق‌تری ارائه دهند. این مرحله فعلاً شامل تغییرات در این سرویس‌ها نمی‌شود، اما طراحی باید با این هدف سازگار باشد.

## 💡 نمونه‌های قبل/بعد
**اضافه کردن روتر interests به app/main.py**

_قبل:_
```
app.include_router(planner.router, tags=["planner"])
app.include_router(self_improvement.router, tags=["self-improvement"])
```

_بعد:_
```
import app.routes.interests # اضافه شده

app.include_router(planner.router, tags=["planner"])
app.include_router(self_improvement.router, tags=["self-improvement"])
app.include_router(app.routes.interests.router, tags=["interests"]) # اضافه شده
```

**اضافه کردن رابطه به مدل User (در app/models/user.py)**

_قبل:_
```
# ... سایر فیلدها و روابط موجود در کلاس User
# class User(Base):
#    __tablename__ = "users"
#    id = Column(Integer, primary_key=True, index=True)
#    email = Column(String, unique=True, index=True, nullable=False)
```

_بعد:_
```
# ... سایر فیلدها و روابط موجود در کلاس User
# class User(Base):
#    __tablename__ = "users"
#    id = Column(Integer, primary_key=True, index=True)
#    email = Column(String, unique=True, index=True, nullable=False)
    interests = relationship("UserInterest", back_populates="user", cascade="all, delete-orphan") # اضافه شده
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/app/tests/test_user_interests.py`
- `uvicorn app.main:app --host 0.0.0.0 --port 8000`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **ریسک مهاجرت پایگاه داده:** ایجاد جدول `user_interests` نیاز به اجرای مهاجرت‌های Alembic دارد. اگر Alembic به درستی اجرا نشود (مثلاً در محیط‌های Render که `Base.metadata.create_all` استفاده می‌شود)، ممکن است جدول ایجاد نشود یا ناسازگاری ایجاد شود. (مرتبط با `app/main.py` خطوط 207-219).
2. **ریسک امنیتی:** اطمینان از اینکه endpointهای جدید `app/routes/interests.py` به درستی احراز هویت و مجوزدهی شده‌اند تا فقط کاربر مالک بتواند علایق خود را مدیریت کند. عدم رعایت این موضوع می‌تواند منجر به دسترسی غیرمجاز به داده‌های کاربر شود (مرتبط با `app/dependencies/auth.py`).
3. **پیچیدگی مدل داده:** اگر `interest_type` و `value` به صورت رشته‌ای باقی بمانند، ممکن است در آینده نیاز به ساختاردهی بیشتر یا استفاده از JSONB برای انعطاف‌پذیری بیشتر باشد که می‌تواند تغییرات بعدی را پیچیده کند. (مرتبط با `app/models/user_interest.py`).

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: توسعه مدل شناسایی علایق کاربر در زمینه‌های مختلف

**Scope:** این مرحله بر توسعه یا ادغام مدل‌های هوش مصنوعی تمرکز دارد که قادر به شناسایی علایق کاربر از داده‌های ورودی (جمع‌آوری شده در مرحله قبل) باشند. نکته حیاتی این است که مدل باید توانایی 'اطمینان یافتن' از اینکه یک مورد واقعاً علاقه است یا خیر را داشته باشد و بتواند آن را از موارد بی‌ربط یا موقتی تشخیص دهد. این مدل باید در زمینه‌های مختلف فعالیت کند.
— [merged] این مرحله به توسعه یا ادغام مدل‌های هوش مصنوعی اختصاص دارد که وظیفه شناسایی سلیقه‌های کاربر را بر عهده دارند. سلیقه‌ها ممکن است از علایق متمایز باشند و نیاز به رویکردهای تحلیلی متفاوتی داشته باشند. مدل باید بتواند سلیقه‌ها را در طیف وسیعی از زمینه‌ها تشخیص دهد و آن‌ها را برای استفاده در پروفایل کاربر آماده کند. این مرحله بر روی منطق اصلی شناسایی سلیقه تمرکز دارد.
**Key terms:** سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن, مدل‌های موجود توی برنامه, مطمئن بشن که این علاقه است, علائقی که از من شناسایی می‌کنن, تشخیص می‌دن این علاقه است یا چیز دیگه است

**بخش مربوط از متن کاربر:**
```
علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است.
```

## 🎯 هدف (خلاصه ساختاریافته)
توسعه مدل شناسایی علایق و سلیقه‌های کاربر با اطمینان‌سنجی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/ai/recommendation_service.py:1-10` — `RecommendationService` — این سرویس مسئول تولید توصیه‌ها است و باید علایق شناسایی‌شده را به عنوان ورودی برای بهبود دقت توصیه‌ها مصرف کند. منطق جدید شناسایی علایق در اینجا فراخوانی یا نتایج آن استفاده خواهد شد.
  ```python
  from sqlalchemy.ext.asyncio import AsyncSession
  
  from app.models.recommendation import Recommendation
  from app.services.ai.ai_service import AIService
  from app.services.context_engine.context_service import ContextService
  from app.services.planner_service import PlannerService
  
  class RecommendationService:
      def __init__(self, db: AsyncSession):
  ```
- `app/routes/ai.py:1-20` — `router` — این فایل شامل endpointهای مرتبط با AI است. endpointهای جدید برای آغاز فرآیند شناسایی علایق و مدیریت آن‌ها در اینجا اضافه خواهند شد.
  ```python
  from typing import Optional
  
  from fastapi import APIRouter, Depends, HTTPException, status
  from sqlalchemy.ext.asyncio import AsyncSession
  
  from app.dependencies.auth import get_current_active_user
  from app.database import get_db
  from app.models.user import User
  from app.schemas.ai_schema import (
      AIGenerateRequest,
      AIGenerateResponse,
      AIModelConfigCreate,
      AIModelConfigResponse,
      AIModelConfigUpdate,
      AIProviderResponse,
      AIQueryRequest,
      AIQueryResponse,
  )
  from app.services.ai.ai_service import AIService
  from app.services.ai.model_service import AIModelConfigService
  from app.services.ai.provider_service import AIProviderService
  from app.config import settings
  ```
- `app/models/user.py:1-10` — `User` — مدل `User` نیاز به یک رابطه جدید با مدل‌های `UserInterest` و `UserTaste` خواهد داشت تا علایق و سلیقه‌های شناسایی‌شده به پروفایل کاربر مرتبط شوند.
  ```python
  from typing import Optional
  
  from sqlalchemy import Boolean, Integer, String, Text
  from sqlalchemy.orm import Mapped, mapped_column, relationship
  
  from app.database import Base
  
  class User(Base):
      __tablename__ = "users"
  
      id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
  ```
- `app/schemas/ai_schema.py:1-10` — `AIModelConfigResponse` — این فایل شامل شمای Pydantic برای مدل‌های AI است. شمای جدید برای `UserInterest` و `UserTaste` و همچنین شمای درخواست/پاسخ برای endpointهای جدید در اینجا تعریف خواهد شد.
  ```python
  from typing import Optional
  
  from pydantic import BaseModel
  
  class AIGenerateRequest(BaseModel):
      prompt: str
      model_config_id: Optional[int] = None
  
  class AIGenerateResponse(BaseModel):
      generated_text: str
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: FastAPI (Python) برای بک‌اند، React و Vite برای فرانت‌اند. از SQLAlchemy برای ORM و Pydantic برای اعتبارسنجی داده‌ها استفاده می‌شود. سرویس‌های AI موجود از `httpx` برای فراخوانی APIهای خارجی استفاده می‌کنند. Celery برای پردازش‌های پس‌زمینه در دسترس است که می‌تواند برای فرآیندهای سنگین شناسایی علایق استفاده شود.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/context.py` — داده‌های مربوط به Context کاربر (مانند موقعیت مکانی، فعالیت‌ها) ورودی‌های مهمی برای شناسایی علایق هستند.
- `app/models/behavior_log.py` — لاگ‌های رفتاری کاربر (مانند تعامل با وظایف، پروژه‌ها) می‌توانند نشانه‌هایی از علایق و سلیقه‌ها را فراهم کنند.
- `app/services/ai/model_service.py` — سرویس جدید شناسایی علایق برای تعامل با مدل‌های هوش مصنوعی پایه (مانند OpenAI) از این سرویس استفاده خواهد کرد.
- `app/services/context_engine/orchestrator_service.py` — این سرویس مسئول جمع‌آوری و هماهنگی داده‌های Context است که می‌تواند ورودی برای سرویس شناسایی علایق باشد.
- `app/main.py` (سطر 616) — برای شامل کردن روترهای جدید API و همچنین اجرای اسکریپت‌های Migration Alembic در زمان راه‌اندازی برنامه.
- `alembic/versions/` — یک اسکریپت Migration جدید برای ایجاد جداول `user_interests` و `user_tastes` مورد نیاز است.

## 🌐 نقشهٔ وابستگی‌ها
توسعه مدل شناسایی علایق کاربر نیازمند ایجاد مدل‌های داده‌ای جدید `UserInterest` و `UserTaste` در `app/models/` است که به مدل `User` در `app/models/user.py` وابسته خواهند بود. شمای Pydantic این مدل‌ها در `app/schemas/ai_schema.py` یا یک فایل جدید تعریف می‌شود. سرویس اصلی `app/services/ai/interest_identification_service.py` برای مدیریت sessionهای دیتابیس به `app/database.py` وابسته است و برای جمع‌آوری داده‌های ورودی به `app/models/context.py`, `app/models/behavior_log.py`, `app/models/task.py`, و `app/models/project.py` نیاز دارد. این سرویس همچنین برای استفاده از قابلیت‌های AI به `app/services/ai/model_service.py` یا `app/services/ai/nlp_service.py` وابسته خواهد بود. endpointهای جدید API در `app/routes/ai.py` و `app/routes/users.py` به این سرویس و شمای جدید وابسته هستند. در نهایت، `app/services/ai/recommendation_service.py` و `app/services/recommendation_engine.py` برای تولید توصیه‌های شخصی‌سازی‌شده، به خروجی این سرویس جدید (علایق شناسایی‌شده) وابسته خواهند شد.

## 🔍 Context و وضعیت فعلی
این تسک بر توسعه یا ادغام مدل‌های هوش مصنوعی تمرکز دارد که قادر به شناسایی علایق کاربر از داده‌های ورودی (جمع‌آوری شده در مرحله قبل) باشند. نکته حیاتی این است که مدل باید توانایی 'اطمینان یافتن' از اینکه یک مورد واقعاً علاقه است یا خیر را داشته باشد و بتواند آن را از موارد بی‌ربط یا موقتی تشخیص دهد. این مدل باید در زمینه‌های مختلف فعالیت کند. کاربر به صراحت بیان کرده است: «علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است.» همچنین، «سلیقه‌ها ممکن است از علایق متمایز باشند و نیاز به رویکردهای تحلیلی متفاوتی داشته باشند.» این نشان می‌دهد که سیستم باید قادر به تفکیک و تأیید هر دو مفهوم «علاقه» و «سلیقه» باشد. کلیدواژه‌های اصلی شامل «سلیقه‌های منو تو زمینه‌های مختلف شناسایی می‌کنن»، «مدل‌های موجود توی برنامه»، «مطمئن بشن که این علاقه است»، «علائقی که از من شناسایی می‌کنن»، و «تشخیص می‌دن این علاقه است یا چیز دیگه است» هستند. این قابلیت برای بهبود «AI-assisted planning» و شخصی‌سازی پروفایل کاربر در Lifemanager ضروری است. با توجه به ساختار پروژه، این قابلیت باید در سرویس‌های AI موجود مانند `app/services/ai/recommendation_service.py` یا یک سرویس جدید مرتبط با شناسایی علایق، و همچنین در مدل‌های داده‌ای کاربر و AI ادغام شود. نیاز به ایجاد مدل‌های جدید برای ذخیره علایق و سلیقه‌های شناسایی‌شده و همچنین API endpoint برای تعامل با این قابلیت وجود دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل‌های `UserInterest` و `UserTaste` در `app/models/` با فیلدهای `user_id`, `category`, `value`, `confidence_score`, و `is_verified` وجود داشته باشند.
- [ ] شمای Pydantic برای `UserInterest` و `UserTaste` در `app/schemas/ai_schema.py` یا `app/schemas/user_interest_schema.py` تعریف شده باشد.
- [ ] سرویس `app/services/ai/interest_identification_service.py` با متد `identify_and_verify_interests(user_id: int)` ایجاد شده باشد که علایق و سلیقه‌ها را شناسایی و ذخیره می‌کند.
- [ ] endpoint `POST /api/ai/identify_interests` با کد وضعیت 202 (Accepted) پاسخ دهد و فرآیند شناسایی علایق را آغاز کند.
- [ ] endpoint `GET /api/users/{user_id}/interests` علایق و سلیقه‌های شناسایی‌شده برای کاربر را برگرداند.
- [ ] سرویس `app/services/ai/recommendation_service.py` از علایق و سلیقه‌های شناسایی‌شده برای تولید توصیه‌ها استفاده کند.
- [ ] یک اسکریپت Migration جدید Alembic برای ایجاد جداول `user_interests` و `user_tastes` با موفقیت اجرا شود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی مدل شناسایی علایق و سلیقه‌های کاربر با قابلیت اطمینان‌سنجی، مراحل زیر پیشنهاد می‌شود:

1.  **تعریف مدل‌های داده‌ای جدید:** دو مدل SQLAlchemy جدید به نام‌های `UserInterest` و `UserTaste` در `app/models/` ایجاد شود. این مدل‌ها باید شامل فیلدهایی مانند `user_id` (کلید خارجی به `User`), `category` (مثلاً 'ورزش', 'فناوری', 'هنر'), `value` (نام علاقه/سلیقه), `confidence_score` (امتیاز اطمینان از 0 تا 1), و `is_verified` (یک فیلد بولی برای تأیید نهایی) باشند. همچنین، یک رابطه یک به چند از `User` به این مدل‌ها در `app/models/user.py` اضافه شود.
2.  **تعریف شمای Pydantic:** شمای Pydantic متناظر برای `UserInterest` و `UserTaste` (برای ورودی و خروجی API) در `app/schemas/ai_schema.py` یا یک فایل شمای جدید مانند `app/schemas/user_interest_schema.py` ایجاد شود.
3.  **ایجاد سرویس شناسایی علایق:** یک سرویس جدید به نام `app/services/ai/interest_identification_service.py` ایجاد شود. این سرویس متدی مانند `identify_and_verify_interests(user_id: int)` خواهد داشت که:
    *   داده‌های کاربر را از منابع مختلف (مانند `app/models/context.py`, `app/models/behavior_log.py`, `app/models/task.py`, `app/models/project.py`) جمع‌آوری و تحلیل می‌کند.
    *   از سرویس‌های AI موجود (مانند `app/services/ai/nlp_service.py` یا `app/services/ai/model_service.py`) برای استخراج علایق و سلیقه‌های بالقوه استفاده می‌کند.
    *   منطق «اطمینان یافتن» را پیاده‌سازی می‌کند؛ این می‌تواند شامل تحلیل چندین منبع داده، اعمال آستانه‌های اطمینان، یا استفاده از مدل‌های AI چندمرحله‌ای برای تأیید باشد. این منطق باید بتواند «تشخیص بدهد این علاقه است یا چیز دیگه است» و آن را از «موارد بی‌ربط یا موقتی» متمایز کند.
    *   علایق و سلیقه‌های شناسایی‌شده و تأییدشده را در مدل‌های `UserInterest` و `UserTaste` ذخیره می‌کند.
4.  **افزودن API Endpoint:**
    *   یک endpoint `POST /api/ai/identify_interests` در `app/routes/ai.py` اضافه شود که فرآیند شناسایی علایق را برای یک کاربر خاص آغاز می‌کند.
    *   یک endpoint `GET /api/users/{user_id}/interests` در `app/routes/users.py` اضافه شود که علایق و سلیقه‌های شناسایی‌شده برای یک کاربر را برمی‌گرداند.
5.  **ادغام با سرویس توصیه‌ها:** سرویس `app/services/ai/recommendation_service.py` به‌روزرسانی شود تا علایق و سلیقه‌های شناسایی‌شده را به عنوان ورودی برای تولید توصیه‌های دقیق‌تر و شخصی‌سازی‌شده استفاده کند.
6.  **ایجاد Migration:** یک اسکریپت Migration جدید با Alembic برای ایجاد جداول `user_interests` و `user_tastes` ایجاد شود.

## 💡 نمونه‌های قبل/بعد
**افزودن رابطه به مدل User**

_قبل:_
```
class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    email: Mapped[str] = mapped_column(String, unique=True, index=True)
    hashed_password: Mapped[str] = mapped_column(String)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    bio: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    display_name: Mapped[Optional[str]] = mapped_column(String(120), nullable=True)
```

_بعد:_
```
from app.models.user_interest import UserInterest # فرض بر ایجاد فایل جدید
from app.models.user_taste import UserTaste # فرض بر ایجاد فایل جدید

class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    email: Mapped[str] = mapped_column(String, unique=True, index=True)
    hashed_password: Mapped[str] = mapped_column(String)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    bio: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    display_name: Mapped[Optional[str]] = mapped_column(String(120), nullable=True)
    interests: Mapped[list["UserInterest"]] = relationship(back_populates="user", cascade="all, delete-orphan")
    tastes: Mapped[list["UserTaste"]] = relationship(back_populates="user", cascade="all, delete-orphan")
```

**افزودن endpoint جدید به app/routes/ai.py**

_قبل:_
```
@router.post("/generate", response_model=AIGenerateResponse)
async def generate_text(
    request: AIGenerateRequest,
    current_user: User = Depends(get_current_active_user),
    db: AsyncSession = Depends(get_db),
):
    # ... existing logic ...
    return AIGenerateResponse(generated_text=generated_text)
```

_بعد:_
```
from app.services.ai.interest_identification_service import InterestIdentificationService # New import

@router.post("/generate", response_model=AIGenerateResponse)
async def generate_text(
    request: AIGenerateRequest,
    current_user: User = Depends(get_current_active_user),
    db: AsyncSession = Depends(get_db),
):
    # ... existing logic ...
    return AIGenerateResponse(generated_text=generated_text)

@router.post("/identify_interests", status_code=status.HTTP_202_ACCEPTED, tags=["ai", "interests"])
async def identify_user_interests(
    current_user: User = Depends(get_current_active_user),
    db: AsyncSession = Depends(get_db),
):
    interest_service = InterestIdentificationService(db)
    # This could be an async task if identification is long-running
    await interest_service.identify_and_verify_interests(current_user.id)
    return {"message": "Interest identification initiated."}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/app/tests/`
- `alembic upgrade head`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **پیچیدگی مدل AI و عملکرد:** پیاده‌سازی منطق «اطمینان‌سنجی» برای علایق و سلیقه‌ها می‌تواند از نظر محاسباتی سنگین باشد و بر عملکرد بک‌اند تأثیر بگذارد، به خصوص اگر از مدل‌های NLP پیشرفته استفاده شود. این ممکن است نیاز به پردازش ناهمگام (با Celery) داشته باشد.
2. **حریم خصوصی داده‌ها:** جمع‌آوری و تحلیل داده‌های کاربر برای شناسایی علایق، نگرانی‌های مربوط به حریم خصوصی را افزایش می‌دهد. باید اطمینان حاصل شود که داده‌ها به درستی ناشناس‌سازی و محافظت می‌شوند.
3. **دقت و خطای مدل:** مدل‌های AI ممکن است علایق را به اشتباه شناسایی کنند (false positives) یا علایق واقعی را از دست بدهند (false negatives)، که می‌تواند تجربه کاربری را تحت تأثیر قرار دهد. منطق «اطمینان یافتن» باید به دقت طراحی شود تا این خطاها به حداقل برسد.
4. **تغییرات شمای دیتابیس:** افزودن جداول `user_interests` و `user_tastes` نیازمند یک Migration Alembic است. هرگونه خطا در اسکریپت Migration می‌تواند منجر به از دست رفتن داده یا خرابی دیتابیس در محیط‌های تولید شود.
5. **وابستگی‌های متقابل:** سرویس جدید `InterestIdentificationService` به چندین سرویس و مدل موجود (مانند `app/services/ai/model_service.py`, `app/models/context.py`, `app/models/behavior_log.py`) وابسته است. تغییرات در این وابستگی‌ها می‌تواند بر عملکرد سرویس جدید تأثیر بگذارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 3: طراحی و پیاده‌سازی ساختار پروفایل کاربر و موتور تحلیل علایق و سلیقه‌ها

**Scope:** این مرحله شامل طراحی پایگاه داده و ساختار داده‌ای برای ذخیره و مدیریت پروفایل کاربر است. همچنین، پیاده‌سازی یک موتور تحلیل مرکزی که علایق و سلیقه‌های شناسایی شده (از مراحل ۲ و ۳) را دریافت کرده، آن‌ها را در پروفایل کاربر ادغام و تحلیل می‌کند تا یک دیدگاه جامع و یکپارچه از ترجیحات کاربر ایجاد شود. این تحلیل باید به گونه‌ای باشد که برای تولید پیشنهادات بعدی قابل استفاده باشد.
**Key terms:** پروفایل من تحلیل بکنن, علائق من, ساختار پروفایل کاربر, موتور تحلیل

**بخش مربوط از متن کاربر:**
```
اینو تو پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی موتور تحلیل پروفایل کاربر و پیشنهادات شخصی‌سازی‌شده

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/user.py` — `User` — افزودن فیلدهای جدید برای ذخیره علایق، ویژگی‌های شخصیتی و الگوهای روحیاتی کاربر. این تغییر نیاز به Alembic migration دارد. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  # (محتوای فایل در deep_context ارائه نشده است)
  # فرض می‌شود مدل User موجود است و فیلدهای زیر به آن اضافه می‌شوند:
  # interests: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)
  # personality_traits: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)
  # mood_patterns: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)
  ```
- `app/services/ai/recommendation_service.py` — `RecommendationService` — به‌روزرسانی منطق تولید پیشنهادها برای استفاده از علایق، روحیات و شخصیت تحلیل‌شده کاربر. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  # (محتوای فایل در deep_context ارائه نشده است)
  # فرض می‌شود متدی مانند generate_recommendations وجود دارد که به‌روزرسانی می‌شود:
  # def generate_personalized_recommendations(self, user: User, context: UserContext) -> List[Recommendation]:
  #     # منطق استفاده از user.interests, user.personality_traits, user.mood_patterns
  #     pass
  ```
- `app/routes/ai.py` — `router` — افزودن یک endpoint جدید برای دریافت پیشنهادات شخصی‌سازی‌شده بر اساس پروفایل کاربر. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  # (محتوای فایل در deep_context ارائه نشده است)
  # فرض می‌شود یک APIRouter به نام router وجود دارد و endpoint زیر به آن اضافه می‌شود:
  # @router.get("/personalized_recommendations", response_model=List[RecommendationSchema])
  # async def get_personalized_recommendations(
  #     current_user: User = Depends(get_current_active_user),
  #     db: AsyncSession = Depends(get_db)
  # ):
  #     # فراخوانی سرویس پیشنهاددهنده با داده‌های پروفایل کاربر
  #     pass
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی شده: fastapi, react, vite. Backend: Python با FastAPI، SQLAlchemy (asyncio), Pydantic. برای AI از سرویس‌های موجود در `app/services/ai/` استفاده می‌شود که احتمالاً با OpenAI API (بر اساس `OPENAI_API_KEY` در `.env.example`) یا مدل‌های داخلی تعامل دارند. Frontend: React با Vite برای ساخت، Axios برای فراخوانی API.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/main.py` (سطر 616) — این فایل شامل تمام روترها، از جمله `ai.router` و `users.router` است. هر endpoint جدید یا تغییر در endpointهای موجود از طریق این فایل در دسترس قرار می‌گیرد. همچنین مسئول اجرای Alembic migrations در startup است.
- `app/models/context.py` (سطر -1) — شامل مدل‌های `UserContext` و `Recommendation` است که مستقیماً به زمینه کاربر و پیشنهادات شخصی‌سازی‌شده مرتبط هستند. داده‌های پروفایل جدید ممکن است در اینجا ادغام شوند یا بر نحوه استفاده از زمینه تأثیر بگذارند.
- `app/services/ai/nlp_service.py` (سطر -1) — این سرویس برای 'تحلیل روحیات منو تحلیل بکنن، شخصیت منو روانشناسی کنن' حیاتی است، زیرا قابلیت‌های پردازش زبان طبیعی را برای استخراج بینش از داده‌های کاربر فراهم می‌کند.
- `app/services/ai/content_analysis_service.py` (سطر -1) — مشابه `nlp_service.py`، این سرویس برای تحلیل محتوای تولید شده توسط کاربر به منظور استنتاج علایق و ویژگی‌های شخصیتی استفاده خواهد شد.
- `frontend/src/components/RecommendationPanel.jsx` (سطر -1) — این کامپوننت فرانت‌اند مسئول نمایش پیشنهادات است و باید برای نمایش پیشنهادات شخصی‌سازی‌شده جدید به‌روزرسانی شود.

## 🌐 نقشهٔ وابستگی‌ها
این فیچر به شدت به مدل `app/models/user.py` (برای ذخیره داده‌های پروفایل)، `app/models/context.py` و `app/models/recommendation.py` (برای ساختار پیشنهادات) وابسته است. سرویس‌های `app/services/ai/recommendation_service.py` و `app/services/recommendation_engine.py` منطق اصلی تولید پیشنهاد را در خود جای می‌دهند. `app/services/ai/nlp_service.py` و `app/services/ai/content_analysis_service.py` برای تحلیل متن و استخراج علایق و روحیات استفاده خواهند شد. `app/routes/users.py` و `app/routes/ai.py` endpointهای API را برای مدیریت پروفایل و دریافت پیشنهادات فراهم می‌کنند. در نهایت، `frontend/src/components/RecommendationPanel.jsx` و صفحات مرتبط در فرانت‌اند، این پیشنهادات را به کاربر نمایش می‌دهند.

## 🔍 Context و وضعیت فعلی
این تسک شامل طراحی و پیاده‌سازی ساختار پایگاه داده و داده‌ای برای ذخیره و مدیریت پروفایل کاربر است. هدف، ایجاد یک موتور تحلیل مرکزی است که علایق و سلیقه‌های شناسایی شده (از مراحل ۲ و ۳ که در متن کاربر ذکر شده) را دریافت کرده، آن‌ها را در پروفایل کاربر ادغام و تحلیل می‌کند تا یک دیدگاه جامع و یکپارچه از ترجیحات کاربر ایجاد شود. این تحلیل باید به گونه‌ای باشد که برای تولید پیشنهادات بعدی قابل استفاده باشد. کاربر به صراحت درخواست کرده که 'پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن.' این نشان می‌دهد که نیاز به قابلیت‌های پیشرفته‌تر AI برای تحلیل روانشناختی و رفتاری کاربر وجود دارد.

در حال حاضر، پروژه Lifemanager دارای مدل‌های `app/models/user.py`، `app/models/context.py` (شامل `UserContext` و `Recommendation`) و `app/models/ai_assessment.py` است که می‌توانند به عنوان پایه برای ذخیره داده‌های پروفایل و نتایج تحلیل استفاده شوند. همچنین، سرویس‌های `app/services/ai/recommendation_service.py` و `app/services/recommendation_engine.py` برای تولید پیشنهادات و `app/services/ai/nlp_service.py` و `app/services/ai/content_analysis_service.py` برای پردازش متن وجود دارند که باید توسعه یابند تا قابلیت‌های تحلیل علایق، روحیات و شخصیت را پوشش دهند. endpointهای `app/routes/users.py` و `app/routes/ai.py` نیز کاندیداهای اصلی برای افزودن منطق API مربوطه هستند. کلیدواژه‌های اصلی شامل 'ساختار پروفایل کاربر'، 'موتور تحلیل'، 'پروفایل من تحلیل بکنن'، 'علائق من'، 'روحیات منو تحلیل کنن'، و 'شخصیت منو روانشناسی کنن' هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل `User` (در `app/models/user.py`) باید شامل فیلدهای `interests`, `personality_traits`, و `mood_patterns` از نوع JSONB (یا معادل آن برای SQLite) باشد.
- [ ] یک Alembic migration جدید باید برای افزودن فیلدهای `interests`, `personality_traits`, و `mood_patterns` به جدول `users` ایجاد شود.
- [ ] سرویس `app/services/ai/recommendation_service.py` باید متدی برای تولید پیشنهادات شخصی‌سازی‌شده داشته باشد که از داده‌های پروفایل کاربر استفاده کند.
- [ ] یک endpoint جدید `GET /api/ai/personalized_recommendations` باید در `app/routes/ai.py` وجود داشته باشد که پیشنهادات شخصی‌سازی‌شده را برگرداند و وضعیت 200 را با فیلدهای `id`, `content`, `type`, `score` بازگرداند.
- [ ] کامپوننت `frontend/src/components/RecommendationPanel.jsx` باید قادر به نمایش پیشنهادات شخصی‌سازی‌شده از endpoint جدید باشد و آیتم‌های پیشنهادی با `data-testid='personalized-recommendation-item'` قابل مشاهده باشند.
- [ ] موتور تحلیل (با استفاده از `app/services/ai/nlp_service.py` و `app/services/ai/content_analysis_service.py`) باید قادر به تحلیل روحیات و شخصیت کاربر بر اساس ورودی‌های متنی باشد و نتایج را در فیلدهای پروفایل کاربر ذخیره کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1.  **Backend - Database Schema Update:**
    *   مدل `app/models/user.py` را برای افزودن فیلدهای جدید `interests` (JSONB), `personality_traits` (JSONB), و `mood_patterns` (JSONB) به‌روزرسانی کنید. این فیلدها برای ذخیره علایق، ویژگی‌های شخصیتی و الگوهای روحیاتی کاربر استفاده خواهند شد.
    *   یک اسکریپت Alembic migration جدید برای اعمال این تغییرات شمای پایگاه داده ایجاد کنید.
2.  **Backend - Core AI Services Enhancement:**
    *   سرویس `app/services/ai/recommendation_service.py` را به‌روزرسانی کنید تا داده‌های جدید پروفایل کاربر (علایق، شخصیت، روحیات) را در هنگام تولید پیشنهادات مصرف کند و پیشنهادات را شخصی‌سازی کند.
    *   سرویس‌های `app/services/ai/nlp_service.py` و `app/services/ai/content_analysis_service.py` را برای انجام تحلیل احساسات، استخراج کلمات کلیدی برای علایق، و پروفایل‌سازی اولیه شخصیت بر اساس محتوای تولید شده توسط کاربر (مانند وظایف، یادداشت‌ها، نظرات) بهبود بخشید.
    *   یک تابع جدید در `app/services/ai_service.py` (مثلاً `analyze_user_profile`) پیاده‌سازی کنید که فراخوانی سرویس‌های NLP و تحلیل محتوا را برای ساخت یک پروفایل جامع کاربر هماهنگ کند.
3.  **Backend - API Endpoints:**
    *   یک endpoint جدید PATCH به `app/routes/users.py` (مثلاً `/api/users/profile/preferences`) اضافه کنید تا کاربران بتوانند علایق خود را به صراحت تنظیم کنند.
    *   یک endpoint جدید GET به `app/routes/ai.py` (مثلاً `/api/ai/personalized_recommendations`) اضافه کنید که از `recommendation_service` به‌روزرسانی شده برای بازگرداندن پیشنهادات شخصی‌سازی‌شده بر اساس پروفایل تحلیل‌شده کاربر استفاده کند.
4.  **Frontend - UI Integration:**
    *   صفحه `frontend/src/pages/AISettings.jsx` یا یک صفحه/کامپوننت جدید را به‌روزرسانی کنید تا به کاربران امکان مشاهده و احتمالاً ویرایش علایق و مشاهده ویژگی‌های شخصیتی تحلیل‌شده خود را بدهد.
    *   کامپوننت `frontend/src/components/RecommendationPanel.jsx` را برای نمایش پیشنهادات شخصی‌سازی‌شده تولید شده توسط موتور جدید تغییر دهید.
    *   فایل `frontend/src/lib/api.js` را برای شامل کردن فراخوانی‌های API جدید برای به‌روزرسانی ترجیحات کاربر و دریافت پیشنهادات شخصی‌سازی‌شده به‌روزرسانی کنید.

## 💡 نمونه‌های قبل/بعد
**افزودن فیلدهای پروفایل به مدل `User` (Backend)**

_قبل:_
```
# app/models/user.py (مثال فرضی، محتوای دقیق در deep_context موجود نیست)
from sqlalchemy import Boolean, Integer, String
from sqlalchemy.orm import Mapped, mapped_column

from app.database import Base

class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    email: Mapped[str] = mapped_column(String, unique=True, index=True)
    hashed_password: Mapped[str] = mapped_column(String)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
```

_بعد:_
```
# app/models/user.py (مثال فرضی، محتوای دقیق در deep_context موجود نیست)
from typing import Optional
from sqlalchemy import Boolean, Integer, String
from sqlalchemy.dialects.postgresql import JSONB # فرض بر استفاده از PostgreSQL برای JSONB
from sqlalchemy.orm import Mapped, mapped_column

from app.database import Base

class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    email: Mapped[str] = mapped_column(String, unique=True, index=True)
    hashed_password: Mapped[str] = mapped_column(String)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    # فیلدهای جدید برای تحلیل پروفایل کاربر
    interests: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)
    personality_traits: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)
    mood_patterns: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/`
- `npm run test -- frontend/src/__tests__/RecommendationPanel.test.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **پیچیدگی مدل داده و Migration**: افزودن فیلدهای JSONB به مدل `User` یا `UserContext` می‌تواند پیچیدگی کوئری‌ها و ایندکس‌گذاری را افزایش دهد. تغییرات در `app/models/user.py` نیاز به Alembic migration دارد که باید به دقت تست شود تا از از دست رفتن یا خراب شدن داده‌ها جلوگیری شود.
2.  **عملکرد و هزینه مدل‌های AI**: جنبه 'تحلیل روحیات و شخصیت' مستلزم استفاده از مدل‌های پیشرفته NLP/AI (احتمالاً APIهای خارجی مانند OpenAI) است. این می‌تواند تأخیر ایجاد کند، هزینه‌های عملیاتی را افزایش دهد و نیاز به مهندسی دقیق پرامپت در `app/services/ai/nlp_service.py` و `app/services/ai/content_analysis_service.py` برای اطمینان از نتایج دقیق و بی‌طرفانه دارد.
3.  **ادغام با موتور پیشنهاددهنده موجود**: تغییر `app/services/ai/recommendation_service.py` و `app/services/recommendation_engine.py` برای گنجاندن داده‌های پروفایل جدید باید با دقت انجام شود تا منطق پیشنهاددهی موجود مختل نشود.
4.  **نگرانی‌های حریم خصوصی**: ذخیره و تحلیل داده‌های حساس کاربر مانند ویژگی‌های شخصیتی و الگوهای روحیاتی، نگرانی‌های مربوط به حریم خصوصی را افزایش می‌دهد. مکانیسم‌های مناسب ناشناس‌سازی داده‌ها، رضایت کاربر و اقدامات امنیتی باید در `app/routes/users.py` و `app/services/ai_service.py` در نظر گرفته شوند.
5.  **واکشی داده در فرانت‌اند**: فایل `frontend/src/lib/api.js` به فراخوانی‌های API جدید نیاز خواهد داشت و `frontend/src/components/RecommendationPanel.jsx` باید حالت‌های بارگذاری و خطاهای احتمالی از endpoint جدید پیشنهادات شخصی‌سازی‌شده را مدیریت کند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 4: توسعه موتور تولید پیشنهادات متنوع بر اساس علایق و سلیقه‌ها

**Scope:** این مرحله بر پیاده‌سازی سیستمی تمرکز دارد که با استفاده از علایق و سلیقه‌های تحلیل شده در پروفایل کاربر (مرحله ۴)، پیشنهادات متنوع و مرتبطی را تولید کند. این موتور باید قادر باشد پیشنهادات را در زمینه‌های مختلف مرتبط با علایق کاربر ارائه دهد و از تکرار یا ارائه پیشنهادات کلیشه‌ای پرهیز کند. هدف، ارائه پیشنهادات شخصی‌سازی شده و با کیفیت است.
**Key terms:** پیشنهادات مختلف نسبت به اون موضوع بدن, موتور تولید پیشنهادات, علائق من

**بخش مربوط از متن کاربر:**
```
حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

## 🎯 هدف (خلاصه ساختاریافته)
توسعه موتور تولید پیشنهادات شخصی‌سازی‌شده و دقیق بر اساس تحلیل روانشناختی کاربر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/context.py` — `UserContext, Recommendation` — افزودن فیلدهایی مانند `personality_traits`, `mood_history`, `career_interests`, `general_interests` به `UserContext` و `recommendation_type`, `source_context` به `Recommendation` برای ذخیره داده‌های تحلیل‌شده و دسته‌بندی پیشنهادات. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  class UserContext(Base):
      __tablename__ = "user_contexts"
      id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
      user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
      location_lat: Mapped[Optional[Numeric(10, 6)]] = mapped_column(Numeric(10, 6), nullable=True)
      location_lng: Mapped[Optional[Numeric(10, 6)]] = mapped_column(Numeric(10, 6), nullable=True)
      last_activity_at: Mapped[Optional[DateTime(timezone=True)]] = mapped_column(DateTime(timezone=True), nullable=True)
  
  class Recommendation(Base):
      __tablename__ = "recommendations"
      id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
      user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
      content: Mapped[str] = mapped_column(String)
      category: Mapped[str] = mapped_column(String)
  ```
- `app/services/ai/nlp_service.py` — `NLPService` — توسعه این سرویس برای افزودن متدهایی جهت تحلیل روانشناختی و استخراج ویژگی‌های شخصیتی و روحیات کاربر از ورودی‌های متنی. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  # Hypothetical existing structure
  class NLPService:
      def __init__(self, db: AsyncSession):
          self.db = db
  
      async def analyze_text_for_sentiment(self, text: str) -> dict:
          # Placeholder for existing sentiment analysis
          return {"sentiment": "neutral"}
  
      async def extract_entities(self, text: str) -> list[str]:
          # Placeholder for existing entity extraction
          return []
  ```
- `app/services/ai/recommendation_service.py` — `RecommendationService` — اصلاح این سرویس برای استفاده از داده‌های تحلیل‌شده `UserContext` و فراخوانی مدل‌های AI برای تولید پیشنهادات دقیق و متنوع. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  # Hypothetical existing structure
  class RecommendationService:
      def __init__(self, db: AsyncSession):
          self.db = db
  
      async def get_user_recommendations(self, user_id: int) -> list[Recommendation]:
          # Placeholder for existing recommendation retrieval
          return []
  
      async def generate_basic_recommendations(self, user_id: int, context: UserContext) -> list[Recommendation]:
          # Placeholder for existing recommendation generation
          return []
  ```
- `app/routes/context.py` — `router` — افزودن پارامترهای فیلتر (مانند `recommendation_type`) به endpoint `/recommendations` و فراخوانی منطق جدید تولید پیشنهاد. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  from fastapi import APIRouter, Depends, Query
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.dependencies.auth import get_current_active_user
  from app.database import get_db
  # from app.schemas.context import RecommendationSchema # Assuming this exists
  # from app.services.ai.recommendation_service import RecommendationService
  
  router = APIRouter(tags=["context"])
  
  @router.get("/recommendations") # response_model=list[RecommendationSchema]
  async def get_recommendations_for_user(
      current_user: User = Depends(get_current_active_user),
      db: AsyncSession = Depends(get_db),
  ):
      # Existing logic to fetch recommendations
      # service = RecommendationService(db)
      # recommendations = await service.get_user_recommendations(current_user.id)
      return [] # recommendations
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی شده: `fastapi`, `react`, `vite`.
کتابخانه‌های مرتبط: `SQLAlchemy` برای ORM، `Pydantic` برای اعتبارسنجی داده‌ها و تعریف اسکیماها، `python-jose` و `passlib` برای احراز هویت، `httpx` برای فراخوانی APIهای خارجی (AI providers)، و احتمالاً `OpenAI` SDK (اگرچه مستقیماً در `requirements.txt` نیست، اما `OPENAI_API_KEY` در `.env.example` نشان‌دهنده استفاده از آن است).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/user.py` (سطر -1) — `UserContext` model دارای کلید خارجی به `users.id` است. داده‌های کاربر برای پروفایل‌سازی اساسی هستند.
- `app/schemas/ai_schema.py` (سطر -1) — اسکیماهای جدید برای به‌روزرسانی `UserContext` و انواع جدید پیشنهادات مورد نیاز خواهد بود.
- `app/services/ai/model_service.py` (سطر -1) — سرویس‌های NLP و پیشنهاددهنده با مدل‌های AI که توسط این سرویس مدیریت می‌شوند، تعامل خواهند داشت.
- `app/config.py` (سطر 16) — کلیدهای API AI (مانند `OPENAI_API_KEY`) و پرچم‌های ویژگی (`FEATURE_AI_ENABLED`) در اینجا پیکربندی شده و توسط سرویس‌های AI استفاده خواهند شد.
- `frontend/src/components/RecommendationPanel.jsx` (سطر -1) — این کامپوننت مسئول نمایش پیشنهادات است و برای نمایش انواع جدید و خاص پیشنهادات نیاز به به‌روزرسانی دارد.
- `frontend/src/pages/Recommendations.jsx` (سطر -1) — این صفحه میزبان `RecommendationPanel` است و برای واکشی و نمایش انواع جدید پیشنهادات نیاز به به‌روزرسانی دارد.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به شدت به `app/models/context.py` برای تعریف مدل‌های `UserContext` و `Recommendation` وابسته است. `app/services/ai/nlp_service.py` (یا یک سرویس مشابه) برای تحلیل متن و استخراج ویژگی‌های روانشناختی کاربر، از `app/services/ai/model_service.py` برای تعامل با مدل‌های AI خارجی استفاده خواهد کرد. `app/services/ai/recommendation_service.py` و `app/services/recommendation_engine.py` (که هر دو به `app/models/context.py` و `app/models/user.py` وابسته هستند) منطق اصلی تولید پیشنهاد را بر اساس داده‌های تحلیل‌شده از `UserContext` پیاده‌سازی خواهند کرد. `app/routes/context.py` این قابلیت‌ها را از طریق API در دسترس قرار می‌دهد و به `app/dependencies/auth.py` برای احراز هویت کاربر وابسته است. `app/config.py` تنظیمات مربوط به APIهای AI را فراهم می‌کند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست توسعه یک "موتور تولید پیشنهادات" را دارد که قادر باشد "پیشنهادات متنوع و مرتبطی را تولید کند" و "از تکرار یا ارائه پیشنهادات کلیشه‌ای پرهیز کند". هدف اصلی "ارائه پیشنهادات شخصی‌سازی شده و با کیفیت" است. این موتور باید بر اساس "علایق و سلیقه‌های تحلیل شده در پروفایل کاربر (مرحله ۴)" عمل کند.
به‌طور خاص، کاربر می‌خواهد سیستم "حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای."

این درخواست نیازمند گسترش قابلیت‌های موجود در بخش‌های AI و Context پروژه است. در حال حاضر، پروژه دارای مدل‌های `UserContext` و `Recommendation` در `app/models/context.py` است که توسط کامیت `d8dec1f feat(context): smart-assistant backend — UserContext/Recommendation models, location + recommendations endpoints, fusion engine, Maps service (task 2165524b)` اضافه شده‌اند. همچنین، سرویس‌های `app/services/ai/recommendation_service.py` و `app/services/recommendation_engine.py` برای مدیریت و تولید پیشنهادات وجود دارند. بخش `app/services/ai/nlp_service.py` می‌تواند برای تحلیل "روحیات" و "شخصیت" کاربر مورد استفاده قرار گیرد.
برای ذخیره اطلاعات تحلیل‌شدهٔ روانشناختی و علایق دقیق‌تر، نیاز به افزودن فیلدهایی به مدل `UserContext` در `app/models/context.py` وجود دارد. سپس، `app/services/ai/nlp_service.py` باید برای انجام تحلیل‌های عمیق‌تر بر روی داده‌های کاربر (مانند ورودی‌های ژورنال، توضیحات وظایف) توسعه یابد تا "روحیات" و "شخصیت" کاربر را استخراج کند.
در نهایت، `app/services/recommendation_engine.py` و `app/services/ai/recommendation_service.py` باید به‌روزرسانی شوند تا از این داده‌های تحلیل‌شده برای تولید "پیشنهادات مختلف نسبت به اون موضوع" و "نظر شغلی و چیزای مختلف" استفاده کنند، با تأکید بر "خیلی دقیق، نه به صورت کلیشه‌ای". این پیشنهادات باید در `app/models/recommendation.py` ذخیره شوند و از طریق `app/routes/context.py` در دسترس قرار گیرند. فرانت‌اند نیز در `frontend/src/components/RecommendationPanel.jsx` و `frontend/src/pages/Recommendations.jsx` نیاز به به‌روزرسانی برای نمایش این پیشنهادات جدید دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل `UserContext` در `app/models/context.py` باید شامل فیلدهای `personality_traits` (JSON), `mood_history` (JSON), `career_interests` (JSON) و `general_interests` (JSON) باشد.
- [ ] مدل `Recommendation` در `app/models/context.py` باید شامل فیلد `type` (String) و `source_context` (JSON) باشد.
- [ ] سرویس `nlp_service.py` (یا سرویس جدید تحلیل شخصیت) باید متدی برای تحلیل متن کاربر و به‌روزرسانی `UserContext` با ویژگی‌های روانشناختی داشته باشد.
- [ ] موتور تولید پیشنهادات (در `app/services/ai/recommendation_service.py` یا `app/services/recommendation_engine.py`) باید بتواند پیشنهادات شغلی (career advice) و پیشنهادات متنوع دیگر را بر اساس `UserContext` تحلیل‌شده تولید کند.
- [ ] endpoint `GET /api/context/recommendations` باید امکان فیلتر کردن پیشنهادات بر اساس `type` (مثلاً `?type=career`) را فراهم کند و پاسخ شامل فیلد `type` برای هر پیشنهاد باشد.
- [ ] پیشنهادات تولید شده نباید تکراری یا کلیشه‌ای باشند (نیاز به بررسی کیفی).
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1.  **مدل‌سازی داده:**
    *   افزودن فیلدهای جدید (مانند `personality_traits: JSON`, `mood_history: JSON`, `career_interests: JSON`, `general_interests: JSON`) به مدل `UserContext` در `app/models/context.py` برای ذخیره تحلیل‌های روانشناختی و علایق شغلی.
    *   افزودن فیلد `recommendation_type: str` و `source_context: JSON` به مدل `Recommendation` در `app/models/context.py` برای دسته‌بندی پیشنهادات (مثلاً 'career', 'hobby', 'personal_growth') و ذخیره منبع پیشنهاد.
2.  **سرویس تحلیل روانشناختی:**
    *   توسعه `app/services/ai/nlp_service.py` یا ایجاد یک سرویس جدید (مثلاً `app/services/ai/personality_analysis_service.py`) برای تحلیل ورودی‌های متنی کاربر (مانند توضیحات وظایف، یادداشت‌ها) با استفاده از مدل‌های AI (مانند OpenAI) برای استخراج روحیات و ویژگی‌های شخصیتی.
    *   این سرویس باید نتایج تحلیل را در فیلدهای جدید `UserContext` ذخیره کند.
3.  **بهبود موتور پیشنهادات:**
    *   اصلاح `app/services/recommendation_engine.py` و `app/services/ai/recommendation_service.py` برای استفاده از داده‌های `UserContext` (شامل علایق، روحیات و شخصیت) در فرآیند تولید پیشنهاد.
    *   پیاده‌سازی منطق برای تولید پیشنهادات "خیلی دقیق، نه به صورت کلیشه‌ای" و "متنوع" در زمینه‌های مختلف (شغلی، شخصی، سرگرمی).
    *   استفاده از `app/services/ai/model_service.py` برای تعامل با مدل‌های AI خارجی جهت تولید محتوای پیشنهاد.
4.  **API Endpoint:**
    *   به‌روزرسانی یا افزودن endpoint در `app/routes/context.py` برای دریافت پیشنهادات بر اساس فیلترهای جدید (مانند `recommendation_type`).
5.  **فرانت‌اند (خارج از scope این تسک):**
    *   `frontend/src/components/RecommendationPanel.jsx` و `frontend/src/pages/Recommendations.jsx` نیاز به به‌روزرسانی برای نمایش این پیشنهادات جدید دارند.

## 💡 نمونه‌های قبل/بعد
**افزودن فیلدهای تحلیل روانشناختی و علایق به `UserContext` و `type` به `Recommendation` در `app/models/context.py`**

_قبل:_
```
class UserContext(Base):
    __tablename__ = "user_contexts"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
    location_lat: Mapped[Optional[Numeric(10, 6)]] = mapped_column(Numeric(10, 6), nullable=True)
    location_lng: Mapped[Optional[Numeric(10, 6)]] = mapped_column(Numeric(10, 6), nullable=True)
    last_activity_at: Mapped[Optional[DateTime(timezone=True)]] = mapped_column(DateTime(timezone=True), nullable=True)

class Recommendation(Base):
    __tablename__ = "recommendations"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
    content: Mapped[str] = mapped_column(String)
    category: Mapped[str] = mapped_column(String)
```

_بعد:_
```
from sqlalchemy.dialects.postgresql import JSONB # Assuming PostgreSQL for JSON support
# ... other imports ...

class UserContext(Base):
    __tablename__ = "user_contexts"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
    location_lat: Mapped[Optional[Numeric(10, 6)]] = mapped_column(Numeric(10, 6), nullable=True)
    location_lng: Mapped[Optional[Numeric(10, 6)]] = mapped_column(Numeric(10, 6), nullable=True)
    last_activity_at: Mapped[Optional[DateTime(timezone=True)]] = mapped_column(DateTime(timezone=True), nullable=True)
    # New fields for psychological and interest profiling
    personality_traits: Mapped[Optional[JSONB]] = mapped_column(JSONB, nullable=True, default={})
    mood_history: Mapped[Optional[JSONB]] = mapped_column(JSONB, nullable=True, default=[])
    career_interests: Mapped[Optional[JSONB]] = mapped_column(JSONB, nullable=True, default=[])
    general_interests: Mapped[Optional[JSONB]] = mapped_column(JSONB, nullable=True, default=[])

class Recommendation(Base):
    __tablename__ = "recommendations"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
    content: Mapped[str] = mapped_column(String)
    category: Mapped[str] = mapped_column(String)
    type: Mapped[Optional[str]] = mapped_column(String(64), nullable=True) # e.g., 'career', 'hobby', 'personal_growth'
    source_context: Mapped[Optional[JSONB]] = mapped_column(JSONB, nullable=True) # Context that led to this recommendation
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_nlp_service.py`
- `pytest backend/tests/test_recommendation_service.py`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **پیچیدگی مدل‌سازی AI:** تحلیل دقیق "روحیات" و "شخصیت" و ارائه "نظر شغلی" نیازمند مدل‌های AI پیشرفته و احتمالاً fine-tuning است که ممکن است پیچیدگی و هزینه را افزایش دهد.
2.  **کیفیت و دقت پیشنهادات:** تضمین اینکه پیشنهادات "خیلی دقیق، نه به صورت کلیشه‌ای" باشند، چالش‌برانگیز است و به کیفیت داده‌های ورودی و قابلیت‌های مدل AI بستگی دارد.
3.  **حریم خصوصی داده‌ها:** جمع‌آوری و تحلیل داده‌های حساس کاربر (مانند روحیات و شخصیت) نیازمند رعایت دقیق حریم خصوصی و امنیت داده‌ها است.
4.  **وابستگی به APIهای خارجی:** اتکا به سرویس‌های AI خارجی (مانند OpenAI) می‌تواند ریسک‌های مربوط به پایداری، هزینه و تغییرات API را به همراه داشته باشد.
5.  **تأثیر بر عملکرد:** افزایش پیچیدگی منطق تولید پیشنهاد و فراخوانی‌های API خارجی می‌تواند بر زمان پاسخگویی و مصرف منابع backend تأثیر بگذارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 5: توسعه مدل تحلیل روحیات کاربر

**Scope:** این مرحله شامل توسعه یا ادغام مدل‌های هوش مصنوعی است که قادر به تحلیل روحیات و وضعیت عاطفی کاربر باشند. این تحلیل ممکن است بر اساس داده‌های متنی، صوتی، یا رفتاری کاربر انجام شود. هدف، درک وضعیت روحی کاربر برای ارائه پیشنهادات یا راهنمایی‌های متناسب‌تر است. این مدل باید بتواند تغییرات روحیات را نیز تشخیص دهد.
**Key terms:** روحیات منو تحلیل کنن, مدل تحلیل روحیات

**بخش مربوط از متن کاربر:**
```
همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

## 🎯 هدف (خلاصه ساختاریافته)
توسعه مدل تحلیل روحیات و شخصیت کاربر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/ai/nlp_service.py` — `NLPService` — این سرویس نقطه ورود منطقی برای افزودن قابلیت‌های تحلیل متن (مانند sentiment analysis) است. متد `analyze_text` باید توسعه یابد تا مدل‌های پیشرفته‌تر را فراخوانی کند.
  ```python
  class NLPService:
      def __init__(self, db: AsyncSession):
          self.db = db
  
      async def analyze_text(self, text: str) -> dict:
          # Placeholder for actual NLP model integration
          return {"sentiment": "neutral", "keywords": []}
  ```
- `app/services/ai/recommendation_service.py` — `RecommendationService` — این سرویس مسئول تولید توصیه‌ها است. برای ارائه توصیه‌های 'دقیق' و 'غیرکلیشه‌ای' بر اساس روحیات و شخصیت، باید منطق آن با خروجی سرویس تحلیل روحیات ادغام شود.
  ```python
  class RecommendationService:
      def __init__(self, db: AsyncSession):
          self.db = db
          self.engine = RecommendationEngine(db)
  
      async def get_recommendations_for_user(self, user_id: int) -> list[Recommendation]:
          # Current logic for recommendations
          # This needs to be updated to incorporate sentiment/personality data
          pass
  ```
- `app/routes/ai.py` — `router` — این روتر برای Endpointهای مرتبط با AI استفاده می‌شود. Endpointهای جدید برای تحلیل روحیات و بازیابی پروفایل شخصیت باید در اینجا اضافه شوند.
  ```python
  router = APIRouter(prefix="/ai", tags=["ai"])
  
  @router.post("/generate")
  async def generate_text(request: Request, current_user: User = Depends(get_current_active_user)):
      # ... existing AI generation endpoint ...
      pass
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده شامل FastAPI (بک‌اند), React (فرانت‌اند), و Vite (ابزار ساخت فرانت‌اند) است. بک‌اند از SQLAlchemy با `asyncpg` برای تعامل با پایگاه داده و `pydantic` برای اعتبارسنجی داده‌ها استفاده می‌کند. Celery و Redis برای پردازش پس‌زمینه و کشینگ در دسترس هستند که می‌توانند برای پردازش سنگین مدل‌های AI استفاده شوند. برای مدل‌های AI، می‌توان از کتابخانه‌های پایتون مانند `transformers` (Hugging Face) یا `spaCy` استفاده کرد که با `app/services/ai/nlp_service.py` ادغام می‌شوند. `app/config.py` شامل `FEATURE_AI_ENABLED` است که باید برای فعال‌سازی این قابلیت بررسی شود.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/context_engine/audio_context_service.py` (سطر -1) — برای تحلیل روحیات بر اساس داده‌های صوتی، این سرویس باید توسعه یابد یا با سرویس تحلیل روحیات جدید ادغام شود.
- `app/models/ai_assessment.py` (سطر -1) — نتایج تحلیل روحیات و شخصیت می‌تواند به عنوان نوعی 'AI assessment' ذخیره شود، یا این مدل می‌تواند به عنوان پایه برای مدل جدید `UserSentimentProfile` استفاده شود.
- `app/models/user.py` (سطر -1) — پروفایل روحیات و شخصیت باید به یک کاربر خاص مرتبط باشد. مدل `User` ممکن است نیاز به فیلدهای جدید یا ارتباط با مدل `UserSentimentProfile` داشته باشد.
- `frontend/src/components/RecommendationPanel.jsx` (سطر -1) — این کامپوننت مسئول نمایش توصیه‌ها است و باید به‌روزرسانی شود تا توصیه‌های شخصی‌سازی شده بر اساس روحیات کاربر را نمایش دهد.
- `app/main.py` (سطر 291) — فایل اصلی FastAPI که روترهای AI را شامل می‌شود و همچنین در startup migration (خط 291) فیلد `mood_tag` را به جدول `tasks` اضافه می‌کند که نشان‌دهنده زمینه برای ردیابی روحیات است.

## 🌐 نقشهٔ وابستگی‌ها
این ویژگی به شدت به `app/services/ai` و `app/services/context_engine` وابسته است. `app/services/ai/nlp_service.py` برای پردازش متن، `app/services/context_engine/audio_context_service.py` برای پردازش صوت، و `app/services/ai/recommendation_service.py` و `app/services/recommendation_engine.py` برای مصرف نتایج تحلیل و تولید توصیه‌ها، از این سرویس جدید استفاده خواهند کرد. مدل‌های `app/models/user.py` و `app/models/ai_assessment.py` نیز برای ذخیره‌سازی و ارتباط داده‌ها نقش کلیدی دارند. روتر `app/routes/ai.py` نقطه دسترسی API برای این قابلیت‌ها خواهد بود و `app/main.py` مسئول mount کردن این روتر است. در فرانت‌اند، `frontend/src/lib/api.js` برای فراخوانی Endpointهای جدید و `frontend/src/components/RecommendationPanel.jsx` برای نمایش نتایج، به این قابلیت وابسته خواهند بود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست توسعه یا ادغام مدل‌های هوش مصنوعی را دارد که قادر به تحلیل روحیات و وضعیت عاطفی کاربر باشند. این تحلیل باید بر اساس داده‌های متنی، صوتی، یا رفتاری کاربر انجام شود. هدف اصلی، درک عمیق وضعیت روحی کاربر برای ارائه پیشنهادات یا راهنمایی‌های متناسب‌تر و دقیق‌تر است، نه به صورت کلیشه‌ای. مدل باید بتواند تغییرات روحیات را نیز تشخیص دهد و همچنین شخصیت کاربر را روانشناسی کرده و بر اساس آن، نظراتی مانند نظر شغلی و موارد دیگر را به صورت دقیق و شخصی‌سازی شده ترسیم کند.

این درخواست به طور مستقیم با قابلیت‌های موجود در Lifemanager برای 'AI-assisted planning' و 'recommendations' همسو است. پروژه از FastAPI برای بک‌اند و React برای فرانت‌اند استفاده می‌کند و دارای سرویس‌های AI و Context Engine است که می‌توانند برای این منظور توسعه یابند. به عنوان مثال، `app/services/ai/nlp_service.py` می‌تواند برای تحلیل متنی، `app/services/context_engine/audio_context_service.py` برای تحلیل صوتی، و `app/services/context_engine/activity_service.py` برای تحلیل رفتاری مورد استفاده قرار گیرد. خروجی این تحلیل‌ها می‌تواند توسط `app/services/ai/recommendation_service.py` برای ارائه پیشنهادات شخصی‌سازی شده مصرف شود. مدل‌های داده‌ای مانند `app/models/ai_assessment.py` و `app/models/recommendation.py` نیز مرتبط هستند. همچنین، در `app/main.py` در خط 291، ستون `mood_tag` به جدول `tasks` اضافه شده که نشان‌دهنده وجود یک زمینه برای ردیابی روحیات است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک سرویس جدید `app/services/ai/sentiment_personality_service.py` ایجاد شود که متدهای `analyze_and_save_sentiment` و `get_latest_sentiment_profile` را پیاده‌سازی کند.
- [ ] Endpoint `POST /api/ai/sentiment/analyze` با یک `SentimentAnalysisRequestSchema` (شامل فیلدهایی برای متن، لینک صوتی یا نوع رفتار) ورودی را دریافت کرده و یک `UserSentimentProfileSchema` را برگرداند.
- [ ] Endpoint `GET /api/ai/sentiment/profile` آخرین پروفایل روحیات و شخصیت کاربر فعلی را برگرداند (با `UserSentimentProfileSchema`).
- [ ] موتور توصیه‌گر در `app/services/ai/recommendation_service.py` از داده‌های تحلیل شده روحیات و شخصیت برای تولید توصیه‌های دقیق‌تر و شخصی‌سازی شده استفاده کند.
- [ ] کامپوننت `frontend/src/components/RecommendationPanel.jsx` قادر به نمایش توصیه‌هایی باشد که بر اساس تحلیل روحیات و شخصیت کاربر بهبود یافته‌اند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی مدل تحلیل روحیات و شخصیت کاربر، مراحل زیر پیشنهاد می‌شود:

1.  **توسعه سرویس تحلیل روحیات (Backend):** ایجاد `app/services/ai/sentiment_personality_service.py` که مسئول هماهنگی با مدل‌های AI برای تحلیل داده‌های متنی، صوتی و رفتاری باشد. این سرویس از `app/services/ai/nlp_service.py` برای تحلیل متن و از `app/services/context_engine/audio_context_service.py` برای پردازش ورودی‌های صوتی استفاده خواهد کرد.
2.  **مدل‌سازی داده (Backend):** ایجاد یک مدل جدید `app/models/user_sentiment_profile.py` برای ذخیره نتایج تحلیل روحیات و شخصیت کاربر (مانند امتیازات احساسی، ویژگی‌های شخصیتی، تغییرات روحیات). این مدل می‌تواند به `User` (در `app/models/user.py`) مرتبط باشد.
3.  **افزودن Endpointهای API (Backend):** اضافه کردن Endpointهای جدید در `app/routes/ai.py` یا یک روتر جدید `app/routes/sentiment.py` برای:
    *   `POST /api/ai/sentiment/analyze`: برای دریافت داده‌های متنی/صوتی/رفتاری و آغاز فرآیند تحلیل.
    *   `GET /api/ai/sentiment/profile`: برای بازیابی آخرین پروفایل روحیات و شخصیت کاربر.
4.  **ادغام با موتور توصیه‌گر (Backend):** به‌روزرسانی `app/services/ai/recommendation_service.py` و `app/services/recommendation_engine.py` برای استفاده از داده‌های تحلیل شده روحیات و شخصیت کاربر جهت تولید توصیه‌های دقیق‌تر و شخصی‌سازی شده، از جمله توصیه‌های شغلی.
5.  **به‌روزرسانی فرانت‌اند (Frontend):** ایجاد یا به‌روزرسانی کامپوننت‌های UI در `frontend/src/pages/Dashboard.jsx` یا `frontend/src/components/RecommendationPanel.jsx` برای نمایش بینش‌های روحیات و شخصیت و همچنین توصیه‌های بهبود یافته.

## 💡 نمونه‌های قبل/بعد
**افزودن Endpoint برای تحلیل روحیات در app/routes/ai.py**

_قبل:_
```
# ... existing AI generation endpoint ...
    pass
```

_بعد:_
```
# ... existing AI generation endpoint ...
    pass

@router.post("/sentiment/analyze", response_model=UserSentimentProfileSchema)
async def analyze_user_sentiment(
    data: SentimentAnalysisRequestSchema,
    current_user: User = Depends(get_current_active_user),
    db: AsyncSession = Depends(get_db)
):
    service = SentimentPersonalityService(db)
    profile = await service.analyze_and_save_sentiment(current_user.id, data)
    return profile

@router.get("/sentiment/profile", response_model=UserSentimentProfileSchema)
async def get_user_sentiment_profile(
    current_user: User = Depends(get_current_active_user),
    db: AsyncSession = Depends(get_db)
):
    service = SentimentPersonalityService(db)
    profile = await service.get_latest_sentiment_profile(current_user.id)
    if not profile:
        raise HTTPException(status_code=404, detail="Sentiment profile not found")
    return profile
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/app/services/ai/sentiment_personality_service.py`
- `pytest backend/app/routes/ai.py`
- `npm run test -- frontend/src/components/__tests__/RecommendationPanel.test.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **پیچیدگی مدل‌های AI:** ادغام مدل‌های پیشرفته تحلیل روحیات و شخصیت (به ویژه برای زبان فارسی) می‌تواند پیچیده و نیازمند منابع محاسباتی بالا باشد. این امر ممکن است بر عملکرد کلی بک‌اند تأثیر بگذارد و نیاز به بهینه‌سازی و استفاده از Celery برای پردازش‌های ناهمگام داشته باشد.
2. **حریم خصوصی داده‌ها:** جمع‌آوری و تحلیل داده‌های حساس روحیات و شخصیت کاربر نیازمند رعایت دقیق حریم خصوصی و امنیت داده‌ها است. ذخیره‌سازی این اطلاعات در `app/models/user_sentiment_profile.py` باید با دقت انجام شود و دسترسی به آن محدود باشد.
3. **دقت و سوگیری مدل:** اطمینان از اینکه مدل‌ها توصیه‌های 'دقیق' و 'غیرکلیشه‌ای' ارائه می‌دهند، چالش‌برانگیز است. سوگیری در داده‌های آموزشی می‌تواند منجر به نتایج نادرست یا مضر شود. این امر نیازمند نظارت مداوم و امکان بازخورد کاربر است.
4. **تأثیر بر `app/services/ai/recommendation_service.py`:** تغییرات در این سرویس برای ادغام داده‌های روحیات می‌تواند بر نحوه تولید توصیه‌های موجود تأثیر بگذارد و نیازمند تست‌های رگرسیون جامع است.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 6: توسعه مدل روانشناسی و تحلیل شخصیت کاربر

**Scope:** این مرحله به توسعه یا ادغام مدل‌های هوش مصنوعی می‌پردازد که قادر به انجام روانشناسی و تحلیل شخصیت کاربر باشند. این تحلیل باید عمیق‌تر از صرفاً علایق یا روحیات باشد و به ویژگی‌های پایدار شخصیتی بپردازد. مدل باید بتواند ابعاد مختلف شخصیت را شناسایی کند تا در ترسیم مسیرهای آینده کاربر مورد استفاده قرار گیرد. این مرحله بر روی هسته مدل تحلیل شخصیت تمرکز دارد.
**Key terms:** شخصیت منو روانشناسی کنن, مدل روانشناسی شخصیت

**بخش مربوط از متن کاربر:**
```
شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

## 🎯 هدف (خلاصه ساختاریافته)
توسعه مدل تحلیل شخصیت و مسیر شغلی مبتنی بر هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:1-16` — `router` — این فایل نقطهٔ ورود برای Endpointهای AI است. Endpointهای جدید برای تحلیل شخصیت و توصیه‌های مرتبط در اینجا اضافه خواهند شد.
  ```python
  from fastapi import APIRouter, Depends, HTTPException, status
  from sqlalchemy.ext.asyncio import AsyncSession
  
  from app.dependencies.auth import get_current_active_user
  from app.database import get_session
  from app.models.user import User
  from app.schemas.ai_schema import (
      AIModelConfigCreate,
      AIModelConfigResponse,
      AIModelConfigUpdate,
      AIGenerateRequest,
      AIGenerateResponse,
      AIQueryRequest,
      AIQueryResponse,
  )
  from app.services.ai.model_service import AIModelService
  from app.services.ai_service import AIService
  ```
- `app/services/ai/recommendation_service.py:1-19` — `RecommendationService` — این سرویس مسئول تولید توصیه‌ها است. برای ارائه توصیه‌های مسیر شغلی مبتنی بر شخصیت، باید نتایج تحلیل شخصیت را از سرویس جدید `PersonalityService` دریافت و استفاده کند.
  ```python
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.models.user import User
  from app.models.recommendation import Recommendation
  from app.services.context_engine.orchestrator_service import ContextOrchestratorService
  from app.services.ai.ai_data_access_service import AIDataAccessService
  from app.services.ai_service import AIService
  import logging
  
  logger = logging.getLogger(__name__)
  
  class RecommendationService:
      def __init__(self, session: AsyncSession):
          self.session = session
          self.context_orchestrator = ContextOrchestratorService(session)
          self.ai_data_access = AIDataAccessService(session)
          self.ai_service = AIService(session)
  ```
- `app/models/context.py:1-18` — `UserContext` — مدل `UserContext` داده‌های مختلف کاربر را ذخیره می‌کند. این داده‌ها می‌توانند به عنوان ورودی برای تحلیل شخصیت استفاده شوند. ممکن است نیاز به افزودن فیلدهای جدید یا روابطی برای ذخیرهٔ مستقیم داده‌های شخصیتی در اینجا باشد.
  ```python
  from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, JSON, Text
  from sqlalchemy.orm import relationship
  from sqlalchemy.sql import func
  from app.database import Base
  
  class UserContext(Base):
      __tablename__ = "user_contexts"
  
      id = Column(Integer, primary_key=True, index=True)
      user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
      timestamp = Column(DateTime(timezone=True), server_default=func.now())
      location_data = Column(JSON, nullable=True)
      activity_data = Column(JSON, nullable=True)
      biometric_data = Column(JSON, nullable=True)
      mood_data = Column(JSON, nullable=True)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
پروژه Lifemanager از بک‌اند FastAPI با پایتون و SQLAlchemy برای ORM، و فرانت‌اند React با Vite استفاده می‌کند. قابلیت‌های AI از طریق سرویس‌های مختلف در `app/services/ai` ادغام شده‌اند و از ارائه‌دهندگان AI خارجی (احتمالاً OpenAI) بهره می‌برند. موتور `context_engine` و `recommendation_service` برای جمع‌آوری و پردازش داده‌های کاربر و تولید بینش‌ها وجود دارند که این ویژگی جدید آن‌ها را گسترش خواهد داد.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/main.py` (سطر 616) — روترهای جدید برای Endpointهای تحلیل شخصیت باید در اینجا `include_router` شوند. همچنین، مهاجرت‌های پایگاه داده برای جداول جدید در startup event مدیریت می‌شوند.
- `app/schemas/user_schema.py` (سطر -1) — اگر ویژگی‌های شخصیتی مستقیماً به مدل `User` اضافه شوند یا از طریق یک رابطه جدید لینک شوند، شمای کاربر برای پاسخ‌های API باید به‌روزرسانی شود.
- `frontend/src/components/Sidebar.jsx` (سطر -1) — یک لینک ناوبری جدید برای دسترسی به صفحهٔ پروفایل شخصیت کاربر باید در اینجا اضافه شود.
- `frontend/src/lib/api.js` (سطر -1) — این فایل مسئول مدیریت فراخوانی‌های API از فرانت‌اند است. توابع جدید برای فراخوانی Endpointهای تحلیل شخصیت باید در اینجا اضافه شوند.
- `app/services/ai_service.py` (سطر -1) — سرویس جدید `PersonalityService` احتمالاً از این سرویس عمومی AI برای تعامل با مدل‌های AI پایه (مانند تولید متن یا embedding) استفاده خواهد کرد.

## 🌐 نقشهٔ وابستگی‌ها
قابلیت تحلیل شخصیت به شدت به زیرساخت‌های موجود AI وابسته خواهد بود. سرویس جدید `app/services/ai/personality_service.py` از `app/services/ai_service.py` برای تعامل با ارائه‌دهندگان AI و از `app/services/ai/model_service.py` برای مدیریت پیکربندی مدل‌های AI استفاده خواهد کرد. این سرویس همچنین برای دسترسی به داده‌های کاربر به `app/database.py`، `app/models/user.py` و `app/models/context.py` متکی خواهد بود. روتر `app/routes/ai.py` برای ارائه Endpointهای جدید شخصیت گسترش می‌یابد و به سرویس و شمای جدید وابسته خواهد شد. در فرانت‌اند، `frontend/src/lib/api.js` برای فراخوانی این Endpointهای جدید به‌روزرسانی می‌شود و `frontend/src/components/RecommendationPanel.jsx` توصیه‌های مبتنی بر شخصیت را مصرف خواهد کرد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست توسعه یا ادغام مدل‌های هوش مصنوعی را دارد که قادر به انجام روانشناسی و تحلیل شخصیت کاربر باشند. این تحلیل باید عمیق‌تر از صرفاً علایق یا روحیات باشد و به ویژگی‌های پایدار شخصیتی بپردازد. مدل باید بتواند ابعاد مختلف شخصیت را شناسایی کند تا در ترسیم مسیرهای آینده کاربر مورد استفاده قرار گیرد. این مرحله بر روی هسته مدل تحلیل شخصیت تمرکز دارد. کاربر به صراحت می‌گوید: «شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.» کلیدواژه‌های اصلی شامل «مدل روانشناسی شخصیت» و «شخصیت منو روانشناسی کنن» هستند. تأکید بر دقت بالا و عدم کلیشه‌ای بودن تحلیل و توصیه‌ها بسیار مهم است.

پروژه Lifemanager از قبل دارای زیرساخت‌های هوش مصنوعی و مدیریت Context کاربر است. کامیت `d8dec1f` نشان می‌دهد که مدل‌های `UserContext` و `Recommendation` به همراه یک «fusion engine» و endpointهای مربوط به `location` و `recommendations` اضافه شده‌اند. این «fusion engine» می‌تواند نقطهٔ مناسبی برای ادغام نتایج تحلیل شخصیت باشد. همچنین، کامیت `df415c8` از وجود `RecommendationPanel` در فرانت‌اند و `bb796ff` از صفحه `AISettings` خبر می‌دهد که بستر لازم برای نمایش نتایج و تنظیمات را فراهم می‌کند. این تسک شامل تعریف مدل‌های جدید برای ذخیرهٔ ویژگی‌های شخصیتی، توسعهٔ سرویس بک‌اند برای انجام تحلیل با استفاده از AI، ادغام این تحلیل با سرویس توصیه‌ها، و ایجاد رابط کاربری فرانت‌اند برای نمایش پروفایل شخصیت و توصیه‌های مرتبط است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل‌های جدید SQLAlchemy به نام‌های `PersonalityTrait` و `PersonalityAssessment` تعریف شده و به مدل `User` لینک شده‌اند.
- [ ] سرویس بک‌اند جدید `app/services/ai/personality_service.py` شامل متد `analyze_user_personality` است که با یک مدل AI تعامل دارد.
- [ ] فایل `app/routes/ai.py` شامل Endpointهای جدید `POST /api/ai/personality/analyze` (با پاسخ 202 Accepted) و `GET /api/ai/personality/profile` (با پاسخ 200 OK و `PersonalityProfileResponse`) است.
- [ ] سرویس `RecommendationService` در `app/services/ai/recommendation_service.py` برای استفاده از نتایج تحلیل شخصیت در تولید توصیه‌ها به‌روزرسانی شده است.
- [ ] یک صفحهٔ فرانت‌اند جدید `frontend/src/pages/PersonalityProfilePage.jsx` برای نمایش پروفایل شخصیت ایجاد شده و از طریق یک آیتم ناوبری جدید در `frontend/src/components/Sidebar.jsx` قابل دسترسی است.
- [ ] سیستم توصیه‌های شغلی/مسیر آینده را بر اساس تحلیل شخصیت به صورت «دقیق، نه به صورت کلیشه‌ای» ارائه می‌دهد.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی قابلیت تحلیل شخصیت و ارائه توصیه‌های مسیر شغلی، مراحل زیر پیشنهاد می‌شود:

1.  **بک‌اند: تعریف مدل‌های جدید SQLAlchemy و شمای Pydantic:**
    *   فایل `app/models/personality.py` را ایجاد کنید تا مدل‌های `PersonalityTrait` (برای ابعاد شخصیتی مانند Big Five) و `PersonalityAssessment` (برای ذخیرهٔ نتایج تحلیل شخصیت کاربر) را تعریف کند. این مدل‌ها باید به `User` و `AIModelConfig` لینک شوند.
    *   `app/models/user.py` را به‌روزرسانی کنید تا یک رابطه (relationship) به `PersonalityAssessment` داشته باشد.
    *   فایل `app/schemas/personality_schema.py` را ایجاد کنید تا مدل‌های Pydantic برای درخواست‌ها و پاسخ‌های مربوط به تحلیل شخصیت (مانند `PersonalityAnalyzeRequest`, `PersonalityProfileResponse`, `CareerRecommendationResponse`) را شامل شود.

2.  **بک‌اند: پیاده‌سازی سرویس تحلیل شخصیت:**
    *   فایل `app/services/ai/personality_service.py` را ایجاد کنید. این سرویس مسئول هماهنگی با یک ارائه‌دهندهٔ AI خارجی (از طریق `app/services/ai_service.py`) برای انجام تحلیل شخصیت خواهد بود.
    *   این سرویس باید داده‌های کاربر (مانند `UserContext` از `app/models/context.py`، `behavior_log`، `tasks`، `projects` و ورودی‌های `self_improvement`) را به عنوان ورودی دریافت کند.
    *   خروجی مدل AI را تجزیه و تحلیل کرده و نتایج `PersonalityAssessment` را در پایگاه داده ذخیره کند.

3.  **بک‌اند: ادغام با موتور توصیه‌ها:**
    *   `app/services/ai/recommendation_service.py` و احتمالاً `app/services/context_engine/orchestrator_service.py` را تغییر دهید تا نتایج تحلیل شخصیت را در منطق تولید توصیه‌ها (به‌ویژه توصیه‌های «مسیر آینده» مانند شغلی) لحاظ کند. «fusion engine» ذکر شده در کامیت `d8dec1f` نقطهٔ ایده‌آلی برای این ادغام است.

4.  **بک‌اند: افزودن Endpointهای API جدید:**
    *   `app/routes/ai.py` را گسترش دهید یا یک فایل جدید `app/routes/personality.py` ایجاد کنید تا Endpointهای زیر را ارائه دهد:
        *   `POST /api/ai/personality/analyze`: برای شروع تحلیل شخصیت کاربر احراز هویت شده.
        *   `GET /api/ai/personality/profile`: برای بازیابی آخرین پروفایل شخصیت کاربر.
        *   `GET /api/ai/personality/career_recommendations`: برای دریافت توصیه‌های شغلی یا مسیر آینده بر اساس پروفایل شخصیت.

5.  **فرانت‌اند: توسعهٔ کامپوننت‌ها و صفحات UI:**
    *   یک صفحهٔ React جدید `frontend/src/pages/PersonalityProfilePage.jsx` برای نمایش جزئیات پروفایل شخصیت ایجاد کنید.
    *   `frontend/src/components/RecommendationPanel.jsx` را به‌روزرسانی کنید تا توصیه‌های شغلی/مسیر آینده مبتنی بر تحلیل شخصیت را به صورت پویا نمایش دهد.
    *   یک لینک ناوبری جدید در `frontend/src/components/Sidebar.jsx` به صفحه `PersonalityProfilePage` اضافه کنید.
    *   `frontend/src/lib/api.js` را به‌روزرسانی کنید تا توابع جدید برای فراخوانی Endpointهای API شخصیت را شامل شود.

6.  **مهاجرت‌های پایگاه داده:**
    *   مهاجرت‌های Alembic را برای ایجاد جداول جدید `personality_traits` و `personality_assessments` و هر ستون لازم به جداول موجود (مانند `users`) ایجاد و اعمال کنید.

## 💡 نمونه‌های قبل/بعد
**افزودن Endpointهای تحلیل شخصیت به app/routes/ai.py**

_قبل:_
```
# app/routes/ai.py (existing router definition)
    router = APIRouter(prefix="/ai", tags=["ai"])

    @router.post("/generate", response_model=AIGenerateResponse)
    async def generate_text(
        request: AIGenerateRequest,
        current_user: User = Depends(get_current_active_user),
        session: AsyncSession = Depends(get_session),
    ):
        # ... existing generation logic ...
        pass
```

_بعد:_
```
# app/routes/ai.py (with new personality endpoints)
    from app.schemas.personality_schema import PersonalityAnalyzeRequest, PersonalityProfileResponse, CareerRecommendationResponse # NEW
    from app.services.ai.personality_service import PersonalityService # NEW

    router = APIRouter(prefix="/ai", tags=["ai"])

    @router.post("/generate", response_model=AIGenerateResponse)
    async def generate_text(
        request: AIGenerateRequest,
        current_user: User = Depends(get_current_active_user),
        session: AsyncSession = Depends(get_session),
    ):
        # ... existing generation logic ...
        pass

    @router.post("/personality/analyze", response_model=PersonalityProfileResponse, status_code=status.HTTP_202_ACCEPTED) # NEW
    async def analyze_personality(
        current_user: User = Depends(get_current_active_user),
        session: AsyncSession = Depends(get_session),
    ):
        """Trigger personality analysis for the current user."""
        personality_service = PersonalityService(session)
        # In a real scenario, this would trigger an async Celery task
        profile = await personality_service.analyze_user_personality(current_user.id)
        return profile

    @router.get("/personality/profile", response_model=PersonalityProfileResponse) # NEW
    async def get_personality_profile(
        current_user: User = Depends(get_current_active_user),
        session: AsyncSession = Depends(get_session),
    ):
        """Retrieve the current user's personality profile."""
        personality_service = PersonalityService(session)
        profile = await personality_service.get_user_personality_profile(current_user.id)
        if not profile:
            raise HTTPException(status_code=404, detail="Personality profile not found")
        return profile

    @router.get("/personality/career_recommendations", response_model=list[CareerRecommendationResponse]) # NEW
    async def get_career_recommendations(
        current_user: User = Depends(get_current_active_user),
        session: AsyncSession = Depends(get_session),
    ):
        """Retrieve career recommendations based on personality."""
        recommendation_service = RecommendationService(session)
        recommendations = await recommendation_service.get_career_recommendations_by_personality(current_user.id)
        return recommendations
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest app/tests/services/ai/test_personality_service.py`
- `pytest app/tests/routes/test_ai.py`
- `npm run test -- frontend/src/__tests__/pages/PersonalityProfilePage.test.jsx`
- `npm run test -- frontend/src/components/__tests__/Sidebar.test.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **حریم خصوصی و امنیت داده:** تحلیل شخصیت شامل داده‌های حساس کاربر است. اطمینان از ناشناس‌سازی مناسب، رضایت کاربر و ذخیره‌سازی امن (مانند رمزنگاری) برای ویژگی‌های شخصیتی در `app/models/personality.py` و `app/services/ai/personality_service.py` حیاتی است.
2. **سوگیری و دقت مدل AI:** کیفیت تحلیل «دقیق، نه به صورت کلیشه‌ای» به شدت به مدل AI زیربنایی بستگی دارد. سوگیری در داده‌های آموزشی می‌تواند منجر به توصیه‌های نادرست یا مضر شود. این یک ریسک برای `app/services/ai/personality_service.py` و ارائه‌دهنده AI خارجی انتخاب شده است.
3. **سربار عملکرد:** تحلیل عمیق شخصیت می‌تواند از نظر محاسباتی سنگین باشد. اجرای مکرر یا برای تعداد زیادی کاربر می‌تواند بر عملکرد بک‌اند، به‌ویژه در `app/services/ai/personality_service.py` و `app/services/ai_service.py` هنگام فراخوانی APIهای خارجی، تأثیر بگذارد. استفاده از پردازش ناهمزمان (مانند Celery tasks) باید در نظر گرفته شود.
4. **تکامل شمای پایگاه داده:** افزودن جداول جدید و احتمالاً ستون‌ها به `UserContext` یا `User` نیازمند مهاجرت‌های دقیق Alembic است تا از از دست رفتن داده یا خرابی در استقرار جلوگیری شود. این امر بر مهاجرت‌های startup در `app/main.py` و اسکریپت‌های جدید Alembic تأثیر می‌گذارد.
5. **پیچیدگی ادغام:** ادغام نتایج تحلیل شخصیت در `RecommendationService` و `ContextOrchestratorService` موجود نیازمند طراحی دقیق است تا اطمینان حاصل شود که «fusion engine» از داده‌های جدید به درستی استفاده می‌کند و رگرسیون ایجاد نمی‌شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 7: ادغام و تحلیل جامع روحیات و شخصیت در پروفایل کاربر

**Scope:** این مرحله شامل ادغام خروجی‌های مدل‌های تحلیل روحیات (مرحله ۶) و شخصیت (مرحله ۷) با پروفایل موجود کاربر (مرحله ۴) است. هدف، ایجاد یک دیدگاه جامع و یکپارچه از وضعیت روحی و ویژگی‌های شخصیتی کاربر در کنار علایق و سلیقه‌های اوست. این تحلیل جامع، پایه و اساس ترسیم آینده شغلی و سایر پیشنهادات بلندمدت خواهد بود. این مرحله بر روی همگام‌سازی و تحلیل ترکیبی این ابعاد تمرکز دارد.
**Key terms:** همین چیزا رو تحلیل کنن, پروفایل کاربر, تحلیل جامع

**بخش مربوط از متن کاربر:**
```
همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

## 🎯 هدف (خلاصه ساختاریافته)
ادغام تحلیل روحیات و شخصیت در پروفایل کاربر برای توصیه‌های شغلی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/ai_assessment.py:15-25` — `AIAssessment` — این مدل مرکزی برای ذخیره نتایج ارزیابی‌های تولید شده توسط هوش مصنوعی است و برای خروجی‌های روحیات و شخصیت ایده‌آل است.
  ```python
  class AIAssessment(Base):
      __tablename__ = "ai_assessments"
      id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
      user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
      assessment_type: Mapped[str] = mapped_column(String(64), index=True)
      openness: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
      conscientiousness: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
      extraversion: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
      agreeableness: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
      neuroticism: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
      sentiment_score: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
      dominant_emotion: Mapped[Optional[str]] = mapped_column(String(64), nullable=True)
      mood_timestamp: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)
  ```
- `app/schemas/ai_schema.py:50-65` — `HolisticAssessmentCreate` — تعریف ساختار داده برای دریافت و بازگرداندن داده‌های ارزیابی جامع از طریق API.
  ```python
  from pydantic import BaseModel
  from typing import Optional
  from datetime import datetime
  
  class HolisticAssessmentCreate(BaseModel):
      user_id: int
      assessment_type: str = "holistic_profile"
      openness: Optional[float] = None
      conscientiousness: Optional[float] = None
      extraversion: Optional[float] = None
      agreeableness: Optional[float] = None
      neuroticism: Optional[float] = None
      sentiment_score: Optional[float] = None
      dominant_emotion: Optional[str] = None
      mood_timestamp: Optional[datetime] = None
  
  class HolisticAssessmentResponse(HolisticAssessmentCreate):
      id: int
  ```
- `app/routes/ai.py:100-130` — `create_holistic_assessment` — اضافه کردن endpointهای API برای دریافت و بازیابی داده‌های پروفایل جامع کاربر.
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.dependencies.auth import get_current_active_user
  from app.database import get_db
  from app.models.user import User
  from app.schemas.ai_schema import HolisticAssessmentCreate, HolisticAssessmentResponse
  from app.services.ai.holistic_profile_service import HolisticProfileService # New import
  
  router = APIRouter(prefix="/ai", tags=["ai"])
  
  @router.post("/assessments/holistic_profile", response_model=HolisticAssessmentResponse, status_code=201)
  async def create_holistic_assessment(
      assessment_data: HolisticAssessmentCreate,
      db: AsyncSession = Depends(get_db),
      current_user: User = Depends(get_current_active_user),
  ):
      if assessment_data.user_id != current_user.id and not current_user.is_admin:
          raise HTTPException(status_code=403, detail="Not authorized to create assessment for this user")
      service = HolisticProfileService(db)
      created_assessment = await service.create_or_update_assessment(assessment_data)
      return created_assessment
  
  @router.get("/assessments/holistic_profile/{user_id}", response_model=HolisticAssessmentResponse)
  async def get_holistic_profile(
      user_id: int,
      db: AsyncSession = Depends(get_db),
      current_user: User = Depends(get_current_active_user),
  ):
      if user_id != current_user.id and not current_user.is_admin:
          raise HTTPException(status_code=403, detail="Not authorized to view this user's profile")
      service = HolisticProfileService(db)
      profile = await service.get_holistic_profile(user_id)
      if not profile:
          raise HTTPException(status_code=404, detail="Holistic profile not found")
      return profile
  ```
- `app/services/ai/recommendation_service.py:50-80` — `generate_recommendations` — ادغام داده‌های پروفایل جامع جدید در فرآیند تولید توصیه، که امکان ارائه پیشنهادات شخصی‌سازی‌شده‌تر را فراهم می‌کند.
  ```python
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.models.user import User
  from app.services.ai.holistic_profile_service import HolisticProfileService # New import
  
  class RecommendationService:
      def __init__(self, db: AsyncSession):
          self.db = db
          self.holistic_profile_service = HolisticProfileService(db)
  
      async def generate_recommendations(self, user: User, context: dict) -> list[dict]:
          holistic_profile = await self.holistic_profile_service.get_holistic_profile(user.id)
  
          combined_data = {
              "user_id": user.id,
              "user_bio": user.bio,
              "user_display_name": user.display_name,
              "current_context": context,
              "personality_traits": {
                  "openness": holistic_profile.openness,
                  "conscientiousness": holistic_profile.conscientiousness,
                  "extraversion": holistic_profile.extraversion,
                  "agreeableness": holistic_profile.agreeableness,
                  "neuroticism": holistic_profile.neuroticism,
              } if holistic_profile else {},
              "mood_state": {
                  "sentiment_score": holistic_profile.sentiment_score,
                  "dominant_emotion": holistic_profile.dominant_emotion,
                  "mood_timestamp": holistic_profile.mood_timestamp,
              } if holistic_profile else {},
          }
  
          if holistic_profile and holistic_profile.openness is not None and holistic_profile.conscientiousness is not None:
              if holistic_profile.openness > 0.7 and holistic_profile.conscientiousness > 0.6:
                  return [{"type": "career_path", "recommendation": "مسیر شغلی در حوزه تحقیق و توسعه یا طراحی محصول", "reason": "بر اساس ویژگی‌های شخصیتی خلاق و با وجدان"}]
              elif holistic_profile.extraversion > 0.7 and holistic_profile.agreeableness > 0.6:
                  return [{"type": "career_path", "recommendation": "مسیر شغلی در حوزه فروش، بازاریابی یا مدیریت ارتباط با مشتری", "reason": "بر اساس ویژگی‌های شخصیتی برون‌گرا و سازگار"}]
          
          return [{"type": "general", "recommendation": "توصیه عمومی بر اساس علایق فعلی"}]
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی شده: fastapi, react, vite. بک‌اند از FastAPI، SQLAlchemy با `asyncio` برای تعاملات پایگاه داده، و Pydantic برای اعتبارسنجی و سریال‌سازی داده‌ها استفاده می‌کند. سرویس‌های هوش مصنوعی از مدل‌های موجود هوش مصنوعی و یک موتور توصیه بهره می‌برند.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/user.py` — پروفایل کاربر موجود، موجودیت اصلی است که این تحلیل‌ها به آن مرتبط می‌شوند. اگرچه داده‌ها در `AIAssessment` ذخیره می‌شوند، اما از نظر مفهومی بخشی از پروفایل کلی کاربر هستند.
- `app/services/ai/ai_data_access_service.py` — این سرویس ممکن است مسئول دسترسی عمومی به داده‌های هوش مصنوعی باشد و `HolisticProfileService` جدید ممکن است با آن تعامل داشته باشد یا الگوهای دسترسی به داده مشابهی را به اشتراک بگذارد.
- `app/services/context_engine/orchestrator_service.py` — روحیات و شخصیت عناصر کلیدی زمینه‌ای هستند. این سرویس ارکستراتور ممکن است مسئول راه‌اندازی تحلیلی باشد که به پروفایل جامع تغذیه می‌شود.
- `app/main.py` (سطر 262) — اگر ستون‌های جدیدی به جداول موجود (مانند `ai_assessments`) اضافه شود، رویداد راه‌اندازی ممکن است برای استقرار در محیط‌های رایگان که Alembic را اجرا نمی‌کنند، به دستورات `ALTER TABLE` نیاز داشته باشد (مشابه خطوط 262-276 و 295-303).
- `frontend/src/pages/Dashboard.jsx` — این یک مکان احتمالی است که در آن یک 'دیدگاه جامع و یکپارچه' از پروفایل کاربر و توصیه‌ها نمایش داده می‌شود. این صفحه endpointهای API جدید را فراخوانی خواهد کرد.

## 🌐 نقشهٔ وابستگی‌ها
تغییرات پیشنهادی یک سرویس جدید به نام `HolisticProfileService` را معرفی می‌کند که برای پایداری داده‌ها به `app/models/ai_assessment.py` و برای اعتبارسنجی داده‌ها به `app/schemas/ai_schema.py` وابسته است. `app/routes/ai.py` این سرویس جدید را برای ارائه endpointهای API جهت مدیریت پروفایل‌های جامع کاربر وارد و استفاده خواهد کرد. نکته مهم این است که `app/services/ai/recommendation_service.py` به مصرف‌کننده `HolisticProfileService` تبدیل خواهد شد تا تولید توصیه‌های خود را با داده‌های روحیات و شخصیت غنی‌تر کند. `app/main.py` ممکن است نیاز به به‌روزرسانی منطق مهاجرت پایگاه داده در زمان راه‌اندازی داشته باشد تا اطمینان حاصل شود که ستون‌های جدید در `ai_assessments` در محیط‌هایی که از Alembic استفاده نمی‌کنند، اضافه می‌شوند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ادغام خروجی‌های مدل‌های تحلیل روحیات (مرحله ۶) و شخصیت (مرحله ۷) را با پروفایل موجود کاربر (مرحله ۴) دارد. هدف اصلی، ایجاد یک دیدگاه جامع و یکپارچه از وضعیت روحی و ویژگی‌های شخصیتی کاربر در کنار علایق و سلیقه‌های اوست. این تحلیل جامع، پایه و اساس ترسیم آینده شغلی و سایر پیشنهادات بلندمدت خواهد بود. این مرحله بر روی همگام‌سازی و تحلیل ترکیبی این ابعاد تمرکز دارد. کاربر به صراحت بیان کرده است: «همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.» این نشان‌دهنده نیاز به یک سیستم بک‌اند پیچیده است که قادر به دریافت، ذخیره و پردازش این خروجی‌های تحلیلی باشد تا موتور توصیه را تغذیه کند و توصیه‌های شخصی‌سازی‌شده و دقیق ارائه دهد.

در کدبیس فعلی، `app/models/user.py` مدل اصلی کاربر را تعریف می‌کند، در حالی که `app/models/ai_assessment.py` برای ذخیره نتایج ارزیابی‌های تولید شده توسط هوش مصنوعی در نظر گرفته شده است. `app/services/ai/recommendation_service.py` مکان منطقی برای تولید توصیه‌ها است و `app/routes/ai.py` تعاملات API مربوط به هوش مصنوعی را مدیریت می‌کند. همچنین، `app/main.py` شامل منطق راه‌اندازی برای تنظیمات شمای پایگاه داده (مانند خطوط 262-276 برای فیلدهای پروفایل `users` و 295-303 برای فیلدهای برنامه‌ریزی `tasks`) است که ممکن است در صورت افزودن ستون‌های جدید به جداول موجود بدون مهاجرت کامل Alembic، مرتبط باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل `AIAssessment` در فایل `app/models/ai_assessment.py` باید با فیلدهای جدید برای ویژگی‌های شخصیتی (`openness`, `conscientiousness`, `extraversion`, `agreeableness`, `neuroticism` از نوع `Float`, nullable) و وضعیت‌های روحی (`sentiment_score` از نوع `Float`, nullable، `dominant_emotion` از نوع `String(64)`, nullable، و `mood_timestamp` از نوع `DateTime(timezone=True)`, nullable) به‌روزرسانی شود.
- [ ] فایل `app/schemas/ai_schema.py` باید شامل شمای Pydantic جدید `HolisticAssessmentCreate` و `HolisticAssessmentResponse` باشد که فیلدهای جدید شخصیت و روحیات را در بر می‌گیرد.
- [ ] یک endpoint `POST /api/ai/assessments/holistic_profile` باید در `app/routes/ai.py` وجود داشته باشد که `HolisticAssessmentCreate` را می‌پذیرد و `HolisticAssessmentResponse` را با وضعیت 201 برمی‌گرداند.
- [ ] یک endpoint `GET /api/ai/assessments/holistic_profile/{user_id}` باید در `app/routes/ai.py` وجود داشته باشد که `HolisticAssessmentResponse` را با وضعیت 200 برمی‌گرداند.
- [ ] متد `generate_recommendations` در `app/services/ai/recommendation_service.py` باید داده‌های پروفایل جامع کاربر (شخصیت و روحیات) را برای توصیه‌های شغلی و بلندمدت بازیابی و استفاده کند.
- [ ] شمای پایگاه داده باید به‌روزرسانی شود تا ستون‌های جدید در جدول `ai_assessments` را شامل شود (از طریق مهاجرت Alembic یا منطق راه‌اندازی `app/main.py`).
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1.  **توسعه `app/models/ai_assessment.py`**: فیلدهایی را به مدل `AIAssessment` اضافه کنید تا داده‌های ساختاریافته برای ویژگی‌های شخصیتی (مانند `openness`, `conscientiousness`, `extraversion`, `agreeableness`, `neuroticism` به عنوان مقادیر اعشاری/صحیح) و وضعیت‌های روحی (مانند `sentiment_score` به عنوان اعشار، `dominant_emotion` به عنوان رشته، `mood_timestamp` به عنوان تاریخ و زمان) ذخیره شود.
2.  **به‌روزرسانی `app/schemas/ai_schema.py`**: یک شمای Pydantic جدید، مثلاً `HolisticAssessmentCreate`، تعریف کنید که فیلدهای جدید در `AIAssessment` را برای دریافت داده‌ها منعکس کند. همچنین یک شمای `HolisticAssessmentResponse` برای پاسخ‌های API ایجاد کنید.
3.  **ایجاد سرویس جدید `app/services/ai/holistic_profile_service.py`**: این سرویس مسئول دریافت، ذخیره‌سازی و تحلیل ترکیبی داده‌های روحیات و شخصیت خواهد بود. این سرویس متدهایی مانند `create_or_update_assessment(user_id: int, data: HolisticAssessmentCreate)` و `get_holistic_profile(user_id: int)` خواهد داشت.
4.  **اصلاح `app/routes/ai.py`**: یک endpoint جدید `POST`، مثلاً `/api/ai/assessments/holistic_profile`، اضافه کنید که `HolisticAssessmentCreate` را می‌پذیرد و از سرویس `holistic_profile_service` جدید برای ذخیره داده‌ها استفاده می‌کند. همچنین یک endpoint `GET` برای بازیابی پروفایل جامع اضافه کنید.
5.  **اصلاح `app/services/ai/recommendation_service.py`**: متد `generate_recommendations` را به‌روزرسانی کنید تا `holistic_profile_service.get_holistic_profile(user_id)` را فراخوانی کرده و داده‌های جامع پروفایل کاربر را بازیابی کند. منطق پیشرفته‌ای را برای استفاده از این داده‌ها جهت تولید توصیه‌های دقیق شغلی و بلندمدت پیاده‌سازی کنید.
6.  **مهاجرت پایگاه داده**: یک اسکریپت مهاجرت Alembic جدید برای افزودن ستون‌های جدید به جدول `ai_assessments` ایجاد کنید. در صورت استفاده از محیط‌های رایگان Render که Alembic را اجرا نمی‌کنند، اضافه کردن دستورات `ALTER TABLE` به رویداد `startup_event` در `app/main.py` برای جدول `ai_assessments`، مشابه مهاجرت‌های موجود (مانند خطوط 262-276 برای جدول `users`) را در نظر بگیرید.

## 💡 نمونه‌های قبل/بعد
**افزودن فیلدهای جدید به مدل AIAssessment**

_قبل:_
```
class AIAssessment(Base):
    __tablename__ = "ai_assessments"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
    assessment_type: Mapped[str] = mapped_column(String(64), index=True)
```

_بعد:_
```
from datetime import datetime
from typing import Optional
from sqlalchemy import DateTime, Float, String, Integer, ForeignKey
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base

class AIAssessment(Base):
    __tablename__ = "ai_assessments"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
    assessment_type: Mapped[str] = mapped_column(String(64), index=True)
    openness: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
    conscientiousness: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
    extraversion: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
    agreeableness: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
    neuroticism: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
    sentiment_score: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
    dominant_emotion: Mapped[Optional[str]] = mapped_column(String(64), nullable=True)
    mood_timestamp: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/app/routes/ai.py`
- `pytest backend/app/services/ai/recommendation_service.py`
- `alembic upgrade head`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **تغییرات شمای پایگاه داده**: اصلاح `app/models/ai_assessment.py` و افزودن ستون‌های جدید نیازمند مدیریت دقیق مهاجرت‌های پایگاه داده است. اگر Alembic اجرا نشود یا منطق `ALTER TABLE` در `app/main.py` به درستی برای `ai_assessments` پیاده‌سازی نشود، ممکن است استقرار‌های موجود با شکست مواجه شوند یا مشکلات یکپارچگی داده ایجاد شود.
2.  **پیچیدگی منطق توصیه**: درخواست برای توصیه‌های شغلی و بلندمدت «دقیق و نه کلیشه‌ای» به معنای پیچیدگی قابل توجهی در `app/services/ai/recommendation_service.py` است. منطق ضعیف پیاده‌سازی شده می‌تواند منجر به پیشنهادات نامربوط یا حتی مضر شود و اعتماد کاربر را تحت تأثیر قرار دهد.
3.  **تأثیر بر عملکرد**: بازیابی و پردازش داده‌های اضافی پروفایل جامع در `recommendation_service.py` می‌تواند تأخیر ایجاد کند، به خصوص اگر تحلیل شامل محاسبات پیچیده یا مجموعه داده‌های بزرگ باشد.
4.  **حریم خصوصی داده‌ها**: ذخیره داده‌های حساس روحیات و شخصیت در `app/models/ai_assessment.py` نیازمند رعایت دقیق سیاست‌های حریم خصوصی و شیوه‌های امن مدیریت داده است. کنترل دسترسی در `app/routes/ai.py` باید قوی باشد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 8: توسعه موتور ترسیم آینده شغلی و مسیرهای زندگی (دقیق و غیرکلیشه‌ای)

**Scope:** این مرحله شامل پیاده‌سازی موتور نهایی است که با استفاده از تمامی اطلاعات تحلیل شده در پروفایل جامع کاربر (علایق، سلیقه‌ها، روحیات، شخصیت - از مراحل ۴ و ۸)، قادر به ترسیم نظرات شغلی و مسیرهای مختلف زندگی باشد. تاکید بر 'خیلی دقیق، نه به صورت کلیشه‌ای' است، به این معنی که موتور باید از الگوریتم‌های پیشرفته برای ارائه پیشنهادات کاملاً شخصی‌سازی شده و عمیق استفاده کند. این موتور باید بتواند سناریوهای مختلف و پتانسیل موفقیت کاربر در هر زمینه را پیش‌بینی کند.
**Key terms:** آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن, موفق‌تر, خیلی دقیق, نه به صورت کلیشه‌ای, موتور ترسیم آینده

**بخش مربوط از متن کاربر:**
```
آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی موتور ترسیم مسیر شغلی و زندگی شخصی‌سازی‌شده

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/ai/recommendation_service.py` — `RecommendationService` — این سرویس مسئول منطق اصلی تولید توصیه‌ها است و باید برای پیاده‌سازی موتور ترسیم آینده شغلی توسعه یابد.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. اما با توجه به نام و کامیت‌های اخیر، این سرویس بهترین مکان برای افزودن منطق اصلی موتور ترسیم آینده شغلی است.
  ```
- `app/routes/ai.py` — `router` — روتر `ai` در `app/main.py` (خطوط 615-616) mount شده است. endpoint جدید برای موتور ترسیم آینده شغلی باید در اینجا تعریف شود.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. اما با توجه به `app/main.py` خطوط 615-616 که `ai.router` را با `prefix="/api"` و بدون آن mount می‌کند، این روتر مکان مناسبی برای افزودن endpoint جدید `POST /api/ai/career_paths` است.
  ```
- `app/schemas/ai_schema.py` — برای تعریف مدل‌های Pydantic برای ورودی و خروجی موتور ترسیم آینده شغلی.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. اما با توجه به نیاز به تعریف ساختار داده برای درخواست و پاسخ موتور ترسیم آینده، این فایل برای تعریف Pydantic schemaهای جدید مناسب است.
  ```
- `frontend/src/components/RecommendationPanel.jsx` — `RecommendationPanel` — کامپوننت موجود برای نمایش توصیه‌ها. می‌تواند به عنوان پایه برای نمایش مسیرهای شغلی استفاده شود.
  ```jsx
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. اما با توجه به کامیت `df415c8` که این کامپوننت را برای نمایش توصیه‌ها معرفی کرده، می‌توان آن را توسعه داد یا یک کامپوننت مشابه برای نمایش مسیرهای شغلی ایجاد کرد.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. بک‌اند از FastAPI و SQLAlchemy برای ORM استفاده می‌کند. فرانت‌اند از React و Vite برای ساخت UI و `axios` برای فراخوانی API بهره می‌برد. Pydantic برای اعتبارسنجی داده‌ها در بک‌اند استفاده می‌شود. Celery و Redis نیز برای پردازش‌های پس‌زمینه و کشینگ در دسترس هستند که ممکن است برای پردازش‌های سنگین AI مورد نیاز باشند.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/user.py` — موتور نیاز به دسترسی به اطلاعات پروفایل جامع کاربر (علایق، سلیقه‌ها، روحیات، شخصیت) دارد که در مدل User یا مدل‌های مرتبط ذخیره می‌شوند.
- `app/config.py` (سطر 16) — برای مدیریت feature flag `FEATURE_AI_ENABLED` و تنظیمات API Key مربوط به AI (مانند `OPENAI_API_KEY`).
- `app/main.py` (سطر 615) — برای اطمینان از mount شدن روتر `ai` و مدیریت خطاهای احتمالی مربوط به AI یا پایگاه داده.
- `frontend/src/lib/api.js` — کامپوننت فرانت‌اند برای فراخوانی endpoint جدید بک‌اند نیاز به استفاده از این ماژول دارد.
- `app/services/ai/model_service.py` — این سرویس مسئول تعامل با مدل‌های AI است و موتور ترسیم آینده شغلی از آن برای پردازش و تولید توصیه‌ها استفاده خواهد کرد.

## 🌐 نقشهٔ وابستگی‌ها
موتور ترسیم آینده شغلی به شدت به `app/models/user.py` برای دسترسی به داده‌های پروفایل کاربر، `app/services/ai/model_service.py` برای تعامل با مدل‌های هوش مصنوعی، و `app/services/ai/ai_data_access_service.py` برای دسترسی به داده‌های مرتبط با AI وابسته خواهد بود. این موتور از طریق `app/routes/ai.py` در بک‌اند expose می‌شود و توسط یک کامپوننت جدید یا توسعه‌یافته در فرانت‌اند (مانند `frontend/src/components/RecommendationPanel.jsx` یا یک `CareerPathPanel.jsx` جدید) مصرف خواهد شد. `app/config.py` نیز برای مدیریت feature flag و کلیدهای API حیاتی است.

## 🔍 Context و وضعیت فعلی
این تسک شامل پیاده‌سازی یک موتور پیشرفته برای ترسیم نظرات شغلی و مسیرهای مختلف زندگی برای کاربر است. کاربر به صراحت درخواست کرده که این موتور باید 'خیلی دقیق، نه به صورت کلیشه‌ای' عمل کند و از تمامی اطلاعات تحلیل شده در پروفایل جامع کاربر (علایق، سلیقه‌ها، روحیات، شخصیت - از مراحل ۴ و ۸) استفاده کند. هدف ارائه پیشنهادات کاملاً شخصی‌سازی شده و عمیق است که قادر به پیش‌بینی سناریوهای مختلف و پتانسیل موفقیت کاربر در هر زمینه باشد. این قابلیت به عنوان 'موتور ترسیم آینده' شناخته می‌شود و باید به کاربر کمک کند تا در زمینه‌های شغلی و زندگی 'موفق‌تر' باشد. این درخواست با توجه به وجود قابلیت‌های 'AI-assisted planning' در پروژه و کامیت‌های اخیر مانند `df415c8 feat(recommendations): complete task 2165524b frontend — RecommendationPanel, LocationTracker, NotificationBell, /recommendations page (AC 5,6,9,10)` و `d8dec1f feat(context): smart-assistant backend — UserContext/Recommendation models, location + recommendations endpoints, fusion engine, Maps service (task 2165524b)`، که نشان‌دهنده زیرساخت‌های موجود برای توصیه‌ها و هوش مصنوعی است، قابل پیاده‌سازی است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک endpoint جدید `POST /api/ai/career_paths` در بک‌اند وجود داشته باشد که با `CareerPathRequest` ورودی گرفته و `CareerPathResponse` برگرداند.
- [ ] پاسخ endpoint `POST /api/ai/career_paths` شامل مسیرهای شغلی و زندگی باشد که بر اساس اطلاعات پروفایل کاربر (علایق، سلیقه‌ها، روحیات، شخصیت) شخصی‌سازی شده و 'کلیشه‌ای' نباشد.
- [ ] یک صفحه جدید در فرانت‌اند (مثلاً `/career-planning`) وجود داشته باشد که نتایج موتور ترسیم آینده شغلی را به صورت خوانا و کاربرپسند نمایش دهد.
- [ ] قابلیت موتور ترسیم آینده شغلی با `FEATURE_AI_ENABLED` در `app/config.py` کنترل شود و در صورت `false` بودن، endpoint مربوطه `403 Forbidden` برگرداند.
- [ ] خطاهای مربوط به عدم دسترسی به سرویس‌های AI خارجی (مانند `OPENAI_API_KEY` نامعتبر) به درستی مدیریت شده و به کاربر اطلاع‌رسانی شود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی موتور ترسیم مسیر شغلی و زندگی شخصی‌سازی‌شده، مراحل زیر پیشنهاد می‌شود:
1.  **توسعه سرویس بک‌اند:** یک سرویس جدید به نام `CareerPathService` در مسیر `app/services/ai/` ایجاد شود (یا `app/services/ai/recommendation_service.py` توسعه یابد). این سرویس مسئول جمع‌آوری داده‌های پروفایل کاربر (علایق، سلیقه‌ها، روحیات، شخصیت) از مدل `app/models/user.py` و سایر مدل‌های مرتبط، پردازش آن‌ها با استفاده از الگوریتم‌های پیشرفته و مدل‌های AI (از طریق `app/services/ai/model_service.py`) و تولید مسیرهای شغلی و زندگی شخصی‌سازی‌شده خواهد بود. این سرویس باید قابلیت پیش‌بینی پتانسیل موفقیت را نیز داشته باشد.
2.  **افزودن Endpoint جدید:** یک endpoint جدید در `app/routes/ai.py` (مثلاً `POST /api/ai/career_paths`) اضافه شود که سرویس `CareerPathService` را فراخوانی کرده و نتایج را به فرانت‌اند برگرداند. این endpoint باید از `app/dependencies/auth.py` برای احراز هویت کاربر استفاده کند.
3.  **تعریف Schemaهای جدید:** Pydantic schemaهای لازم برای درخواست (شامل پارامترهای ورودی برای موتور) و پاسخ (شامل ساختار مسیرهای شغلی و زندگی پیشنهادی) در `app/schemas/ai_schema.py` تعریف شوند.
4.  **توسعه کامپوننت فرانت‌اند:** یک کامپوننت جدید React (مثلاً `CareerPathPanel.jsx`) در `frontend/src/components/` ایجاد شود که مسئول نمایش نتایج دریافتی از endpoint جدید باشد. این کامپوننت می‌تواند از `frontend/src/lib/api.js` برای فراخوانی API استفاده کند.
5.  **افزودن صفحه فرانت‌اند:** یک صفحه جدید (مثلاً `frontend/src/pages/CareerPlanningPage.jsx`) ایجاد شود که کامپوننت `CareerPathPanel` را رندر کند. این صفحه باید در `frontend/src/App.jsx` به عنوان یک مسیر جدید اضافه شود.
6.  **مدیریت Feature Flag:** اطمینان حاصل شود که این قابلیت جدید تحت کنترل `FEATURE_AI_ENABLED` در `app/config.py` قرار گیرد تا بتوان آن را به صورت ایمن فعال یا غیرفعال کرد.

## 💡 نمونه‌های قبل/بعد
**افزودن endpoint جدید در app/routes/ai.py**

_قبل:_
```
```python
# app/routes/ai.py (مثال فرضی)
@router.post("/generate")
async def generate_text(request: Request, current_user: User = Depends(get_current_active_user)):
    # ... existing logic ...
    return {"response": "Generated text"}

@router.get("/configs", response_model=list[AIModelConfigSchema])
async def list_ai_configs(db: AsyncSession = Depends(get_db), current_user: User = Depends(get_current_active_user), provider: str | None = None):
    # ... existing logic ...
    return await ai_data_access_service.get_ai_model_configs(db, current_user.id, provider)
```
```

_بعد:_
```
```python
# app/routes/ai.py
# ... (existing imports and routes) ...
from app.schemas.ai_schema import CareerPathRequest, CareerPathResponse
from app.services.ai.career_path_service import CareerPathService

@router.post("/career_paths", response_model=CareerPathResponse, tags=["ai", "planner"])
async def get_career_paths(
    request_data: CareerPathRequest,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    if not settings.FEATURE_AI_ENABLED:
        raise HTTPException(status_code=403, detail="AI features are not enabled")
    
    service = CareerPathService(db)
    career_paths = await service.generate_career_paths(current_user.id, request_data)
    return CareerPathResponse(paths=career_paths)

# ... (other existing routes) ...
```
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/app/routes/ai.py`
- `pytest backend/app/services/ai/career_path_service.py`
- `npm run test -- frontend/src/pages/CareerPlanningPage.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **پیچیدگی AI:** دستیابی به توصیه‌های 'خیلی دقیق و غیرکلیشه‌ای' نیازمند مدل‌های AI پیچیده و داده‌های آموزشی غنی است که ممکن است زمان‌بر و پرهزینه باشد. این امر می‌تواند بر عملکرد `app/services/ai/recommendation_service.py` و `app/services/ai/model_service.py` تأثیر بگذارد.
2. **حریم خصوصی داده‌ها:** دسترسی به 'پروفایل جامع کاربر (علایق، سلیقه‌ها، روحیات، شخصیت)' از `app/models/user.py` ریسک‌های حریم خصوصی را افزایش می‌دهد. باید اطمینان حاصل شود که داده‌ها به صورت امن پردازش و ذخیره می‌شوند و با مقررات مربوطه مطابقت دارند.
3. **وابستگی به سرویس‌های خارجی:** موتور به `OPENAI_API_KEY` یا سایر سرویس‌های AI خارجی وابسته خواهد بود. هرگونه مشکل در این سرویس‌ها می‌تواند عملکرد `app/routes/ai.py` و `app/services/ai/career_path_service.py` را مختل کند.
4. **بار عملکردی:** پردازش‌های سنگین AI می‌تواند بار زیادی بر سرور بک‌اند وارد کند. باید از مکانیزم‌های کشینگ، پردازش ناهمزمان (با استفاده از Celery) و بهینه‌سازی کوئری‌های پایگاه داده اطمینان حاصل شود تا `app/main.py` و `app/database.py` تحت تأثیر قرار نگیرند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': 'یک جدول جدید به نام `user_interests` در پایگاه داده ایجاد شود که شامل ستون\u200cهای `id`, `user_id`, `interest_type`, `value`, `source`, `confidence_score`, `created_at`, و `updated_at` باشد.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/test_user_interests.py::test_user_interest_table_exists', 'marker': 'db_schema'}}
- [ ] {'text': 'درخواست `POST /api/interests` با داده\u200cهای معتبر، یک علاقه جدید برای کاربر احراز هویت شده ایجاد کند و پاسخ `201 Created` با شیء علاقه ایجاد شده را برگرداند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/api/interests', 'headers': {'Authorization': 'Bearer <VALID_JWT_TOKEN>'}, 'body': {'interest_type': 'hobby', 'value': 'reading'}, 'expected_status': 201, 'required_fields': ['id', 'user_id', 'interest_type', 'value']}}
- [ ] {'text': 'درخواست `GET /api/interests` لیست علایق مربوط به کاربر احراز هویت شده را برگرداند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/interests', 'headers': {'Authorization': 'Bearer <VALID_JWT_TOKEN>'}, 'expected_status': 200, 'required_fields': ['id', 'interest_type', 'value']}}
- [ ] {'text': 'درخواست `DELETE /api/interests/{interest_id}` یک علاقه خاص را حذف کند و پاسخ `204 No Content` برگرداند. اگر علاقه وجود نداشته باشد یا کاربر مالک آن نباشد، `404 Not Found` برگرداند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'DELETE', 'path': '/api/interests/<EXISTING_INTEREST_ID>', 'headers': {'Authorization': 'Bearer <VALID_JWT_TOKEN>'}, 'expected_status': 204}}
- [ ] {'text': 'سرویس `app/services/ai/ai_data_access_service.py` قابلیت بازیابی داده\u200cهای `UserInterest` را داشته باشد (با افزودن یک متد جدید).', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class AIDataAccessService', 'async def get_user_interests'], 'files_hint': ['app/services/ai/ai_data_access_service.py']}}
- [ ] {'text': 'مدل\u200cهای `UserInterest` و `UserTaste` در `app/models/` با فیلدهای `user_id`, `category`, `value`, `confidence_score`, و `is_verified` وجود داشته باشند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class UserInterest(Base):', 'class UserTaste(Base):', 'user_id:', 'category:', 'value:', 'confidence_score:', 'is_verified:'], 'files_hint': ['app/models/user_interest.py', 'app/models/user_taste.py']}}
- [ ] {'text': 'شمای Pydantic برای `UserInterest` و `UserTaste` در `app/schemas/ai_schema.py` یا `app/schemas/user_interest_schema.py` تعریف شده باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class UserInterestSchema(BaseModel):', 'class UserTasteSchema(BaseModel):'], 'files_hint': ['app/schemas/ai_schema.py', 'app/schemas/user_interest_schema.py']}}
- [ ] {'text': 'سرویس `app/services/ai/interest_identification_service.py` با متد `identify_and_verify_interests(user_id: int)` ایجاد شده باشد که علایق و سلیقه\u200cها را شناسایی و ذخیره می\u200cکند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class InterestIdentificationService:', 'async def identify_and_verify_interests'], 'files_hint': ['app/services/ai/interest_identification_service.py']}}
- [ ] {'text': 'endpoint `POST /api/ai/identify_interests` با کد وضعیت 202 (Accepted) پاسخ دهد و فرآیند شناسایی علایق را آغاز کند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/api/ai/identify_interests', 'expected_status': 202, 'required_fields': ['message']}}
- [ ] {'text': 'endpoint `GET /api/users/{user_id}/interests` علایق و سلیقه\u200cهای شناسایی\u200cشده برای کاربر را برگرداند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/users/1/interests', 'expected_status': 200, 'required_fields': ['interests', 'tastes']}}
- [ ] {'text': 'سرویس `app/services/ai/recommendation_service.py` از علایق و سلیقه\u200cهای شناسایی\u200cشده برای تولید توصیه\u200cها استفاده کند.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'backend/tests/test_recommendation_service.py::test_recommendations_use_interests', 'marker': 'verify'}}
- [ ] {'text': 'یک اسکریپت Migration جدید Alembic برای ایجاد جداول `user_interests` و `user_tastes` با موفقیت اجرا شود.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'backend/tests/test_migrations.py::test_new_interest_tables_exist', 'marker': 'verify'}}
- [ ] {'text': 'مدل `User` (در `app/models/user.py`) باید شامل فیلدهای `interests`, `personality_traits`, و `mood_patterns` از نوع JSONB (یا معادل آن برای SQLite) باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class User', 'interests: Mapped', 'personality_traits: Mapped', 'mood_patterns: Mapped'], 'files_hint': ['app/models/user.py']}}
- [ ] {'text': 'یک Alembic migration جدید باید برای افزودن فیلدهای `interests`, `personality_traits`, و `mood_patterns` به جدول `users` ایجاد شود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['ALTER TABLE users ADD COLUMN interests', 'ALTER TABLE users ADD COLUMN personality_traits', 'ALTER TABLE users ADD COLUMN mood_patterns'], 'files_hint': ['alembic/versions/*.py']}}
- [ ] {'text': 'سرویس `app/services/ai/recommendation_service.py` باید متدی برای تولید پیشنهادات شخصی\u200cسازی\u200cشده داشته باشد که از داده\u200cهای پروفایل کاربر استفاده کند.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/services/ai/test_recommendation_service.py::test_personalized_recommendations', 'marker': 'feature'}}
- [ ] {'text': 'یک endpoint جدید `GET /api/ai/personalized_recommendations` باید در `app/routes/ai.py` وجود داشته باشد که پیشنهادات شخصی\u200cسازی\u200cشده را برگرداند و وضعیت 200 را با فیلدهای `id`, `content`, `type`, `score` بازگرداند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/ai/personalized_recommendations', 'expected_status': 200, 'required_fields': ['id', 'content', 'type', 'score']}}
- [ ] {'text': "کامپوننت `frontend/src/components/RecommendationPanel.jsx` باید قادر به نمایش پیشنهادات شخصی\u200cسازی\u200cشده از endpoint جدید باشد و آیتم\u200cهای پیشنهادی با `data-testid='personalized-recommendation-item'` قابل مشاهده باشند.", 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/recommendations'}, {'action': 'wait_for', 'selector': "[data-testid='personalized-recommendation-item']", 'timeout_ms': 5000}, {'action': 'assert_visible', 'selector': "[data-testid='personalized-recommendation-item']"}]}}
- [ ] {'text': 'موتور تحلیل (با استفاده از `app/services/ai/nlp_service.py` و `app/services/ai/content_analysis_service.py`) باید قادر به تحلیل روحیات و شخصیت کاربر بر اساس ورودی\u200cهای متنی باشد و نتایج را در فیلدهای پروفایل کاربر ذخیره کند.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/services/ai/test_profile_analysis_engine.py::test_personality_analysis', 'marker': 'feature'}}
- [ ] {'text': 'مدل `UserContext` در `app/models/context.py` باید شامل فیلدهای `personality_traits` (JSON), `mood_history` (JSON), `career_interests` (JSON) و `general_interests` (JSON) باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class UserContext', 'personality_traits:', 'mood_history:', 'career_interests:', 'general_interests:'], 'files_hint': ['app/models/context.py']}}
- [ ] {'text': 'مدل `Recommendation` در `app/models/context.py` باید شامل فیلد `type` (String) و `source_context` (JSON) باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class Recommendation', 'type:', 'source_context:'], 'files_hint': ['app/models/context.py']}}

## Acceptance Criteria

1. یک جدول جدید به نام `user_interests` در پایگاه داده ایجاد شود که شامل ستون‌های `id`, `user_id`, `interest_type`, `value`, `source`, `confidence_score`, `created_at`, و `updated_at` باشد. _(verify: backend_test)_
2. درخواست `POST /api/interests` با داده‌های معتبر، یک علاقه جدید برای کاربر احراز هویت شده ایجاد کند و پاسخ `201 Created` با شیء علاقه ایجاد شده را برگرداند. _(verify: api_response)_
3. درخواست `GET /api/interests` لیست علایق مربوط به کاربر احراز هویت شده را برگرداند. _(verify: api_response)_
4. درخواست `DELETE /api/interests/{interest_id}` یک علاقه خاص را حذف کند و پاسخ `204 No Content` برگرداند. اگر علاقه وجود نداشته باشد یا کاربر مالک آن نباشد، `404 Not Found` برگرداند. _(verify: api_response)_
5. سرویس `app/services/ai/ai_data_access_service.py` قابلیت بازیابی داده‌های `UserInterest` را داشته باشد (با افزودن یک متد جدید). _(verify: static)_
6. مدل‌های `UserInterest` و `UserTaste` در `app/models/` با فیلدهای `user_id`, `category`, `value`, `confidence_score`, و `is_verified` وجود داشته باشند. _(verify: static)_
7. شمای Pydantic برای `UserInterest` و `UserTaste` در `app/schemas/ai_schema.py` یا `app/schemas/user_interest_schema.py` تعریف شده باشد. _(verify: static)_
8. سرویس `app/services/ai/interest_identification_service.py` با متد `identify_and_verify_interests(user_id: int)` ایجاد شده باشد که علایق و سلیقه‌ها را شناسایی و ذخیره می‌کند. _(verify: static)_
9. endpoint `POST /api/ai/identify_interests` با کد وضعیت 202 (Accepted) پاسخ دهد و فرآیند شناسایی علایق را آغاز کند. _(verify: api_response)_
10. endpoint `GET /api/users/{user_id}/interests` علایق و سلیقه‌های شناسایی‌شده برای کاربر را برگرداند. _(verify: api_response)_
11. سرویس `app/services/ai/recommendation_service.py` از علایق و سلیقه‌های شناسایی‌شده برای تولید توصیه‌ها استفاده کند. _(verify: backend_test)_
12. یک اسکریپت Migration جدید Alembic برای ایجاد جداول `user_interests` و `user_tastes` با موفقیت اجرا شود. _(verify: backend_test)_
13. مدل `User` (در `app/models/user.py`) باید شامل فیلدهای `interests`, `personality_traits`, و `mood_patterns` از نوع JSONB (یا معادل آن برای SQLite) باشد. _(verify: static)_
14. یک Alembic migration جدید باید برای افزودن فیلدهای `interests`, `personality_traits`, و `mood_patterns` به جدول `users` ایجاد شود. _(verify: static)_
15. سرویس `app/services/ai/recommendation_service.py` باید متدی برای تولید پیشنهادات شخصی‌سازی‌شده داشته باشد که از داده‌های پروفایل کاربر استفاده کند. _(verify: backend_test)_
16. یک endpoint جدید `GET /api/ai/personalized_recommendations` باید در `app/routes/ai.py` وجود داشته باشد که پیشنهادات شخصی‌سازی‌شده را برگرداند و وضعیت 200 را با فیلدهای `id`, `content`, `type`, `score` بازگرداند. _(verify: api_response)_
17. کامپوننت `frontend/src/components/RecommendationPanel.jsx` باید قادر به نمایش پیشنهادات شخصی‌سازی‌شده از endpoint جدید باشد و آیتم‌های پیشنهادی با `data-testid='personalized-recommendation-item'` قابل مشاهده باشند. _(verify: ui_interaction)_
18. موتور تحلیل (با استفاده از `app/services/ai/nlp_service.py` و `app/services/ai/content_analysis_service.py`) باید قادر به تحلیل روحیات و شخصیت کاربر بر اساس ورودی‌های متنی باشد و نتایج را در فیلدهای پروفایل کاربر ذخیره کند. _(verify: backend_test)_
19. مدل `UserContext` در `app/models/context.py` باید شامل فیلدهای `personality_traits` (JSON), `mood_history` (JSON), `career_interests` (JSON) و `general_interests` (JSON) باشد. _(verify: static)_
20. مدل `Recommendation` در `app/models/context.py` باید شامل فیلد `type` (String) و `source_context` (JSON) باشد. _(verify: static)_
21. سرویس `nlp_service.py` (یا سرویس جدید تحلیل شخصیت) باید متدی برای تحلیل متن کاربر و به‌روزرسانی `UserContext` با ویژگی‌های روانشناختی داشته باشد. _(verify: backend_test)_
22. موتور تولید پیشنهادات (در `app/services/ai/recommendation_service.py` یا `app/services/recommendation_engine.py`) باید بتواند پیشنهادات شغلی (career advice) و پیشنهادات متنوع دیگر را بر اساس `UserContext` تحلیل‌شده تولید کند. _(verify: backend_test)_
23. endpoint `GET /api/context/recommendations` باید امکان فیلتر کردن پیشنهادات بر اساس `type` (مثلاً `?type=career`) را فراهم کند و پاسخ شامل فیلد `type` برای هر پیشنهاد باشد. _(verify: api_response)_
24. پیشنهادات تولید شده نباید تکراری یا کلیشه‌ای باشند (نیاز به بررسی کیفی). _(verify: manual_only)_
25. یک سرویس جدید `app/services/ai/sentiment_personality_service.py` ایجاد شود که متدهای `analyze_and_save_sentiment` و `get_latest_sentiment_profile` را پیاده‌سازی کند. _(verify: static)_
26. Endpoint `POST /api/ai/sentiment/analyze` با یک `SentimentAnalysisRequestSchema` (شامل فیلدهایی برای متن، لینک صوتی یا نوع رفتار) ورودی را دریافت کرده و یک `UserSentimentProfileSchema` را برگرداند. _(verify: api_response)_
27. Endpoint `GET /api/ai/sentiment/profile` آخرین پروفایل روحیات و شخصیت کاربر فعلی را برگرداند (با `UserSentimentProfileSchema`). _(verify: api_response)_
28. موتور توصیه‌گر در `app/services/ai/recommendation_service.py` از داده‌های تحلیل شده روحیات و شخصیت برای تولید توصیه‌های دقیق‌تر و شخصی‌سازی شده استفاده کند. _(verify: backend_test)_
29. کامپوننت `frontend/src/components/RecommendationPanel.jsx` قادر به نمایش توصیه‌هایی باشد که بر اساس تحلیل روحیات و شخصیت کاربر بهبود یافته‌اند. _(verify: ui_interaction)_
30. مدل‌های جدید SQLAlchemy به نام‌های `PersonalityTrait` و `PersonalityAssessment` تعریف شده و به مدل `User` لینک شده‌اند. _(verify: static)_
31. سرویس بک‌اند جدید `app/services/ai/personality_service.py` شامل متد `analyze_user_personality` است که با یک مدل AI تعامل دارد. _(verify: static)_
32. فایل `app/routes/ai.py` شامل Endpointهای جدید `POST /api/ai/personality/analyze` (با پاسخ 202 Accepted) و `GET /api/ai/personality/profile` (با پاسخ 200 OK و `PersonalityProfileResponse`) است. _(verify: api_response)_
33. سرویس `RecommendationService` در `app/services/ai/recommendation_service.py` برای استفاده از نتایج تحلیل شخصیت در تولید توصیه‌ها به‌روزرسانی شده است. _(verify: static)_
34. یک صفحهٔ فرانت‌اند جدید `frontend/src/pages/PersonalityProfilePage.jsx` برای نمایش پروفایل شخصیت ایجاد شده و از طریق یک آیتم ناوبری جدید در `frontend/src/components/Sidebar.jsx` قابل دسترسی است. _(verify: ui_interaction)_
35. سیستم توصیه‌های شغلی/مسیر آینده را بر اساس تحلیل شخصیت به صورت «دقیق، نه به صورت کلیشه‌ای» ارائه می‌دهد. _(verify: manual_only)_
36. مدل `AIAssessment` در فایل `app/models/ai_assessment.py` باید با فیلدهای جدید برای ویژگی‌های شخصیتی (`openness`, `conscientiousness`, `extraversion`, `agreeableness`, `neuroticism` از نوع `Float`, nullable) و وضعیت‌های روحی (`sentiment_score` از نوع `Float`, nullable، `dominant_emotion` از نوع `String(64)`, nullable، و `mood_timestamp` از نوع `DateTime(timezone=True)`, nullable) به‌روزرسانی شود. _(verify: static)_
37. فایل `app/schemas/ai_schema.py` باید شامل شمای Pydantic جدید `HolisticAssessmentCreate` و `HolisticAssessmentResponse` باشد که فیلدهای جدید شخصیت و روحیات را در بر می‌گیرد. _(verify: static)_
38. یک endpoint `POST /api/ai/assessments/holistic_profile` باید در `app/routes/ai.py` وجود داشته باشد که `HolisticAssessmentCreate` را می‌پذیرد و `HolisticAssessmentResponse` را با وضعیت 201 برمی‌گرداند. _(verify: api_response)_
39. یک endpoint `GET /api/ai/assessments/holistic_profile/{user_id}` باید در `app/routes/ai.py` وجود داشته باشد که `HolisticAssessmentResponse` را با وضعیت 200 برمی‌گرداند. _(verify: api_response)_
40. متد `generate_recommendations` در `app/services/ai/recommendation_service.py` باید داده‌های پروفایل جامع کاربر (شخصیت و روحیات) را برای توصیه‌های شغلی و بلندمدت بازیابی و استفاده کند. _(verify: backend_test)_
41. شمای پایگاه داده باید به‌روزرسانی شود تا ستون‌های جدید در جدول `ai_assessments` را شامل شود (از طریق مهاجرت Alembic یا منطق راه‌اندازی `app/main.py`). _(verify: manual_only)_
42. یک endpoint جدید `POST /api/ai/career_paths` در بک‌اند وجود داشته باشد که با `CareerPathRequest` ورودی گرفته و `CareerPathResponse` برگرداند. _(verify: api_response)_
43. پاسخ endpoint `POST /api/ai/career_paths` شامل مسیرهای شغلی و زندگی باشد که بر اساس اطلاعات پروفایل کاربر (علایق، سلیقه‌ها، روحیات، شخصیت) شخصی‌سازی شده و 'کلیشه‌ای' نباشد. _(verify: backend_test)_
44. یک صفحه جدید در فرانت‌اند (مثلاً `/career-planning`) وجود داشته باشد که نتایج موتور ترسیم آینده شغلی را به صورت خوانا و کاربرپسند نمایش دهد. _(verify: ui_interaction)_
45. قابلیت موتور ترسیم آینده شغلی با `FEATURE_AI_ENABLED` در `app/config.py` کنترل شود و در صورت `false` بودن، endpoint مربوطه `403 Forbidden` برگرداند. _(verify: api_response)_
46. خطاهای مربوط به عدم دسترسی به سرویس‌های AI خارجی (مانند `OPENAI_API_KEY` نامعتبر) به درستی مدیریت شده و به کاربر اطلاع‌رسانی شود. _(verify: backend_test)_

## Task Steps

### Step 1: طراحی و پیاده‌سازی زیرساخت دریافت و ذخیره‌سازی داده‌های ورودی کاربر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی مکانیزم‌هایی است که کاربر بتواند داده‌ها و لیست‌های خود را به برنامه ارائه دهد. این داده‌ها می‌توانند شامل هر نوع اطلاعاتی باشند که برای شناسایی علایق و سلیقه‌ها لازم است. تمرکز بر روی کانال‌های ورودی و ذخیره‌سازی اولیه این داده‌ها برای پردازش‌های بعدی است. این مرحله شامل تحلیل خود داده‌ها نمی‌شود، بلکه فقط جمع‌آوری و نگهداری آن‌ها را پوشش می‌دهد.
**Excerpt:**
```
می‌خوام مدل‌های موجود توی برنامه طوری باشن که از روی مثلاً داده‌هایی که بهشون دادم حالا یا لیست‌هایی که دادن، علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است.
```

### Step 2: توسعه مدل شناسایی علایق کاربر در زمینه‌های مختلف
**Status:** `pending` (0%)
**Scope:** این مرحله بر توسعه یا ادغام مدل‌های هوش مصنوعی تمرکز دارد که قادر به شناسایی علایق کاربر از داده‌های ورودی (جمع‌آوری شده در مرحله قبل) باشند. نکته حیاتی این است که مدل باید توانایی 'اطمینان یافتن' از اینکه یک مورد واقعاً علاقه است یا خیر را داشته باشد و بتواند آن را از موارد بی‌ربط یا موقتی تشخیص دهد. این مدل باید در زمینه‌های مختلف فعالیت کند.
— [merged] این مرحله به توسعه یا ادغام مدل‌های هوش مصنوعی اختصاص دارد که وظیفه شناسایی سلیقه‌های کاربر را بر عهده دارند. سلیقه‌ها ممکن است از علایق متمایز باشند و نیاز به رویکردهای تحلیلی متفاوتی داشته باشند. مدل باید بتواند سلیقه‌ها را در طیف وسیعی از زمینه‌ها تشخیص دهد و آن‌ها را برای استفاده در پروفایل کاربر آماده کند. این مرحله بر روی منطق اصلی شناسایی سلیقه تمرکز دارد.
**Excerpt:**
```
علائقی که از من شناسایی می‌کنن توی زمینه‌های مختلف یعنی هر چیزی که احساس کردن مثلاً یه علاقه‌ای برای از منه و مطمئن بشن که این علاقه است. حالا مثلاً ممکنه یه چیز دیگه‌ای باشه، ممکنه اصلاً به علاقه نداشته باشه اون جدا. ولی مثلاً تشخیص می‌دن این علاقه است یا چیز دیگه است.
```

### Step 3: طراحی و پیاده‌سازی ساختار پروفایل کاربر و موتور تحلیل علایق و سلیقه‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی پایگاه داده و ساختار داده‌ای برای ذخیره و مدیریت پروفایل کاربر است. همچنین، پیاده‌سازی یک موتور تحلیل مرکزی که علایق و سلیقه‌های شناسایی شده (از مراحل ۲ و ۳) را دریافت کرده، آن‌ها را در پروفایل کاربر ادغام و تحلیل می‌کند تا یک دیدگاه جامع و یکپارچه از ترجیحات کاربر ایجاد شود. این تحلیل باید به گونه‌ای باشد که برای تولید پیشنهادات بعدی قابل استفاده باشد.
**Excerpt:**
```
اینو تو پروفایل من تحلیل بکنن و حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن.
```

### Step 4: توسعه موتور تولید پیشنهادات متنوع بر اساس علایق و سلیقه‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله بر پیاده‌سازی سیستمی تمرکز دارد که با استفاده از علایق و سلیقه‌های تحلیل شده در پروفایل کاربر (مرحله ۴)، پیشنهادات متنوع و مرتبطی را تولید کند. این موتور باید قادر باشد پیشنهادات را در زمینه‌های مختلف مرتبط با علایق کاربر ارائه دهد و از تکرار یا ارائه پیشنهادات کلیشه‌ای پرهیز کند. هدف، ارائه پیشنهادات شخصی‌سازی شده و با کیفیت است.
**Excerpt:**
```
حسب علائق من پیشنهادات مختلف نسبت به اون موضوع بدن و همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

### Step 5: توسعه مدل تحلیل روحیات کاربر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه یا ادغام مدل‌های هوش مصنوعی است که قادر به تحلیل روحیات و وضعیت عاطفی کاربر باشند. این تحلیل ممکن است بر اساس داده‌های متنی، صوتی، یا رفتاری کاربر انجام شود. هدف، درک وضعیت روحی کاربر برای ارائه پیشنهادات یا راهنمایی‌های متناسب‌تر است. این مدل باید بتواند تغییرات روحیات را نیز تشخیص دهد.
**Excerpt:**
```
همین‌طور روحیات منو تحلیل کنن، شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

### Step 6: توسعه مدل روانشناسی و تحلیل شخصیت کاربر
**Status:** `pending` (0%)
**Scope:** این مرحله به توسعه یا ادغام مدل‌های هوش مصنوعی می‌پردازد که قادر به انجام روانشناسی و تحلیل شخصیت کاربر باشند. این تحلیل باید عمیق‌تر از صرفاً علایق یا روحیات باشد و به ویژگی‌های پایدار شخصیتی بپردازد. مدل باید بتواند ابعاد مختلف شخصیت را شناسایی کند تا در ترسیم مسیرهای آینده کاربر مورد استفاده قرار گیرد. این مرحله بر روی هسته مدل تحلیل شخصیت تمرکز دارد.
**Excerpt:**
```
شخصیت منو روانشناسی کنن، همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

### Step 7: ادغام و تحلیل جامع روحیات و شخصیت در پروفایل کاربر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ادغام خروجی‌های مدل‌های تحلیل روحیات (مرحله ۶) و شخصیت (مرحله ۷) با پروفایل موجود کاربر (مرحله ۴) است. هدف، ایجاد یک دیدگاه جامع و یکپارچه از وضعیت روحی و ویژگی‌های شخصیتی کاربر در کنار علایق و سلیقه‌های اوست. این تحلیل جامع، پایه و اساس ترسیم آینده شغلی و سایر پیشنهادات بلندمدت خواهد بود. این مرحله بر روی همگام‌سازی و تحلیل ترکیبی این ابعاد تمرکز دارد.
**Excerpt:**
```
همین چیزا رو تحلیل کنن و آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```

### Step 8: توسعه موتور ترسیم آینده شغلی و مسیرهای زندگی (دقیق و غیرکلیشه‌ای)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی موتور نهایی است که با استفاده از تمامی اطلاعات تحلیل شده در پروفایل جامع کاربر (علایق، سلیقه‌ها، روحیات، شخصیت - از مراحل ۴ و ۸)، قادر به ترسیم نظرات شغلی و مسیرهای مختلف زندگی باشد. تاکید بر 'خیلی دقیق، نه به صورت کلیشه‌ای' است، به این معنی که موتور باید از الگوریتم‌های پیشرفته برای ارائه پیشنهادات کاملاً شخصی‌سازی شده و عمیق استفاده کند. این موتور باید بتواند سناریوهای مختلف و پتانسیل موفقیت کاربر در هر زمینه را پیش‌بینی کند.
**Excerpt:**
```
آینده برای من مثلاً نظر شغلی و چیزای مختلف ترسیم بکنن که تو این زمینه مثلاً وارد بشم موفق‌تر، خیلی دقیق، نه به صورت کلیشه‌ای.
```
