---
task_id: 3965b89f-8d60-4e2e-a80a-ca4fddaf3821
title: پیاده‌سازی تنظیمات اطلاع‌رسانی کاربر
type: other
priority: medium
execution_priority: 3000
status: done
external_status: pending
verification_status: done
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T21:25:16.417130+00:00'
updated_at: '2026-05-29T20:35:34.930182+00:00'
archived: true
archived_at: '2026-05-28T12:03:48.677331+00:00'
target_files:
- app/routes/notifications.py
- app/services/notification_service.py
- app/models/user_notification_setting.py
- frontend/src/App.jsx
- frontend/src/components/Sidebar.jsx
- app/models/user.py
- app/models/integration.py
- app/routes/integrations.py
- app/routes/users.py
- frontend/src/pages/Notifications.jsx
- app/main.py
- app/schemas/notification_schema.py
- app/models/notification.py
- app/routes/auth.py
- app/routes/tasks.py
- app/routes/auth_google.py
- docs/API.md
---

# پیاده‌سازی تنظیمات اطلاع‌رسانی کاربر

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_437950_AgADsh0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد • 5,325 char متن_

## هدف اصلی و ارزش پیشنهادی اپلیکیشن  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه.
[00:08] حالا لازم نیست جدا جدا مثلاً توی همون قسمتی
[00:12] با هم باشن یا ذیل تنظیمات هر جا که شلوغش هم نکنیم.
[00:17] و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این
[00:24] و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم
[00:30] بتونم اضافه کنم، لازم نباشه اینا رو تو هندلر اضافه بکنم.
[00:33] تو همون جا بتونم اضافه کنم، ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا
[00:40] با صدا اعلام بشه، چیا بدون صدا اعلام بشه.
[00:44] همه اینا توی اون قسمت تنظیمات و چیزش باشه.
[00:48] حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید
[00:53] تنظیماتشون هم جدا باشه.
[00:55] و آره یه جور داینامیک باشه که هرچی هم اضافه میشه
[01:01] اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## قابلیت‌های محوری و امکانات کلیدی  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات هر جا که شلوغشم نکنیم. و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم آیدی دستگاه‌ها رو هم بتونم اضافه کنم لازم نباشه اینا رو تو رندر اضافه بکنم. تو همونجا بتونم اضافه کنم ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه. همه اینا توی اون قسمت تنظیمات و چیزش باشه. حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه. و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## جامعه هدف و نیازهای خاص دانشجویان  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه.
[00:08] حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات، هر جا که شلوغشم نکنیم.
[00:17] و تمام چیزهایی که توی برنامه هست و قرار بیاد، باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم
[00:30] بتونم اضافه کنم، لازم نباشه اینا رو تو هندلر اضافه بکنم.
[00:33] تو همون جا بتونم اضافه کنم، ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه.
[00:44] همه اینا توی اون قسمت تنظیمات و چیزش باشه.
[00:48] حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه.
[00:55] و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه،
[01:00] اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## مکانیسم‌های اعتمادسازی و تعامل کاربران  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشند یا ذیل تنظیمات هر جا که شلوغش هم نکنیم. و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم آیدی دستگاه‌ها رو هم بتونم اضافه کنم لازم نباشه اینا رو تو هندلر اضافه بکنم. تو همونجا بتونم اضافه کنم ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه. همه اینا توی اون قسمت تنظیمات و چیزش باشه. حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه. و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## معیارهای دسته‌بندی و فیلترینگ کتاب‌ها  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات هر جا که شلوغشم نکنیم. و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم آیدی دستگاه‌ها رو هم بتونم اضافه کنم لازم نباشه اینا رو تو رندر اضافه بکنم. تو همون جا بتونم اضافه کنم ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه. همه اینا توی اون قسمت تنظیمات و چیزش باشه. حالا برای تلگرام و ایمیل هستش یه خورده شرایط هر کدومشون باید تنظیماتشون هم جدا باشه. و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

---
**توضیح:** محتوای فایل صوتی ارائه شده، مربوط به تنظیمات و قابلیت‌های اطلاع‌رسانی از طریق تلگرام و ایمیل است و هیچ اشاره‌ای به "معیارهای دسته‌بندی و فیلترینگ کتاب‌ها" ندارد.

## قابلیت‌های ویژه و پتانسیل توسعه  _(at: audio/ogg)_

### قابلیت‌های ویژه و پتانسیل توسعه

[00:00] گوینده ۱: می‌خوام برای تلگرام، اطلاع‌رسانی تلگرام و ایمیل، توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات، هر جا که شلوغشم نکنیم. و تمام چیزهایی که توی برنامه هست و قرار بیاد، باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم بتونم اضافه کنم، لازم نباشه اینا رو تو رندر اضافه بکنم. تو همون جا بتونم اضافه کنم، ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه. همه اینا توی اون قسمت تنظیمات و چیزش باشه. حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه. و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه، اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_437950_AgADsh0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد • 5,325 char متن_

## هدف اصلی و ارزش پیشنهادی اپلیکیشن  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه.
[00:08] حالا لازم نیست جدا جدا مثلاً توی همون قسمتی
[00:12] با هم باشن یا ذیل تنظیمات هر جا که شلوغش هم نکنیم.
[00:17] و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این
[00:24] و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم
[00:30] بتونم اضافه کنم، لازم نباشه اینا رو تو هندلر اضافه بکنم.
[00:33] تو همون جا بتونم اضافه کنم، ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا
[00:40] با صدا اعلام بشه، چیا بدون صدا اعلام بشه.
[00:44] همه اینا توی اون قسمت تنظیمات و چیزش باشه.
[00:48] حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید
[00:53] تنظیماتشون هم جدا باشه.
[00:55] و آره یه جور داینامیک باشه که هرچی هم اضافه میشه
[01:01] اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## قابلیت‌های محوری و امکانات کلیدی  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات هر جا که شلوغشم نکنیم. و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم آیدی دستگاه‌ها رو هم بتونم اضافه کنم لازم نباشه اینا رو تو رندر اضافه بکنم. تو همونجا بتونم اضافه کنم ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه. همه اینا توی اون قسمت تنظیمات و چیزش باشه. حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه. و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## جامعه هدف و نیازهای خاص دانشجویان  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه.
[00:08] حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات، هر جا که شلوغشم نکنیم.
[00:17] و تمام چیزهایی که توی برنامه هست و قرار بیاد، باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم
[00:30] بتونم اضافه کنم، لازم نباشه اینا رو تو هندلر اضافه بکنم.
[00:33] تو همون جا بتونم اضافه کنم، ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه.
[00:44] همه اینا توی اون قسمت تنظیمات و چیزش باشه.
[00:48] حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه.
[00:55] و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه،
[01:00] اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## مکانیسم‌های اعتمادسازی و تعامل کاربران  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشند یا ذیل تنظیمات هر جا که شلوغش هم نکنیم. و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم آیدی دستگاه‌ها رو هم بتونم اضافه کنم لازم نباشه اینا رو تو هندلر اضافه بکنم. تو همونجا بتونم اضافه کنم ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه. همه اینا توی اون قسمت تنظیمات و چیزش باشه. حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه. و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

## معیارهای دسته‌بندی و فیلترینگ کتاب‌ها  _(at: audio/ogg)_

[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات هر جا که شلوغشم نکنیم. و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم آیدی دستگاه‌ها رو هم بتونم اضافه کنم لازم نباشه اینا رو تو رندر اضافه بکنم. تو همون جا بتونم اضافه کنم ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه. همه اینا توی اون قسمت تنظیمات و چیزش باشه. حالا برای تلگرام و ایمیل هستش یه خورده شرایط هر کدومشون باید تنظیماتشون هم جدا باشه. و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.

---
**توضیح:** محتوای فایل صوتی ارائه شده، مربوط به تنظیمات و قابلیت‌های اطلاع‌رسانی از طریق تلگرام و ایمیل است و هیچ اشاره‌ای به "معیارهای دسته‌بندی و فیلترینگ کتاب‌ها" ندارد.

## قابلیت‌های ویژه و پتانسیل توسعه  _(at: audio/ogg)_

### قابلیت‌های ویژه و پتانسیل توسعه

[00:00] گوینده ۱: می‌خوام برای تلگرام، اطلاع‌رسانی تلگرام و ایمیل، توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات، هر جا که شلوغشم نکنیم. و تمام چیزهایی که توی برنامه هست و قرار بیاد، باید متصل بشن به این و خیلی پربار و جامع باشه و بتونم مثلاً توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم بتونم اضافه کنم، لازم نباشه اینا رو تو رندر اضافه بکنم. تو همون جا بتونم اضافه کنم، ثبتش بکنم و بتونم تست بزنم و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه، چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه. همه اینا توی اون قسمت تنظیمات و چیزش باشه. حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه. و آره یه جوری داینامیک باشه که هرچی هم اضافه میشه، اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.
```

## 📋 چک‌لیست مراحل (7 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: ایجاد بخش اصلی تنظیمات اطلاع‌رسانی در فرانت‌اند** — این مرحله شامل طراحی و پیاده‌سازی بخش اصلی رابط کاربری (UI) در فرانت‌اند است که به عنوان نقطه مرکزی برای مدیریت تمام تنظیمات اطلاع‌رسانی تلگرام و ایمیل عمل می‌کند. هدف این است که یک فضای مشخص و سازمان‌یافته برای کاربر فراهم شود، بدون اینکه بخش‌های مختلف اطلاع‌رسانی به صورت جداگانه و پراکنده نمایش دا
- [x] **مرحله 2: پیاده‌سازی مدیریت توکن و آیدی دستگاه برای تلگرام و تنظیمات ایمیل (با تفکیک)** — این مرحله شامل توسعه رابط کاربری (UI) و منطق بک‌اند برای افزودن، ثبت، و مدیریت توکن‌های تلگرام و آیدی دستگاه‌ها، و همچنین تنظیمات مربوط به اطلاع‌رسانی ایمیل است. تنظیمات هر دو پلتفرم (تلگرام و ایمیل) باید به صورت جداگانه مدیریت شوند. این شامل تعریف مدل داده برای ذخیره‌سازی این اطلاعات و ایجاد APIهای
- [x] **مرحله 3: توسعه سرویس مرکزی ارسال اطلاع‌رسانی (Notification Dispatcher) و API مربوطه** — این مرحله بر روی طراحی و پیاده‌سازی یک سرویس بک‌اند متمرکز است که به عنوان هسته سیستم اطلاع‌رسانی عمل می‌کند. این سرویس باید قادر باشد درخواست‌های اطلاع‌رسانی را از بخش‌های مختلف برنامه دریافت کرده و آن‌ها را بر اساس تنظیمات کاربر (ذخیره شده در مرحله ۲) به تلگرام یا ایمیل ارسال کند. طراحی باید به گو
- [x] **مرحله 4: پیاده‌سازی قابلیت ارسال اطلاع‌رسانی تستی (UI فرانت‌اند و Endpoint بک‌اند)** — این مرحله شامل توسعه یک دکمه یا رابط کاربری مشابه در فرانت‌اند است که به کاربر اجازه می‌دهد یک اطلاع‌رسانی تستی را برای تلگرام و ایمیل ارسال کند. همچنین، یک Endpoint در بک‌اند باید پیاده‌سازی شود که این درخواست تستی را دریافت کرده و از طریق سرویس Notification Dispatcher (مرحله ۳) یک اطلاع‌رسانی آزما
- [x] **مرحله 5: پیاده‌سازی تنظیمات پویا برای انواع اطلاع‌رسانی (UI فرانت‌اند و منطق بک‌اند)** — این مرحله شامل توسعه رابط کاربری (UI) است که به کاربر اجازه می‌دهد انتخاب کند 'چیا اعلام بشه، چیا اعلام نشه'. این بدان معناست که کاربر می‌تواند انواع خاصی از رویدادها یا اطلاعات را برای دریافت اطلاع‌رسانی فعال یا غیرفعال کند. در بک‌اند، منطق لازم برای ذخیره‌سازی این ترجیحات و فیلتر کردن اطلاع‌رسانی‌
- [x] **مرحله 6: یکپارچه‌سازی ماژول‌های موجود برنامه با سیستم اطلاع‌رسانی جدید** — این مرحله شامل شناسایی و اصلاح ماژول‌ها و رویدادهای موجود در برنامه است که باید از طریق سیستم اطلاع‌رسانی جدید (با استفاده از سرویس Notification Dispatcher توسعه یافته در مرحله ۳) اطلاع‌رسانی ارسال کنند. هدف این است که 'تمام چیزهایی که توی برنامه هست' به این سیستم 'متصل بشن' تا از قابلیت‌های جامع و 
- [x] **مرحله 7: تعریف و مستندسازی استراتژی یکپارچه‌سازی پویا برای قابلیت‌های آینده** — این مرحله بر روی ایجاد یک چارچوب یا دستورالعمل‌های واضح برای توسعه‌دهندگان آینده تمرکز دارد تا بتوانند به راحتی قابلیت‌های جدید برنامه را با سیستم اطلاع‌رسانی 'مچ' کنند. هدف این است که سیستم 'داینامیک' باشد و 'هرچی هم اضافه میشه' بتواند خود را با آن تطبیق دهد. این شامل تعریف الگوهای طراحی، توابع کمک

---

# 🔹 مرحله 1: ایجاد بخش اصلی تنظیمات اطلاع‌رسانی در فرانت‌اند

**Scope:** این مرحله شامل طراحی و پیاده‌سازی بخش اصلی رابط کاربری (UI) در فرانت‌اند است که به عنوان نقطه مرکزی برای مدیریت تمام تنظیمات اطلاع‌رسانی تلگرام و ایمیل عمل می‌کند. هدف این است که یک فضای مشخص و سازمان‌یافته برای کاربر فراهم شود، بدون اینکه بخش‌های مختلف اطلاع‌رسانی به صورت جداگانه و پراکنده نمایش داده شوند. این مرحله شامل ایجاد مسیر (route) و کامپوننت‌های اصلی UI برای این بخش است.
**Key terms:** تلگرام, ایمیل, فرانت, اطلاع‌رسانی, تنظیمات, UI

**بخش مربوط از متن کاربر:**
```
[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. [00:08] حالا لازم نیست جدا جدا مثلاً توی همون قسمتی [00:12] با هم باشن یا ذیل تنظیمات هر جا که شلوغش هم نکنیم.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد بخش تنظیمات اطلاع‌رسانی تلگرام و ایمیل در فرانت‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/notifications.py:132-168` — `get_user_notification_settings_endpoint, update_user_notification_settings_endpoint` — اینجا مسیرهای جدید API برای دریافت و به‌روزرسانی تنظیمات اطلاع‌رسانی کاربر اضافه می‌شوند.
  ```python
  # --- User Notification Settings ---------------------------------------------
      
      @router.get(
          "/settings",
          response_model=UserNotificationSettingOut,
          tags=["notifications"],
      )
      async def get_user_notification_settings_endpoint(
          db: AsyncSession = Depends(get_db),
          current_user: User = Depends(get_current_user),
      ):
          notification_service = NotificationService(db)
          settings = await notification_service.get_user_notification_settings(current_user.id)
          if not settings:
              settings = await notification_service.create_default_notification_settings(current_user.id)
          return settings
      
      @router.put(
          "/settings",
          response_model=UserNotificationSettingOut,
          tags=["notifications"],
      )
      async def update_user_notification_settings_endpoint(
          settings_data: UserNotificationSettingUpdate,
          db: AsyncSession = Depends(get_db),
          current_user: User = Depends(get_current_user),
      ):
          notification_service = NotificationService(db)
          settings = await notification_service.update_user_notification_settings(
              current_user.id, settings_data
          )
          if not settings:
              raise HTTPException(
                  status_code=status.HTTP_404_NOT_FOUND, detail="Notification settings not found for user"
              )
          return settings
  ```
- `app/services/notification_service.py` — `get_user_notification_settings, create_user_notification_settings, update_user_notification_settings` — این فایل deep-read نشده است. متدهای جدید برای مدیریت منطق کسب‌وکار تنظیمات اطلاع‌رسانی کاربر به این سرویس اضافه خواهند شد. مجری باید مسیر را خود تأیید کند.
- `app/models/user_notification_setting.py` — `UserNotificationSetting` — این یک فایل جدید خواهد بود که مدل SQLAlchemy برای ذخیره ترجیحات اطلاع‌رسانی کاربر را تعریف می‌کند.
- `frontend/src/App.jsx` — `App` — مسیر جدید `/settings/notifications` برای کامپوننت `NotificationSettings` به این فایل اضافه می‌شود.
  ```jsx
  <Routes>
        <Route path="/login" element={<Login />} />
        <Route path="/register" element={<Register />} />
        <Route path="/" element={<Home />} />
        <Route element={<ProtectedRoute />}>
          <Route path="/dashboard" element={<Dashboard />} />
          <Route path="/projects" element={<Projects />} />
          <Route path="/tasks" element={<Tasks />} />
          <Route path="/lists" element={<Lists />} />
          <Route path="/lists/:listId" element={<ListDetail />} />
          <Route path="/notifications" element={<Notifications />} />
          {/* New route for notification settings */}
          <Route path="/settings/notifications" element={<NotificationSettings />} />
        </Route>
      </Routes>
  ```
- `frontend/src/components/Sidebar.jsx` — `Sidebar` — یک لینک ناوبری جدید برای دسترسی به صفحه تنظیمات اطلاع‌رسانی به نوار کناری اضافه می‌شود.
  ```jsx
  <NavLink to="/dashboard" className={({ isActive }) => isActive ? 'active' : ''}>
              <FaHome /> داشبورد
            </NavLink>
            <NavLink to="/projects" className={({ isActive }) => isActive ? 'active' : ''}>
              <FaProjectDiagram /> پروژه‌ها
            </NavLink>
            <NavLink to="/tasks" className={({ isActive }) => isActive ? 'active' : ''}>
              <FaTasks /> وظایف
            </NavLink>
            <NavLink to="/lists" className={({ isActive }) => isActive ? 'active' : ''}>
              <FaListAlt /> لیست‌ها
            </NavLink>
            <NavLink to="/notifications" className={({ isActive }) => isActive ? 'active' : ''}>
              <FaBell /> اعلان‌ها
            </NavLink>
            {/* New navigation link for Notification Settings */}
            <NavLink to="/settings/notifications" className={({ isActive }) => isActive ? 'active' : ''}>
              <FaCog /> تنظیمات اعلان
            </NavLink>
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. بک‌اند از FastAPI، SQLAlchemy (با asyncpg) و Pydantic استفاده می‌کند. فرانت‌اند از React، Vite و react-router-dom برای مسیریابی استفاده می‌کند.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/user.py` — مدل `User` نیاز به یک رابطه (relationship) با مدل جدید `UserNotificationSetting` خواهد داشت تا تنظیمات به کاربر مرتبط شوند.
- `app/dependencies/auth.py` (سطر 14) — تابع `get_current_user` برای احراز هویت و اطمینان از دسترسی کاربر به تنظیمات خود در مسیرهای جدید API استفاده خواهد شد.
- `app/schemas/notification_schema.py` — این فایل برای تعریف اسکیماهای Pydantic جدید برای `UserNotificationSetting` و `UserNotificationSettingUpdate` استفاده خواهد شد.
- `frontend/src/lib/api.js` — این فایل برای اضافه کردن توابع جدید جهت فراخوانی APIهای بک‌اند مربوط به تنظیمات اطلاع‌رسانی (GET و PUT) به‌روزرسانی خواهد شد.
- `migrations/versions/` — یک اسکریپت مهاجرت جدید Alembic برای ایجاد جدول `user_notification_settings` در دیتابیس مورد نیاز است.

## 🌐 نقشهٔ وابستگی‌ها
ویژگی جدید تنظیمات اطلاع‌رسانی، مدل `UserNotificationSetting` را معرفی می‌کند که به مدل موجود `User` (`app/models/user.py`) از طریق یک کلید خارجی وابسته است. `app/services/notification_service.py` برای مدیریت این تنظیمات گسترش می‌یابد و برای مدیریت سشن به `app/database.py` متکی است. مسیرهای جدید API در `app/routes/notifications.py` به `app/dependencies/auth.py` برای احراز هویت کاربر و به `app/schemas/user_notification_setting_schema.py` برای اعتبارسنجی درخواست/پاسخ وابسته خواهند بود. در فرانت‌اند، `frontend/src/pages/NotificationSettings.jsx` برای ارتباط با بک‌اند به `frontend/src/lib/api.js` و برای تعریف مسیر به `frontend/src/App.jsx` وابسته است. `frontend/src/components/Sidebar.jsx` نیز ناوبری به صفحه جدید را فراهم می‌کند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که یک بخش اصلی و متمرکز برای «تنظیمات اطلاع‌رسانی» در «فرانت‌اند» ایجاد شود. این بخش باید به عنوان نقطه مرکزی برای مدیریت تمام تنظیمات مربوط به «اطلاع‌رسانی تلگرام» و «ایمیل» عمل کند. هدف این است که یک فضای مشخص و سازمان‌یافته برای کاربر فراهم شود، بدون اینکه بخش‌های مختلف اطلاع‌رسانی به صورت جداگانه و پراکنده نمایش داده شوند. کاربر به صراحت بیان کرده است: «می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. حالا لازم نیست جدا جدا مثلاً توی همون قسمتی با هم باشن یا ذیل تنظیمات هر جا که شلوغش هم نکنیم.» این درخواست شامل ایجاد مسیر (route) جدید و کامپوننت‌های اصلی UI برای این بخش است. در حال حاضر، پروژه دارای `app/routes/notifications.py` برای مدیریت APIهای مربوط به اعلان‌ها (مانند لیست کردن، ایجاد، علامت‌گذاری به عنوان خوانده شده و حذف) و `frontend/src/pages/Notifications.jsx` برای نمایش اعلان‌ها است. با این حال، هیچ API یا رابط کاربری اختصاصی برای مدیریت «تنظیمات» و «ترجیحات اطلاع‌رسانی» کاربر (مانند فعال/غیرفعال کردن ایمیل یا تلگرام، تنظیم فرکانس‌ها یا انتخاب انواع رویدادها) وجود ندارد. این تسک نیازمند توسعه بک‌اند برای پشتیبانی از این ترجیحات و سپس پیاده‌سازی رابط کاربری مربوطه در فرانت‌اند است تا یک تجربه کاربری یکپارچه و بدون درهم‌ریختگی فراهم شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک endpoint `GET /notifications/settings` در `app/routes/notifications.py` وجود دارد که ترجیحات اطلاع‌رسانی کاربر فعلی را برمی‌گرداند (شامل `email_enabled` و `telegram_enabled`).
- [ ] یک endpoint `PUT /notifications/settings` در `app/routes/notifications.py` وجود دارد که payload `UserNotificationSettingUpdate` را می‌پذیرد و ترجیحات اطلاع‌رسانی کاربر را به‌روزرسانی می‌کند.
- [ ] یک مسیر جدید `/settings/notifications` در `frontend/src/App.jsx` تعریف شده است که کامپوننت `NotificationSettings` را رندر می‌کند.
- [ ] کامپوننت `frontend/src/pages/NotificationSettings.jsx` سوئیچ‌ها یا چک‌باکس‌هایی را برای فعال/غیرفعال کردن اعلان‌های ایمیل و تلگرام نمایش می‌دهد.
- [ ] یک لینک ناوبری به «تنظیمات اعلان» در `frontend/src/components/Sidebar.jsx` وجود دارد که به `/settings/notifications` هدایت می‌کند.
- [ ] یک مدل SQLAlchemy جدید به نام `UserNotificationSetting` در `app/models/user_notification_setting.py` با فیلدهایی مانند `user_id`, `email_enabled`, `telegram_enabled` تعریف شده است.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی بخش مرکزی تنظیمات اطلاع‌رسانی در فرانت‌اند و بک‌اند، مراحل زیر پیشنهاد می‌شود:
1.  **بک‌اند (API):**
    *   **مدل جدید:** یک مدل SQLAlchemy جدید به نام `UserNotificationSetting` در فایل `app/models/user_notification_setting.py` ایجاد شود. این مدل شامل فیلدهایی مانند `user_id` (کلید خارجی به `User`), `email_enabled` (boolean), `telegram_enabled` (boolean), `email_frequency` (string/enum), `telegram_event_types` (JSONB/array of strings) برای ذخیره ترجیحات اطلاع‌رسانی کاربر خواهد بود.
    *   **اسکیماهای جدید:** اسکیماهای Pydantic مربوطه (`UserNotificationSettingCreate`, `UserNotificationSettingUpdate`, `UserNotificationSettingOut`) در فایل `app/schemas/user_notification_setting_schema.py` تعریف شوند.
    *   **سرویس:** متدهایی برای `get_user_notification_settings`, `create_user_notification_settings` و `update_user_notification_settings` به `app/services/notification_service.py` اضافه شود. این متدها منطق CRUD را برای مدل `UserNotificationSetting` پیاده‌سازی می‌کنند.
    *   **مسیرهای جدید:** دو endpoint جدید به `app/routes/notifications.py` اضافه شود: `GET /notifications/settings` برای دریافت تنظیمات کاربر و `PUT /notifications/settings` برای به‌روزرسانی آن‌ها. این مسیرها باید از `Depends(get_current_user)` برای احراز هویت و محدود کردن دسترسی به کاربر فعلی استفاده کنند.
    *   **مهاجرت دیتابیس:** یک اسکریپت مهاجرت Alembic جدید برای ایجاد جدول `user_notification_settings` در دیتابیس ایجاد شود.
2.  **فرانت‌اند (UI):**
    *   **کامپوننت صفحه جدید:** یک کامپوننت React جدید به نام `NotificationSettings.jsx` در مسیر `frontend/src/pages/NotificationSettings.jsx` ایجاد شود. این کامپوننت رابط کاربری برای نمایش و ویرایش تنظیمات اطلاع‌رسانی را رندر می‌کند.
    *   **تعریف مسیر:** یک مسیر جدید `/settings/notifications` در `frontend/src/App.jsx` تعریف شود که به کامپوننت `NotificationSettings.jsx` اشاره کند.
    *   **عناصر UI:** در `NotificationSettings.jsx`، عناصر UI مناسب (مانند سوئیچ‌ها، چک‌باکس‌ها و فیلدهای ورودی) برای فعال/غیرفعال کردن اعلان‌های ایمیل و تلگرام و سایر ترجیحات پیاده‌سازی شود.
    *   **ادغام API:** از `frontend/src/lib/api.js` برای فراخوانی APIهای بک‌اند (`GET /notifications/settings` هنگام بارگذاری کامپوننت و `PUT /notifications/settings` هنگام ذخیره تغییرات) استفاده شود.
    *   **لینک ناوبری:** یک لینک ناوبری به `/settings/notifications` در `frontend/src/components/Sidebar.jsx` اضافه شود تا کاربران بتوانند به راحتی به این بخش جدید دسترسی پیدا کنند.

## 💡 نمونه‌های قبل/بعد
**افزودن مسیرهای تنظیمات به app/routes/notifications.py**

_قبل:_
```
```python
@router.delete(
    "/{notification_id}",
    status_code=status.HTTP_204_NO_CONTENT,
)
async def delete_notification(
    notification_id: int,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    notification_service = NotificationService(db)
    success = await notification_service.delete_notification(
        notification_id, current_user.id
    )
    if not success:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND, detail="Notification not found"
        )
```
```

_بعد:_
```
```python
@router.delete(
    "/{notification_id}",
    status_code=status.HTTP_204_NO_CONTENT,
)
async def delete_notification(
    notification_id: int,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    notification_service = NotificationService(db)
    success = await notification_service.delete_notification(
        notification_id, current_user.id
    )
    if not success:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND, detail="Notification not found"
        )

# --- User Notification Settings ---------------------------------------------

@router.get(
    "/settings",
    response_model=UserNotificationSettingOut,
    tags=["notifications"],
)
async def get_user_notification_settings_endpoint(
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    notification_service = NotificationService(db)
    settings = await notification_service.get_user_notification_settings(current_user.id)
    if not settings:
        settings = await notification_service.create_default_notification_settings(current_user.id)
    return settings

@router.put(
    "/settings",
    response_model=UserNotificationSettingOut,
    tags=["notifications"],
)
async def update_user_notification_settings_endpoint(
    settings_data: UserNotificationSettingUpdate,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    notification_service = NotificationService(db)
    settings = await notification_service.update_user_notification_settings(
        current_user.id, settings_data
    )
    if not settings:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND, detail="Notification settings not found for user"
        )
    return settings
```
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_notifications.py`
- `npm run test -- frontend/src/pages/__tests__/NotificationSettings.test.jsx`
- `npm run build`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **مهاجرت دیتابیس:** معرفی مدل جدید `UserNotificationSetting` نیازمند یک اسکریپت مهاجرت Alembic جدید است. مهاجرت نادرست می‌تواند منجر به از دست رفتن داده‌ها یا ناسازگاری دیتابیس شود.
2. **محدوده دسترسی کاربر (User ID Scope):** اطمینان از اینکه تنظیمات اطلاع‌رسانی در تمام عملیات بک‌اند به `current_user.id` محدود شده است، برای جلوگیری از دسترسی یا تغییر غیرمجاز تنظیمات سایر کاربران حیاتی است. وابستگی `get_current_user` کمک‌کننده است، اما پیاده‌سازی دقیق در لایه سرویس ضروری است.
3. **مدیریت وضعیت فرانت‌اند:** مدیریت وضعیت تنظیمات اطلاع‌رسانی در فرانت‌اند (دریافت، به‌روزرسانی، مدیریت خطا) باید قوی باشد تا تجربه کاربری خوبی ارائه دهد.
4. **ادغام با منطق ارسال اعلان:** تنظیمات جدید باید توسط منطق واقعی ارسال اعلان (مثلاً در `app/services/notification_service.py` یا یک سرویس اختصاصی ارسال اعلان) مصرف شوند تا تعیین شود که آیا و چگونه اعلان‌ها باید ارسال شوند. این یک وابستگی ضمنی است که مستقیماً توسط این تسک UI/API پوشش داده نمی‌شود، اما در آینده اگر در نظر گرفته نشود، یک ریسک محسوب می‌شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 2: پیاده‌سازی مدیریت توکن و آیدی دستگاه برای تلگرام و تنظیمات ایمیل (با تفکیک)

**Scope:** این مرحله شامل توسعه رابط کاربری (UI) و منطق بک‌اند برای افزودن، ثبت، و مدیریت توکن‌های تلگرام و آیدی دستگاه‌ها، و همچنین تنظیمات مربوط به اطلاع‌رسانی ایمیل است. تنظیمات هر دو پلتفرم (تلگرام و ایمیل) باید به صورت جداگانه مدیریت شوند. این شامل تعریف مدل داده برای ذخیره‌سازی این اطلاعات و ایجاد APIهای لازم برای عملیات CRUD (ایجاد، خواندن، به‌روزرسانی، حذف) در بک‌اند است.
**Key terms:** توکن, آیدی دستگاه‌ها, هندلر, ثبت, تلگرام, ایمیل, تنظیمات جدا, API

**بخش مربوط از متن کاربر:**
```
[00:24] و بتونم مثلاً توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم [00:30] بتونم اضافه کنم، لازم نباشه اینا رو تو هندلر اضافه بکنم. [00:33] تو همون جا بتونم اضافه کنم، ثبتش بکنم و ... [00:48] حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید [00:53] تنظیماتشون هم جدا باشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی مدیریت توکن تلگرام و تنظیمات ایمیل کاربر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/user.py` — `User` — فیلدهای جدید `email_notifications_enabled` و `email_notification_frequency` باید به مدل `User` اضافه شوند.
- `app/models/integration.py:14` — `Integration` — این فیلد `config_json` برای ذخیره تنظیمات خاص تلگرام مانند `telegram_token` و `device_ids` استفاده خواهد شد.
  ```python
  config_json: Mapped[dict] = mapped_column(JSON, nullable=True)
  ```
- `app/routes/integrations.py` — `router` — این روتر میزبان نقاط پایانی API برای مدیریت تنظیمات اینتگریشن تلگرام خواهد بود.
- `app/routes/users.py` — `router` — این روتر میزبان نقاط پایانی API برای مدیریت تنظیمات اطلاع‌رسانی ایمیل کاربر خواهد بود.
- `frontend/src/pages/Notifications.jsx` — `Notifications` — این صفحه یا یک صفحه جدید در کنار آن، نقطه ورود UI برای مدیریت تنظیمات اطلاع‌رسانی خواهد بود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
پروژه از FastAPI برای بک‌اند و React با Vite برای فرانت‌اند استفاده می‌کند. SQLAlchemy برای ORM با درایور asyncpg به کار رفته است. Pydantic برای اعتبارسنجی و سریال‌سازی داده‌ها استفاده می‌شود. راه‌حل از قابلیت‌های مسیریابی و تزریق وابستگی FastAPI، قابلیت‌های ORM SQLAlchemy و توسعه UI مبتنی بر کامپوننت React بهره خواهد برد.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/schemas/user_schema.py` — نیاز به به‌روزرسانی اسکیماهای `UserUpdate` و `UserResponse` برای شامل شدن فیلدهای جدید اطلاع‌رسانی ایمیل.
- `app/schemas/integration_schema.py` — نیاز به تعریف اسکیما `TelegramIntegrationConfig` و به‌روزرسانی `IntegrationCreate`/`IntegrationUpdate`.
- `app/services/integration_service.py` — نیاز به متدهای جدید یا تغییر متدهای موجود برای مدیریت پیکربندی اینتگریشن تلگرام.
- `app/services/notification_service.py` — نیاز به به‌روزرسانی برای بازیابی و استفاده از تنظیمات جدید تلگرام و ایمیل هنگام ارسال اطلاع‌رسانی.
- `frontend/src/components/Sidebar.jsx` — یک لینک ناوبری جدید به صفحه تنظیمات اطلاع‌رسانی اضافه خواهد شد.
- `frontend/src/lib/api.js` — نیاز به توابع جدید برای فراخوانی نقاط پایانی API بک‌اند برای مدیریت تنظیمات اطلاع‌رسانی.

## 🌐 نقشهٔ وابستگی‌ها
هسته این ویژگی بر توسعه مدل‌های داده و سرویس‌های موجود استوار است. `app/models/user.py` که توسط `app/routes/users.py`، `app/schemas/user_schema.py` و `app/dependencies/auth.py` ایمپورت می‌شود، توسعه خواهد یافت. فیلد `config_json` در `app/models/integration.py` که توسط `app/routes/integrations.py` و `app/schemas/integration_schema.py` ایمپورت می‌شود، برای تنظیمات تلگرام استفاده خواهد شد. `app/services/integration_service.py` که توسط `app/routes/integrations.py` ایمپورت می‌شود، جزء کلیدی برای تنظیمات تلگرام خواهد بود. `app/services/notification_service.py` (ایمپورت شده توسط `app/routes/notifications.py`) این تنظیمات جدید را مصرف خواهد کرد. کامپوننت‌های فرانت‌اند مانند `frontend/src/pages/Notifications.jsx` و `frontend/src/components/Sidebar.jsx` به نقاط پایانی API جدید و احتمالاً `frontend/src/context/AuthContext.jsx` برای تنظیمات خاص کاربر وابسته خواهند بود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی مدیریت توکن تلگرام و آیدی دستگاه‌ها، و همچنین تنظیمات اطلاع‌رسانی ایمیل را دارد. این قابلیت‌ها باید شامل رابط کاربری (UI) و منطق بک‌اند برای افزودن، ثبت، و مدیریت این تنظیمات باشند. به طور خاص، کاربر اشاره کرده است که "توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم بتونم اضافه کنم، لازم نباشه اینا رو تو هندلر اضافه بکنم. تو همون جا بتونم اضافه کنم، ثبتش بکنم و ... حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید تنظیماتشون هم جدا باشه." این به این معنی است که تنظیمات هر دو پلتفرم (تلگرام و ایمیل) باید به صورت جداگانه و از طریق UI قابل مدیریت باشند، نه از طریق تغییرات مستقیم در کد (هندلر). این درخواست بر روی بهبود تجربه کاربری و انعطاف‌پذیری سیستم اطلاع‌رسانی Lifemanager متمرکز است.

برای پیاده‌سازی این درخواست، نیاز به تعریف مدل‌های داده جدید یا توسعه مدل‌های موجود برای ذخیره‌سازی توکن تلگرام، آیدی دستگاه‌ها و تنظیمات ایمیل کاربر داریم. همچنین، ایجاد APIهای لازم برای عملیات CRUD (ایجاد، خواندن، به‌روزرسانی، حذف) در بک‌اند برای این تنظیمات ضروری است.

در کد فعلی پروژه Lifemanager، ما `app/models/integration.py` را داریم که شامل `config_json: Mapped[dict] = mapped_column(JSON, nullable=True)` (خط 14 در `app/models/integration.py` در deep_context) است که می‌تواند برای ذخیره توکن تلگرام و آیدی دستگاه‌ها استفاده شود. همچنین `app/routes/integrations.py` (که در خط 375 در `app/main.py` با `app.include_router(integrations.router, prefix="/integrations", tags=["integrations"])` اضافه شده است) برای مدیریت اینتگریشن‌ها وجود دارد. برای تنظیمات ایمیل، مدل `app/models/user.py` و روتر `app/routes/users.py` (که در خط 373 در `app/main.py` با `app.include_router(users.router, prefix="/users", tags=["users"])` اضافه شده است) مرتبط هستند. سرویس `app/services/notification_service.py` نیز باید برای استفاده از این تنظیمات به‌روزرسانی شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل `User` در `app/models/user.py` باید شامل فیلدهای `email_notifications_enabled` (Boolean, پیش‌فرض True) و `email_notification_frequency` (String, پیش‌فرض "instant") باشد.
- [ ] یک نقطه پایانی API برای ایجاد/به‌روزرسانی اینتگریشن تلگرام (`POST /api/integrations` یا `PUT /api/integrations/{integration_id}`) باید با موفقیت یک اینتگریشن از نوع `telegram` را با `telegram_token` و `device_ids` ذخیره شده در `config_json` ایجاد/به‌روزرسانی کند.
- [ ] یک نقطه پایانی API برای به‌روزرسانی تنظیمات ایمیل کاربر (`PATCH /api/users/me/settings/email` یا مشابه) باید به کاربر احراز هویت شده اجازه دهد تا فیلدهای `email_notifications_enabled` و `email_notification_frequency` خود را به‌روزرسانی کند.
- [ ] یک بخش یا صفحه اختصاصی در فرانت‌اند (مانند `frontend/src/pages/NotificationSettings.jsx`) باید شامل فرمی برای ورود و ذخیره توکن تلگرام و آیدی دستگاه‌ها باشد.
- [ ] فرانت‌اند باید یک رابط کاربری (UI) برای فعال/غیرفعال کردن اطلاع‌رسانی‌های ایمیل و انتخاب فرکانس (مثلاً در `frontend/src/pages/NotificationSettings.jsx` یا صفحه پروفایل کاربر) ارائه دهد.
- [ ] سرویس `app/services/notification_service.py` باید به‌روزرسانی شود تا هنگام تلاش برای ارسال اطلاع‌رسانی‌ها، این تنظیمات جدید را بازیابی و رعایت کند (مثلاً بررسی `email_notifications_enabled` قبل از ارسال ایمیل، یا استفاده از `telegram_token` و `device_ids` برای اطلاع‌رسانی‌های تلگرام).
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی مدیریت توکن تلگرام و تنظیمات ایمیل کاربر، مراحل زیر پیشنهاد می‌شود:

1.  **Backend - توسعه مدل داده:**
    *   **`app/models/user.py`**: دو ستون جدید برای تنظیمات اطلاع‌رسانی ایمیل به مدل `User` اضافه شود: `email_notifications_enabled: Mapped[bool] = mapped_column(Boolean, default=True)` و `email_notification_frequency: Mapped[str] = mapped_column(String(50), default="instant")`.
    *   **`app/models/integration.py`**: از فیلد `config_json` موجود برای ذخیره تنظیمات خاص تلگرام (مانند `telegram_token` و `device_ids`) استفاده شود. نیازی به تغییر مستقیم مدل نیست.

2.  **Backend - تعریف/به‌روزرسانی اسکیما:**
    *   **`app/schemas/user_schema.py`**: اسکیماهای Pydantic `UserUpdate` و `UserResponse` به‌روزرسانی شوند تا فیلدهای جدید اطلاع‌رسانی ایمیل را شامل شوند.
    *   **`app/schemas/integration_schema.py`**: یک اسکیما Pydantic جدید به نام `TelegramIntegrationConfig` برای محتوای فیلد `config_json` تعریف شود که شامل `telegram_token: Optional[str]` و `device_ids: Optional[list[str]]` باشد. اسکیماهای `IntegrationCreate` و `IntegrationUpdate` نیز برای پذیرش این پیکربندی به‌روزرسانی شوند.

3.  **Backend - منطق سرویس:**
    *   **`app/services/integration_service.py`**: منطق لازم برای ایجاد، به‌روزرسانی و بازیابی اینتگریشن‌های تلگرام پیاده‌سازی شود، به‌ویژه برای مدیریت `telegram_token` و `device_ids` در `config_json`.
    *   **`app/services/user_service.py`**: (در صورت وجود، در غیر این صورت مستقیماً در روتر `app/routes/users.py`) منطق به‌روزرسانی تنظیمات اطلاع‌رسانی ایمیل کاربر پیاده‌سازی شود.
    *   **`app/services/notification_service.py`**: منطق ارسال اطلاع‌رسانی موجود به‌روزرسانی شود تا قبل از ارسال، تنظیمات اطلاع‌رسانی ایمیل کاربر را بررسی کرده و توکن تلگرام و آیدی دستگاه‌ها را از سرویس اینتگریشن بازیابی کند.

4.  **Backend - نقاط پایانی API:**
    *   **`app/routes/integrations.py`**: نقاط پایانی جدیدی مانند `POST /api/integrations/telegram` و `GET /api/integrations/telegram` (یا توسعه نقاط پایانی موجود `PUT /api/integrations/{integration_id}`) برای مدیریت تنظیمات اینتگریشن تلگرام اضافه شود.
    *   **`app/routes/users.py`**: نقطه پایانی `PATCH /api/users/{user_id}` توسعه یابد یا یک نقطه پایانی جدید مانند `PATCH /api/users/me/settings/email` برای به‌روزرسانی تنظیمات اطلاع‌رسانی ایمیل کاربر اضافه شود.

5.  **Frontend - توسعه UI:**
    *   یک صفحه React جدید به نام `frontend/src/pages/NotificationSettings.jsx` برای مدیریت این تنظیمات ایجاد شود.
    *   فرم‌هایی برای ورود توکن تلگرام، آیدی دستگاه‌ها و تنظیمات ایمیل توسعه یابد.
    *   یک لینک ناوبری به این صفحه جدید در `frontend/src/components/Sidebar.jsx` اضافه شود.
    *   `frontend/src/lib/api.js` برای تعامل با نقاط پایانی جدید بک‌اند به‌روزرسانی شود.

6.  **مهاجرت‌های پایگاه داده:**
    *   اسکریپت‌های مهاجرت Alembic برای اضافه کردن ستون‌های جدید به جدول `users` تولید و اعمال شوند.

## 💡 نمونه‌های قبل/بعد
**افزودن فیلدهای اطلاع‌رسانی ایمیل به مدل User**

_قبل:_
```
class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    email: Mapped[str] = mapped_column(String, unique=True, index=True)
    hashed_password: Mapped[str] = mapped_column(String)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
```

_بعد:_
```
class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    email: Mapped[str] = mapped_column(String, unique=True, index=True)
    hashed_password: Mapped[str] = mapped_column(String)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    email_notifications_enabled: Mapped[bool] = mapped_column(Boolean, default=True)
    email_notification_frequency: Mapped[str] = mapped_column(String(50), default="instant")
```

**تعریف اسکیما TelegramIntegrationConfig در app/schemas/integration_schema.py**

_قبل:_
```
class IntegrationBase(BaseModel):
    name: str
    type: str
    config_json: Optional[dict] = None

class IntegrationCreate(IntegrationBase):
    pass
```

_بعد:_
```
class TelegramIntegrationConfig(BaseModel):
    telegram_token: Optional[str] = None
    device_ids: Optional[list[str]] = Field(default_factory=list)

class IntegrationBase(BaseModel):
    name: str
    type: str
    config_json: Optional[dict] = None

class IntegrationCreate(IntegrationBase):
    pass

class IntegrationUpdate(IntegrationBase):
    pass
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_users.py`
- `pytest tests/test_integrations.py`
- `npm run test -- frontend/src/pages/__tests__/NotificationSettings.test.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **پیچیدگی مهاجرت پایگاه داده:** اضافه کردن ستون‌های جدید به جدول `users` نیازمند یک مهاجرت Alembic جدید است. مهاجرت نادرست می‌تواند منجر به از دست رفتن داده یا خرابی برنامه در زمان راه‌اندازی شود، به‌ویژه در محیط‌هایی که از `Base.metadata.create_all` استفاده می‌کنند (مانند راه‌اندازی Render در `app/main.py` خطوط 185-286).
2.  **پیکربندی CORS:** مسیرهای فرانت‌اند یا فراخوانی‌های API جدید ممکن است با مشکلات CORS مواجه شوند اگر `ALLOWED_ORIGINS` در `.env.example` (و `app/main.py` خطوط 52-58) برای URL فرانت‌اند به درستی پیکربندی نشده باشد.
3.  **امنیت توکن‌ها/آیدی‌ها:** ذخیره توکن‌های تلگرام و آیدی دستگاه‌ها در `config_json` نیازمند مدیریت دقیق برای جلوگیری از افشا است. اطمینان از کنترل دسترسی مناسب و احتمالاً رمزگذاری برای داده‌های حساس ضروری است. تابع `__maskUrl` در `frontend/index.html` (خطوط 166-175) نشان‌دهنده آگاهی از پوشاندن داده‌های حساس است، اما ذخیره‌سازی بک‌اند نیز به ملاحظات مشابهی نیاز دارد.
4.  **تأثیر بر منطق اطلاع‌رسانی موجود:** تغییر `app/services/notification_service.py` برای بازیابی تنظیمات جدید می‌تواند در صورت عدم آزمایش کامل، رگرسیون‌هایی را ایجاد کند، زیرا این یک جزء اصلی برای ارسال همه اطلاع‌رسانی‌ها است.
5.  **مسیریابی فرانت‌اند:** اضافه کردن یک صفحه جدید `NotificationSettings.jsx` و لینک دادن آن در `Sidebar.jsx` باید با تنظیمات موجود React Router سازگار باشد تا از خطاهای 404 یا رندر نادرست جلوگیری شود. مسیر catch-all در `app/main.py` (خطوط 417-457) مسیریابی SPA را مدیریت می‌کند، بنابراین مسیر جدید فرانت‌اند باید به درستی توسط React Router مدیریت شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 3: توسعه سرویس مرکزی ارسال اطلاع‌رسانی (Notification Dispatcher) و API مربوطه

**Scope:** این مرحله بر روی طراحی و پیاده‌سازی یک سرویس بک‌اند متمرکز است که به عنوان هسته سیستم اطلاع‌رسانی عمل می‌کند. این سرویس باید قادر باشد درخواست‌های اطلاع‌رسانی را از بخش‌های مختلف برنامه دریافت کرده و آن‌ها را بر اساس تنظیمات کاربر (ذخیره شده در مرحله ۲) به تلگرام یا ایمیل ارسال کند. طراحی باید به گونه‌ای باشد که 'پربار و جامع' بوده و 'داینامیک' باشد تا بتواند با قابلیت‌های جدید برنامه به راحتی 'مچ' شود.
**Key terms:** برنامه, متصل, پربار, جامع, داینامیک, مچ, Notification Dispatcher, API

**بخش مربوط از متن کاربر:**
```
[00:17] و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و ... [00:55] و آره یه جور داینامیک باشه که هرچی هم اضافه میشه [01:01] اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن پشتیبانی تلگرام و تنظیمات کانال کاربر به سرویس اطلاع‌رسانی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/notification_service.py:611-670` — `send_sms` — محل افزودن تابع `send_telegram` جدید، مشابه `send_sms` و `send_push`.
  ```python
  def send_sms(*, to: str, body: str) -> bool:
      """Stub SMS transport.
  
      Reads ``SMS_PROVIDER_URL`` + ``SMS_PROVIDER_TOKEN`` from the env. In
      dev / test (no env set) it logs and returns True so the test suite
      can exercise the call site without a paid SMS provider account.
      """
      import os
  
      provider = os.environ.get("SMS_PROVIDER_URL")
      if not provider:
          logger.info("send_sms (no SMS_PROVIDER_URL): to=%s body=%r", to, body[:80])
          return True
  
      try:
          import httpx
  
          token = os.environ.get("SMS_PROVIDER_TOKEN", "")
          with httpx.Client(timeout=15.0) as client:
              r = client.post(
                  provider,
                  headers={"Authorization": f"Bearer {token}"},
                  json={"to": to, "body": body},
              )
              return 200 <= r.status_code < 300
      except Exception as exc:
          logger.warning("send_sms failed: %r", exc)
          return False
  
  
  def send_push(*, device_token: str, title: str, body: str) -> bool:
      """Stub push-notification transport.
  
      Same shape as send_sms — reads PUSH_PROVIDER_URL / PUSH_PROVIDER_TOKEN,
      logs-and-returns-True when unset.
      """
  ```
- `app/services/notification_service.py:96-135` — `send_notification` — این متد باید برای استفاده از تنظیمات کاربر و دیسپچ به کانال‌های مختلف (از جمله تلگرام) بازنگری شود یا یک متد دیسپچ سطح بالاتر اضافه شود.
  ```python
  async def send_notification(
          self,
          user_id: int,
          message: str,
          notification_type: str = "info",
          *,
          title: Optional[str] = None,
          email: Optional[str] = None,
          silent: bool = False,
          priority: str = "normal",
          channel: Optional[str] = None,
      ):
          """Build, persist, and (best-effort) dispatch one notification.
  
          Raises ``ValueError`` for an unknown ``notification_type`` so
          callers learn about typos at the boundary instead of silently
          writing a row no consumer recognises.
          """
          if notification_type not in self.VALID_TYPES:
              raise ValueError(f"Invalid notification type: {notification_type}")
  
          record = await self._save_notification(
              user_id=user_id,
              message=message,
              notification_type=notification_type,
              title=title or (message[:64] if message else notification_type),
              priority=priority,
              silent=silent,
              channel=channel,
          )
          if email or notification_type == "email":
              await self._send_email(email or "", message)
          # SMS / push channel fan-out — module-level helpers handle the
          # provider call. Channel is encoded in the notification_type so
          # the same send_notification() entry point covers every transport.
          if notification_type == "sms":
              send_sms(to=channel or "", body=message)
          elif notification_type == "push":
              send_push(device_token=channel or "", title=title or "notification", body=message)
          return record
  ```
- `app/models/user.py:1-20` — `User` — افزودن فیلد `notification_settings` (مثلاً از نوع `JSONB` یا `String` برای ذخیره JSON) به مدل `User`.
  ```python
  from datetime import datetime
  from typing import List, Optional
  
  from sqlalchemy import Boolean, Column, DateTime, Integer, String
  from sqlalchemy.orm import relationship
  
  from app.database import Base
  
  
  class User(Base):
      __tablename__ = "users"
  
      id = Column(Integer, primary_key=True, index=True)
      email = Column(String, unique=True, index=True, nullable=False)
      hashed_password = Column(String, nullable=False)
      is_active = Column(Boolean, default=True)
      created_at = Column(DateTime, default=datetime.utcnow)
      updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
  
      # Relationships
      tasks: List["Task"] = relationship("Task", back_populates="owner")
  ```
- `app/main.py:237-251` — `startup_event` — افزودن یک migration برای فیلد `notification_settings` به جدول `users` در تابع `startup_event`.
  ```python
  # users profile fields — bio / display_name — added so the
      # /api/users/profile sanitiser can actually persist the sanitised
      # values. Idempotent ADD COLUMN IF NOT EXISTS for legacy DBs.
      _user_profile_columns = [
          ("bio", "TEXT"),
          ("display_name", "VARCHAR(120)"),
      ]
      for col_name, col_type in _user_profile_columns:
          try:
              async with engine.begin() as conn:
                  await conn.execute(
                      text(f"ALTER TABLE users ADD COLUMN IF NOT EXISTS {col_name} {col_type}")
                  )
          except Exception as exc:
              logger.debug("skip users.%s migration: %s", col_name, exc)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. کتابخانه‌های مرتبط: SQLAlchemy برای مدل‌سازی دیتابیس، Pydantic برای اعتبارسنجی داده‌ها، Celery برای پردازش پس‌زمینه (در صورت نیاز به ارسال غیرهمزمان تلگرام)، httpx برای فراخوانی APIهای خارجی (تلگرام).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/schemas/user_schema.py` — نیاز به به‌روزرسانی شمای Pydantic برای شامل کردن فیلد `notification_settings` در ورودی‌ها و خروجی‌های کاربر.
- `app/routes/users.py` — نیاز به افزودن یا به‌روزرسانی endpoint برای مدیریت تنظیمات اطلاع‌رسانی کاربر.
- `frontend/src/pages/Notifications.jsx` (سطر 97) — این کامپوننت UI اعلان‌ها را نمایش می‌دهد و در آینده ممکن است نیاز به نمایش یا لینک به تنظیمات کانال‌های اطلاع‌رسانی داشته باشد.
- `app/tasks.py` — اگر ارسال اعلان‌های تلگرام نیز قرار است از طریق Celery انجام شود، این فایل نیاز به به‌روزرسانی دارد.
- `.env.example` — برای پیکربندی API تلگرام (مانند `TELEGRAM_BOT_TOKEN`, `TELEGRAM_API_URL`) نیاز به افزودن متغیرهای محیطی جدید است.

## 🌐 نقشهٔ وابستگی‌ها
سرویس `NotificationService` در `app/services/notification_service.py` یک هاب مرکزی است که توسط `app/routes/notifications.py` (خط 6) برای مدیریت API اعلان‌ها و توسط `app/services/auth_service.py` (از طریق `notify_event`) برای اعلان‌های سیستمی import می‌شود. تغییرات در مدل `User` در `app/models/user.py` بر `app/routes/users.py`، `app/schemas/user_schema.py` و هر سرویس دیگری که با پروفایل کاربر تعامل دارد، تأثیر می‌گذارد. تابع `startup_event` در `app/main.py` مسئول اجرای migrationهای اولیه دیتابیس است و باید برای فیلد جدید `notification_settings` به‌روزرسانی شود. افزودن کانال تلگرام نیازمند یک تابع `send_telegram` جدید است که در `app/services/notification_service.py` تعریف می‌شود و ممکن است از کتابخانه‌های HTTP مانند `httpx` (که در `requirements.txt` موجود است) استفاده کند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست توسعه یک سرویس بک‌اند مرکزی برای اطلاع‌رسانی (Notification Dispatcher) و API مربوطه را دارد. این سرویس باید قادر باشد درخواست‌های اطلاع‌رسانی را از بخش‌های مختلف برنامه دریافت کرده و آن‌ها را بر اساس تنظیمات کاربر به تلگرام یا ایمیل ارسال کند. کاربر تأکید دارد که طراحی باید 'پربار و جامع' و 'داینامیک' باشد تا بتواند با قابلیت‌های جدید برنامه به راحتی 'مچ' شود. به گفته کاربر: "تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و ... و آره یه جور داینامیک باشه که هرچی هم اضافه میشه اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه." این درخواست شامل افزودن کانال تلگرام و مدیریت ترجیحات کاربر برای کانال‌های اطلاع‌رسانی (ایمیل، تلگرام) است.

بررسی کدبیس نشان می‌دهد که `app/services/notification_service.py` (خطوط 80-476) از قبل یک سرویس اطلاع‌رسانی جامع با قابلیت ارسال به ایمیل (`_send_email` در خط 196) و استاب‌هایی برای SMS و Push (`send_sms` در خط 611 و `send_push` در خط 641) دارد. همچنین، این سرویس از `VALID_NOTIFICATION_TYPES` (خطوط 42-53) برای دسته‌بندی انواع اعلان‌ها استفاده می‌کند. `app/routes/notifications.py` (خطوط 1-131) APIهای مربوط به اعلان‌ها را ارائه می‌دهد و `frontend/src/pages/Notifications.jsx` (خطوط 1-134) رابط کاربری برای نمایش اعلان‌ها را مدیریت می‌کند. برای برآورده کردن درخواست کاربر، نیاز به افزودن پشتیبانی از تلگرام به عنوان یک کانال جدید و همچنین مکانیزمی برای ذخیره و اعمال تنظیمات کانال کاربر در مدل `User` و سرویس `NotificationService` داریم.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فیلد `notification_settings` از نوع JSONB به مدل `User` در `app/models/user.py` اضافه شود.
- [ ] تابع `send_telegram` در `app/services/notification_service.py` تعریف شده باشد و پیام‌ها را لاگ کند (در صورت عدم وجود `TELEGRAM_BOT_TOKEN`).
- [ ] `VALID_NOTIFICATION_TYPES` در `app/services/notification_service.py` شامل 'telegram' باشد.
- [ ] یک endpoint API برای به‌روزرسانی `notification_settings` کاربر (مثلاً `PATCH /users/{user_id}/settings`) وجود داشته باشد و با موفقیت تنظیمات را ذخیره کند.
- [ ] هنگام فراخوانی `NotificationService.dispatch_user_notification` (یا متد مشابه)، اعلان‌ها بر اساس `notification_settings` کاربر به ایمیل و/یا تلگرام ارسال شوند.
- [ ] متغیرهای محیطی `TELEGRAM_BOT_TOKEN` و `TELEGRAM_API_URL` (اختیاری) در `env.example.` اضافه شوند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی یک سرویس مرکزی اطلاع‌رسانی داینامیک با پشتیبانی از تلگرام و ایمیل بر اساس تنظیمات کاربر، مراحل زیر پیشنهاد می‌شود:

1.  **افزودن تابع `send_telegram`:** یک تابع `send_telegram` مشابه `send_sms` و `send_push` در `app/services/notification_service.py` ایجاد شود. این تابع در ابتدا می‌تواند یک استاب باشد که پیام را لاگ می‌کند و در آینده به API واقعی تلگرام متصل شود. همچنین `VALID_NOTIFICATION_TYPES` باید با 'telegram' به‌روزرسانی شود.
2.  **به‌روزرسانی مدل `User`:** یک فیلد جدید (مثلاً `notification_settings` از نوع JSONB) به مدل `User` در `app/models/user.py` اضافه شود تا ترجیحات کاربر برای کانال‌های اطلاع‌رسانی (مانند `email_enabled`, `telegram_enabled`, `telegram_chat_id`) ذخیره شود.
3.  **به‌روزرسانی شمای `User`:** شمای Pydantic مربوط به `User` در `app/schemas/user_schema.py` به‌روزرسانی شود تا فیلد `notification_settings` را شامل شود.
4.  **افزودن/به‌روزرسانی API تنظیمات کاربر:** یک endpoint در `app/routes/users.py` (مثلاً `PATCH /users/{user_id}/settings`) ایجاد یا به‌روزرسانی شود تا کاربران بتوانند تنظیمات اطلاع‌رسانی خود را مدیریت کنند.
5.  **اصلاح `NotificationService` برای دیسپچ داینامیک:** متد `send_notification` در `app/services/notification_service.py` یا یک متد جدید (مثلاً `dispatch_user_notification`) باید اصلاح شود تا `user_id` را دریافت کرده، `notification_settings` کاربر را از دیتابیس بخواند و بر اساس آن تصمیم بگیرد که اعلان را به کدام کانال‌ها (ایمیل، تلگرام) ارسال کند. این متد باید `_save_notification` را فراخوانی کرده و سپس بر اساس تنظیمات، `_send_email` و `send_telegram` را صدا بزند.
6.  **به‌روزرسانی `notify_event`:** تابع `notify_event` در `app/services/notification_service.py` (خطوط 494-533) باید از متد دیسپچ جدید استفاده کند تا از مکانیزم مرکزی و داینامیک بهره‌مند شود.
7.  **افزودن Migration:** یک migration در `app/main.py` (در `startup_event`) برای افزودن فیلد `notification_settings` به جدول `users` اضافه شود.

## 💡 نمونه‌های قبل/بعد
**افزودن فیلد notification_settings به مدل User**

_قبل:_
```
class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    email = Column(String, unique=True, index=True, nullable=False)
    hashed_password = Column(String, nullable=False)
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

_بعد:_
```
from sqlalchemy.dialects.postgresql import JSONB

class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    email = Column(String, unique=True, index=True, nullable=False)
    hashed_password = Column(String, nullable=False)
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    notification_settings = Column(JSONB, default={})
```

**افزودن تابع send_telegram به notification_service.py**

_قبل:_
```
def send_push(*, device_token: str, title: str, body: str) -> bool:
    """Stub push-notification transport.

    Same shape as send_sms — reads PUSH_PROVIDER_URL / PUSH_PROVIDER_TOKEN,
    logs-and-returns-True when unset.
    """
    import os

    provider = os.environ.get("PUSH_PROVIDER_URL")
    if not provider:
        logger.info(
            "send_push (no PUSH_PROVIDER_URL): device=%s title=%r", device_token, title
        )
        return True
```

_بعد:_
```
def send_push(*, device_token: str, title: str, body: str) -> bool:
    """Stub push-notification transport.

    Same shape as send_sms — reads PUSH_PROVIDER_URL / PUSH_PROVIDER_TOKEN,
    logs-and-returns-True when unset.
    """
    import os

    provider = os.environ.get("PUSH_PROVIDER_URL")
    if not provider:
        logger.info(
            "send_push (no PUSH_PROVIDER_URL): device=%s title=%r", device_token, title
        )
        return True

    # ... (existing send_push implementation)

def send_telegram(*, chat_id: str, body: str) -> bool:
    """Stub Telegram transport.

    Reads TELEGRAM_BOT_TOKEN from the env. In dev / test (no env set)
    it logs and returns True so the test suite can exercise the call site.
    """
    import os

    bot_token = os.environ.get("TELEGRAM_BOT_TOKEN")
    if not bot_token:
        logger.info("send_telegram (no TELEGRAM_BOT_TOKEN): chat_id=%s body=%r", chat_id, body[:80])
        return True

    try:
        import httpx
        telegram_api_url = f"https://api.telegram.org/bot{bot_token}/sendMessage"
        with httpx.Client(timeout=15.0) as client:
            r = client.post(
                telegram_api_url,
                json={"chat_id": chat_id, "text": body},
            )
            return 200 <= r.status_code < 300
    except Exception as exc:
        logger.warning("send_telegram failed: %r", exc)
        return False
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_notification_service.py`
- `pytest tests/test_users.py`
- `grep -q 'notification_settings = Column(JSONB' app/models/user.py`
- `grep -q 'def send_telegram' app/services/notification_service.py`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **تأثیر بر جریان‌های اعلان موجود:** تغییرات در `NotificationService.send_notification` یا افزودن یک متد دیسپچ جدید می‌تواند بر فراخوانی‌های موجود از این سرویس (مانند `notify_event` در `app/services/auth_service.py` و `app/webhook/router.py`) تأثیر بگذارد و نیاز به بازبینی دقیق دارد.
2.  **Migration دیتابیس:** افزودن فیلد `notification_settings` به مدل `User` در `app/models/user.py` نیازمند یک migration دیتابیس است. اگر این migration به درستی در `app/main.py` (خطوط 237-251) مدیریت نشود، می‌تواند منجر به خطاهای راه‌اندازی یا از دست رفتن داده‌ها در محیط‌های موجود شود.
3.  **امنیت API تلگرام:** مدیریت `TELEGRAM_BOT_TOKEN` به عنوان متغیر محیطی در `.env.example` و استفاده از آن در `send_telegram` نیازمند رعایت بهترین شیوه‌های امنیتی است تا از افشای توکن جلوگیری شود.
4.  **پیچیدگی منطق دیسپچ:** منطق تصمیم‌گیری برای ارسال اعلان به کانال‌های مختلف بر اساس `notification_settings` کاربر در `NotificationService` می‌تواند پیچیده شود و نیاز به تست‌های واحد جامع دارد تا از رفتار صحیح در سناریوهای مختلف اطمینان حاصل شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 4: پیاده‌سازی قابلیت ارسال اطلاع‌رسانی تستی (UI فرانت‌اند و Endpoint بک‌اند)

**Scope:** این مرحله شامل توسعه یک دکمه یا رابط کاربری مشابه در فرانت‌اند است که به کاربر اجازه می‌دهد یک اطلاع‌رسانی تستی را برای تلگرام و ایمیل ارسال کند. همچنین، یک Endpoint در بک‌اند باید پیاده‌سازی شود که این درخواست تستی را دریافت کرده و از طریق سرویس Notification Dispatcher (مرحله ۳) یک اطلاع‌رسانی آزمایشی را ارسال کند. این قابلیت برای تأیید صحت تنظیمات کاربر ضروری است.
**Key terms:** تست, UI, Endpoint, تلگرام, ایمیل

**بخش مربوط از متن کاربر:**
```
[00:33] و بتونم تست بزنم.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت ارسال اطلاع‌رسانی تستی (ایمیل و تلگرام)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/schemas/notification_schema.py` — `TestNotificationRequest` — تعریف Pydantic schema برای بدنه درخواست ارسال اعلان تستی.
  ```python
  from pydantic import BaseModel, Field
  
  class TestNotificationRequest(BaseModel):
      channel_type: str = Field(..., description="Type of channel to send test notification to (e.g., 'email', 'sms')")
      message: Optional[str] = Field("این یک پیام تست است.", description="Optional test message content")
  ```
- `app/routes/notifications.py:115-131` — `delete_notification` — Endpoint جدید `send_test_notification` باید در این فایل و در کنار سایر Endpointهای اعلان‌ها اضافه شود. این snippet نشان‌دهنده محل قرارگیری منطقی است.
  ```python
  async def delete_notification(
      notification_id: int,
      db: AsyncSession = Depends(get_db),
      current_user: User = Depends(get_current_user),
  ):
      notification_service = NotificationService(db)
      success = await notification_service.delete_notification(
          notification_id, current_user.id
      )
      if not success:
          raise HTTPException(
              status_code=status.HTTP_404_NOT_FOUND, detail="Notification not found"
          )
  ```
- `frontend/src/pages/Notifications.jsx:91-131` — `Notifications` — بخش جدید UI برای ارسال اعلان تستی باید در این کامپوننت React، قبل از نمایش لیست اعلان‌ها اضافه شود.
  ```jsx
  return (
      <div className="min-h-screen bg-gray-50 py-8">
        <div className="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex items-center justify-between mb-6">
            <div>
              <h1 className="text-3xl font-bold text-gray-900">اعلان‌ها</h1>
              <p className="text-gray-500 mt-1">
                {unreadCount > 0 ? `${unreadCount} اعلان خوانده‌نشده` : 'همه اعلان‌ها خوانده شده‌اند'}
              </p>
            </div>
          </div>
  
          {error && (
            <div className="mb-4 bg-red-50 border border-red-100 rounded-xl p-4 text-sm text-red-600">{error}</div>
          )}
  
          <div className="bg-white rounded-xl shadow-sm border border-gray-100">
            {loading ? (
              <div className="p-8 text-center text-gray-400">
                <svg className="w-8 h-8 mx-auto mb-2 animate-spin" fill="none" viewBox="0 0 24 24">
                  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
                  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8H4z" />
                </svg>
                در حال بارگذاری...
              </div>
            ) : notifications.length === 0 ? (
              <div className="p-12 text-center">
                <svg className="w-16 h-16 mx-auto mb-4 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
                </svg>
                <p className="text-gray-500 font-medium">اعلانی وجود ندارد</p>
              </div>
            ) : (
              notifications.map(n => (
                <NotificationItem key={n.id} notification={n} onMarkRead={handleMarkRead} />
              ))
            )}
          </div>
        </div>
      </div>
    );
  ```
- `app/services/notification_service.py:96-135` — `send_notification` — این متد هسته اصلی ارسال اعلان‌ها است و Endpoint جدید بک‌اند از آن برای ارسال اعلان‌های تستی استفاده خواهد کرد. باید اطمینان حاصل شود که `notification_type`های 'email' و 'sms' به درستی مدیریت می‌شوند.
  ```python
  async def send_notification(
          self,
          user_id: int,
          message: str,
          notification_type: str = "info",
          *,
          title: Optional[str] = None,
          email: Optional[str] = None,
          silent: bool = False,
          priority: str = "normal",
          channel: Optional[str] = None,
      ):
          """Build, persist, and (best-effort) dispatch one notification.
  
          Raises ``ValueError`` for an unknown ``notification_type`` so
          callers learn about typos at the boundary instead of silently
          writing a row no consumer recognises.
          """
          if notification_type not in self.VALID_TYPES:
              raise ValueError(f"Invalid notification type: {notification_type}")
  
          record = await self._save_notification(
              user_id=user_id,
              message=message,
              notification_type=notification_type,
              title=title or (message[:64] if message else notification_type),
              priority=priority,
              silent=silent,
              channel=channel,
          )
          if email or notification_type == "email":
              await self._send_email(email or "", message)
          # SMS / push channel fan-out — module-level helpers handle the
          # provider call. Channel is encoded in the notification_type so
          # the same send_notification() entry point covers every transport.
          if notification_type == "sms":
              send_sms(to=channel or "", body=message)
          elif notification_type == "push":
              send_push(device_token=channel or "", title=title or "notification", body=message)
          return record
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
fastapi, react, vite. بک‌اند از FastAPI برای API و SQLAlchemy برای ORM استفاده می‌کند. فرانت‌اند با React و Vite ساخته شده است. احراز هویت از طریق JWT انجام می‌شود. سرویس اعلان‌ها از SMTP برای ایمیل و یک stub برای SMS/Push استفاده می‌کند که قابلیت توسعه به سرویس‌های واقعی مانند تلگرام را دارد.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/user.py` (سطر -1) — برای دسترسی به ایمیل کاربر فعلی (`current_user.email`) هنگام ارسال اعلان تستی ایمیل.
- `app/dependencies/auth.py` (سطر 8) — برای استفاده از `get_current_user` جهت احراز هویت در Endpoint جدید `/notifications/test-send`.
- `app/main.py` (سطر 366) — این فایل روتر `notifications.router` را شامل می‌شود و Endpoint جدید در آن ثبت خواهد شد. همچنین، تنظیمات CORS و مدیریت خطا در این فایل تعریف شده‌اند که بر روی Endpoint جدید تأثیر می‌گذارند.
- `frontend/src/context/AuthContext.jsx` (سطر 2) — کامپوننت `Notifications.jsx` از `useAuth` برای دریافت توکن احراز هویت استفاده می‌کند که برای فراخوانی Endpoint جدید بک‌اند ضروری است.
- `.env.example` (سطر 45) — متغیرهای محیطی مانند `SMTP_HOST` و `SMS_PROVIDER_URL` در این فایل تعریف شده‌اند و تعیین می‌کنند که آیا `_send_email` و `send_sms` در `notification_service.py` واقعاً اعلان ارسال می‌کنند یا فقط لاگ می‌کنند. این برای تست تنظیمات کاربر حیاتی است.

## 🌐 نقشهٔ وابستگی‌ها
Endpoint جدید `/notifications/test-send` در `app/routes/notifications.py` به `app/schemas/notification_schema.py` برای اعتبارسنجی درخواست و به `app/services/notification_service.py` برای منطق ارسال اعلان وابسته است. این Endpoint همچنین از `app/dependencies/auth.py` برای تزریق `current_user` (که از `app/models/user.py` می‌آید) استفاده می‌کند. `NotificationService` نیز به `app/models/notification.py` برای تعامل با پایگاه داده و به توابع کمکی سطح ماژول مانند `send_email` و `send_sms` (که از متغیرهای محیطی در `.env.example` استفاده می‌کنند) وابسته است. در فرانت‌اند، `frontend/src/pages/Notifications.jsx` به `frontend/src/context/AuthContext.jsx` برای مدیریت احراز هویت و به Endpoint جدید بک‌اند برای ارسال درخواست‌های تست وابسته است.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیتی را دارد که به او اجازه می‌دهد یک اطلاع‌رسانی تستی را برای کانال‌های 'تلگرام' و 'ایمیل' ارسال کند. این مرحله شامل توسعه یک دکمه یا رابط کاربری مشابه در فرانت‌اند است که به کاربر اجازه می‌دهد یک اطلاع‌رسانی تستی را برای تلگرام و ایمیل ارسال کند. همچنین، یک Endpoint در بک‌اند باید پیاده‌سازی شود که این درخواست تستی را دریافت کرده و از طریق سرویس Notification Dispatcher (که در `app/services/notification_service.py` پیاده‌سازی شده) یک اطلاع‌رسانی آزمایشی را ارسال کند. این قابلیت برای تأیید صحت تنظیمات کاربر ضروری است. کاربر به صراحت بیان کرده است: 'و بتونم تست بزنم.' کلیدواژه‌های اصلی این درخواست عبارتند از: تست, UI, Endpoint, تلگرام, ایمیل.

در حال حاضر، `app/routes/notifications.py` شامل Endpointهایی برای مدیریت اعلان‌ها مانند `list_notifications` (خطوط 65-75)، `create_notification` (خطوط 78-92)، `mark_as_read` (خطوط 95-112) و `delete_notification` (خطوط 115-131) است. سرویس اصلی ارسال اعلان‌ها در `app/services/notification_service.py` قرار دارد که متدهایی مانند `send_notification` (خطوط 96-135)، `_send_email` (خطوط 196-200) و `send_sms` (خطوط 611-638) را ارائه می‌دهد. صفحه `frontend/src/pages/Notifications.jsx` نیز رابط کاربری فعلی برای نمایش اعلان‌ها را مدیریت می‌کند. این تسک نیازمند افزودن یک Endpoint جدید در بک‌اند و یک بخش جدید در UI فرانت‌اند برای فعال کردن این قابلیت تست است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک Pydantic schema جدید به نام `TestNotificationRequest` در `app/schemas/notification_schema.py` تعریف شده باشد که شامل فیلدهای `channel_type` (رشته) و `message` (رشته اختیاری) باشد.
- [ ] یک Endpoint جدید `POST /notifications/test-send` در `app/routes/notifications.py` وجود داشته باشد که `TestNotificationRequest` را دریافت کرده و با احراز هویت کاربر، یک اعلان تستی ارسال کند.
- [ ] صفحه `frontend/src/pages/Notifications.jsx` دارای یک بخش جدید با عنوان 'ارسال اعلان تستی' و دو دکمه 'ارسال تست ایمیل' و 'ارسال تست تلگرام (SMS)' باشد.
- [ ] کلیک بر روی دکمه 'ارسال تست ایمیل' در فرانت‌اند، یک درخواست `POST` به `/notifications/test-send` با `channel_type: 'email'` ارسال کرده و پیام موفقیت یا خطا را نمایش دهد.
- [ ] کلیک بر روی دکمه 'ارسال تست تلگرام (SMS)' در فرانت‌اند، یک درخواست `POST` به `/notifications/test-send` با `channel_type: 'sms'` ارسال کرده و پیام موفقیت یا خطا را نمایش دهد.
- [ ] متد `send_notification` در `app/services/notification_service.py` باید `notification_type='email'` را به `_send_email` و `notification_type='sms'` را به `send_sms` هدایت کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی قابلیت ارسال اطلاع‌رسانی تستی (ایمیل و تلگرام) مراحل زیر پیشنهاد می‌شود:

1.  **تعریف Schema جدید در بک‌اند:** در فایل `app/schemas/notification_schema.py`، یک Pydantic `BaseModel` جدید به نام `TestNotificationRequest` تعریف شود. این مدل شامل فیلدهایی مانند `channel_type` (برای تعیین نوع کانال مانند 'email' یا 'sms') و یک `message` اختیاری برای محتوای پیام تست خواهد بود.
2.  **پیاده‌سازی Endpoint جدید در بک‌اند:** در فایل `app/routes/notifications.py`، یک Endpoint جدید از نوع `POST` با مسیر `/notifications/test-send` اضافه شود. این Endpoint از `app.dependencies.auth.get_current_user` برای احراز هویت کاربر استفاده کرده و `TestNotificationRequest` را به عنوان بدنه درخواست دریافت می‌کند. سپس، با استفاده از `NotificationService` (از `app/services/notification_service.py`)، متد `send_notification` را فراخوانی می‌کند. برای `channel_type='email'`، ایمیل کاربر فعلی (`current_user.email`) استفاده می‌شود. برای `channel_type='sms'` (که به عنوان نماینده تلگرام در این سیستم عمل می‌کند)، یک پیام تست به کانال SMS ارسال می‌شود (که در حال حاضر یک stub است و فقط لاگ می‌کند).
3.  **افزودن رابط کاربری در فرانت‌اند:** در فایل `frontend/src/pages/Notifications.jsx`، یک بخش جدید در بالای لیست اعلان‌ها اضافه شود. این بخش شامل عنوان 'ارسال اعلان تستی' و دو دکمه خواهد بود: 'ارسال تست ایمیل' و 'ارسال تست تلگرام (SMS)'.
4.  **پیاده‌سازی منطق فرانت‌اند:** برای هر دکمه، یک `onClick` handler پیاده‌سازی شود که یک درخواست `POST` احراز هویت شده به Endpoint جدید `/notifications/test-send` در بک‌اند ارسال کند. نوع کانال (`email` یا `sms`) در بدنه درخواست JSON ارسال می‌شود. پس از دریافت پاسخ، یک پیام موفقیت یا خطا به کاربر نمایش داده شود.

## 💡 نمونه‌های قبل/بعد
**افزودن TestNotificationRequest به app/schemas/notification_schema.py**

_قبل:_
```
from pydantic import BaseModel
from typing import List, Optional

from app.models.notification import NotificationType


class NotificationCreate(BaseModel):
    title: str
    message: Optional[str] = None
    type: NotificationType = NotificationType.INFO
    priority: str = "normal"
    silent: bool = False


class NotificationOut(NotificationCreate):
    id: int
    user_id: int
    is_read: bool
    created_at: datetime
    updated_at: datetime

    class Config:
        orm_mode = True
```

_بعد:_
```
from datetime import datetime
from pydantic import BaseModel, Field
from typing import List, Optional

from app.models.notification import NotificationType


class NotificationCreate(BaseModel):
    title: str
    message: Optional[str] = None
    type: NotificationType = NotificationType.INFO
    priority: str = "normal"
    silent: bool = False


class NotificationOut(NotificationCreate):
    id: int
    user_id: int
    is_read: bool
    created_at: datetime
    updated_at: datetime

    class Config:
        orm_mode = True


class TestNotificationRequest(BaseModel):
    channel_type: str = Field(..., description="Type of channel to send test notification to (e.g., 'email', 'sms')")
    message: Optional[str] = Field("این یک پیام تست است.", description="Optional test message content")
```

**افزودن Endpoint جدید به app/routes/notifications.py**

_قبل:_
```
if not success:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND, detail="Notification not found"
        )
```

_بعد:_
```
if not success:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND, detail="Notification not found"
        )


@router.post(
    "/test-send",
    status_code=status.HTTP_200_OK,
    tags=["notifications"],
    summary="Send a test notification to a specified channel (email/sms)",
)
async def send_test_notification(
    test_request: TestNotificationRequest,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    notification_service = NotificationService(db)
    message = test_request.message or "این یک پیام تست از Lifemanager است."
    title = f"تست اعلان: {test_request.channel_type}"

    if test_request.channel_type == "email":
        if not current_user.email:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail="User does not have an email address configured.",
            )
        await notification_service.send_notification(
            user_id=current_user.id,
            message=message,
            notification_type="email",
            title=title,
            email=current_user.email,
            channel="email",
        )
        return {"status": "success", "message": f"Test email sent to {current_user.email}"}
    elif test_request.channel_type == "sms":
        await notification_service.send_notification(
            user_id=current_user.id,
            message=message,
            notification_type="sms",
            title=title,
            channel="test_sms_channel",
        )
        return {"status": "success", "message": "Test SMS notification dispatched (check logs for details)."}
    else:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=f"Unsupported channel type: {test_request.channel_type}",
        )
```

**افزودن بخش ارسال تست اعلان به frontend/src/pages/Notifications.jsx**

_قبل:_
```
{error && (
          <div className="mb-4 bg-red-50 border border-red-100 rounded-xl p-4 text-sm text-red-600">{error}</div>
        )}

        <div className="bg-white rounded-xl shadow-sm border border-gray-100">
          {loading ? (
            <div className="p-8 text-center text-gray-400">
              <svg className="w-8 h-8 mx-auto mb-2 animate-spin" fill="none" viewBox="0 0 24 24">
                <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
                <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8H4z" />
              </svg>
              در حال بارگذاری...
            </div>
          ) : notifications.length === 0 ? (
            <div className="p-12 text-center">
              <svg className="w-16 h-16 mx-auto mb-4 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
              </svg>
              <p className="text-gray-500 font-medium">اعلانی وجود ندارد</p>
            </div>
          ) : (
            notifications.map(n => (
              <NotificationItem key={n.id} notification={n} onMarkRead={handleMarkRead} />
            ))
          )}
        </div>
```

_بعد:_
```
{/* New section for test notifications */}
        <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-6">
          <h2 className="text-xl font-semibold text-gray-800 mb-4">ارسال اعلان تستی</h2>
          <div className="flex space-x-4">
            <button
              onClick={() => sendTestNotification('email')}
              className="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors"
            >
              ارسال تست ایمیل
            </button>
            <button
              onClick={() => sendTestNotification('sms')}
              className="px-4 py-2 bg-purple-600 text-white rounded-md hover:bg-purple-700 transition-colors"
            >
              ارسال تست تلگرام (SMS)
            </button>
          </div>
          {testNotificationStatus && (
            <div className={`mt-4 p-3 rounded-md text-sm ${testNotificationStatus.type === 'success' ? 'bg-green-50 text-green-700' : 'bg-red-50 text-red-700'}`}>
              {testNotificationStatus.message}
            </div>
          )}
        </div>
        {/* End new section */}

        {error && (
          <div className="mb-4 bg-red-50 border border-red-100 rounded-xl p-4 text-sm text-red-600">{error}</div>
        )}

        <div className="bg-white rounded-xl shadow-sm border border-gray-100">
          {loading ? (
            <div className="p-8 text-center text-gray-400">
              <svg className="w-8 h-8 mx-auto mb-2 animate-spin" fill="none" viewBox="0 0 24 24">
                <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
                <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8H4z" />
              </svg>
              در حال بارگذاری...
            </div>
          ) : notifications.length === 0 ? (
            <div className="p-12 text-center">
              <svg className="w-16 h-16 mx-auto mb-4 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
              </svg>
              <p className="text-gray-500 font-medium">اعلانی وجود ندارد</p>
            </div>
          ) : (
            notifications.map(n => (
              <NotificationItem key={n.id} notification={n} onMarkRead={handleMarkRead} />
            ))
          )}
        </div>
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_notifications.py`
- `npm run test -- frontend/src/pages/__tests__/Notifications.test.jsx`
- `curl -X POST -H "Authorization: Bearer <YOUR_JWT_TOKEN>" -H "Content-Type: application/json" -d '{"channel_type": "email", "message": "Test email from curl"}' http://localhost:8000/notifications/test-send`
- `curl -X POST -H "Authorization: Bearer <YOUR_JWT_TOKEN>" -H "Content-Type: application/json" -d '{"channel_type": "sms", "message": "Test SMS from curl"}' http://localhost:8000/notifications/test-send`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **ریسک امنیتی (Spam/Abuse):** Endpoint جدید `/notifications/test-send` امکان ارسال اعلان را فراهم می‌کند. اگرچه با `get_current_user` محافظت شده است، اما در صورت عدم وجود اعتبارسنجی کافی برای `channel` در آینده (مثلاً اگر کاربر بتواند شماره تلفن دلخواه را وارد کند)، می‌تواند منجر به سوءاستفاده یا ارسال اسپم شود. در حال حاضر، برای ایمیل از ایمیل کاربر و برای SMS از یک کانال placeholder استفاده می‌شود که این ریسک را کاهش می‌دهد.
2.  **وابستگی به تنظیمات محیطی:** عملکرد واقعی ارسال ایمیل و SMS به متغیرهای محیطی `SMTP_HOST` و `SMS_PROVIDER_URL` بستگی دارد که در `.env.example` تعریف شده‌اند. اگر این متغیرها تنظیم نشده باشند، `app/services/notification_service.py` فقط لاگ می‌کند و اعلان واقعی ارسال نمی‌شود. این ممکن است برای کاربرانی که انتظار دریافت اعلان واقعی را دارند، گیج‌کننده باشد. پیام‌های UI باید این موضوع را روشن کنند.
3.  **توسعه‌پذیری کانال تلگرام:** در حال حاضر، 'تلگرام' با کانال 'sms' در `notification_service.py` نگاشت شده است که یک stub است. برای پشتیبانی کامل از تلگرام، نیاز به یک سرویس مجزا و احتمالاً فیلدهای جدید در مدل `User` برای ذخیره Telegram ID کاربر خواهد بود که فراتر از scope این تسک است.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: پیاده‌سازی تنظیمات پویا برای انواع اطلاع‌رسانی (UI فرانت‌اند و منطق بک‌اند)

**Scope:** این مرحله شامل توسعه رابط کاربری (UI) است که به کاربر اجازه می‌دهد انتخاب کند 'چیا اعلام بشه، چیا اعلام نشه'. این بدان معناست که کاربر می‌تواند انواع خاصی از رویدادها یا اطلاعات را برای دریافت اطلاع‌رسانی فعال یا غیرفعال کند. در بک‌اند، منطق لازم برای ذخیره‌سازی این ترجیحات و فیلتر کردن اطلاع‌رسانی‌ها بر اساس آن‌ها باید پیاده‌سازی شود. این نیازمند مکانیزمی برای تعریف و ثبت انواع اطلاع‌رسانی‌های موجود در سیستم است.
— [merged] این مرحله شامل توسعه رابط کاربری (UI) است که به کاربر امکان می‌دهد تعیین کند 'چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه'. این تنظیمات باید برای هر نوع اطلاع‌رسانی (که در مرحله ۵ مدیریت می‌شود) قابل پیکربندی باشد. در بک‌اند، منطق لازم برای ذخیره‌سازی این ترجیحات و ارسال دستورالعمل‌های مربوط به صدا به سرویس‌های اطلاع‌رسانی (در صورت پشتیبانی) یا مدیریت آن در سمت کلاینت (در صورت امکان) باید پیاده‌سازی شود.
**Key terms:** بدون صدا, اعلام, انواع اطلاع‌رسانی, تنظیمات, صدا, UI, تنظیم, منطق بک‌اند

**بخش مربوط از متن کاربر:**
```
[00:36] و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی تنظیمات پویا اطلاع‌رسانی (UI و بک‌اند)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/notification.py` — `UserNotificationPreference` — این فایل برای تعریف مدل جدید `UserNotificationPreference` برای ذخیره ترجیحات کاربر استفاده خواهد شد.
- `app/schemas/notification_schema.py` — `NotificationPreferenceSchema` — شمای Pydantic برای اعتبارسنجی و سریالایز کردن داده‌های ترجیحات اعلان.
- `app/services/notification_service.py` — `NotificationService` — منطق اصلی برای مدیریت، ذخیره و فیلتر کردن اعلان‌ها بر اساس ترجیحات کاربر در این سرویس پیاده‌سازی می‌شود.
- `app/routes/notifications.py` — `router` — این روتر برای اضافه کردن endpointهای جدید جهت دریافت و به‌روزرسانی ترجیحات اعلان کاربر استفاده خواهد شد.
- `frontend/src/pages/Notifications.jsx` — `Notifications` — این صفحه برای نمایش و مدیریت تنظیمات اعلان‌های پویا توسط کاربر استفاده خواهد شد.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. کتابخانه‌های مرتبط: SQLAlchemy (برای مدل‌های پایگاه داده), Pydantic (برای شمای داده), `axios` (در فرانت‌اند برای فراخوانی API).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/main.py` (سطر 185) — نیاز به به‌روزرسانی `startup_event` (خط 185) برای اجرای migration جدید (ایجاد جدول `user_notification_preferences`) در محیط‌هایی که Alembic دستی اجرا نمی‌شود.
- `app/models/user.py` — مدل `User` ممکن است نیاز به یک رابطه با `UserNotificationPreference` داشته باشد یا در زمان ایجاد کاربر، ترجیحات پیش‌فرض برای او ایجاد شود.
- `app/dependencies/auth.py` — برای دسترسی به `current_user` در endpointهای جدید ترجیحات اعلان.
- `frontend/src/lib/api.js` — برای اضافه کردن توابع کمکی جهت فراخوانی endpointهای جدید بک‌اند مربوط به ترجیحات اعلان.
- `frontend/src/components/Sidebar.jsx` — برای اضافه کردن یک لینک ناوبری به صفحه تنظیمات اعلان‌ها.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به شدت به مدل‌های داده `app/models/notification.py` و `app/models/user.py` وابسته است. `app/schemas/notification_schema.py` برای تعریف ساختار داده‌ها در API استفاده می‌شود. `app/services/notification_service.py` منطق اصلی کسب‌وکار را برای ذخیره، بازیابی و اعمال ترجیحات اعلان‌ها پیاده‌سازی می‌کند و توسط `app/routes/notifications.py` فراخوانی می‌شود. `app/routes/notifications.py` endpointهای API را برای مدیریت ترجیحات اعلان‌ها فراهم می‌کند. در فرانت‌اند، `frontend/src/pages/Notifications.jsx` از `frontend/src/lib/api.js` برای ارتباط با بک‌اند استفاده می‌کند و `frontend/src/components/Sidebar.jsx` یا `frontend/src/components/Header.jsx` ممکن است برای ناوبری به این صفحه به‌روزرسانی شوند. `app/main.py` نیز برای اجرای migrationهای پایگاه داده در زمان راه‌اندازی، تحت تأثیر قرار می‌گیرد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی تنظیمات پویا برای انواع اطلاع‌رسانی را دارد، شامل رابط کاربری (UI) و منطق بک‌اند. این قابلیت به کاربر اجازه می‌دهد تا انتخاب کند 'چیا اعلام بشه، چیا اعلام نشه' و همچنین 'چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه'. این تنظیمات باید برای هر نوع اطلاع‌رسانی (که در مرحله ۵ مدیریت می‌شود) قابل پیکربندی باشد. در بک‌اند، منطق لازم برای ذخیره‌سازی این ترجیحات و فیلتر کردن اطلاع‌رسانی‌ها بر اساس آن‌ها باید پیاده‌سازی شود. این نیازمند مکانیزمی برای تعریف و ثبت انواع اطلاع‌رسانی‌های موجود در سیستم است. منطق ارسال دستورالعمل‌های مربوط به صدا به سرویس‌های اطلاع‌رسانی (در صورت پشتیبانی) یا مدیریت آن در سمت کلاینت (در صورت امکان) نیز باید در نظر گرفته شود.

برای پیاده‌سازی این قابلیت، نیاز به تغییرات در مدل‌های داده، شمای Pydantic، سرویس‌های بک‌اند و روترهای API داریم. همچنین، رابط کاربری فرانت‌اند در `frontend/src/pages/Notifications.jsx` باید به‌روزرسانی شود تا این تنظیمات را نمایش داده و امکان ویرایش آن‌ها را فراهم کند.

شواهد در کد:
- `app/models/notification.py`: مدل فعلی برای اعلان‌ها. نیاز به یک مدل جدید برای ذخیره ترجیحات کاربر داریم.
- `app/schemas/notification_schema.py`: شمای Pydantic برای اعلان‌ها. نیاز به شمای جدید برای ترجیحات داریم.
- `app/routes/notifications.py` (که در `app/main.py` در خطوط 366-370 شامل می‌شود): این روترها باید برای مدیریت ترجیحات کاربر گسترش یابند.
- `app/services/notification_service.py`: منطق اصلی ارسال و مدیریت اعلان‌ها در این سرویس قرار دارد و باید برای اعمال فیلترها و تنظیمات صدا تغییر کند.
- `frontend/src/pages/Notifications.jsx`: صفحه فعلی اعلان‌ها که باید برای نمایش و ویرایش تنظیمات گسترش یابد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل `UserNotificationPreference` در `app/models/notification.py` با فیلدهای `user_id`, `notification_type`, `enabled`, `sound_enabled` و یک UniqueConstraint روی `(user_id, notification_type)` تعریف شده باشد.
- [ ] شمای `NotificationPreferenceSchema` و `NotificationPreferenceUpdate` در `app/schemas/notification_schema.py` تعریف شده باشند.
- [ ] endpoint `GET /api/notifications/preferences` ترجیحات اعلان کاربر فعلی را برمی‌گرداند (status 200).
- [ ] endpoint `PATCH /api/notifications/preferences/{notification_type}` ترجیح اعلان مشخصی را به‌روزرسانی می‌کند و ترجیح به‌روز شده را برمی‌گرداند (status 200).
- [ ] صفحه `frontend/src/pages/Notifications.jsx` لیستی از انواع اعلان‌ها را با دو سوئیچ (toggle) برای فعال/غیرفعال کردن و فعال/غیرفعال کردن صدا نمایش می‌دهد.
- [ ] منطق ارسال اعلان در `app/services/notification_service.py` قبل از ارسال، ترجیحات کاربر را بررسی کرده و فقط اعلان‌های فعال را ارسال می‌کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1.  **Backend - تعریف انواع اعلان:** ایجاد یک Enum یا لیست ثابت از انواع اعلان‌ها (مانند `NotificationType.TASK_CREATED`, `NotificationType.PROJECT_UPDATED`) در `app/core/constants.py` (اگر وجود ندارد، ایجاد شود) یا `app/services/notification_service.py`.
2.  **Backend - مدل پایگاه داده:** در `app/models/notification.py`، یک مدل جدید `UserNotificationPreference` با فیلدهای `id`, `user_id` (ForeignKey به `users.id`), `notification_type` (String), `enabled` (Boolean, default True), `sound_enabled` (Boolean, default True) ایجاد شود. یک UniqueConstraint روی `(user_id, notification_type)` اضافه شود.
3.  **Backend - Alembic Migration:** یک اسکریپت Alembic جدید برای ایجاد جدول `user_notification_preferences` و به‌روزرسانی `app/main.py` (در `startup_event` خط 218) برای اجرای migration در محیط‌هایی که Alembic دستی اجرا نمی‌شود.
4.  **Backend - شمای Pydantic:** در `app/schemas/notification_schema.py`، شمای `NotificationPreferenceSchema` (برای پاسخ) و `NotificationPreferenceUpdate` (برای به‌روزرسانی) ایجاد شود.
5.  **Backend - سرویس اعلان (`app/services/notification_service.py`):** توابعی برای `get_user_preferences(db, user_id)`, `update_preference(db, user_id, type, enabled, sound_enabled)`, و `create_default_preferences_for_user(db, user_id)` اضافه شود. منطق ارسال اعلان‌ها (در تابع `send_notification` یا توابع مشابه) به‌روزرسانی شود تا قبل از ارسال، ترجیحات کاربر را بررسی کند.
6.  **Backend - روتر اعلان (`app/routes/notifications.py`):** دو endpoint جدید اضافه شود: `GET /api/notifications/preferences` برای دریافت تمام ترجیحات کاربر فعلی و `PATCH /api/notifications/preferences/{notification_type}` برای به‌روزرسانی یک ترجیح خاص.
7.  **Frontend - یکپارچه‌سازی API (`frontend/src/lib/api.js`):** توابع `getNotificationPreferences()` و `updateNotificationPreference(type, enabled, soundEnabled)` برای فراخوانی endpointهای جدید بک‌اند اضافه شود.
8.  **Frontend - UI تنظیمات اعلان (`frontend/src/pages/Notifications.jsx`):** کامپوننت به‌روزرسانی شود تا ترجیحات را از API دریافت و نمایش دهد. برای هر نوع اعلان، دو سوئیچ (toggle) برای فعال/غیرفعال کردن و فعال/غیرفعال کردن صدا اضافه شود. هندلرهای رویداد برای ارسال تغییرات به بک‌اند پیاده‌سازی شود.
9.  **Frontend - ناوبری:** یک لینک به صفحه تنظیمات اعلان‌ها در `frontend/src/components/Sidebar.jsx` یا `frontend/src/components/Header.jsx` اضافه شود.

## 💡 نمونه‌های قبل/بعد
**افزودن مدل `UserNotificationPreference` در `app/models/notification.py`**

_قبل:_
```
from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Boolean
from sqlalchemy.orm import relationship
from app.database import Base
import datetime

class Notification(Base):
    __tablename__ = "notifications"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id"))
    message = Column(String, index=True)
    created_at = Column(DateTime, default=datetime.datetime.utcnow)
    read = Column(Boolean, default=False)
    # ... other existing columns ...
    status = Column(String(32), default='pending')
    attempts = Column(Integer, default=0)
    priority = Column(String(16), default='normal')
    silent = Column(Boolean, default=False)
    channel = Column(String(32))
    last_error = Column(String)
    delivered_at = Column(DateTime)

    owner = relationship("User", back_populates="notifications")
```

_بعد:_
```
from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Boolean, UniqueConstraint
from sqlalchemy.orm import relationship
from app.database import Base
import datetime

class Notification(Base):
    __tablename__ = "notifications"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id"))
    message = Column(String, index=True)
    created_at = Column(DateTime, default=datetime.datetime.utcnow)
    read = Column(Boolean, default=False)
    # ... other existing columns ...
    status = Column(String(32), default='pending')
    attempts = Column(Integer, default=0)
    priority = Column(String(16), default='normal')
    silent = Column(Boolean, default=False)
    channel = Column(String(32))
    last_error = Column(String)
    delivered_at = Column(DateTime)

    owner = relationship("User", back_populates="notifications")

class UserNotificationPreference(Base):
    __tablename__ = "user_notification_preferences"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    notification_type = Column(String, index=True, nullable=False) # e.g., 'task_created', 'project_updated'
    enabled = Column(Boolean, default=True, nullable=False)
    sound_enabled = Column(Boolean, default=True, nullable=False)

    __table_args__ = (UniqueConstraint('user_id', 'notification_type', name='_user_notification_type_uc'),)

    user = relationship("User", back_populates="notification_preferences")
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **Database Migration:** اضافه کردن جدول جدید `user_notification_preferences` و فیلدهای جدید نیاز به migration صحیح دارد. اگر `app/main.py` برای اجرای migration در `startup_event` (خط 218) به‌روزرسانی نشود، ممکن است در محیط‌های deploy شده (مانند Render) جدول ایجاد نشود.
2.  **Performance Impact:** فیلتر کردن اعلان‌ها بر اساس ترجیحات کاربر در `app/services/notification_service.py` می‌تواند سربار (overhead) ایجاد کند، به‌ویژه اگر تعداد اعلان‌ها یا کاربران زیاد باشد. نیاز به بهینه‌سازی کوئری‌ها و کش کردن ترجیحات کاربر وجود دارد.
3.  **Backward Compatibility:** تغییر در منطق ارسال اعلان‌ها ممکن است بر اعلان‌های موجود یا سرویس‌هایی که به آن‌ها وابسته هستند، تأثیر بگذارد.
4.  **Frontend State Management:** مدیریت وضعیت (state) ترجیحات اعلان در فرانت‌اند (در `frontend/src/pages/Notifications.jsx`) باید به‌درستی انجام شود تا تغییرات کاربر به‌صورت لحظه‌ای و بدون مشکل نمایش داده شوند و به بک‌اند ارسال گردند.
5.  **Admin Access:** endpointهای `admin/pending-users` و `admin/approve-user/{user_id}` در `app/routes/auth_google.py` نشان می‌دهد که سیستم دارای نقش ادمین است. باید اطمینان حاصل شود که ترجیحات اعلان‌ها فقط توسط کاربر مربوطه قابل ویرایش است و ادمین‌ها دسترسی غیرمجاز ندارند (مگر اینکه قابلیت مدیریت ترجیحات سایر کاربران برای ادمین تعریف شود).

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: یکپارچه‌سازی ماژول‌های موجود برنامه با سیستم اطلاع‌رسانی جدید

**Scope:** این مرحله شامل شناسایی و اصلاح ماژول‌ها و رویدادهای موجود در برنامه است که باید از طریق سیستم اطلاع‌رسانی جدید (با استفاده از سرویس Notification Dispatcher توسعه یافته در مرحله ۳) اطلاع‌رسانی ارسال کنند. هدف این است که 'تمام چیزهایی که توی برنامه هست' به این سیستم 'متصل بشن' تا از قابلیت‌های جامع و متمرکز آن بهره‌مند شوند. این شامل فراخوانی API سرویس Dispatcher در نقاط مناسب کد موجود است.
**Key terms:** برنامه, متصل, ماژول‌ها, رویدادها, API, Notification Dispatcher

**بخش مربوط از متن کاربر:**
```
[00:17] و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
یکپارچه‌سازی ماژول‌های اصلی با سیستم اطلاع‌رسانی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/notification_service.py:42-53` — `VALID_NOTIFICATION_TYPES` — این لیست باید با انواع رویدادهای جدید گسترش یابد تا سیستم آن‌ها را شناسایی کند.
  ```python
  VALID_NOTIFICATION_TYPES = {
      "task_reminder",
      "project_update",
      "system",
      "info",
      "warning",
      "error",
      "email",
      "sms",
      "push",
      "verify_failed",
  }
  ```
- `app/services/notification_service.py:544-549` — `_DEFAULT_EVENT_MESSAGES` — این دیکشنری‌ها باید برای رویدادهای جدید (مانند ثبت‌نام کاربر، ایجاد وظیفه، ایجاد پروژه) گسترش یابند تا پیام‌های پیش‌فرض فارسی ارائه شود.
  ```python
  _DEFAULT_EVENT_MESSAGES = {
      "verify_failed": VERIFY_FAILED_MESSAGE_FA,
  }
  _DEFAULT_EVENT_TITLES = {
      "verify_failed": VERIFY_FAILED_TITLE_FA,
  }
  ```
- `app/routes/auth.py:40-50` — `register_user` — پس از فراخوانی `auth_service.register_user`، باید `notify_event` فراخوانی شود. (ایده‌آل این است که فراخوانی `notify_event` در خود `auth_service.register_user` انجام شود، اما چون `auth_service.py` در deep context نیست، این نقطه به عنوان مثال آورده شده است.)
  ```python
  @router.post(
      "/register",
      response_model=TokenResponse,
      status_code=status.HTTP_201_CREATED,
      dependencies=[Depends(limiter.limit(settings.RATE_LIMIT_REGISTER))],
  )
  async def register_user(
      user_data: UserCreate, db: AsyncSession = Depends(get_db)
  ):
      return await auth_service.register_user(user_data, db)
  ```
- `app/routes/tasks.py:40-50` — `create_task` — پس از فراخوانی `task_service.create_task`، باید `notify_event` فراخوانی شود. (ایده‌آل این است که فراخوانی `notify_event` در خود `task_service.create_task` انجام شود.)
  ```python
  @router.post(
      "/",
      response_model=TaskOut,
      status_code=status.HTTP_201_CREATED,
  )
  async def create_task(
      task: TaskCreate,
      db: AsyncSession = Depends(get_db),
      current_user: User = Depends(get_current_user),
  ):
      return await task_service.create_task(task, current_user.id, db)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, react, vite. بک‌اند از FastAPI برای APIها و SQLAlchemy برای ORM استفاده می‌کند. فرانت‌اند با React و Vite ساخته شده است. سرویس `NotificationService` از SQLAlchemy `AsyncSession` برای تعامل با پایگاه داده استفاده می‌کند.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/pages/Notifications.jsx` (سطر 56) — این کامپوننت اعلان‌های کاربر را از بک‌اند دریافت و نمایش می‌دهد و باید اعلان‌های جدید را نیز نشان دهد.
- `app/main.py` (سطر 366) — این فایل روتر `notifications` را mount می‌کند و نقطه ورود اصلی برنامه است.
- `app/models/notification.py` (سطر 1) — این فایل مدل پایگاه داده برای اعلان‌ها را تعریف می‌کند که توسط `NotificationService` استفاده می‌شود.
- `app/schemas/notification_schema.py` (سطر 5) — این فایل شمای Pydantic برای اعلان‌ها را تعریف می‌کند که برای ورودی و خروجی API استفاده می‌شود.
- `app/services/auth_service.py` (سطر -1) — این سرویس مسئول منطق ثبت‌نام کاربر است و باید پس از ثبت‌نام موفقیت‌آمیز، اعلان ارسال کند. (این فایل در deep context نیست اما از طریق `app/routes/auth.py` فراخوانی می‌شود).

## 🌐 نقشهٔ وابستگی‌ها
این تسک بر روی `app/services/notification_service.py` متمرکز است که به عنوان 'Notification Dispatcher' عمل می‌کند. این سرویس توسط `app/routes/notifications.py` برای مدیریت API اعلان‌ها استفاده می‌شود و همچنین توسط ماژول‌های دیگر مانند `auth_service` (که توسط `app/routes/auth.py` فراخوانی می‌شود) برای ارسال اعلان‌های رویدادی فراخوانی خواهد شد. `app/models/notification.py` و `app/schemas/notification_schema.py` ساختار داده‌های اعلان را تعریف می‌کنند. `app/main.py` روتر اعلان‌ها را در سیستم کلی برنامه mount می‌کند. تغییرات در `notification_service.py` بر تمام نقاطی که از `notify_event` استفاده می‌کنند، تأثیر می‌گذارد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که «تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این [سیستم اطلاع‌رسانی جدید] و خیلی پربار و جامع باشه». این درخواست بر یکپارچه‌سازی ماژول‌ها و رویدادهای موجود در برنامه با سیستم اطلاع‌رسانی جدید، که از طریق سرویس Notification Dispatcher توسعه یافته در مرحله ۳ پیاده‌سازی شده، تمرکز دارد. هدف اصلی این است که قابلیت‌های جامع و متمرکز این سیستم جدید به طور کامل مورد بهره‌برداری قرار گیرد. این کار شامل شناسایی نقاط مناسب در کد موجود و فراخوانی API سرویس Dispatcher در آنجا است. به عنوان مثال، رویدادهایی مانند ثبت‌نام کاربر جدید، ایجاد وظیفه (Task) و ایجاد پروژه (Project) کاندیداهای اصلی برای ارسال اعلان از طریق این سیستم هستند. سرویس `NotificationService` در فایل `app/services/notification_service.py` به عنوان 'Notification Dispatcher' عمل می‌کند و تابع `notify_event` (خطوط 494-532) نقطه ورود اصلی برای ارسال اعلان‌ها است. این تابع از `VALID_NOTIFICATION_TYPES` (خطوط 42-53) برای دسته‌بندی اعلان‌ها استفاده می‌کند و شامل قالب‌های پیام پیش‌فرض (مانند `_DEFAULT_EVENT_MESSAGES` در خط 544) است که باید برای رویدادهای جدید گسترش یابد. این یکپارچه‌سازی به بهبود تجربه کاربری و قابلیت ردیابی رویدادهای مهم در سیستم کمک می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل `app/services/notification_service.py` شامل `user_registered`, `task_created`, `project_created` در `VALID_NOTIFICATION_TYPES` باشد.
- [ ] فایل `app/services/notification_service.py` شامل پیام‌ها و عناوین پیش‌فرض فارسی برای `user_registered`, `task_created`, `project_created` در `_DEFAULT_EVENT_MESSAGES` و `_DEFAULT_EVENT_TITLES` باشد.
- [ ] پس از ثبت‌نام موفقیت‌آمیز یک کاربر جدید، یک اعلان با `event_name="user_registered"` برای آن کاربر در پایگاه داده ایجاد شود.
- [ ] پس از ایجاد موفقیت‌آمیز یک وظیفه جدید، یک اعلان با `event_name="task_created"` برای کاربر ایجادکننده در پایگاه داده ایجاد شود.
- [ ] پس از ایجاد موفقیت‌آمیز یک پروژه جدید، یک اعلان با `event_name="project_created"` برای کاربر ایجادکننده در پایگاه داده ایجاد شود.
- [ ] اعلان‌های ایجاد شده در صفحه اعلان‌های فرانت‌اند (`frontend/src/pages/Notifications.jsx`) برای کاربر مربوطه قابل مشاهده باشند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای یکپارچه‌سازی ماژول‌های موجود با سیستم اطلاع‌رسانی جدید، مراحل زیر پیشنهاد می‌شود:

1.  **گسترش `NotificationService`**: در فایل `app/services/notification_service.py`، لیست `VALID_NOTIFICATION_TYPES` را با انواع رویدادهای جدید مانند `user_registered`, `task_created`, `project_created` به‌روزرسانی کنید. همچنین، دیکشنری‌های `_DEFAULT_EVENT_MESSAGES` و `_DEFAULT_EVENT_TITLES` را برای این رویدادهای جدید با پیام‌های فارسی مناسب گسترش دهید.
2.  **یکپارچه‌سازی ثبت‌نام کاربر**: در سرویس `auth_service` (که توسط `app/routes/auth.py` در خط 49 فراخوانی می‌شود)، پس از ثبت‌نام موفقیت‌آمیز کاربر، تابع `notify_event` را فراخوانی کنید. این اعلان باید شامل `user_id` کاربر جدید و `event_name="user_registered"` باشد.
3.  **یکپارچه‌سازی ایجاد وظیفه**: در سرویس `task_service` (که توسط `app/routes/tasks.py` در خط 49 فراخوانی می‌شود)، پس از ایجاد موفقیت‌آمیز یک وظیفه، تابع `notify_event` را فراخوانی کنید. این اعلان باید شامل `user_id` کاربر فعلی و `event_name="task_created"` باشد.
4.  **یکپارچه‌سازی ایجاد پروژه**: در سرویس `project_service` (که توسط `app/routes/projects.py` در خط 49 فراخوانی می‌شود)، پس از ایجاد موفقیت‌آمیز یک پروژه، تابع `notify_event` را فراخوانی کنید. این اعلان باید شامل `user_id` کاربر فعلی و `event_name="project_created"` باشد.

این تغییرات تضمین می‌کند که رویدادهای کلیدی در برنامه به طور متمرکز از طریق سیستم اطلاع‌رسانی جدید مدیریت می‌شوند.

## 💡 نمونه‌های قبل/بعد
**گسترش VALID_NOTIFICATION_TYPES در app/services/notification_service.py**

_قبل:_
```
VALID_NOTIFICATION_TYPES = {
    "task_reminder",
    "project_update",
    "system",
    "info",
    "warning",
    "error",
    "email",
    "sms",
    "push",
    "verify_failed",
}
```

_بعد:_
```
VALID_NOTIFICATION_TYPES = {
    "task_reminder",
    "project_update",
    "system",
    "info",
    "warning",
    "error",
    "email",
    "sms",
    "push",
    "verify_failed",
    "user_registered",
    "task_created",
    "project_created",
}
```

**افزودن فراخوانی notify_event در app/routes/auth.py (مثال)**

_قبل:_
```
async def register_user(
    user_data: UserCreate, db: AsyncSession = Depends(get_db)
):
    return await auth_service.register_user(user_data, db)
```

_بعد:_
```
from app.services.notification_service import notify_event

async def register_user(
    user_data: UserCreate, db: AsyncSession = Depends(get_db)
):
    new_user = await auth_service.register_user(user_data, db)
    await notify_event(
        event_name="user_registered",
        user_id=new_user.id,
        message="به Lifemanager خوش آمدید! حساب کاربری شما با موفقیت ایجاد شد.",
        db=db
    )
    return new_user
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_auth.py`
- `pytest tests/test_tasks.py`
- `pytest tests/test_projects.py`
- `npm run test -- frontend/src/pages/__tests__/Notifications.test.jsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **افزایش بار پایگاه داده**: افزودن اعلان برای هر رویداد ایجاد (کاربر، وظیفه، پروژه) می‌تواند منجر به افزایش تعداد ردیف‌ها در جدول `notifications` شود. این امر ممکن است بر عملکرد پایگاه داده در مقیاس بزرگ تأثیر بگذارد، به خصوص در عملیات `get_user_notifications`.
2. **تأخیر در پاسخ API**: فراخوانی `notify_event` (که شامل عملیات `db.add` و `db.commit` است) در مسیرهای API مانند `register_user`، `create_task` و `create_project`، می‌تواند زمان پاسخ این APIها را کمی افزایش دهد. این تأخیر باید در حد قابل قبول باشد.
3. **تغییر در `auth_service`، `task_service`، `project_service`**: از آنجایی که این سرویس‌ها در deep context نیستند، فرض بر این است که متدهای `register_user`, `create_task`, `create_project` در آن‌ها وجود دارد. تغییر در این سرویس‌ها نیازمند بررسی دقیق‌تر وابستگی‌های داخلی آن‌ها است تا از بروز رگرسیون جلوگیری شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 7: تعریف و مستندسازی استراتژی یکپارچه‌سازی پویا برای قابلیت‌های آینده

**Scope:** این مرحله بر روی ایجاد یک چارچوب یا دستورالعمل‌های واضح برای توسعه‌دهندگان آینده تمرکز دارد تا بتوانند به راحتی قابلیت‌های جدید برنامه را با سیستم اطلاع‌رسانی 'مچ' کنند. هدف این است که سیستم 'داینامیک' باشد و 'هرچی هم اضافه میشه' بتواند خود را با آن تطبیق دهد. این شامل تعریف الگوهای طراحی، توابع کمکی، یا مستندسازی فرآیند ثبت رویدادهای جدید اطلاع‌رسانی در سرویس Dispatcher است.
**Key terms:** داینامیک, مچ, قابلیت‌های آینده, استراتژی یکپارچه‌سازی, مستندسازی, چارچوب

**بخش مربوط از متن کاربر:**
```
[00:55] و آره یه جور داینامیک باشه که هرچی هم اضافه میشه [01:01] اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.
```

## 🎯 هدف (خلاصه ساختاریافته)
تعریف و مستندسازی استراتژی یکپارچه‌سازی پویا برای اطلاع‌رسانی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:366-370` — `app.include_router(notifications.router)` — اینجا جایی است که روترهای مربوط به اعلان‌ها در برنامه اصلی FastAPI ثبت می‌شوند. هرگونه تغییر در مسیرهای اعلان‌ها یا افزودن یک endpoint جدید برای Dispatcher، از این طریق در دسترس قرار می‌گیرد.
  ```python
  app.include_router(notifications.router, prefix="/notifications", tags=["notifications"])
  # Absolute-path routes (/api/notifications/...) live on a sibling router
  # so they aren't prefixed twice. The status endpoint is the AC for the
  # delivery-tracking subtask.
  app.include_router(notifications.api_router, tags=["notifications"])
  ```
- `app/routes/auth_google.py:190-197` — `approve_pending_user` — این endpoint برای تأیید کاربران در انتظار است. پس از تأیید موفقیت‌آمیز کاربر، می‌توان یک رویداد اطلاع‌رسانی (NotificationEvent) ایجاد و به Dispatcher جدید ارسال کرد تا کاربر از تأیید حساب خود مطلع شود. این یک مثال عملی از 'قابلیت‌های آینده' است که با سیستم اطلاع‌رسانی 'مچ' می‌شود.
  ```python
  user = await approve_user(db, user_id, permissions)
      if not user:
          raise HTTPException(
              status_code=status.HTTP_404_NOT_FOUND,
              detail="User not found."
          )
      return user
  ```
- `app/services/notification_service.py` — `dispatch_notification_event` — این فایل در deep context موجود نیست — مجری باید مسیر را خود تأیید کند. این مکان اصلی برای پیاده‌سازی منطق Dispatcher مرکزی و توابع کمکی ارسال به کانال‌های مختلف (ایمیل، تلگرام) است.
- `app/schemas/notification_schema.py` — `NotificationEvent` — این فایل در deep context موجود نیست — مجری باید مسیر را خود تأیید کند. این مکان برای تعریف Pydantic Schema جدید `NotificationEvent` است که ساختار استاندارد رویدادهای اطلاع‌رسانی را مشخص می‌کند.
- `docs/API.md` — این فایل برای مستندسازی API استفاده می‌شود. یک بخش جدید برای توضیح استراتژی یکپارچه‌سازی پویا و نحوه استفاده از Dispatcher اطلاع‌رسانی اضافه خواهد شد.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: `fastapi`, `react`, `vite`. این تسک عمدتاً بر روی بخش بک‌اند (FastAPI) و استفاده از Pydantic برای تعریف Schema و SQLAlchemy برای تعامل با پایگاه داده متمرکز است. پیاده‌سازی Dispatcher از قابلیت‌های ناهمزمان پایتون (async/await) استفاده خواهد کرد.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/notification.py` — مدل SQLAlchemy برای ذخیره اعلان‌ها. Dispatcher ممکن است نیاز به ایجاد یا به‌روزرسانی این مدل داشته باشد.
- `app/tasks.py` — اگر ارسال اعلان‌ها به صورت ناهمزمان (مثلاً با Celery) انجام شود، وظایف مربوط به ارسال در این فایل تعریف خواهند شد و Dispatcher آن‌ها را فراخوانی می‌کند.
- `app/services/auth_service.py` — این سرویس `notification_service` را import می‌کند و ممکن است در آینده نیاز به ارسال اعلان‌های مرتبط با احراز هویت (مانند تغییر رمز عبور) از طریق Dispatcher جدید داشته باشد.
- `app/core/config.py` (سطر 17) — تنظیمات مربوط به APIهای ایمیل یا تلگرام (مانند کلیدهای API) که توسط Dispatcher برای ارسال اعلان‌ها استفاده می‌شوند، در این فایل یا `app/config.py` تعریف می‌شوند.

## 🌐 نقشهٔ وابستگی‌ها
این تسک بر روی `app/services/notification_service.py` متمرکز است که به عنوان هاب اصلی برای منطق Dispatcher عمل خواهد کرد. این سرویس توسط `app/routes/notifications.py` برای مدیریت اعلان‌ها و همچنین توسط `app/services/auth_service.py` برای ارسال اعلان‌های مرتبط با احراز هویت import می‌شود. `app/schemas/notification_schema.py` برای تعریف ساختار داده `NotificationEvent` مورد نیاز است که Dispatcher آن را مصرف می‌کند. `app/main.py` روترهای اعلان‌ها را شامل می‌شود و هرگونه تغییر در API از طریق آن در دسترس قرار می‌گیرد. `app/models/notification.py` مدل داده‌ای است که Dispatcher با آن تعامل خواهد داشت. `app/tasks.py` نیز در صورت نیاز به پردازش ناهمزمان اعلان‌ها، به عنوان یک وابستگی عمل خواهد کرد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست تعریف و مستندسازی یک استراتژی یکپارچه‌سازی پویا برای قابلیت‌های آینده را دارد. هدف این است که یک چارچوب یا دستورالعمل‌های واضح برای توسعه‌دهندگان آینده ایجاد شود تا بتوانند به راحتی قابلیت‌های جدید برنامه را با سیستم اطلاع‌رسانی موجود (تلگرام و ایمیل) 'مچ' کنند. کاربر به صراحت بیان می‌کند که سیستم باید 'داینامیک' باشد و 'هرچی هم اضافه میشه' بتواند خود را با آن تطبیق دهد. این شامل تعریف الگوهای طراحی، توابع کمکی، یا مستندسازی فرآیند ثبت رویدادهای جدید اطلاع‌رسانی در یک 'سرویس Dispatcher' است. این درخواست به منظور افزایش مقیاس‌پذیری و کاهش پیچیدگی توسعه در آینده مطرح شده است.

در بررسی کد، سیستم اطلاع‌رسانی فعلی از طریق `app/routes/notifications.py` و `app/models/notification.py` مدیریت می‌شود. فایل `app/services/notification_service.py` (که در deep context موجود نیست اما از نامش پیداست) احتمالاً حاوی منطق اصلی ارسال اعلان‌ها است. برای دستیابی به 'داینامیک' بودن و 'مچ' شدن قابلیت‌های آینده، نیاز به یک لایه انتزاعی (Dispatcher) داریم که رویدادهای مختلف را با یک فرمت استاندارد دریافت کرده و به کانال‌های اطلاع‌رسانی مناسب هدایت کند. این رویکرد به توسعه‌دهندگان اجازه می‌دهد تا بدون نیاز به تغییرات عمیق در منطق ارسال اعلان، قابلیت‌های جدید را اضافه کنند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک Pydantic Schema جدید به نام `NotificationEvent` در `app/schemas/notification_schema.py` تعریف شده باشد که شامل فیلدهای `event_type`, `recipient_email`, `payload`, `channels`, و `priority` باشد.
- [ ] تابعی به نام `dispatch_notification_event` در `app/services/notification_service.py` وجود داشته باشد که یک شیء `NotificationEvent` را به عنوان ورودی بپذیرد.
- [ ] تابع `dispatch_notification_event` بتواند بر اساس فیلد `channels` در `NotificationEvent`، ارسال اعلان به کانال‌های 'email' و 'telegram' را شبیه‌سازی کند (مثلاً با لاگ کردن پیام).
- [ ] در `app/routes/auth_google.py`، در تابع `approve_pending_user`، پس از تأیید کاربر، یک `NotificationEvent` ایجاد و از طریق `dispatch_notification_event` ارسال شود.
- [ ] مستندات جدیدی در `docs/API.md` یا یک فایل جدید (مثلاً `docs/NOTIFICATION_INTEGRATION.md`) اضافه شده باشد که نحوه استفاده از استراتژی یکپارچه‌سازی پویا برای اعلان‌ها را توضیح دهد.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی یک استراتژی یکپارچه‌سازی پویا برای قابلیت‌های آینده در سیستم اطلاع‌رسانی، مراحل زیر پیشنهاد می‌شود:

1.  **تعریف Schema رویداد اطلاع‌رسانی (`app/schemas/notification_schema.py`):** یک Pydantic Schema جدید به نام `NotificationEvent` ایجاد شود که ساختار استاندارد برای هر رویداد اطلاع‌رسانی را تعریف کند. این Schema باید شامل فیلدهایی مانند `event_type` (مثلاً 'user_approved', 'task_deadline_approaching'), `user_id` یا `recipient_email`, `payload` (داده‌های مرتبط با رویداد), `channels` (لیست کانال‌های مورد نظر مانند 'email', 'telegram'), و `priority` باشد.
2.  **پیاده‌سازی Dispatcher مرکزی (`app/services/notification_service.py`):** یک تابع یا کلاس جدید به نام `NotificationDispatcher` یا `dispatch_notification_event` در `app/services/notification_service.py` اضافه شود. این Dispatcher مسئول دریافت `NotificationEvent`، پردازش آن، و ارسال به کانال‌های مربوطه (ایمیل، تلگرام) خواهد بود. این تابع باید شامل منطق شرطی برای انتخاب کانال‌ها و فراخوانی توابع ارسال مربوطه باشد.
3.  **ایجاد توابع کمکی ارسال کانال (`app/services/notification_service.py`):** توابع داخلی (مانند `_send_email_notification`, `_send_telegram_notification`) برای کپسوله‌سازی منطق ارسال به هر کانال خاص ایجاد شوند. این توابع توسط Dispatcher فراخوانی می‌شوند.
4.  **یکپارچه‌سازی با قابلیت‌های موجود/آینده (مثال: `app/routes/auth_google.py`):** به عنوان یک مثال، در تابع `approve_pending_user` در `app/routes/auth_google.py`، پس از تأیید کاربر، یک `NotificationEvent` ساخته شده و به `dispatch_notification_event` ارسال شود تا یک اعلان تأیید به کاربر ارسال گردد. این نشان می‌دهد که چگونه قابلیت‌های جدید می‌توانند به صورت 'داینامیک' با سیستم اطلاع‌رسانی 'مچ' شوند.
5.  **مستندسازی (`docs/API.md` یا `docs/NOTIFICATION_INTEGRATION.md`):** یک بخش جدید در `docs/API.md` یا یک فایل مستندسازی جدید (مثلاً `docs/NOTIFICATION_INTEGRATION.md`) اضافه شود که استراتژی یکپارچه‌سازی پویا، نحوه تعریف `NotificationEvent`، و چگونگی استفاده از `dispatch_notification_event` را برای توسعه‌دهندگان آینده توضیح دهد. این مستندات باید شامل مثال‌های کد باشد.

## 💡 نمونه‌های قبل/بعد
**مثال: قبل و بعد از استفاده از Dispatcher برای ارسال اعلان تأیید کاربر**

_قبل:_
```
```python
# app/routes/auth_google.py (conceptual before)
# ...
    user = await approve_user(db, user_id, permissions)
    if not user:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="User not found.")
    # منطق ارسال اعلان مستقیم و غیرپویا (مثلاً فراخوانی مستقیم سرویس ایمیل)
    # await email_service.send_approval_email(user.email, user.name)
    return user
```
```

_بعد:_
```
```python
# app/routes/auth_google.py (conceptual after)
from app.schemas.notification_schema import NotificationEvent
from app.services.notification_service import dispatch_notification_event
# ...
    user = await approve_user(db, user_id, permissions)
    if not user:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="User not found.")
    
    # ایجاد یک رویداد اطلاع‌رسانی پویا و ارسال آن از طریق Dispatcher
    event = NotificationEvent(
        event_type="user_approved",
        recipient_email=user.email,
        payload={"user_name": user.name, "permissions": permissions},
        channels=["email", "telegram"], # کانال‌های مورد نظر
        priority="high"
    )
    await dispatch_notification_event(db, event)
    return user
```
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_notification_service.py`
- `pytest tests/test_auth_google_notifications.py`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **تأثیر بر منطق موجود:** تغییرات در `app/services/notification_service.py` برای پیاده‌سازی Dispatcher می‌تواند بر منطق موجود ارسال اعلان‌ها تأثیر بگذارد و نیاز به بازبینی دقیق دارد. 2. **وابستگی‌های جدید:** معرفی `NotificationEvent` و Dispatcher، وابستگی‌های جدیدی را در سراسر کدبیس ایجاد می‌کند که باید به درستی مدیریت شوند. 3. **پیچیدگی تست:** اطمینان از اینکه Dispatcher به درستی رویدادها را به کانال‌های مختلف هدایت می‌کند، نیاز به تست‌های جامع دارد. 4. **عدم وجود deep context:** از آنجایی که `app/services/notification_service.py` و `app/schemas/notification_schema.py` در deep context موجود نیستند، مجری باید ساختار این فایل‌ها را تأیید کند که می‌تواند منجر به تغییرات پیش‌بینی نشده شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': 'یک endpoint `GET /notifications/settings` در `app/routes/notifications.py` وجود دارد که ترجیحات اطلاع\u200cرسانی کاربر فعلی را برمی\u200cگرداند (شامل `email_enabled` و `telegram_enabled`).', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/notifications/settings', 'expected_status': 200, 'required_fields': ['email_enabled', 'telegram_enabled']}}
- [ ] {'text': 'یک endpoint `PUT /notifications/settings` در `app/routes/notifications.py` وجود دارد که payload `UserNotificationSettingUpdate` را می\u200cپذیرد و ترجیحات اطلاع\u200cرسانی کاربر را به\u200cروزرسانی می\u200cکند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'PUT', 'path': '/notifications/settings', 'request_body': {'email_enabled': True, 'telegram_enabled': False}, 'expected_status': 200, 'required_fields': ['email_enabled', 'telegram_enabled']}}
- [ ] {'text': 'یک مسیر جدید `/settings/notifications` در `frontend/src/App.jsx` تعریف شده است که کامپوننت `NotificationSettings` را رندر می\u200cکند.', 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/settings/notifications'}, {'action': 'assert_visible', 'selector': "h1:contains('تنظیمات اعلان')"}]}}
- [ ] {'text': 'کامپوننت `frontend/src/pages/NotificationSettings.jsx` سوئیچ\u200cها یا چک\u200cباکس\u200cهایی را برای فعال/غیرفعال کردن اعلان\u200cهای ایمیل و تلگرام نمایش می\u200cدهد.', 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/settings/notifications'}, {'action': 'assert_visible', 'selector': "input[type='checkbox'][name='email_enabled']"}, {'action': 'assert_visible', 'selector': "input[type='checkbox'][name='telegram_enabled']"}]}}
- [ ] {'text': 'یک لینک ناوبری به «تنظیمات اعلان» در `frontend/src/components/Sidebar.jsx` وجود دارد که به `/settings/notifications` هدایت می\u200cکند.', 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/'}, {'action': 'click', 'selector': "a[href='/settings/notifications']"}, {'action': 'assert_url', 'url': '/settings/notifications'}]}}
- [ ] {'text': 'یک مدل SQLAlchemy جدید به نام `UserNotificationSetting` در `app/models/user_notification_setting.py` با فیلدهایی مانند `user_id`, `email_enabled`, `telegram_enabled` تعریف شده است.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class UserNotificationSetting(Base):', 'user_id: Mapped[int] = mapped_column(ForeignKey("users.id"))', 'email_enabled: Mapped[bool] = mapped_column(Boolean, default=True)', 'telegram_enabled: Mapped[bool] = mapped_column(Boolean, default=False)'], 'files_hint': ['app/models/user_notification_setting.py']}}
- [ ] {'text': 'مدل `User` در `app/models/user.py` باید شامل فیلدهای `email_notifications_enabled` (Boolean, پیش\u200cفرض True) و `email_notification_frequency` (String, پیش\u200cفرض "instant") باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class User', 'email_notifications_enabled:', 'email_notification_frequency:'], 'files_hint': ['app/models/user.py']}}
- [ ] {'text': 'یک نقطه پایانی API برای ایجاد/به\u200cروزرسانی اینتگریشن تلگرام (`POST /api/integrations` یا `PUT /api/integrations/{integration_id}`) باید با موفقیت یک اینتگریشن از نوع `telegram` را با `telegram_token` و `device_ids` ذخیره شده در `config_json` ایجاد/به\u200cروزرسانی کند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/api/integrations', 'body': {'name': 'My Telegram', 'type': 'telegram', 'config_json': {'telegram_token': 'abc', 'device_ids': ['123', '456']}}, 'expected_status': 201, 'required_fields': ['id', 'name', 'type', 'config_json']}}
- [ ] {'text': 'یک نقطه پایانی API برای به\u200cروزرسانی تنظیمات ایمیل کاربر (`PATCH /api/users/me/settings/email` یا مشابه) باید به کاربر احراز هویت شده اجازه دهد تا فیلدهای `email_notifications_enabled` و `email_notification_frequency` خود را به\u200cروزرسانی کند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'PATCH', 'path': '/api/users/{user_id}/settings/email', 'body': {'email_notifications_enabled': False, 'email_notification_frequency': 'daily'}, 'expected_status': 200, 'required_fields': ['email_notifications_enabled', 'email_notification_frequency']}}
- [ ] {'text': 'یک بخش یا صفحه اختصاصی در فرانت\u200cاند (مانند `frontend/src/pages/NotificationSettings.jsx`) باید شامل فرمی برای ورود و ذخیره توکن تلگرام و آیدی دستگاه\u200cها باشد.', 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/settings/notifications'}, {'action': 'assert_visible', 'selector': "input[name='telegramToken']"}, {'action': 'assert_visible', 'selector': "textarea[name='telegramDeviceIds']"}]}}
- [ ] {'text': 'فرانت\u200cاند باید یک رابط کاربری (UI) برای فعال/غیرفعال کردن اطلاع\u200cرسانی\u200cهای ایمیل و انتخاب فرکانس (مثلاً در `frontend/src/pages/NotificationSettings.jsx` یا صفحه پروفایل کاربر) ارائه دهد.', 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/settings/notifications'}, {'action': 'assert_visible', 'selector': "input[type='checkbox'][name='emailNotificationsEnabled']"}, {'action': 'assert_visible', 'selector': "select[name='emailNotificationFrequency']"}]}}
- [ ] {'text': 'سرویس `app/services/notification_service.py` باید به\u200cروزرسانی شود تا هنگام تلاش برای ارسال اطلاع\u200cرسانی\u200cها، این تنظیمات جدید را بازیابی و رعایت کند (مثلاً بررسی `email_notifications_enabled` قبل از ارسال ایمیل، یا استفاده از `telegram_token` و `device_ids` برای اطلاع\u200cرسانی\u200cهای تلگرام).', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['notification_service.py', 'user.email_notifications_enabled', "integration.config_json.get('telegram_token')"], 'files_hint': ['app/services/notification_service.py']}}
- [ ] {'text': 'فیلد `notification_settings` از نوع JSONB به مدل `User` در `app/models/user.py` اضافه شود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class User', 'notification_settings = Column(JSONB'], 'files_hint': ['app/models/user.py']}}
- [ ] {'text': 'تابع `send_telegram` در `app/services/notification_service.py` تعریف شده باشد و پیام\u200cها را لاگ کند (در صورت عدم وجود `TELEGRAM_BOT_TOKEN`).', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['def send_telegram', 'logger.info("send_telegram (no TELEGRAM_BOT_TOKEN)"'], 'files_hint': ['app/services/notification_service.py']}}
- [ ] {'text': "`VALID_NOTIFICATION_TYPES` در `app/services/notification_service.py` شامل 'telegram' باشد.", 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['VALID_NOTIFICATION_TYPES = {', '"telegram"'], 'files_hint': ['app/services/notification_service.py']}}
- [ ] {'text': 'یک endpoint API برای به\u200cروزرسانی `notification_settings` کاربر (مثلاً `PATCH /users/{user_id}/settings`) وجود داشته باشد و با موفقیت تنظیمات را ذخیره کند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'PATCH', 'path': '/users/{user_id}/settings', 'expected_status': 200, 'required_fields': ['notification_settings'], 'body': {'notification_settings': {'telegram_enabled': True, 'email_enabled': False}}}}
- [ ] {'text': 'هنگام فراخوانی `NotificationService.dispatch_user_notification` (یا متد مشابه)، اعلان\u200cها بر اساس `notification_settings` کاربر به ایمیل و/یا تلگرام ارسال شوند.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/test_notification_service.py', 'marker': 'verify_telegram_dispatch'}}
- [ ] {'text': 'متغیرهای محیطی `TELEGRAM_BOT_TOKEN` و `TELEGRAM_API_URL` (اختیاری) در `env.example.` اضافه شوند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['TELEGRAM_BOT_TOKEN', 'TELEGRAM_API_URL'], 'files_hint': ['.env.example']}}
- [ ] {'text': 'یک Pydantic schema جدید به نام `TestNotificationRequest` در `app/schemas/notification_schema.py` تعریف شده باشد که شامل فیلدهای `channel_type` (رشته) و `message` (رشته اختیاری) باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class TestNotificationRequest(BaseModel):', 'channel_type: str', 'message: Optional[str]'], 'files_hint': ['app/schemas/notification_schema.py']}}
- [ ] {'text': 'یک Endpoint جدید `POST /notifications/test-send` در `app/routes/notifications.py` وجود داشته باشد که `TestNotificationRequest` را دریافت کرده و با احراز هویت کاربر، یک اعلان تستی ارسال کند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/notifications/test-send', 'headers': {'Authorization': 'Bearer <VALID_JWT_TOKEN>'}, 'body': {'channel_type': 'email', 'message': 'Test email from AC'}, 'expected_status': 200, 'required_fields': ['status', 'message']}}

## Acceptance Criteria

1. یک endpoint `GET /notifications/settings` در `app/routes/notifications.py` وجود دارد که ترجیحات اطلاع‌رسانی کاربر فعلی را برمی‌گرداند (شامل `email_enabled` و `telegram_enabled`). _(verify: api_response)_
2. یک endpoint `PUT /notifications/settings` در `app/routes/notifications.py` وجود دارد که payload `UserNotificationSettingUpdate` را می‌پذیرد و ترجیحات اطلاع‌رسانی کاربر را به‌روزرسانی می‌کند. _(verify: api_response)_
3. یک مسیر جدید `/settings/notifications` در `frontend/src/App.jsx` تعریف شده است که کامپوننت `NotificationSettings` را رندر می‌کند. _(verify: ui_interaction)_
4. کامپوننت `frontend/src/pages/NotificationSettings.jsx` سوئیچ‌ها یا چک‌باکس‌هایی را برای فعال/غیرفعال کردن اعلان‌های ایمیل و تلگرام نمایش می‌دهد. _(verify: ui_interaction)_
5. یک لینک ناوبری به «تنظیمات اعلان» در `frontend/src/components/Sidebar.jsx` وجود دارد که به `/settings/notifications` هدایت می‌کند. _(verify: ui_interaction)_
6. یک مدل SQLAlchemy جدید به نام `UserNotificationSetting` در `app/models/user_notification_setting.py` با فیلدهایی مانند `user_id`, `email_enabled`, `telegram_enabled` تعریف شده است. _(verify: static)_
7. مدل `User` در `app/models/user.py` باید شامل فیلدهای `email_notifications_enabled` (Boolean, پیش‌فرض True) و `email_notification_frequency` (String, پیش‌فرض "instant") باشد. _(verify: static)_
8. یک نقطه پایانی API برای ایجاد/به‌روزرسانی اینتگریشن تلگرام (`POST /api/integrations` یا `PUT /api/integrations/{integration_id}`) باید با موفقیت یک اینتگریشن از نوع `telegram` را با `telegram_token` و `device_ids` ذخیره شده در `config_json` ایجاد/به‌روزرسانی کند. _(verify: api_response)_
9. یک نقطه پایانی API برای به‌روزرسانی تنظیمات ایمیل کاربر (`PATCH /api/users/me/settings/email` یا مشابه) باید به کاربر احراز هویت شده اجازه دهد تا فیلدهای `email_notifications_enabled` و `email_notification_frequency` خود را به‌روزرسانی کند. _(verify: api_response)_
10. یک بخش یا صفحه اختصاصی در فرانت‌اند (مانند `frontend/src/pages/NotificationSettings.jsx`) باید شامل فرمی برای ورود و ذخیره توکن تلگرام و آیدی دستگاه‌ها باشد. _(verify: ui_interaction)_
11. فرانت‌اند باید یک رابط کاربری (UI) برای فعال/غیرفعال کردن اطلاع‌رسانی‌های ایمیل و انتخاب فرکانس (مثلاً در `frontend/src/pages/NotificationSettings.jsx` یا صفحه پروفایل کاربر) ارائه دهد. _(verify: ui_interaction)_
12. سرویس `app/services/notification_service.py` باید به‌روزرسانی شود تا هنگام تلاش برای ارسال اطلاع‌رسانی‌ها، این تنظیمات جدید را بازیابی و رعایت کند (مثلاً بررسی `email_notifications_enabled` قبل از ارسال ایمیل، یا استفاده از `telegram_token` و `device_ids` برای اطلاع‌رسانی‌های تلگرام). _(verify: static)_
13. فیلد `notification_settings` از نوع JSONB به مدل `User` در `app/models/user.py` اضافه شود. _(verify: static)_
14. تابع `send_telegram` در `app/services/notification_service.py` تعریف شده باشد و پیام‌ها را لاگ کند (در صورت عدم وجود `TELEGRAM_BOT_TOKEN`). _(verify: static)_
15. `VALID_NOTIFICATION_TYPES` در `app/services/notification_service.py` شامل 'telegram' باشد. _(verify: static)_
16. یک endpoint API برای به‌روزرسانی `notification_settings` کاربر (مثلاً `PATCH /users/{user_id}/settings`) وجود داشته باشد و با موفقیت تنظیمات را ذخیره کند. _(verify: api_response)_
17. هنگام فراخوانی `NotificationService.dispatch_user_notification` (یا متد مشابه)، اعلان‌ها بر اساس `notification_settings` کاربر به ایمیل و/یا تلگرام ارسال شوند. _(verify: backend_test)_
18. متغیرهای محیطی `TELEGRAM_BOT_TOKEN` و `TELEGRAM_API_URL` (اختیاری) در `env.example.` اضافه شوند. _(verify: static)_
19. یک Pydantic schema جدید به نام `TestNotificationRequest` در `app/schemas/notification_schema.py` تعریف شده باشد که شامل فیلدهای `channel_type` (رشته) و `message` (رشته اختیاری) باشد. _(verify: static)_
20. یک Endpoint جدید `POST /notifications/test-send` در `app/routes/notifications.py` وجود داشته باشد که `TestNotificationRequest` را دریافت کرده و با احراز هویت کاربر، یک اعلان تستی ارسال کند. _(verify: api_response)_
21. صفحه `frontend/src/pages/Notifications.jsx` دارای یک بخش جدید با عنوان 'ارسال اعلان تستی' و دو دکمه 'ارسال تست ایمیل' و 'ارسال تست تلگرام (SMS)' باشد. _(verify: ui_interaction)_
22. کلیک بر روی دکمه 'ارسال تست ایمیل' در فرانت‌اند، یک درخواست `POST` به `/notifications/test-send` با `channel_type: 'email'` ارسال کرده و پیام موفقیت یا خطا را نمایش دهد. _(verify: ui_interaction)_
23. کلیک بر روی دکمه 'ارسال تست تلگرام (SMS)' در فرانت‌اند، یک درخواست `POST` به `/notifications/test-send` با `channel_type: 'sms'` ارسال کرده و پیام موفقیت یا خطا را نمایش دهد. _(verify: ui_interaction)_
24. متد `send_notification` در `app/services/notification_service.py` باید `notification_type='email'` را به `_send_email` و `notification_type='sms'` را به `send_sms` هدایت کند. _(verify: backend_test)_
25. مدل `UserNotificationPreference` در `app/models/notification.py` با فیلدهای `user_id`, `notification_type`, `enabled`, `sound_enabled` و یک UniqueConstraint روی `(user_id, notification_type)` تعریف شده باشد. _(verify: static)_
26. شمای `NotificationPreferenceSchema` و `NotificationPreferenceUpdate` در `app/schemas/notification_schema.py` تعریف شده باشند. _(verify: static)_
27. endpoint `GET /api/notifications/preferences` ترجیحات اعلان کاربر فعلی را برمی‌گرداند (status 200). _(verify: api_response)_
28. endpoint `PATCH /api/notifications/preferences/{notification_type}` ترجیح اعلان مشخصی را به‌روزرسانی می‌کند و ترجیح به‌روز شده را برمی‌گرداند (status 200). _(verify: api_response)_
29. صفحه `frontend/src/pages/Notifications.jsx` لیستی از انواع اعلان‌ها را با دو سوئیچ (toggle) برای فعال/غیرفعال کردن و فعال/غیرفعال کردن صدا نمایش می‌دهد. _(verify: ui_interaction)_
30. منطق ارسال اعلان در `app/services/notification_service.py` قبل از ارسال، ترجیحات کاربر را بررسی کرده و فقط اعلان‌های فعال را ارسال می‌کند. _(verify: backend_test)_
31. فایل `app/services/notification_service.py` شامل `user_registered`, `task_created`, `project_created` در `VALID_NOTIFICATION_TYPES` باشد. _(verify: static)_
32. فایل `app/services/notification_service.py` شامل پیام‌ها و عناوین پیش‌فرض فارسی برای `user_registered`, `task_created`, `project_created` در `_DEFAULT_EVENT_MESSAGES` و `_DEFAULT_EVENT_TITLES` باشد. _(verify: static)_
33. پس از ثبت‌نام موفقیت‌آمیز یک کاربر جدید، یک اعلان با `event_name="user_registered"` برای آن کاربر در پایگاه داده ایجاد شود. _(verify: backend_test)_
34. پس از ایجاد موفقیت‌آمیز یک وظیفه جدید، یک اعلان با `event_name="task_created"` برای کاربر ایجادکننده در پایگاه داده ایجاد شود. _(verify: backend_test)_
35. پس از ایجاد موفقیت‌آمیز یک پروژه جدید، یک اعلان با `event_name="project_created"` برای کاربر ایجادکننده در پایگاه داده ایجاد شود. _(verify: backend_test)_
36. اعلان‌های ایجاد شده در صفحه اعلان‌های فرانت‌اند (`frontend/src/pages/Notifications.jsx`) برای کاربر مربوطه قابل مشاهده باشند. _(verify: ui_interaction)_
37. یک Pydantic Schema جدید به نام `NotificationEvent` در `app/schemas/notification_schema.py` تعریف شده باشد که شامل فیلدهای `event_type`, `recipient_email`, `payload`, `channels`, و `priority` باشد. _(verify: static)_
38. تابعی به نام `dispatch_notification_event` در `app/services/notification_service.py` وجود داشته باشد که یک شیء `NotificationEvent` را به عنوان ورودی بپذیرد. _(verify: static)_
39. تابع `dispatch_notification_event` بتواند بر اساس فیلد `channels` در `NotificationEvent`، ارسال اعلان به کانال‌های 'email' و 'telegram' را شبیه‌سازی کند (مثلاً با لاگ کردن پیام). _(verify: backend_test)_
40. در `app/routes/auth_google.py`، در تابع `approve_pending_user`، پس از تأیید کاربر، یک `NotificationEvent` ایجاد و از طریق `dispatch_notification_event` ارسال شود. _(verify: static)_
41. مستندات جدیدی در `docs/API.md` یا یک فایل جدید (مثلاً `docs/NOTIFICATION_INTEGRATION.md`) اضافه شده باشد که نحوه استفاده از استراتژی یکپارچه‌سازی پویا برای اعلان‌ها را توضیح دهد. _(verify: manual_only)_

## Task Steps

### Step 1: ایجاد بخش اصلی تنظیمات اطلاع‌رسانی در فرانت‌اند
**Status:** `done` (100%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی بخش اصلی رابط کاربری (UI) در فرانت‌اند است که به عنوان نقطه مرکزی برای مدیریت تمام تنظیمات اطلاع‌رسانی تلگرام و ایمیل عمل می‌کند. هدف این است که یک فضای مشخص و سازمان‌یافته برای کاربر فراهم شود، بدون اینکه بخش‌های مختلف اطلاع‌رسانی به صورت جداگانه و پراکنده نمایش داده شوند. این مرحله شامل ایجاد مسیر (route) و کامپوننت‌های اصلی UI برای این بخش است.
**Excerpt:**
```
[00:00] می‌خوام برای تلگرام اطلاع‌رسانی تلگرام و ایمیل توی فرانت یه قسمتی ایجاد بشه. [00:08] حالا لازم نیست جدا جدا مثلاً توی همون قسمتی [00:12] با هم باشن یا ذیل تنظیمات هر جا که شلوغش هم نکنیم.
```

### Step 2: پیاده‌سازی مدیریت توکن و آیدی دستگاه برای تلگرام و تنظیمات ایمیل (با تفکیک)
**Status:** `done` (100%)
**Scope:** این مرحله شامل توسعه رابط کاربری (UI) و منطق بک‌اند برای افزودن، ثبت، و مدیریت توکن‌های تلگرام و آیدی دستگاه‌ها، و همچنین تنظیمات مربوط به اطلاع‌رسانی ایمیل است. تنظیمات هر دو پلتفرم (تلگرام و ایمیل) باید به صورت جداگانه مدیریت شوند. این شامل تعریف مدل داده برای ذخیره‌سازی این اطلاعات و ایجاد APIهای لازم برای عملیات CRUD (ایجاد، خواندن، به‌روزرسانی، حذف) در بک‌اند است.
**Excerpt:**
```
[00:24] و بتونم مثلاً توکنی که اضافه می‌کنم، آیدی دستگاه‌ها رو هم [00:30] بتونم اضافه کنم، لازم نباشه اینا رو تو هندلر اضافه بکنم. [00:33] تو همون جا بتونم اضافه کنم، ثبتش بکنم و ... [00:48] حالا برای تلگرام و ایمیل هر کدوم شرایط هر کدومشون باید [00:53] تنظیماتشون هم جدا باشه.
```

### Step 3: توسعه سرویس مرکزی ارسال اطلاع‌رسانی (Notification Dispatcher) و API مربوطه
**Status:** `done` (100%)
**Scope:** این مرحله بر روی طراحی و پیاده‌سازی یک سرویس بک‌اند متمرکز است که به عنوان هسته سیستم اطلاع‌رسانی عمل می‌کند. این سرویس باید قادر باشد درخواست‌های اطلاع‌رسانی را از بخش‌های مختلف برنامه دریافت کرده و آن‌ها را بر اساس تنظیمات کاربر (ذخیره شده در مرحله ۲) به تلگرام یا ایمیل ارسال کند. طراحی باید به گونه‌ای باشد که 'پربار و جامع' بوده و 'داینامیک' باشد تا بتواند با قابلیت‌های جدید برنامه به راحتی 'مچ' شود.
**Excerpt:**
```
[00:17] و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه و ... [00:55] و آره یه جور داینامیک باشه که هرچی هم اضافه میشه [01:01] اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.
```

### Step 4: پیاده‌سازی قابلیت ارسال اطلاع‌رسانی تستی (UI فرانت‌اند و Endpoint بک‌اند)
**Status:** `done` (100%)
**Scope:** این مرحله شامل توسعه یک دکمه یا رابط کاربری مشابه در فرانت‌اند است که به کاربر اجازه می‌دهد یک اطلاع‌رسانی تستی را برای تلگرام و ایمیل ارسال کند. همچنین، یک Endpoint در بک‌اند باید پیاده‌سازی شود که این درخواست تستی را دریافت کرده و از طریق سرویس Notification Dispatcher (مرحله ۳) یک اطلاع‌رسانی آزمایشی را ارسال کند. این قابلیت برای تأیید صحت تنظیمات کاربر ضروری است.
**Excerpt:**
```
[00:33] و بتونم تست بزنم.
```

### Step 5: پیاده‌سازی تنظیمات پویا برای انواع اطلاع‌رسانی (UI فرانت‌اند و منطق بک‌اند)
**Status:** `done` (100%)
**Scope:** این مرحله شامل توسعه رابط کاربری (UI) است که به کاربر اجازه می‌دهد انتخاب کند 'چیا اعلام بشه، چیا اعلام نشه'. این بدان معناست که کاربر می‌تواند انواع خاصی از رویدادها یا اطلاعات را برای دریافت اطلاع‌رسانی فعال یا غیرفعال کند. در بک‌اند، منطق لازم برای ذخیره‌سازی این ترجیحات و فیلتر کردن اطلاع‌رسانی‌ها بر اساس آن‌ها باید پیاده‌سازی شود. این نیازمند مکانیزمی برای تعریف و ثبت انواع اطلاع‌رسانی‌های موجود در سیستم است.
— [merged] این مرحله شامل توسعه رابط کاربری (UI) است که به کاربر امکان می‌دهد تعیین کند 'چیا با صدا اعلام بشه، چیا بدون صدا اعلام بشه'. این تنظیمات باید برای هر نوع اطلاع‌رسانی (که در مرحله ۵ مدیریت می‌شود) قابل پیکربندی باشد. در بک‌اند، منطق لازم برای ذخیره‌سازی این ترجیحات و ارسال دستورالعمل‌های مربوط به صدا به سرویس‌های اطلاع‌رسانی (در صورت پشتیبانی) یا مدیریت آن در سمت کلاینت (در صورت امکان) باید پیاده‌سازی شود.
**Excerpt:**
```
[00:36] و بتونم تنظیم کنم چیا اعلام بشه، چیا اعلام نشه.
```

### Step 6: یکپارچه‌سازی ماژول‌های موجود برنامه با سیستم اطلاع‌رسانی جدید
**Status:** `done` (100%)
**Scope:** این مرحله شامل شناسایی و اصلاح ماژول‌ها و رویدادهای موجود در برنامه است که باید از طریق سیستم اطلاع‌رسانی جدید (با استفاده از سرویس Notification Dispatcher توسعه یافته در مرحله ۳) اطلاع‌رسانی ارسال کنند. هدف این است که 'تمام چیزهایی که توی برنامه هست' به این سیستم 'متصل بشن' تا از قابلیت‌های جامع و متمرکز آن بهره‌مند شوند. این شامل فراخوانی API سرویس Dispatcher در نقاط مناسب کد موجود است.
**Excerpt:**
```
[00:17] و تمام چیزهایی که توی برنامه هست و قرار بیاد باید متصل بشن به این و خیلی پربار و جامع باشه.
```

### Step 7: تعریف و مستندسازی استراتژی یکپارچه‌سازی پویا برای قابلیت‌های آینده
**Status:** `done` (100%)
**Scope:** این مرحله بر روی ایجاد یک چارچوب یا دستورالعمل‌های واضح برای توسعه‌دهندگان آینده تمرکز دارد تا بتوانند به راحتی قابلیت‌های جدید برنامه را با سیستم اطلاع‌رسانی 'مچ' کنند. هدف این است که سیستم 'داینامیک' باشد و 'هرچی هم اضافه میشه' بتواند خود را با آن تطبیق دهد. این شامل تعریف الگوهای طراحی، توابع کمکی، یا مستندسازی فرآیند ثبت رویدادهای جدید اطلاع‌رسانی در سرویس Dispatcher است.
**Excerpt:**
```
[00:55] و آره یه جور داینامیک باشه که هرچی هم اضافه میشه [01:01] اون اطلاع‌رسانی تلگرام و ایمیل بتونه خودشو با اون مچ بکنه.
```
