---
task_id: 513a8b39-2bbe-402e-9c0c-b4cb8f2043df
title: پیاده‌سازی سرویس برنامه‌ریز (planner_service)
type: feature_request
priority: high
execution_priority: 100
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-19T20:23:24.866977+00:00'
updated_at: '2026-05-25T06:42:17.616303+00:00'
archived: true
archived_at: '2026-05-25T06:42:17.616303+00:00'
tags:
- merged
target_files:
- app/services/planner_service.py
---

# پیاده‌سازی سرویس برنامه‌ریز (planner_service)

## Raw Idea

فایل app/services/planner_service.py وجود دارد اما به نظر می‌رسد فقط یک placeholder است. هیچ منطق واقعی برای برنامه‌ریزی وظایف یا تولید برنامه روزانه پیاده‌سازی نشده است. این یکی از核心ترین قابلیت‌های پروژه (مدیریت زمان و برنامه‌ریزی) است.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
سرویس برنامه‌ریز (planner_service) پیاده‌سازی نشده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/planner_service.py:1-50` — `PlannerService` — کل سرویس باید پیاده‌سازی شود
  ```python
  class PlannerService:
      def __init__(self):
          pass
      
      async def generate_daily_plan(self, user_id: int):
          # TODO: Implement
          return []
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
Python 3.10+ با asyncio

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/task.py` (سطر 1) — مدل وظایف که برای برنامه‌ریزی استفاده می‌شود
- `app/services/ai_service.py` (سطر 1) — برای پیشنهادات هوشمند برنامه‌ریزی

## 🌐 نقشهٔ وابستگی‌ها
این سرویس توسط routeهای tasks و projects استفاده خواهد شد. همچنین با notification_service برای یادآوری‌ها ارتباط دارد.

## 🔍 Context و وضعیت فعلی
فایل app/services/planner_service.py وجود دارد اما به نظر می‌رسد فقط یک placeholder است. هیچ منطق واقعی برای برنامه‌ریزی وظایف یا تولید برنامه روزانه پیاده‌سازی نشده است. این یکی از核心ترین قابلیت‌های پروژه (مدیریت زمان و برنامه‌ریزی) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] سرویس planner_service می‌تواند برنامه روزانه تولید کند
- [ ] برنامه تولیدشده شامل وظایف اولویت‌بندی‌شده است
- [ ] تست‌های واحد برای هر متد سرویس اضافه شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. پیاده‌سازی کامل سرویس planner_service با قابلیت‌های: دریافت وظایف از دیتابیس، اولویت‌بندی، زمان‌بندی، و تولید برنامه روزانه/هفتگی. از ai_service برای پیشنهادات هوشمند استفاده کنید.

## 💡 نمونه‌های قبل/بعد
**پیاده‌سازی متد generate_daily_plan**

_قبل:_
```
async def generate_daily_plan(self, user_id: int):
    # TODO: Implement
    return []
```

_بعد:_
```
async def generate_daily_plan(self, user_id: int):
    tasks = await self.get_user_tasks(user_id)
    prioritized = self.prioritize_tasks(tasks)
    schedule = self.create_schedule(prioritized)
    return schedule
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_planner.py -v`
- `python -c "from app.services.planner_service import PlannerService; print('OK')"`

## ⚠️ ریسک‌ها و موارد احتیاط
وابستگی به پیاده‌سازی ai_service؛ ممکن است نیاز به تغییر مدل دیتابیس داشته باشد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

## Acceptance Criteria

1. سرویس planner_service می‌تواند برنامه روزانه تولید کند _(verify: api_response)_
2. برنامه تولیدشده شامل وظایف اولویت‌بندی‌شده است _(verify: api_response)_
3. تست‌های واحد برای هر متد سرویس اضافه شود _(verify: backend_test)_
