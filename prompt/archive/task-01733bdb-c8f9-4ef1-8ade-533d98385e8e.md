---
task_id: 01733bdb-c8f9-4ef1-8ade-533d98385e8e
title: مدیریت وضعیت (state management) پیاده‌سازی نشده است
type: refactor
priority: medium
execution_priority: 100
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-19T20:23:42.316148+00:00'
updated_at: '2026-05-25T06:39:24.095324+00:00'
archived: true
archived_at: '2026-05-25T06:39:24.095324+00:00'
tags:
- merged
target_files:
- frontend/src/context/AuthContext.tsx
- frontend/src/context/ProjectContext.tsx
- frontend/src/context/TaskContext.tsx
---

# مدیریت وضعیت (state management) پیاده‌سازی نشده است

## Raw Idea

هیچ فایل یا کتابخانه‌ای برای مدیریت وضعیت (مانند Redux, Zustand, Context API) در پروژه یافت نشد. برای مدیریت وضعیت کاربر (لاگین/لاگوت)، وضعیت پروژه‌ها و وظایف، نیاز به یک راهکار مدیریت وضعیت است.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
مدیریت وضعیت (state management) پیاده‌سازی نشده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/context/AuthContext.tsx:1-50` — `AuthContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/context/ProjectContext.tsx:1-50` — `ProjectContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/context/TaskContext.tsx:1-50` — `TaskContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
React Context API + TypeScript

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/auth_service.py` (سطر 1) — سرویس احراز هویت backend
- `app/services/planner_service.py` (سطر 1) — سرویس برنامه‌ریزی backend

## 🌐 نقشهٔ وابستگی‌ها
این contextها توسط تمام صفحات و کامپوننت‌ها برای دسترسی به وضعیت کاربر و داده‌ها استفاده می‌شوند.

## 🔍 Context و وضعیت فعلی
هیچ فایل یا کتابخانه‌ای برای مدیریت وضعیت (مانند Redux, Zustand, Context API) در پروژه یافت نشد. برای مدیریت وضعیت کاربر (لاگین/لاگوت)، وضعیت پروژه‌ها و وظایف، نیاز به یک راهکار مدیریت وضعیت است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] AuthContext وضعیت کاربر (user, token, isAuthenticated) را مدیریت کند
- [ ] ProjectContext لیست پروژه‌ها را مدیریت کند
- [ ] TaskContext لیست وظایف را مدیریت کند
- [ ] هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. پیاده‌سازی مدیریت وضعیت با استفاده از React Context API یا Zustand برای مدیریت وضعیت کاربر (user, token, isAuthenticated) و وضعیت پروژه‌ها و وظایف.

## 💡 نمونه‌های قبل/بعد
**ایجاد AuthContext**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
export const AuthContext = createContext({ user: null, login: async (email, password) => {}, logout: () => {} });
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `بررسی کنسول مرورگر برای خطاهای context`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به هماهنگی با backend برای نوع داده‌ها

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. AuthContext وضعیت کاربر (user, token, isAuthenticated) را مدیریت کند _(verify: static)_
2. ProjectContext لیست پروژه‌ها را مدیریت کند _(verify: static)_
3. TaskContext لیست وظایف را مدیریت کند _(verify: static)_
4. هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد _(verify: static)_

## Task Steps

### Step 1: ایجاد AuthContext برای مدیریت وضعیت احراز هویت کاربر
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد فایل frontend/src/context/AuthContext.tsx با استفاده از React Context API و TypeScript است. AuthContext باید وضعیت لاگین/لاگوت کاربر، توکن احراز هویت و اطلاعات کاربر را مدیریت کند. این context باید با سرویس backend app/services/auth_service.py برای عملیات لاگین، لاگوت و بررسی وضعیت احراز هویت ارتباط برقرار کند. خارج از این مرحله: پیاده‌سازی ProjectContext و TaskContext، اتصال به کامپوننت‌های UI، و مدیریت خطاهای خاص. نکته حیاتی: این context باید از React.createContext و useReducer یا useState برای مدیریت وضعیت استفاده کند و Provider کامپوننت را در بالاترین سطح اپلیکیشن قرار دهد.
**Excerpt:**
```
- `frontend/src/context/AuthContext.tsx:1-50` — `AuthContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `app/services/auth_service.py` (سطر 1) — سرویس احراز هویت backend
```

### Step 2: ایجاد ProjectContext برای مدیریت وضعیت پروژه‌ها
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد فایل frontend/src/context/ProjectContext.tsx با استفاده از React Context API و TypeScript است. ProjectContext باید وضعیت لیست پروژه‌ها، پروژه جاری، و عملیات CRUD روی پروژه‌ها را مدیریت کند. این context باید با سرویس backend app/services/planner_service.py برای دریافت و ذخیره پروژه‌ها ارتباط برقرار کند. خارج از این مرحله: پیاده‌سازی AuthContext و TaskContext، اتصال به کامپوننت‌های UI، و مدیریت خطاهای خاص. نکته حیاتی: این context باید از React.createContext و useReducer برای مدیریت وضعیت پیچیده پروژه‌ها استفاده کند و Provider را در سطح مناسب اپلیکیشن قرار دهد.
**Excerpt:**
```
- `frontend/src/context/ProjectContext.tsx:1-50` — `ProjectContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `app/services/planner_service.py` (سطر 1) — سرویس برنامه‌ریزی backend
```

### Step 3: ایجاد TaskContext برای مدیریت وضعیت وظایف
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد فایل frontend/src/context/TaskContext.tsx با استفاده از React Context API و TypeScript است. TaskContext باید وضعیت لیست وظایف، وظیفه جاری، و عملیات CRUD روی وظایف را مدیریت کند. این context باید با سرویس backend app/services/planner_service.py برای دریافت و ذخیره وظایف مرتبط با پروژه جاری ارتباط برقرار کند. خارج از این مرحله: پیاده‌سازی AuthContext و ProjectContext، اتصال به کامپوننت‌های UI، و مدیریت خطاهای خاص. نکته حیاتی: این context باید از React.createContext و useReducer برای مدیریت وضعیت پیچیده وظایف استفاده کند و به ProjectContext وابسته باشد تا وظایف مربوط به پروژه جاری را نمایش دهد.
**Excerpt:**
```
- `frontend/src/context/TaskContext.tsx:1-50` — `TaskContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `app/services/planner_service.py` (سطر 1) — سرویس برنامه‌ریزی backend
```

### Step 4: یکپارچه‌سازی Context‌ها و ارائه Provider در بالاترین سطح اپلیکیشن
**Status:** `partial` (33%)
**Scope:** این مرحله شامل یکپارچه‌سازی سه Context ایجاد شده (AuthContext، ProjectContext، TaskContext) و قرار دادن Providerهای آن‌ها در بالاترین سطح اپلیکیشن (معمولاً App.tsx یا main.tsx) است. باید ترتیب nesting Providerها به درستی رعایت شود: AuthContext در بیرونی‌ترین لایه، سپس ProjectContext، و سپس TaskContext در داخلی‌ترین لایه. خارج از این مرحله: پیاده‌سازی خود Context‌ها، اتصال به کامپوننت‌های UI خاص، و مدیریت خطاهای خاص. نکته حیاتی: Providerها باید به صورت nested و با ترتیب صحیح قرار گیرند تا Contextهای داخلی بتوانند به Contextهای بیرونی دسترسی داشته باشند.
**Excerpt:**
```
این contextها توسط تمام صفحات و کامپوننت‌ها برای دسترسی به وضعیت کاربر و داده‌ها استفاده می‌شوند.

## 🔍 Context و وضعیت فعلی
هیچ فایل یا کتابخانه‌ای برای مدیریت وضعیت (مانند Redux, Zustand, Context API) در پروژه یافت نشد. برای مدیریت وضعیت کاربر (لاگین/لاگوت)، وضعیت پروژه‌ها و وظایف، نیاز به یک راهکار مدیریت وضعیت است.
```
