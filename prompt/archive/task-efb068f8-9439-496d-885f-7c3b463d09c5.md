---
task_id: efb068f8-9439-496d-885f-7c3b463d09c5
title: کامپوننت‌های فرانت‌اند برای صفحات اصلی (ورود، ثبت‌نام، داشبورد) وجود ندارند
type: bug
priority: critical
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-19T20:23:24.717584+00:00'
updated_at: '2026-05-29T20:24:16.395648+00:00'
archived: true
archived_at: '2026-05-19T20:24:40.190035+00:00'
target_files:
- frontend/src/pages/login.tsx
- frontend/src/pages/register.tsx
- frontend/src/pages/dashboard.tsx
- frontend/src/pages/projects.tsx
- frontend/src/pages/tasks.tsx
- frontend/src/pages/notifications.tsx
---

# کامپوننت‌های فرانت‌اند برای صفحات اصلی (ورود، ثبت‌نام، داشبورد) وجود ندارند

## Raw Idea

با بررسی فایل‌های پروژه، هیچ فایل فرانت‌اندی (مانند pages/login.tsx, components/Dashboard.tsx) یافت نشد. این بدان معناست که کل فرانت‌اند یا در ریپو موجود نیست یا در شاخه دیگری قرار دارد. بدون این صفحات، کاربر نمی‌تواند از برنامه استفاده کند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
کامپوننت‌های فرانت‌اند برای صفحات اصلی (ورود، ثبت‌نام، داشبورد) وجود ندارند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/login.tsx:1-50` — `LoginPage` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/pages/register.tsx:1-50` — `RegisterPage` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/pages/dashboard.tsx:1-80` — `DashboardPage` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/pages/projects.tsx:1-60` — `ProjectsPage` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/pages/tasks.tsx:1-60` — `TasksPage` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/pages/notifications.tsx:1-40` — `NotificationsPage` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
React/Next.js + FastAPI + REST API

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/auth.py` (سطر 1) — endpoint‌های ورود و ثبت‌نام
- `app/routes/projects.py` (سطر 1) — endpoint‌های پروژه‌ها
- `app/routes/tasks.py` (سطر 1) — endpoint‌های وظایف
- `app/routes/notifications.py` (سطر 1) — endpoint‌های اعلان‌ها

## 🌐 نقشهٔ وابستگی‌ها
این صفحات به سرویس‌های backend و کتابخانه‌های فرانت‌اند (axios, react-router) وابسته هستند.

## 🔍 Context و وضعیت فعلی
با بررسی فایل‌های پروژه، هیچ فایل فرانت‌اندی (مانند pages/login.tsx, components/Dashboard.tsx) یافت نشد. این بدان معناست که کل فرانت‌اند یا در ریپو موجود نیست یا در شاخه دیگری قرار دارد. بدون این صفحات، کاربر نمی‌تواند از برنامه استفاده کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] صفحه ورود در مسیر /login قابل دسترسی باشد
- [ ] صفحه ثبت‌نام در مسیر /register قابل دسترسی باشد
- [ ] صفحه داشبورد در مسیر /dashboard قابل دسترسی باشد
- [ ] هر صفحه به endpoint مربوطه متصل شود و داده‌ها را نمایش دهد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد ساختار کامل فرانت‌اند با React/Next.js شامل صفحات: ورود، ثبت‌نام، داشبورد، پروژه‌ها، وظایف، تنظیمات و اعلان‌ها. هر صفحه باید به endpoint مربوطه متصل شود.

## 💡 نمونه‌های قبل/بعد
**ایجاد صفحه ورود**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
export default function LoginPage() { return <form onSubmit={handleLogin}>...</form>; }
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `curl http://localhost:3000/login`
- `curl http://localhost:3000/dashboard`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به تعریف مسیرها و احراز هویت در فرانت‌اند

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: large

## Acceptance Criteria

1. صفحه ورود در مسیر /login قابل دسترسی باشد _(verify: ui_interaction)_
2. صفحه ثبت‌نام در مسیر /register قابل دسترسی باشد _(verify: ui_interaction)_
3. صفحه داشبورد در مسیر /dashboard قابل دسترسی باشد _(verify: ui_interaction)_
4. هر صفحه به endpoint مربوطه متصل شود و داده‌ها را نمایش دهد _(verify: ui_interaction)_

## Task Steps

### Step 1: ایجاد صفحات اصلی فرانت‌اند (ورود، ثبت‌نام، داشبورد، پروژه‌ها، وظایف، اعلان‌ها)
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد شش فایل صفحه فرانت‌اند است که در مسیر frontend/src/pages/ قرار می‌گیرند: login.tsx، register.tsx، dashboard.tsx، projects.tsx، tasks.tsx، notifications.tsx. هر صفحه باید یک کامپوننت React/Next.js ساده با placeholder UI باشد که قابلیت نمایش اولیه را داشته باشد. خارج از این مرحله: اتصال به API، مدیریت state، احراز هویت، استایل‌دهی پیشرفته، و منطق تجاری. نکته حیاتی: این صفحات باید با ساختار مسیریابی موجود در پروژه هماهنگ باشند و از naming convention یکسان پیروی کنند.
**Excerpt:**
```
کامپوننت‌های فرانت‌اند برای صفحات اصلی (ورود، ثبت‌نام، داشبورد) وجود ندارند

- `frontend/src/pages/login.tsx:1-50` — `LoginPage` — نیاز به ایجاد
- `frontend/src/pages/register.tsx:1-50` — `RegisterPage` — نیاز به ایجاد
- `frontend/src/pages/dashboard.tsx:1-80` — `DashboardPage` — نیاز به ایجاد
- `frontend/src/pages/projects.tsx:1-60` — `ProjectsPage` — نیاز به ایجاد
- `frontend/src/pages/tasks.tsx:1-60` — `TasksPage` — نیاز به ایجاد
- `frontend/src/pages/notifications.tsx:1-40` — `NotificationsPage` — نیاز به ایجاد
```

### Step 2: بررسی و تکمیل endpoint‌های بک‌اند مرتبط با صفحات فرانت‌اند
**Status:** `done` (100%)
**Scope:** این مرحله شامل بررسی و در صورت نیاز تکمیل endpoint‌های بک‌اند در فایل‌های app/routes/auth.py، app/routes/projects.py، app/routes/tasks.py است. باید اطمینان حاصل شود که endpoint‌های ورود، ثبت‌نام، پروژه‌ها، وظایف و اعلان‌ها وجود دارند و با نیازهای صفحات فرانت‌اند هماهنگ هستند. خارج از این مرحله: ایجاد صفحات فرانت‌اند، تست‌های جامع، و تغییرات در ساختار دیتابیس. نکته حیاتی: endpoint‌ها باید با naming convention و ساختار RESTful موجود در پروژه هماهنگ باشند.
**Excerpt:**
```
فایل‌های مرتبط (Cross-references)
- `app/routes/auth.py` (سطر 1) — endpoint‌های ورود و ثبت‌نام
- `app/routes/projects.py` (سطر 1) — endpoint‌های پروژه‌ها
- `app/routes/tasks.py` (سطر 1) — endpoint‌های وظایف
- `app/routes/noti...`
```

### Step 3: یکپارچه‌سازی صفحات فرانت‌اند با API بک‌اند و تست end-to-end
**Status:** `done` (100%)
**Scope:** این مرحله شامل اتصال صفحات فرانت‌اند ایجاد شده در مرحله 1 به endpoint‌های بک‌اند از مرحله 2 است. باید فراخوانی‌های API برای ورود، ثبت‌نام، دریافت پروژه‌ها، وظایف و اعلان‌ها در صفحات مربوطه پیاده‌سازی شود. همچنین یک تست end-to-end ساده برای اطمینان از کارکرد صحیح جریان کاربر (ثبت‌نام → ورود → مشاهده داشبورد) انجام می‌شود. خارج از این مرحله: استایل‌دهی پیشرفته، مدیریت خطاهای پیچیده، و بهینه‌سازی performance. نکته حیاتی: از fetch API یا axios برای فراخوانی‌ها استفاده شود و پاسخ‌ها به درستی در UI نمایش داده شوند.
**Excerpt:**
```
با بررسی فایل‌های پروژه، هیچ فایل فرانت‌اندی (مانند pages/login.tsx, components/Dashboard.tsx) یافت نشد. این بدان معناست که کل فرانت‌اند یا در ریپو موجود نیست یا در شاخه دیگری قرار دارد. بدون این صفحات، کاربر نمی‌تواند از برنامه استفاده کند.

🧱 پشتهٔ فناوری و معماری
React/Next.js + FastAPI + REST API
```
