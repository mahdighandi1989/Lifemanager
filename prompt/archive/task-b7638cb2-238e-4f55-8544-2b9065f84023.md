---
task_id: b7638cb2-238e-4f55-8544-2b9065f84023
title: Align User and OAuthUser in Auth Pipeline
type: logic_audit
priority: medium
execution_priority: 3350
status: done
external_status: done
verification_status: done
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:27:42.490198+00:00'
updated_at: '2026-06-05T04:46:28.606207+00:00'
archived: true
archived_at: '2026-06-05T04:46:22.522916+00:00'
target_files:
- app/dependencies/auth.py
- app/models/user_oauth.py
- tests/test_auth_dependencies.py
---

# Align User and OAuthUser in Auth Pipeline

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

`app/dependencies/auth.py` lists `app.models.user.User` as an interaction, while `app/models/user_oauth.py` defines `OAuthUser` and is described as defining 'the SQLAlchemy ORM model for an OAuthUser entity'. It's unclear if `app.models.user.User` refers to the `OAuthUser` defined in `user_oauth.py` or if there's a separate, undefined `User` model. This inconsistency in naming and lack of clarity about which model is the primary user entity can lead to confusion, potential errors in data access,

## 💥 پیامد (impact)
If different user models are implicitly used or if the naming is inconsistent, it could lead to: Incorrect data retrieval or storage; Bugs in authentication or authorization logic if the wrong model's attributes (e.g., roles, permissions) are accessed; Maintenance overhead due to confusion; Security

## 🛠 پیشنهاد رفع اولیه
Clarify the relationship between `User` and `OAuthUser`. If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`. If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

`app/dependencies/auth.py` lists `app.models.user.User` as an interaction, while `app/models/user_oauth.py` defines `OAuthUser` and is described as defining 'the SQLAlchemy ORM model for an OAuthUser entity'. It's unclear if `app.models.user.User` refers to the `OAuthUser` defined in `user_oauth.py` or if there's a separate, undefined `User` model. This inconsistency in naming and lack of clarity about which model is the primary user entity can lead to confusion, potential errors in data access,

## 💥 پیامد (impact)
If different user models are implicitly used or if the naming is inconsistent, it could lead to: Incorrect data retrieval or storage; Bugs in authentication or authorization logic if the wrong model's attributes (e.g., roles, permissions) are accessed; Maintenance overhead due to confusion; Security

## 🛠 پیشنهاد رفع اولیه
Clarify the relationship between `User` and `OAuthUser`. If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`. If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

## 📋 چک‌لیست مراحل (5 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: تحلیل و مستندسازی رابطه بین User و OAuthUser در pipeline احراز هویت** — این مرحله شامل تحلیل کامل کدهای موجود در فایل‌های app/dependencies/auth.py و app/models/user_oauth.py برای شناسایی دقیق نحوه ارجاع به مدل‌های User و OAuthUser است. باید مشخص شود که آیا User در auth.py به OAuthUser اشاره دارد یا یک مدل مجزاست. خروجی این مرحله یک مستندات داخلی (مثلاً در قالب کامنت یا 
- [x] **مرحله 2: به‌روزرسانی ارجاعات در app/dependencies/auth.py برای استفاده از OAuthUser در صورت یکسان بودن** — این مرحله شامل تغییر مستقیم کد در فایل app/dependencies/auth.py است. اگر در مرحله قبل مشخص شد که User همان OAuthUser است، تمام ارجاعات به User (مانند app.models.user.User) باید به app.models.user_oauth.OAuthUser تغییر یابند. همچنین importهای مربوطه به‌روزرسانی می‌شوند. خارج از این مرحله است: تغییر د
- [x] **مرحله 3: تعریف و مستندسازی مدل مجزای User در app/models/user.py در صورت وجود دو مدل مجزا** — این مرحله شامل ایجاد فایل جدید app/models/user.py (در صورت عدم وجود) یا به‌روزرسانی آن است. اگر تحلیل مرحله 1 نشان دهد که User و OAuthUser دو مدل مجزا هستند، باید مدل User به‌طور کامل تعریف شود (شامل فیلدها، روابط، و نقش آن در pipeline). همچنین مستندات نقش هر مدل و نحوه تعامل آنها در احراز هویت اضاف
- [x] **مرحله 4: به‌روزرسانی ارجاعات در auth.py برای استفاده از مدل مناسب در صورت وجود دو مدل مجزا** — این مرحله شامل تغییر در فایل app/dependencies/auth.py است. اگر دو مدل مجزا (User و OAuthUser) وجود داشته باشند، باید مشخص شود که auth.py باید به کدام مدل ارجاع دهد (بر اساس نقش آنها). سپس ارجاعات به‌روزرسانی می‌شوند تا از مدل صحیح استفاده کنند. خارج از این مرحله است: تغییر در logic احراز هویت، تغییر
- [x] **مرحله 5: نوشتن تست‌های unit برای بررسی صحت ارجاعات مدل در auth.py** — این مرحله شامل نوشتن تست‌های unit برای فایل app/dependencies/auth.py است تا اطمینان حاصل شود که ارجاعات به مدل‌های User یا OAuthUser صحیح و سازگار هستند. تست‌ها باید بررسی کنند که importها درست هستند و type hints با مدل‌های واقعی مطابقت دارند. خارج از این مرحله است: تست‌های integration، تست‌های end-

---

# 🔹 مرحله 1: تحلیل و مستندسازی رابطه بین User و OAuthUser در pipeline احراز هویت

**Scope:** این مرحله شامل تحلیل کامل کدهای موجود در فایل‌های app/dependencies/auth.py و app/models/user_oauth.py برای شناسایی دقیق نحوه ارجاع به مدل‌های User و OAuthUser است. باید مشخص شود که آیا User در auth.py به OAuthUser اشاره دارد یا یک مدل مجزاست. خروجی این مرحله یک مستندات داخلی (مثلاً در قالب کامنت یا فایل README) است که رابطه این دو مدل را توضیح می‌دهد. خارج از این مرحله است: تغییر مستقیم کد، رفع باگ، یا ایجاد مدل جدید. نکته حیاتی: این مرحله صرفاً تحلیلی است و هیچ تغییری در کد ایجاد نمی‌کند.
**Key terms:** app/dependencies/auth.py, app/models/user_oauth.py, User, OAuthUser, app.models.user.User, app.models.user_oauth.OAuthUser

**بخش مربوط از متن کاربر:**
```
Clarify the relationship between `User` and `OAuthUser`. If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`. If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.
```

## 🎯 هدف (خلاصه ساختاریافته)
تحلیل و مستندسازی رابطه User و OAuthUser در pipeline احراز هویت

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
تحلیل و مستندسازی رابطه بین User و OAuthUser در pipeline احراز هویت. این مرحله شامل تحلیل کامل کدهای موجود در فایل‌های app/dependencies/auth.py و app/models/user_oauth.py برای شناسایی دقیق نحوه ارجاع به مدل‌های User و OAuthUser است. باید مشخص شود که آیا User در auth.py به OAuthUser اشاره دارد یا یک مدل مجزاست. خروجی این مرحله یک مستندات داخلی (مثلاً در قالب کامنت یا فایل README) است که رابطه این دو مدل را توضیح می‌دهد. خارج از این مرحله است: تغییر مستقیم کد، رفع باگ، یا ایجاد مدل جدید. نکته حیاتی: این مرحله صرفاً تحلیلی است و هیچ تغییری در کد ایجاد نمی‌کند.

--- بخش مربوط از درخواست اصلی کاربر ---
Clarify the relationship between `User` and `OAuthUser`. If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`. If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.

--- کلیدواژه‌ها ---
app/dependencies/auth.py, app/models/user_oauth.py, User, OAuthUser, app.models.user.User, app.models.user_oauth.OAuthUser

شواهد در کد واقعی پروژه:
- فایل app/models/user_oauth.py (خطوط 1-26): مدل OAuthUser را با فیلدهای email, name, role, permissions, status تعریف می‌کند. این مدل در جدول oauth_users ذخیره می‌شود.
- فایل app/models/user.py: مدل User را تعریف می‌کند (محتوای کامل آن در deep context موجود نیست، اما در ساختار پروژه وجود دارد).
- فایل app/dependencies/auth.py: وابستگی‌های احراز هویت را مدیریت می‌کند (محتوای کامل آن در deep context موجود نیست، اما در نقشه Importها مشخص است که توسط 20 فایل import می‌شود).
- فایل app/routes/auth_google.py: از OAuthUser برای مدیریت کاربران OAuth استفاده می‌کند.
- فایل app/routes/auth.py: از User برای احراز هویت سنتی (نام کاربری/رمز عبور) استفاده می‌کند.
- فایل app/main.py (خطوط 656-659): روتر auth_google را به صورت شرطی mount می‌کند (فقط در صورت تنظیم GOOGLE_CLIENT_ID).

تحلیل اولیه: دو مدل مجزای User و OAuthUser در پروژه وجود دارند. User برای احراز هویت سنتی (نام کاربری/رمز عبور) و OAuthUser برای احراز هویت از طریق Google OAuth استفاده می‌شود. این دو مدل در جداول مجزا (users و oauth_users) ذخیره می‌شوند و ارتباط مستقیمی با یکدیگر ندارند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. تحلیل کامل فایل app/dependencies/auth.py برای شناسایی نحوه ارجاع به مدل‌های User و OAuthUser.
2. تحلیل کامل فایل app/models/user.py برای شناسایی ساختار مدل User.
3. تحلیل کامل فایل app/models/user_oauth.py برای شناسایی ساختار مدل OAuthUser.
4. مقایسه دو مدل و شناسایی تفاوت‌ها و شباهت‌ها.
5. شناسایی تمام فایل‌هایی که از User یا OAuthUser استفاده می‌کنند (با استفاده از نقشه Importها).
6. ایجاد یک فایل مستندات داخلی (مثلاً docs/AUTH_MODELS.md) که رابطه این دو مدل را توضیح می‌دهد.
7. افزودن کامنت‌های توضیحی در فایل‌های app/dependencies/auth.py و app/models/user_oauth.py برای شفاف‌سازی.
8. اطمینان از اینکه هیچ تغییری در کد ایجاد نمی‌شود (صرفاً تحلیلی).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: docs
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: به‌روزرسانی ارجاعات در app/dependencies/auth.py برای استفاده از OAuthUser در صورت یکسان بودن

**Scope:** این مرحله شامل تغییر مستقیم کد در فایل app/dependencies/auth.py است. اگر در مرحله قبل مشخص شد که User همان OAuthUser است، تمام ارجاعات به User (مانند app.models.user.User) باید به app.models.user_oauth.OAuthUser تغییر یابند. همچنین importهای مربوطه به‌روزرسانی می‌شوند. خارج از این مرحله است: تغییر در مدل‌های دیگر، تغییر در logic احراز هویت، یا ایجاد مدل جدید. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که تحلیل مرحله 1 نشان دهد User و OAuthUser یکی هستند.
**Key terms:** app/dependencies/auth.py, app.models.user_oauth.OAuthUser, app.models.user.User, OAuthUser, User

**بخش مربوط از متن کاربر:**
```
If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`.
```

## 🎯 هدف (خلاصه ساختاریافته)
به‌روزرسانی ارجاعات User به OAuthUser در app/dependencies/auth.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/dependencies/auth.py:1-50 (تخمینی — فایل deep-read نشده)` — `get_current_user (تخمینی)`
  ```python
  فایل deep-read نشده است.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
تحلیل درخواست کاربر: کاربر درخواست 'به‌روزرسانی ارجاعات در app/dependencies/auth.py برای استفاده از OAuthUser در صورت یکسان بودن' را داده است. این درخواست از نوع logic_audit با اولویت medium است. متن کامل کاربر: 'این مرحله شامل تغییر مستقیم کد در فایل app/dependencies/auth.py است. اگر در مرحله قبل مشخص شد که User همان OAuthUser است، تمام ارجاعات به User (مانند app.models.user.User) باید به app.models.user_oauth.OAuthUser تغییر یابند. همچنین importهای مربوطه به‌روزرسانی می‌شوند. خارج از این مرحله است: تغییر در مدل‌های دیگر، تغییر در logic احراز هویت، یا ایجاد مدل جدید. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که تحلیل مرحله 1 نشان دهد User و OAuthUser یکی هستند.' همچنین بخش مربوط از درخواست اصلی کاربر: 'If OAuthUser is the primary user model, update all references (e.g., in app/dependencies/auth.py) to explicitly use app.models.user_oauth.OAuthUser.' کلیدواژه‌ها: app/dependencies/auth.py, app.models.user_oauth.OAuthUser, app.models.user.User, OAuthUser, User. شواهد در کد واقعی پروژه: فایل app/models/user_oauth.py (خطوط 1-26) مدل OAuthUser را با __tablename__ = 'oauth_users' و فیلدهای id, email, name, role, permissions, status, created_at, updated_at تعریف می‌کند. فایل app/models/user.py (deep-read نشده) مدل User را تعریف می‌کند. فایل app/dependencies/auth.py (deep-read نشده) احتمالاً از User استفاده می‌کند. فایل app/main.py (خطوط 601-659) auth_google.router را به‌صورت شرطی mount می‌کند و از settings.GOOGLE_CLIENT_ID استفاده می‌کند. فایل app/routes/auth_google.py (deep-read نشده) احتمالاً از OAuthUser استفاده می‌کند. فایل app/models/__init__.py (deep-read نشده) احتمالاً هر دو مدل را export می‌کند. تحلیل: کاربر می‌خواهد اگر User و OAuthUser یکی هستند (یعنی OAuthUser مدل اصلی کاربر است)، تمام ارجاعات به User در app/dependencies/auth.py به OAuthUser تغییر یابد. این شامل importها و type hints می‌شود. تغییرات نباید به مدل‌های دیگر، logic احراز هویت، یا ایجاد مدل جدید سرایت کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ابتدا تأیید کنید که در مرحله قبل (تحلیل مرحله 1) مشخص شده است که User و OAuthUser یکی هستند. اگر چنین است، مراحل زیر را انجام دهید. 2. فایل app/dependencies/auth.py را باز کنید. 3. خط import مربوط به User را پیدا کنید (مثلاً 'from app.models.user import User'). 4. آن را به 'from app.models.user_oauth import OAuthUser' تغییر دهید. 5. تمام ارجاعات به 'User' در بدنه فایل (type hints، return types، variable annotations) را به 'OAuthUser' تغییر دهید. 6. اگر فایل از 'User' در توابعی مانند get_current_user استفاده می‌کند، type hint خروجی را به OAuthUser تغییر دهید. 7. اطمینان حاصل کنید که هیچ import اضافی از app.models.user باقی نمانده است. 8. تغییرات را commit کنید. توجه: این مرحله فقط در صورتی اجرا می‌شود که تحلیل مرحله 1 نشان دهد User و OAuthUser یکی هستند. خارج از این مرحله است: تغییر در مدل‌های دیگر، تغییر در logic احراز هویت، یا ایجاد مدل جدید.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: small

---

# 🔹 مرحله 3: تعریف و مستندسازی مدل مجزای User در app/models/user.py در صورت وجود دو مدل مجزا

**Scope:** این مرحله شامل ایجاد فایل جدید app/models/user.py (در صورت عدم وجود) یا به‌روزرسانی آن است. اگر تحلیل مرحله 1 نشان دهد که User و OAuthUser دو مدل مجزا هستند، باید مدل User به‌طور کامل تعریف شود (شامل فیلدها، روابط، و نقش آن در pipeline). همچنین مستندات نقش هر مدل و نحوه تعامل آنها در احراز هویت اضافه می‌شود. خارج از این مرحله است: تغییر در OAuthUser، تغییر در auth.py، یا تغییر در logic موجود. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که دو مدل مجزا باشند.
**Key terms:** app/models/user.py, app.models.user.User, app.models.user_oauth.OAuthUser, User, OAuthUser

**بخش مربوط از متن کاربر:**
```
If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.
```

## 🎯 هدف (خلاصه ساختاریافته)
تعریف و مستندسازی مدل مجزای User در app/models/user.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/user_oauth.py:1-26` — `OAuthUser`
  ```python
  from sqlalchemy import Column, Integer, String, DateTime, Enum as SAEnum
  from sqlalchemy.sql import func
  from app.database import Base
  import enum
  
  class UserRole(str, enum.Enum):
      ADMIN = "admin"
      PENDING = "pending"
      APPROVED = "approved"
  
  class UserPermission(str, enum.Enum):
      READ_ONLY = "read-only"
      EDITOR = "editor"
      ADMIN = "admin
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
تحلیل درخواست کاربر: کاربر درخواست 'تعریف و مستندسازی مدل مجزای User در app/models/user.py در صورت وجود دو مدل مجزا' را داده است. کلیدواژه‌های اصلی: app/models/user.py, app.models.user.User, app.models.user_oauth.OAuthUser, User, OAuthUser. متن اصلی کاربر: 'این مرحله شامل ایجاد فایل جدید app/models/user.py (در صورت عدم وجود) یا به‌روزرسانی آن است. اگر تحلیل مرحله 1 نشان دهد که User و OAuthUser دو مدل مجزا هستند، باید مدل User به‌طور کامل تعریف شود (شامل فیلدها، روابط، و نقش آن در pipeline). همچنین مستندات نقش هر مدل و نحوه تعامل آنها در احراز هویت اضافه می‌شود. خارج از این مرحله است: تغییر در OAuthUser، تغییر در auth.py، یا تغییر در logic موجود. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که دو مدل مجزا باشند.' و 'If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.' شواهد در کد واقعی پروژه: فایل app/models/user_oauth.py (خطوط 1-26) مدل OAuthUser را با فیلدهای id, email, name, role, permissions, status, created_at, updated_at تعریف کرده است. فایل app/models/user.py در ساختار پروژه موجود نیست (بر اساس 'ساختار کامل پروژه'، فایل app/models/user.py در لیست فایل‌ها دیده نمی‌شود). فایل app/routes/auth.py (deep-read شده) احتمالاً از مدل User استفاده می‌کند. فایل app/dependencies/auth.py (deep-read شده) احتمالاً وابستگی‌های احراز هویت را مدیریت می‌کند. فایل app/config.py (deep-read شده) تنظیمات JWT را دارد. نتیجه تحلیل: دو مدل مجزا (User و OAuthUser) در پروژه وجود دارند. مدل User در app/models/user.py تعریف نشده است و باید ایجاد شود. مدل OAuthUser در app/models/user_oauth.py تعریف شده است. این تسک فقط بر تعریف و مستندسازی مدل User متمرکز است و تغییری در OAuthUser، auth.py، یا logic موجود ایجاد نمی‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید app/models/user.py با تعریف کامل مدل User شامل فیلدهای: id (Integer, primary key), email (String, unique, index, not null), hashed_password (String, not null), name (String, nullable), role (String, default 'user'), is_active (Boolean, default True), created_at (DateTime, server_default=func.now()), updated_at (DateTime, onupdate=func.now()). 2. اضافه کردن روابط (relationships) در صورت نیاز (مثلاً به tasks, projects). 3. مستندسازی نقش مدل User به عنوان مدل اصلی احراز هویت (password-based) و مدل OAuthUser به عنوان مدل احراز هویت OAuth (Google). 4. توضیح نحوه تعامل: User برای login/register استفاده می‌شود، OAuthUser برای Google OAuth flow. 5. اطمینان از اینکه هیچ تغییری در OAuthUser، auth.py، یا logic موجود ایجاد نمی‌شود. 6. به‌روزرسانی مستندات (مثلاً docs/API.md) برای توضیح نقش هر مدل.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: به‌روزرسانی ارجاعات در auth.py برای استفاده از مدل مناسب در صورت وجود دو مدل مجزا

**Scope:** این مرحله شامل تغییر در فایل app/dependencies/auth.py است. اگر دو مدل مجزا (User و OAuthUser) وجود داشته باشند، باید مشخص شود که auth.py باید به کدام مدل ارجاع دهد (بر اساس نقش آنها). سپس ارجاعات به‌روزرسانی می‌شوند تا از مدل صحیح استفاده کنند. خارج از این مرحله است: تغییر در logic احراز هویت، تغییر در مدل‌ها، یا ایجاد فایل جدید. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که دو مدل مجزا باشند و مرحله 3 انجام شده باشد.
**Key terms:** app/dependencies/auth.py, app.models.user.User, app.models.user_oauth.OAuthUser, User, OAuthUser

**بخش مربوط از متن کاربر:**
```
If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.
```

## 🎯 هدف (خلاصه ساختاریافته)
به‌روزرسانی ارجاعات auth.py برای مدل OAuthUser

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/dependencies/auth.py:1-50 (تخمینی — فایل deep-read نشده)` — `get_current_user` — فایل اصلی که باید تغییر کند. بر اساس نقشه Importها، 20 فایل آن را import می‌کنند. تابع get_current_user باید به‌روزرسانی شود تا بین User و OAuthUser تمایز قائل شود.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `app/models/user_oauth.py:16-26` — `OAuthUser`
  ```python
  class OAuthUser(Base):
      __tablename__ = "oauth_users"
  
      id = Column(Integer, primary_key
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
بر اساس درخواست کاربر: «به‌روزرسانی ارجاعات در auth.py برای استفاده از مدل مناسب در صورت وجود دو مدل مجزا». این مرحله شامل تغییر در فایل app/dependencies/auth.py است. اگر دو مدل مجزا (User و OAuthUser) وجود داشته باشند، باید مشخص شود که auth.py باید به کدام مدل ارجاع دهد (بر اساس نقش آنها). سپس ارجاعات به‌روزرسانی می‌شوند تا از مدل صحیح استفاده کنند. خارج از این مرحله است: تغییر در logic احراز هویت، تغییر در مدل‌ها، یا ایجاد فایل جدید. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که دو مدل مجزا باشند و مرحله 3 انجام شده باشد.

کلیدواژه‌های کاربر: app/dependencies/auth.py, app.models.user.User, app.models.user_oauth.OAuthUser, User, OAuthUser

شواهد در کد واقعی:
- فایل app/models/user_oauth.py (خطوط 1-26) مدل OAuthUser را با فیلدهای id, email, name, role, permissions, status, created_at, updated_at تعریف کرده است. این مدل از UserRole (ADMIN, PENDING, APPROVED) و UserPermission (READ_ONLY, EDITOR, ADMIN) استفاده می‌کند.
- فایل app/dependencies/auth.py در deep context موجود نیست، اما در نقشه Importهای داخلی مشخص شده که 20 فایل آن را import می‌کنند.
- فایل app/routes/auth_google.py (که در deep context موجود است) از OAuthUser استفاده می‌کند.
- فایل app/main.py (خطوط 656-659) نشان می‌دهد که auth_google.router به صورت شرطی mount می‌شود: if settings.GOOGLE_CLIENT_ID.

ارجاع به بخش اصلی درخواست کاربر: «If there are two distinct user models, define app.models.user.User and clearly document their respective roles and how they interact within the authentication pipeline.»

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی فایل app/dependencies/auth.py برای شناسایی تمام ارجاعات به مدل User.
2. تعیین منطق انتخاب مدل: اگر کاربر از طریق OAuth Google احراز هویت شده، از OAuthUser استفاده شود؛ در غیر این صورت از User استفاده شود.
3. به‌روزرسانی تابع get_current_user در app/dependencies/auth.py به گونه‌ای که:
   - ابتدا توکن JWT را decode کند.
   - بر اساس فیلد provider یا source در payload توکن، مشخص کند که کاربر از کدام مسیر احراز هویت شده.
   - اگر provider == 'google' باشد، از OAuthUser استفاده کند.
   - در غیر این صورت از User استفاده کند.
4. اطمینان از اینکه تابع get_current_user همچنان یک شیء با فیلدهای id, email, role, permissions بازمی‌گرداند (صرف‌نظر از مدل).
5. به‌روزرسانی type hints در auth.py برای پشتیبانی از Union[User, OAuthUser].
6. خارج از این مرحله: تغییر در logic احراز هویت، تغییر در مدل‌ها، یا ایجاد فایل جدید.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: نوشتن تست‌های unit برای بررسی صحت ارجاعات مدل در auth.py

**Scope:** این مرحله شامل نوشتن تست‌های unit برای فایل app/dependencies/auth.py است تا اطمینان حاصل شود که ارجاعات به مدل‌های User یا OAuthUser صحیح و سازگار هستند. تست‌ها باید بررسی کنند که importها درست هستند و type hints با مدل‌های واقعی مطابقت دارند. خارج از این مرحله است: تست‌های integration، تست‌های end-to-end، یا تست‌های مربوط به logic احراز هویت. نکته حیاتی: این تست‌ها باید در فایل tests/test_auth_dependencies.py یا مشابه قرار گیرند.
**Key terms:** app/dependencies/auth.py, app.models.user_oauth.OAuthUser, app.models.user.User, tests/test_auth_dependencies.py, User, OAuthUser

**بخش مربوط از متن کاربر:**
```
Clarify the relationship between `User` and `OAuthUser`. If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`.
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های unit برای بررسی صحت ارجاعات مدل در auth.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `tests/test_auth_dependencies.py:1-91` — `test_type_mismatch_edge_case` — فایل تست موجود — تست‌های جدید برای بررسی importها و type hints باید به این فایل اضافه شوند.
  ```python
  class _StubUser:
      def __init__(self, email: str):
          self.email = email
  
  class _StubOAuthUser:
      def __init__(self, email: str, status_value: str):
          self.email = email
          self.status = status_value
  ```
- `app/models/user_oauth.py:16-24`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های unit برای فایل `app/dependencies/auth.py` را دارد تا اطمینان حاصل شود که ارجاعات به مدل‌های `User` یا `OAuthUser` صحیح و سازگار هستند. متن خام کاربر: «نوشتن تست‌های unit برای بررسی صحت ارجاعات مدل در auth.py. این مرحله شامل نوشتن تست‌های unit برای فایل app/dependencies/auth.py است تا اطمینان حاصل شود که ارجاعات به مدل‌های User یا OAuthUser صحیح و سازگار هستند. تست‌ها باید بررسی کنند که importها درست هستند و type hints با مدل‌های واقعی مطابقت دارند. خارج از این مرحله است: تست‌های integration، تست‌های end-to-end، یا تست‌های مربوط به logic احراز هویت. نکته حیاتی: این تست‌ها باید در فایل tests/test_auth_dependencies.py یا مشابه قرار گیرند. --- بخش مربوط از درخواست اصلی کاربر --- Clarify the relationship between `User` and `OAuthUser`. If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`. --- کلیدواژه‌ها --- app/dependencies/auth.py, app.models.user_oauth.OAuthUser, app.models.user.User, tests/test_auth_dependencies.py, User, OAuthUser». شواهد در کد واقعی: فایل `tests/test_auth_dependencies.py` از deep context شامل تست‌های موجود برای `get_current_active_user` و `get_current_admin_user` است که از `_StubUser` و `_StubOAuthUser` استفاده می‌کنند. فایل `app/dependencies/auth.py` (که deep-read نشده) باید شامل importهای `User` از `app.models.user` و `OAuthUser` از `app.models.user_oauth` باشد. فایل `app/models/user_oauth.py` (deep-read شده) کلاس `OAuthUser` را با فیلدهای `status` و `email` تعریف می‌کند. فایل `app/models/user.py` (deep-read نشده) کلاس `User` را تعریف می‌کند. تست‌های جدید باید بررسی کنند که type hints در `get_current_active_user` و `get_current_admin_user` با مدل‌های واقعی مطابقت دارند و importها درست هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. فایل `tests/test_auth_dependencies.py` را باز کن و تست‌های موجود را بررسی کن (خطوط 1-91 از deep context). 2. یک تست جدید به نام `test_imports_are_correct` اضافه کن که importهای `User` و `OAuthUser` را از مسیرهای `app.models.user` و `app.models.user_oauth` بررسی کند. 3. یک تست جدید به نام `test_type_hints_match_models` اضافه کن که type hints توابع `get_current_active_user` و `get_current_admin_user` را با مدل‌های واقعی مقایسه کند. 4. از `inspect.signature` برای استخراج type hints استفاده کن. 5. اطمینان حاصل کن که تست‌ها در فایل `tests/test_auth_dependencies.py` قرار می‌گیرند. 6. تست‌ها را با `pytest tests/test_auth_dependencies.py -v` اجرا کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: small

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: manual_only)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: manual_only)_
3. integration test برای pipeline `auth` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: manual_only)_

## Task Steps

### Step 1: تحلیل و مستندسازی رابطه بین User و OAuthUser در pipeline احراز هویت
**Status:** `done` (100%)
**Scope:** این مرحله شامل تحلیل کامل کدهای موجود در فایل‌های app/dependencies/auth.py و app/models/user_oauth.py برای شناسایی دقیق نحوه ارجاع به مدل‌های User و OAuthUser است. باید مشخص شود که آیا User در auth.py به OAuthUser اشاره دارد یا یک مدل مجزاست. خروجی این مرحله یک مستندات داخلی (مثلاً در قالب کامنت یا فایل README) است که رابطه این دو مدل را توضیح می‌دهد. خارج از این مرحله است: تغییر مستقیم کد، رفع باگ، یا ایجاد مدل جدید. نکته حیاتی: این مرحله صرفاً تحلیلی است و هیچ تغییری در کد ایجاد نمی‌کند.
**Excerpt:**
```
Clarify the relationship between `User` and `OAuthUser`. If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`. If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.
```

### Step 2: به‌روزرسانی ارجاعات در app/dependencies/auth.py برای استفاده از OAuthUser در صورت یکسان بودن
**Status:** `done` (100%)
**Scope:** این مرحله شامل تغییر مستقیم کد در فایل app/dependencies/auth.py است. اگر در مرحله قبل مشخص شد که User همان OAuthUser است، تمام ارجاعات به User (مانند app.models.user.User) باید به app.models.user_oauth.OAuthUser تغییر یابند. همچنین importهای مربوطه به‌روزرسانی می‌شوند. خارج از این مرحله است: تغییر در مدل‌های دیگر، تغییر در logic احراز هویت، یا ایجاد مدل جدید. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که تحلیل مرحله 1 نشان دهد User و OAuthUser یکی هستند.
**Excerpt:**
```
If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`.
```

### Step 3: تعریف و مستندسازی مدل مجزای User در app/models/user.py در صورت وجود دو مدل مجزا
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد فایل جدید app/models/user.py (در صورت عدم وجود) یا به‌روزرسانی آن است. اگر تحلیل مرحله 1 نشان دهد که User و OAuthUser دو مدل مجزا هستند، باید مدل User به‌طور کامل تعریف شود (شامل فیلدها، روابط، و نقش آن در pipeline). همچنین مستندات نقش هر مدل و نحوه تعامل آنها در احراز هویت اضافه می‌شود. خارج از این مرحله است: تغییر در OAuthUser، تغییر در auth.py، یا تغییر در logic موجود. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که دو مدل مجزا باشند.
**Excerpt:**
```
If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.
```

### Step 4: به‌روزرسانی ارجاعات در auth.py برای استفاده از مدل مناسب در صورت وجود دو مدل مجزا
**Status:** `done` (100%)
**Scope:** این مرحله شامل تغییر در فایل app/dependencies/auth.py است. اگر دو مدل مجزا (User و OAuthUser) وجود داشته باشند، باید مشخص شود که auth.py باید به کدام مدل ارجاع دهد (بر اساس نقش آنها). سپس ارجاعات به‌روزرسانی می‌شوند تا از مدل صحیح استفاده کنند. خارج از این مرحله است: تغییر در logic احراز هویت، تغییر در مدل‌ها، یا ایجاد فایل جدید. نکته حیاتی: این مرحله فقط در صورتی اجرا می‌شود که دو مدل مجزا باشند و مرحله 3 انجام شده باشد.
**Excerpt:**
```
If there are two distinct user models, define `app.models.user.User` and clearly document their respective roles and how they interact within the authentication pipeline.
```

### Step 5: نوشتن تست‌های unit برای بررسی صحت ارجاعات مدل در auth.py
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های unit برای فایل app/dependencies/auth.py است تا اطمینان حاصل شود که ارجاعات به مدل‌های User یا OAuthUser صحیح و سازگار هستند. تست‌ها باید بررسی کنند که importها درست هستند و type hints با مدل‌های واقعی مطابقت دارند. خارج از این مرحله است: تست‌های integration، تست‌های end-to-end، یا تست‌های مربوط به logic احراز هویت. نکته حیاتی: این تست‌ها باید در فایل tests/test_auth_dependencies.py یا مشابه قرار گیرند.
**Excerpt:**
```
Clarify the relationship between `User` and `OAuthUser`. If `OAuthUser` is the primary user model, update all references (e.g., in `app/dependencies/auth.py`) to explicitly use `app.models.user_oauth.OAuthUser`.
```
