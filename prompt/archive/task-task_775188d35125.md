---
task_id: task_775188d35125
title: توسعه و ایمن‌سازی قابلیت‌های AI، برنامه‌ریزی و Webhook
type: other
priority: critical
execution_priority: 100
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-25T06:41:55.075484+00:00'
updated_at: '2026-05-25T20:07:31.119289+00:00'
archived: true
archived_at: '2026-05-25T20:07:31.119276+00:00'
tags:
- consolidated
- post_verify_merge
---

# توسعه و ایمن‌سازی قابلیت‌های AI، برنامه‌ریزی و Webhook

## Raw Idea

🧬 این یک تسک تلفیقی است — از 6 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): این خوشه شامل تسک‌های مرتبط با قابلیت‌های هوش مصنوعی، سرویس برنامه‌ریز و یکپارچه‌سازی Webhook است. این تسک‌ها شامل رفع باگ‌های ایمپورت، رفع آسیب‌پذیری‌های SQL Injection، ایمن‌سازی و مکانیزم‌های تلاش مجدد برای Webhookها، اعتبارسنجی ورودی/خروجی AI و پیاده‌سازی سرویس برنامه‌ریز می‌شوند.
🎯 theme: پیاده‌سازی و بهبود سرویس‌های AI، برنامه‌ریز و Webhook
💎 estimated_difficulty: large

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 6
  id: 0aeb1ddd-8415-44de-845f-90b31ac36f12
  عنوان اصلی: رفع ایمپورت شکسته در app/services/ai_service.py
  اولویت اصلی: critical
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/models/__init__.py, app/services/ai_service.py

📋 acceptance_criteria کامل:
  - ایمپورت from app.models.ai_model_config import AIModelConfig بدون خطا کار می‌کند [verify_method=static] [verify_plan={"grep_patterns": ["from app.models.ai_model_config import AIModelConfig", "import app.models.ai_model_config"], "files_hint": ["app/services/ai_service.py", "app/models/__init__.py"]}]
  - سرویس AI بدون خطا اجرا می‌شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai_service.py::test_ai_service_runs", "timeout_seconds": 30}]
  - تست‌های مربوط به AI پاس می‌شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai_service.py", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
Import شکسته در app/services/ai_service.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/ai_service.py:1-5` — `import` — خط ایمپورت که باعث خطا می‌شود
  ```python
  from app.models.ai_model_config import AIModelConfig
  from app.models.user import User
  ...
  ```
- `app/models/__init__.py:1-10` — `__init__.py` — فایل __init__.py که باید به‌روز شود
  ```python
  from .user import User
  from .task import Task
  from .project import Project
  from .notification import Notification
  # AIModelConfig اینجا نیست!
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
Python، SQLAlchemy، FastAPI

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/ai_model_config.py` (سطر 1) — مدل مورد نظر
- `app/routes/ai.py` (سطر 10) — از ai_service استفاده می‌کند

## 🌐 نقشهٔ وابستگی‌ها
این باگ باعث می‌شود تمام endpointهای مربوط به AI از کار بیفتند.

## 🔍 Context و وضعیت فعلی
در فایل app/services/ai_service.py، ماژول app.models.ai_model_config ایمپورت شده است، اما این ماژول در فایل app/models/__init__.py اکسپورت نشده است. این موضوع باعث خطای ImportError در زمان اجرا می‌شود. شواهد: در app/models/__init__.py، فقط مدل‌های User، Task، Project و Notification ایمپورت شده‌اند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] ایمپورت from app.models.ai_model_config import AIModelConfig بدون خطا کار می‌کند
- [ ] سرویس AI بدون خطا اجرا می‌شود
- [ ] تست‌های مربوط به AI پاس می‌شوند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ماژول ai_model_config را به فایل app/models/__init__.py اضافه کنید تا به درستی اکسپورت شود. همچنین، مطمئن شوید که مسیر ایمپورت در ai_service.py صحیح است.

## 💡 نمونه‌های قبل/بعد
**رفع ایمپورت**

_قبل:_
```
# app/models/__init__.py
from .user import User
from .task import Task
from .project import Project
from .notification import Notification
```

_بعد:_
```
from .user import User
from .task import Task
from .project import Project
from .notification import Notification
from .ai_model_config import AIModelConfig
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `python -c "from app.models.ai_model_config import AIModelConfig"`
- `pytest tests/test_ai.py`

## ⚠️ ریسک‌ها و موارد احتیاط
کم. فقط یک خط به فایل __init__.py اضافه می‌شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: small

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 2 از 6
  id: ffb6235f-638c-46d8-92a7-470b6f9ce8cd
  عنوان اصلی: Fix SQL injection in dynamic query building
  اولویت اصلی: critical
  وضعیت verify قبلی: pending
  فایل‌های دخیل: app/services/planner_service.py

📋 acceptance_criteria کامل:
  - Search with SQL injection payload (' OR 1=1--) returns no unexpected data [verify_method=api_response] [verify_plan={"method": "GET", "path": "/api/search?q=' OR 1=1--", "headers": null, "json_body": null, "expected_status": 200, "required_fields": ["results"], "json_contains": null}]
  - All search queries use parameterized statements [verify_method=static] [verify_plan={"grep_patterns": ["cursor\\.execute\\(.*%", "cursor\\.execute\\(.*format", "cursor\\.execute\\(.*f\"", "cursor\\.execute\\(.*\\+"], "files_hint": ["app/services/planner_service.py"]}]
  - Security scan passes for SQL injection vectors [verify_method=backend_test] [verify_plan={"test_node": "tests/test_security.py::test_sql_injection_scan", "timeout_seconds": 120}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
SQL injection vector in dynamic query building

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/planner_service.py:40-60` — `search_tasks` — Vulnerable dynamic query
  ```python
  query = f"SELECT * FROM tasks WHERE title LIKE '%{user_input}%'"  # ⚠️ SQL injection
  result = db.execute(query)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
SQLAlchemy + PostgreSQL

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/task.py` (سطر 10) — Task model used in queries
- `app/routes/tasks.py` (سطر 50) — Route that exposes search endpoint

## 🌐 نقشهٔ وابستگی‌ها
Affects all search functionality across tasks, projects, and users.

## 🔍 Context و وضعیت فعلی
The project search functionality builds SQL queries using string formatting with user input. This creates a SQL injection vulnerability that can be exploited to read, modify, or delete database records.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Search with SQL injection payload (' OR 1=1--) returns no unexpected data
- [ ] All search queries use parameterized statements
- [ ] Security scan passes for SQL injection vectors
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. Replace string formatting with parameterized queries using SQLAlchemy's ORM methods or raw SQL with bind parameters.

## 💡 نمونه‌های قبل/بعد
**Use parameterized query**

_قبل:_
```
query = f"SELECT * FROM tasks WHERE title LIKE '%{user_input}%'"
```

_بعد:_
```
query = text("SELECT * FROM tasks WHERE title LIKE :pattern")
result = db.execute(query, {"pattern": f"%{user_input}%"})
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl 'http://localhost:8000/api/tasks/search?q=%27%20OR%201%3D1--'`
- `bandit -r app/services/planner_service.py`

## ⚠️ ریسک‌ها و موارد احتیاط
Must ensure all existing search functionality continues to work after migration

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: critical
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 3 از 6
  id: e2fc0eb3-408a-47cd-84fb-4c83d836a06b
  عنوان اصلی: Secure and retry outgoing webhooks
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/routes/webhook.py

📋 acceptance_criteria کامل:
  - Webhooks without valid signature return 401 [verify_method=api_response] [verify_plan={"method": "POST", "path": "/webhook", "headers": {"X-Webhook-Signature": "invalid_signature"}, "json_body": {"event": "test", "data": {}}, "expected_status": 401, "required_fields": [], "json_contain]
  - Webhook signature verification uses SHA256 HMAC [verify_method=static] [verify_plan={"grep_patterns": ["hmac.new", "SHA256", "sha256", "hashlib.sha256", "hmac.compare_digest"], "files_hint": ["app/routes/webhook.py", "app/services/integration_service.py"]}]
  - Failed outgoing webhooks are retried 3 times with backoff [verify_method=static] [verify_plan={"grep_patterns": ["retry", "backoff", "tenacity", "retrying", "for.*attempt", "while.*retry"], "files_hint": ["app/services/integration_service.py"]}]
  - Webhook delivery attempts are logged with timestamps [verify_method=static] [verify_plan={"grep_patterns": ["logging", "logger", "print.*webhook", "timestamp", "datetime"], "files_hint": ["app/services/integration_service.py"]}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
Integration service lacks webhook signature verification and retry logic

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/webhook.py:1-40` — `handle_webhook` — Missing webhook signature verification
  ```python
  @router.post('/webhook/{provider}')
  async def handle_webhook(provider: str, payload: dict):
      # No signature verification
      await integration_service.process_webhook(provider, payload)
      return {'status': 'ok'}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
FastAPI + HMAC + Third-party APIs

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/integration_service.py` (سطر 1) — Processes webhook data
- `app/models/notification.py` — May need webhook delivery tracking fields

## 🌐 نقشهٔ وابستگی‌ها
Affects all third-party integrations. Requires storing provider secrets securely.

## 🔍 Context و وضعیت فعلی
The integration service at app/services/integration_service.py and webhook route at app/routes/webhook.py show no evidence of webhook signature verification. Without verifying that incoming webhooks are from trusted sources, the system is vulnerable to spoofed webhook attacks. Additionally, there's no retry mechanism for failed outgoing webhook deliveries.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Webhooks without valid signature return 401
- [ ] Webhook signature verification uses SHA256 HMAC
- [ ] Failed outgoing webhooks are retried 3 times with backoff
- [ ] Webhook delivery attempts are logged with timestamps
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. Implement HMAC signature verification for incoming webhooks using shared secrets. Add webhook delivery retry logic with exponential backoff for outgoing webhooks. Store webhook delivery attempts and status in the database.

## 💡 نمونه‌های قبل/بعد
**Add webhook signature verification**

_قبل:_
```
async def handle_webhook(provider, payload):
    await integration_service.process_webhook(provider, payload)
```

_بعد:_
```
async def handle_webhook(provider, payload, signature: str = Header(None)):
    secret = await get_provider_secret(provider)
    if not verify_signature(payload, signature, secret):
        raise HTTPException(401, 'Invalid signature')
    await integration_service.process_webhook(provider, payload)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl -X POST http://localhost:8000/api/webhook/github -H 'X-Hub-Signature-256: invalid' -d '{}' -w '%{http_code}'`
- `pytest tests/test_integrations.py -k webhook`

## ⚠️ ریسک‌ها و موارد احتیاط
Requires secure secret storage; may break existing integrations if signatures are enforced immediately

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 4 از 6
  id: 08fed8b6-7762-4441-aa2b-7c5fe5093248
  عنوان اصلی: Validate AI endpoint inputs and responses
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/services/ai_service.py

📋 acceptance_criteria کامل:
  - Prompts with SQL injection patterns are rejected with 400 [verify_method=static] [verify_plan={"grep_patterns": ["Prompts", "injection", "patterns", "rejected"], "files_hint": []}]
  - Prompts exceeding 1000 characters are truncated or rejected [verify_method=static] [verify_plan={"grep_patterns": ["Prompts", "exceeding", "characters", "truncated", "rejected"], "files_hint": []}]
  - AI responses are validated against expected schema before returning [verify_method=static] [verify_plan={"grep_patterns": ["responses", "validated", "against", "expected", "schema", "before", "returning"], "files_hint": []}]
  - Sanitization doesn't break legitimate multi-language prompts [verify_method=static] [verify_plan={"grep_patterns": ["Sanitization", "doesn", "break", "legitimate", "multi", "language", "prompts"], "files_hint": []}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
AI service endpoint lacks input sanitization and response validation

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/ai_service.py:1-50` — `process_prompt` — Missing input sanitization and response validation
  ```python
  async def process_prompt(prompt: str):
      # No sanitization
      response = await ai_model.generate(prompt)
      return response
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
FastAPI + OpenAI/LLM API

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/ai.py` (سطر 1) — Endpoint that calls ai_service
- `app/schemas/ai_schema.py` — Should define validated response schema

## 🌐 نقشهٔ وابستگی‌ها
Affects all AI-related endpoints. Changes to response format may impact frontend AI features.

## 🔍 Context و وضعیت فعلی
The AI service at app/services/ai_service.py and its route at app/routes/ai.py likely accept user prompts without proper sanitization. This could lead to prompt injection attacks, where malicious users manipulate the AI model's behavior. Additionally, there's no validation on the AI response format, which could cause parsing errors in downstream services.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Prompts with SQL injection patterns are rejected with 400
- [ ] Prompts exceeding 1000 characters are truncated or rejected
- [ ] AI responses are validated against expected schema before returning
- [ ] Sanitization doesn't break legitimate multi-language prompts
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. Add input sanitization to strip or escape potentially dangerous characters and patterns. Implement a whitelist of allowed prompt patterns. Add response validation to ensure the AI output matches expected schema before returning to client.

## 💡 نمونه‌های قبل/بعد
**Add sanitization and validation**

_قبل:_
```
response = await ai_model.generate(prompt)
return response
```

_بعد:_
```
sanitized_prompt = sanitize_input(prompt, max_length=1000)
if not is_valid_prompt(sanitized_prompt):
    raise HTTPException(400, 'Invalid prompt')
response = await ai_model.generate(sanitized_prompt)
validated_response = validate_ai_response(response)
return validated_response
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl -X POST http://localhost:8000/api/ai/generate -d '{"prompt": "DROP TABLE users;"}' -w '%{http_code}'`
- `pytest tests/test_ai.py -k sanitization`

## ⚠️ ریسک‌ها و موارد احتیاط
Overly aggressive sanitization may block legitimate use cases; needs careful tuning

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 5 از 6
  id: 67ef57ac-be8c-49fc-b698-96587206ef5e
  عنوان اصلی: پیاده‌سازی پاسخ‌های واقعی AI و ذخیره‌سازی Webhook
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/routes/ai.py, app/routes/webhook.py

📋 acceptance_criteria کامل:
  - endpoint /ai/generate با ورودی معتبر، پاسخ واقعی از AI برمی‌گرداند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/ai/generate", "headers": {"Content-Type": "application/json"}, "json_body": {"prompt": "test prompt"}, "expected_status": 200, "required_fields": ["generated_text"], "json]
  - endpoint /webhook رویدادها را در دیتابیس ذخیره می‌کند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/webhook", "headers": {"Content-Type": "application/json"}, "json_body": {"event": "task_created", "data": {"task_id": 1}}, "expected_status": 200, "required_fields": ["sta]
  - تست‌های واحد برای هر دو endpoint اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai.py::test_generate or tests/test_webhook.py::test_webhook_store", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
پایان‌نامه‌های AI و Webhook فقط پاسخ‌های ساختگی برمی‌گردانند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:20-30` — `generate_ai_content` — این endpoint باید به ai_service متصل شود
  ```python
  @router.post("/generate")
  async def generate_ai_content(request: Request):
      return {"status": "ok", "data": {"content": "This is a dummy AI response"}}
  ```
- `app/routes/webhook.py:15-25` — `handle_webhook` — این endpoint باید رویدادها را پردازش کند
  ```python
  @router.post("/webhook")
  async def handle_webhook(request: Request):
      return {"status": "received"}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
FastAPI + Python 3.10+

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/ai_service.py` (سطر 1) — سرویس AI که باید منطق واقعی را پیاده‌سازی کند
- `app/schemas/ai_schema.py` (سطر 1) — طرح‌های داده‌ای برای ورودی/خروجی AI

## 🌐 نقشهٔ وابستگی‌ها
این endpointها توسط فرانت‌اند (در صورت وجود) و تست‌ها استفاده می‌شوند. تغییر آنها نیازمند هماهنگی با تیم فرانت‌اند است.

## 🔍 Context و وضعیت فعلی
در فایل app/routes/ai.py و app/routes/webhook.py، endpointها فقط پاسخ‌های ساختگی (dummy) برمی‌گردانند. برای مثال، endpoint /ai/generate در خط ۲۵ فقط یک JSON ثابت برمی‌گرداند و هیچ منطق واقعی برای تولید محتوا با AI ندارد. این باعث می‌شود قابلیت اصلی پروژه (استفاده از AI برای مدیریت وظایف) ناقص بماند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] endpoint /ai/generate با ورودی معتبر، پاسخ واقعی از AI برمی‌گرداند
- [ ] endpoint /webhook رویدادها را در دیتابیس ذخیره می‌کند
- [ ] تست‌های واحد برای هر دو endpoint اضافه شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. پیاده‌سازی منطق واقعی برای endpointهای AI با استفاده از سرویس ai_service.py و اتصال به یک مدل AI واقعی (مثلاً OpenAI API). برای webhookها، منطق پردازش رویدادهای دریافتی را پیاده‌سازی کنید.

## 💡 نمونه‌های قبل/بعد
**پیاده‌سازی واقعی AI endpoint**

_قبل:_
```
return {"status": "ok", "data": {"content": "This is a dummy AI response"}}
```

_بعد:_
```
result = await ai_service.generate_content(request_data)
return {"status": "ok", "data": result}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_ai.py -v`
- `pytest tests/test_webhook.py -v`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به API key برای سرویس AI خارجی؛ ممکن است هزینه‌بر باشد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 6 از 6
  id: 513a8b39-2bbe-402e-9c0c-b4cb8f2043df
  عنوان اصلی: پیاده‌سازی سرویس برنامه‌ریز (planner_service)
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/services/planner_service.py

📋 acceptance_criteria کامل:
  - سرویس planner_service می‌تواند برنامه روزانه تولید کند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/planner/generate", "headers": null, "json_body": {"date": "2025-03-30"}, "expected_status": 200, "required_fields": ["daily_plan", "tasks"], "json_contains": null}]
  - برنامه تولیدشده شامل وظایف اولویت‌بندی‌شده است [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/planner/generate", "headers": null, "json_body": {"date": "2025-03-30"}, "expected_status": 200, "required_fields": ["tasks"], "json_contains": null}]
  - تست‌های واحد برای هر متد سرویس اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_planner_service.py", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
سرویس برنامه‌ریز (planner_service) پیاده‌سازی نشده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/planner_service.py:1-50` — `PlannerService` — کل سرویس باید پیاده‌سازی شود
  ```python
  class PlannerService:
      def __init__(self):
          pass
      
      async def generate_daily_plan(self, user_id: int):
          # TODO: Implement
          return []
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
Python 3.10+ با asyncio

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/task.py` (سطر 1) — مدل وظایف که برای برنامه‌ریزی استفاده می‌شود
- `app/services/ai_service.py` (سطر 1) — برای پیشنهادات هوشمند برنامه‌ریزی

## 🌐 نقشهٔ وابستگی‌ها
این سرویس توسط routeهای tasks و projects استفاده خواهد شد. همچنین با notification_service برای یادآوری‌ها ارتباط دارد.

## 🔍 Context و وضعیت فعلی
فایل app/services/planner_service.py وجود دارد اما به نظر می‌رسد فقط یک placeholder است. هیچ منطق واقعی برای برنامه‌ریزی وظایف یا تولید برنامه روزانه پیاده‌سازی نشده است. این یکی از核心ترین قابلیت‌های پروژه (مدیریت زمان و برنامه‌ریزی) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] سرویس planner_service می‌تواند برنامه روزانه تولید کند
- [ ] برنامه تولیدشده شامل وظایف اولویت‌بندی‌شده است
- [ ] تست‌های واحد برای هر متد سرویس اضافه شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. پیاده‌سازی کامل سرویس planner_service با قابلیت‌های: دریافت وظایف از دیتابیس، اولویت‌بندی، زمان‌بندی، و تولید برنامه روزانه/هفتگی. از ai_service برای پیشنهادات هوشمند استفاده کنید.

## 💡 نمونه‌های قبل/بعد
**پیاده‌سازی متد generate_daily_plan**

_قبل:_
```
async def generate_daily_plan(self, user_id: int):
    # TODO: Implement
    return []
```

_بعد:_
```
async def generate_daily_plan(self, user_id: int):
    tasks = await self.get_user_tasks(user_id)
    prioritized = self.prioritize_tasks(tasks)
    schedule = self.create_schedule(prioritized)
    return schedule
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_planner.py -v`
- `python -c "from app.services.planner_service import PlannerService; print('OK')"`

## ⚠️ ریسک‌ها و موارد احتیاط
وابستگی به پیاده‌سازی ai_service؛ ممکن است نیاز به تغییر مدل دیتابیس داشته باشد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 نکات استاندارد (همان bullet هایی که در ساخت پرامپت‌های معمولی پروژه رعایت می‌شود — وراثت کامل، نه کپی):
- ساختار AC ها: acceptance_criteria با verify_method و verify_plan و evidence_locations برای هر AC
- edge cases را در نظر بگیر و در پرامپت ذکر کن
- وابستگی‌ها را اول حل کن (dependency-aware ordering)
- اگر بخشی از یکی از تسک‌ها قبلاً done است (pre_done در بالا)، تکرار نکن — فقط روی remaining_parts تمرکز کن
- در commit message: `merged-from: 0aeb1ddd-8415-44de-845f-90b31ac36f12, ffb6235f-638c-46d8-92a7-470b6f9ce8cd, e2fc0eb3-408a-47cd-84fb-4c83d836a06b, 08fed8b6-7762-4441-aa2b-7c5fe5093248, 67ef57ac-be8c-49fc-b698-96587206ef5e, 513a8b39-2bbe-402e-9c0c-b4cb8f2043df`
- task_steps را با dependency-aware ordering مرتب کن
- هیچ کار قبلاً done شده‌ای نباید دوباره انجام شود
- هیچ خلاصه‌سازی نکن — جزئیات کامل از همهٔ منابع باید حفظ شوند


## Prompt

🧬 این یک تسک تلفیقی است — از 6 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): این خوشه شامل تسک‌های مرتبط با قابلیت‌های هوش مصنوعی، سرویس برنامه‌ریز و یکپارچه‌سازی Webhook است. این تسک‌ها شامل رفع باگ‌های ایمپورت، رفع آسیب‌پذیری‌های SQL Injection، ایمن‌سازی و مکانیزم‌های تلاش مجدد برای Webhookها، اعتبارسنجی ورودی/خروجی AI و پیاده‌سازی سرویس برنامه‌ریز می‌شوند.
🎯 theme: پیاده‌سازی و بهبود سرویس‌های AI، برنامه‌ریز و Webhook
💎 estimated_difficulty: large

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 6
  id: 0aeb1ddd-8415-44de-845f-90b31ac36f12
  عنوان اصلی: رفع ایمپورت شکسته در app/services/ai_service.py
  اولویت اصلی: critical
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/models/__init__.py, app/services/ai_service.py

📋 acceptance_criteria کامل:
  - ایمپورت from app.models.ai_model_config import AIModelConfig بدون خطا کار می‌کند [verify_method=static] [verify_plan={"grep_patterns": ["from app.models.ai_model_config import AIModelConfig", "import app.models.ai_model_config"], "files_hint": ["app/services/ai_service.py", "app/models/__init__.py"]}]
  - سرویس AI بدون خطا اجرا می‌شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai_service.py::test_ai_service_runs", "timeout_seconds": 30}]
  - تست‌های مربوط به AI پاس می‌شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai_service.py", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
Import شکسته در app/services/ai_service.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/ai_service.py:1-5` — `import` — خط ایمپورت که باعث خطا می‌شود
  ```python
  from app.models.ai_model_config import AIModelConfig
  from app.models.user import User
  ...
  ```
- `app/models/__init__.py:1-10` — `__init__.py` — فایل __init__.py که باید به‌روز شود
  ```python
  from .user import User
  from .task import Task
  from .project import Project
  from .notification import Notification
  # AIModelConfig اینجا نیست!
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
Python، SQLAlchemy، FastAPI

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/ai_model_config.py` (سطر 1) — مدل مورد نظر
- `app/routes/ai.py` (سطر 10) — از ai_service استفاده می‌کند

## 🌐 نقشهٔ وابستگی‌ها
این باگ باعث می‌شود تمام endpointهای مربوط به AI از کار بیفتند.

## 🔍 Context و وضعیت فعلی
در فایل app/services/ai_service.py، ماژول app.models.ai_model_config ایمپورت شده است، اما این ماژول در فایل app/models/__init__.py اکسپورت نشده است. این موضوع باعث خطای ImportError در زمان اجرا می‌شود. شواهد: در app/models/__init__.py، فقط مدل‌های User، Task، Project و Notification ایمپورت شده‌اند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] ایمپورت from app.models.ai_model_config import AIModelConfig بدون خطا کار می‌کند
- [ ] سرویس AI بدون خطا اجرا می‌شود
- [ ] تست‌های مربوط به AI پاس می‌شوند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ماژول ai_model_config را به فایل app/models/__init__.py اضافه کنید تا به درستی اکسپورت شود. همچنین، مطمئن شوید که مسیر ایمپورت در ai_service.py صحیح است.

## 💡 نمونه‌های قبل/بعد
**رفع ایمپورت**

_قبل:_
```
# app/models/__init__.py
from .user import User
from .task import Task
from .project import Project
from .notification import Notification
```

_بعد:_
```
from .user import User
from .task import Task
from .project import Project
from .notification import Notification
from .ai_model_config import AIModelConfig
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `python -c "from app.models.ai_model_config import AIModelConfig"`
- `pytest tests/test_ai.py`

## ⚠️ ریسک‌ها و موارد احتیاط
کم. فقط یک خط به فایل __init__.py اضافه می‌شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: small

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 2 از 6
  id: ffb6235f-638c-46d8-92a7-470b6f9ce8cd
  عنوان اصلی: Fix SQL injection in dynamic query building
  اولویت اصلی: critical
  وضعیت verify قبلی: pending
  فایل‌های دخیل: app/services/planner_service.py

📋 acceptance_criteria کامل:
  - Search with SQL injection payload (' OR 1=1--) returns no unexpected data [verify_method=api_response] [verify_plan={"method": "GET", "path": "/api/search?q=' OR 1=1--", "headers": null, "json_body": null, "expected_status": 200, "required_fields": ["results"], "json_contains": null}]
  - All search queries use parameterized statements [verify_method=static] [verify_plan={"grep_patterns": ["cursor\\.execute\\(.*%", "cursor\\.execute\\(.*format", "cursor\\.execute\\(.*f\"", "cursor\\.execute\\(.*\\+"], "files_hint": ["app/services/planner_service.py"]}]
  - Security scan passes for SQL injection vectors [verify_method=backend_test] [verify_plan={"test_node": "tests/test_security.py::test_sql_injection_scan", "timeout_seconds": 120}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
SQL injection vector in dynamic query building

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/planner_service.py:40-60` — `search_tasks` — Vulnerable dynamic query
  ```python
  query = f"SELECT * FROM tasks WHERE title LIKE '%{user_input}%'"  # ⚠️ SQL injection
  result = db.execute(query)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
SQLAlchemy + PostgreSQL

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/task.py` (سطر 10) — Task model used in queries
- `app/routes/tasks.py` (سطر 50) — Route that exposes search endpoint

## 🌐 نقشهٔ وابستگی‌ها
Affects all search functionality across tasks, projects, and users.

## 🔍 Context و وضعیت فعلی
The project search functionality builds SQL queries using string formatting with user input. This creates a SQL injection vulnerability that can be exploited to read, modify, or delete database records.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Search with SQL injection payload (' OR 1=1--) returns no unexpected data
- [ ] All search queries use parameterized statements
- [ ] Security scan passes for SQL injection vectors
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. Replace string formatting with parameterized queries using SQLAlchemy's ORM methods or raw SQL with bind parameters.

## 💡 نمونه‌های قبل/بعد
**Use parameterized query**

_قبل:_
```
query = f"SELECT * FROM tasks WHERE title LIKE '%{user_input}%'"
```

_بعد:_
```
query = text("SELECT * FROM tasks WHERE title LIKE :pattern")
result = db.execute(query, {"pattern": f"%{user_input}%"})
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl 'http://localhost:8000/api/tasks/search?q=%27%20OR%201%3D1--'`
- `bandit -r app/services/planner_service.py`

## ⚠️ ریسک‌ها و موارد احتیاط
Must ensure all existing search functionality continues to work after migration

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: critical
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 3 از 6
  id: e2fc0eb3-408a-47cd-84fb-4c83d836a06b
  عنوان اصلی: Secure and retry outgoing webhooks
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/routes/webhook.py

📋 acceptance_criteria کامل:
  - Webhooks without valid signature return 401 [verify_method=api_response] [verify_plan={"method": "POST", "path": "/webhook", "headers": {"X-Webhook-Signature": "invalid_signature"}, "json_body": {"event": "test", "data": {}}, "expected_status": 401, "required_fields": [], "json_contain]
  - Webhook signature verification uses SHA256 HMAC [verify_method=static] [verify_plan={"grep_patterns": ["hmac.new", "SHA256", "sha256", "hashlib.sha256", "hmac.compare_digest"], "files_hint": ["app/routes/webhook.py", "app/services/integration_service.py"]}]
  - Failed outgoing webhooks are retried 3 times with backoff [verify_method=static] [verify_plan={"grep_patterns": ["retry", "backoff", "tenacity", "retrying", "for.*attempt", "while.*retry"], "files_hint": ["app/services/integration_service.py"]}]
  - Webhook delivery attempts are logged with timestamps [verify_method=static] [verify_plan={"grep_patterns": ["logging", "logger", "print.*webhook", "timestamp", "datetime"], "files_hint": ["app/services/integration_service.py"]}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
Integration service lacks webhook signature verification and retry logic

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/webhook.py:1-40` — `handle_webhook` — Missing webhook signature verification
  ```python
  @router.post('/webhook/{provider}')
  async def handle_webhook(provider: str, payload: dict):
      # No signature verification
      await integration_service.process_webhook(provider, payload)
      return {'status': 'ok'}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
FastAPI + HMAC + Third-party APIs

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/integration_service.py` (سطر 1) — Processes webhook data
- `app/models/notification.py` — May need webhook delivery tracking fields

## 🌐 نقشهٔ وابستگی‌ها
Affects all third-party integrations. Requires storing provider secrets securely.

## 🔍 Context و وضعیت فعلی
The integration service at app/services/integration_service.py and webhook route at app/routes/webhook.py show no evidence of webhook signature verification. Without verifying that incoming webhooks are from trusted sources, the system is vulnerable to spoofed webhook attacks. Additionally, there's no retry mechanism for failed outgoing webhook deliveries.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Webhooks without valid signature return 401
- [ ] Webhook signature verification uses SHA256 HMAC
- [ ] Failed outgoing webhooks are retried 3 times with backoff
- [ ] Webhook delivery attempts are logged with timestamps
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. Implement HMAC signature verification for incoming webhooks using shared secrets. Add webhook delivery retry logic with exponential backoff for outgoing webhooks. Store webhook delivery attempts and status in the database.

## 💡 نمونه‌های قبل/بعد
**Add webhook signature verification**

_قبل:_
```
async def handle_webhook(provider, payload):
    await integration_service.process_webhook(provider, payload)
```

_بعد:_
```
async def handle_webhook(provider, payload, signature: str = Header(None)):
    secret = await get_provider_secret(provider)
    if not verify_signature(payload, signature, secret):
        raise HTTPException(401, 'Invalid signature')
    await integration_service.process_webhook(provider, payload)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl -X POST http://localhost:8000/api/webhook/github -H 'X-Hub-Signature-256: invalid' -d '{}' -w '%{http_code}'`
- `pytest tests/test_integrations.py -k webhook`

## ⚠️ ریسک‌ها و موارد احتیاط
Requires secure secret storage; may break existing integrations if signatures are enforced immediately

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 4 از 6
  id: 08fed8b6-7762-4441-aa2b-7c5fe5093248
  عنوان اصلی: Validate AI endpoint inputs and responses
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/services/ai_service.py

📋 acceptance_criteria کامل:
  - Prompts with SQL injection patterns are rejected with 400 [verify_method=static] [verify_plan={"grep_patterns": ["Prompts", "injection", "patterns", "rejected"], "files_hint": []}]
  - Prompts exceeding 1000 characters are truncated or rejected [verify_method=static] [verify_plan={"grep_patterns": ["Prompts", "exceeding", "characters", "truncated", "rejected"], "files_hint": []}]
  - AI responses are validated against expected schema before returning [verify_method=static] [verify_plan={"grep_patterns": ["responses", "validated", "against", "expected", "schema", "before", "returning"], "files_hint": []}]
  - Sanitization doesn't break legitimate multi-language prompts [verify_method=static] [verify_plan={"grep_patterns": ["Sanitization", "doesn", "break", "legitimate", "multi", "language", "prompts"], "files_hint": []}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
AI service endpoint lacks input sanitization and response validation

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/ai_service.py:1-50` — `process_prompt` — Missing input sanitization and response validation
  ```python
  async def process_prompt(prompt: str):
      # No sanitization
      response = await ai_model.generate(prompt)
      return response
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
FastAPI + OpenAI/LLM API

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/ai.py` (سطر 1) — Endpoint that calls ai_service
- `app/schemas/ai_schema.py` — Should define validated response schema

## 🌐 نقشهٔ وابستگی‌ها
Affects all AI-related endpoints. Changes to response format may impact frontend AI features.

## 🔍 Context و وضعیت فعلی
The AI service at app/services/ai_service.py and its route at app/routes/ai.py likely accept user prompts without proper sanitization. This could lead to prompt injection attacks, where malicious users manipulate the AI model's behavior. Additionally, there's no validation on the AI response format, which could cause parsing errors in downstream services.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Prompts with SQL injection patterns are rejected with 400
- [ ] Prompts exceeding 1000 characters are truncated or rejected
- [ ] AI responses are validated against expected schema before returning
- [ ] Sanitization doesn't break legitimate multi-language prompts
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. Add input sanitization to strip or escape potentially dangerous characters and patterns. Implement a whitelist of allowed prompt patterns. Add response validation to ensure the AI output matches expected schema before returning to client.

## 💡 نمونه‌های قبل/بعد
**Add sanitization and validation**

_قبل:_
```
response = await ai_model.generate(prompt)
return response
```

_بعد:_
```
sanitized_prompt = sanitize_input(prompt, max_length=1000)
if not is_valid_prompt(sanitized_prompt):
    raise HTTPException(400, 'Invalid prompt')
response = await ai_model.generate(sanitized_prompt)
validated_response = validate_ai_response(response)
return validated_response
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl -X POST http://localhost:8000/api/ai/generate -d '{"prompt": "DROP TABLE users;"}' -w '%{http_code}'`
- `pytest tests/test_ai.py -k sanitization`

## ⚠️ ریسک‌ها و موارد احتیاط
Overly aggressive sanitization may block legitimate use cases; needs careful tuning

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 5 از 6
  id: 67ef57ac-be8c-49fc-b698-96587206ef5e
  عنوان اصلی: پیاده‌سازی پاسخ‌های واقعی AI و ذخیره‌سازی Webhook
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/routes/ai.py, app/routes/webhook.py

📋 acceptance_criteria کامل:
  - endpoint /ai/generate با ورودی معتبر، پاسخ واقعی از AI برمی‌گرداند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/ai/generate", "headers": {"Content-Type": "application/json"}, "json_body": {"prompt": "test prompt"}, "expected_status": 200, "required_fields": ["generated_text"], "json]
  - endpoint /webhook رویدادها را در دیتابیس ذخیره می‌کند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/webhook", "headers": {"Content-Type": "application/json"}, "json_body": {"event": "task_created", "data": {"task_id": 1}}, "expected_status": 200, "required_fields": ["sta]
  - تست‌های واحد برای هر دو endpoint اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai.py::test_generate or tests/test_webhook.py::test_webhook_store", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
پایان‌نامه‌های AI و Webhook فقط پاسخ‌های ساختگی برمی‌گردانند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:20-30` — `generate_ai_content` — این endpoint باید به ai_service متصل شود
  ```python
  @router.post("/generate")
  async def generate_ai_content(request: Request):
      return {"status": "ok", "data": {"content": "This is a dummy AI response"}}
  ```
- `app/routes/webhook.py:15-25` — `handle_webhook` — این endpoint باید رویدادها را پردازش کند
  ```python
  @router.post("/webhook")
  async def handle_webhook(request: Request):
      return {"status": "received"}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
FastAPI + Python 3.10+

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/ai_service.py` (سطر 1) — سرویس AI که باید منطق واقعی را پیاده‌سازی کند
- `app/schemas/ai_schema.py` (سطر 1) — طرح‌های داده‌ای برای ورودی/خروجی AI

## 🌐 نقشهٔ وابستگی‌ها
این endpointها توسط فرانت‌اند (در صورت وجود) و تست‌ها استفاده می‌شوند. تغییر آنها نیازمند هماهنگی با تیم فرانت‌اند است.

## 🔍 Context و وضعیت فعلی
در فایل app/routes/ai.py و app/routes/webhook.py، endpointها فقط پاسخ‌های ساختگی (dummy) برمی‌گردانند. برای مثال، endpoint /ai/generate در خط ۲۵ فقط یک JSON ثابت برمی‌گرداند و هیچ منطق واقعی برای تولید محتوا با AI ندارد. این باعث می‌شود قابلیت اصلی پروژه (استفاده از AI برای مدیریت وظایف) ناقص بماند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] endpoint /ai/generate با ورودی معتبر، پاسخ واقعی از AI برمی‌گرداند
- [ ] endpoint /webhook رویدادها را در دیتابیس ذخیره می‌کند
- [ ] تست‌های واحد برای هر دو endpoint اضافه شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. پیاده‌سازی منطق واقعی برای endpointهای AI با استفاده از سرویس ai_service.py و اتصال به یک مدل AI واقعی (مثلاً OpenAI API). برای webhookها، منطق پردازش رویدادهای دریافتی را پیاده‌سازی کنید.

## 💡 نمونه‌های قبل/بعد
**پیاده‌سازی واقعی AI endpoint**

_قبل:_
```
return {"status": "ok", "data": {"content": "This is a dummy AI response"}}
```

_بعد:_
```
result = await ai_service.generate_content(request_data)
return {"status": "ok", "data": result}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_ai.py -v`
- `pytest tests/test_webhook.py -v`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به API key برای سرویس AI خارجی؛ ممکن است هزینه‌بر باشد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 6 از 6
  id: 513a8b39-2bbe-402e-9c0c-b4cb8f2043df
  عنوان اصلی: پیاده‌سازی سرویس برنامه‌ریز (planner_service)
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: app/services/planner_service.py

📋 acceptance_criteria کامل:
  - سرویس planner_service می‌تواند برنامه روزانه تولید کند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/planner/generate", "headers": null, "json_body": {"date": "2025-03-30"}, "expected_status": 200, "required_fields": ["daily_plan", "tasks"], "json_contains": null}]
  - برنامه تولیدشده شامل وظایف اولویت‌بندی‌شده است [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/planner/generate", "headers": null, "json_body": {"date": "2025-03-30"}, "expected_status": 200, "required_fields": ["tasks"], "json_contains": null}]
  - تست‌های واحد برای هر متد سرویس اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_planner_service.py", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
سرویس برنامه‌ریز (planner_service) پیاده‌سازی نشده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/planner_service.py:1-50` — `PlannerService` — کل سرویس باید پیاده‌سازی شود
  ```python
  class PlannerService:
      def __init__(self):
          pass
      
      async def generate_daily_plan(self, user_id: int):
          # TODO: Implement
          return []
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
Python 3.10+ با asyncio

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/task.py` (سطر 1) — مدل وظایف که برای برنامه‌ریزی استفاده می‌شود
- `app/services/ai_service.py` (سطر 1) — برای پیشنهادات هوشمند برنامه‌ریزی

## 🌐 نقشهٔ وابستگی‌ها
این سرویس توسط routeهای tasks و projects استفاده خواهد شد. همچنین با notification_service برای یادآوری‌ها ارتباط دارد.

## 🔍 Context و وضعیت فعلی
فایل app/services/planner_service.py وجود دارد اما به نظر می‌رسد فقط یک placeholder است. هیچ منطق واقعی برای برنامه‌ریزی وظایف یا تولید برنامه روزانه پیاده‌سازی نشده است. این یکی از核心ترین قابلیت‌های پروژه (مدیریت زمان و برنامه‌ریزی) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] سرویس planner_service می‌تواند برنامه روزانه تولید کند
- [ ] برنامه تولیدشده شامل وظایف اولویت‌بندی‌شده است
- [ ] تست‌های واحد برای هر متد سرویس اضافه شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. پیاده‌سازی کامل سرویس planner_service با قابلیت‌های: دریافت وظایف از دیتابیس، اولویت‌بندی، زمان‌بندی، و تولید برنامه روزانه/هفتگی. از ai_service برای پیشنهادات هوشمند استفاده کنید.

## 💡 نمونه‌های قبل/بعد
**پیاده‌سازی متد generate_daily_plan**

_قبل:_
```
async def generate_daily_plan(self, user_id: int):
    # TODO: Implement
    return []
```

_بعد:_
```
async def generate_daily_plan(self, user_id: int):
    tasks = await self.get_user_tasks(user_id)
    prioritized = self.prioritize_tasks(tasks)
    schedule = self.create_schedule(prioritized)
    return schedule
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_planner.py -v`
- `python -c "from app.services.planner_service import PlannerService; print('OK')"`

## ⚠️ ریسک‌ها و موارد احتیاط
وابستگی به پیاده‌سازی ai_service؛ ممکن است نیاز به تغییر مدل دیتابیس داشته باشد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 نکات استاندارد (همان bullet هایی که در ساخت پرامپت‌های معمولی پروژه رعایت می‌شود — وراثت کامل، نه کپی):
- ساختار AC ها: acceptance_criteria با verify_method و verify_plan و evidence_locations برای هر AC
- edge cases را در نظر بگیر و در پرامپت ذکر کن
- وابستگی‌ها را اول حل کن (dependency-aware ordering)
- اگر بخشی از یکی از تسک‌ها قبلاً done است (pre_done در بالا)، تکرار نکن — فقط روی remaining_parts تمرکز کن
- در commit message: `merged-from: 0aeb1ddd-8415-44de-845f-90b31ac36f12, ffb6235f-638c-46d8-92a7-470b6f9ce8cd, e2fc0eb3-408a-47cd-84fb-4c83d836a06b, 08fed8b6-7762-4441-aa2b-7c5fe5093248, 67ef57ac-be8c-49fc-b698-96587206ef5e, 513a8b39-2bbe-402e-9c0c-b4cb8f2043df`
- task_steps را با dependency-aware ordering مرتب کن
- هیچ کار قبلاً done شده‌ای نباید دوباره انجام شود
- هیچ خلاصه‌سازی نکن — جزئیات کامل از همهٔ منابع باید حفظ شوند


## Acceptance Criteria

1. ایمپورت from app.models.ai_model_config import AIModelConfig بدون خطا کار می‌کند _(verify: static)_
2. سرویس AI بدون خطا اجرا می‌شود _(verify: backend_test)_
3. تست‌های مربوط به AI پاس می‌شوند _(verify: backend_test)_
4. Search with SQL injection payload (' OR 1=1--) returns no unexpected data _(verify: api_response)_
5. All search queries use parameterized statements _(verify: static)_
6. Security scan passes for SQL injection vectors _(verify: backend_test)_
7. Webhooks without valid signature return 401 _(verify: api_response)_
8. Webhook signature verification uses SHA256 HMAC _(verify: static)_
9. Failed outgoing webhooks are retried 3 times with backoff _(verify: static)_
10. Webhook delivery attempts are logged with timestamps _(verify: static)_
11. Prompts with SQL injection patterns are rejected with 400 _(verify: static)_
12. Prompts exceeding 1000 characters are truncated or rejected _(verify: static)_
13. AI responses are validated against expected schema before returning _(verify: static)_
14. Sanitization doesn't break legitimate multi-language prompts _(verify: static)_
15. endpoint /ai/generate با ورودی معتبر، پاسخ واقعی از AI برمی‌گرداند _(verify: api_response)_
16. endpoint /webhook رویدادها را در دیتابیس ذخیره می‌کند _(verify: api_response)_
17. تست‌های واحد برای هر دو endpoint اضافه شود _(verify: backend_test)_
18. سرویس planner_service می‌تواند برنامه روزانه تولید کند _(verify: api_response)_
19. برنامه تولیدشده شامل وظایف اولویت‌بندی‌شده است _(verify: api_response)_
20. تست‌های واحد برای هر متد سرویس اضافه شود _(verify: backend_test)_

## Task Steps

### Step 1: رفع ایمپورت شکسته در app/services/ai_service.py - اضافه کردن AIModelConfig به __init__.py
**Status:** `done` (100%)
**Scope:** اضافه کردن خط `from .ai_model_config import AIModelConfig` به فایل `app/models/__init__.py`. این مرحله فقط مربوط به رفع خطای ImportError است و شامل تغییر در منطق سرویس AI یا تست‌ها نمی‌شود. نکته حیاتی: باید دقیقاً همان نام کلاس `AIModelConfig` و مسیر `app.models.ai_model_config` استفاده شود.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
Import شکسته در app/services/ai_service.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/ai_service.py:1-5` — `import` — خط ایمپورت که باعث خطا می‌شود
  ```python
  from app.models.ai_model_config import AIModelConfig
  from app.models.user import User
  ...
  ```
- `app/models/__init__.py:1-10` — `__init__.py` — فایل __init__.py که باید به‌روز شود
  ```python
  from .user import User
  from .task import Task
  from .project import Project
  from .notification import Notification
  # AIModelConfig اینجا نیست!
  ```
```

### Step 2: رفع ایمپورت شکسته در app/services/ai_service.py - اجرای تست‌های AI
**Status:** `done` (100%)
**Scope:** اجرای تست‌های مربوط به AI (`tests/test_ai_service.py`) برای اطمینان از پاس شدن آن‌ها پس از رفع ایمپورت. این مرحله فقط اجرای تست‌هاست و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest` با timeout 60 ثانیه استفاده شود.
**Excerpt:**
```
- تست‌های مربوط به AI پاس می‌شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai_service.py", "timeout_seconds": 60}]
```

### Step 3: رفع SQL Injection در planner_service - جایگزینی با پارامترایز شده
**Status:** `done` (100%)
**Scope:** جایگزینی queryهای داینامیک در `app/services/planner_service.py` با statementهای پارامترایز شده با استفاده از SQLAlchemy. این مرحله شامل تغییر تابع `search_tasks` و هر query دیگری که از string formatting استفاده می‌کند. نکته حیاتی: باید از `text()` و bind parameters استفاده شود، نه از f-string یا % formatting.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
SQL injection vector in dynamic query building

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/planner_service.py:40-60` — `search_tasks` — Vulnerable dynamic query
  ```python
  query = f"SELECT * FROM tasks WHERE title LIKE '%{user_input}%'"  # ⚠️ SQL injection
  result = db.execute(query)
  ```
```

### Step 4: رفع SQL Injection در planner_service - اجرای اسکن امنیتی
**Status:** `done` (100%)
**Scope:** اجرای اسکن امنیتی با استفاده از `bandit` روی `app/services/planner_service.py` برای اطمینان از عدم وجود آسیب‌پذیری SQL Injection. این مرحله فقط اجرای ابزار static analysis است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `bandit -r app/services/planner_service.py` استفاده شود.
**Excerpt:**
```
- Security scan passes for SQL injection vectors [verify_method=backend_test] [verify_plan={"test_node": "tests/test_security.py::test_sql_injection_scan", "timeout_seconds": 120}]
```

### Step 5: رفع SQL Injection در planner_service - اجرای تست‌های امنیتی
**Status:** `done` (100%)
**Scope:** اجرای تست `tests/test_security.py::test_sql_injection_scan` برای اطمینان از پاس شدن آن. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: timeout 120 ثانیه است.
**Excerpt:**
```
- Security scan passes for SQL injection vectors [verify_method=backend_test] [verify_plan={"test_node": "tests/test_security.py::test_sql_injection_scan", "timeout_seconds": 120}]
```

### Step 6: ایمن‌سازی Webhook - پیاده‌سازی اعتبارسنجی امضا با SHA256 HMAC
**Status:** `done` (100%)
**Scope:** اضافه کردن اعتبارسنجی امضای HMAC-SHA256 برای webhookهای ورودی در `app/routes/webhook.py` و `app/services/integration_service.py`. این مرحله شامل ایجاد تابع `verify_signature` و استفاده از `hmac.compare_digest` است. نکته حیاتی: باید از SHA256 استفاده شود و کلید secret از provider دریافت شود.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
Integration service lacks webhook signature verification and retry logic

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/webhook.py:1-40` — `handle_webhook` — Missing webhook signature verification
  ```python
  @router.post('/webhook/{provider}')
  async def handle_webhook(provider: str, payload: dict):
      # No signature verification
      await integration_service.process_webhook(provider, payload)
      return {'status': 'ok'}
  ```
```

### Step 7: ایمن‌سازی Webhook - پیاده‌سازی retry logic با backoff
**Status:** `partial` (50%)
**Scope:** اضافه کردن مکانیزم retry با exponential backoff برای webhookهای خروجی در `app/services/integration_service.py`. این مرحله شامل استفاده از کتابخانه `tenacity` یا پیاده‌سازی دستی با حلقه `for` و `while` است. نکته حیاتی: باید 3 بار تلاش مجدد با backoff انجام شود.
**Excerpt:**
```
- Failed outgoing webhooks are retried 3 times with backoff [verify_method=static] [verify_plan={"grep_patterns": ["retry", "backoff", "tenacity", "retrying", "for.*attempt", "while.*retry"], "files_hint": ["app/services/integration_service.py"]}]
```

### Step 8: ایمن‌سازی Webhook - پیاده‌سازی logging با timestamp
**Status:** `partial` (50%)
**Scope:** اضافه کردن logging برای تلاش‌های ارسال webhook در `app/services/integration_service.py`. این مرحله شامل ثبت timestamp، status و خطاهای هر تلاش است. نکته حیاتی: باید از ماژول `logging` استاندارد پایتون استفاده شود.
**Excerpt:**
```
- Webhook delivery attempts are logged with timestamps [verify_method=static] [verify_plan={"grep_patterns": ["logging", "logger", "print.*webhook", "timestamp", "datetime"], "files_hint": ["app/services/integration_service.py"]}]
```

### Step 9: ایمن‌سازی Webhook - اجرای تست‌های webhook
**Status:** `not_done` (0%)
**Scope:** اجرای تست‌های مربوط به webhook در `tests/test_integrations.py` برای اطمینان از پاس شدن آن‌ها. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest tests/test_integrations.py -k webhook` استفاده شود.
**Excerpt:**
```
- `pytest tests/test_integrations.py -k webhook`
```

### Step 10: اعتبارسنجی ورودی AI - رد کردن پرامپت‌های دارای الگوی SQL Injection
**Status:** `done` (100%)
**Scope:** اضافه کردن منطق تشخیص و رد پرامپت‌های حاوی الگوهای SQL Injection در `app/services/ai_service.py`. این مرحله شامل ایجاد تابع `sanitize_input` یا مشابه آن است. نکته حیاتی: پرامپت‌های خطرناک باید با status 400 رد شوند.
**Excerpt:**
```
- Prompts with SQL injection patterns are rejected with 400 [verify_method=static] [verify_plan={"grep_patterns": ["Prompts", "injection", "patterns", "rejected"], "files_hint": []}]
```

### Step 11: اعتبارسنجی ورودی AI - محدود کردن طول پرامپت به 1000 کاراکتر
**Status:** `done` (100%)
**Scope:** اضافه کردن منطق truncate یا reject برای پرامپت‌های بیش از 1000 کاراکتر در `app/services/ai_service.py`. این مرحله شامل بررسی طول پرامپت و اعمال محدودیت است. نکته حیاتی: باید تصمیم گرفته شود که پرامپت‌های طولانی truncate شوند یا با 400 رد شوند.
**Excerpt:**
```
- Prompts exceeding 1000 characters are truncated or rejected [verify_method=static] [verify_plan={"grep_patterns": ["Prompts", "exceeding", "characters", "truncated", "rejected"], "files_hint": []}]
```

### Step 12: اعتبارسنجی ورودی AI - اعتبارسنجی پاسخ‌های AI بر اساس schema
**Status:** `done` (100%)
**Scope:** اضافه کردن اعتبارسنجی پاسخ‌های AI بر اساس schema تعریف‌شده در `app/schemas/ai_schema.py` در `app/services/ai_service.py`. این مرحله شامل ایجاد تابع `validate_ai_response` است. نکته حیاتی: پاسخ‌های نامعتبر نباید به کاربر برگردانده شوند.
**Excerpt:**
```
- AI responses are validated against expected schema before returning [verify_method=static] [verify_plan={"grep_patterns": ["responses", "validated", "against", "expected", "schema", "before", "returning"], "files_hint": []}]
```

### Step 13: اعتبارسنجی ورودی AI - اطمینان از عدم شکستن پرامپت‌های چندزبانه
**Status:** `done` (100%)
**Scope:** بررسی و اصلاح منطق sanitization در `app/services/ai_service.py` برای اطمینان از اینکه پرامپت‌های معتبر چندزبانه (مانند فارسی، عربی، چینی) شکسته نمی‌شوند. این مرحله شامل تست با پرامپت‌های چندزبانه است. نکته حیاتی: sanitization نباید کاراکترهای یونیکد معتبر را حذف کند.
**Excerpt:**
```
- Sanitization doesn't break legitimate multi-language prompts [verify_method=static] [verify_plan={"grep_patterns": ["Sanitization", "doesn", "break", "legitimate", "multi", "language", "prompts"], "files_hint": []}]
```

### Step 14: اعتبارسنجی ورودی AI - اجرای تست‌های sanitization
**Status:** `not_done` (0%)
**Scope:** اجرای تست‌های مربوط به sanitization در `tests/test_ai.py` برای اطمینان از پاس شدن آن‌ها. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest tests/test_ai.py -k sanitization` استفاده شود.
**Excerpt:**
```
- `pytest tests/test_ai.py -k sanitization`
```

### Step 15: پیاده‌سازی پاسخ‌های واقعی AI - اتصال endpoint /ai/generate به ai_service
**Status:** `done` (100%)
**Scope:** جایگزینی پاسخ ساختگی در `app/routes/ai.py` با فراخوانی واقعی `ai_service.generate_content()`. این مرحله شامل تغییر endpoint `POST /ai/generate` است. نکته حیاتی: باید از `ai_service` موجود استفاده شود و پاسخ شامل فیلد `generated_text` باشد.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
پایان‌نامه‌های AI و Webhook فقط پاسخ‌های ساختگی برمی‌گردانند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/ai.py:20-30` — `generate_ai_content` — این endpoint باید به ai_service متصل شود
  ```python
  @router.post("/generate")
  async def generate_ai_content(request: Request):
      return {"status": "ok", "data": {"content": "This is a dummy AI response"}}
  ```
```

### Step 16: پیاده‌سازی پاسخ‌های واقعی AI - ذخیره‌سازی رویدادهای webhook در دیتابیس
**Status:** `done` (100%)
**Scope:** جایگزینی پاسخ ساختگی در `app/routes/webhook.py` با منطق واقعی ذخیره‌سازی رویدادها در دیتابیس. این مرحله شامل تغییر endpoint `POST /webhook` است. نکته حیاتی: رویدادها باید در جدول `notifications` یا مشابه ذخیره شوند.
**Excerpt:**
```
- `app/routes/webhook.py:15-25` — `handle_webhook` — این endpoint باید رویدادها را پردازش کند
  ```python
  @router.post("/webhook")
  async def handle_webhook(request: Request):
      return {"status": "received"}
  ```
```

### Step 17: پیاده‌سازی پاسخ‌های واقعی AI - اضافه کردن تست‌های واحد برای endpoint AI
**Status:** `not_done` (0%)
**Scope:** اضافه کردن تست‌های واحد برای endpoint `POST /ai/generate` در `tests/test_ai.py`. این مرحله شامل نوشتن تست `test_generate` است. نکته حیاتی: تست باید ورودی معتبر و پاسخ واقعی را بررسی کند.
— [merged] اضافه کردن تست‌های واحد برای endpoint `POST /webhook` در `tests/test_webhook.py`. این مرحله شامل نوشتن تست `test_webhook_store` است. نکته حیاتی: تست باید ذخیره‌سازی رویداد در دیتابیس را بررسی کند.
**Excerpt:**
```
- تست‌های واحد برای هر دو endpoint اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai.py::test_generate or tests/test_webhook.py::test_webhook_store", "timeout_seconds": 60}]
```

### Step 18: پیاده‌سازی سرویس برنامه‌ریز - پیاده‌سازی متد generate_daily_plan
**Status:** `done` (100%)
**Scope:** پیاده‌سازی متد `generate_daily_plan` در `app/services/planner_service.py` که برنامه روزانه تولید می‌کند. این مرحله شامل دریافت وظایف از دیتابیس، اولویت‌بندی و زمان‌بندی است. نکته حیاتی: خروجی باید شامل فیلدهای `daily_plan` و `tasks` باشد.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
سرویس برنامه‌ریز (planner_service) پیاده‌سازی نشده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/planner_service.py:1-50` — `PlannerService` — کل سرویس باید پیاده‌سازی شود
  ```python
  class PlannerService:
      def __init__(self):
          pass
      
      async def generate_daily_plan(self, user_id: int):
          # TODO: Implement
          return []
  ```
```

### Step 19: پیاده‌سازی سرویس برنامه‌ریز - اطمینان از اولویت‌بندی وظایف در برنامه
**Status:** `done` (100%)
**Scope:** تکمیل متد `generate_daily_plan` در `app/services/planner_service.py` برای اطمینان از اینکه وظایف تولیدشده اولویت‌بندی شده‌اند. این مرحله شامل اضافه کردن منطق اولویت‌بندی بر اساس deadline، importance و ... است. نکته حیاتی: فیلد `tasks` باید شامل وظایف مرتب‌شده بر اساس اولویت باشد.
**Excerpt:**
```
- برنامه تولیدشده شامل وظایف اولویت‌بندی‌شده است [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/planner/generate", "headers": null, "json_body": {"date": "2025-03-30"}, "expected_status": 200, "required_fields": ["tasks"], "json_contains": null}]
```

### Step 20: پیاده‌سازی سرویس برنامه‌ریز - اضافه کردن تست‌های واحد برای planner_service
**Status:** `partial` (50%)
**Scope:** اضافه کردن تست‌های واحد برای تمام متدهای `PlannerService` در `tests/test_planner_service.py`. این مرحله شامل نوشتن تست برای `generate_daily_plan` و سایر متدها است. نکته حیاتی: تست‌ها باید با timeout 60 ثانیه اجرا شوند.
**Excerpt:**
```
- تست‌های واحد برای هر متد سرویس اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_planner_service.py", "timeout_seconds": 60}]
```

### Step 21: اجرای تست‌های AI (تسک 1) - اجرای تست test_ai_service_runs
**Status:** `done` (100%)
**Scope:** اجرای تست `tests/test_ai_service.py::test_ai_service_runs` برای اطمینان از پاس شدن آن پس از رفع ایمپورت. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: timeout 30 ثانیه است.
**Excerpt:**
```
- سرویس AI بدون خطا اجرا می‌شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai_service.py::test_ai_service_runs", "timeout_seconds": 30}]
```

### Step 22: اجرای تست‌های AI (تسک 1) - اجرای تمام تست‌های test_ai_service.py
**Status:** `done` (100%)
**Scope:** اجرای تمام تست‌های موجود در `tests/test_ai_service.py` برای اطمینان از پاس شدن آن‌ها. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: timeout 60 ثانیه است.
**Excerpt:**
```
- تست‌های مربوط به AI پاس می‌شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_ai_service.py", "timeout_seconds": 60}]
```

### Step 23: اجرای تست‌های AI (تسک 4) - اجرای تست‌های sanitization
**Status:** `not_done` (0%)
**Scope:** اجرای تست‌های مربوط به sanitization در `tests/test_ai.py` برای اطمینان از پاس شدن آن‌ها. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest tests/test_ai.py -k sanitization` استفاده شود.
**Excerpt:**
```
- `pytest tests/test_ai.py -k sanitization`
```

### Step 24: اجرای تست‌های planner_service (تسک 6) - اجرای تست‌های test_planner.py
**Status:** `done` (100%)
**Scope:** اجرای تست‌های موجود در `tests/test_planner.py` برای اطمینان از پاس شدن آن‌ها. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest tests/test_planner.py -v` استفاده شود.
**Excerpt:**
```
- `pytest tests/test_planner.py -v`
```

### Step 25: اجرای تست‌های planner_service (تسک 6) - اجرای تست‌های test_planner_service.py
**Status:** `done` (100%)
**Scope:** اجرای تست‌های موجود در `tests/test_planner_service.py` برای اطمینان از پاس شدن آن‌ها. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest tests/test_planner_service.py -v` استفاده شود.
**Excerpt:**
```
- تست‌های واحد برای هر متد سرویس اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_planner_service.py", "timeout_seconds": 60}]
```

### Step 26: اجرای تست‌های AI (تسک 5) - اجرای تست‌های test_ai.py
**Status:** `done` (100%)
**Scope:** اجرای تست‌های موجود در `tests/test_ai.py` برای اطمینان از پاس شدن آن‌ها. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest tests/test_ai.py -v` استفاده شود.
**Excerpt:**
```
- `pytest tests/test_ai.py -v`
```

### Step 27: اجرای تست‌های Webhook (تسک 5) - اجرای تست‌های test_webhook.py
**Status:** `not_done` (0%)
**Scope:** اجرای تست‌های موجود در `tests/test_webhook.py` برای اطمینان از پاس شدن آن‌ها. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest tests/test_webhook.py -v` استفاده شود.
**Excerpt:**
```
- `pytest tests/test_webhook.py -v`
```

### Step 28: بررسی وجود فایل ai_model_config.py (تسک 1)
**Status:** `done` (100%)
**Scope:** بررسی وجود فایل `app/models/ai_model_config.py` و کلاس `AIModelConfig` در آن. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: اگر فایل وجود ندارد، باید ایجاد شود.
**Excerpt:**
```
- `app/models/ai_model_config.py` (سطر 1) — مدل مورد نظر
```

### Step 29: بررسی route AI (تسک 1) - بررسی استفاده از ai_service در app/routes/ai.py
**Status:** `done` (100%)
**Scope:** بررسی فایل `app/routes/ai.py` برای اطمینان از اینکه از `ai_service` استفاده می‌کند. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: اگر route از ai_service استفاده نمی‌کند، باید اصلاح شود.
**Excerpt:**
```
- `app/routes/ai.py` (سطر 10) — از ai_service استفاده می‌کند
```

### Step 30: بررسی مدل Task (تسک 2) - بررسی مدل Task در app/models/task.py
**Status:** `done` (100%)
**Scope:** بررسی فایل `app/models/task.py` برای اطمینان از وجود مدل `Task` و فیلدهای مورد نیاز برای جستجو. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: مدل باید شامل فیلد `title` باشد.
— [merged] بررسی فایل `app/models/task.py` برای اطمینان از وجود فیلدهای مورد نیاز برای برنامه‌ریزی (مانند deadline، priority). این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: مدل باید شامل فیلدهای `deadline` و `priority` باشد.
**Excerpt:**
```
- `app/models/task.py` (سطر 10) — Task model used in queries
```

### Step 31: بررسی route Tasks (تسک 2) - بررسی endpoint جستجو در app/routes/tasks.py
**Status:** `done` (100%)
**Scope:** بررسی فایل `app/routes/tasks.py` برای اطمینان از وجود endpoint جستجو. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: endpoint باید از `planner_service.search_tasks` استفاده کند.
— [merged] بررسی فایل `app/routes/ai.py` برای اطمینان از وجود endpoint `POST /ai/generate`. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: endpoint باید از `ai_service.process_prompt` استفاده کند.
**Excerpt:**
```
- `app/routes/tasks.py` (سطر 50) — Route that exposes search endpoint
```

### Step 32: بررسی integration_service (تسک 3) - بررسی فایل app/services/integration_service.py
**Status:** `partial` (80%)
**Scope:** بررسی فایل `app/services/integration_service.py` برای اطمینان از وجود توابع `process_webhook` و `get_provider_secret`. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: اگر توابع وجود ندارند، باید ایجاد شوند.
**Excerpt:**
```
- `app/services/integration_service.py` (سطر 1) — Processes webhook data
```

### Step 33: بررسی مدل Notification (تسک 3) - بررسی مدل Notification در app/models/notification.py
**Status:** `done` (100%)
**Scope:** بررسی فایل `app/models/notification.py` برای اطمینان از وجود فیلدهای مورد نیاز برای tracking تحویل webhook. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: مدل باید شامل فیلدهای `status` و `timestamp` باشد.
**Excerpt:**
```
- `app/models/notification.py` — May need webhook delivery tracking fields
```

### Step 34: بررسی schema AI (تسک 4) - بررسی فایل app/schemas/ai_schema.py
**Status:** `done` (100%)
**Scope:** بررسی فایل `app/schemas/ai_schema.py` برای اطمینان از وجود schema مورد نیاز برای اعتبارسنجی پاسخ AI. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: schema باید شامل فیلد `generated_text` باشد.
— [merged] بررسی فایل `app/schemas/ai_schema.py` برای اطمینان از وجود schema ورودی/خروجی. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: schema باید شامل فیلدهای `prompt` و `generated_text` باشد.
**Excerpt:**
```
- `app/schemas/ai_schema.py` — Should define validated response schema
```

### Step 35: بررسی سرویس AI (تسک 5) - بررسی فایل app/services/ai_service.py
**Status:** `done` (100%)
**Scope:** بررسی فایل `app/services/ai_service.py` برای اطمینان از وجود تابع `generate_content`. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: تابع باید منطق واقعی تولید محتوا را داشته باشد.
**Excerpt:**
```
- `app/services/ai_service.py` (سطر 1) — سرویس AI که باید منطق واقعی را پیاده‌سازی کند
```

### Step 36: بررسی سرویس AI (تسک 6) - بررسی ارتباط planner_service با ai_service
**Status:** `done` (100%)
**Scope:** بررسی فایل `app/services/planner_service.py` برای اطمینان از اینکه از `ai_service` برای پیشنهادات هوشمند استفاده می‌کند. این مرحله فقط بررسی است و شامل تغییر کد نمی‌شود. نکته حیاتی: اگر ارتباط وجود ندارد، باید اضافه شود.
**Excerpt:**
```
- `app/services/ai_service.py` (سطر 1) — برای پیشنهادات هوشمند برنامه‌ریزی
```

### Step 37: اجرای تست‌های جامع (همه تسک‌ها) - اجرای تمام تست‌ها با pytest
**Status:** `done` (100%)
**Scope:** اجرای تمام تست‌های پروژه با `pytest` برای اطمینان از اینکه هیچ تستی fail نمی‌شود. این مرحله فقط اجرای تست است و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از `pytest` بدون هیچ فیلتری استفاده شود.
**Excerpt:**
```
- هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
```

### Step 38: اجرای linter و type-check (همه تسک‌ها)
**Status:** `partial` (50%)
**Scope:** اجرای linter (مثلاً `flake8` یا `pylint`) و type-check (مثلاً `mypy`) برای اطمینان از عبور بدون warning. این مرحله فقط اجرای ابزارهاست و شامل تغییر کد نمی‌شود. نکته حیاتی: باید از دستورات مشخص‌شده در AC استفاده شود.
**Excerpt:**
```
- linter بدون warning عبور می‌کند
- type-check موفق است (`tsc --noEmit` / `mypy`)
```
