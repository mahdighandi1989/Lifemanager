---
task_id: 06baa237-dc29-4fdc-81f3-93a71ef1376f
title: پیاده‌سازی کنترل دسترسی API با feature flag
type: bug
priority: high
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:26:05.802790+00:00'
updated_at: '2026-05-26T23:01:41.889901+00:00'
archived: true
archived_at: '2026-05-26T23:01:30.615095+00:00'
target_files:
- app/main.py
- app/routes/ai.py
- app/routes/integrations.py
---

# پیاده‌سازی کنترل دسترسی API با feature flag

## Raw Idea

در app/main.py خط 336، FEATURE_AI_ENABLED و FEATURE_INTEGRATIONS_ENABLED از app.config import شده‌اند، اما در app/config.py (که در فایل‌های کلیدی نمایش داده نشده) و .env.example این flagها تعریف شده‌اند. با این حال، هیچ route یا middlewareای در کد از این flagها برای غیرفعال کردن مسیرها استفاده نمی‌کند. این یعنی feature flags صرفاً در endpoint /api/oversight/status گزارش می‌شوند و عملاً تأثیری روی رفتار برنامه ندارند. این یک ناهماهنگی cross-cutting است: README و .env.example ادعای feature flagging دارند اما پیاده‌سازی ناقص است.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
عدم تطابق بین feature flags در config و استفاده در main.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/main.py:336-345` — `oversight_status` — feature flags فقط گزارش می‌شوند، اعمال نمی‌شوند
  ```python
  from app.config import FEATURE_AI_ENABLED, FEATURE_INTEGRATIONS_ENABLED
  
  return {
      "status": "ok",
      "service": "lifemanager",
      "feature_flags": {
          "FEATURE_AI_ENABLED": FEATURE_AI_ENABLED,
          "FEATURE_INTEGRATIONS_ENABLED": FEATURE_INTEGRATIONS_ENABLED,
      },
  }
  ```
- `app/routes/ai.py:32` — `router` — این router بدون بررسی FEATURE_AI_ENABLED mount شده
  ```python
  router = APIRouter(prefix="/ai", tags=["ai"])
  ```
- `app/routes/integrations.py:10` — `router` — این router بدون بررسی FEATURE_INTEGRATIONS_ENABLED mount شده
  ```python
  router = APIRouter()
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
FastAPI + feature flags از env var

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `.env.example` (سطر 50) — تعریف feature flags
- `app/config.py` (سطر 1) — محل خواندن feature flags از env
- `app/middleware.py` — `main.py` این فایل را import می‌کند
- `config/settings.py` — `main.py` این فایل را import می‌کند
- `app/database.py` — `main.py` این فایل را import می‌کند
- `main.py` — این فایل `main.py` را import می‌کند (caller)
- `app/dependencies/auth.py` — `ai.py` این فایل را import می‌کند

## 🌐 نقشهٔ وابستگی‌ها
تغییر روی app/main.py و دو فایل router تأثیر می‌گذارد. feature flags در config/settings.py خوانده می‌شوند.

## 🔍 Context و وضعیت فعلی
در app/main.py خط 336، FEATURE_AI_ENABLED و FEATURE_INTEGRATIONS_ENABLED از app.config import شده‌اند، اما در app/config.py (که در فایل‌های کلیدی نمایش داده نشده) و .env.example این flagها تعریف شده‌اند. با این حال، هیچ route یا middlewareای در کد از این flagها برای غیرفعال کردن مسیرها استفاده نمی‌کند. این یعنی feature flags صرفاً در endpoint /api/oversight/status گزارش می‌شوند و عملاً تأثیری روی رفتار برنامه ندارند. این یک ناهماهنگی cross-cutting است: README و .env.example ادعای feature flagging دارند اما پیاده‌سازی ناقص است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] وقتی FEATURE_AI_ENABLED=false باشد، /ai/generate 404 برگرداند
- [ ] وقتی FEATURE_INTEGRATIONS_ENABLED=false باشد، /integrations/ 404 برگرداند
- [ ] endpoint /api/oversight/status همچنان وضعیت واقعی flagها را نشان دهد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. یک middleware یا dependency injection برای بررسی feature flags در routerهای ai و integrations اضافه شود. اگر flag مربوطه false باشد، routeها باید 503 یا 404 برگردانند. همچنین می‌توان از APIRouter.dependencies برای اعمال سراسری روی prefix مربوطه استفاده کرد.

## 💡 نمونه‌های قبل/بعد
**اعمال feature flag روی router ai**

_قبل:_
```
app.include_router(ai.router, tags=["ai"])
```

_بعد:_
```
if FEATURE_AI_ENABLED:
    app.include_router(ai.router, tags=["ai"])
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl -X POST http://localhost:8000/ai/generate -H 'Content-Type: application/json' -d '{"prompt":"test"}' # باید 404 بدهد`
- `curl http://localhost:8000/api/oversight/status | jq '.feature_flags.FEATURE_AI_ENABLED' # باید false باشد`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر کوچک و ایزوله، ریسک پایین

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: small

## Acceptance Criteria

1. وقتی FEATURE_AI_ENABLED=false باشد، /ai/generate 404 برگرداند _(verify: api_response)_
2. وقتی FEATURE_INTEGRATIONS_ENABLED=false باشد، /integrations/ 404 برگرداند _(verify: api_response)_
3. endpoint /api/oversight/status همچنان وضعیت واقعی flagها را نشان دهد _(verify: api_response)_

## Task Steps

### Step 1: اضافه کردن middleware شرطی برای feature flags در app/main.py
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد یک middleware در app/main.py است که قبل از پردازش هر درخواست، مسیرهای /ai و /integrations را بر اساس مقادیر FEATURE_AI_ENABLED و FEATURE_INTEGRATIONS_ENABLED از app.config بررسی کند. اگر flag مربوطه False باشد، middleware باید یک پاسخ 503 Service Unavailable برگرداند. خارج از این مرحله: تغییر در app/routes/ai.py یا app/routes/integrations.py، تغییر در app/config.py، یا اضافه کردن endpoint جدید. نکته حیاتی: middleware باید بعد از CORS middleware اما قبل از route handlers اجرا شود.
**Excerpt:**
```
در app/main.py خط 336، FEATURE_AI_ENABLED و FEATURE_INTEGRATIONS_ENABLED از app.config import شده‌اند، اما در app/config.py (که در فایل‌های کلیدی نمایش داده نشده) و .env.example این flagها تعریف شده‌اند. با این حال، هیچ route یا middlewareای در کد از این flagها برای غیرفعال کردن مسیرها استفاده نمی‌کند. این یعنی feature flags صرفاً در endpoint /api/oversight/status گزارش می‌شوند و عملاً تأثیری روی رفتار برنامه ندارند.
```

### Step 2: اضافه کردن بررسی شرطی در app/routes/ai.py برای FEATURE_AI_ENABLED
**Status:** `done` (100%)
**Scope:** این مرحله شامل اصلاح app/routes/ai.py است تا قبل از پردازش هر درخواست در این router، مقدار FEATURE_AI_ENABLED از app.config بررسی شود. اگر False باشد، یک پاسخ 503 برگردانده شود. خارج از این مرحله: تغییر در app/main.py، app/routes/integrations.py، یا app/config.py. نکته حیاتی: این یک لایه دفاعی دوم است؛ middleware در main.py اولویت دارد.
**Excerpt:**
```
app/routes/ai.py:32 — router — این router بدون بررسی FEATURE_AI_ENABLED mount شده
  router = APIRouter(prefix="/ai", tags=["ai"])
```

### Step 3: اضافه کردن بررسی شرطی در app/routes/integrations.py برای FEATURE_INTEGRATIONS_ENABLED
**Status:** `done` (100%)
**Scope:** این مرحله شامل اصلاح app/routes/integrations.py است تا قبل از پردازش هر درخواست در این router، مقدار FEATURE_INTEGRATIONS_ENABLED از app.config بررسی شود. اگر False باشد، یک پاسخ 503 برگردانده شود. خارج از این مرحله: تغییر در app/main.py، app/routes/ai.py، یا app/config.py. نکته حیاتی: این یک لایه دفاعی دوم است؛ middleware در main.py اولویت دارد.
**Excerpt:**
```
app/routes/integrations.py:10 — router — این router بدون بررسی FEATURE_INTEGRATIONS_ENABLED mount شده
  router = APIRouter()
```
