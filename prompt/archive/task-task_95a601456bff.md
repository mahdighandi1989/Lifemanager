---
task_id: task_95a601456bff
title: پیاده‌سازی کامپوننت‌های اصلی فرانت‌اند
type: other
priority: high
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-25T06:39:06.962895+00:00'
updated_at: '2026-05-29T20:32:50.881916+00:00'
archived: true
archived_at: '2026-05-25T16:51:13.230181+00:00'
tags:
- consolidated
- post_verify_merge
---

# پیاده‌سازی کامپوننت‌های اصلی فرانت‌اند

## Raw Idea

🧬 این یک تسک تلفیقی است — از 4 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): این خوشه شامل تمام تسک‌های مربوط به توسعه رابط کاربری فرانت‌اند، از جمله پیاده‌سازی کامپوننت‌های مشترک، صفحات اصلی، یکپارچه‌سازی سرویس‌های API با Axios و پیاده‌سازی مدیریت وضعیت (state management) در اپلیکیشن است.
🎯 theme: توسعه رابط کاربری و مدیریت وضعیت فرانت‌اند
💎 estimated_difficulty: medium

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 4
  id: b8facb63-ebbc-4d9e-b5ad-ea1fdff8569c
  عنوان اصلی: پیاده‌سازی کامپوننت‌های اشتراکی (Header, Sidebar, Footer)
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: frontend/src/components/Footer.tsx, frontend/src/components/Header.tsx, frontend/src/components/Sidebar.tsx

📋 acceptance_criteria کامل:
  - Header در تمام صفحات نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='header']"}], "expecte]
  - Sidebar شامل لینک به صفحات اصلی باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='sidebar']"}, {"action]
  - Footer در پایین صفحات نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='footer']"}], "expecte]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
کامپوننت‌های اشتراکی (Header, Sidebar, Footer) وجود ندارند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/components/Header.tsx:1-40` — `Header` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/components/Sidebar.tsx:1-50` — `Sidebar` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/components/Footer.tsx:1-20` — `Footer` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
React/Next.js + CSS/Tailwind

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/auth.py` (سطر 1) — endpoint خروج از سیستم

## 🌐 نقشهٔ وابستگی‌ها
این کامپوننت‌ها در تمام صفحات استفاده می‌شوند و به مسیرهای مختلف لینک می‌دهند.

## 🔍 Context و وضعیت فعلی
در پروژه هیچ کامپوننت اشتراکی مانند Header، Sidebar یا Footer یافت نشد. این کامپوننت‌ها برای ناوبری و نمایش اطلاعات عمومی در تمام صفحات ضروری هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Header در تمام صفحات نمایش داده شود
- [ ] Sidebar شامل لینک به صفحات اصلی باشد
- [ ] Footer در پایین صفحات نمایش داده شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد کامپوننت‌های اشتراکی Header (شامل منوی ناوبری و دکمه خروج)، Sidebar (شامل لینک به صفحات مختلف) و Footer (شامل اطلاعات تماس).

## 💡 نمونه‌های قبل/بعد
**ایجاد Header**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
export default function Header() { return <nav><Link href='/dashboard'>داشبورد</Link><button onClick={logout}>خروج</button></nav>; }
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `باز کردن مرورگر و بررسی نمایش کامپوننت‌ها`

## ⚠️ ریسک‌ها و موارد احتیاط
ندارد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 2 از 4
  id: 90c83ffe-19a1-4741-ab97-e1cdd409b398
  عنوان اصلی: پیاده‌سازی صفحه اصلی با لینک‌های ورود/ثبت‌نام
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: frontend/src/pages/index.tsx

📋 acceptance_criteria کامل:
  - صفحه اصلی در مسیر / قابل دسترسی باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "screenshot", "label": "homepage_loaded"}, {"action": "assert_visi]
  - شامل لینک به صفحه ورود و ثبت‌نام باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='login-link']"}, {"act]
  - بدون خطا در مرورگر نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "screenshot", "label": "homepage_no_errors"}, {"action": "assert_n]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
صفحه اصلی (index) وجود ندارد یا placeholder است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/index.tsx:1-30` — `HomePage` — نیاز به ایجاد فایل جدید
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
React/Next.js + FastAPI backend

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/auth.py` (سطر 1) — endpoint‌های احراز هویت که صفحه اصلی باید به آن‌ها متصل شود

## 🌐 نقشهٔ وابستگی‌ها
این صفحه به هیچ فایل دیگری وابسته نیست، اما تمام صفحات دیگر به آن لینک می‌دهند.

## 🔍 Context و وضعیت فعلی
در پروژه هیچ فایل فرانت‌اندی (مانند index.html, App.jsx, pages/index.tsx) یافت نشد. این نشان می‌دهد که یا فرانت‌اند هنوز پیاده‌سازی نشده یا در ریپو موجود نیست. بدون صفحه اصلی، کاربر نمی‌تواند وارد برنامه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] صفحه اصلی در مسیر / قابل دسترسی باشد
- [ ] شامل لینک به صفحه ورود و ثبت‌نام باشد
- [ ] بدون خطا در مرورگر نمایش داده شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد یک صفحه اصلی ساده با React/Next.js که شامل دکمه ورود و ثبت‌نام باشد و به endpoint /api/auth/login و /api/auth/register متصل شود.

## 💡 نمونه‌های قبل/بعد
**ایجاد صفحه اصلی**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
export default function HomePage() { return <div><h1>Lifemanager</h1><a href='/login'>ورود</a></div>; }
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `curl http://localhost:3000`

## ⚠️ ریسک‌ها و موارد احتیاط
ندارد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: small

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 3 از 4
  id: bebf9cac-1563-408d-8ded-caab33de1921
  عنوان اصلی: ایجاد سرویس API با Axios
  اولویت اصلی: high
  وضعیت verify قبلی: pending
  فایل‌های دخیل: frontend/src/lib/api.ts

📋 acceptance_criteria کامل:
  - فایل api.ts با axios instance ایجاد شود [verify_method=static] [verify_plan={"grep_patterns": ["axios", "create"], "files_hint": ["frontend/src/lib/api.ts"]}]
  - baseURL به آدرس backend تنظیم شود [verify_method=static] [verify_plan={"grep_patterns": ["baseURL"], "files_hint": ["frontend/src/lib/api.ts"]}]
  - interceptor برای اضافه کردن توکن JWT پیاده‌سازی شود [verify_method=static] [verify_plan={"grep_patterns": ["interceptor", "request", "Authorization", "Bearer"], "files_hint": ["frontend/src/lib/api.ts"]}]
  - interceptor برای مدیریت خطاهای 401 پیاده‌سازی شود [verify_method=static] [verify_plan={"grep_patterns": ["interceptor", "response", "401"], "files_hint": ["frontend/src/lib/api.ts"]}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
سرویس API (axios instance) برای ارتباط با backend وجود ندارد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/lib/api.ts:1-30` — `api` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
Axios + JWT + FastAPI

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/main.py` (سطر 1) — پایه URL backend
- `app/middleware.py` (سطر 1) — میان‌افزار احراز هویت

## 🌐 نقشهٔ وابستگی‌ها
این سرویس توسط تمام صفحات و کامپوننت‌ها برای فراخوانی API استفاده می‌شود.

## 🔍 Context و وضعیت فعلی
هیچ فایل سرویس API برای ارتباط با backend (مانند api.ts یا axiosInstance.ts) در پروژه یافت نشد. بدون این سرویس، فرانت‌اند نمی‌تواند با backend ارتباط برقرار کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل api.ts با axios instance ایجاد شود
- [ ] baseURL به آدرس backend تنظیم شود
- [ ] interceptor برای اضافه کردن توکن JWT پیاده‌سازی شود
- [ ] interceptor برای مدیریت خطاهای 401 پیاده‌سازی شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد فایل سرویس API با axios که شامل baseURL (http://localhost:8000/api)، interceptors برای اضافه کردن توکن JWT به هدر Authorization و مدیریت خطاها باشد.

## 💡 نمونه‌های قبل/بعد
**ایجاد سرویس API**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
import axios from 'axios';
const api = axios.create({ baseURL: 'http://localhost:8000/api' });
api.interceptors.request.use(config => { const token = localStorage.getItem('token'); if (token) config.headers.Authorization = `Bearer ${token}`; return config; });
export default api;
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `بررسی کنسول مرورگر برای خطاهای شبکه`

## ⚠️ ریسک‌ها و موارد احتیاط
ندارد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: small

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 4 از 4
  id: 01733bdb-c8f9-4ef1-8ade-533d98385e8e
  عنوان اصلی: مدیریت وضعیت (state management) پیاده‌سازی نشده است
  اولویت اصلی: medium
  وضعیت verify قبلی: partial
  فایل‌های دخیل: frontend/src/context/AuthContext.tsx, frontend/src/context/ProjectContext.tsx, frontend/src/context/TaskContext.tsx

📋 acceptance_criteria کامل:
  - AuthContext وضعیت کاربر (user, token, isAuthenticated) را مدیریت کند [verify_method=static] [verify_plan={"grep_patterns": ["createContext", "useState", "user", "token", "isAuthenticated"], "files_hint": ["frontend/src/context/AuthContext.tsx"]}]
  - ProjectContext لیست پروژه‌ها را مدیریت کند [verify_method=static] [verify_plan={"grep_patterns": ["createContext", "useState", "projects"], "files_hint": ["frontend/src/context/ProjectContext.tsx"]}]
  - TaskContext لیست وظایف را مدیریت کند [verify_method=static] [verify_plan={"grep_patterns": ["createContext", "useState", "tasks"], "files_hint": ["frontend/src/context/TaskContext.tsx"]}]
  - هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد [verify_method=static] [verify_plan={"grep_patterns": ["function.*update", "function.*set", "dispatch"], "files_hint": ["frontend/src/context/AuthContext.tsx", "frontend/src/context/ProjectContext.tsx", "frontend/src/context/TaskContext]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
مدیریت وضعیت (state management) پیاده‌سازی نشده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/context/AuthContext.tsx:1-50` — `AuthContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/context/ProjectContext.tsx:1-50` — `ProjectContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/context/TaskContext.tsx:1-50` — `TaskContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
React Context API + TypeScript

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/auth_service.py` (سطر 1) — سرویس احراز هویت backend
- `app/services/planner_service.py` (سطر 1) — سرویس برنامه‌ریزی backend

## 🌐 نقشهٔ وابستگی‌ها
این contextها توسط تمام صفحات و کامپوننت‌ها برای دسترسی به وضعیت کاربر و داده‌ها استفاده می‌شوند.

## 🔍 Context و وضعیت فعلی
هیچ فایل یا کتابخانه‌ای برای مدیریت وضعیت (مانند Redux, Zustand, Context API) در پروژه یافت نشد. برای مدیریت وضعیت کاربر (لاگین/لاگوت)، وضعیت پروژه‌ها و وظایف، نیاز به یک راهکار مدیریت وضعیت است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] AuthContext وضعیت کاربر (user, token, isAuthenticated) را مدیریت کند
- [ ] ProjectContext لیست پروژه‌ها را مدیریت کند
- [ ] TaskContext لیست وظایف را مدیریت کند
- [ ] هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. پیاده‌سازی مدیریت وضعیت با استفاده از React Context API یا Zustand برای مدیریت وضعیت کاربر (user, token, isAuthenticated) و وضعیت پروژه‌ها و وظایف.

## 💡 نمونه‌های قبل/بعد
**ایجاد AuthContext**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
export const AuthContext = createContext({ user: null, login: async (email, password) => {}, logout: () => {} });
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `بررسی کنسول مرورگر برای خطاهای context`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به هماهنگی با backend برای نوع داده‌ها

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  - ایجاد AuthContext برای مدیریت وضعیت احراز هویت کاربر
  - ایجاد ProjectContext برای مدیریت وضعیت پروژه‌ها
  - ایجاد TaskContext برای مدیریت وضعیت وظایف

🔧 مراحل remaining که در super-task باید انجام شوند:
  - یکپارچه‌سازی Context‌ها و ارائه Provider در بالاترین سطح اپلیکیشن — ProjectProvider و TaskProvider در App.jsx قرار داده نشده‌اند

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 نکات استاندارد (همان bullet هایی که در ساخت پرامپت‌های معمولی پروژه رعایت می‌شود — وراثت کامل، نه کپی):
- ساختار AC ها: acceptance_criteria با verify_method و verify_plan و evidence_locations برای هر AC
- edge cases را در نظر بگیر و در پرامپت ذکر کن
- وابستگی‌ها را اول حل کن (dependency-aware ordering)
- اگر بخشی از یکی از تسک‌ها قبلاً done است (pre_done در بالا)، تکرار نکن — فقط روی remaining_parts تمرکز کن
- در commit message: `merged-from: b8facb63-ebbc-4d9e-b5ad-ea1fdff8569c, 90c83ffe-19a1-4741-ab97-e1cdd409b398, bebf9cac-1563-408d-8ded-caab33de1921, 01733bdb-c8f9-4ef1-8ade-533d98385e8e`
- task_steps را با dependency-aware ordering مرتب کن
- هیچ کار قبلاً done شده‌ای نباید دوباره انجام شود
- هیچ خلاصه‌سازی نکن — جزئیات کامل از همهٔ منابع باید حفظ شوند


## Prompt

🧬 این یک تسک تلفیقی است — از 4 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): این خوشه شامل تمام تسک‌های مربوط به توسعه رابط کاربری فرانت‌اند، از جمله پیاده‌سازی کامپوننت‌های مشترک، صفحات اصلی، یکپارچه‌سازی سرویس‌های API با Axios و پیاده‌سازی مدیریت وضعیت (state management) در اپلیکیشن است.
🎯 theme: توسعه رابط کاربری و مدیریت وضعیت فرانت‌اند
💎 estimated_difficulty: medium

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 4
  id: b8facb63-ebbc-4d9e-b5ad-ea1fdff8569c
  عنوان اصلی: پیاده‌سازی کامپوننت‌های اشتراکی (Header, Sidebar, Footer)
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: frontend/src/components/Footer.tsx, frontend/src/components/Header.tsx, frontend/src/components/Sidebar.tsx

📋 acceptance_criteria کامل:
  - Header در تمام صفحات نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='header']"}], "expecte]
  - Sidebar شامل لینک به صفحات اصلی باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='sidebar']"}, {"action]
  - Footer در پایین صفحات نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='footer']"}], "expecte]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
کامپوننت‌های اشتراکی (Header, Sidebar, Footer) وجود ندارند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/components/Header.tsx:1-40` — `Header` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/components/Sidebar.tsx:1-50` — `Sidebar` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/components/Footer.tsx:1-20` — `Footer` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
React/Next.js + CSS/Tailwind

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/auth.py` (سطر 1) — endpoint خروج از سیستم

## 🌐 نقشهٔ وابستگی‌ها
این کامپوننت‌ها در تمام صفحات استفاده می‌شوند و به مسیرهای مختلف لینک می‌دهند.

## 🔍 Context و وضعیت فعلی
در پروژه هیچ کامپوننت اشتراکی مانند Header، Sidebar یا Footer یافت نشد. این کامپوننت‌ها برای ناوبری و نمایش اطلاعات عمومی در تمام صفحات ضروری هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Header در تمام صفحات نمایش داده شود
- [ ] Sidebar شامل لینک به صفحات اصلی باشد
- [ ] Footer در پایین صفحات نمایش داده شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد کامپوننت‌های اشتراکی Header (شامل منوی ناوبری و دکمه خروج)، Sidebar (شامل لینک به صفحات مختلف) و Footer (شامل اطلاعات تماس).

## 💡 نمونه‌های قبل/بعد
**ایجاد Header**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
export default function Header() { return <nav><Link href='/dashboard'>داشبورد</Link><button onClick={logout}>خروج</button></nav>; }
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `باز کردن مرورگر و بررسی نمایش کامپوننت‌ها`

## ⚠️ ریسک‌ها و موارد احتیاط
ندارد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 2 از 4
  id: 90c83ffe-19a1-4741-ab97-e1cdd409b398
  عنوان اصلی: پیاده‌سازی صفحه اصلی با لینک‌های ورود/ثبت‌نام
  اولویت اصلی: high
  وضعیت verify قبلی: partial
  فایل‌های دخیل: frontend/src/pages/index.tsx

📋 acceptance_criteria کامل:
  - صفحه اصلی در مسیر / قابل دسترسی باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "screenshot", "label": "homepage_loaded"}, {"action": "assert_visi]
  - شامل لینک به صفحه ورود و ثبت‌نام باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='login-link']"}, {"act]
  - بدون خطا در مرورگر نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "screenshot", "label": "homepage_no_errors"}, {"action": "assert_n]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
صفحه اصلی (index) وجود ندارد یا placeholder است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/index.tsx:1-30` — `HomePage` — نیاز به ایجاد فایل جدید
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
React/Next.js + FastAPI backend

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/auth.py` (سطر 1) — endpoint‌های احراز هویت که صفحه اصلی باید به آن‌ها متصل شود

## 🌐 نقشهٔ وابستگی‌ها
این صفحه به هیچ فایل دیگری وابسته نیست، اما تمام صفحات دیگر به آن لینک می‌دهند.

## 🔍 Context و وضعیت فعلی
در پروژه هیچ فایل فرانت‌اندی (مانند index.html, App.jsx, pages/index.tsx) یافت نشد. این نشان می‌دهد که یا فرانت‌اند هنوز پیاده‌سازی نشده یا در ریپو موجود نیست. بدون صفحه اصلی، کاربر نمی‌تواند وارد برنامه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] صفحه اصلی در مسیر / قابل دسترسی باشد
- [ ] شامل لینک به صفحه ورود و ثبت‌نام باشد
- [ ] بدون خطا در مرورگر نمایش داده شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد یک صفحه اصلی ساده با React/Next.js که شامل دکمه ورود و ثبت‌نام باشد و به endpoint /api/auth/login و /api/auth/register متصل شود.

## 💡 نمونه‌های قبل/بعد
**ایجاد صفحه اصلی**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
export default function HomePage() { return <div><h1>Lifemanager</h1><a href='/login'>ورود</a></div>; }
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `curl http://localhost:3000`

## ⚠️ ریسک‌ها و موارد احتیاط
ندارد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: small

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 3 از 4
  id: bebf9cac-1563-408d-8ded-caab33de1921
  عنوان اصلی: ایجاد سرویس API با Axios
  اولویت اصلی: high
  وضعیت verify قبلی: pending
  فایل‌های دخیل: frontend/src/lib/api.ts

📋 acceptance_criteria کامل:
  - فایل api.ts با axios instance ایجاد شود [verify_method=static] [verify_plan={"grep_patterns": ["axios", "create"], "files_hint": ["frontend/src/lib/api.ts"]}]
  - baseURL به آدرس backend تنظیم شود [verify_method=static] [verify_plan={"grep_patterns": ["baseURL"], "files_hint": ["frontend/src/lib/api.ts"]}]
  - interceptor برای اضافه کردن توکن JWT پیاده‌سازی شود [verify_method=static] [verify_plan={"grep_patterns": ["interceptor", "request", "Authorization", "Bearer"], "files_hint": ["frontend/src/lib/api.ts"]}]
  - interceptor برای مدیریت خطاهای 401 پیاده‌سازی شود [verify_method=static] [verify_plan={"grep_patterns": ["interceptor", "response", "401"], "files_hint": ["frontend/src/lib/api.ts"]}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
سرویس API (axios instance) برای ارتباط با backend وجود ندارد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/lib/api.ts:1-30` — `api` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
Axios + JWT + FastAPI

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/main.py` (سطر 1) — پایه URL backend
- `app/middleware.py` (سطر 1) — میان‌افزار احراز هویت

## 🌐 نقشهٔ وابستگی‌ها
این سرویس توسط تمام صفحات و کامپوننت‌ها برای فراخوانی API استفاده می‌شود.

## 🔍 Context و وضعیت فعلی
هیچ فایل سرویس API برای ارتباط با backend (مانند api.ts یا axiosInstance.ts) در پروژه یافت نشد. بدون این سرویس، فرانت‌اند نمی‌تواند با backend ارتباط برقرار کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل api.ts با axios instance ایجاد شود
- [ ] baseURL به آدرس backend تنظیم شود
- [ ] interceptor برای اضافه کردن توکن JWT پیاده‌سازی شود
- [ ] interceptor برای مدیریت خطاهای 401 پیاده‌سازی شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد فایل سرویس API با axios که شامل baseURL (http://localhost:8000/api)، interceptors برای اضافه کردن توکن JWT به هدر Authorization و مدیریت خطاها باشد.

## 💡 نمونه‌های قبل/بعد
**ایجاد سرویس API**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
import axios from 'axios';
const api = axios.create({ baseURL: 'http://localhost:8000/api' });
api.interceptors.request.use(config => { const token = localStorage.getItem('token'); if (token) config.headers.Authorization = `Bearer ${token}`; return config; });
export default api;
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `بررسی کنسول مرورگر برای خطاهای شبکه`

## ⚠️ ریسک‌ها و موارد احتیاط
ندارد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: small

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 4 از 4
  id: 01733bdb-c8f9-4ef1-8ade-533d98385e8e
  عنوان اصلی: مدیریت وضعیت (state management) پیاده‌سازی نشده است
  اولویت اصلی: medium
  وضعیت verify قبلی: partial
  فایل‌های دخیل: frontend/src/context/AuthContext.tsx, frontend/src/context/ProjectContext.tsx, frontend/src/context/TaskContext.tsx

📋 acceptance_criteria کامل:
  - AuthContext وضعیت کاربر (user, token, isAuthenticated) را مدیریت کند [verify_method=static] [verify_plan={"grep_patterns": ["createContext", "useState", "user", "token", "isAuthenticated"], "files_hint": ["frontend/src/context/AuthContext.tsx"]}]
  - ProjectContext لیست پروژه‌ها را مدیریت کند [verify_method=static] [verify_plan={"grep_patterns": ["createContext", "useState", "projects"], "files_hint": ["frontend/src/context/ProjectContext.tsx"]}]
  - TaskContext لیست وظایف را مدیریت کند [verify_method=static] [verify_plan={"grep_patterns": ["createContext", "useState", "tasks"], "files_hint": ["frontend/src/context/TaskContext.tsx"]}]
  - هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد [verify_method=static] [verify_plan={"grep_patterns": ["function.*update", "function.*set", "dispatch"], "files_hint": ["frontend/src/context/AuthContext.tsx", "frontend/src/context/ProjectContext.tsx", "frontend/src/context/TaskContext]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
مدیریت وضعیت (state management) پیاده‌سازی نشده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/context/AuthContext.tsx:1-50` — `AuthContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/context/ProjectContext.tsx:1-50` — `ProjectContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
- `frontend/src/context/TaskContext.tsx:1-50` — `TaskContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست

## 🧱 پشتهٔ فناوری و معماری
React Context API + TypeScript

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/auth_service.py` (سطر 1) — سرویس احراز هویت backend
- `app/services/planner_service.py` (سطر 1) — سرویس برنامه‌ریزی backend

## 🌐 نقشهٔ وابستگی‌ها
این contextها توسط تمام صفحات و کامپوننت‌ها برای دسترسی به وضعیت کاربر و داده‌ها استفاده می‌شوند.

## 🔍 Context و وضعیت فعلی
هیچ فایل یا کتابخانه‌ای برای مدیریت وضعیت (مانند Redux, Zustand, Context API) در پروژه یافت نشد. برای مدیریت وضعیت کاربر (لاگین/لاگوت)، وضعیت پروژه‌ها و وظایف، نیاز به یک راهکار مدیریت وضعیت است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] AuthContext وضعیت کاربر (user, token, isAuthenticated) را مدیریت کند
- [ ] ProjectContext لیست پروژه‌ها را مدیریت کند
- [ ] TaskContext لیست وظایف را مدیریت کند
- [ ] هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. پیاده‌سازی مدیریت وضعیت با استفاده از React Context API یا Zustand برای مدیریت وضعیت کاربر (user, token, isAuthenticated) و وضعیت پروژه‌ها و وظایف.

## 💡 نمونه‌های قبل/بعد
**ایجاد AuthContext**

_قبل:_
```
// وجود ندارد
```

_بعد:_
```
export const AuthContext = createContext({ user: null, login: async (email, password) => {}, logout: () => {} });
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `npm run dev`
- `بررسی کنسول مرورگر برای خطاهای context`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به هماهنگی با backend برای نوع داده‌ها

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  - ایجاد AuthContext برای مدیریت وضعیت احراز هویت کاربر
  - ایجاد ProjectContext برای مدیریت وضعیت پروژه‌ها
  - ایجاد TaskContext برای مدیریت وضعیت وظایف

🔧 مراحل remaining که در super-task باید انجام شوند:
  - یکپارچه‌سازی Context‌ها و ارائه Provider در بالاترین سطح اپلیکیشن — ProjectProvider و TaskProvider در App.jsx قرار داده نشده‌اند

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 نکات استاندارد (همان bullet هایی که در ساخت پرامپت‌های معمولی پروژه رعایت می‌شود — وراثت کامل، نه کپی):
- ساختار AC ها: acceptance_criteria با verify_method و verify_plan و evidence_locations برای هر AC
- edge cases را در نظر بگیر و در پرامپت ذکر کن
- وابستگی‌ها را اول حل کن (dependency-aware ordering)
- اگر بخشی از یکی از تسک‌ها قبلاً done است (pre_done در بالا)، تکرار نکن — فقط روی remaining_parts تمرکز کن
- در commit message: `merged-from: b8facb63-ebbc-4d9e-b5ad-ea1fdff8569c, 90c83ffe-19a1-4741-ab97-e1cdd409b398, bebf9cac-1563-408d-8ded-caab33de1921, 01733bdb-c8f9-4ef1-8ade-533d98385e8e`
- task_steps را با dependency-aware ordering مرتب کن
- هیچ کار قبلاً done شده‌ای نباید دوباره انجام شود
- هیچ خلاصه‌سازی نکن — جزئیات کامل از همهٔ منابع باید حفظ شوند


## Acceptance Criteria

1. Header در تمام صفحات نمایش داده شود _(verify: ui_interaction)_
2. Sidebar شامل لینک به صفحات اصلی باشد _(verify: ui_interaction)_
3. Footer در پایین صفحات نمایش داده شود _(verify: ui_interaction)_
4. صفحه اصلی در مسیر / قابل دسترسی باشد _(verify: ui_interaction)_
5. شامل لینک به صفحه ورود و ثبت‌نام باشد _(verify: ui_interaction)_
6. بدون خطا در مرورگر نمایش داده شود _(verify: ui_interaction)_
7. فایل api.ts با axios instance ایجاد شود _(verify: static)_
8. baseURL به آدرس backend تنظیم شود _(verify: static)_
9. interceptor برای اضافه کردن توکن JWT پیاده‌سازی شود _(verify: static)_
10. interceptor برای مدیریت خطاهای 401 پیاده‌سازی شود _(verify: static)_
11. AuthContext وضعیت کاربر (user, token, isAuthenticated) را مدیریت کند _(verify: static)_
12. ProjectContext لیست پروژه‌ها را مدیریت کند _(verify: static)_
13. TaskContext لیست وظایف را مدیریت کند _(verify: static)_
14. هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد _(verify: static)_

## Task Steps

### Step 1: ایجاد کامپوننت Header با منوی ناوبری و دکمه خروج
**Status:** `done` (100%)
**Scope:** ایجاد فایل frontend/src/components/Header.tsx با کامپوننت React که شامل منوی ناوبری (لینک به صفحات اصلی) و دکمه خروج از سیستم است. این کامپوننت باید در تمام صفحات نمایش داده شود و از data-testid='header' برای تست‌پذیری استفاده کند. خارج از این مرحله: طراحی Sidebar و Footer، منطق احراز هویت، استایل‌دهی پیشرفته.
**Excerpt:**
```
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.
```

### Step 2: ایجاد کامپوننت Sidebar با لینک به صفحات اصلی
**Status:** `done` (100%)
**Scope:** ایجاد فایل frontend/src/components/Sidebar.tsx با کامپوننت React که شامل لینک به صفحات اصلی (داشبورد، پروژه‌ها، وظایف) است. باید از data-testid='sidebar' استفاده کند. خارج از این مرحله: طراحی Header و Footer، منطق مسیریابی، استایل‌دهی پیشرفته.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
کامپوننت‌های اشتراکی (Header, Sidebar, Footer) وجود ندارند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/components/Sidebar.tsx:1-50` — `Sidebar` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
```

### Step 3: ایجاد کامپوننت Footer با اطلاعات تماس
**Status:** `done` (100%)
**Scope:** ایجاد فایل frontend/src/components/Footer.tsx با کامپوننت React که در پایین صفحات نمایش داده شود و شامل اطلاعات تماس (مانند ایمیل، تلفن) باشد. باید از data-testid='footer' استفاده کند. خارج از این مرحله: طراحی Header و Sidebar، منطق احراز هویت.
**Excerpt:**
```
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.
```

### Step 4: تأیید نمایش Header در تمام صفحات
**Status:** `done` (100%)
**Scope:** بررسی اینکه کامپوننت Header در تمام صفحات (مانند /، /login، /dashboard) نمایش داده شود. این مرحله شامل تست UI با Playwright یا ابزار مشابه است. خارج از این مرحله: طراحی Sidebar و Footer.
**Excerpt:**
```
- Header در تمام صفحات نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='header']"}], "expecte]
```

### Step 5: تأیید وجود لینک‌های اصلی در Sidebar
**Status:** `done` (100%)
**Scope:** بررسی اینکه Sidebar شامل لینک به صفحات اصلی (داشبورد، پروژه‌ها، وظایف) است. این مرحله شامل تست UI با Playwright است. خارج از این مرحله: طراحی Header و Footer.
**Excerpt:**
```
- Sidebar شامل لینک به صفحات اصلی باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='sidebar']"}, {"action]
```

### Step 6: تأیید نمایش Footer در پایین صفحات
**Status:** `done` (100%)
**Scope:** بررسی اینکه Footer در پایین تمام صفحات نمایش داده شود. این مرحله شامل تست UI با Playwright است. خارج از این مرحله: طراحی Header و Sidebar.
**Excerpt:**
```
- Footer در پایین صفحات نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='footer']"}], "expecte]
```

### Step 7: ایجاد صفحه اصلی (index) با لینک‌های ورود/ثبت‌نام
**Status:** `done` (100%)
**Scope:** ایجاد فایل frontend/src/pages/index.tsx با کامپوننت HomePage که در مسیر / قابل دسترسی است و شامل لینک به صفحه ورود و ثبت‌نام می‌باشد. باید از data-testid='login-link' و data-testid='register-link' استفاده کند. خارج از این مرحله: طراحی Header, Sidebar, Footer، منطق احراز هویت.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
صفحه اصلی (index) وجود ندارد یا placeholder است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/index.tsx:1-30` — `HomePage` — نیاز به ایجاد فایل جدید
  ```tsx
  // فایل وجود ندارد
  ```
```

### Step 8: تأیید دسترسی به صفحه اصلی در مسیر /
**Status:** `done` (100%)
**Scope:** بررسی اینکه صفحه اصلی در مسیر / بدون خطا قابل دسترسی است. این مرحله شامل تست UI با Playwright است. خارج از این مرحله: طراحی لینک‌ها.
**Excerpt:**
```
- صفحه اصلی در مسیر / قابل دسترسی باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "screenshot", "label": "homepage_loaded"}, {"action": "assert_visi]
```

### Step 9: تأیید وجود لینک‌های ورود و ثبت‌نام در صفحه اصلی
**Status:** `done` (100%)
**Scope:** بررسی اینکه صفحه اصلی شامل لینک به صفحه ورود و ثبت‌نام است. این مرحله شامل تست UI با Playwright است. خارج از این مرحله: طراحی صفحه اصلی.
**Excerpt:**
```
- شامل لینک به صفحه ورود و ثبت‌نام باشد [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "assert_visible", "selector": "[data-testid='login-link']"}, {"act]
```

### Step 10: تأیید نمایش صفحه اصلی بدون خطا در مرورگر
**Status:** `done` (100%)
**Scope:** بررسی اینکه صفحه اصلی بدون خطا در مرورگر نمایش داده شود. این مرحله شامل تست UI با Playwright است. خارج از این مرحله: طراحی لینک‌ها.
**Excerpt:**
```
- بدون خطا در مرورگر نمایش داده شود [verify_method=ui_interaction] [verify_plan={"base": "frontend", "ui_steps": [{"action": "navigate", "url": "/"}, {"action": "wait_for_load", "state": "networkidle"}, {"action": "screenshot", "label": "homepage_no_errors"}, {"action": "assert_n]
```

### Step 11: ایجاد فایل سرویس API با Axios و baseURL
**Status:** `done` (100%)
**Scope:** ایجاد فایل frontend/src/lib/api.ts با یک axios instance که baseURL آن به آدرس backend (http://localhost:8000/api) تنظیم شده است. خارج از این مرحله: پیاده‌سازی interceptors، مدیریت خطاها.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
سرویس API (axios instance) برای ارتباط با backend وجود ندارد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/lib/api.ts:1-30` — `api` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
```

### Step 12: تنظیم baseURL در سرویس API به آدرس backend
**Status:** `done` (100%)
**Scope:** اطمینان از اینکه baseURL در axios instance به آدرس backend (http://localhost:8000/api) تنظیم شده است. خارج از این مرحله: ایجاد فایل api.ts، پیاده‌سازی interceptors.
**Excerpt:**
```
- baseURL به آدرس backend تنظیم شود [verify_method=static] [verify_plan={"grep_patterns": ["baseURL"], "files_hint": ["frontend/src/lib/api.ts"]}]
```

### Step 13: پیاده‌سازی interceptor برای اضافه کردن توکن JWT به درخواست‌ها
**Status:** `done` (100%)
**Scope:** اضافه کردن یک request interceptor به axios instance که توکن JWT را از localStorage خوانده و به هدر Authorization اضافه کند. خارج از این مرحله: ایجاد فایل api.ts، تنظیم baseURL، مدیریت خطاهای 401.
**Excerpt:**
```
- interceptor برای اضافه کردن توکن JWT پیاده‌سازی شود [verify_method=static] [verify_plan={"grep_patterns": ["interceptor", "request", "Authorization", "Bearer"], "files_hint": ["frontend/src/lib/api.ts"]}]
```

### Step 14: پیاده‌سازی interceptor برای مدیریت خطاهای 401
**Status:** `done` (100%)
**Scope:** اضافه کردن یک response interceptor به axios instance که خطاهای 401 را مدیریت کند (مانند هدایت به صفحه ورود). خارج از این مرحله: ایجاد فایل api.ts، تنظیم baseURL، اضافه کردن توکن JWT.
**Excerpt:**
```
- interceptor برای مدیریت خطاهای 401 پیاده‌سازی شود [verify_method=static] [verify_plan={"grep_patterns": ["interceptor", "response", "401"], "files_hint": ["frontend/src/lib/api.ts"]}]
```

### Step 15: ایجاد AuthContext برای مدیریت وضعیت کاربر
**Status:** `done` (100%)
**Scope:** ایجاد فایل frontend/src/context/AuthContext.tsx با React Context که وضعیت کاربر (user, token, isAuthenticated) را مدیریت کند و توابع login و logout را ارائه دهد. خارج از این مرحله: ایجاد ProjectContext و TaskContext، یکپارچه‌سازی Provider.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
مدیریت وضعیت (state management) پیاده‌سازی نشده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/context/AuthContext.tsx:1-50` — `AuthContext` — نیاز به ایجاد
  ```tsx
  // فایل وجود ندارد
  ```
```

### Step 16: ایجاد ProjectContext برای مدیریت لیست پروژه‌ها
**Status:** `done` (100%)
**Scope:** ایجاد فایل frontend/src/context/ProjectContext.tsx با React Context که لیست پروژه‌ها را مدیریت کند و توابع به‌روزرسانی را ارائه دهد. خارج از این مرحله: ایجاد AuthContext و TaskContext، یکپارچه‌سازی Provider.
**Excerpt:**
```
- ProjectContext لیست پروژه‌ها را مدیریت کند [verify_method=static] [verify_plan={"grep_patterns": ["createContext", "useState", "projects"], "files_hint": ["frontend/src/context/ProjectContext.tsx"]}]
```

### Step 17: ایجاد TaskContext برای مدیریت لیست وظایف
**Status:** `done` (100%)
**Scope:** ایجاد فایل frontend/src/context/TaskContext.tsx با React Context که لیست وظایف را مدیریت کند و توابع به‌روزرسانی را ارائه دهد. خارج از این مرحله: ایجاد AuthContext و ProjectContext، یکپارچه‌سازی Provider.
**Excerpt:**
```
- TaskContext لیست وظایف را مدیریت کند [verify_method=static] [verify_plan={"grep_patterns": ["createContext", "useState", "tasks"], "files_hint": ["frontend/src/context/TaskContext.tsx"]}]
```

### Step 18: ارائه توابع به‌روزرسانی وضعیت در AuthContext
**Status:** `done` (100%)
**Scope:** اطمینان از اینکه AuthContext توابع لازم برای به‌روزرسانی وضعیت (مانند login, logout, updateUser) را ارائه می‌دهد. خارج از این مرحله: ایجاد ProjectContext و TaskContext.
**Excerpt:**
```
- هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد [verify_method=static] [verify_plan={"grep_patterns": ["function.*update", "function.*set", "dispatch"], "files_hint": ["frontend/src/context/AuthContext.tsx", "frontend/src/context/ProjectContext.tsx", "frontend/src/context/TaskContext]
```

### Step 19: ارائه توابع به‌روزرسانی وضعیت در ProjectContext
**Status:** `done` (100%)
**Scope:** اطمینان از اینکه ProjectContext توابع لازم برای به‌روزرسانی وضعیت (مانند addProject, updateProject, deleteProject) را ارائه می‌دهد. خارج از این مرحله: ایجاد AuthContext و TaskContext.
**Excerpt:**
```
- هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد [verify_method=static] [verify_plan={"grep_patterns": ["function.*update", "function.*set", "dispatch"], "files_hint": ["frontend/src/context/AuthContext.tsx", "frontend/src/context/ProjectContext.tsx", "frontend/src/context/TaskContext]
```

### Step 20: ارائه توابع به‌روزرسانی وضعیت در TaskContext
**Status:** `done` (100%)
**Scope:** اطمینان از اینکه TaskContext توابع لازم برای به‌روزرسانی وضعیت (مانند addTask, updateTask, deleteTask) را ارائه می‌دهد. خارج از این مرحله: ایجاد AuthContext و ProjectContext.
**Excerpt:**
```
- هر context توابع لازم برای به‌روزرسانی وضعیت را ارائه دهد [verify_method=static] [verify_plan={"grep_patterns": ["function.*update", "function.*set", "dispatch"], "files_hint": ["frontend/src/context/AuthContext.tsx", "frontend/src/context/ProjectContext.tsx", "frontend/src/context/TaskContext]
```

### Step 21: یکپارچه‌سازی AuthProvider در بالاترین سطح اپلیکیشن
**Status:** `done` (100%)
**Scope:** قرار دادن AuthProvider در بالاترین سطح اپلیکیشن (مانند App.tsx یا _app.tsx) تا تمام کامپوننت‌ها به وضعیت احراز هویت دسترسی داشته باشند. خارج از این مرحله: یکپارچه‌سازی ProjectProvider و TaskProvider.
**Excerpt:**
```
🔧 مراحل remaining که در super-task باید انجام شوند:
  - یکپارچه‌سازی Context‌ها و ارائه Provider در بالاترین سطح اپلیکیشن — ProjectProvider و TaskProvider در App.jsx قرار داده نشده‌اند
```

### Step 22: یکپارچه‌سازی ProjectProvider در بالاترین سطح اپلیکیشن
**Status:** `done` (100%)
**Scope:** قرار دادن ProjectProvider در بالاترین سطح اپلیکیشن (مانند App.tsx یا _app.tsx) تا تمام کامپوننت‌ها به وضعیت پروژه‌ها دسترسی داشته باشند. خارج از این مرحله: یکپارچه‌سازی AuthProvider و TaskProvider.
**Excerpt:**
```
🔧 مراحل remaining که در super-task باید انجام شوند:
  - یکپارچه‌سازی Context‌ها و ارائه Provider در بالاترین سطح اپلیکیشن — ProjectProvider و TaskProvider در App.jsx قرار داده نشده‌اند
```

### Step 23: یکپارچه‌سازی TaskProvider در بالاترین سطح اپلیکیشن
**Status:** `done` (100%)
**Scope:** قرار دادن TaskProvider در بالاترین سطح اپلیکیشن (مانند App.tsx یا _app.tsx) تا تمام کامپوننت‌ها به وضعیت وظایف دسترسی داشته باشند. خارج از این مرحله: یکپارچه‌سازی AuthProvider و ProjectProvider.
**Excerpt:**
```
🔧 مراحل remaining که در super-task باید انجام شوند:
  - یکپارچه‌سازی Context‌ها و ارائه Provider در بالاترین سطح اپلیکیشن — ProjectProvider و TaskProvider در App.jsx قرار داده نشده‌اند
```

### Step 24: اجرای تست‌های واحد برای کامپوننت‌های اشتراکی
**Status:** `done` (100%)
**Scope:** نوشتن و اجرای تست‌های واحد برای کامپوننت‌های Header, Sidebar, Footer با استفاده از Jest و React Testing Library. خارج از این مرحله: تست‌های یکپارچه‌سازی، تست‌های E2E.
**Excerpt:**
```
- هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
```

### Step 25: اجرای تست‌های واحد برای صفحه اصلی
**Status:** `done` (100%)
**Scope:** نوشتن و اجرای تست‌های واحد برای صفحه اصلی (index.tsx) با استفاده از Jest و React Testing Library. خارج از این مرحله: تست‌های یکپارچه‌سازی، تست‌های E2E.
**Excerpt:**
```
- هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
```

### Step 26: اجرای تست‌های واحد برای سرویس API
**Status:** `done` (100%)
**Scope:** نوشتن و اجرای تست‌های واحد برای سرویس API (api.ts) با استفاده از Jest و Mock Axios. خارج از این مرحله: تست‌های یکپارچه‌سازی، تست‌های E2E.
**Excerpt:**
```
- هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
```

### Step 27: اجرای تست‌های واحد برای Context‌ها
**Status:** `done` (100%)
**Scope:** نوشتن و اجرای تست‌های واحد برای AuthContext, ProjectContext, TaskContext با استفاده از Jest و React Testing Library. خارج از این مرحله: تست‌های یکپارچه‌سازی، تست‌های E2E.
**Excerpt:**
```
- هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
```

### Step 28: بررسی عبور linter و type-check
**Status:** `done` (100%)
**Scope:** اجرای linter (مانند ESLint) و type-check (tsc --noEmit) برای اطمینان از عدم وجود warning و خطا. خارج از این مرحله: اجرای تست‌ها.
**Excerpt:**
```
- linter بدون warning عبور می‌کند
- type-check موفق است (`tsc --noEmit` / `mypy`)
```
