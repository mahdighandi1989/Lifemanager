---
task_id: d2146781-09cc-47a1-aeda-a737d477b759
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (11 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-26T20:47:05.225531+00:00'
updated_at: '2026-05-28T19:45:32.183530+00:00'
target_files:
- backend/app/models/project.py
- backend/app/services/oversight_service.py
- backend/app/api/routes/projects.py
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (11 مرحله)

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_197353_AgADoh0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 5,254 char متن_

## درخواست اصلی کاربر  _(at: audio/ogg)_

[00:00] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و بتونه دسترسی لازمو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه همه رو بتونه مدیریت کنه. البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا سبک مدیریت و نوعشو و میزان دخالت این چیزاش مشخص بشه.
[00:31] حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، این خودش یه پروژه است که سایر پروژه‌ها داخلش لود کردم. اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت دسترسی‌های لازمو داشته باشه تا تو اون حیطه ای که براش هست مدیریت داشته باشه. مثلاً یک یکی از حیطه هایی که هست اینه که مثلاً بتونه زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها تنظیم بکنه همه چیزو.
[01:09] و با وقت هم بسنجه چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری. اینجا مثلاً فلان مشکل هست. خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## هدف از ایجاد پرامپت  _(at: audio/ogg)_

### هدف از ایجاد پرامپت

[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه، متصل باشه و بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه، همه رو بتونه مدیریت کنه. البته حالا این به صورت کلیه. یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه.
[00:31] حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، خودش یه پروژه است که سایر پروژه‌ها داخلش لود کردم. اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت دسترسی لازم رو داشته باشه تا تو اون حیطه ای که براش هست مدیریت داشته باشه. مثلاً یکی از حیطه هایی که هست اینه که مثلاً بتونه زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها تنظیم بکنه همه چیزو.
[01:09] و با وقت هم بسنجه. چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری. اینجا مثلاً فلان مشکل هست. خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## محتوای مبنا برای تحلیل  _(at: audio/ogg)_

محتوای مبنا برای تحلیل:
[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و [00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه. [00:31] حالا یه پروژه‌ای هست مثلاً خودش پراجکت منجمنت، خودش یه پروژه است که سایر پروژه‌ها داخلش [00:37] لود کردم. [00:40] اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی [00:45] باید خود اینم به سایر پروژه‌ها از جمله اون پراجکت منجمنت [00:49] دسترسی لازم رو داشته باشه تا [00:52] تو اون حیطه ای که براش هست [00:55] مدیریت داشته باشه. مثلاً یک [00:58] یکی از حیطه هایی که هست اینه که مثلاً بتونه [01:01] زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها [01:06] تنظیم بکنه همه چیزو. [01:09] و [01:11] با وقت هم بسنجه [01:13] چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری [01:18] اینجا مثلاً فلان مشکل هست. [01:20] خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## ویژگی‌های پرامپت مطلوب  _(at: audio/ogg)_

این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه همه رو بتونه مدیریت کنه. البته حالا این به صورت کلیه یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه. حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، خودش پروژه‌ست که سایر پروژه‌ها داخلش لود کردم. اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت دسترسی لازم رو داشته باشه تا تو اون حیطه ای که براش هست مدیریت داشته باشه. مثلاً یکی از حیطه هایی که هست اینه که مثلاً بتونه زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها تنظیم بکنه همه چیزو. و با وقت هم بسنجه چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری اینجا مثلاً فلان مشکل هست. خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## مخاطب نهایی پرامپت  _(at: audio/ogg)_

[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و [00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه. البته حالا این به صورت کلیه یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص [00:30] بشه. [00:31] حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، خودش یه پروژه است که سایر پروژه‌ها داخلش [00:37] لود کردم. [00:39] اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی [00:45] باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت [00:49] دسترسی لازم رو داشته باشه تا [00:52] تو اون حیطه ای که براش هست [00:55] مدیریت داشته باشه. مثلاً یک [00:58] یکی از حیطه هایی که هست اینه که مثلاً بتونه [01:01] زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها [01:05] تنظیم بکنه همه چیزو. [01:09] و [01:10] با وقت هم بسنجه [01:12] چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری [01:17] اینجا مثلاً فلان مشکل هست. [01:20] خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند. بخش‌های بعدی توسط AI ساختار داده شده‌اند و ممکن است ناقص باشند — این متن مرجع اصلی است.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_197353_AgADoh0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 5,254 char متن_

## درخواست اصلی کاربر  _(at: audio/ogg)_

[00:00] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و بتونه دسترسی لازمو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه همه رو بتونه مدیریت کنه. البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا سبک مدیریت و نوعشو و میزان دخالت این چیزاش مشخص بشه.
[00:31] حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، این خودش یه پروژه است که سایر پروژه‌ها داخلش لود کردم. اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت دسترسی‌های لازمو داشته باشه تا تو اون حیطه ای که براش هست مدیریت داشته باشه. مثلاً یک یکی از حیطه هایی که هست اینه که مثلاً بتونه زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها تنظیم بکنه همه چیزو.
[01:09] و با وقت هم بسنجه چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری. اینجا مثلاً فلان مشکل هست. خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## هدف از ایجاد پرامپت  _(at: audio/ogg)_

### هدف از ایجاد پرامپت

[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه، متصل باشه و بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه، همه رو بتونه مدیریت کنه. البته حالا این به صورت کلیه. یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه.
[00:31] حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، خودش یه پروژه است که سایر پروژه‌ها داخلش لود کردم. اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت دسترسی لازم رو داشته باشه تا تو اون حیطه ای که براش هست مدیریت داشته باشه. مثلاً یکی از حیطه هایی که هست اینه که مثلاً بتونه زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها تنظیم بکنه همه چیزو.
[01:09] و با وقت هم بسنجه. چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری. اینجا مثلاً فلان مشکل هست. خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## محتوای مبنا برای تحلیل  _(at: audio/ogg)_

محتوای مبنا برای تحلیل:
[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و [00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه. [00:31] حالا یه پروژه‌ای هست مثلاً خودش پراجکت منجمنت، خودش یه پروژه است که سایر پروژه‌ها داخلش [00:37] لود کردم. [00:40] اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی [00:45] باید خود اینم به سایر پروژه‌ها از جمله اون پراجکت منجمنت [00:49] دسترسی لازم رو داشته باشه تا [00:52] تو اون حیطه ای که براش هست [00:55] مدیریت داشته باشه. مثلاً یک [00:58] یکی از حیطه هایی که هست اینه که مثلاً بتونه [01:01] زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها [01:06] تنظیم بکنه همه چیزو. [01:09] و [01:11] با وقت هم بسنجه [01:13] چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری [01:18] اینجا مثلاً فلان مشکل هست. [01:20] خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## ویژگی‌های پرامپت مطلوب  _(at: audio/ogg)_

این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه همه رو بتونه مدیریت کنه. البته حالا این به صورت کلیه یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه. حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، خودش پروژه‌ست که سایر پروژه‌ها داخلش لود کردم. اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت دسترسی لازم رو داشته باشه تا تو اون حیطه ای که براش هست مدیریت داشته باشه. مثلاً یکی از حیطه هایی که هست اینه که مثلاً بتونه زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها تنظیم بکنه همه چیزو. و با وقت هم بسنجه چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری اینجا مثلاً فلان مشکل هست. خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.

## مخاطب نهایی پرامپت  _(at: audio/ogg)_

[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و [00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه. البته حالا این به صورت کلیه یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص [00:30] بشه. [00:31] حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، خودش یه پروژه است که سایر پروژه‌ها داخلش [00:37] لود کردم. [00:39] اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی [00:45] باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت [00:49] دسترسی لازم رو داشته باشه تا [00:52] تو اون حیطه ای که براش هست [00:55] مدیریت داشته باشه. مثلاً یک [00:58] یکی از حیطه هایی که هست اینه که مثلاً بتونه [01:01] زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها [01:05] تنظیم بکنه همه چیزو. [01:09] و [01:10] با وقت هم بسنجه [01:12] چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری [01:17] اینجا مثلاً فلان مشکل هست. [01:20] خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی زیرساخت اتصال و مدیریت بین‌پروژه‌ای با قابلیت زمان‌بندی و نظارت

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/models/project.py:1-100` — `Project` — مدل فعلی Project که مدل جدید ExternalProject باید در کنار آن یا به صورت جداگانه تعریف شود.
  ```python
  class Project(Base):
      __tablename__ = "projects"
      id = Column(Integer, primary_key=True, index=True)
      title = Column(String, index=True)
      description = Column(Text, nullable=True)
      user_id = Column(Integer, ForeignKey("users.id"))
      created_at = Column(DateTime, default=datetime.utcnow)
      updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
  ```
- `backend/app/services/oversight_service.py:1-50` — `OversightService` — سرویس نظارت فعلی که می‌تواند با قابلیت‌های بین‌پروژه‌ای گسترش یابد.
  ```python
  class OversightService:
      def __init__(self, db: Session):
          self.db = db
      
      def check_task_overdue(self, task_id: int) -> bool:
          task = self.db.query(Task).filter(Task.id == task_id).first()
          if not task:
              return False
          return task.due_date and task.due_date < datetime.utcnow()
  ```
- `backend/app/api/routes/projects.py:1-30` — `router` — مسیر API فعلی برای پروژه‌ها. مسیر جدید `/external-projects` باید مشابه این ساختار ایجاد شود.
  ```python
  router = APIRouter(prefix="/projects", tags=["projects"])
  
  @router.get("/", response_model=List[ProjectResponse])
  def list_projects(db: Session = Depends(get_db), current_user: User = Depends(get_current_user)):
      projects = project_service.get_user_projects(db, current_user.id)
      return projects
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
FastAPI (Python 3.10+), SQLAlchemy, Celery, React (TypeScript), Vite, Alembic برای migrations.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/schemas/project.py` (سطر 1) — برای ایجاد Schemaهای ExternalProject از ساختار این فایل الگو گرفته می‌شود.
- `backend/app/db/base.py` (سطر 1) — برای ثبت مدل جدید ExternalProject در Base metadata.
- `backend/app/services/project_service.py` (سطر 1) — برای الگوبرداری از الگوی سرویس‌دهی و مدیریت پروژه‌های داخلی.
- `backend/app/core/security.py` (سطر 1) — برای رمزگذاری API Key پروژه‌های خارجی.
- `frontend/src/App.tsx` (سطر 1) — برای افزودن مسیر جدید `/external-projects` به روتر.
- `backend/app/worker/celery_app.py` (سطر 1) — برای ثبت Task زمان‌بندی شده برای همگام‌سازی و تحلیل.

## 🌐 نقشهٔ وابستگی‌ها
این قابلیت به شدت به مدل‌های دیتابیس (Project, ExternalProject) وابسته است. سرویس OversightService باید گسترش یابد تا داده‌های پروژه‌های خارجی را نیز تحلیل کند. APIهای جدید به سرویس ExternalProjectService وابسته هستند. در frontend، کامپوننت‌های جدید به مسیر `/external-projects` وابسته هستند. همچنین نیاز به مدیریت API Keyها (رمزگذاری/رمزگشایی) در `backend/app/core/security.py` وجود دارد. Taskهای Celery برای همگام‌سازی خودکار به `backend/app/worker/celery_app.py` و `backend/app/worker/tasks.py` وابسته هستند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی زیرساختی برای اتصال این پروژه (Lifemanager) به سایر پروژه‌های خارجی و مدیریت آن‌ها را دارد. این درخواست از فایل صوتی `voice_197353_AgADoh0A.ogg` استخراج شده است. کاربر توضیح می‌دهد که این پروژه باید بتواند به سایر پروژه‌ها متصل شود و دسترسی لازم را داشته باشد تا بتواند آن‌ها را مدیریت کند. او به یک پروژه خاص به نام 'پروجکت منجمنت' اشاره می‌کند که خود یک پروژه است و سایر پروژه‌ها داخل آن لود شده‌اند. کاربر می‌گوید: 'این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و بتونه دسترسی لازمو داشته باشه بهشون'. او همچنین به نیاز به زمان‌بندی و بررسی زمان اختصاص داده شده به هر پروژه اشاره می‌کند: 'بتونه زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها تنظیم بکنه همه چیزو. و با وقت هم بسنجه چیزایی که مثلاً ازش مغفول مونده رو بگه'. در نهایت، کاربر تأکید می‌کند که ابتدا زیرساخت فراهم شود تا 'سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه'. این یک درخواست سطح بالا برای طراحی معماری و پیاده‌سازی اولیه است. با توجه به deep context، پروژه از FastAPI و React استفاده می‌کند و دارای مدل‌های Task، Project، Notification و ... است. فایل‌های مرتبط شامل `backend/app/models/project.py`، `backend/app/services/project_service.py`، `backend/app/api/routes/projects.py` و `backend/app/schemas/project.py` هستند. همچنین فایل `backend/app/services/oversight_service.py` برای مدیریت نظارت وجود دارد که می‌تواند پایه‌ای برای این قابلیت باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل `ExternalProject` در فایل `backend/app/models/project.py` با فیلدهای `id`, `name`, `base_url`, `api_key_encrypted`, `project_type`, `is_active`, `last_sync_at`, `sync_interval_minutes`, `created_at`, `user_id` ایجاد شود.
- [ ] API endpoint `POST /api/external-projects/register` یک پروژه خارجی جدید ثبت کند و در صورت موفقیت status 201 و شیء پروژه را برگرداند.
- [ ] API endpoint `GET /api/external-projects/` لیست پروژه‌های خارجی کاربر جاری را برگرداند.
- [ ] سرویس `ExternalProjectService` دارای متد `sync_project_data()` باشد که بتواند داده‌ها را از یک پروژه خارجی نمونه (mock) دریافت کند.
- [ ] در `OversightService` تابع `analyze_time_allocation()` اضافه شود که زمان اختصاص داده شده به پروژه‌های خارجی را تحلیل کند.
- [ ] صفحه `/external-projects` در frontend قابل دسترسی باشد و لیست پروژه‌های خارجی را نمایش دهد.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. **ایجاد مدل `ExternalProject`**: در فایل `backend/app/models/project.py` یک مدل جدید به نام `ExternalProject` ایجاد شود که شامل فیلدهای `id`، `name`، `base_url`، `api_key` (رمزگذاری شده)، `project_type` (مثلاً 'project_management'، 'code_repo'، 'time_tracker')، `is_active`، `last_sync_at`، `sync_interval_minutes` و `created_at` باشد. این مدل نمایانگر یک پروژه خارجی است که این سیستم به آن متصل می‌شود.

2. **ایجاد سرویس `ExternalProjectService`**: در فایل `backend/app/services/external_project_service.py` یک سرویس جدید ایجاد شود که وظایف زیر را انجام دهد:
   - `register_project()`: ثبت یک پروژه خارجی جدید با اعتبارسنجی اتصال.
   - `get_project_status()`: بررسی وضعیت و سلامت اتصال به پروژه خارجی.
   - `sync_project_data()`: همگام‌سازی داده‌ها از پروژه خارجی (مانند تسک‌ها، زمان‌ها).
   - `list_projects()`: لیست پروژه‌های خارجی ثبت‌شده.

3. **ایجاد API Endpoints**: در فایل `backend/app/api/routes/external_projects.py`:
   - `POST /api/external-projects/register`: ثبت پروژه خارجی جدید.
   - `GET /api/external-projects/`: لیست پروژه‌های خارجی.
   - `GET /api/external-projects/{id}/status`: وضعیت اتصال.
   - `POST /api/external-projects/{id}/sync`: همگام‌سازی دستی.

4. **طراحی مکانیزم زمان‌بندی و نظارت**: در فایل `backend/app/services/oversight_service.py`:
   - افزودن تابع `analyze_time_allocation()` که زمان اختصاص داده شده به هر پروژه خارجی را بررسی کند.
   - افزودن تابع `detect_neglected_tasks()` که تسک‌های مغفول‌مانده را شناسایی کند.
   - ایجاد یک Task Celery زمان‌بندی شده برای اجرای دوره‌ای این تحلیلها.

5. **ایجاد Schemaهای مربوطه**: در فایل `backend/app/schemas/external_project.py`:
   - `ExternalProjectCreate`: برای ثبت پروژه جدید.
   - `ExternalProjectResponse`: برای پاسخ API.
   - `ExternalProjectStatus`: برای وضعیت اتصال.

6. **ایپدیت Frontend**: در فایل `frontend/src/`:
   - افزودن صفحه `ExternalProjectsPage.tsx` برای مدیریت پروژه‌های خارجی.
   - افزودن کامپوننت `ExternalProjectCard.tsx` برای نمایش هر پروژه.
   - افزودن مسیر `/external-projects` در `App.tsx`.

7. **پیاده‌سازی اتصال به 'Project Management' خاص**: به عنوان یک مورد خاص، یک آداپتور برای اتصال به پروژه مدیریت پروژه (که کاربر به آن اشاره کرده) ایجاد شود. این آداپتور باید بتواند تسک‌ها، زمان‌ها و وضعیت پروژه‌های داخل آن را بخواند.

## 💡 نمونه‌های قبل/بعد
**افزودن مدل ExternalProject به models/project.py**

_قبل:_
```
# backend/app/models/project.py
class Project(Base):
    __tablename__ = "projects"
    id = Column(Integer, primary_key=True, index=True)
    title = Column(String, index=True)
    description = Column(Text, nullable=True)
    user_id = Column(Integer, ForeignKey("users.id"))
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

_بعد:_
```
# backend/app/models/project.py
class Project(Base):
    __tablename__ = "projects"
    id = Column(Integer, primary_key=True, index=True)
    title = Column(String, index=True)
    description = Column(Text, nullable=True)
    user_id = Column(Integer, ForeignKey("users.id"))
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

class ExternalProject(Base):
    __tablename__ = "external_projects"
    id = Column(Integer, primary_key=True, index=True)
    name = Column(String, index=True)
    base_url = Column(String)
    api_key_encrypted = Column(String)
    project_type = Column(String)  # e.g., 'project_management', 'code_repo'
    is_active = Column(Boolean, default=True)
    last_sync_at = Column(DateTime, nullable=True)
    sync_interval_minutes = Column(Integer, default=60)
    created_at = Column(DateTime, default=datetime.utcnow)
    user_id = Column(Integer, ForeignKey("users.id"))
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/services/test_external_project_service.py -v`
- `pytest backend/tests/api/test_external_projects.py -v`
- `cd frontend && npm run test -- --testPathPattern=ExternalProjects`
- `alembic upgrade head && alembic check`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک امنیتی: ذخیره‌سازی API Key پروژه‌های خارجی نیازمند رمزگذاری قوی است. اگر `backend/app/core/security.py` به درستی پیکربندی نشود، کلیدها در معرض خطر قرار می‌گیرند. ریسک عملکردی: همگام‌سازی همزمان با چندین پروژه خارجی می‌تواند باعث کاهش performance شود. باید از Celery برای async processing استفاده شود. ریسک وابستگی: تغییر در مدل `Project` (اضافه کردن `ExternalProject`) نیازمند migration دیتابیس است و ممکن است با داده‌های موجود تداخل ایجاد کند. ریسک یکپارچگی: اتصال به پروژه مدیریت پروژه (که کاربر اشاره کرده) نیازمند یک API استاندارد یا آداپتور خاص است که ممکن است در دسترس نباشد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

## Acceptance Criteria

1. مدل `ExternalProject` در فایل `backend/app/models/project.py` با فیلدهای `id`, `name`, `base_url`, `api_key_encrypted`, `project_type`, `is_active`, `last_sync_at`, `sync_interval_minutes`, `created_at`, `user_id` ایجاد شود. _(verify: static)_
2. API endpoint `POST /api/external-projects/register` یک پروژه خارجی جدید ثبت کند و در صورت موفقیت status 201 و شیء پروژه را برگرداند. _(verify: api_response)_
3. API endpoint `GET /api/external-projects/` لیست پروژه‌های خارجی کاربر جاری را برگرداند. _(verify: api_response)_
4. سرویس `ExternalProjectService` دارای متد `sync_project_data()` باشد که بتواند داده‌ها را از یک پروژه خارجی نمونه (mock) دریافت کند. _(verify: backend_test)_
5. در `OversightService` تابع `analyze_time_allocation()` اضافه شود که زمان اختصاص داده شده به پروژه‌های خارجی را تحلیل کند. _(verify: static)_
6. صفحه `/external-projects` در frontend قابل دسترسی باشد و لیست پروژه‌های خارجی را نمایش دهد. _(verify: ui_interaction)_

## Task Steps

### Step 1: طراحی و تعریف رابط اتصال به پروژه‌های خارجی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی یک رابط (interface) یا قرارداد (contract) استاندارد برای نحوه اتصال و تعامل با انواع پروژه‌های خارجی است. هدف این است که یک چارچوب یکپارچه برای ارتباط با سیستم‌های مختلف فراهم شود، بدون اینکه جزئیات پیاده‌سازی هر سیستم در این مرحله در نظر گرفته شود. این رابط باید متدهایی برای احراز هویت، دریافت لیست پروژه‌ها، و دسترسی اولیه به اطلاعات پروژه تعریف کند. این مرحله صرفاً به تعریف و طراحی می‌پردازد و شامل پیاده‌سازی واقعی اتصال نیست.
**Excerpt:**
```
[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و [00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه.
```

### Step 2: پیاده‌سازی ماژول عمومی اتصال به پروژه‌های خارجی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی اولیه ماژول یا سرویسی است که از رابط تعریف شده در مرحله قبل استفاده می‌کند تا بتواند به صورت عمومی به انواع پروژه‌های خارجی متصل شود. این ماژول باید قابلیت مدیریت چندین اتصال به پروژه‌های مختلف را داشته باشد و بتواند اطلاعات پایه پروژه‌ها را از طریق این اتصالات دریافت کند. تمرکز بر روی ایجاد یک مکانیزم اتصال پایدار و قابل اعتماد است.
**Excerpt:**
```
[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و [00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه.
```

### Step 3: توسعه سیستم کنترل دسترسی (RBAC) برای پروژه‌های خارجی
**Status:** `pending` (0%)
**Scope:** این مرحله بر روی پیاده‌سازی یک سیستم کنترل دسترسی مبتنی بر نقش (Role-Based Access Control - RBAC) متمرکز است که تعیین می‌کند پروژه فعلی چه سطحی از دسترسی به هر پروژه خارجی متصل شده دارد. این سیستم باید قابلیت تعریف نقش‌ها، اختصاص مجوزها به نقش‌ها و اعمال این مجوزها در زمان تلاش برای دسترسی به منابع پروژه‌های خارجی را داشته باشد. این امر برای اطمینان از 'دسترسی لازم' و جلوگیری از 'دخالت بیش از حد' حیاتی است.
**Excerpt:**
```
[00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه. [00:45] باید خود اینم به سایر پروژه‌ها از جمله اون پراجکت منجمنت [00:49] دسترسی لازم رو داشته باشه تا [00:52] تو اون حیطه ای که براش هست [00:55] مدیریت داشته باشه.
```

### Step 4: یکپارچه‌سازی اختصاصی با سیستم 'پروجکت منجمنت' موجود
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک ماژول اتصال و یکپارچه‌سازی خاص برای سیستم 'پروجکت منجمنت' است که کاربر به آن اشاره کرده است. با توجه به اینکه این سیستم خودش پروژه‌های دیگر را 'لود' کرده و یک موجودیت خاص است، نیاز به یکپارچه‌سازی اختصاصی دارد که ممکن است شامل نگاشت داده‌ها و فراخوانی‌های API خاص آن سیستم باشد. این ماژول باید از رابط عمومی اتصال به پروژه‌های خارجی (مرحله ۱) استفاده کند اما جزئیات پیاده‌سازی آن مختص 'پروجکت منجمنت' باشد.
**Excerpt:**
```
[00:31] حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، خودش یه پروژه است که سایر پروژه‌ها داخلش [00:37] لود کردم. [00:40] اون از یه طرف مثلاً مدیریت می‌کنه پروژه‌ها رو ولی به صورت کلی از یه جهاتی [00:45] باید خود اینم به سایر پروژه‌ها از جمله اون پروجکت منجمنت [00:49] دسترسی لازم رو داشته باشه تا [00:52] تو اون حیطه ای که براش هست [00:55] مدیریت داشته باشه.
```

### Step 5: پیاده‌سازی قابلیت کشف و لیست کردن پروژه‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه منطقی است که پروژه‌های متصل شده (چه از طریق ماژول عمومی و چه از طریق یکپارچه‌سازی 'پروجکت منجمنت') را کشف کرده و یک لیست یکپارچه از آن‌ها را برای نمایش و مدیریت فراهم می‌کند. این لیست باید شامل اطلاعات پایه هر پروژه مانند نام، شناسه و وضعیت اتصال باشد. این قابلیت یک بخش اساسی از 'زیرساخت' برای هرگونه مدیریت بعدی است.
**Excerpt:**
```
[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و [00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه. [00:31] حالا یه پروژه‌ای هست مثلاً خودش پروجکت منجمنت، خودش یه پروژه است که سایر پروژه‌ها داخلش [00:37] لود کردم.
```

### Step 6: پیاده‌سازی قابلیت تخصیص زمان به پروژه‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه منطقی است که به کاربر اجازه می‌دهد تا 'زمانی که باید برای هر کدومشون بذاره' را برای هر پروژه مدیریت شده تعیین و ذخیره کند. این قابلیت باید شامل ذخیره‌سازی زمان تخصیص یافته (مثلاً به صورت ساعت یا روز) برای هر پروژه باشد. این زمان می‌تواند یک مقدار کلی یا بر اساس فازهای پروژه باشد، اما در این مرحله، تمرکز بر روی یک مقدار کلی است.
**Excerpt:**
```
[00:58] یکی از حیطه هایی که هست اینه که مثلاً بتونه [01:01] زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها [01:06] تنظیم بکنه همه چیزو.
```

### Step 7: پیاده‌سازی مکانیزم بررسی و ردیابی وضعیت پروژه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه یک مکانیزم داخلی برای 'بررسیشون بکنه' است. این مکانیزم می‌تواند شامل قابلیت افزودن یادداشت‌های بررسی، تغییر وضعیت پروژه (مثلاً 'در حال بررسی'، 'تکمیل شده'، 'نیاز به توجه') یا ثبت تاریخچه بررسی‌ها باشد. هدف این است که یک راهکار ساده برای ردیابی وضعیت نظارتی هر پروژه فراهم شود.
**Excerpt:**
```
[00:58] یکی از حیطه هایی که هست اینه که مثلاً بتونه [01:01] زمانی که باید برای هر کدومشون بذاره و بررسیشون بکنه و اینها [01:06] تنظیم بکنه همه چیزو.
```

### Step 8: توسعه منطق مقایسه زمان تخصیص یافته با زمان واقعی
**Status:** `pending` (0%)
**Scope:** این مرحله بر روی پیاده‌سازی منطقی متمرکز است که می‌تواند 'با وقت هم بسنجه'. این منطق نیاز به دسترسی به زمان تخصیص یافته (از مرحله ۶) و همچنین یک منبع فرضی برای 'زمان واقعی صرف شده' دارد (که فرض می‌شود از سیستم‌های خارجی یا ورودی دستی تامین می‌شود). هدف این است که تفاوت بین این دو مقدار را محاسبه کرده و یک شاخص برای انحراف زمانی ارائه دهد.
**Excerpt:**
```
[01:09] و [01:11] با وقت هم بسنجه [01:13] چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری [01:18] اینجا مثلاً فلان مشکل هست. [01:20] خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.
```

### Step 9: پیاده‌سازی منطق شناسایی موارد 'مغفول مانده'
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه منطقی است که می‌تواند 'چیزایی که مثلاً ازش مغفول مونده رو بگه'. این منطق ممکن است بر اساس معیارهایی مانند عدم به‌روزرسانی طولانی مدت، عدم تخصیص زمان، یا عدم بررسی در یک دوره مشخص عمل کند. هدف این است که سیستم بتواند به صورت خودکار مواردی را که نیاز به توجه دارند، شناسایی و برجسته کند.
**Excerpt:**
```
[01:09] و [01:11] با وقت هم بسنجه [01:13] چیزایی که مثلاً ازش مغفول مونده رو بگه مثلاً باید برای این اینقدر وقت بذاری [01:18] اینجا مثلاً فلان مشکل هست. [01:20] خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.
```

### Step 10: پیاده‌سازی منطق شناسایی 'مشکلات' در پروژه‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه منطقی است که می‌تواند 'اینجا مثلاً فلان مشکل هست' را شناسایی کند. این منطق ممکن است بر اساس انحرافات بزرگ زمانی (از مرحله ۸)، وضعیت‌های خاص پروژه (از مرحله ۷)، یا سایر معیارهای تعریف شده برای 'اخلال' عمل کند. هدف این است که سیستم بتواند مشکلات بالقوه یا واقعی را در پروژه‌ها تشخیص داده و آن‌ها را برجسته کند.
**Excerpt:**
```
[00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه. [01:18] اینجا مثلاً فلان مشکل هست. [01:20] خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.
```

### Step 11: ایجاد داشبورد اولیه مدیریت و نمایش وضعیت پروژه‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل توسعه یک رابط کاربری (UI) اولیه یا یک endpoint تجمیع‌کننده است که اطلاعات کلیدی از پروژه‌های مدیریت شده را به صورت یکپارچه نمایش می‌دهد. این داشبورد باید شامل لیست پروژه‌ها (از مرحله ۵)، زمان تخصیص یافته (از مرحله ۶)، وضعیت بررسی (از مرحله ۷)، انحرافات زمانی (از مرحله ۸)، موارد مغفول مانده (از مرحله ۹) و مشکلات شناسایی شده (از مرحله ۱۰) باشد. هدف این است که یک نمای کلی و قابل مشاهده از قابلیت‌های مدیریتی فراهم شود.
**Excerpt:**
```
[00:01] این پروژه باید به سایر پروژه‌های دیگه جهت اینکه بتونه مدیریتشون بکنه متصل باشه و [00:11] بتونه دسترسی لازم رو داشته باشه بهشون تا اونجایی که لازمه مثلاً اخلالی ایجاد میشه فلان میشه [00:19] همه رو بتونه مدیریت کنه البته حالا این به صورت کلی یعنی می‌خوام زیرساختش فعلاً فراهم بشه تا [00:26] سبک مدیریت و نوعش و میزان دخالت این چیزاش مشخص بشه. [01:20] خیلی چیزا رو باید مثلاً مدیریت داشته باشه روش.
```
