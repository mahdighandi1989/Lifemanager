---
task_id: 3cc09436-d889-4cee-b22b-9af05c82a7b2
title: توسعه سیستم مدیریت پروفایل و تحلیل رفتاری
type: other
priority: medium
execution_priority: 3300
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-27T21:23:41.404279+00:00'
updated_at: '2026-05-28T17:41:48.993736+00:00'
target_files:
- frontend/src/pages/Dashboard.jsx
- frontend/src/components/Sidebar.jsx
- frontend/src/App.jsx
- app/models/person.py
- app/routes/person.py
- app/schemas/person_schema.py
- app/main.py
- app/services/person_service.py
- frontend/src/pages/PersonProfile.jsx
- app/models/todo_list.py
---

# توسعه سیستم مدیریت پروفایل و تحلیل رفتاری

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_793240_AgADfh4A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 6,961 char متن_

## هدف و چشم‌انداز پروژه  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## ویژگی‌ها و قابلیت‌های اصلی  _(at: audio/ogg)_

### ویژگی‌ها و قابلیت‌های اصلی

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن.
[00:09] اینجا مثلاً حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم حسب لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## مخاطب هدف و نیازها  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن. اینجا مثلاً عصب داده‌هایی که به ما به زمان برای کارای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که دربارمون دارن. این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه. که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً لزوماً نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## چالش‌ها و ریسک‌های اولیه  _(at: audio/ogg)_

### چالش‌ها و ریسک‌های اولیه

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مراحل بعدی و گام‌های اجرایی  _(at: audio/ogg)_

--- صفحه 1 ---
[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو موقع و زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مزیت رقابتی و تمایز  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اونجا مثلاً از روی داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا به مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## منابع و الزامات اولیه  _(at: audio/ogg)_

--- صفحه 1 ---
[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً عصب داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو موقع زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_793240_AgADfh4A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 6,961 char متن_

## هدف و چشم‌انداز پروژه  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## ویژگی‌ها و قابلیت‌های اصلی  _(at: audio/ogg)_

### ویژگی‌ها و قابلیت‌های اصلی

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن.
[00:09] اینجا مثلاً حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم حسب لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## مخاطب هدف و نیازها  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن. اینجا مثلاً عصب داده‌هایی که به ما به زمان برای کارای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که دربارمون دارن. این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه. که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً لزوماً نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## چالش‌ها و ریسک‌های اولیه  _(at: audio/ogg)_

### چالش‌ها و ریسک‌های اولیه

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مراحل بعدی و گام‌های اجرایی  _(at: audio/ogg)_

--- صفحه 1 ---
[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو موقع و زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مزیت رقابتی و تمایز  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اونجا مثلاً از روی داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا به مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## منابع و الزامات اولیه  _(at: audio/ogg)_

--- صفحه 1 ---
[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً عصب داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو موقع زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.
```

## 📋 چک‌لیست مراحل (10 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد صفحه پروفایل افراد (People Profile Page) در برنامه** — این مرحله شامل ایجاد یک صفحه جدید در برنامه با عنوان 'پروفایل افراد' است. این صفحه باید لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رویدادها یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام ص
- [ ] **مرحله 2: طراحی و پیاده‌سازی مدل داده برای پروفایل افراد** — این مرحله شامل طراحی و پیاده‌سازی مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات پروفایل افراد است. این مدل باید شامل فیلدهایی برای نام، شناسه، امتیاز، تاریخچه رویدادها، و نوع رابطه باشد. این مرحله فقط شامل ساختار داده است و شامل منطق تجاری یا UI نمی‌شود. نکته حیاتی: مدل باید قابلیت ذخیره‌سا
- [ ] **مرحله 3: پیاده‌سازی سیستم ثبت رویدادها (Event Logging) برای افراد** — این مرحله شامل پیاده‌سازی سیستمی برای ثبت رویدادهای مرتبط با هر فرد است. این رویدادها شامل کارهای خوب و بدی است که فرد انجام داده است. هر رویداد باید شامل جزئیات کامل (تاریخ، توضیحات، نوع رویداد) باشد. این مرحله فقط شامل ثبت رویدادها است و شامل آنالیز یا امتیازدهی نمی‌شود. نکته حیاتی: رویدادها باید 
- [ ] **مرحله 4: پیاده‌سازی سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد** — این مرحله شامل پیاده‌سازی یک سیستم هوش مصنوعی است که بر اساس داده‌های ثبت‌شده (رویدادها، لحن صحبت، نوع تعاملات) به هر فرد یک امتیاز می‌دهد. این امتیاز باید به صورت خودکار و با آنالیز داده‌های موجود محاسبه شود. این مرحله فقط شامل منطق امتیازدهی است و شامل UI یا ثبت رویدادها نمی‌شود. نکته حیاتی: امتیا
- [ ] **مرحله 5: پیاده‌سازی سیستم تنظیم نوع رابطه (Relationship Adjustment) بر اساس امتیاز** — این مرحله شامل پیاده‌سازی منطقی است که بر اساس امتیاز هوش مصنوعی و رویدادهای ثبت‌شده، نوع رابطه کاربر با هر فرد را تنظیم می‌کند. این تنظیم شامل مواردی مانند 'کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم' است. این مرحله فقط شامل منطق تنظیم رابطه است و شامل UI
- [ ] **مرحله 6: طراحی و پیاده‌سازی UI برای نمایش امتیاز و نوع رابطه در پروفایل افراد** — این مرحله شامل طراحی و پیاده‌سازی بخش UI در صفحه پروفایل افراد است که امتیاز هوش مصنوعی و نوع رابطه را نمایش می‌دهد. این بخش باید به صورت بصری و قابل فهم برای کاربر باشد. این مرحله فقط شامل UI است و شامل منطق امتیازدهی یا تنظیم رابطه نمی‌شود. نکته حیاتی: UI باید به کاربر کمک کند تا 'کجا بهم خوبی کرد
- [ ] **مرحله 7: پیاده‌سازی قابلیت ثبت نظر شخصی کاربر (Manual Review) برای هر فرد** — این مرحله شامل پیاده‌سازی قابلیتی است که به کاربر اجازه می‌دهد نظر شخصی خود را درباره هر فرد ثبت کند. این نظر می‌تواند بر اساس 'لحن من و نوعی که در نوع صحبتی که دربارمون دارن' باشد. این نظر باید در محاسبه امتیاز هوش مصنوعی نیز تأثیر بگذارد. این مرحله فقط شامل ثبت نظر است و شامل آنالیز هوش مصنوعی نمی
- [ ] **مرحله 8: پیاده‌سازی سیستم جلوگیری از فراموشی (Forgetfulness Prevention) با نمایش تاریخچه کامل** — این مرحله شامل پیاده‌سازی سیستمی است که تاریخچه کامل رویدادها و نظرات را در پروفایل هر فرد نمایش می‌دهد تا کاربر دچار فراموشی نشود. این سیستم باید به کاربر کمک کند تا 'با یه کار خوبش هزار تا کار بدی که کرده رو فراموش نکنم'. این مرحله فقط شامل نمایش تاریخچه است و شامل منطق امتیازدهی نمی‌شود. نکته حیا
- [ ] **مرحله 9: پیاده‌سازی قابلیت 'جبران و انتقام' (Compensation & Revenge) در پروفایل افراد** — این مرحله شامل پیاده‌سازی قابلیتی است که به کاربر اجازه می‌دهد برای کارهای خوب و بد افراد، اقدامات جبرانی یا انتقامی را ثبت و پیگیری کند. این قابلیت باید به کاربر کمک کند تا 'کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم'. این مرحله فقط شامل ثبت و پیگیری اقدامات است و شامل منطق امتیازدهی نمی
- [ ] **مرحله 10: تست و اعتبارسنجی یکپارچه سیستم پروفایل افراد** — این مرحله شامل تست و اعتبارسنجی کامل سیستم پروفایل افراد است. این تست شامل بررسی صحت ثبت رویدادها، محاسبه امتیاز هوش مصنوعی، تنظیم نوع رابطه، نمایش UI، و قابلیت جبران و انتقام است. این مرحله فقط شامل تست است و شامل توسعه ویژگی جدید نمی‌شود. نکته حیاتی: تمام سناریوهای کاربری باید تست شوند.

---

# 🔹 مرحله 1: ایجاد صفحه پروفایل افراد (People Profile Page) در برنامه

**Scope:** این مرحله شامل ایجاد یک صفحه جدید در برنامه با عنوان 'پروفایل افراد' است. این صفحه باید لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رویدادها یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام صفحه باید دقیقاً 'پروفایل افراد' باشد و از داده‌های موجود در برنامه برای نمایش افراد استفاده کند.
**Key terms:** پروفایل افراد, People Profile Page, صفحه افراد, لیست افراد

**بخش مربوط از متن کاربر:**
```
یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد صفحه پروفایل افراد با مسیریابی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/Dashboard.jsx:1-50` — `Dashboard` — این فایل deep-read شده — می‌توان از اینجا لینک به صفحه پروفایل افراد اضافه کرد
  ```jsx
  // فایل deep-read شده — ساختار کلی Dashboard که می‌تواند لینک به صفحه جدید داشته باشد
  ```
- `frontend/src/components/Sidebar.jsx:1-50` — `Sidebar` — اینجا باید آیتم منوی 'پروفایل افراد' اضافه شود
  ```jsx
  // فایل deep-read شده — کامپوننت Sidebar که آیتم‌های منو را مدیریت می‌کند
  ```
- `frontend/src/App.jsx:1-30` — `App` — اینجا مسیر /people به PeopleProfile اضافه شود
  ```jsx
  // فایل deep-read شده — مسیریابی اصلی برنامه
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/routes/person.py` (سطر 1) — این روتر endpointهای /api/persons را ارائه می‌دهد که صفحه جدید از آن داده می‌گیرد
- `app/services/person_service.py` (سطر 1) — سرویس پشتیبان برای عملیات CRUD افراد
- `app/models/person.py` (سطر 1) — مدل Person که ساختار داده افراد را مشخص می‌کند
- `frontend/src/lib/api.js` (سطر 1) — کتابخانه API که برای فراخوانی endpointها استفاده می‌شود

## 🌐 نقشهٔ وابستگی‌ها
این تسک به

## 🔍 Context و وضعیت فعلی
ایجاد یک صفحه جدید در برنامه با عنوان دقیق 'پروفایل افراد' (People Profile Page) که لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رویدادها یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام صفحه باید دقیقاً 'پروفایل افراد' باشد و از داده‌های موجود در برنامه برای نمایش افراد استفاده کند.

بر اساس بررسی کدبیس:
- مدل Person در `app/models/person.py` وجود دارد
- سرویس person_service در `app/services/person_service.py` وجود دارد
- روتر person در `app/routes/person.py` با endpointهای `/api/persons` موجود است
- در frontend صفحه‌ای برای نمایش افراد وجود ندارد
- مسیریابی frontend در `frontend/src/App.jsx` مدیریت می‌شود
- صفحه Dashboard در `frontend/src/pages/Dashboard.jsx` وجود دارد که می‌تواند لینک به صفحه جدید داشته باشد
- کامپوننت Sidebar در `frontend/src/components/Sidebar.jsx` برای ناوبری استفاده می‌شود

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل `frontend/src/pages/PeopleProfile.jsx` با کامپوننت PeopleProfile که:
   - از API `/api/persons` برای دریافت لیست افراد استفاده کند
   - داده‌ها را در قالب کارت/لیست نمایش دهد
   - عنوان صفحه 'پروفایل افراد' باشد

2. افزودن مسیر جدید در `frontend/src/App.jsx`:
   - مسیر `/people` به صفحه PeopleProfile

3. افزودن لینک در `frontend/src/components/Sidebar.jsx`:
   - آیتم منو با عنوان 'پروفایل افراد' و لینک به `/people`

4. (اختیاری) افزودن لینک در `frontend/src/pages/Dashboard.jsx` برای دسترسی سریع

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: small

---

# 🔹 مرحله 2: طراحی و پیاده‌سازی مدل داده برای پروفایل افراد

**Scope:** این مرحله شامل طراحی و پیاده‌سازی مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات پروفایل افراد است. این مدل باید شامل فیلدهایی برای نام، شناسه، امتیاز، تاریخچه رویدادها، و نوع رابطه باشد. این مرحله فقط شامل ساختار داده است و شامل منطق تجاری یا UI نمی‌شود. نکته حیاتی: مدل باید قابلیت ذخیره‌سازی رویدادهای خوب و بد را داشته باشد.
**Key terms:** مدل داده, پروفایل, امتیاز, رویداد, نوع رابطه, دیتابیس

**بخش مربوط از متن کاربر:**
```
کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی مدل داده پروفایل افراد با رویدادها و امتیاز

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `Person (model)` — مدل Person فعلی باید بررسی شود و فیلدهای relationship_type و score به آن اضافه شود. همچنین مدل PersonEvent باید ایجاد شود.
  ```python
  فایل موجود در ساختار پروژه — محتوای دقیق در deep-read موجود نیست
  ```
- `app/routes/person.py:1-100` — `person router` — این فایل endpointهای CRUD برای Person را دارد. باید endpointهای جدید برای ثبت رویداد و محاسبه امتیاز اضافه شود.
  ```python
  فایل در deep-read موجود است — محتوای دقیق router
  ```
- `app/schemas/person_schema.py:1-50` — `PersonSchema` — Schemaهای Pydantic برای Person و PersonEvent باید ایجاد/به‌روزرسانی شوند.
  ```python
  فایل موجود در ساختار پروژه — محتوای دقیق در deep-read موجود نیست
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/services/person_service.py` (سطر 1) — این سرویس منطق تجاری Person را پیاده‌سازی می‌کند و باید برای PersonEvent به‌روزرسانی شود
- `app/database.py` (سطر 1) — برای ثبت مدل‌های جدید در Base.metadata و ایجاد جداول
- `app`

## 🔍 Context و وضعیت فعلی
طراحی و پیاده‌سازی مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات پروفایل افراد. این مدل باید شامل فیلدهایی برای نام، شناسه، امتیاز، تاریخچه رویدادها، و نوع رابطه باشد. این مرحله فقط شامل ساختار داده است و شامل منطق تجاری یا UI نمی‌شود. نکته حیاتی: مدل باید قابلیت ذخیره‌سازی رویدادهای خوب و بد را داشته باشد.

بخش مربوط از درخواست اصلی کاربر:
"کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه"

کلیدواژه‌ها: مدل داده, پروفایل, امتیاز, رویداد, نوع رابطه, دیتابیس

شواهد در کد واقعی پروژه:
- فایل `app/models/person.py` در ساختار پروژه موجود است (مدل Person فعلی)
- فایل `app/routes/person.py` در deep-read موجود است
- فایل `app/schemas/person_schema.py` در ساختار پروژه موجود است
- فایل `app/services/person_service.py` در ساختار پروژه موجود است
- مدل‌های مشابه مانند `app/models/task.py` و `app/models/project.py` الگوی SQLAlchemy را نشان می‌دهند

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل PersonProfile با فیلدهای: id, name, identifier, score (امتیاز کلی), relationship_type (نوع رابطه: دوست/خانواده/همکار/سایر), created_at, updated_at
2. ایجاد مدل PersonEvent برای ثبت رویدادهای خوب و بد با فیلدهای: id, person_id (FK), event_type (good/bad), title, description, score_change (تغییر امتیاز), created_at
3. به‌روزرسانی مدل Person موجود در `app/models/person.py` یا ایجاد مدل جدید
4. ایجاد schemaهای Pydantic در `app/schemas/person_schema.py`
5. ایجاد سرویس CRUD در `app/services/person_service.py`
6. ایجاد endpointهای REST در `app/routes/person.py`
7. ایجاد migration در `migrations/versions/`

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: پیاده‌سازی سیستم ثبت رویدادها (Event Logging) برای افراد

**Scope:** این مرحله شامل پیاده‌سازی سیستمی برای ثبت رویدادهای مرتبط با هر فرد است. این رویدادها شامل کارهای خوب و بدی است که فرد انجام داده است. هر رویداد باید شامل جزئیات کامل (تاریخ، توضیحات، نوع رویداد) باشد. این مرحله فقط شامل ثبت رویدادها است و شامل آنالیز یا امتیازدهی نمی‌شود. نکته حیاتی: رویدادها باید به مرور زمان به پروفایل فرد اضافه شوند.
**Key terms:** رویداد, ثبت, کار خوب, کار بد, جزئیات, تاریخچه

**بخش مربوط از متن کاربر:**
```
کارای بد و خوبش ثبت می‌شه. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن EventLog model و CRUD برای افراد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `Person model` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مدل Person در این فایل تعریف شده است. فیلدهای event_logs relationship باید به مدل اضافه شود.
- `app/routes/person.py:1-100` — `person router` — فایل deep-read شده اما محتوای کامل آن در context موجود نیست. روتر person در این فایل تعریف شده است. endpointهای جدید برای ثبت و دریافت رویدادها باید به این فایل اضافه شوند.
- `app/main.py:244-255` — `startup_event function`
  ```python
  # users profile fields — bio / display_name — added so the
      # /api/users/profile sanitiser can actually persist the sanitised
      # values. Idempotent ADD COLUMN IF NOT EXISTS for legacy DBs.
      _user
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
پیاده‌سازی سیستم ثبت رویدادها (Event Logging) برای افراد. این مرحله شامل پیاده‌سازی سیستمی برای ثبت رویدادهای مرتبط با هر فرد است. این رویدادها شامل کارهای خوب و بدی است که فرد انجام داده است. هر رویداد باید شامل جزئیات کامل (تاریخ، توضیحات، نوع رویداد) باشد. این مرحله فقط شامل ثبت رویدادها است و شامل آنالیز یا امتیازدهی نمی‌شود. نکته حیاتی: رویدادها باید به مرور زمان به پروفایل فرد اضافه شوند.

--- بخش مربوط از درخواست اصلی کاربر ---
کارای بد و خوبش ثبت می‌شه. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه

--- کلیدواژه‌ها ---
رویداد, ثبت, کار خوب, کار بد, جزئیات, تاریخچه

شواهد در کد: مدل Person در `app/models/person.py` وجود دارد (فایل deep-read نشده اما در ساختار پروژه موجود است). روتر person در `app/routes/person.py` (deep-read شده) CRUD پایه را ارائه می‌دهد. هیچ مدل EventLog یا رویدادی در کدبیس فعلی وجود ندارد. فایل `app/models/__init__.py` مدل‌ها را export می‌کند. فایل `app/database.py` engine و SessionLocal را فراهم می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل EventLog در `app/models/person.py` (یا فایل جدید `app/models/event_log.py`) با فیلدهای: id, person_id (FK به Person), event_type (Enum: good/bad), description (Text), event_date (DateTime), created_at (DateTime).
2. به‌روزرسانی `app/models/__init__.py` برای export مدل جدید.
3. ایجاد Pydantic schema در فایل جدید `app/schemas/event_log_schema.py` با EventLogCreate, EventLogResponse.
4. ایجاد سرویس `app/services/event_log_service.py` با توابع create_event_log, get_event_logs_by_person.
5. افزودن endpointهای جدید به `app/routes/person.py`: POST /api/persons/{person_id}/events (ثبت رویداد), GET /api/persons/{person_id}/events (لیست رویدادهای یک فرد).
6. ایجاد migration جدید در `migrations/versions/` برای ایجاد جدول event_logs.
7. افزودن startup ALTER در `app/main.py` (پس از خط 255) برای idempotent ADD COLUMN در محیط‌های بدون alembic.
8. به‌روزرسانی frontend در `frontend/src/pages/PersonDetail.jsx` (فایل موجود نیست — باید ایجاد شود) برای نمایش و ثبت رویدادها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: پیاده‌سازی سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد

**Scope:** این مرحله شامل پیاده‌سازی یک سیستم هوش مصنوعی است که بر اساس داده‌های ثبت‌شده (رویدادها، لحن صحبت، نوع تعاملات) به هر فرد یک امتیاز می‌دهد. این امتیاز باید به صورت خودکار و با آنالیز داده‌های موجود محاسبه شود. این مرحله فقط شامل منطق امتیازدهی است و شامل UI یا ثبت رویدادها نمی‌شود. نکته حیاتی: امتیاز باید بر اساس 'لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن' نیز محاسبه شود.
**Key terms:** هوش مصنوعی, آنالیز, امتیاز, لحن, نوع صحبت, AI Scoring

**بخش مربوط از متن کاربر:**
```
هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `class Person`
  ```python
  class Person(Base):
      __
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک سیستم هوش مصنوعی برای امتیازدهی به افراد (AI Scoring) را دارد. این سیستم باید بر اساس داده‌های ثبت‌شده شامل رویدادها (events)، لحن صحبت (tone of speech)، و نوع تعاملات (interaction types) به هر فرد یک امتیاز (score) بدهد. امتیاز باید به صورت خودکار و با آنالیز داده‌های موجود محاسبه شود. نکته حیاتی که کاربر تأکید کرده: امتیاز باید بر اساس 'لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن' نیز محاسبه شود. یعنی لحن و نوع صحبت دیگران درباره کاربر (نه فقط لحن خود کاربر) در امتیازدهی تأثیر دارد. این مرحله فقط شامل منطق امتیازدهی است و شامل UI یا ثبت رویدادها نمی‌شود. کلیدواژه‌های کاربر: هوش مصنوعی, آنالیز, امتیاز, لحن, نوع صحبت, AI Scoring. در کد فعلی پروژه، مدل Person در `app/models/person.py` وجود دارد که می‌تواند فیلد score یا امتیاز را دریافت کند. همچنین سرویس `app/services/person_service.py` و روتر `app/routes/person.py` برای مدیریت افراد وجود دارد. سرویس‌های AI در `app/services/ai/` شامل `nlp_service.py` (برای تحلیل متن و NLP)، `model_service.py` (برای مدیریت مدل‌ها)، و `provider_service.py` (برای ارتباط با providerهای AI) هستند. endpointهای AI در `app/routes/ai.py` شامل POST /ai/generate، POST /ai/query، و GET /ai/metrics هستند. مدل‌های Task و Project نیز می‌توانند رویدادهای مرتبط با افراد را ذخیره کنند. فایل `app/models/notification.py` برای اعلان‌ها و `app/models/todo_item.py` برای آیتم‌های todo که ممکن است شامل تعاملات باشند. همچنین `app/services/ai_service.py` سرویس اصلی AI است که می‌تواند منطق امتیازدهی را پیاده‌سازی کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلد `ai_score` (Float، nullable) و `ai_score_metadata` (JSON، nullable) به مدل Person در `app/models/person.py`. 2. ایجاد یک سرویس جدید `app/services/ai_scoring_service.py` که شامل: تابع `calculate_person_score(person_id: int, db: AsyncSession) -> float` برای محاسبه امتیاز بر اساس رویدادها، لحن، و تعاملات. تابع `analyze_tone(text: str) -> dict` برای تحلیل لحن متن با استفاده از `app/services/ai/nlp_service.py`. تابع `analyze_interaction_type(interaction: str) -> dict` برای نوع‌بندی تعاملات. تابع `aggregate_events(person_id: int, db: AsyncSession) -> list` برای جمع‌آوری رویدادهای مرتبط با فرد از Task، Project، TodoItem، و Notification. 3. افزودن endpoint جدید POST /api/persons/{person_id}/calculate-score در `app/routes/person.py` که سرویس امتیازدهی را فراخوانی کرده و امتیاز را در Person.ai_score ذخیره کند. 4. استفاده از `app/services/ai/nlp_service.py` (که شامل تابع `analyze_sentiment` و `extract_keywords` است) برای تحلیل لحن و نوع صحبت. 5. ذخیره تاریخچه امتیازها در یک مدل جدید `PersonScoreHistory` با فیلدهای person_id, score, metadata, created_at. 6. افزودن یک task دوره‌ای در `app/tasks.py` (با Celery) برای محاسبه خودکار امتیازها در بازه‌های زمانی مشخص.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: پیاده‌سازی سیستم تنظیم نوع رابطه (Relationship Adjustment) بر اساس امتیاز

**Scope:** این مرحله شامل پیاده‌سازی منطقی است که بر اساس امتیاز هوش مصنوعی و رویدادهای ثبت‌شده، نوع رابطه کاربر با هر فرد را تنظیم می‌کند. این تنظیم شامل مواردی مانند 'کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم' است. این مرحله فقط شامل منطق تنظیم رابطه است و شامل UI یا ثبت رویدادها نمی‌شود. نکته حیاتی: این سیستم باید از فراموشی کارهای خوب و بد جلوگیری کند.
**Key terms:** نوع رابطه, تنظیم, جبران, انتقام, فراموشی, Relationship Adjustment

**بخش مربوط از متن کاربر:**
```
نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سیستم تنظیم نوع رابطه بر اساس امتیاز

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `Person` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، این فایل در app/models/person.py است.
  ```python
  مدل Person موجود در پروژه که باید فیلد relationship_type به آن اضافه شود و مدل‌های RelationshipEvent و RelationshipScore در کنار آن ایجاد شوند.
  ```
- `app/services/person_service.py:1-100` — `PersonService` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، این فایل در app/services/person_service.py است.
  ```python
  سرویس موجود برای مدیریت افراد که باید توابع جدید برای ثبت رویداد و محاسبه امتیاز به آن اضافه شود.
  ```
- `app/routes/person.py:1-50` — `router` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، این فایل
  ```python
  روتر موجود برای endpointهای مربوط به افراد که باید endpoint جدید برای ثبت رویداد و دریافت وضعیت رابطه اضافه شود.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
پیاده‌سازی سیستم تنظیم نوع رابطه (Relationship Adjustment) بر اساس امتیاز هوش مصنوعی و رویدادهای ثبت‌شده. این مرحله شامل پیاده‌سازی منطقی است که بر اساس امتیاز هوش مصنوعی و رویدادهای ثبت‌شده، نوع رابطه کاربر با هر فرد را تنظیم می‌کند. این تنظیم شامل مواردی مانند 'کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم' است. این مرحله فقط شامل منطق تنظیم رابطه است و شامل UI یا ثبت رویدادها نمی‌شود. نکته حیاتی: این سیستم باید از فراموشی کارهای خوب و بد جلوگیری کند. نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره. کلیدواژه‌ها: نوع رابطه, تنظیم, جبران, انتقام, فراموشی, Relationship Adjustment. بر اساس کد موجود در پروژه، مدل Person در `app/models/person.py` و سرویس person_service در `app/services/person_service.py` و روتر person در `app/routes/person.py` وجود دارد. همچنین مدل‌های task و project و notification برای ثبت رویدادها موجودند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک مدل جدید `RelationshipEvent` در `app/models/person.py` برای ثبت رویدادهای مثبت و منفی (کارهای خوب و بد) با فیلدهای: person_id, event_type (good/bad), description, score, timestamp, source (AI/user). 2. ایجاد یک مدل `RelationshipScore` در همان فایل برای ذخیره امتیاز کلی رابطه با فیلدهای: person_id, total_score, last_adjusted, adjustment_history (JSON). 3. ایجاد یک سرویس جدید `relationship_service.py` در `app/services/` با توابع: `record_event()`, `calculate_adjustment()`, `get_relationship_type()`. 4. تابع `calculate_adjustment` باید بر اساس مجموع امتیازها نوع رابطه را تعیین کند: مثبت (دوستانه/خوب)، منفی (نیاز به جبران/انتقام)، خنثی. 5. اضافه کردن فیلد `relationship_type` به مدل Person. 6. ایجاد یک endpoint جدید در `app/routes/person.py` برای دریافت و تنظیم رویدادها. 7. اطمینان از عدم فراموشی با ذخیره تمام رویدادها در دیتابیس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: طراحی و پیاده‌سازی UI برای نمایش امتیاز و نوع رابطه در پروفایل افراد

**Scope:** این مرحله شامل طراحی و پیاده‌سازی بخش UI در صفحه پروفایل افراد است که امتیاز هوش مصنوعی و نوع رابطه را نمایش می‌دهد. این بخش باید به صورت بصری و قابل فهم برای کاربر باشد. این مرحله فقط شامل UI است و شامل منطق امتیازدهی یا تنظیم رابطه نمی‌شود. نکته حیاتی: UI باید به کاربر کمک کند تا 'کجا بهم خوبی کرد' و 'کجا باید کمش بذارم' را ببیند.
**Key terms:** UI, امتیاز, نوع رابطه, نمایش, پروفایل

**بخش مربوط از متن کاربر:**
```
کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی UI نمایش امتیاز و نوع رابطه در پروفایل افراد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/PersonProfile.jsx:1-100` — `PersonProfile` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. احتمالاً PersonDetail.jsx یا PersonProfile.jsx در frontend/src/pages/
  ```jsx
  // فایل در deep context موجود نیست — بر اساس ساختار سطحی پروژه
  ```
- `app/models/person.py:1-50` — `Person` — این فایل deep-read نشده — مجری باید فیلدهای relationship_type و ai_score را در مدل Person تأیید کند
  ```python
  // فایل در deep context موجود نیست — بر اساس ساختار سطحی پروژه
  ```
- `app/routes/person.py:1-50` — `get_person` — این فایل deep-read نشده — مجری باید endpoint GET /api/persons/{person_id} را بررسی کند
  ```python
  // فایل در deep context موجود نیست — بر اساس ساختار سطحی پروژه
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/context/PersonContext.jsx` (سطر 1) — احتمالاً state مدیریت person را دارد و باید داده‌های امتیاز و رابطه را نیز مدیریت کند
- `frontend/src/lib/api.js` (سطر 1) — توابع API برای فراخوانی endpoint person در این فایل تعریف شده‌اند
- `app/services/person_service.py` (سطر 1) — منطق سرویس person که داده‌ها را از دیتابیس می‌خواند
- `app/schemas/person_schema.py` — شما Pydantic برای سریالایز کردن داده‌های person (شامل relationship_type و ai_score)

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی و پیاده‌سازی بخش UI در صفحه پروفایل افراد (Person Profile) داده است که امتیاز هوش مصنوعی (AI Score) و نوع رابطه (Relationship Type) را نمایش دهد. این بخش باید به صورت بصری و قابل فهم باشد و به کاربر کمک کند تا 'کجا بهم خوبی کرد' (نقاط قوت رابطه) و 'کجا باید کمش بذارم' (نقاط ضعف رابطه) را ببیند. این مرحله فقط شامل UI است و شامل منطق امتیازدهی یا تنظیم رابطه نمی‌شود. کلیدواژه‌های اصلی: UI, امتیاز, نوع رابطه, نمایش, پروفایل. بر اساس ساختار پروژه، صفحه پروفایل افراد احتمالاً در frontend/src/pages/PersonDetail.jsx یا frontend/src/pages/PersonProfile.jsx قرار دارد (فایل دقیق در deep context موجود نیست). مدل Person در app/models/person.py تعریف شده و احتمالاً فیلدهای relationship_type و ai_score را دارد. روتر person در app/routes/person.py مدیریت می‌شود. API endpoint مربوطه احتمالاً GET /api/persons/{person_id} است که داده‌های person را برمی‌گرداند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی مدل Person در app/models/person.py برای اطمینان از وجود فیلدهای relationship_type و ai_score. 2. ایجاد یا ویرایش کامپوننت PersonProfile در frontend/src/pages/PersonProfile.jsx (یا PersonDetail.jsx). 3. طراحی بخش UI شامل: نمایش امتیاز (عدد یا نوار پیشرفت رنگی)، نمایش نوع رابطه (برچسب متنی با آیکون)، نمایش نقاط قوت و ضعف (لیست کوتاه از key moments). 4. اتصال به API GET /api/persons/{person_id} برای دریافت داده‌ها. 5. استفاده از Tailwind CSS برای استایل‌دهی (بر اساس frontend/tailwind.config.js). 6. افزودن state management در PersonContext (frontend/src/context/PersonContext.jsx) در صورت نیاز.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 7: پیاده‌سازی قابلیت ثبت نظر شخصی کاربر (Manual Review) برای هر فرد

**Scope:** این مرحله شامل پیاده‌سازی قابلیتی است که به کاربر اجازه می‌دهد نظر شخصی خود را درباره هر فرد ثبت کند. این نظر می‌تواند بر اساس 'لحن من و نوعی که در نوع صحبتی که دربارمون دارن' باشد. این نظر باید در محاسبه امتیاز هوش مصنوعی نیز تأثیر بگذارد. این مرحله فقط شامل ثبت نظر است و شامل آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نظر شخصی کاربر باید به عنوان یک ورودی برای سیستم امتیازدهی در نظر گرفته شود.
**Key terms:** نظر شخصی, Manual Review, لحن, نوع صحبت, ورودی کاربر

**بخش مربوط از متن کاربر:**
```
یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن Manual Review شخصی به Person و تأثیر در امتیاز AI

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-30` — `class Person` — مدل اصلی Person که باید فیلدهای manual_review و user_rating به آن اضافه شود.
  ```python
  class Person(Base):
      __tablename__ = "persons"
      id = Column(Integer, primary_key=True, index=True)
      name = Column(String, nullable=False)
      ...
  ```
- `app/routes/person.py:1-50` — `router` — روتر Person که باید endpoint جدید PATCH /api/persons/{person_id}/review به آن اضافه شود.
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.services.person_service import ...
  ```
- `app/services/person_service.py:1-40` — `person_service functions` — سرویس Person که باید تابع update_person_review به آن اضافه شود.
  ```python
  async def get_person(db: AsyncSession, person_id: int):
      ...
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/schemas/person_schema.py` (سطر 1) — Schema مربوط به Person که باید فیلدهای manual_review و user_rating را شامل شود.
- `app/services/ai`

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت ثبت نظر شخصی کاربر (Manual Review) برای هر فرد (Person) را دارد. این نظر بر اساس 'لحن من و نوعی که در نوع صحبتی که دربارمون دارن' است و باید در محاسبه امتیاز هوش مصنوعی (AI score) تأثیر بگذارد. این مرحله فقط شامل ثبت نظر است و شامل آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نظر شخصی کاربر باید به عنوان یک ورودی برای سیستم امتیازدهی در نظر گرفته شود. کلیدواژه‌ها: نظر شخصی, Manual Review, لحن, نوع صحبت, ورودی کاربر. در کد فعلی، مدل Person در `app/models/person.py` تعریف شده است. روتر person در `app/routes/person.py` شامل CRUD پایه است. سرویس person در `app/services/person_service.py` منطق تجاری را مدیریت می‌کند. هیچ فیلد `manual_review` یا `user_rating` در مدل Person وجود ندارد. همچنین، سیستم امتیازدهی AI (احتمالاً در `app/services/ai/nlp_service.py` یا `app/services/ai_service.py`) باید اصلاح شود تا این ورودی جدید را در نظر بگیرد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلد `manual_review` از نوع `Text` و `user_rating` از نوع `Float` (یا `Integer`) به مدل Person در `app/models/person.py`. 2. به‌روزرسانی schema مربوط به Person در `app/schemas/person_schema.py` برای شامل شدن فیلدهای جدید. 3. افزودن endpoint جدید `PATCH /api/persons/{person_id}/review` در `app/routes/person.py` برای دریافت و ذخیره نظر و امتیاز کاربر. 4. به‌روزرسانی `app/services/person_service.py` با تابع `update_person_review` که نظر و امتیاز را ذخیره می‌کند. 5. اصلاح تابع محاسبه امتیاز AI در `app/services/ai/nlp_service.py` (یا `app/services/ai_service.py`) تا `user_rating` را به عنوان یک feature ورودی در نظر بگیرد. 6. افزودن migration جدید در `migrations/versions/` برای اضافه کردن ستون‌های جدید به جدول persons. 7. به‌روزرسانی `app/main.py` برای اجرای migration در startup (مانند سایر فیلدها).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 8: پیاده‌سازی سیستم جلوگیری از فراموشی (Forgetfulness Prevention) با نمایش تاریخچه کامل

**Scope:** این مرحله شامل پیاده‌سازی سیستمی است که تاریخچه کامل رویدادها و نظرات را در پروفایل هر فرد نمایش می‌دهد تا کاربر دچار فراموشی نشود. این سیستم باید به کاربر کمک کند تا 'با یه کار خوبش هزار تا کار بدی که کرده رو فراموش نکنم'. این مرحله فقط شامل نمایش تاریخچه است و شامل منطق امتیازدهی نمی‌شود. نکته حیاتی: تاریخچه باید به صورت زمانی و با جزئیات کامل نمایش داده شود.
**Key terms:** تاریخچه, فراموشی, نمایش, رویداد, جزئیات

**بخش مربوط از متن کاربر:**
```
باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً لزومی نداره خیلی کینه بگیرم
```

## 🎯 هدف (خلاصه ساختاریافته)
نمایش تاریخچه کامل رویدادها در پروفایل افراد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-30` — `Person` — مدل Person فعلی — باید مدل PersonEvent را به این فایل اضافه کنیم یا فایل جداگانه person_event.py بسازیم
  ```python
  class Person(Base):
      __tablename__ = "persons"
      id = Column(Integer, primary_key=True, index=True)
      name = Column(String(100), nullable=False)
      # ... other fields
  ```
- `app/routes/person.py:1-50` — `router` — روتر person — باید endpoint جدید GET /{person_id}/history به اینجا اضافه شود
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.services.person_service import *
  
  router = APIRouter(prefix="/api/persons", tags=["persons"])
  ```
- `app/services/person_service.py:1-40` — `person_service functions` — سرویس person — باید تابع get_person_history را به اینجا اضافه کنیم
  ```python
  from sqlalchemy.ext.asyncio import AsyncSession
  from sqlalchemy import select
  from app.models.person import Person
  
  async def get_persons(db: AsyncSession, skip: int = 0, limit: int = 100):
      result = await db.execute(select(Person).offset(skip).limit(limit))
      return result.scalars().all()
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/schemas/person_schema.py` (سطر 1) — نیاز به schemaهای PersonEventCreate و PersonEventResponse برای validation و serialization

## 🔍 Context و وضعیت فعلی
پیاده‌سازی سیستم جلوگیری از فراموشی (Forgetfulness Prevention) با نمایش تاریخچه کامل رویدادها و نظرات در پروفایل هر فرد. این سیستم باید به کاربر کمک کند تا 'با یه کار خوبش هزار تا کار بدی که کرده رو فراموش نکنم'. این مرحله فقط شامل نمایش تاریخچه است و شامل منطق امتیازدهی نمی‌شود. نکته حیاتی: تاریخچه باید به صورت زمانی و با جزئیات کامل نمایش داده شود. کلیدواژه‌ها: تاریخچه, فراموشی, نمایش, رویداد, جزئیات. بر اساس کد واقعی پروژه، مدل Person در `app/models/person.py` تعریف شده و روتر person در `app/routes/person.py` با endpointهای `/api/persons` موجود است. همچنین سرویس person_service در `app/services/person_service.py` عملیات CRUD را انجام می‌دهد. برای نمایش تاریخچه، نیاز به یک مدل جدید PersonEvent یا افزودن فیلد history به Person داریم که رویدادها و نظرات مرتبط با هر فرد را ذخیره کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل PersonEvent در `app/models/person.py` با فیلدهای: id, person_id (FK به Person), event_type (enum: note_added, interaction, status_change, etc.), description (TEXT), created_at (TIMESTAMP), created_by (FK به User). 2. ایجاد schema مربوطه در `app/schemas/person_schema.py` با نام PersonEventCreate, PersonEventResponse. 3. افزودن endpoint GET /api/persons/{person_id}/history در `app/routes/person.py` که تاریخچه را به صورت زمانی نزولی برمی‌گرداند. 4. افزودن سرویس get_person_history در `app/services/person_service.py`. 5. ایجاد کامپوننت frontend جدید در `frontend/src/pages/PersonHistory.jsx` یا افزودن بخش history به صفحه PersonDetail موجود. 6. به‌روزرسانی `app/main.py` برای mount کردن روتر جدید اگر نیاز باشد. 7. ایجاد migration در `migrations/versions/` برای ایجاد جدول person_events.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 9: پیاده‌سازی قابلیت 'جبران و انتقام' (Compensation & Revenge) در پروفایل افراد

**Scope:** این مرحله شامل پیاده‌سازی قابلیتی است که به کاربر اجازه می‌دهد برای کارهای خوب و بد افراد، اقدامات جبرانی یا انتقامی را ثبت و پیگیری کند. این قابلیت باید به کاربر کمک کند تا 'کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم'. این مرحله فقط شامل ثبت و پیگیری اقدامات است و شامل منطق امتیازدهی نمی‌شود. نکته حیاتی: این قابلیت باید به صورت یک لیست وظایف (To-Do List) در پروفایل فرد نمایش داده شود.
**Key terms:** جبران, انتقام, اقدام, پیگیری, To-Do List

**بخش مربوط از متن کاربر:**
```
کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن قابلیت جبران و انتقام به پروفایل افراد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `Person` — مدل Person که باید یک رابطه به TodoList جبران و انتقام داشته باشد.
  ```python
  class Person(Base):
      __tablename__ = "persons"
      id = Column(Integer, primary_key=True, index=True)
      name = Column(String, nullable=False)
      user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
      ...
  ```
- `app/routes/person.py:1-100` — `router` — مسیرهای API مربوط به Person که باید endpoint جدید برای جبران و انتقام اضافه شود.
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.models.person import Person
  from app.schemas.person_schema import PersonCreate, PersonResponse
  ...
  ```
- `app/models/todo_list.py:1-50` — `TodoList` — مدل TodoList که برای ذخیره لیست جبران و انتقام استفاده می‌شود
  ```python
  class TodoList(Base):
      __tablename__ = "todo_lists"
      id = Column(Integer, primary_key=True, index=True)
      name = Column(String, nullable=False)
      user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
      ...
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت 'جبران و انتقام' (Compensation & Revenge) در پروفایل افراد را داده است. این قابلیت به کاربر اجازه می‌دهد برای کارهای خوب و بد افراد، اقدامات جبرانی یا انتقامی را ثبت و پیگیری کند. هدف این است که کاربر بتواند مشخص کند 'کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم'. این مرحله فقط شامل ثبت و پیگیری اقدامات است و شامل منطق امتیازدهی نمی‌شود. نکته حیاتی: این قابلیت باید به صورت یک لیست وظایف (To-Do List) در پروفایل فرد نمایش داده شود.

کلیدواژه‌های کاربر: جبران, انتقام, اقدام, پیگیری, To-Do List

شواهد در کد: در پروژه فعلی، سیستم To-Do List از طریق مدل‌های `TodoList` و `TodoItem` در `app/models/todo_list.py` و `app/models/todo_item.py` پیاده‌سازی شده است. همچنین مسیرهای API مربوطه در `app/routes/lists.py` و `app/routes/todo_items.py` وجود دارند. مدل `Person` در `app/models/person.py` تعریف شده و مسیرهای API آن در `app/routes/person.py` قرار دارند. برای پیاده‌سازی این قابلیت، باید یک TodoList جدید به ازای هر Person ایجاد شود که شامل آیتم‌های جبران و انتقام باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک TodoList جدید برای هر Person با نام پیش‌فرض 'جبران و انتقام' (Compensation & Revenge) در زمان ایجاد Person یا به صورت دستی.
2. افزودن endpoint جدید به `app/routes/person.py` برای دریافت/ایجاد/به‌روزرسانی آیتم‌های این لیست.
3. ایجاد یک سرویس جدید `app/services/compensation_service.py` برای مدیریت منطق تجاری.
4. نمایش این لیست در پروفایل فرد در فرانت‌اند (احتمالاً در `frontend/src/pages/PersonDetail.jsx` یا صفحه مشابه).
5. استفاده از مدل‌های موجود `TodoList` و `TodoItem` برای ذخیره‌سازی.
6. اطمینان از اینکه این لیست در پروفایل فرد به صورت یک بخش مجزا نمایش داده می‌شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 10: تست و اعتبارسنجی یکپارچه سیستم پروفایل افراد

**Scope:** این مرحله شامل تست و اعتبارسنجی کامل سیستم پروفایل افراد است. این تست شامل بررسی صحت ثبت رویدادها، محاسبه امتیاز هوش مصنوعی، تنظیم نوع رابطه، نمایش UI، و قابلیت جبران و انتقام است. این مرحله فقط شامل تست است و شامل توسعه ویژگی جدید نمی‌شود. نکته حیاتی: تمام سناریوهای کاربری باید تست شوند.
**Key terms:** تست, اعتبارسنجی, یکپارچه, سناریو, کیفیت

**بخش مربوط از متن کاربر:**
```
همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
تست و اعتبارسنجی یکپارچه سیستم پروفایل افراد

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
این تسک شامل تست و اعتبارسنجی کامل سیستم پروفایل افراد است. کاربر درخواست کرده: 'تست و اعتبارسنجی یکپارچه سیستم پروفایل افراد. این مرحله شامل تست و اعتبارسنجی کامل سیستم پروفایل افراد است. این تست شامل بررسی صحت ثبت رویدادها، محاسبه امتیاز هوش مصنوعی، تنظیم نوع رابطه، نمایش UI، و قابلیت جبران و انتقام است. این مرحله فقط شامل تست است و شامل توسعه ویژگی جدید نمی‌شود. نکته حیاتی: تمام سناریوهای کاربری باید تست شوند.' همچنین کاربر اشاره کرده: 'همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه'. کلیدواژه‌ها: تست, اعتبارسنجی, یکپارچه, سناریو, کیفیت. در کد واقعی پروژه، فایل app/routes/person.py شامل endpointهای CRUD برای افراد است (GET /api/persons, POST /api/persons, GET /api/persons/{person_id}, PUT /api/persons/{person_id}, DELETE /api/persons/{person_id}). فایل app/models/person.py مدل Person را تعریف می‌کند. فایل app/services/person_service.py سرویس‌های مربوط به افراد را پیاده‌سازی می‌کند. فایل frontend/src/pages/Dashboard.jsx و frontend/src/pages/Home.jsx صفحات UI مرتبط با افراد را نمایش می‌دهند. همچنین فایل app/routes/ai.py شامل endpointهای هوش مصنوعی است که ممکن است برای محاسبه امتیاز استفاده شود. فایل app/services/ai/nlp_service.py سرویس NLP را ارائه می‌دهد. فایل app/models/notification.py برای ثبت رویدادها استفاده می‌شود. فایل app/routes/notifications.py endpointهای اعلان‌ها را مدیریت می‌کند. فایل app/services/notification_service.py سرویس اعلان‌ها را پیاده‌سازی می‌کند. فایل app/models/task.py و app/routes/tasks.py برای ثبت جزئیات کارها استفاده می‌شوند. فایل app/services/integration_service.py برای یکپارچه‌سازی با سرویس‌های خارجی استفاده می‌شود. فایل app/dependencies/auth.py برای احراز هویت استفاده می‌شود. فایل app/database.py برای اتصال به پایگاه داده استفاده می‌شود. فایل app/config.py تنظیمات برنامه را شامل می‌شود. فایل app/main.py شامل middleware و exception handlerها است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی فایل app/routes/person.py برای شناسایی endpointهای موجود و اطمینان از وجود تست‌های یکپارچه برای هر endpoint. 2. بررسی فایل app/models/person.py برای اطمینان از صحت مدل Person و فیلدهای مربوط به رویدادها، امتیاز هوش مصنوعی، نوع رابطه، و قابلیت جبران و انتقام. 3. بررسی فایل app/services/person_service.py برای اطمینان از صحت منطق تجاری ثبت رویدادها، محاسبه امتیاز هوش مصنوعی، تنظیم نوع رابطه، و قابلیت جبران و انتقام. 4. بررسی فایل frontend/src/pages/Dashboard.jsx و frontend/src/pages/Home.jsx برای اطمینان از نمایش صحیح UI پروفایل افراد. 5. بررسی فایل app/routes/ai.py و app/services/ai/nlp_service.py برای اطمینان از صحت محاسبه امتیاز هوش مصنوعی. 6. بررسی فایل app/routes/notifications.py و app/services/notification_service.py برای اطمینان از صحت ثبت رویدادها. 7. بررسی فایل app/routes/tasks.py و app/models/task.py برای اطمینان از صحت ثبت جزئیات کارها. 8. بررسی فایل app/services/integration_service.py برای اطمینان از یکپارچه‌سازی صحیح با سرویس‌های خارجی.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. مدل `Person` با فیلدهای `id`, `user_id`, `name`, `email`, `phone`, `created_at`, `updated_at` در `app/models/person.py` تعریف شده باشد و `user_id` به `users.id` ارجاع دهد. _(verify: static)_
2. مدل `Interaction` با فیلدهای `id`, `person_id`, `type` (با Enum), `date`, `summary`, `notes`, `created_at`, `updated_at` در `app/models/interaction.py` تعریف شده باشد و `person_id` به `persons.id` ارجاع دهد. _(verify: static)_
3. مدل `AIAssessment` با فیلدهای `id`, `person_id`, `interaction_id`, `score`, `sentiment`, `analysis_text`, `created_at` در `app/models/ai_assessment.py` تعریف شده باشد. _(verify: static)_
4. مدل `UserComment` با فیلدهای `id`, `user_id`, `person_id`, `interaction_id`, `comment_text`, `created_at` در `app/models/user_comment.py` تعریف شده باشد. _(verify: static)_
5. مدل `BehaviorLog` با فیلدهای `id`, `person_id`, `behavior_type` (با Enum), `description`, `observed_date`, `user_id`, `created_at` در `app/models/behavior_log.py` تعریف شده باشد. _(verify: static)_
6. فایل `app/models/__init__.py` شامل importهای صحیح برای تمام مدل‌های جدید باشد. _(verify: static)_
7. یک Alembic migration جدید ایجاد شده باشد که جداول `persons`, `interactions`, `ai_assessments`, `user_comments`, `behavior_logs` را به پایگاه داده اضافه کند. _(verify: backend_test)_
8. اسکیماهای Pydantic اولیه برای مدل‌های `Person`, `Interaction`, `AIAssessment`, `UserComment`, `BehaviorLog` در `app/schemas/` تعریف شده باشند. _(verify: static)_
9. یک مدل SQLAlchemy به نام `Person` در `app/models/person.py` با فیلدهای `id`, `user_id`, `name`, `email`, `phone`, `notes` تعریف شده باشد. _(verify: static)_
10. رابطه `persons` به مدل `User` در `app/models/user.py` اضافه شده باشد. _(verify: static)_
11. اسکیماهای Pydantic (`PersonBase`, `PersonCreate`, `PersonUpdate`, `PersonResponse`) در `app/schemas/person_schema.py` تعریف شده باشند. _(verify: static)_
12. سرویس `PersonService` در `app/services/person_service.py` با توابع CRUD (مانند `create_person`, `get_person`, `update_person`, `delete_person`, `get_all_persons_for_user`) پیاده‌سازی شده باشد. _(verify: static)_
13. روتر `person` در `app/routes/person.py` با نقاط پایانی `POST /api/persons`, `GET /api/persons/{person_id}`, `PUT /api/persons/{person_id}`, `DELETE /api/persons/{person_id}` و `GET /api/persons` پیاده‌سازی شده باشد. _(verify: static)_
14. روتر `person` در `app/main.py` شامل شده باشد. _(verify: static)_
15. فراخوانی `POST /api/persons` با داده‌های معتبر و کاربر احراز هویت شده، یک پروفایل جدید ایجاد کرده و کد وضعیت 201 و داده‌های پروفایل ایجاد شده را برگرداند. _(verify: api_response)_
16. فراخوانی `GET /api/persons/{person_id}` برای یک پروفایل موجود و متعلق به کاربر احراز هویت شده، کد وضعیت 200 و داده‌های پروفایل را برگرداند. _(verify: api_response)_
17. فراخوانی `GET /api/persons` برای کاربر احراز هویت شده، کد وضعیت 200 و لیستی از پروفایل‌های متعلق به آن کاربر را برگرداند. _(verify: api_response)_
18. فراخوانی `PUT /api/persons/{person_id}` با داده‌های به‌روزرسانی شده و کاربر احراز هویت شده، پروفایل را به‌روزرسانی کرده و کد وضعیت 200 را برگرداند. _(verify: api_response)_
19. فراخوانی `DELETE /api/persons/{person_id}` برای یک پروفایل موجود و متعلق به کاربر احراز هویت شده، پروفایل را حذف کرده و کد وضعیت 204 را برگرداند. _(verify: api_response)_
20. فایل `app/models/user_activity.py` با مدل `UserActivity` شامل فیلدهای `id`, `user_id`, `activity_type`, `related_entity_type`, `related_entity_id`, `details`, و `timestamp` ایجاد شود. _(verify: static)_
21. مدل `User` در `app/models/user.py` باید به‌روزرسانی شود تا یک `relationship` به `UserActivity` داشته باشد. _(verify: static)_
22. فایل `app/schemas/user_activity_schema.py` با شمای‌های `UserActivityBase`, `UserActivityCreate`, و `UserActivityResponse` ایجاد شود. _(verify: static)_
23. فایل `app/services/user_activity_service.py` با توابع `create_activity` و `get_activities_for_user` پیاده‌سازی شود. _(verify: static)_
24. endpoint `POST /api/user-activities/` با `status_code=201` یک فعالیت جدید را با موفقیت ثبت کند و `UserActivityResponse` را برگرداند. _(verify: api_response)_
25. endpoint `GET /api/user-activities/` لیست فعالیت‌های کاربر احراز هویت شده را برگرداند. _(verify: api_response)_
26. ایجاد یک تسک جدید از طریق `POST /api/tasks` باید منجر به ثبت یک `UserActivity` با `activity_type='task_created'` شود. _(verify: backend_test)_
27. مدل `Feedback` در دیتابیس ایجاد شود: یک جدول `feedback` با ستون‌های `id`, `target_user_id`, `author_user_id`, `content`, `sentiment_score`, `speech_type`, `created_at` در دیتابیس وجود داشته باشد. _(verify: backend_test)_
28. endpoint `POST /api/feedback` با موفقیت نظر جدید ثبت کند: ارسال یک نظر معتبر به این endpoint (با احراز هویت) باید منجر به ذخیره آن در دیتابیس شود و پاسخ `201 Created` را برگرداند. _(verify: api_response)_
29. endpoint `GET /api/users/{user_id}/feedback` نظرات مربوط به کاربر را برگرداند: فراخوانی این endpoint با `user_id` معتبر باید لیستی از نظرات ثبت شده برای آن کاربر را برگرداند. _(verify: api_response)_
30. فیلدهای `sentiment_score` و `speech_type` در مدل `Feedback` قابل Null باشند: این فیلدها باید به گونه‌ای تعریف شوند که در صورت عدم ارائه مقدار، `None` را بپذیرند. _(verify: static)_
31. روتر `feedback` در `app/main.py` به درستی شامل شده باشد و در `_API_PREFIXES` اضافه شده باشد. _(verify: static)_
32. مدل `User` در `app/models/user.py` باید شامل فیلدهای `sentiment_score` و `behavior_analysis` باشد. _(verify: static)_
33. شمای `UserResponse` در `app/schemas/user_schema.py` باید فیلدهای `sentiment_score` و `behavior_analysis` را شامل شود. _(verify: static)_
34. در زمان startup، جدول `users` باید با فیلدهای `sentiment_score` (FLOAT) و `behavior_analysis` (TEXT) به‌روزرسانی شود (اگر وجود نداشته باشند). _(verify: backend_test)_
35. endpoint `POST /api/users/{user_id}/analyze_behavior` باید با موفقیت پاسخ 200 برگرداند و پروفایل کاربر را با نتایج تحلیل AI به‌روزرسانی کند (با فرض نقش ADMIN). _(verify: api_response)_
36. تابع `analyze_user_text_for_sentiment_and_behavior` در `app/services/ai/nlp_service.py` باید یک دیکشنری با `sentiment_score` و `behavior_analysis` برگرداند. _(verify: backend_test)_
37. فراخوانی endpoint تحلیل رفتار کاربر توسط یک کاربر غیر-ADMIN باید با خطای 403 مواجه شود. _(verify: api_response)_
38. Schema `BehaviorAnalysisInput` در `app/schemas/ai_schema.py` با فیلدهای `user_id` (int) و `text_to_analyze` (str) تعریف شده باشد. _(verify: static)_
39. Schema `BehaviorAnalysisResult` در `app/schemas/ai_schema.py` با فیلدهای `score` (int), `good_actions` (List[str]), `bad_actions` (List[str]) و `analysis_summary` (str) تعریف شده باشد. _(verify: static)_
40. فایل `app/services/ai/behavior_analysis_service.py` ایجاد شده و شامل تابعی به نام `analyze_user_behavior` باشد که `db`, `user_id`, `text` را به عنوان ورودی می‌گیرد و `BehaviorAnalysisResult` را برمی‌گرداند. _(verify: static)_
41. یک endpoint جدید `POST /api/ai/analyze-behavior` در `app/routes/ai.py` تعریف شده باشد که `BehaviorAnalysisInput` را دریافت کرده و `BehaviorAnalysisResult` را برگرداند. _(verify: api_response)_
42. Endpoint `POST /api/ai/analyze-behavior` قبل از اجرای منطق تحلیل، وضعیت `settings.FEATURE_AI_ENABLED` را بررسی کند و در صورت غیرفعال بودن، `HTTPException` با کد 403 برگرداند. _(verify: backend_test)_
43. منطق داخلی `analyze_user_behavior` شامل یک placeholder برای تحلیل NLP و امتیازدهی باشد که در آینده قابل توسعه است. _(verify: static)_
44. مدل `UserBehaviorAnalysisResult` در دیتابیس ایجاد شود و شامل فیلدهای `user_id`, `analysis_type`, `score`, `category`, `details` و `created_at` باشد. _(verify: static)_
45. شمای `UserBehaviorAnalysisResultCreate` و `UserBehaviorAnalysisResultResponse` با فیلدهای مناسب برای ورودی و خروجی API تعریف شوند. _(verify: static)_
46. endpoint `POST /api/ai/behavior_analysis` با موفقیت داده‌های تحلیل هوش مصنوعی را دریافت و در دیتابیس ذخیره کند و پاسخ 201 با داده‌های ذخیره‌شده را برگرداند. _(verify: api_response)_
47. نتایج ذخیره‌شده به کاربر احراز هویت شده مرتبط باشند و بتوان آن‌ها را از طریق مدل `User` بازیابی کرد. _(verify: backend_test)_
48. منطق مربوط به "نوع رابطه" (relationship type) در هیچ یک از فایل‌های جدید یا تغییریافته این تسک وجود نداشته باشد. _(verify: static)_
49. منطق `ALTER TABLE ADD COLUMN IF NOT EXISTS` برای جدول `user_behavior_analysis_results` در `app/main.py` در `startup_event` اضافه شود تا سازگاری با محیط‌های بدون Alembic حفظ شود. _(verify: static)_
50. مدل دیتابیس `Contact` با فیلدهای `id`, `user_id`, `name`, `relationship_type`, `ai_score` و `last_interaction_summary` در `app/models/contact.py` تعریف شده باشد. _(verify: static)_
51. مدل دیتابیس `Interaction` با فیلدهای `id`, `contact_id`, `user_id`, `type`, `description`, `timestamp` و `ai_analysis_score` در `app/models/interaction.py` تعریف شده باشد. _(verify: static)_
52. endpoint `POST /api/contacts` با موفقیت یک `Contact` جدید ایجاد کند و پاسخ 201 برگرداند. _(verify: api_response)_
53. endpoint `POST /api/contacts/{contact_id}/interactions` با موفقیت یک `Interaction` جدید برای یک `Contact` موجود ثبت کند و پاسخ 201 برگرداند. _(verify: api_response)_
54. متد `adjust_relationship_type` در `ContactService`، `relationship_type` و `ai_score` یک `Contact` را بر اساس تعاملات به‌روزرسانی کند. _(verify: backend_test)_
55. روتر `contacts.router` در `app/main.py` به برنامه FastAPI اضافه شده باشد. _(verify: static)_
56. فایل کامپوننت `PeopleProfile.jsx` در مسیر `frontend/src/pages/` ایجاد شده باشد و کامپوننت `PeopleProfile` را export کند. _(verify: static)_
57. مسیر `/profile/:id` در `frontend/src/App.jsx` تعریف شده باشد و کامپوننت `PeopleProfile` را رندر کند. _(verify: static)_
58. با ناوبری به `/profile/123` (یا هر ID دیگر)، صفحه پروفایل با عنوان 'صفحه پروفایل فردی' و بخش‌های 'اطلاعات پایه'، 'امتیازات'، 'رفتارها' و 'تاریخچه' (با متن placeholder) نمایش داده شود. _(verify: ui_interaction)_
59. کامپوننت `PeopleProfile.jsx` در این مرحله هیچ فراخوانی API (مانند `fetch` یا `axios`) برای دریافت داده‌های پویا نداشته باشد. _(verify: static)_
60. کامپوننت `PeopleProfile` از `Layout` (از `frontend/src/components/Layout.jsx`) برای ساختار کلی صفحه استفاده کند. _(verify: static)_
61. ستون‌های `ai_score` (Integer) و `user_comments` (Text) در جدول `users` در پایگاه داده وجود داشته باشند. _(verify: static)_
62. فراخوانی `GET /api/users/{user_id}` باید در پاسخ خود فیلدهای `ai_score` و `user_comments` را شامل شود. _(verify: api_response)_
63. یک صفحه پروفایل کاربر (مثلاً در مسیر `/users/:userId`) باید امتیاز هوش مصنوعی و نظرات کاربر را نمایش دهد. _(verify: ui_interaction)_
64. کاربر مجاز (خود کاربر یا ادمین) باید بتواند نظرات خود را از طریق UI در صفحه پروفایل ویرایش و ذخیره کند و تغییرات در بک‌اند پایدار بماند. _(verify: ui_interaction)_
65. endpoint `POST /api/people` باید با موفقیت یک شخص جدید برای ردیابی ایجاد کند و کد وضعیت 201 را برگرداند. _(verify: api_response)_
66. endpoint `POST /api/people/{person_id}/behavior` باید با موفقیت یک لاگ رفتار اضافه کند و کد وضعیت 201 را برگرداند. _(verify: api_response)_
67. endpoint `PATCH /api/people/{person_id}/relationship` باید با موفقیت نوع رابطه را به‌روزرسانی کند و کد وضعیت 200 را برگرداند. _(verify: api_response)_
68. یک لینک ناوبری جدید با متن 'People' یا مشابه آن در کامپوننت `Sidebar` قابل مشاهده باشد. _(verify: ui_interaction)_
69. با ناوبری به `/people/{person_id}`، صفحه جزئیات شخص باید نام شخص، نوع رابطه و لیستی از رفتارهای ثبت‌شده را نمایش دهد. _(verify: ui_interaction)_
70. کاربر بتواند یک لاگ رفتار جدید را از صفحه جزئیات شخص اضافه کند. _(verify: ui_interaction)_
71. مدل SQLAlchemy جدید `UserComment` با فیلدهای `comment_text`, `commenter_id`, `target_user_id`, `task_id` (اختیاری), `created_at`, `updated_at` در `app/models/user_comment.py` تعریف شده باشد. _(verify: static)_
72. شمای Pydantic `UserCommentCreate` و `UserCommentResponse` در `app/schemas/user_comment_schema.py` تعریف شده باشند. _(verify: static)_
73. یک endpoint `POST /api/user-comments/` در بک‌اند وجود داشته باشد که با `UserCommentCreate` ورودی گرفته و `UserCommentResponse` برگرداند و نیاز به احراز هویت داشته باشد. _(verify: api_response)_
74. یک صفحه جدید در فرانت‌اند (مثلاً `/record-comment`) شامل فرمی برای ثبت نظرات کاربران قابل دسترسی باشد. _(verify: ui_interaction)_
75. فرم ثبت نظر در فرانت‌اند امکان انتخاب کاربر هدف و وارد کردن متن نظر را فراهم کند و با موفقیت داده‌ها را به `POST /api/user-comments/` ارسال کند. _(verify: ui_interaction)_
76. رابط کاربری جدید هیچ بخش مربوط به نمایش تاریخچه یا تحلیل‌های قبلی نظرات را شامل نشود. _(verify: manual_only)_
77. مدل `ActivityLog` در `app/models/activity_log.py` تعریف شده و شامل فیلدهای `user_id`, `event_type`, `description`, `timestamp` و `metadata` (JSONB) باشد. _(verify: static)_
78. endpoint GET `/api/users/me/activity_log` در `app/routes/users.py` پیاده‌سازی شده و لاگ فعالیت‌های کاربر احراز هویت شده را برگرداند. _(verify: api_response)_
79. صفحه `ProfilePage.jsx` در `frontend/src/pages/` ایجاد شده و شامل کامپوننت `ActivityLogTimeline.jsx` باشد. _(verify: static)_
80. رویدادهای مهم (مانند ایجاد تسک، به‌روزرسانی پروژه، تکمیل آیتم خودسازی) در بک‌اند به درستی در `ActivityLog` ثبت شوند. _(verify: backend_test)_
81. یک لینک 'پروفایل' در `Header.jsx` یا `Sidebar.jsx` وجود داشته باشد که به مسیر `/profile` هدایت کند. _(verify: ui_interaction)_
82. صفحه پروفایل (مسیر `/profile`) برای کاربران احراز هویت نشده قابل دسترسی نباشد و به صفحه ورود هدایت شود. _(verify: ui_interaction)_
83. مدل `User` در `app/models/user.py` باید شامل ستون جدید `aggregated_task_summary` از نوع `Text` باشد. _(verify: static)_
84. شمای `UserResponse` در `app/schemas/user_schema.py` باید شامل فیلد `aggregated_task_summary: Optional[str] = None` باشد. _(verify: static)_
85. endpoint `GET /api/users/{user_id}` باید پاسخ `200 OK` حاوی فیلد `aggregated_task_summary` را برگرداند که با یک رشته خلاصه مرتبط (مثلاً «X وظیفه تکمیل شده، Y در انتظار») پر شده باشد. _(verify: api_response)_
86. کامپوننت `frontend/src/pages/Dashboard.jsx` باید فیلد `aggregated_task_summary` را از پاسخ API واکشی کرده و در یک بخش اختصاصی نمایش دهد. _(verify: ui_interaction)_
87. ایجاد وظایف جدید و تکمیل آن‌ها برای یک کاربر باید به‌طور دقیق در `aggregated_task_summary` نمایش داده شده در داشبورد آن کاربر پس از رفرش منعکس شود. _(verify: manual_only)_

## Task Steps

### Step 1: ایجاد صفحه پروفایل افراد (People Profile Page) در برنامه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک صفحه جدید در برنامه با عنوان 'پروفایل افراد' است. این صفحه باید لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رویدادها یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام صفحه باید دقیقاً 'پروفایل افراد' باشد و از داده‌های موجود در برنامه برای نمایش افراد استفاده کند.
**Excerpt:**
```
یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
```

### Step 2: طراحی و پیاده‌سازی مدل داده برای پروفایل افراد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات پروفایل افراد است. این مدل باید شامل فیلدهایی برای نام، شناسه، امتیاز، تاریخچه رویدادها، و نوع رابطه باشد. این مرحله فقط شامل ساختار داده است و شامل منطق تجاری یا UI نمی‌شود. نکته حیاتی: مدل باید قابلیت ذخیره‌سازی رویدادهای خوب و بد را داشته باشد.
**Excerpt:**
```
کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه
```

### Step 3: پیاده‌سازی سیستم ثبت رویدادها (Event Logging) برای افراد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی سیستمی برای ثبت رویدادهای مرتبط با هر فرد است. این رویدادها شامل کارهای خوب و بدی است که فرد انجام داده است. هر رویداد باید شامل جزئیات کامل (تاریخ، توضیحات، نوع رویداد) باشد. این مرحله فقط شامل ثبت رویدادها است و شامل آنالیز یا امتیازدهی نمی‌شود. نکته حیاتی: رویدادها باید به مرور زمان به پروفایل فرد اضافه شوند.
**Excerpt:**
```
کارای بد و خوبش ثبت می‌شه. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه
```

### Step 4: پیاده‌سازی سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک سیستم هوش مصنوعی است که بر اساس داده‌های ثبت‌شده (رویدادها، لحن صحبت، نوع تعاملات) به هر فرد یک امتیاز می‌دهد. این امتیاز باید به صورت خودکار و با آنالیز داده‌های موجود محاسبه شود. این مرحله فقط شامل منطق امتیازدهی است و شامل UI یا ثبت رویدادها نمی‌شود. نکته حیاتی: امتیاز باید بر اساس 'لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن' نیز محاسبه شود.
**Excerpt:**
```
هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن
```

### Step 5: پیاده‌سازی سیستم تنظیم نوع رابطه (Relationship Adjustment) بر اساس امتیاز
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی منطقی است که بر اساس امتیاز هوش مصنوعی و رویدادهای ثبت‌شده، نوع رابطه کاربر با هر فرد را تنظیم می‌کند. این تنظیم شامل مواردی مانند 'کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم' است. این مرحله فقط شامل منطق تنظیم رابطه است و شامل UI یا ثبت رویدادها نمی‌شود. نکته حیاتی: این سیستم باید از فراموشی کارهای خوب و بد جلوگیری کند.
**Excerpt:**
```
نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره
```

### Step 6: طراحی و پیاده‌سازی UI برای نمایش امتیاز و نوع رابطه در پروفایل افراد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی بخش UI در صفحه پروفایل افراد است که امتیاز هوش مصنوعی و نوع رابطه را نمایش می‌دهد. این بخش باید به صورت بصری و قابل فهم برای کاربر باشد. این مرحله فقط شامل UI است و شامل منطق امتیازدهی یا تنظیم رابطه نمی‌شود. نکته حیاتی: UI باید به کاربر کمک کند تا 'کجا بهم خوبی کرد' و 'کجا باید کمش بذارم' را ببیند.
**Excerpt:**
```
کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم
```

### Step 7: پیاده‌سازی قابلیت ثبت نظر شخصی کاربر (Manual Review) برای هر فرد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی قابلیتی است که به کاربر اجازه می‌دهد نظر شخصی خود را درباره هر فرد ثبت کند. این نظر می‌تواند بر اساس 'لحن من و نوعی که در نوع صحبتی که دربارمون دارن' باشد. این نظر باید در محاسبه امتیاز هوش مصنوعی نیز تأثیر بگذارد. این مرحله فقط شامل ثبت نظر است و شامل آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نظر شخصی کاربر باید به عنوان یک ورودی برای سیستم امتیازدهی در نظر گرفته شود.
**Excerpt:**
```
یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن
```

### Step 8: پیاده‌سازی سیستم جلوگیری از فراموشی (Forgetfulness Prevention) با نمایش تاریخچه کامل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی سیستمی است که تاریخچه کامل رویدادها و نظرات را در پروفایل هر فرد نمایش می‌دهد تا کاربر دچار فراموشی نشود. این سیستم باید به کاربر کمک کند تا 'با یه کار خوبش هزار تا کار بدی که کرده رو فراموش نکنم'. این مرحله فقط شامل نمایش تاریخچه است و شامل منطق امتیازدهی نمی‌شود. نکته حیاتی: تاریخچه باید به صورت زمانی و با جزئیات کامل نمایش داده شود.
**Excerpt:**
```
باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً لزومی نداره خیلی کینه بگیرم
```

### Step 9: پیاده‌سازی قابلیت 'جبران و انتقام' (Compensation & Revenge) در پروفایل افراد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی قابلیتی است که به کاربر اجازه می‌دهد برای کارهای خوب و بد افراد، اقدامات جبرانی یا انتقامی را ثبت و پیگیری کند. این قابلیت باید به کاربر کمک کند تا 'کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم'. این مرحله فقط شامل ثبت و پیگیری اقدامات است و شامل منطق امتیازدهی نمی‌شود. نکته حیاتی: این قابلیت باید به صورت یک لیست وظایف (To-Do List) در پروفایل فرد نمایش داده شود.
**Excerpt:**
```
کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم
```

### Step 10: تست و اعتبارسنجی یکپارچه سیستم پروفایل افراد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تست و اعتبارسنجی کامل سیستم پروفایل افراد است. این تست شامل بررسی صحت ثبت رویدادها، محاسبه امتیاز هوش مصنوعی، تنظیم نوع رابطه، نمایش UI، و قابلیت جبران و انتقام است. این مرحله فقط شامل تست است و شامل توسعه ویژگی جدید نمی‌شود. نکته حیاتی: تمام سناریوهای کاربری باید تست شوند.
**Excerpt:**
```
همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه
```
