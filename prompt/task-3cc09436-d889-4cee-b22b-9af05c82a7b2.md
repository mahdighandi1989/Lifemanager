---
task_id: 3cc09436-d889-4cee-b22b-9af05c82a7b2
title: توسعه سیستم مدیریت پروفایل و تحلیل رفتاری
type: other
priority: medium
execution_priority: 3300
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-27T21:23:41.404279+00:00'
updated_at: '2026-05-28T18:09:20.311002+00:00'
target_files:
- frontend/src/pages/PeopleProfile.jsx
- frontend/src/App.jsx
- app/routes/person.py
- app/models/person.py
- app/services/person_service.py
- app/routes/tasks.py
- app/routes/projects.py
- app/routes/p
- app
- app/services/notification_service.py
---

# توسعه سیستم مدیریت پروفایل و تحلیل رفتاری

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_793240_AgADfh4A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 6,961 char متن_

## هدف و چشم‌انداز پروژه  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## ویژگی‌ها و قابلیت‌های اصلی  _(at: audio/ogg)_

### ویژگی‌ها و قابلیت‌های اصلی

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن.
[00:09] اینجا مثلاً حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم حسب لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## مخاطب هدف و نیازها  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن. اینجا مثلاً عصب داده‌هایی که به ما به زمان برای کارای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که دربارمون دارن. این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه. که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً لزوماً نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## چالش‌ها و ریسک‌های اولیه  _(at: audio/ogg)_

### چالش‌ها و ریسک‌های اولیه

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مراحل بعدی و گام‌های اجرایی  _(at: audio/ogg)_

--- صفحه 1 ---
[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو موقع و زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مزیت رقابتی و تمایز  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اونجا مثلاً از روی داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا به مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## منابع و الزامات اولیه  _(at: audio/ogg)_

--- صفحه 1 ---
[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً عصب داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو موقع زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_793240_AgADfh4A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 6,961 char متن_

## هدف و چشم‌انداز پروژه  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## ویژگی‌ها و قابلیت‌های اصلی  _(at: audio/ogg)_

### ویژگی‌ها و قابلیت‌های اصلی

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن.
[00:09] اینجا مثلاً حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم حسب لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## مخاطب هدف و نیازها  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن. اینجا مثلاً عصب داده‌هایی که به ما به زمان برای کارای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که دربارمون دارن. این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه. که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً لزوماً نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## چالش‌ها و ریسک‌های اولیه  _(at: audio/ogg)_

### چالش‌ها و ریسک‌های اولیه

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مراحل بعدی و گام‌های اجرایی  _(at: audio/ogg)_

--- صفحه 1 ---
[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو موقع و زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مزیت رقابتی و تمایز  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اونجا مثلاً از روی داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا به مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## منابع و الزامات اولیه  _(at: audio/ogg)_

--- صفحه 1 ---
[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً عصب داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو موقع زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.
```

## 📋 چک‌لیست مراحل (10 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد صفحه پروفایل افراد (People Profile Page) در برنامه** — این مرحله شامل ایجاد یک صفحه جدید در برنامه با عنوان 'پروفایل افراد' است. این صفحه باید لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رفتار، یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام صفح
- [ ] **مرحله 2: طراحی و پیاده‌سازی مدل داده برای ذخیره‌سازی اطلاعات افراد و تعاملات** — این مرحله شامل طراحی و پیاده‌سازی یک مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات مربوط به هر فرد (نام، شناسه، و غیره) و همچنین تاریخچه تعاملات با آن فرد است. این مدل باید قابلیت ذخیره‌سازی رویدادها، نظرات، امتیازات و نوع رابطه را داشته باشد. نکته حیاتی: مدل باید به گونه‌ای طراحی شود که بت
- [ ] **مرحله 3: پیاده‌سازی قابلیت ثبت دستی نظر توسط کاربر درباره یک فرد** — این مرحله شامل ایجاد یک فرم یا بخش در صفحه پروفایل افراد است که به کاربر اجازه می‌دهد به صورت دستی یک نظر (مثبت یا منفی) درباره یک فرد ثبت کند. این نظر باید در مدل داده ذخیره شود. نکته حیاتی: کاربر باید بتواند نظر خود را بر اساس لحن و نوع صحبت فرد (که در متن ذکر شده) ثبت کند، اما تحلیل لحن در این مر
- [ ] **مرحله 4: پیاده‌سازی قابلیت ثبت خودکار تعاملات (رویدادها) توسط سیستم** — این مرحله شامل ایجاد مکانیزمی برای ثبت خودکار تعاملات کاربر با افراد (مانند انجام یک کار مشترک، مکالمه، و غیره) در پروفایل آن فرد است. این تعاملات باید به صورت خودکار و بر اساس داده‌های موجود در برنامه (مثلاً تسک‌ها، پروژه‌ها) ثبت شوند. نکته حیاتی: این مرحله فقط ثبت رویداد است و شامل تحلیل یا امتیاز
- [ ] **مرحله 5: پیاده‌سازی سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد** — این مرحله شامل پیاده‌سازی یک ماژول هوش مصنوعی است که بر اساس داده‌های ثبت شده (تعاملات، نظرات کاربر، لحن و نوع صحبت) به هر فرد یک امتیاز (Score) اختصاص می‌دهد. این امتیاز باید به صورت پویا و با گذشت زمان به‌روزرسانی شود. نکته حیاتی: الگوریتم امتیازدهی باید بتواند بین کارهای خوب و بد تمایز قائل شود و
- [ ] **مرحله 6: پیاده‌سازی سیستم تنظیم خودکار نوع رابطه (Relationship Type) بر اساس امتیاز و تعاملات** — این مرحله شامل پیاده‌سازی منطقی است که بر اساس امتیاز هوش مصنوعی و تاریخچه تعاملات، نوع رابطه کاربر با هر فرد را به صورت خودکار تنظیم و به‌روزرسانی می‌کند (مثلاً 'دوست خوب'، 'نیاز به احتیاط'، 'باید جبران کنم'). نکته حیاتی: این سیستم باید پویا باشد و با تغییر امتیاز، نوع رابطه نیز تغییر کند.
- [ ] **مرحله 7: طراحی و پیاده‌سازی بخش 'تاریخچه تعاملات' در صفحه پروفایل فرد** — این مرحله شامل طراحی و پیاده‌سازی یک بخش در صفحه پروفایل هر فرد است که تمام تعاملات ثبت شده (خودکار و دستی) را به صورت یک لیست زمانی (Timeline) نمایش می‌دهد. هر آیتم باید شامل جزئیات کامل (تاریخ، نوع تعامل، نتیجه، نظر کاربر) باشد. نکته حیاتی: این بخش باید قابلیت فیلتر کردن بر اساس نوع تعامل (خوب/بد)
- [ ] **مرحله 8: پیاده‌سازی قابلیت 'یادآوری' برای تعاملات مهم (کارهای خوب و بد)** — این مرحله شامل ایجاد یک سیستم یادآوری است که به کاربر اطلاع می‌دهد که یک فرد خاص کار خوب یا بدی برای او انجام داده است که نباید فراموش شود. این یادآوری‌ها می‌توانند به صورت نوتیفیکیشن یا در یک بخش خاص در صفحه پروفایل نمایش داده شوند. نکته حیاتی: این سیستم باید بر اساس اهمیت تعامل (امتیاز بالاتر) اول
- [ ] **مرحله 9: طراحی و پیاده‌سازی بخش 'پیشنهادات عملی' بر اساس نوع رابطه و تاریخچه** — این مرحله شامل ایجاد یک بخش در صفحه پروفایل فرد است که بر اساس نوع رابطه و تاریخچه تعاملات، پیشنهادات عملی به کاربر می‌دهد (مثلاً 'این فرد به شما کمک کرده است، شاید بهتر باشد از او تشکر کنید' یا 'این فرد به شما بدی کرده است، مراقب باشید'). نکته حیاتی: این پیشنهادات باید هوشمندانه و بر اساس تحلیل داد
- [ ] **مرحله 10: پیاده‌سازی قابلیت 'تحلیل لحن' (Tone Analysis) برای نظرات ثبت شده** — این مرحله شامل پیاده‌سازی یک ماژول تحلیل متن (احتمالاً با استفاده از یک API یا کتابخانه NLP) است که لحن و نوع صحبت (مثبت، منفی، خنثی) را در نظرات ثبت شده توسط کاربر یا دیگران تحلیل می‌کند. نتیجه این تحلیل باید در امتیازدهی هوش مصنوعی و تنظیم نوع رابطه تأثیرگذار باشد. نکته حیاتی: این تحلیل باید به صو

---

# 🔹 مرحله 1: ایجاد صفحه پروفایل افراد (People Profile Page) در برنامه

**Scope:** این مرحله شامل ایجاد یک صفحه جدید در برنامه با عنوان 'پروفایل افراد' است. این صفحه باید لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رفتار، یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام صفحه باید دقیقاً 'پروفایل افراد' باشد و از داده‌های موجود در برنامه برای نمایش لیست افراد استفاده کند.
**Key terms:** پروفایل افراد, People Profile Page, صفحه پروفایل, لیست افراد

**بخش مربوط از متن کاربر:**
```
یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد صفحه پروفایل افراد (People Profile Page) با مسیریابی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/pages/PeopleProfile.jsx:1-50` — `PeopleProfile (new component)` — این فایل جدید باید ایجاد شود. از api.js برای fetch استفاده کند و لیست افراد را نمایش دهد.
- `frontend/src/App.jsx:1-30` — `App component with routes` — مسیر جدید /people-profile باید به Routes اضافه شود.
  ```jsx
  import { BrowserRouter, Routes, Route } from 'react-router-dom';
  import PeopleProfile from './pages/PeopleProfile';
  ```
- `app/routes/person.py:1-30` — `person router` — این روتر endpoint GET /api/persons را ارائه می‌دهد که frontend از آن استفاده خواهد کرد.
  ```python
  from fastapi import APIRouter, Depends
  from app.dependencies.auth import get_current_user
  from app.services.person_service import PersonService
  
  router = APIRouter(prefix='/api/persons', tags=['persons'])
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/lib/api.js` — این فایل تابع api.get را برای فراخوانی endpoint /api/persons فراهم می‌کند. People

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک صفحه جدید در برنامه با عنوان 'پروفایل افراد' (People Profile Page) را دارد. این صفحه باید لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رفتار، یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام صفحه باید دقیقاً 'پروفایل افراد' باشد و از داده‌های موجود در برنامه برای نمایش لیست افراد استفاده کند. متن خام کاربر: 'یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.' کلیدواژه‌ها: پروفایل افراد, People Profile Page, صفحه پروفایل, لیست افراد. در کد فعلی، مدل Person در `app/models/person.py` وجود دارد و روتر person در `app/routes/person.py` تعریف شده است. همچنین صفحه‌های موجود در `frontend/src/pages/` شامل Dashboard.jsx, Home.jsx, ListDetail.jsx, Lists.jsx, Login.jsx, Notifications.jsx, Projects.jsx, Register.jsx, Tasks.jsx هستند. مسیریابی در `frontend/src/App.jsx` با react-router-dom انجام می‌شود. برای پیاده‌سازی، باید یک صفحه جدید `PeopleProfile.jsx` در `frontend/src/pages/` ایجاد شود، مسیر `/people-profile` به `App.jsx` اضافه شود، و از داده‌های endpoint موجود (GET /api/persons) برای نمایش لیست افراد استفاده شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل `frontend/src/pages/PeopleProfile.jsx` با کامپوننت PeopleProfile که از `frontend/src/lib/api.js` برای فراخوانی GET /api/persons استفاده می‌کند و لیست افراد را به صورت کارت نمایش می‌دهد. 2. افزودن مسیر `/people-profile` در `frontend/src/App.jsx` با import کامپوننت PeopleProfile و استفاده از `<Route path="/people-profile" element={<PeopleProfile />} />`. 3. افزودن لینک به صفحه پروفایل افراد در `frontend/src/components/Sidebar.jsx` یا `frontend/src/components/Header.jsx` با عنوان 'پروفایل افراد'. 4. اطمینان از اینکه endpoint GET /api/persons در `app/routes/person.py` داده‌های لازم (id, name, email, phone, relationship) را برمی‌گرداند. 5. ایجاد یک کامپوننت کارت ساده برای نمایش هر فرد با نام و اطلاعات پایه.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: small

---

# 🔹 مرحله 2: طراحی و پیاده‌سازی مدل داده برای ذخیره‌سازی اطلاعات افراد و تعاملات

**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات مربوط به هر فرد (نام، شناسه، و غیره) و همچنین تاریخچه تعاملات با آن فرد است. این مدل باید قابلیت ذخیره‌سازی رویدادها، نظرات، امتیازات و نوع رابطه را داشته باشد. نکته حیاتی: مدل باید به گونه‌ای طراحی شود که بتواند داده‌های ورودی از منابع مختلف (کاربر، هوش مصنوعی) را ذخیره کند.
**Key terms:** مدل داده, دیتابیس, دیتاکلاس, Person, Interaction, RelationshipType, Score

**بخش مربوط از متن کاربر:**
```
حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار... این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی و پیاده‌سازی مدل داده Person و Interaction با قابلیت ذخیره‌سازی از منابع مختلف

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی و پیاده‌سازی یک مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات افراد (Person) و تاریخچه تعاملات با آن‌ها را دارد. این مدل باید شامل فیلدهای نام، شناسه، رویدادها، نظرات، امتیازات و نوع رابطه (RelationshipType) باشد. نکته حیاتی: مدل باید بتواند داده‌های ورودی از منابع مختلف (کاربر، هوش مصنوعی) را ذخیره کند. بر اساس متن کاربر: «حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار... این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.» کلیدواژه‌های اصلی: مدل داده, دیتابیس, دیتاکلاس, Person, Interaction, RelationshipType, Score. در کد فعلی پروژه، فایل `app/models/person.py` وجود دارد (مسیر واقعی: `app/models/person.py`) که مدل Person را تعریف می‌کند. همچنین فایل `app/routes/person.py` (مسیر واقعی: `app/routes/person.py`) روترهای API مربوط به افراد را شامل می‌شود. فایل `app/schemas/person_schema.py` (مسیر واقعی: `app/schemas/person_schema.py`) برای اعتبارسنجی Pydantic وجود دارد. فایل `app/services/person_service.py` (مسیر واقعی: `app/services/person_service.py`) سرویس‌های منطق تجاری را پیاده‌سازی می‌کند. همچنین فایل `app/models/__init__.py` (مسیر واقعی: `app/models/__init__.py`) مدل‌ها را export می‌کند. فایل `app/database.py` (مسیر واقعی: `app/database.py`) برای اتصال دیتابیس استفاده می‌شود. فایل `app/routes/__init__.py` (مسیر واقعی: `app/routes/__init__.py`) روترها را export می‌کند. فایل `app/main.py` (مسیر واقعی: `app/main.py`) در خط 592 روتر person را mount کرده است: `app.include_router(person.router)`. فایل `app/services/__init__.py` (مسیر واقعی: `app/services/__init__.py`) سرویس‌ها را export می‌کند. فایل `app/schemas/__init__.py` (مسیر واقعی: `app/schemas/__init__.py`) schemaها را export می‌کند. فایل `app/models/user.py` (مسیر واقعی: `app/models/user.py`) مدل User را تعریف می‌کند که می‌تواند با Person رابطه داشته باشد. فایل `app/models/task.py` (مسیر واقعی: `app/models/task.py`) مدل Task را تعریف می‌کند که می‌تواند به Person مرتبط شود. فایل `app/models/project.py` (مسیر واقعی: `app/models/project.py`) مدل Project را تعریف می‌کند. فایل `app/models/notification.py` (مسیر واقعی: `app/models/notification.py`) مدل Notification را تعریف می‌کند. فایل `app/models/integration.py` (مسیر واقعی: `app/models/integration.py`) مدل Integration را تعریف می‌کند. فایل `app/models/ai_model_config.py` (مسیر واقعی: `app/models/ai_model_config.py`) مدل AI Model Config را تعریف می‌کند. فایل `app/models/ai_provider.py` (مسیر واقعی: `app/models/ai_provider.py`) مدل AI Provider را تعریف می‌کند. فایل `app/models/local_file_entry.py` (مسیر واقعی: `app/models/local_file_entry.py`) مدل Local File Entry را تعریف می‌کند. فایل `app/models/self_improvement.py` (مسیر واقعی: `app/models/self_

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: پیاده‌سازی قابلیت ثبت دستی نظر توسط کاربر درباره یک فرد

**Scope:** این مرحله شامل ایجاد یک فرم یا بخش در صفحه پروفایل افراد است که به کاربر اجازه می‌دهد به صورت دستی یک نظر (مثبت یا منفی) درباره یک فرد ثبت کند. این نظر باید در مدل داده ذخیره شود. نکته حیاتی: کاربر باید بتواند نظر خود را بر اساس لحن و نوع صحبت فرد (که در متن ذکر شده) ثبت کند، اما تحلیل لحن در این مرحله انجام نمی‌شود.
**Key terms:** نظر, ثبت دستی, فرم نظر, Comment, Feedback

**بخش مربوط از متن کاربر:**
```
یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن... کارای بد و خوبش ثبت می‌شه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن فرم ثبت دستی نظر کاربر درباره یک فرد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-30` — `Person model` — مدل Person موجود — باید مدل PersonComment به این فایل اضافه شود
  ```python
  class Person(Base):
      __tablename__ = "persons"
      id = Column(Integer, primary_key=True, index=True)
      name = Column(String, nullable=False)
      ...
  ```
- `app/routes/person.py:1-50` — `person router` — روتر موجود برای افراد — باید endpointهای comment به اینجا اضافه شود
  ```python
  router = APIRouter(prefix="/persons", tags=["persons"])
  
  @router.get("/")
  async def list_persons(...):
      ...
  ```
- `app/services/person_service.py:1-30` — `person service` — سرویس موجود — باید متدهای create_comment و get_comments اضافه شود
  ```python
  class PersonService:
      async def get_persons(self, db: AsyncSession):
          ...
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/schemas/person_schema.py` (سطر 1) — نیاز به schemaهای PersonCommentCreate و PersonCommentResponse
- `frontend/src/pages/ListDetail.jsx` (سطر 1) — صفحه جزئیات فرد — محل افزودن فرم نظر و لیست نظرات
- `frontend/src/lib/api.js` — نیاز به توابع API

## 🔍 Context و وضعیت فعلی
پیاده‌سازی قابلیت ثبت دستی نظر توسط کاربر درباره یک فرد. این مرحله شامل ایجاد یک فرم یا بخش در صفحه پروفایل افراد است که به کاربر اجازه می‌دهد به صورت دستی یک نظر (مثبت یا منفی) درباره یک فرد ثبت کند. این نظر باید در مدل داده ذخیره شود. نکته حیاتی: کاربر باید بتواند نظر خود را بر اساس لحن و نوع صحبت فرد (که در متن ذکر شده) ثبت کند، اما تحلیل لحن در این مرحله انجام نمی‌شود.

بخش مربوط از درخواست اصلی کاربر:
"یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن... کارای بد و خوبش ثبت می‌شه"

کلیدواژه‌ها: نظر, ثبت دستی, فرم نظر, Comment, Feedback

شواهد در کد: فایل app/routes/person.py (deep-read شده) احتمالاً شامل endpointهای CRUD برای افراد است. مدل Person در app/models/person.py تعریف شده. فایل frontend/src/pages/ListDetail.jsx (deep-read شده) می‌تواند برای افزودن بخش نظر استفاده شود. فایل app/services/person_service.py (در ساختار پروژه موجود) سرویس مربوطه است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل PersonComment در app/models/person.py با فیلدهای: id, person_id (FK به Person), user_id (FK به User), content (TEXT), sentiment (VARCHAR(10) برای 'positive'/'negative'), created_at (TIMESTAMP).
2. ایجاد schema PersonCommentCreate و PersonCommentResponse در app/schemas/person_schema.py.
3. افزودن endpoint POST /api/persons/{person_id}/comments در app/routes/person.py.
4. افزودن endpoint GET /api/persons/{person_id}/comments برای دریافت نظرات.
5. ایجاد تابع create_comment و get_comments در app/services/person_service.py.
6. افزودن بخش UI در frontend/src/pages/ListDetail.jsx برای نمایش فرم ثبت نظر و لیست نظرات.
7. افزودن کامپوننت CommentForm.jsx و CommentList.jsx در frontend/src/components/.
8. به‌روزرسانی frontend/src/lib/api.js با تابع createComment و getComments.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: پیاده‌سازی قابلیت ثبت خودکار تعاملات (رویدادها) توسط سیستم

**Scope:** این مرحله شامل ایجاد مکانیزمی برای ثبت خودکار تعاملات کاربر با افراد (مانند انجام یک کار مشترک، مکالمه، و غیره) در پروفایل آن فرد است. این تعاملات باید به صورت خودکار و بر اساس داده‌های موجود در برنامه (مثلاً تسک‌ها، پروژه‌ها) ثبت شوند. نکته حیاتی: این مرحله فقط ثبت رویداد است و شامل تحلیل یا امتیازدهی نمی‌شود.
**Key terms:** ثبت خودکار, تعامل, رویداد, Event, Interaction Log, Activity

**بخش مربوط از متن کاربر:**
```
از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار... تمام جزئیات کارا ثبت بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد Event Log برای ثبت خودکار تعاملات با افراد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `class Person` — مدل Person در این فایل تعریف شده است. باید relationship جدیدی به EventLog اضافه شود.
  ```python
  مدل Person فعلی که باید EventLog به آن مرتبط شود.
  ```
- `app/routes/tasks.py:100-150` — `update_task` — باید بعد از به‌روزرسانی موفق Task، اگر status='done' شد و task دارای person_id بود، log_event فراخوانی شود.
  ```python
  Endpoint به‌روزرسانی Task که باید بعد از تغییر status به 'done'، EventLog ثبت کند.
  ```
- `app/routes/projects.py:80-130` — `update_project` — باید بعد از به‌روزرسانی موفق Project، اگر status='completed' شد و project دارای person_id بود، log_event فراخوانی شود.
  ```python
  Endpoint به‌روزرسانی Project که باید بعد از تغییر status به 'completed'، EventLog ثبت کند.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
پیاده‌سازی قابلیت ثبت خودکار تعاملات (رویدادها) توسط سیستم. این مرحله شامل ایجاد مکانیزمی برای ثبت خودکار تعاملات کاربر با افراد (مانند انجام یک کار مشترک، مکالمه، و غیره) در پروفایل آن فرد است. این تعاملات باید به صورت خودکار و بر اساس داده‌های موجود در برنامه (مثلاً تسک‌ها، پروژه‌ها) ثبت شوند. نکته حیاتی: این مرحله فقط ثبت رویداد است و شامل تحلیل یا امتیازدهی نمی‌شود. از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار... تمام جزئیات کارا ثبت بشه. کلیدواژه‌ها: ثبت خودکار, تعامل, رویداد, Event, Interaction Log, Activity. در کد فعلی، مدل Person در `app/models/person.py` وجود دارد و روتر person در `app/routes/person.py` تعریف شده است. همچنین مدل Task در `app/models/task.py` و Project در `app/models/project.py` وجود دارند که می‌توانند به Person مرتبط شوند. هیچ مکانیزمی برای ثبت خودکار تعاملات بر اساس تغییرات Task/Project در Person وجود ندارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل جدید `EventLog` در `app/models/event_log.py` با فیلدهای: id, person_id (FK به Person), event_type (enum: task_completed, project_completed, call, meeting, message), description (text), related_entity_type (varchar: 'task', 'project'), related_entity_id (integer), created_at (timestamp). 2. ایجاد Pydantic schema در `app/schemas/event_log_schema.py`. 3. ایجاد سرویس `app/services/event_log_service.py` با تابع `log_event(person_id, event_type, description, related_entity_type, related_entity_id)`. 4. ایجاد روتر `app/routes/event_log.py` با endpoint های GET /api/event-logs (لیست رویدادها با فیلتر person_id) و POST /api/event-logs (ثبت دستی). 5. افزودن فراخوانی `log_event` در روترهای tasks و projects: در `app/routes/tasks.py` بعد از تغییر status به 'done' و در `app/routes/projects.py` بعد از تغییر status به 'completed'. 6. ایجاد migration در `migrations/versions/0012_event_log.py`. 7. افزودن نمایش EventLog در پروفایل Person در فرانت‌اند (فایل `frontend/src/pages/PersonDetail.jsx` که باید ساخته شود).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: پیاده‌سازی سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد

**Scope:** این مرحله شامل پیاده‌سازی یک ماژول هوش مصنوعی است که بر اساس داده‌های ثبت شده (تعاملات، نظرات کاربر، لحن و نوع صحبت) به هر فرد یک امتیاز (Score) اختصاص می‌دهد. این امتیاز باید به صورت پویا و با گذشت زمان به‌روزرسانی شود. نکته حیاتی: الگوریتم امتیازدهی باید بتواند بین کارهای خوب و بد تمایز قائل شود و تأثیر آنها را در طول زمان تعدیل کند.
**Key terms:** هوش مصنوعی, AI, امتیازدهی, Scoring, Score, آنالیز, Analysis

**بخش مربوط از متن کاربر:**
```
هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده... این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه... باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی ماژول AI Scoring برای افراد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `Person class` — مدل اصلی Person که باید فیلدهای ai_score و score_history به آن اضافه شود
  ```python
  class Person(Base):
      __tablename__ = "persons"
      id = Column(Integer, primary_key=True, index=True)
      name = Column(String, nullable=False)
      # ... other fields
  ```
- `app/services/person_service.py:1-100` — `PersonService class` — سرویس موجود برای مدیریت افراد که باید متدهای جدید برای به‌روزرسانی امتیاز به آن اضافه شود
  ```python
  class PersonService:
      def __init__(self, db: AsyncSession):
          self.db = db
      async def get_person(self, person_id: int) -> Person:
          ...
  ```
- `app/routes/p`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد را دارد. بر اساس متن کاربر: «هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده... این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه... باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه». این ماژول باید بر اساس داده‌های ثبت شده (تعاملات، نظرات کاربر، لحن و نوع صحبت) به هر فرد یک امتیاز (Score) اختصاص دهد. امتیاز باید به صورت پویا و با گذشت زمان به‌روزرسانی شود. الگوریتم امتیازدهی باید بتواند بین کارهای خوب و بد تمایز قائل شود و تأثیر آنها را در طول زمان تعدیل کند. کلیدواژه‌های اصلی: هوش مصنوعی, AI, امتیازدهی, Scoring, Score, آنالیز, Analysis. در کد فعلی، مدل Person در `app/models/person.py` وجود دارد و سرویس `app/services/person_service.py` برای مدیریت افراد. روتر `app/routes/person.py` endpoints مربوط به افراد را ارائه می‌دهد. همچنین سرویس‌های AI در `app/services/ai/` (شامل `nlp_service.py`, `model_service.py`, `provider_service.py`) وجود دارند که می‌توان از آن‌ها برای تحلیل متن و امتیازدهی استفاده کرد. مدل Person فاقد فیلد score و history_score است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلدهای `ai_score` (Float, default=0.0) و `score_history` (JSON, default=[]) به مدل Person در `app/models/person.py`. 2. ایجاد یک سرویس جدید `app/services/ai_scoring_service.py` که شامل: تابع `analyze_interaction(text: str) -> dict` برای تحلیل متن با استفاده از `app/services/ai/nlp_service.py` و استخراج sentiment, tone, keywords. تابع `calculate_score(current_score: float, interaction_data: dict, time_weight: float = 0.95) -> float` برای محاسبه امتیاز جدید با فرمول: `new_score = current_score * time_weight + interaction_score * (1 - time_weight)`. تابع `update_person_score(db: Session, person_id: int, interaction_text: str) -> Person` برای به‌روزرسانی امتیاز و ذخیره تاریخچه. 3. افزودن یک endpoint جدید در `app/routes/person.py`: `POST /api/persons/{person_id}/score` که متن تعامل را دریافت کرده و امتیاز را به‌روزرسانی می‌کند. 4. ایجاد یک task در `app/tasks.py` برای به‌روزرسانی دوره‌ای امتیازها با اعمال time decay. 5. به‌روزرسانی Pydantic schema در `app/schemas/person_schema.py` برای شامل شدن فیلد ai_score در response.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: پیاده‌سازی سیستم تنظیم خودکار نوع رابطه (Relationship Type) بر اساس امتیاز و تعاملات

**Scope:** این مرحله شامل پیاده‌سازی منطقی است که بر اساس امتیاز هوش مصنوعی و تاریخچه تعاملات، نوع رابطه کاربر با هر فرد را به صورت خودکار تنظیم و به‌روزرسانی می‌کند (مثلاً 'دوست خوب'، 'نیاز به احتیاط'، 'باید جبران کنم'). نکته حیاتی: این سیستم باید پویا باشد و با تغییر امتیاز، نوع رابطه نیز تغییر کند.
**Key terms:** نوع رابطه, Relationship Type, تنظیم خودکار, Dynamic Relationship, Friend, Caution, Debt

**بخش مربوط از متن کاربر:**
```
نوع رابطه من با اون تنظیم می‌شه... کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم
```

## 🎯 هدف (خلاصه ساختاریافته)
سیستم تنظیم خودکار نوع رابطه (Dynamic Relationship Type)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/routes/person.py:1-50` — `router` — روتر اصلی افراد — باید endpoint جدید برای به‌روزرسانی خودکار نوع رابطه اضافه شود
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.dependencies.auth import get_current_user
  from app.models.user import User
  from app.services.person_service import PersonService
  
  router = APIRouter(prefix="/api/persons", tags=["persons"])
  ```
- `app/services/person_service.py:1-30` — `PersonService` — سرویس اصلی افراد — باید متدهای جدید برای مدیریت امتیاز و نوع رابطه اضافه شود
  ```python
  class PersonService:
      def __init__(self, db: AsyncSession):
          self.db = db
  
      async def get_person(self, person_id: int) -> Person:
          ...
      async def update_person(self, person_id: int, data: dict) -> Person:
          ...
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
پیاده‌سازی سیستم تنظیم خودکار نوع رابطه (Relationship Type) بر اساس امتیاز هوش مصنوعی و تاریخچه تعاملات. این سیستم باید نوع رابطه کاربر با هر فرد را به صورت خودکار تنظیم و به‌روزرسانی کند (مثلاً 'دوست خوب'، 'نیاز به احتیاط'، 'باید جبران کنم'). نکته حیاتی: این سیستم باید پویا باشد و با تغییر امتیاز، نوع رابطه نیز تغییر کند.

بخش مربوط از درخواست اصلی کاربر:
"نوع رابطه من با اون تنظیم می‌شه... کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم"

کلیدواژه‌ها: نوع رابطه, Relationship Type, تنظیم خودکار, Dynamic Relationship, Friend, Caution, Debt

شواهد در کد واقعی پروژه:
- مدل Person در `app/models/person.py` (فایل deep-read نشده، اما در ساختار پروژه موجود است) احتمالاً فیلد relationship_type یا مشابه دارد.
- سرویس person_service در `app/services/person_service.py` منطق CRUD افراد را مدیریت می‌کند.
- روتر person در `app/routes/person.py` (deep-read شده) endpointهای مربوط به افراد را ارائه می‌دهد.
- مدل‌های امتیازدهی و تعاملات در `app/models/` احتمالاً نیاز به توسعه دارند.
- سیستم AI در `app/services/ai/nlp_service.py` می‌تواند برای تحلیل امتیاز و تعاملات استفاده شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی مدل Person در `app/models/person.py` و افزودن فیلدهای لازم برای ذخیره امتیاز و نوع رابطه.
2. ایجاد یک سرویس جدید `app/services/relationship_service.py` با منطق محاسبه امتیاز و تعیین نوع رابطه.
3. پیاده‌سازی تابع `calculate_relationship_score` که بر اساس تعاملات (مثبت/منفی) امتیاز را محاسبه کند.
4. پیاده‌سازی تابع `determine_relationship_type` که بر اساس امتیاز، نوع رابطه را تعیین کند (مثلاً: score > 80 → 'دوست خوب', score 50-80 → 'آشنا', score 20-50 → 'نیاز به احتیاط', score < 20 → 'باید جبران کنم').
5. افزودن endpoint جدید در `app/routes/person.py` برای به‌روزرسانی خودکار نوع رابطه.
6. یک task زمان‌بندی شده (Celery) برای بازبینی دوره‌ای و به‌روزرسانی انواع روابط.
7. به‌روزرسانی frontend در `frontend/src/pages/` برای نمایش نوع رابطه پویا.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: طراحی و پیاده‌سازی بخش 'تاریخچه تعاملات' در صفحه پروفایل فرد

**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک بخش در صفحه پروفایل هر فرد است که تمام تعاملات ثبت شده (خودکار و دستی) را به صورت یک لیست زمانی (Timeline) نمایش می‌دهد. هر آیتم باید شامل جزئیات کامل (تاریخ، نوع تعامل، نتیجه، نظر کاربر) باشد. نکته حیاتی: این بخش باید قابلیت فیلتر کردن بر اساس نوع تعامل (خوب/بد) را داشته باشد.
**Key terms:** تاریخچه تعاملات, Timeline, Interaction History, جزئیات, فیلتر, Filter

**بخش مربوط از متن کاربر:**
```
تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه... همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن بخش تاریخچه تعاملات (Timeline) به صفحه پروفایل فرد با قابلیت فیلتر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `Person model` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار پروژه، مدل Person در این مسیر است.
  ```python
  مدل Person در این فایل تعریف شده است. باید مدل Interaction را به این فایل اضافه کنیم.
  ```
- `app/routes/person.py:1-100` — `person router` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار پروژه، این فایل شامل مسیرهای CRUD برای Person است.
  ```python
  مسیرهای API مربوط به افراد در این فایل تعریف شده‌اند. باید مسیرهای جدید برای تعاملات اضافه شود.
  ```
- `app`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
طراحی و پیاده‌سازی یک بخش جدید در صفحه پروفایل هر فرد (Person Profile) که تمام تعاملات ثبت شده (خودکار و دستی) را به صورت یک لیست زمانی (Timeline) نمایش می‌دهد. هر آیتم باید شامل جزئیات کامل (تاریخ، نوع تعامل، نتیجه، نظر کاربر) باشد. نکته حیاتی: این بخش باید قابلیت فیلتر کردن بر اساس نوع تعامل (خوب/بد) را داشته باشد.

این درخواست بر اساس نیاز کاربر است که می‌گوید: «تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه... همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم».

کلیدواژه‌ها: تاریخچه تعاملات, Timeline, Interaction History, جزئیات, فیلتر, Filter

شواهد در کد:
- مدل Person در `app/models/person.py` وجود دارد (از ساختار پروژه)
- مسیرهای API مربوط به افراد در `app/routes/person.py` تعریف شده‌اند
- سرویس مربوطه در `app/services/person_service.py` قرار دارد
- صفحه پروفایل فرد در فرانت‌اند احتمالاً در `frontend/src/pages/` یا کامپوننت‌های مرتبط با Person است
- مدل تعاملات (Interaction) هنوز وجود ندارد و باید ساخته شود

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل Interaction در `app/models/person.py` با فیلدهای: id, person_id (FK به Person), interaction_type (enum: good/bad/neutral), description (TEXT), result (TEXT), user_comment (TEXT), created_at (TIMESTAMP), updated_at (TIMESTAMP)
2. ایجاد اسکیماهای Pydantic در `app/schemas/person_schema.py` برای Interaction: InteractionCreate, InteractionUpdate, InteractionResponse
3. افزودن مسیرهای API جدید در `app/routes/person.py`:
   - GET /api/persons/{person_id}/interactions (لیست تعاملات با قابلیت فیلتر بر اساس type)
   - POST /api/persons/{person_id}/interactions (ایجاد تعامل جدید)
   - GET /api/persons/{person_id}/interactions/{interaction_id} (جزئیات یک تعامل)
   - DELETE /api/persons/{person_id}/interactions/{interaction_id} (حذف تعامل)
4. پیاده‌سازی منطق سرویس در `app/services/person_service.py` برای CRUD تعاملات
5. ایجاد کامپوننت Timeline در فرانت‌اند (احتمالاً `frontend/src/components/Timeline.jsx`)
6. افزودن بخش Timeline به صفحه پروفایل فرد در فرانت‌اند
7. پیاده‌سازی فیلتر بر اساس نوع تعامل (خوب/بد) در فرانت‌اند و بک‌اند
8. ایجاد migration برای اضافه کردن جدول interactions به دیتابیس

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 8: پیاده‌سازی قابلیت 'یادآوری' برای تعاملات مهم (کارهای خوب و بد)

**Scope:** این مرحله شامل ایجاد یک سیستم یادآوری است که به کاربر اطلاع می‌دهد که یک فرد خاص کار خوب یا بدی برای او انجام داده است که نباید فراموش شود. این یادآوری‌ها می‌توانند به صورت نوتیفیکیشن یا در یک بخش خاص در صفحه پروفایل نمایش داده شوند. نکته حیاتی: این سیستم باید بر اساس اهمیت تعامل (امتیاز بالاتر) اولویت‌بندی شود.
**Key terms:** یادآوری, Reminder, نوتیفیکیشن, Notification, کار خوب, کار بد, اهمیت

**بخش مربوط از متن کاربر:**
```
که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم... باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن سیستم یادآوری تعاملات مهم افراد در پروفایل و نوتیفیکیشن

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/services/notification_service.py`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت 'یادآوری' برای تعاملات مهم (کارهای خوب و بد) را دارد. این سیستم باید به کاربر اطلاع دهد که یک فرد خاص کار خوب یا بدی برای او انجام داده است که نباید فراموش شود. یادآوری‌ها می‌توانند به صورت نوتیفیکیشن (Notification) یا در یک بخش خاص در صفحه پروفایل نمایش داده شوند. نکته حیاتی: این سیستم باید بر اساس اهمیت تعامل (امتیاز بالاتر) اولویت‌بندی شود. کاربر توضیح داده: 'که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم... باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم'. کلیدواژه‌های اصلی: یادآوری, Reminder, نوتیفیکیشن, Notification, کار خوب, کار بد, اهمیت. در کد فعلی، سیستم نوتیفیکیشن در فایل‌های `app/services/notification_service.py` (خطوط 96-135 برای send_notification، خطوط 494-545 برای notify_event) و `app/routes/notifications.py` (خطوط 65-75 برای list_notifications، خطوط 95-119 برای mark_as_read) و `frontend/src/pages/Notifications.jsx` (خطوط 56-71 برای fetchNotifications، خطوط 75-87 برای handleMarkRead) پیاده‌سازی شده است. مدل Person در `app/models/person.py` و سرویس person_service در `app/services/person_service.py` وجود دارد. برای پیاده‌سازی این قابلیت، باید یک مدل جدید برای ذخیره‌سازی تعاملات مهم (InteractionReminder) ایجاد شود، فیلد importance_score به آن اضافه شود، و نوتیفیکیشن‌های مرتبط با تعاملات با اولویت بالا از طریق NotificationService ارسال شوند. همچنین باید یک بخش جدید در صفحه پروفایل (احتمالاً در `frontend/src/pages/PersonDetail.jsx` یا یک کامپوننت جدید) برای نمایش یادآوری‌ها اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل جدید `InteractionReminder` در `app/models/person.py` با فیلدهای: id, user_id, person_id, action_type (good/bad), description, importance_score (integer), created_at, is_read, notification_sent. 2. ایجاد schema جدید `InteractionReminderCreate` و `InteractionReminderOut` در `app/schemas/person_schema.py`. 3. افزودن متدهای CRUD به `app/services/person_service.py`: create_reminder, get_user_reminders (با مرتب‌سازی بر اساس importance_score DESC), mark_reminder_read, delete_reminder. 4. افزودن endpointهای جدید در `app/routes/person.py`: POST /api/persons/{person_id}/reminders (ایجاد یادآوری), GET /api/persons/reminders (لیست یادآوری‌های کاربر با اولویت‌بندی), PATCH /api/persons/reminders/{reminder_id}/read (علامت‌گذاری به عنوان خوانده شده). 5. یکپارچه‌سازی با NotificationService: هنگام ایجاد یک یادآوری با importance_score >= 8 (یا آستانه قابل تنظیم)، یک نوتیفیکیشن از طریق `notify_event` یا `send_notification` در `app/services/notification_service.py` ارسال شود. 6. افزودن بخش نمایش یادآوری‌ها در frontend: ایجاد کامپوننت `InteractionReminders` در `frontend/src/components/InteractionReminders.jsx` و نمایش آن در صفحه پروفایل فرد (PersonDetail) یا یک تب جدید. 7. افزودن دکمه/فرم برای ثبت یادآوری جدید در صفحه جزئیات فرد. 8. به‌روزرسانی `app/main.py` برای اجرای migrationهای مربوط به مدل جدید در startup (ADD COLUMN IF NOT EXISTS).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 9: طراحی و پیاده‌سازی بخش 'پیشنهادات عملی' بر اساس نوع رابطه و تاریخچه

**Scope:** این مرحله شامل ایجاد یک بخش در صفحه پروفایل فرد است که بر اساس نوع رابطه و تاریخچه تعاملات، پیشنهادات عملی به کاربر می‌دهد (مثلاً 'این فرد به شما کمک کرده است، شاید بهتر باشد از او تشکر کنید' یا 'این فرد به شما بدی کرده است، مراقب باشید'). نکته حیاتی: این پیشنهادات باید هوشمندانه و بر اساس تحلیل داده‌ها باشند.
**Key terms:** پیشنهادات عملی, Actionable Suggestions, توصیه, Recommendation, جبران, انتقام

**بخش مربوط از متن کاربر:**
```
کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم... لزومی نداره خیلی کینه بگیرم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن بخش پیشنهادات عملی به صفحه پروفایل فرد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `app/models/person.py:1-50` — `class Person` — مدل Person فعلی — باید فیلد relationship_type و ارتباط با PersonInteraction جدید را اضافه کنیم
  ```python
  class Person(Base):
      __tablename__ = "persons"
      id = Column(Integer, primary_key=True, index=True)
      name = Column(String, nullable=False)
      relationship_type = Column(String, nullable=True)  # e.g. friend, family, colleague
      ...
  ```
- `app/routes/person.py:1-30` — `router` — روتر person — باید endpoint جدید GET /api/persons/{person_id}/suggestions اضافه شود
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from app.database import get_db
  from app.services.person_service import PersonService
  
  router = APIRouter(prefix="/persons", tags=["persons"])
  ```
- `app/services/person_service.py:1-30` — `class PersonService` — سرویس person_service — می‌توان از آن برای دریافت فرد و تعاملاتش استفاده کرد
  ```python
  class PersonService:
      def __init__(self, db: AsyncSession):
          self.db = db
  
      async def get_person(self, person_id: int) -> Person:
          ...
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `app/models/__init__.py` (سطر 1) — مدل Person در اینجا export می‌شود
- `app/database.py` (سطر 1) — برای مهاجرت دیتابیس و سشن‌ها
- `frontend/src/pages/PersonDetail.jsx` — صفحه پ

## 🔍 Context و وضعیت فعلی
طراحی و پیاده‌سازی بخش 'پیشنهادات عملی' بر اساس نوع رابطه و تاریخچه در صفحه پروفایل فرد. این بخش باید بر اساس نوع رابطه (مثلاً 'کمک‌کننده'، 'آسیب‌رسان') و تاریخچه تعاملات، پیشنهادات عملی به کاربر بدهد. مثال‌ها: 'این فرد به شما کمک کرده است، شاید بهتر باشد از او تشکر کنید' یا 'این فرد به شما بدی کرده است، مراقب باشید'. نکته حیاتی: این پیشنهادات باید هوشمندانه و بر اساس تحلیل داده‌ها باشند. کاربر خواسته: 'کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم... لزومی نداره خیلی کینه بگیرم'. کلیدواژه‌ها: پیشنهادات عملی, Actionable Suggestions, توصیه, Recommendation, جبران, انتقام. در کد فعلی، مدل Person در `app/models/person.py` و روتر person در `app/routes/person.py` وجود دارد. سرویس person_service در `app/services/person_service.py` قرار دارد. صفحه پروفایل فرد در فرانت‌اند احتمالاً در `frontend/src/pages/PersonDetail.jsx` یا مشابه آن است (فایل deep-read نشده).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک مدل جدید `PersonInteraction` در `app/models/person.py` برای ذخیره تاریخچه تعاملات (نوع تعامل: help/harm، توضیحات، تاریخ). 2. ایجاد یک سرویس جدید `app/services/suggestion_service.py` که با تحلیل PersonInteraction و نوع رابطه (فیلد relationship_type در Person) پیشنهادات عملی تولید کند. 3. افزودن endpoint جدید GET /api/persons/{person_id}/suggestions در `app/routes/person.py`. 4. ایجاد کامپوننت فرانت‌اند `SuggestionCard` و افزودن آن به صفحه پروفایل فرد. 5. مهاجرت دیتابیس برای جدول person_interactions.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 10: پیاده‌سازی قابلیت 'تحلیل لحن' (Tone Analysis) برای نظرات ثبت شده

**Scope:** این مرحله شامل پیاده‌سازی یک ماژول تحلیل متن (احتمالاً با استفاده از یک API یا کتابخانه NLP) است که لحن و نوع صحبت (مثبت، منفی، خنثی) را در نظرات ثبت شده توسط کاربر یا دیگران تحلیل می‌کند. نتیجه این تحلیل باید در امتیازدهی هوش مصنوعی و تنظیم نوع رابطه تأثیرگذار باشد. نکته حیاتی: این تحلیل باید به صورت خودکار و در زمان ثبت نظر انجام شود.
**Key terms:** تحلیل لحن, Tone Analysis, NLP, Sentiment Analysis, مثبت, منفی, خنثی

**بخش مربوط از متن کاربر:**
```
از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن... این شخص آنالیز میشه این رفتارش
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن ماژول تحلیل لحن (Tone Analysis) برای نظرات ثبت‌شده در سیستم افراد

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت 'تحلیل لحن' (Tone Analysis) برای نظرات ثبت‌شده در سیستم را دارد. این ماژول باید با استفاده از یک API یا کتابخانه NLP، لحن و نوع صحبت (مثبت، منفی، خنثی) را در نظرات ثبت‌شده توسط کاربر یا دیگران تحلیل کند. کاربر تأکید کرده است: 'نکته حیاتی: این تحلیل باید به صورت خودکار و در زمان ثبت نظر انجام شود.' همچنین کاربر اشاره کرده: 'از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن... این شخص آنالیز میشه این رفتارش' که نشان‌دهندهٔ نیاز به تأثیرگذاری نتیجهٔ تحلیل لحن بر امتیازدهی هوش مصنوعی و تنظیم نوع رابطه (Relationship Type) در سیستم افراد (Person) است. کلیدواژه‌های کاربر: 'تحلیل لحن', 'Tone Analysis', 'NLP', 'Sentiment Analysis', 'مثبت', 'منفی', 'خنثی'. در کد فعلی پروژه، مدل Person در `app/models/person.py` دارای فیلدهای `notes` (TEXT) و `relationship_type` (VARCHAR) است. روتر `app/routes/person.py` شامل endpointهای CRUD برای Person است. سرویس `app/services/person_service.py` عملیات تجاری Person را مدیریت می‌کند. همچنین سرویس NLP موجود در `app/services/ai/nlp_service.py` می‌تواند برای تحلیل متن استفاده شود. endpoint ثبت نظر جدید برای Person در `app/routes/person.py` در خطوط مربوط به POST /api/persons/{person_id}/notes یا PATCH /api/persons/{person_id} (برای به‌روزرسانی notes) قرار دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک تابع جدید در `app/services/ai/nlp_service.py` به نام `analyze_tone(text: str) -> dict` که با استفاده از یک کتابخانه NLP سبک (مانند TextBlob یا VADER از NLTK) یا API خارجی (مانند OpenAI) لحن متن را تحلیل کرده و یک دیکشنری شامل `sentiment` (positive/negative/neutral) و `score` (عدد بین -1 تا 1) برگرداند. 2. در صورت عدم دسترسی به API خارجی، از یک fallback مبتنی بر rule-based (لیست کلمات مثبت/منفی فارسی) استفاده شود. 3. در سرویس `app/services/person_service.py`، تابع `update_person` یا یک تابع جدید `add_note` را اصلاح کند تا پس از ذخیره‌سازی فیلد `notes`، تابع `analyze_tone` را فراخوانی کرده و بر اساس نتیجه، فیلد `relationship_type` را به‌روزرسانی کند (مثلاً اگر sentiment منفی باشد، relationship_type به 'strained' تغییر کند). 4. endpoint مربوطه در `app/routes/person.py` (PATCH /api/persons/{person_id}) باید این جریان خودکار را پشتیبانی کند. 5. یک فیلد جدید `last_tone_analysis` از نوع JSON به مدل Person اضافه شود تا نتیجهٔ آخرین تحلیل لحن در آن ذخیره شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. مدل `Person` با فیلدهای `id`, `user_id`, `name`, `email`, `phone`, `created_at`, `updated_at` در `app/models/person.py` تعریف شده باشد و `user_id` به `users.id` ارجاع دهد. _(verify: static)_
2. مدل `Interaction` با فیلدهای `id`, `person_id`, `type` (با Enum), `date`, `summary`, `notes`, `created_at`, `updated_at` در `app/models/interaction.py` تعریف شده باشد و `person_id` به `persons.id` ارجاع دهد. _(verify: static)_
3. مدل `AIAssessment` با فیلدهای `id`, `person_id`, `interaction_id`, `score`, `sentiment`, `analysis_text`, `created_at` در `app/models/ai_assessment.py` تعریف شده باشد. _(verify: static)_
4. مدل `UserComment` با فیلدهای `id`, `user_id`, `person_id`, `interaction_id`, `comment_text`, `created_at` در `app/models/user_comment.py` تعریف شده باشد. _(verify: static)_
5. مدل `BehaviorLog` با فیلدهای `id`, `person_id`, `behavior_type` (با Enum), `description`, `observed_date`, `user_id`, `created_at` در `app/models/behavior_log.py` تعریف شده باشد. _(verify: static)_
6. فایل `app/models/__init__.py` شامل importهای صحیح برای تمام مدل‌های جدید باشد. _(verify: static)_
7. یک Alembic migration جدید ایجاد شده باشد که جداول `persons`, `interactions`, `ai_assessments`, `user_comments`, `behavior_logs` را به پایگاه داده اضافه کند. _(verify: backend_test)_
8. اسکیماهای Pydantic اولیه برای مدل‌های `Person`, `Interaction`, `AIAssessment`, `UserComment`, `BehaviorLog` در `app/schemas/` تعریف شده باشند. _(verify: static)_
9. یک مدل SQLAlchemy به نام `Person` در `app/models/person.py` با فیلدهای `id`, `user_id`, `name`, `email`, `phone`, `notes` تعریف شده باشد. _(verify: static)_
10. رابطه `persons` به مدل `User` در `app/models/user.py` اضافه شده باشد. _(verify: static)_
11. اسکیماهای Pydantic (`PersonBase`, `PersonCreate`, `PersonUpdate`, `PersonResponse`) در `app/schemas/person_schema.py` تعریف شده باشند. _(verify: static)_
12. سرویس `PersonService` در `app/services/person_service.py` با توابع CRUD (مانند `create_person`, `get_person`, `update_person`, `delete_person`, `get_all_persons_for_user`) پیاده‌سازی شده باشد. _(verify: static)_
13. روتر `person` در `app/routes/person.py` با نقاط پایانی `POST /api/persons`, `GET /api/persons/{person_id}`, `PUT /api/persons/{person_id}`, `DELETE /api/persons/{person_id}` و `GET /api/persons` پیاده‌سازی شده باشد. _(verify: static)_
14. روتر `person` در `app/main.py` شامل شده باشد. _(verify: static)_
15. فراخوانی `POST /api/persons` با داده‌های معتبر و کاربر احراز هویت شده، یک پروفایل جدید ایجاد کرده و کد وضعیت 201 و داده‌های پروفایل ایجاد شده را برگرداند. _(verify: api_response)_
16. فراخوانی `GET /api/persons/{person_id}` برای یک پروفایل موجود و متعلق به کاربر احراز هویت شده، کد وضعیت 200 و داده‌های پروفایل را برگرداند. _(verify: api_response)_
17. فراخوانی `GET /api/persons` برای کاربر احراز هویت شده، کد وضعیت 200 و لیستی از پروفایل‌های متعلق به آن کاربر را برگرداند. _(verify: api_response)_
18. فراخوانی `PUT /api/persons/{person_id}` با داده‌های به‌روزرسانی شده و کاربر احراز هویت شده، پروفایل را به‌روزرسانی کرده و کد وضعیت 200 را برگرداند. _(verify: api_response)_
19. فراخوانی `DELETE /api/persons/{person_id}` برای یک پروفایل موجود و متعلق به کاربر احراز هویت شده، پروفایل را حذف کرده و کد وضعیت 204 را برگرداند. _(verify: api_response)_
20. فایل `app/models/user_activity.py` با مدل `UserActivity` شامل فیلدهای `id`, `user_id`, `activity_type`, `related_entity_type`, `related_entity_id`, `details`, و `timestamp` ایجاد شود. _(verify: static)_
21. مدل `User` در `app/models/user.py` باید به‌روزرسانی شود تا یک `relationship` به `UserActivity` داشته باشد. _(verify: static)_
22. فایل `app/schemas/user_activity_schema.py` با شمای‌های `UserActivityBase`, `UserActivityCreate`, و `UserActivityResponse` ایجاد شود. _(verify: static)_
23. فایل `app/services/user_activity_service.py` با توابع `create_activity` و `get_activities_for_user` پیاده‌سازی شود. _(verify: static)_
24. endpoint `POST /api/user-activities/` با `status_code=201` یک فعالیت جدید را با موفقیت ثبت کند و `UserActivityResponse` را برگرداند. _(verify: api_response)_
25. endpoint `GET /api/user-activities/` لیست فعالیت‌های کاربر احراز هویت شده را برگرداند. _(verify: api_response)_
26. ایجاد یک تسک جدید از طریق `POST /api/tasks` باید منجر به ثبت یک `UserActivity` با `activity_type='task_created'` شود. _(verify: backend_test)_
27. مدل `Feedback` در دیتابیس ایجاد شود: یک جدول `feedback` با ستون‌های `id`, `target_user_id`, `author_user_id`, `content`, `sentiment_score`, `speech_type`, `created_at` در دیتابیس وجود داشته باشد. _(verify: backend_test)_
28. endpoint `POST /api/feedback` با موفقیت نظر جدید ثبت کند: ارسال یک نظر معتبر به این endpoint (با احراز هویت) باید منجر به ذخیره آن در دیتابیس شود و پاسخ `201 Created` را برگرداند. _(verify: api_response)_
29. endpoint `GET /api/users/{user_id}/feedback` نظرات مربوط به کاربر را برگرداند: فراخوانی این endpoint با `user_id` معتبر باید لیستی از نظرات ثبت شده برای آن کاربر را برگرداند. _(verify: api_response)_
30. فیلدهای `sentiment_score` و `speech_type` در مدل `Feedback` قابل Null باشند: این فیلدها باید به گونه‌ای تعریف شوند که در صورت عدم ارائه مقدار، `None` را بپذیرند. _(verify: static)_
31. روتر `feedback` در `app/main.py` به درستی شامل شده باشد و در `_API_PREFIXES` اضافه شده باشد. _(verify: static)_
32. مدل `User` در `app/models/user.py` باید شامل فیلدهای `sentiment_score` و `behavior_analysis` باشد. _(verify: static)_
33. شمای `UserResponse` در `app/schemas/user_schema.py` باید فیلدهای `sentiment_score` و `behavior_analysis` را شامل شود. _(verify: static)_
34. در زمان startup، جدول `users` باید با فیلدهای `sentiment_score` (FLOAT) و `behavior_analysis` (TEXT) به‌روزرسانی شود (اگر وجود نداشته باشند). _(verify: backend_test)_
35. endpoint `POST /api/users/{user_id}/analyze_behavior` باید با موفقیت پاسخ 200 برگرداند و پروفایل کاربر را با نتایج تحلیل AI به‌روزرسانی کند (با فرض نقش ADMIN). _(verify: api_response)_
36. تابع `analyze_user_text_for_sentiment_and_behavior` در `app/services/ai/nlp_service.py` باید یک دیکشنری با `sentiment_score` و `behavior_analysis` برگرداند. _(verify: backend_test)_
37. فراخوانی endpoint تحلیل رفتار کاربر توسط یک کاربر غیر-ADMIN باید با خطای 403 مواجه شود. _(verify: api_response)_
38. Schema `BehaviorAnalysisInput` در `app/schemas/ai_schema.py` با فیلدهای `user_id` (int) و `text_to_analyze` (str) تعریف شده باشد. _(verify: static)_
39. Schema `BehaviorAnalysisResult` در `app/schemas/ai_schema.py` با فیلدهای `score` (int), `good_actions` (List[str]), `bad_actions` (List[str]) و `analysis_summary` (str) تعریف شده باشد. _(verify: static)_
40. فایل `app/services/ai/behavior_analysis_service.py` ایجاد شده و شامل تابعی به نام `analyze_user_behavior` باشد که `db`, `user_id`, `text` را به عنوان ورودی می‌گیرد و `BehaviorAnalysisResult` را برمی‌گرداند. _(verify: static)_
41. یک endpoint جدید `POST /api/ai/analyze-behavior` در `app/routes/ai.py` تعریف شده باشد که `BehaviorAnalysisInput` را دریافت کرده و `BehaviorAnalysisResult` را برگرداند. _(verify: api_response)_
42. Endpoint `POST /api/ai/analyze-behavior` قبل از اجرای منطق تحلیل، وضعیت `settings.FEATURE_AI_ENABLED` را بررسی کند و در صورت غیرفعال بودن، `HTTPException` با کد 403 برگرداند. _(verify: backend_test)_
43. منطق داخلی `analyze_user_behavior` شامل یک placeholder برای تحلیل NLP و امتیازدهی باشد که در آینده قابل توسعه است. _(verify: static)_
44. مدل `UserBehaviorAnalysisResult` در دیتابیس ایجاد شود و شامل فیلدهای `user_id`, `analysis_type`, `score`, `category`, `details` و `created_at` باشد. _(verify: static)_
45. شمای `UserBehaviorAnalysisResultCreate` و `UserBehaviorAnalysisResultResponse` با فیلدهای مناسب برای ورودی و خروجی API تعریف شوند. _(verify: static)_
46. endpoint `POST /api/ai/behavior_analysis` با موفقیت داده‌های تحلیل هوش مصنوعی را دریافت و در دیتابیس ذخیره کند و پاسخ 201 با داده‌های ذخیره‌شده را برگرداند. _(verify: api_response)_
47. نتایج ذخیره‌شده به کاربر احراز هویت شده مرتبط باشند و بتوان آن‌ها را از طریق مدل `User` بازیابی کرد. _(verify: backend_test)_
48. منطق مربوط به "نوع رابطه" (relationship type) در هیچ یک از فایل‌های جدید یا تغییریافته این تسک وجود نداشته باشد. _(verify: static)_
49. منطق `ALTER TABLE ADD COLUMN IF NOT EXISTS` برای جدول `user_behavior_analysis_results` در `app/main.py` در `startup_event` اضافه شود تا سازگاری با محیط‌های بدون Alembic حفظ شود. _(verify: static)_
50. مدل دیتابیس `Contact` با فیلدهای `id`, `user_id`, `name`, `relationship_type`, `ai_score` و `last_interaction_summary` در `app/models/contact.py` تعریف شده باشد. _(verify: static)_
51. مدل دیتابیس `Interaction` با فیلدهای `id`, `contact_id`, `user_id`, `type`, `description`, `timestamp` و `ai_analysis_score` در `app/models/interaction.py` تعریف شده باشد. _(verify: static)_
52. endpoint `POST /api/contacts` با موفقیت یک `Contact` جدید ایجاد کند و پاسخ 201 برگرداند. _(verify: api_response)_
53. endpoint `POST /api/contacts/{contact_id}/interactions` با موفقیت یک `Interaction` جدید برای یک `Contact` موجود ثبت کند و پاسخ 201 برگرداند. _(verify: api_response)_
54. متد `adjust_relationship_type` در `ContactService`، `relationship_type` و `ai_score` یک `Contact` را بر اساس تعاملات به‌روزرسانی کند. _(verify: backend_test)_
55. روتر `contacts.router` در `app/main.py` به برنامه FastAPI اضافه شده باشد. _(verify: static)_
56. فایل کامپوننت `PeopleProfile.jsx` در مسیر `frontend/src/pages/` ایجاد شده باشد و کامپوننت `PeopleProfile` را export کند. _(verify: static)_
57. مسیر `/profile/:id` در `frontend/src/App.jsx` تعریف شده باشد و کامپوننت `PeopleProfile` را رندر کند. _(verify: static)_
58. با ناوبری به `/profile/123` (یا هر ID دیگر)، صفحه پروفایل با عنوان 'صفحه پروفایل فردی' و بخش‌های 'اطلاعات پایه'، 'امتیازات'، 'رفتارها' و 'تاریخچه' (با متن placeholder) نمایش داده شود. _(verify: ui_interaction)_
59. کامپوننت `PeopleProfile.jsx` در این مرحله هیچ فراخوانی API (مانند `fetch` یا `axios`) برای دریافت داده‌های پویا نداشته باشد. _(verify: static)_
60. کامپوننت `PeopleProfile` از `Layout` (از `frontend/src/components/Layout.jsx`) برای ساختار کلی صفحه استفاده کند. _(verify: static)_
61. ستون‌های `ai_score` (Integer) و `user_comments` (Text) در جدول `users` در پایگاه داده وجود داشته باشند. _(verify: static)_
62. فراخوانی `GET /api/users/{user_id}` باید در پاسخ خود فیلدهای `ai_score` و `user_comments` را شامل شود. _(verify: api_response)_
63. یک صفحه پروفایل کاربر (مثلاً در مسیر `/users/:userId`) باید امتیاز هوش مصنوعی و نظرات کاربر را نمایش دهد. _(verify: ui_interaction)_
64. کاربر مجاز (خود کاربر یا ادمین) باید بتواند نظرات خود را از طریق UI در صفحه پروفایل ویرایش و ذخیره کند و تغییرات در بک‌اند پایدار بماند. _(verify: ui_interaction)_
65. endpoint `POST /api/people` باید با موفقیت یک شخص جدید برای ردیابی ایجاد کند و کد وضعیت 201 را برگرداند. _(verify: api_response)_
66. endpoint `POST /api/people/{person_id}/behavior` باید با موفقیت یک لاگ رفتار اضافه کند و کد وضعیت 201 را برگرداند. _(verify: api_response)_
67. endpoint `PATCH /api/people/{person_id}/relationship` باید با موفقیت نوع رابطه را به‌روزرسانی کند و کد وضعیت 200 را برگرداند. _(verify: api_response)_
68. یک لینک ناوبری جدید با متن 'People' یا مشابه آن در کامپوننت `Sidebar` قابل مشاهده باشد. _(verify: ui_interaction)_
69. با ناوبری به `/people/{person_id}`، صفحه جزئیات شخص باید نام شخص، نوع رابطه و لیستی از رفتارهای ثبت‌شده را نمایش دهد. _(verify: ui_interaction)_
70. کاربر بتواند یک لاگ رفتار جدید را از صفحه جزئیات شخص اضافه کند. _(verify: ui_interaction)_
71. مدل SQLAlchemy جدید `UserComment` با فیلدهای `comment_text`, `commenter_id`, `target_user_id`, `task_id` (اختیاری), `created_at`, `updated_at` در `app/models/user_comment.py` تعریف شده باشد. _(verify: static)_
72. شمای Pydantic `UserCommentCreate` و `UserCommentResponse` در `app/schemas/user_comment_schema.py` تعریف شده باشند. _(verify: static)_
73. یک endpoint `POST /api/user-comments/` در بک‌اند وجود داشته باشد که با `UserCommentCreate` ورودی گرفته و `UserCommentResponse` برگرداند و نیاز به احراز هویت داشته باشد. _(verify: api_response)_
74. یک صفحه جدید در فرانت‌اند (مثلاً `/record-comment`) شامل فرمی برای ثبت نظرات کاربران قابل دسترسی باشد. _(verify: ui_interaction)_
75. فرم ثبت نظر در فرانت‌اند امکان انتخاب کاربر هدف و وارد کردن متن نظر را فراهم کند و با موفقیت داده‌ها را به `POST /api/user-comments/` ارسال کند. _(verify: ui_interaction)_
76. رابط کاربری جدید هیچ بخش مربوط به نمایش تاریخچه یا تحلیل‌های قبلی نظرات را شامل نشود. _(verify: manual_only)_
77. مدل `ActivityLog` در `app/models/activity_log.py` تعریف شده و شامل فیلدهای `user_id`, `event_type`, `description`, `timestamp` و `metadata` (JSONB) باشد. _(verify: static)_
78. endpoint GET `/api/users/me/activity_log` در `app/routes/users.py` پیاده‌سازی شده و لاگ فعالیت‌های کاربر احراز هویت شده را برگرداند. _(verify: api_response)_
79. صفحه `ProfilePage.jsx` در `frontend/src/pages/` ایجاد شده و شامل کامپوننت `ActivityLogTimeline.jsx` باشد. _(verify: static)_
80. رویدادهای مهم (مانند ایجاد تسک، به‌روزرسانی پروژه، تکمیل آیتم خودسازی) در بک‌اند به درستی در `ActivityLog` ثبت شوند. _(verify: backend_test)_
81. یک لینک 'پروفایل' در `Header.jsx` یا `Sidebar.jsx` وجود داشته باشد که به مسیر `/profile` هدایت کند. _(verify: ui_interaction)_
82. صفحه پروفایل (مسیر `/profile`) برای کاربران احراز هویت نشده قابل دسترسی نباشد و به صفحه ورود هدایت شود. _(verify: ui_interaction)_
83. مدل `User` در `app/models/user.py` باید شامل ستون جدید `aggregated_task_summary` از نوع `Text` باشد. _(verify: static)_
84. شمای `UserResponse` در `app/schemas/user_schema.py` باید شامل فیلد `aggregated_task_summary: Optional[str] = None` باشد. _(verify: static)_
85. endpoint `GET /api/users/{user_id}` باید پاسخ `200 OK` حاوی فیلد `aggregated_task_summary` را برگرداند که با یک رشته خلاصه مرتبط (مثلاً «X وظیفه تکمیل شده، Y در انتظار») پر شده باشد. _(verify: api_response)_
86. کامپوننت `frontend/src/pages/Dashboard.jsx` باید فیلد `aggregated_task_summary` را از پاسخ API واکشی کرده و در یک بخش اختصاصی نمایش دهد. _(verify: ui_interaction)_
87. ایجاد وظایف جدید و تکمیل آن‌ها برای یک کاربر باید به‌طور دقیق در `aggregated_task_summary` نمایش داده شده در داشبورد آن کاربر پس از رفرش منعکس شود. _(verify: manual_only)_

## Task Steps

### Step 1: ایجاد صفحه پروفایل افراد (People Profile Page) در برنامه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک صفحه جدید در برنامه با عنوان 'پروفایل افراد' است. این صفحه باید لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رفتار، یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام صفحه باید دقیقاً 'پروفایل افراد' باشد و از داده‌های موجود در برنامه برای نمایش لیست افراد استفاده کند.
**Excerpt:**
```
یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
```

### Step 2: طراحی و پیاده‌سازی مدل داده برای ذخیره‌سازی اطلاعات افراد و تعاملات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات مربوط به هر فرد (نام، شناسه، و غیره) و همچنین تاریخچه تعاملات با آن فرد است. این مدل باید قابلیت ذخیره‌سازی رویدادها، نظرات، امتیازات و نوع رابطه را داشته باشد. نکته حیاتی: مدل باید به گونه‌ای طراحی شود که بتواند داده‌های ورودی از منابع مختلف (کاربر، هوش مصنوعی) را ذخیره کند.
**Excerpt:**
```
حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار... این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
```

### Step 3: پیاده‌سازی قابلیت ثبت دستی نظر توسط کاربر درباره یک فرد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک فرم یا بخش در صفحه پروفایل افراد است که به کاربر اجازه می‌دهد به صورت دستی یک نظر (مثبت یا منفی) درباره یک فرد ثبت کند. این نظر باید در مدل داده ذخیره شود. نکته حیاتی: کاربر باید بتواند نظر خود را بر اساس لحن و نوع صحبت فرد (که در متن ذکر شده) ثبت کند، اما تحلیل لحن در این مرحله انجام نمی‌شود.
**Excerpt:**
```
یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن... کارای بد و خوبش ثبت می‌شه
```

### Step 4: پیاده‌سازی قابلیت ثبت خودکار تعاملات (رویدادها) توسط سیستم
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد مکانیزمی برای ثبت خودکار تعاملات کاربر با افراد (مانند انجام یک کار مشترک، مکالمه، و غیره) در پروفایل آن فرد است. این تعاملات باید به صورت خودکار و بر اساس داده‌های موجود در برنامه (مثلاً تسک‌ها، پروژه‌ها) ثبت شوند. نکته حیاتی: این مرحله فقط ثبت رویداد است و شامل تحلیل یا امتیازدهی نمی‌شود.
**Excerpt:**
```
از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار... تمام جزئیات کارا ثبت بشه
```

### Step 5: پیاده‌سازی سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک ماژول هوش مصنوعی است که بر اساس داده‌های ثبت شده (تعاملات، نظرات کاربر، لحن و نوع صحبت) به هر فرد یک امتیاز (Score) اختصاص می‌دهد. این امتیاز باید به صورت پویا و با گذشت زمان به‌روزرسانی شود. نکته حیاتی: الگوریتم امتیازدهی باید بتواند بین کارهای خوب و بد تمایز قائل شود و تأثیر آنها را در طول زمان تعدیل کند.
**Excerpt:**
```
هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده... این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه... باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه
```

### Step 6: پیاده‌سازی سیستم تنظیم خودکار نوع رابطه (Relationship Type) بر اساس امتیاز و تعاملات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی منطقی است که بر اساس امتیاز هوش مصنوعی و تاریخچه تعاملات، نوع رابطه کاربر با هر فرد را به صورت خودکار تنظیم و به‌روزرسانی می‌کند (مثلاً 'دوست خوب'، 'نیاز به احتیاط'، 'باید جبران کنم'). نکته حیاتی: این سیستم باید پویا باشد و با تغییر امتیاز، نوع رابطه نیز تغییر کند.
**Excerpt:**
```
نوع رابطه من با اون تنظیم می‌شه... کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم
```

### Step 7: طراحی و پیاده‌سازی بخش 'تاریخچه تعاملات' در صفحه پروفایل فرد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک بخش در صفحه پروفایل هر فرد است که تمام تعاملات ثبت شده (خودکار و دستی) را به صورت یک لیست زمانی (Timeline) نمایش می‌دهد. هر آیتم باید شامل جزئیات کامل (تاریخ، نوع تعامل، نتیجه، نظر کاربر) باشد. نکته حیاتی: این بخش باید قابلیت فیلتر کردن بر اساس نوع تعامل (خوب/بد) را داشته باشد.
**Excerpt:**
```
تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه... همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم
```

### Step 8: پیاده‌سازی قابلیت 'یادآوری' برای تعاملات مهم (کارهای خوب و بد)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سیستم یادآوری است که به کاربر اطلاع می‌دهد که یک فرد خاص کار خوب یا بدی برای او انجام داده است که نباید فراموش شود. این یادآوری‌ها می‌توانند به صورت نوتیفیکیشن یا در یک بخش خاص در صفحه پروفایل نمایش داده شوند. نکته حیاتی: این سیستم باید بر اساس اهمیت تعامل (امتیاز بالاتر) اولویت‌بندی شود.
**Excerpt:**
```
که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم... باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم
```

### Step 9: طراحی و پیاده‌سازی بخش 'پیشنهادات عملی' بر اساس نوع رابطه و تاریخچه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک بخش در صفحه پروفایل فرد است که بر اساس نوع رابطه و تاریخچه تعاملات، پیشنهادات عملی به کاربر می‌دهد (مثلاً 'این فرد به شما کمک کرده است، شاید بهتر باشد از او تشکر کنید' یا 'این فرد به شما بدی کرده است، مراقب باشید'). نکته حیاتی: این پیشنهادات باید هوشمندانه و بر اساس تحلیل داده‌ها باشند.
**Excerpt:**
```
کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم... لزومی نداره خیلی کینه بگیرم
```

### Step 10: پیاده‌سازی قابلیت 'تحلیل لحن' (Tone Analysis) برای نظرات ثبت شده
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک ماژول تحلیل متن (احتمالاً با استفاده از یک API یا کتابخانه NLP) است که لحن و نوع صحبت (مثبت، منفی، خنثی) را در نظرات ثبت شده توسط کاربر یا دیگران تحلیل می‌کند. نتیجه این تحلیل باید در امتیازدهی هوش مصنوعی و تنظیم نوع رابطه تأثیرگذار باشد. نکته حیاتی: این تحلیل باید به صورت خودکار و در زمان ثبت نظر انجام شود.
**Excerpt:**
```
از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن... این شخص آنالیز میشه این رفتارش
```
