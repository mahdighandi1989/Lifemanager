---
task_id: 3cc09436-d889-4cee-b22b-9af05c82a7b2
title: توسعه سیستم مدیریت پروفایل و تحلیل رفتاری
type: other
priority: medium
execution_priority: 3300
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: 44aa6743-bf59-4b44-85ae-54f8af548cc3
project: mahdighandi1989/Lifemanager
created_at: '2026-05-27T21:23:41.404279+00:00'
updated_at: '2026-05-28T19:39:24.619530+00:00'
target_files:
- backend/app/models/__init__.py
- backend/app/services/ai_service.py
- backend/app/routers/contacts.py
- frontend/src/pages/TaskForm.tsx
- frontend/src/pages/PeopleProfilesPage.tsx
---

# توسعه سیستم مدیریت پروفایل و تحلیل رفتاری

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_793240_AgADfh4A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 6,961 char متن_

## هدف و چشم‌انداز پروژه  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## ویژگی‌ها و قابلیت‌های اصلی  _(at: audio/ogg)_

### ویژگی‌ها و قابلیت‌های اصلی

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن.
[00:09] اینجا مثلاً حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم حسب لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## مخاطب هدف و نیازها  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن. اینجا مثلاً عصب داده‌هایی که به ما به زمان برای کارای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که دربارمون دارن. این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه. که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً لزوماً نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## چالش‌ها و ریسک‌های اولیه  _(at: audio/ogg)_

### چالش‌ها و ریسک‌های اولیه

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مراحل بعدی و گام‌های اجرایی  _(at: audio/ogg)_

--- صفحه 1 ---
[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو موقع و زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مزیت رقابتی و تمایز  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اونجا مثلاً از روی داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا به مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## منابع و الزامات اولیه  _(at: audio/ogg)_

--- صفحه 1 ---
[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً عصب داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو موقع زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند. بخش‌های بعدی توسط AI ساختار داده شده‌اند و ممکن است ناقص باشند — این متن مرجع اصلی است.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_793240_AgADfh4A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 6,961 char متن_

## هدف و چشم‌انداز پروژه  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## ویژگی‌ها و قابلیت‌های اصلی  _(at: audio/ogg)_

### ویژگی‌ها و قابلیت‌های اصلی

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن.
[00:09] اینجا مثلاً حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم حسب لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## مخاطب هدف و نیازها  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستن. اینجا مثلاً عصب داده‌هایی که به ما به زمان برای کارای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار، هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که دربارمون دارن. این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه. که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم. یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً لزوماً نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق باشه و رابطه من نسبت به اون تنظیم بشه.

## چالش‌ها و ریسک‌های اولیه  _(at: audio/ogg)_

### چالش‌ها و ریسک‌های اولیه

[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مراحل بعدی و گام‌های اجرایی  _(at: audio/ogg)_

--- صفحه 1 ---
[00:01] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد، افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً از روی داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که یه کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا تو موقع و زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## مزیت رقابتی و تمایز  _(at: audio/ogg)_

[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اونجا مثلاً از روی داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که دربارمون دارن.
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم.
[01:04] باید همه چیزا به مرور زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.

## منابع و الزامات اولیه  _(at: audio/ogg)_

--- صفحه 1 ---
[00:00] یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
[00:09] اینجا مثلاً عصب داده‌هایی که به ما و زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار،
[00:20] هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده یا خودم مثلاً ممکنه یه نظری بدم عصب لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن،
[00:31] این شخص آنالیز می‌شه این رفتارش و تو پروفایلش اضافه می‌شه و کارهای بد و خوبش ثبت می‌شه و نوع رابطه من با اون تنظیم می‌شه.
[00:41] که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم.
[00:50] یعنی همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم یا یا که کار بدی بوده مثلاً
[01:01] لزومی نداره خیلی کینه بگیرم. باید همه چیزا تو موقع زمان مثلاً به اون پروفایلش اضافه بشه و براش تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه و رابطه من نسبت به اون تنظیم بشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد صفحه پروفایل افراد با آنالیز هوش مصنوعی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/models/__init__.py:1-50` — `__init__.py` — فایل اصلی import مدل‌ها. باید مدل جدید PersonProfile و PersonTask را به اینجا اضافه کرد.
  ```python
  # backend/app/models/__init__.py
  from .user import User
  from .task import Task
  from .project import Project
  from .notification import Notification
  from .contact import Contact
  ```
- `backend/app/services/ai_service.py:1-100` — `ai_service.py` — سرویس AI موجود. باید تابع analyze_person_behavior را به این کلاس اضافه کرد.
  ```python
  class AIService:
      def __init__(self, model: str = "gemini-2.5-flash"):
          self.model = model
          self.client = genai.Client()
  
      async def analyze_task(self, task_data: dict) -> dict:
          # existing analysis logic
          pass
  ```
- `backend/app/routers/contacts.py:1-80` — `contacts.py` — روتر موجود برای مخاطبان. می‌توان endpoints پروفایل افراد را به همین فایل اضافه کرد یا یک فایل جداگانه ایجاد کرد.
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from sqlalchemy.ext.asyncio import AsyncSession
  from backend.app.database import get_db
  from backend.app.models.contact import Contact
  from backend.app.schemas.contact import ContactCreate, ContactResponse
  
  router = APIRouter(prefix="/api/contacts", tags=["contacts"])
  
  @router.get("/", response_model=list[ContactResponse])
  async def get_contacts(db: AsyncSession = Depends(get_db)):
      # existing logic
      pass
  ```
- `frontend/src/pages/TaskForm.tsx:1-150` — `TaskForm` — فرم ایجاد/ویرایش Task. باید فیلد انتخاب افراد مرتبط (PersonProfile) به این فرم اضافه شود.
  ```tsx
  const TaskForm: React.FC = () => {
      const [formData, setFormData] = useState({
          title: '',
          description: '',
          project_id: '',
          priority: 'medium',
          due_date: '',
          assigned_to: ''
      });
      // ... form logic
  }
  ```
- `frontend/src/pages/PeopleProfilesPage.tsx:1-10` — `PeopleProfilesPage` — فایل جدید برای صفحه پروفایل افراد. باید بر اساس نیازهای کاربر پیاده‌سازی شود.
  ```tsx
  // فایل جدید - باید ایجاد شود
  import React from 'react';
  import { DataTable } from '../components/DataTable';
  import { Modal } from '../components/Modal';
  
  const PeopleProfilesPage: React.FC = () => {
      return (
          <div>
              <h1>پروفایل افراد</h1>
              <DataTable />
          </div>
      );
  };
  
  export default PeopleProfilesPage;
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست 
[auto-re-registered from github_import at 2026-05-20T04:25:49.854717+00:00]

## 🧱 پشتهٔ فناوری و معماری
FastAPI (Python) برای بک‌اند، React (TypeScript) برای فرانت‌اند، SQLAlchemy برای ORM، Alembic برای migration، Celery برای task queue (احتمالاً برای تحلیل AI به صورت async)، Gemini API برای AI analysis.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/models/user.py` (سطر 1) — مدل User که PersonProfile به آن FK دارد.
- `backend/app/models/task.py` (سطر 1) — مدل Task که از طریق PersonTask به PersonProfile مرتبط می‌شود.
- `backend/app/schemas/contact.py` (سطر 1) — الگوی Pydantic برای Contact که می‌تواند به عنوان الگو برای PersonProfile استفاده شود.
- `backend/app/database.py` (سطر 1) — فایل دیتابیس که برای ایجاد migration جدید نیاز است.
- `frontend/src/components/DataTable.tsx` (سطر 1) — کامپوننت DataTable که برای نمایش لیست پروفایل‌ها استفاده می‌شود.
- `frontend/src/components/Modal.tsx` (سطر 1) — کامپوننت Modal که برای فرم ایجاد/ویرایش پروفایل استفاده می‌شود.
- `frontend/src/routes.tsx` (سطر 1) — فایل مسیریابی که باید مسیر جدید /people-profiles به آن اضافه شود.

## 🌐 نقشهٔ وابستگی‌ها
این feature به شدت به مدل‌های User و Task وابسته است. مدل جدید PersonProfile باید با User رابطه FK و با Task رابطه Many-to-Many داشته باشد. سرویس AI موجود (ai_service.py) باید توسعه داده شود تا قابلیت تحلیل رفتار افراد را داشته باشد. روتر contacts.py یا یک روتر جدید باید endpoints جدید را مدیریت کند. در فرانت‌اند، کامپوننت‌های DataTable و Modal برای نمایش و مدیریت پروفایل‌ها استفاده می‌شوند. فایل routes.tsx باید برای اضافه کردن مسیر جدید به‌روزرسانی شود. همچنین نیاز به ایجاد migration جدید با Alembic است.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک صفحه جدید در برنامه با نام 'پروفایل افراد' (People Profile) را دارد. این صفحه باید برای افرادی که کاربر با آنها در ارتباط است طراحی شود. ویژگی اصلی این صفحه، آنالیز خودکار توسط هوش مصنوعی بر اساس داده‌هایی است که کاربر در طول زمان برای کارهای مختلف وارد می‌کند و آن شخص در آن کار دخیل بوده است. هوش مصنوعی باید به هر شخص یک امتیاز (Score) بدهد. همچنین کاربر باید بتواند نظر شخصی خود را بر اساس لحن و نوع صحبت‌هایی که درباره آن شخص دارد، ثبت کند. رفتارهای خوب و بد هر شخص باید در پروفایلش ثبت شود و نوع رابطه کاربر با آن شخص (مثلاً: خوبی کرده، باید کم بذارم، باید جبران کنم، انتقام بگیرم) به صورت خودکار تنظیم شود. هدف نهایی این است که همه چیز ثبت شود تا فراموشی پیش نیاید و کاربر با یک کار خوب، هزار کار بد را فراموش نکند و برعکس. تمام جزئیات کارها باید در طول زمان به پروفایل شخص اضافه شود و دقیق باشد. این درخواست بر اساس فایل صوتی پیوست شده (voice_793240_AgADfh4A.ogg) استخراج شده است. با توجه به deep context موجود در پروژه، ساختار فعلی شامل مدل‌های User، Task، Project، Notification و سرویس‌های مرتبط با AI مانند `ai_service.py` و `task_analysis_service.py` است. همچنین یک endpoint برای مدیریت مخاطبان (contacts) در فایل `backend/app/routers/contacts.py` وجود دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] مدل PersonProfile در فایل backend/app/models/person_profile.py ایجاد شده باشد و شامل فیلدهای id, user_id, person_name, ai_score, relationship_type, notes, created_at, updated_at باشد.
- [ ] مدل PersonTask برای ارتباط Many-to-Many بین PersonProfile و Task ایجاد شده باشد.
- [ ] تابع analyze_person_behavior در کلاس AIService در فایل backend/app/services/ai_service.py اضافه شده باشد.
- [ ] Endpoint POST /api/people-profiles با status 201 و body شامل person_name ایجاد شود.
- [ ] Endpoint GET /api/people-profiles لیست پروفایل‌ها را با status 200 برگرداند.
- [ ] Endpoint POST /api/people-profiles/{id}/analyze status 200 و body شامل ai_score و relationship_type برگرداند.
- [ ] صفحه /people-profiles در فرانت‌اند قابل دسترسی باشد و لیست پروفایل‌ها را نمایش دهد.
- [ ] در فرم ایجاد Task، یک فیلد برای انتخاب افراد مرتبط (PersonProfile) وجود داشته باشد.
- [ ] طراحی صفحه پروفایل افراد شیک و کاربرپسند باشد.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. **ایجاد مدل PersonProfile**: در فایل `backend/app/models/` یک مدل جدید به نام `PersonProfile` ایجاد کن. این مدل باید شامل فیلدهای زیر باشد: `id` (UUID), `user_id` (FK به User), `person_name` (string), `ai_score` (float, nullable), `relationship_type` (enum: good_deed, need_compensation, revenge, neutral), `notes` (Text, برای ثبت نظرات کاربر), `created_at`, `updated_at`. همچنین یک جدول واسط `PersonTask` برای ارتباط Many-to-Many بین PersonProfile و Task ایجاد کن. 2. **ایجاد سرویس AI Analysis**: در فایل `backend/app/services/ai_service.py` یک تابع جدید به نام `analyze_person_behavior(person_id: UUID, tasks: List[Task]) -> dict` اضافه کن. این تابع باید با استفاده از مدل AI موجود (مثلاً Gemini یا OpenAI) رفتارهای شخص را بر اساس تسک‌های مرتبط تحلیل کند و یک امتیاز و نوع رابطه پیشنهادی برگرداند. 3. **ایجاد API Endpoints**: در فایل `backend/app/routers/contacts.py` (یا یک فایل جدید `people_profiles.py`) endpoints زیر را اضافه کن: `POST /api/people-profiles` (ایجاد پروفایل جدید), `GET /api/people-profiles` (لیست پروفایل‌ها), `GET /api/people-profiles/{id}` (جزئیات پروفایل), `PUT /api/people-profiles/{id}` (به‌روزرسانی پروفایل), `POST /api/people-profiles/{id}/analyze` (اجرای آنالیز AI). 4. **ایجاد صفحه Frontend**: در پوشه `frontend/src/pages/` یک صفحه جدید به نام `PeopleProfilesPage.tsx` ایجاد کن. این صفحه باید شامل یک لیست از پروفایل‌ها، فرم ایجاد/ویرایش پروفایل، و یک بخش نمایش جزئیات با قابلیت نمایش امتیاز AI، نوع رابطه، و لیست کارهای مرتبط باشد. از کامپوننت‌های موجود مانند `DataTable`, `Modal`, `Form` استفاده کن. 5. **اتصال به Task**: در فرم ایجاد/ویرایش Task (فایل `frontend/src/pages/TaskForm.tsx`)، یک فیلد جدید برای انتخاب افراد مرتبط (PersonProfile) اضافه کن. این ارتباط باید در بک‌اند ذخیره شود. 6. **ثبت رفتارها**: یک سیستم لاگینگ برای ثبت رفتارهای خوب و بد در پروفایل افراد ایجاد کن. این می‌تواند از طریق یک فیلد JSON در مدل PersonProfile یا یک مدل جداگانه `BehaviorLog` انجام شود.

## 💡 نمونه‌های قبل/بعد
**اضافه کردن مدل PersonProfile به models/__init__.py**

_قبل:_
```
from .user import User
from .task import Task
from .project import Project
from .notification import Notification
from .contact import Contact
```

_بعد:_
```
from .user import User
from .task import Task
from .project import Project
from .notification import Notification
from .contact import Contact
from .person_profile import PersonProfile, PersonTask
```

**اضافه کردن تابع analyze_person_behavior به ai_service.py**

_قبل:_
```
class AIService:
    def __init__(self, model: str = "gemini-2.5-flash"):
        self.model = model
        self.client = genai.Client()

    async def analyze_task(self, task_data: dict) -> dict:
        # existing analysis logic
        pass
```

_بعد:_
```
class AIService:
    def __init__(self, model: str = "gemini-2.5-flash"):
        self.model = model
        self.client = genai.Client()

    async def analyze_task(self, task_data: dict) -> dict:
        # existing analysis logic
        pass

    async def analyze_person_behavior(self, person_id: UUID, tasks: list[Task]) -> dict:
        """
        Analyze a person's behavior based on associated tasks.
        Returns a dict with 'score' and 'relationship_type'.
        """
        prompt = f"Analyze the behavior of person {person_id} based on these tasks: {tasks}"
        response = await self.client.generate_content(prompt)
        # Parse response and return score and relationship type
        return {"score": 8.5, "relationship_type": "good_deed"}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_people_profiles.py -v`
- `cd frontend && npm run test -- --testPathPattern=PeopleProfilesPage`
- `alembic upgrade head`
- `cd frontend && npm run build`

## ⚠️ ریسک‌ها و موارد احتیاط
1. تغییر در فایل backend/app/services/ai_service.py ممکن است بر روی تحلیل تسک‌های موجود تأثیر بگذارد. 2. اضافه کردن مدل جدید نیاز به migration دارد که ممکن است با داده‌های موجود conflict ایجاد کند. 3. اضافه کردن فیلد انتخاب افراد در فرم Task ممکن است با فرم‌های موجود conflict داشته باشد. 4. وابستگی به API خارجی (Gemini) ممکن است باعث افزایش latency شود. 5. عدم وجود تست‌های کافی برای endpoints جدید می‌تواند باعث بروز باگ شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

## Acceptance Criteria

1. مدل PersonProfile در فایل backend/app/models/person_profile.py ایجاد شده باشد و شامل فیلدهای id, user_id, person_name, ai_score, relationship_type, notes, created_at, updated_at باشد. _(verify: static)_
2. مدل PersonTask برای ارتباط Many-to-Many بین PersonProfile و Task ایجاد شده باشد. _(verify: static)_
3. تابع analyze_person_behavior در کلاس AIService در فایل backend/app/services/ai_service.py اضافه شده باشد. _(verify: static)_
4. Endpoint POST /api/people-profiles با status 201 و body شامل person_name ایجاد شود. _(verify: api_response)_
5. Endpoint GET /api/people-profiles لیست پروفایل‌ها را با status 200 برگرداند. _(verify: api_response)_
6. Endpoint POST /api/people-profiles/{id}/analyze status 200 و body شامل ai_score و relationship_type برگرداند. _(verify: api_response)_
7. صفحه /people-profiles در فرانت‌اند قابل دسترسی باشد و لیست پروفایل‌ها را نمایش دهد. _(verify: ui_interaction)_
8. در فرم ایجاد Task، یک فیلد برای انتخاب افراد مرتبط (PersonProfile) وجود داشته باشد. _(verify: ui_interaction)_
9. طراحی صفحه پروفایل افراد شیک و کاربرپسند باشد. _(verify: manual_only)_

## Task Steps

### Step 1: ایجاد صفحه پروفایل افراد (People Profile Page) در برنامه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک صفحه جدید در برنامه با عنوان 'پروفایل افراد' است. این صفحه باید لیستی از افرادی که کاربر با آنها در ارتباط است را نمایش دهد. این مرحله فقط شامل ایجاد ساختار اولیه صفحه و مسیریابی (routing) است و شامل منطق امتیازدهی، ثبت رفتار، یا آنالیز هوش مصنوعی نمی‌شود. نکته حیاتی: نام صفحه باید دقیقاً 'پروفایل افراد' باشد و از داده‌های موجود در برنامه برای نمایش لیست افراد استفاده کند.
**Excerpt:**
```
یه جایی باید یه صفحه‌ای مثلاً تو برنامه باشه به اسم پروفایل افراد. افرادی که باهاشون در ارتباط هستم.
```

### Step 2: طراحی و پیاده‌سازی مدل داده برای ذخیره‌سازی اطلاعات افراد و تعاملات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک مدل داده (دیتابیس یا دیتاکلاس) برای ذخیره‌سازی اطلاعات مربوط به هر فرد (نام، شناسه، و غیره) و همچنین تاریخچه تعاملات با آن فرد است. این مدل باید قابلیت ذخیره‌سازی رویدادها، نظرات، امتیازات و نوع رابطه را داشته باشد. نکته حیاتی: مدل باید به گونه‌ای طراحی شود که بتواند داده‌های ورودی از منابع مختلف (کاربر، هوش مصنوعی) را ذخیره کند.
**Excerpt:**
```
حسب داده‌هایی که به مرور زمان برای کارهای مختلف وارد می‌کنن و اون شخص مثلاً دخیل بوده حالا تو اون کار... این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه و کارهای بد و خوبش ثبت میشه و نوع رابطه من با اون تنظیم میشه.
```

### Step 3: پیاده‌سازی قابلیت ثبت دستی نظر توسط کاربر درباره یک فرد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک فرم یا بخش در صفحه پروفایل افراد است که به کاربر اجازه می‌دهد به صورت دستی یک نظر (مثبت یا منفی) درباره یک فرد ثبت کند. این نظر باید در مدل داده ذخیره شود. نکته حیاتی: کاربر باید بتواند نظر خود را بر اساس لحن و نوع صحبت فرد (که در متن ذکر شده) ثبت کند، اما تحلیل لحن در این مرحله انجام نمی‌شود.
**Excerpt:**
```
یا خودم مثلاً ممکنه یه نظری بدم از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن... کارای بد و خوبش ثبت می‌شه
```

### Step 4: پیاده‌سازی قابلیت ثبت خودکار تعاملات (رویدادها) توسط سیستم
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد مکانیزمی برای ثبت خودکار تعاملات کاربر با افراد (مانند انجام یک کار مشترک، مکالمه، و غیره) در پروفایل آن فرد است. این تعاملات باید به صورت خودکار و بر اساس داده‌های موجود در برنامه (مثلاً تسک‌ها، پروژه‌ها) ثبت شوند. نکته حیاتی: این مرحله فقط ثبت رویداد است و شامل تحلیل یا امتیازدهی نمی‌شود.
**Excerpt:**
```
از روی داده‌هایی که به مرور زمان برای کارای مختلف وارد می‌کنم و اون شخص مثلاً دخیل بوده حالا تو اون کار... تمام جزئیات کارا ثبت بشه
```

### Step 5: پیاده‌سازی سیستم امتیازدهی هوش مصنوعی (AI Scoring) برای افراد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک ماژول هوش مصنوعی است که بر اساس داده‌های ثبت شده (تعاملات، نظرات کاربر، لحن و نوع صحبت) به هر فرد یک امتیاز (Score) اختصاص می‌دهد. این امتیاز باید به صورت پویا و با گذشت زمان به‌روزرسانی شود. نکته حیاتی: الگوریتم امتیازدهی باید بتواند بین کارهای خوب و بد تمایز قائل شود و تأثیر آنها را در طول زمان تعدیل کند.
**Excerpt:**
```
هوش مصنوعی آنالیز می‌کنه و یه امتیازی بهش می‌ده... این شخص آنالیز میشه این رفتارش و تو پروفایلش اضافه میشه... باید همه چیزا تو مرور زمان مثلاً به اون پروفایلش اضافه بشه
```

### Step 6: پیاده‌سازی سیستم تنظیم خودکار نوع رابطه (Relationship Type) بر اساس امتیاز و تعاملات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی منطقی است که بر اساس امتیاز هوش مصنوعی و تاریخچه تعاملات، نوع رابطه کاربر با هر فرد را به صورت خودکار تنظیم و به‌روزرسانی می‌کند (مثلاً 'دوست خوب'، 'نیاز به احتیاط'، 'باید جبران کنم'). نکته حیاتی: این سیستم باید پویا باشد و با تغییر امتیاز، نوع رابطه نیز تغییر کند.
**Excerpt:**
```
نوع رابطه من با اون تنظیم می‌شه... کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم
```

### Step 7: طراحی و پیاده‌سازی بخش 'تاریخچه تعاملات' در صفحه پروفایل فرد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک بخش در صفحه پروفایل هر فرد است که تمام تعاملات ثبت شده (خودکار و دستی) را به صورت یک لیست زمانی (Timeline) نمایش می‌دهد. هر آیتم باید شامل جزئیات کامل (تاریخ، نوع تعامل، نتیجه، نظر کاربر) باشد. نکته حیاتی: این بخش باید قابلیت فیلتر کردن بر اساس نوع تعامل (خوب/بد) را داشته باشد.
**Excerpt:**
```
تمام جزئیات کارا ثبت بشه و این چیزا دقیق بشه... همه چیز ثبت بشه که فراموشی نیاره و باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم
```

### Step 8: پیاده‌سازی قابلیت 'یادآوری' برای تعاملات مهم (کارهای خوب و بد)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سیستم یادآوری است که به کاربر اطلاع می‌دهد که یک فرد خاص کار خوب یا بدی برای او انجام داده است که نباید فراموش شود. این یادآوری‌ها می‌توانند به صورت نوتیفیکیشن یا در یک بخش خاص در صفحه پروفایل نمایش داده شوند. نکته حیاتی: این سیستم باید بر اساس اهمیت تعامل (امتیاز بالاتر) اولویت‌بندی شود.
**Excerpt:**
```
که مثلاً کجا بهم خوبی کرد که فراموش نکنم، کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم... باعث نشه که مثلاً با یه کار خوبش هزار تا کار بدی که کرده رو فراموش کنم
```

### Step 9: طراحی و پیاده‌سازی بخش 'پیشنهادات عملی' بر اساس نوع رابطه و تاریخچه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک بخش در صفحه پروفایل فرد است که بر اساس نوع رابطه و تاریخچه تعاملات، پیشنهادات عملی به کاربر می‌دهد (مثلاً 'این فرد به شما کمک کرده است، شاید بهتر باشد از او تشکر کنید' یا 'این فرد به شما بدی کرده است، مراقب باشید'). نکته حیاتی: این پیشنهادات باید هوشمندانه و بر اساس تحلیل داده‌ها باشند.
**Excerpt:**
```
کجا باید کمش بذارم و باید جبران انتقام کارم ازش بگیرم... لزومی نداره خیلی کینه بگیرم
```

### Step 10: پیاده‌سازی قابلیت 'تحلیل لحن' (Tone Analysis) برای نظرات ثبت شده
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک ماژول تحلیل متن (احتمالاً با استفاده از یک API یا کتابخانه NLP) است که لحن و نوع صحبت (مثبت، منفی، خنثی) را در نظرات ثبت شده توسط کاربر یا دیگران تحلیل می‌کند. نتیجه این تحلیل باید در امتیازدهی هوش مصنوعی و تنظیم نوع رابطه تأثیرگذار باشد. نکته حیاتی: این تحلیل باید به صورت خودکار و در زمان ثبت نظر انجام شود.
**Excerpt:**
```
از روی لحن من و نوعی که در نوع صحبتی که درباره‌مون دارن... این شخص آنالیز میشه این رفتارش
```
