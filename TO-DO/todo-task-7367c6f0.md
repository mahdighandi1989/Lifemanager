# TODO — Task 7367c6f0 (نیاز به تکمیل دستی)

> **مدیریت فایل و استخراج متن از Google Drive**

## 🔎 خلاصه وضعیت

- **task_id**: `7367c6f0-ba56-46c4-8436-0970a7075314`
- **repo**: `mahdighandi1989/Lifemanager`
- **verification_status**: `partial`
- **archived_reason**: `max_retries` — Claude به سقف retry رسید بدون اینکه verify=done شود
- **retries_done**: 1
- **verifier confidence**: 0.92
- **verifier model**: `—`
- **report_id**: `9f31cdc6-c0f4-4316-bf4d-94c78c9f3ed0`
- **created_at**: 2026-06-05T05:51:04.550595+00:00

## 🚧 چه چیزی باقی مانده (مهم‌ترین بخش)

- [ ] Endpoint GET /api/files/{id} برای دانلود فایل از Drive وجود ندارد (404)
- [ ] Endpoint POST /api/drive/upload-test پاسخ 405 می‌دهد (Method Not Allowed)
- [ ] Endpoint POST /api/sheets/add-row-test پاسخ 405 می‌دهد (Method Not Allowed)
- [ ] Endpoint GET /api/drive/root-folder وجود ندارد (404)
- [ ] اتصال واقعی به Google Drive API با google-api-python-client انجام نشده (placeholders)
- [ ] نشانگر (badge) و قابلیت دانلود در فرانت‌اند پیاده‌سازی نشده
- [ ] زیرپوشه‌های LifeManagerData در Drive ایجاد نشده‌اند

## 👉 قدم‌های بعدی پیشنهادی (از verifier)

1. اتصال google-api-python-client واقعی به google_drive_service.py
2. رفع 405 برای POST /api/drive/upload-test و /api/sheets/add-row-test
3. ایجاد endpoint GET /api/files/{id} برای دانلود فایل از Drive
4. ایجاد endpoint GET /api/drive/root-folder برای نمایش پوشه اصلی
5. پیاده‌سازی badge و دانلود در فرانت‌اند (DriveFiles.jsx)

## ✅ چه چیزی Claude انجام داد

- [x] سرویس Google Drive با توابع upload_file و build_share_link پیاده‌سازی شده
- [x] سرویس Google Sheets با تابع record_index_entry برای ثبت در LifeManagerIndex پیاده‌سازی شده
- [x] مدل DriveFile شامل فیلدهای storage_location, drive_file_id, drive_link, extracted_text است
- [x] Cold-tiering job با تابع tier_cold_files برای انتقال فایل‌های 30 روزه پیاده‌سازی شده
- [x] Endpoint GET /api/drive/files برای لیست کردن و جستجوی فایل‌ها وجود دارد
- [x] متن استخراج‌شده در فیلد extracted_text مدل DriveFile ذخیره می‌شود
- [x] پوشه اصلی LifeManagerData در google_drive_service.py تعریف شده
- [x] تست‌های واحد برای cold-tiering و ثبت در شیت نوشته شده
- [x] مستندات فنی API و FILE_PROCESSING به‌روز شده
- [x] ✓ پیاده‌سازی احراز هویت Google Drive و Google Sheets API در برنامه (code-aware: implemented)
- [x] ✓ طراحی و پیاده‌سازی مدل داده برای رهگیری فایل‌ها و داده‌های منتقل‌شده در Google Sheet (code-aware: implemented)
- [x] ✓ پیاده‌سازی منطق استخراج متن از فایل‌های صوتی و تصویری (OCR/ASR) (code-aware: implemented)
- [x] ✓ طراحی و پیاده‌سازی منطق تصمیم‌گیری برای انتقال داده‌ها (Data Tiering Logic) (code-aware: implemented)
- [x] ✓ طراحی و پیاده‌سازی رابط کاربری (UI) برای مدیریت و مشاهده داده‌های منتقل‌شده (code-aware: implemented)
- [x] ✓ پیاده‌سازی قابلیت جستجوی پیشرفته در داده‌های منتقل‌شده (شامل متن استخراج‌شده) (code-aware: implemented)
- [x] ✓ نوشتن تست‌های واحد (Unit Tests) برای منطق انتقال داده و API endpoints (code-aware: implemented)
- [x] ✓ بهینه‌سازی عملکرد: پیاده‌سازی کش (Caching) برای لینک‌های Google Drive (code-aware: implemented)
- [x] ✓ نوشتن مستندات فنی (Technical Documentation) و راهنمای کاربر (User Guide) (code-aware: implemented)

## 📝 خلاصهٔ verifier

بخش عمده backend (مدل‌ها، سرویس‌ها، منطق tiering، تست‌ها و مستندات) پیاده‌سازی شده، اما API endpoints deploy شده پاسخ 404/405 می‌دهند و اتصال واقعی به Google Drive API با google-api-python-client انجام نشده. فرانت‌اند نیز badge و دانلود را ندارد.

## 📋 Acceptance Criteria (مرجع کامل)

این لیست معیار done شدن تسک است — هر آیتمی که هنوز satisfy نیست
باید توسط انسان تکمیل شود.

- سرویس Google Drive (backend/app/services/drive_service.py) باید بتواند یک فایل را آپلود کرده و لینک قابل اشتراک‌گذاری (shareable link) برگرداند.
- سرویس Google Sheets (backend/app/services/sheets_service.py) باید بتواند یک ردیف جدید به شیت 'LifeManagerIndex' اضافه کند.
- مدل FileRecord در backend/app/models/file.py باید فیلدهای جدید storage_location, drive_file_id, drive_link, extracted_text را داشته باشد.
- Cold-tiering job در backend/app/services/cold_tiering_service.py باید به صورت خودکار فایل‌هایی که بیش از 30 روز از آخرین دسترسی آن‌ها گذشته است را به Drive منتقل کند.
- Endpoint GET /api/files/{id} باید بتواند فایل‌های ذخیره‌شده در Drive را دانلود کرده و به کاربر برگرداند.
- برای فایل‌های صوتی، متن استخراج‌شده باید در فیلد extracted_text مدل FileRecord ذخیره شود.
- یک پوشه اصلی با نام 'LifeManagerData' در Google Drive ایجاد شود و تمام فایل‌های منتقل‌شده در زیرپوشه‌های آن ذخیره شوند.
- در فرانت‌اند، فایل‌های ذخیره‌شده در Drive باید با یک نشانگر (badge) مشخص شوند و قابل دانلود باشند.

## 🔬 Evidence که verifier پیدا کرد

**Commits:**
- `ce49fb5`
- `ad1d380`
- `76a2d9e`

**Files lams شده:**
- `app/services/google_drive_service.py`
- `app/services/sheets_service.py`
- `app/services/cold_tiering_service.py`
- `app/models/drive_file.py`
- `app/routes/drive.py`
- `tests/test_drive_sheets_tiering_7367c6f0.py`

**Issues:**
- API endpoints deploy شده پاسخ 404/405 می‌دهند
- Google API client واقعی متصل نیست

## 💡 ایدهٔ اصلی تسک

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_103432_AgADrx0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 6,000 char متن_

## درخواست اصلی و هدف کاربر از فایل صوتی  _(at: audio/ogg)_

[00:00] می‌خوام جوری باشه برنامه پایگاه داده اصلیش به گوگل درایو باشه و از گوگل شیت هم استفاده بکنه تو همونجا یه فولدر خاص داشته باشه و تمام داده‌های اصل داده‌ها اونجا ذخیره بشه چون الان پایگاه داده‌م توی رندر دارم ولی اون یه گیگه. می‌خوام چیزایی که خیلی ضروریه اونجا باشه و بقیه موارد منتقل بشه به گوگل درایو و قابل فراخوانی و دیدن و اینا هم باشه البته. حالا اون پایگاه داده‌ای که توی رندر نمی‌دونم شاید مثلاً به خاطر اینکه صحبت دسترسی بیشتر باشه و اینها ولی برای چیزای ضروری اونجا نباید خیلی پر بشه که سرعت بیاد پایین. و تو همون پایگاه داده رندر اگه یه چیزی هم دیگه مثلاً به اون ضرورت قبلیش نباشه منتقل میشه توی گوگل درایو و براش فولدر درست ساخته میشه و توی شیت باید همه چیزا ثبت بشه. همه داده‌ها توی فولدر گوگل درایو هم منتقل بشه مثلاً صوت‌هایی که یه وقت می‌فرستم عکسا فلان چیزا اون موارد لازمش به صورت متنی یا چیز دیگه استخراج میشه و اون فایل‌های چیز اگر دیگه ضرورتی نداره و تو پایگاه داده اصلی توی رندر هم نمی‌تونه باشه باید منتقل بشه گوگل درایو و باید جایی هم تو خود برنامه بتونم فراخوانیش کنم و ببینمش و قابل دسترسی باشه خلاصه.

## شرح ایده کسب‌وکار مطرح شده  _

## 📜 پرامپت اصلی (excerpt)

```
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  با

_[truncated — full prompt در پنل]_
```

---

_این فایل توسط Claude Auto-Runner تولید شده است. تسک با حالت_ `max_retries` _آرشیو شده و دیگر به‌صورت خودکار pickup نمی‌شود._