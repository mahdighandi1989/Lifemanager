# TODO — Task e606cca6 (نیاز به تکمیل دستی)

> **پیاده‌سازی زمینه و بازخورد هوش مصنوعی وظایف**

## 🔎 خلاصه وضعیت

- **task_id**: `e606cca6-409a-4d32-80d7-e950ed124f16`
- **repo**: `mahdighandi1989/Lifemanager`
- **verification_status**: `partial`
- **archived_reason**: `max_retries` — Claude به سقف retry رسید بدون اینکه verify=done شود
- **retries_done**: 1
- **verifier confidence**: 0.95
- **verifier model**: `—`
- **report_id**: `18b20bd5-0243-4e3a-a284-b6d4ace1c7eb`
- **created_at**: 2026-06-05T05:43:22.845608+00:00

## 🚧 چه چیزی باقی مانده (مهم‌ترین بخش)

- [ ] WebSocket handler در ai_stream.py برای استریم real-time فیدبک AI پیاده‌سازی نشده
- [ ] محدودیت توکنی (token_limit) در AIConfig وجود دارد، شرط 'بدون محدودیت توکنی' نقض شده

## 👉 قدم‌های بعدی پیشنهادی (از verifier)

1. پیاده‌سازی WebSocket handler در app/routes/ai_stream.py برای استریم real-time فیدبک AI
2. بررسی و حذف یا افزایش token_limit در AIConfig برای رفع محدودیت توکنی

## ✅ چه چیزی Claude انجام داد

- [x] AIConfig schema با فیلدهای context_type, dynamic_response, token_limit پیاده‌سازی شده
- [x] AIService.get_task_context() context کامل تسک‌ها (total, completed, pending, overdue) را برمی‌گرداند
- [x] Settings.jsx بخش AI Context Settings با dropdown, toggle, slider را نمایش می‌دهد
- [x] POST /api/ai/analyze-tasks با task_id و user_id فیدبک هوشمند برمی‌گرداند
- [x] notification_service.send_ai_feedback() فیدبک AI را به عنوان notification ذخیره می‌کند
- [x] task_analysis.analyze_user_tasks() الگوهای کاری کاربر را شناسایی و گروه‌بندی می‌کند
- [x] سیستم AI context کامل تسک‌ها را می‌بیند (get_task_context پیاده‌سازی شده)

## 📝 خلاصهٔ verifier

6 از 8 معیار پذیرش (AC) به طور کامل پیاده‌سازی شده‌اند. AIConfig schema، get_task_context، UI تنظیمات، endpoint analyze-tasks، notification feedback و task analysis همگی در کد موجود هستند. دو AC باقی‌مانده: WebSocket real-time stream در ai_stream.py پیاده‌سازی نشده، و token_limit در AIConfig با شرط 'بدون محدودیت توکنی' در تضاد است.

## 📋 Acceptance Criteria (مرجع کامل)

این لیست معیار done شدن تسک است — هر آیتمی که هنوز satisfy نیست
باید توسط انسان تکمیل شود.

- AIConfig schema باید فیلدهای context_type, dynamic_response, token_limit را داشته باشد
- AIService.get_task_context() باید context کامل تسک‌های کاربر را برگرداند شامل total, completed, pending, overdue
- Settings.tsx باید بخش AI Context Settings با فیلدهای context_type (dropdown), dynamic_response (toggle), token_limit (slider) را نمایش دهد
- POST /api/ai/analyze-tasks باید با task_id و user_id فیدبک هوشمند برگرداند
- notification_service.send_ai_feedback() باید فیدبک AI را به عنوان notification ذخیره کند
- task_analysis.analyze_user_tasks() باید الگوهای کاری کاربر را شناسایی و گروه‌بندی کند
- WebSocket handler در ai_stream.py باید فیدبک AI را به صورت real-time stream کند
- سیستم AI باید بدون محدودیت توکنی کار کند و context کامل تسک‌ها را ببیند

## 🔬 Evidence که verifier پیدا کرد

**Commits:**
- `3b2a45d`
- `144f628`
- `a56415e`
- `fe7e072`

**Files lams شده:**
- `app/routes/ai.py`
- `app/services/ai_service.py`
- `app/services/finance_ai_service.py`
- `app/services/task_analysis.py`
- `app/services/notification_service.py`
- `frontend/src/pages/Settings.jsx`
- `migrations/versions/0021_ai_config_context_fields.py`

## 💡 ایدهٔ اصلی تسک

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_249747_AgADqR0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد • 5,292 char متن_

## مفهوم اصلی اپلیکیشن  _(at: audio/ogg)_

[00:00] مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم، باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدودیتی توکنی براشون نباشه و هزینه هم مسئله‌ای نباشه.
[00:30] ولی باید بدونن نسبت به اون وظایفی که هست و اضافه می‌شه، باید حسب کارهایی که مثلاً انجام دادم چه عکس‌العملی داشته باشن و حسب اون چیزهایی که من باید انجام بدن چه من باید انجام بدم چه واکنشی باید در پیام‌هایی که برام می‌فرستن و فیدبکی که می‌دن حالا چه توی اعلانات یا توی اونجایی که مثلاً ثبت می‌شه بهم بگن. یعنی خیلی باید تو هر آنالیزی که دارن و هر بررسی که دارن تو این مورد باید خیلی داینامیک و هوشمند عمل بکنن ولی در چارچوب اون پرامپتی که در اون باکس چیز براشون قرار می‌دم.

## ویژگی‌های اصلی و عملکرد  _(at: audio/ogg)_

[00:00] مدل‌هایی که در صفحه تنظیمات قرار می‌دم و براشون پرامپت می‌نویسم باید بدونن نسبت به تسک‌ها و وظایفی که قرار داده شده چه وظیفه‌ای دارند و البته خیلی هم داینامیک عمل بکنن، هاردکد نباشن ولی در چارچوب اون درخواست باشه و درخواست کامل ببینن و هیچ محدود

## 📜 پرامپت اصلی (excerpt)

```
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  با

_[truncated — full prompt در پنل]_
```

---

_این فایل توسط Claude Auto-Runner تولید شده است. تسک با حالت_ `max_retries` _آرشیو شده و دیگر به‌صورت خودکار pickup نمی‌شود._