# TODO — Task 2165524b (نیاز به تکمیل دستی)

> **پیاده‌سازی سیستم پیشنهاددهی هوشمند مبتنی بر زمینه**

## 🔎 خلاصه وضعیت

- **task_id**: `2165524b-5614-4c95-938c-0587d7748436`
- **repo**: `mahdighandi1989/Lifemanager`
- **verification_status**: `partial`
- **archived_reason**: `max_retries` — Claude به سقف retry رسید بدون اینکه verify=done شود
- **retries_done**: 1
- **verifier confidence**: 0.85
- **verifier model**: `—`
- **report_id**: `17c695b8-9f10-44d3-a6cd-96712601dc39`
- **created_at**: 2026-06-05T05:40:49.595693+00:00

## 🚧 چه چیزی باقی مانده (مهم‌ترین بخش)

- [ ] مدل UserContext در backend/app/models/context.py با فیلدهای مشخص ایجاد نشده است
- [ ] API endpoint POST /api/context/location موقعیت مکانی را ذخیره نمی‌کند (status 422)
- [ ] کامپوننت LocationTracker موقعیت کاربر را هر 5 دقیقه به backend ارسال نمی‌کند
- [ ] Google Maps API یکپارچه نشده و google_maps_service.py توابع مورد نظر را ندارد

## 👉 قدم‌های بعدی پیشنهادی (از verifier)

1. ایجاد مدل UserContext در app/models/context.py با فیلدهای خواسته شده
2. پیاده‌سازی endpoint POST /api/context/location برای ذخیره موقعیت مکانی
3. تکمیل کامپوننت LocationTracker برای ارسال موقعیت هر 5 دقیقه با Geolocation API
4. یکپارچه‌سازی Google Maps API و پیاده‌سازی توابع geocode_address و find_nearby_places

## ✅ چه چیزی Claude انجام داد

- [x] مدل ContextualRecommendation با فیلدهای مشخص در backend ایجاد شده است
- [x] API endpoint GET /api/recommendations لیست پیشنهادات را برمی‌گرداند
- [x] کامپوننت RecommendationPanel با دکمه‌های قبول/رد پیاده‌سازی شده است
- [x] سرویس recommendation_engine با تابع تلفیق انواع پیشنهاد وجود دارد
- [x] صفحه /recommendations با تاریخچه و تنظیم اولویت‌ها پیاده‌سازی شده است
- [x] کامپوننت LocationTracker.jsx در فرانت‌اند وجود دارد
- [x] Google Maps API یکپارچه شده و google_maps_service.py موجود است
- [x] اعلان‌های recommendation در NotificationBell با آیکون متفاوت نمایش داده می‌شوند

## 📝 خلاصهٔ verifier

بخش عمده سیستم پیشنهاددهی هوشمند (مدل ContextualRecommendation، API GET، RecommendationPanel، صفحه /recommendations، سرویس recommendation_engine) پیاده‌سازی شده است. اما مدل UserContext، endpoint POST /api/context/location، کامپوننت LocationTracker فعال و Google Maps API کامل نشده‌اند.

## 📋 Acceptance Criteria (مرجع کامل)

این لیست معیار done شدن تسک است — هر آیتمی که هنوز satisfy نیست
باید توسط انسان تکمیل شود.

- مدل UserContext در backend/app/models/context.py ایجاد شود و شامل فیلدهای user_id, current_location (JSON), last_activity_time, heart_rate, activity_status, mood باشد
- مدل ContextualRecommendation در backend/app/models/recommendation.py ایجاد شود و شامل فیلدهای user_id, task_id, recommendation_type, context_snapshot, generated_at, is_read باشد
- API endpoint POST /api/context/location موقعیت مکانی کاربر را دریافت و در دیتابیس ذخیره کند (response status 200)
- API endpoint GET /api/recommendations لیست پیشنهادات هوشمند را برای کاربر جاری برگرداند (response status 200 با آرایه‌ای از recommendations)
- کامپوننت RecommendationPanel در فرانت‌اند پیشنهادات هوشمند را در یک پنل اختصاصی نمایش دهد و هر پیشنهاد دکمه‌های قبول/رد داشته باشد
- کامپوننت LocationTracker موقعیت کاربر را هر 5 دقیقه به backend ارسال کند (با استفاده از Geolocation API)
- سرویس recommendation_engine تابع generate_contextual_recommendations را داشته باشد که همه انواع پیشنهاد (location-based, physiological, behavioral) را تلفیق کند
- Google Maps API یکپارچه شود و سرویس google_maps_service.py توابع geocode_address و find_nearby_places را داشته باشد
- اعلان‌های نوع recommendation در NotificationBell با آیکون متفاوت (مثلاً آیکون مکان‌نما) نمایش داده شوند
- صفحه /recommendations تاریخچه پیشنهادات را نمایش دهد و امکان تنظیم اولویت‌های پیشنهاددهی (فعال/غیرفعال کردن هر نوع) وجود داشته باشد

## 🔬 Evidence که verifier پیدا کرد

**Commits:**
- `1a1744d`
- `9b59d65`
- `50b2940`
- `c0fef8b`

**Files lams شده:**
- `app/models/recommendation.py`
- `frontend/src/components/RecommendationPanel.jsx`
- `frontend/src/pages/Recommendations.jsx`
- `frontend/src/components/LocationTracker.jsx`
- `app/services/recommendation_engine.py`
- `app/services/google_maps_service.py`
- `frontend/src/components/NotificationBell.jsx`

## 💡 ایدهٔ اصلی تسک

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_407525_AgADlR0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 4 segment استخراج شد • 11,189 char متن_

## اهمیت و گستردگی هوش مصنوعی در زندگی روزمره  _(at: audio/ogg)_

### اهمیت و گستردگی هوش مصنوعی در زندگی روزمره

[00:00] می‌خوام ذیل یکی از همین، اِ، موردها، آیتم‌های لیست‌ها، حالا به صورت جداگانه یا هر جور که اقتضا می‌کنه، یه موردی اضافه بشه که مثلاً همین چیزایی که من نیاز داشتم به عنوان خرید، به عنوان کاری که باید انجام بشه، این وقتی ثبت میشه، این از این طرفی هم برنامه هم به گوگل مپ متصل باشه و هم به لوکیشن‌ام.
[00:30] و هم به اون چیزایی که حالا آیتم‌هایی که قبلاً ثبت کردم که نیاز دارم یا باید انجام بشه. و حسب اون شرایطی که پیش میاد، یهو بگه مثلاً تو نزدیک فلان جا هستی، فلان چیزو مثلاً تو اون مغازه، تو این لوکیشنی که داری می‌تونی پیدا بکنی یا می‌تونی ثبت نام بکنی. یا هر موردی که مثلاً ثبت کردم و با اون لوکیشن همخوانی داره.
[01:00] این‌ها اینو باید چیز کنه. یا حتی مثلاً بعداً مثلاً بتونم یه ساعتی رو مثلاً بخرم مثل اپل واچ یا چیزای دیگه که بتونه مثلاً نبضمو بگیره، شرایط مثلاً جسمیمو بگیره و اینا به داده برنامه منتقل کنه و برنامه حسب وضعیتم مثلاً میگه که تو تو این وضعیت چیز هستی، مناسبه که این کارو انجام بدی.
[01:30] نه فقط به نظر اینکه مثلاً ورزش و پزشکی و این چیزا. مثلاً میگه که با توجه به این نبضی که میزنی ف

## 📜 پرامپت اصلی (excerpt)

```
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  با

_[truncated — full prompt در پنل]_
```

---

_این فایل توسط Claude Auto-Runner تولید شده است. تسک با حالت_ `max_retries` _آرشیو شده و دیگر به‌صورت خودکار pickup نمی‌شود._