# TODO — Task 217909d2 (نیاز به تکمیل دستی)

> **پیاده‌سازی پردازش فایل و تحلیل محتوا**

## 🔎 خلاصه وضعیت

- **task_id**: `217909d2-9771-4584-ac19-f7e841afa09d`
- **repo**: `mahdighandi1989/Lifemanager`
- **verification_status**: `partial`
- **archived_reason**: `max_retries` — Claude به سقف retry رسید بدون اینکه verify=done شود
- **retries_done**: 1
- **verifier confidence**: 0.92
- **verifier model**: `—`
- **report_id**: `af00e64f-8298-4074-b320-86d452ff29e3`
- **created_at**: 2026-06-05T06:18:23.613544+00:00

## 🚧 چه چیزی باقی مانده (مهم‌ترین بخش)

- [ ] اتصال واقعی به Google Drive API کامل نشده و با placeholder انجام شده
- [ ] اسکن دوره‌ای فایل‌های موبایل نیاز به تکمیل سمت موبایل دارد (بک‌اند Celery beat پیاده‌سازی شده)

## 👉 قدم‌های بعدی پیشنهادی (از verifier)

1. تکمیل اتصال واقعی Google Drive API (رفع placeholderها)
2. تکمیل سمت موبایل برای اسکن دوره‌ای فایل‌ها

## ✅ چه چیزی Claude انجام داد

- [x] اسکن خودکار فایل‌های محلی در زمان باز شدن برنامه پیاده‌سازی شده (auto scan-on-open در task 217909d2)
- [x] استخراج لیست فیلم‌ها از فولدر مشخص از طریق زیرساخت جستجوی فایل ممکن است
- [x] به‌روزرسانی خودکار لیست فایل‌ها با بازه زمانی قابل تنظیم از طریق Celery beat پیاده‌سازی شده
- [x] ارتباط هوشمند فایل‌ها با وظایف از طریق asset↔task correlation پیاده‌سازی شده
- [x] تشخیص خودکار هارد درایو خارجی از طریق detect_external_drives پیاده‌سازی شده
- [x] جستجوی هوشمند در فایل‌ها از طریق Free-text search پیاده‌سازی شده
- [x] عدم دانلود فایل‌ها و ذخیره فقط متادیتا رعایت شده
- [x] دسترسی به Google Drive به صورت جزئی پیاده‌سازی شده (OAuth و فهرست‌بندی)

## 📝 خلاصهٔ verifier

بیشتر معیارهای پذیرش (7 از 8) پیاده‌سازی شده‌اند. دسترسی به Google Drive به صورت جزئی انجام شده و نیاز به تکمیل اتصال واقعی API دارد. اسکن دوره‌ای موبایل نیز نیاز به تکمیل سمت کلاینت دارد.

## 📋 Acceptance Criteria (مرجع کامل)

این لیست معیار done شدن تسک است — هر آیتمی که هنوز satisfy نیست
باید توسط انسان تکمیل شود.

- اسکن خودکار فایل‌های محلی در زمان باز شدن برنامه: وقتی کاربر برنامه را باز می‌کند، سرویس FileScanner باید تمام فایل‌های موجود در مسیرهای پیش‌فرض (لپ‌تاپ) را اسکن کرده و متادیتای متنی آن‌ها را در دیتابیس ذخیره کند.
- استخراج لیست فیلم‌ها از فولدر مشخص: کاربر باید بتواند لیست تمام فیلم‌های موجود در یک فولدر خاص (مثلاً /Movies) را دریافت کند.
- به‌روزرسانی خودکار لیست فایل‌ها در موبایل: با بازه زمانی قابل تنظیم (مثلاً هر 30 دقیقه)، سیستم باید فایل‌های جدید را به لیست اضافه و فایل‌های حذف‌شده را پاک کند.
- ارتباط هوشمند فایل‌ها با وظایف: وقتی کاربر وظیفه‌ای با موضوع «فیلم» ایجاد می‌کند، سیستم باید فایل‌های ویدئویی مرتبط را پیشنهاد دهد.
- دسترسی به Google Drive: کاربر باید بتواند حساب Google Drive خود را متصل کرده و فایل‌های آنجا را اسکن کند.
- تشخیص خودکار هارد درایو خارجی: وقتی هارد درایو خارجی متصل می‌شود و برنامه باز است، سیستم باید آن را شناسایی کرده و شروع به اسکن کند.
- جستجوی هوشمند در فایل‌ها: کاربر باید بتواند با یک query متنی، فایل‌های مرتبط را جستجو کند (مثلاً «فیلم ایرانی»).
- عدم دانلود فایل‌ها: سیستم فقط متادیتا و لیست فایل‌ها را ذخیره کند، نه خود فایل‌ها را.

## 🔬 Evidence که verifier پیدا کرد

**Commits:**
- `8d15307`
- `983be3c`
- `ce49fb5`
- `efeaffd`
- `5f4bccf`

**Files lams شده:**
- `app/services/data_ingestion_service.py`
- `app/services/google_drive_service.py`
- `app/services/google_auth.py`
- `app/routes/files.py`
- `app/routes/local_files.py`
- `app/models/drive_file.py`
- `app/models/local_file_entry.py`
- `app/tasks.py`
- `app/celery_app.py`
- `TO-DO/task-217909d2-scan-triggers.md`

## 💡 ایدهٔ اصلی تسک

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_619599_AgADrR0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 7 segment استخراج شد • 11,330 char متن_

## اهمیت تشخیص زودهنگام بیماری‌ها  _(at: audio/ogg)_

[00:00] برنامه باید دسترسی‌های لازم به انواع به تمام فایل‌های توی لپ‌تاپم، موبایلم و جاهای دیگه داشته باشه. یعنی هر بار که من این برنامه رو باز می‌کنم، وقتی می‌بینم مثلاً روی لپ‌تاپم، یه بار تمام چیزا، فایل‌ها رو اسکن بکنه و یه خروجی ازشون بگیره. نه اینکه بیاد مثلاً فایل رو دانلود بکنه تو خودشون جا نده، ولی مثلاً به صورت متنی. اگه مثلاً من توی درایوم، توی فایلی مثلاً فیلم‌ها هستن، لیست تمام فیلم‌ها رو بگیره.
[00:30] و یا تمام جاهای دیگه تو فولدرها بره مثلاً لیست تمام چیزا رو بگیره. یا اگه تو موبایل هست بازش می‌کنم، هر از چندگاهی که براش تنظیم می‌کنم، این لیست‌های داده‌هایی که دارم رو به روز بکنه. اگر مثلاً اضافه شدن، اینو به لیستش اضافه بکنه. اگه حذف شدن، ازش پاک بکنه. و این رو در چیزها و تسک‌ها و وظایفی که مثلاً مرتبط هستن با این داده‌هایی که دارم، تأثیر بده. یعنی مثلاً تو آنالیزش مثلاً اگه من بهش گفتم که فلان چیزی دوست دارم ببینم، بعد بگه مثلاً تو فیلم‌ها فلان فیلم رو داری. یا فلان کتاب رو مثلاً دیدم که عکسشو داری که مثلاً این کتاب توی مثلاً فلان کتابخونه‌ته توی ایران یا فلان جا.
[01:13] و اینجوری ارجاع بده. یعنی خیلی هوشمند عمل بکنه. از روی چیزایی که دارم

## 📜 پرامپت اصلی (excerpt)

```
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  با

_[truncated — full prompt در پنل]_
```

---

_این فایل توسط Claude Auto-Runner تولید شده است. تسک با حالت_ `max_retries` _آرشیو شده و دیگر به‌صورت خودکار pickup نمی‌شود._