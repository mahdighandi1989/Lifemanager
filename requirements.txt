fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
# pydantic[email] pulls in pydantic+email-validator in one line so the
# duplicate `pydantic==2.10.4` + `pydantic[email]==2.10.4` pair that the
# audit (task 850097a9) flagged is collapsed to a single declaration.
pydantic[email]==2.10.4
pydantic-core==2.27.2
pydantic-settings==2.7.1
# email-validator is also re-pinned explicitly so pip never has to
# re-resolve the pydantic line just to pull the extras.
email-validator>=2.0.0
cryptography>=42.0.0
python-dotenv==1.0.1
celery==5.4.0
redis==5.2.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 hits an attribute error reading bcrypt 4.1+'s version.
# Pin below 4.1 until passlib ships a fix.
bcrypt<4.1
python-multipart==0.0.19
slowapi==0.1.9
alembic==1.14.0
httpx==0.28.1
aiofiles==24.1.0
# HTML sanitizer for the user-profile bio endpoint. bleach.clean is an
# allowlist-based stripper that handles `<script>` and event-handler
# attributes safely. The route layer falls back to html.escape if the
# import fails so the endpoint stays available even on a stripped image.
bleach==6.2.0
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
aiosqlite==0.20.0

# File processing / Drive integration (audit tasks 217909d2 / 7367c6f0).
# psutil powers external-drive detection (asset_scan_service falls back to a
# mount-dir walk without it); the google-api libs power real Drive listing/
# upload (the services degrade to injectable stubs offline). Pinned but the
# code lazy-imports them so a stripped image still boots.
psutil==6.1.0
google-api-python-client==2.155.0
google-auth-oauthlib==1.2.1
